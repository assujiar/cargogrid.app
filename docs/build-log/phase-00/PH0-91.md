# PH0-91 — Testing Foundation

**Prompt:** `CG-S5-PH0-012` (`CG-AABPP-PH0-091` v0.6.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/05-phase-00-discovery-foundation/91_TESTING_FOUNDATION_PROMPT.md`
**Status:** `COMPLETED` (pending calling-process review to mark `CG-S5-PH0-012` `VERIFIED` in `TASK_LEDGER.md`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app`, working dir `/home/user/cargogrid.app` |
| Branch | `claude/lanjut-btusq6` (this session's harness-assigned/designated branch — first checkpoint run on a branch name other than `agent/cargogrid-autonomous-build`; see §8 for the one test this surfaced) |
| HEAD at authoring time | `92d698f` (`origin/main`, PR #14 merge — "agent: complete Phase 0 design system foundation") |
| Worktree state | Clean at start (confirmed `git status`); this task's writes only (§4) |
| Upstream dependency | `CG-S5-PH0-082..090` (Prompts 82–90) all `VERIFIED` per `TASK_LEDGER.md`/`00_PHASE0_EXECUTION_INDEX.md` row `012` |
| Downstream | `CG-S5-PH0-013` (Prompt 92, Documentation Foundation); Phase 1's first domain factory (`tests/factories/<domain>.ts`) and first `components/ui/` component (accessibility/visual-regression gate) both build on this checkpoint's tooling |
| Mutation performed | New: two ADRs, `docs/standards/TESTING_STANDARDS.md`, `playwright.config.ts`, `e2e/smoke.spec.ts`, `tests/factories/seed.ts`(+test). Edited: `docs/adr/README.md`, `docs/standards/CODING_STANDARDS.md` §6, `package.json`/`pnpm-lock.yaml`, `.github/workflows/ci.yml` (new `e2e` job), `scripts/git/check-worktree-collision.test.ts` (one-line fix, §8). **No domain schema, `server/contracts/<domain>/`, or `components/ui/` code created** — Phase 1 scope, per the same boundary `ADR-0001`/`DESIGN_SYSTEM.md` established. |

## 1. Pre-flight collision check

`mcp__github__list_pull_requests` (owner `assujiar`, repo `cargogrid.app`, state `open`) → `[]`. `mcp__github__list_branches` → 7 branches, none with unmerged work claiming this task-ID range. `pnpm run git:check`: local `origin/main` ref was initially stale (`53e3d4a`, 61 commits behind); `git fetch origin main` corrected it to `92d698f`, matching `HEAD` exactly — confirmed via `git rev-parse HEAD origin/main`. After the fetch, `git:check` reports zero diverged branches and a clean worktree. No collision risk.

## 2. Scope decision — tooling/ADR/foundation only, no domain factories or real-UI tests (disclosed, not silently narrowed)

Prompt 91 §20 task 2 asks for "shared deterministic factories/fixtures for tenants/orgs/roles"; §28 wants "cross-tenant, migration rebuild, API/component/E2E/accessibility/performance harness smoke." Taken literally this implies domain-shaped fixtures (tenants, orgs, roles) and tests against real database/API/UI surfaces. None of those exist yet — no schema (`docs/discovery/04_DATABASE_MIGRATION_BASELINE.md`), no `server/contracts/<domain>/`, no `components/ui/` (`docs/standards/DESIGN_SYSTEM.md` §1). Creating domain-shaped `tests/factories/<domain>.ts` files or component/contract tests now, with no real shape to validate against, would be the identical premature-scaffold pattern `PH0-90.md` §2 already declined for `components/ui/`, generalized from components to fixtures. **Decision (this checkpoint, consistent with that precedent):** resolve both open ADR candidates with real evidence, implement the shared **foundation** every future domain factory/test layer builds on (deterministic-seed primitive, runner/CI wiring, naming/isolation/flake conventions, a real smoke suite proving the E2E/accessibility layer actually works), and explicitly enumerate every layer that is `NOT_RUN` with its owning future task (`docs/standards/TESTING_STANDARDS.md` §7) rather than fabricate a passing step against a non-existent subject (Prompt 91 §22's own Alternative-flow requirement).

## 3. ADR decisions (`ADR-0007`, `ADR-0008`)

1. **Runner/framework stack (`ADR-0007`):** `node:test`+`node:assert/strict` (unit/integration/component, ratifying the provisional pattern — 45→57 passing tests carried forward with zero framework-attributable defect) and `@playwright/test@1.61.1` (E2E/visual-regression/browser automation — verified current stable via `npm view` this checkpoint, not presumed). Factory location: `tests/factories/<domain>.ts`, exactly `10_TESTING_WORKSTREAM.md` §4.2's own recommendation.
2. **DR cadence + accessibility tool (`ADR-0008`):** quarterly DR-rehearsal cadence (policy decided now; first execution gated on Staging/backup infrastructure that does not yet exist — DevOps environment prompts' downstream item, not claimed complete here) and `@axe-core/playwright@4.12.1` (verified current stable) as the automated WCAG checker.
3. **Scope-discrepancy correction (disclosed in `ADR-0008`'s own "Scope discrepancy" section, not silently picked):** `docs/adr/README.md` §5.2's candidate register had reassigned the DR-cadence half of `ADR-CAND-ARCH-023` to "Phase 15 (`HDN-384`)" and silently dropped the accessibility-checker half — inconsistent with the candidate's own source (`10_TESTING_WORKSTREAM.md` §11, "resolve at Phase 0 testing foundation, Prompt 91") and with the current `HANDOFF.md`/`TASK_LEDGER.md` (both still describe the combined scope at Prompt 91). Treated as an unintended transcription narrowing from `PH0-084`, not a deliberate re-scoping (no `docs/architecture/**`/ADR/`CHANGE_MANIFEST.md` entry records a rationale for the reassignment) — resolved both halves here and corrected `docs/adr/README.md`'s row with an explicit note pointing to `ADR-0008` rather than overwriting history silently.

## 4. Files created/edited

| Path | Action | Purpose |
|---|---|---|
| `docs/adr/ADR-0007-test-runner-and-framework-stack.md` | ADD | Resolves `ADR-CAND-ARCH-022` |
| `docs/adr/ADR-0008-dr-rehearsal-cadence-and-accessibility-tooling.md` | ADD | Resolves `ADR-CAND-ARCH-023` (both halves) |
| `docs/standards/TESTING_STANDARDS.md` | ADD | Runner/naming/tag/environment/artifact, isolation/cleanup, flake/quarantine, factory-foundation, coverage-as-signal, baseline-vs-regression, `NOT_RUN`-layer conventions |
| `docs/adr/README.md` | EDIT | `ADR-CAND-ARCH-022/023` marked `ACCEPTED`; `ADR-0007`/`ADR-0008` added to index; §5.2 row corrected (§3 item 3) |
| `docs/standards/CODING_STANDARDS.md` | EDIT | §6 test conventions: provisional → ratified, cross-referenced to `TESTING_STANDARDS.md` |
| `package.json`/`pnpm-lock.yaml` | EDIT | `@playwright/test`, `@axe-core/playwright` devDependencies; `test` script glob widened to include `tests/**/*.test.ts`; new `test:coverage`, `test:e2e` scripts |
| `playwright.config.ts` | ADD | Single-Chromium-project E2E config (§6 explains why not a full cross-browser matrix yet) |
| `e2e/smoke.spec.ts` | ADD | Proves Playwright + `@axe-core/playwright` wiring against synthetic inline content only (§7) |
| `tests/factories/seed.ts` | ADD | Shared deterministic-seed primitive (mulberry32 PRNG, no domain shape — §2) |
| `tests/factories/seed.test.ts` | ADD | Determinism/range/error-path proof (11 tests) |
| `.github/workflows/ci.yml` | EDIT | New `e2e` job (parallel to `quality`) installing Playwright Chromium and running `pnpm run test:e2e`; header comment updated |
| `scripts/git/check-worktree-collision.test.ts` | EDIT | One-line fix, out of this task's literal file scope but disclosed — see §8 |
| `docs/runtime/KNOWN_ISSUES.md` | EDIT | New `ISS-2026-004` record (§8) |

## 5. Database / API / UI / security / performance / audit / data-migration effects

**Database (§13):** none. **API (§14):** none. **UI (§15):** none — no component exists to test against real markup yet; `e2e/smoke.spec.ts` uses synthetic inline HTML only. **Security (§16):** every fixture in `e2e/smoke.spec.ts` is inline/synthetic, zero external network call, zero tenant data; no secret is defined or required by the new `e2e` CI job (mirrors `ADR-0004`'s existing `quality`-job least-privilege pattern — `permissions: contents: read` inherited at workflow level, no per-job override). **Performance (§17):** `e2e` CI job runs in parallel with `quality`, not serialized after it (§3 of `TESTING_STANDARDS.md`); not measured as an SLA (no page corpus exists). **Audit (§18):** this build log + both ADRs are the record; `docs/runtime/KNOWN_ISSUES.md` `ISS-2026-004` records the one test-fragility defect found and fixed this checkpoint. **Data migration (§19):** none.

## 6. Tests / commands / results (§28/§30)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **PASS** (exit 0) |
| `pnpm run lint` | **PASS** (exit 0) — one pre-existing informational message ("Pages directory cannot be found"), unrelated, unchanged from `PH0-90.md` |
| `pnpm run test` (`node:test`) | **PASS** — 58/58 after this checkpoint's fix (§8); was 57/58 (1 known-cause failure, not a regression) before the fix, reproduced and explained before being corrected |
| `pnpm run test:coverage` | **PASS** — new script exercised this checkpoint; coverage reported as a signal only (`TESTING_STANDARDS.md` §6), no threshold gate |
| `pnpm run test:e2e` (Playwright + axe-core) | **PASS** — 3/3. First run correctly *failed* 1/3 (axe-core's `region` best-practice rule flagged the "accessible" fixture's content not being inside a landmark) — real evidence the checker was not vacuously passing; fixed by wrapping the fixture in `<main>`, re-ran green. The "inaccessible fixture" test correctly failed to launch before the matching Chromium revision (`v1228`, matching the pinned `@playwright/test@1.61.1`) was installed — confirms the config is version-coupled to real browser binaries, not a fabricated pass. |
| `pnpm run standards:check` | **PASS** — 0 suppression-governance violations |
| `pnpm run git:check` | **PASS** after `git fetch origin main` corrected a stale local `origin/main` ref (§1) |
| Real registry evidence (`npm view @playwright/test version dist-tags`, `npm view @axe-core/playwright version dist-tags`) | **PASS** — `1.61.1` and `4.12.1` respectively, both confirmed current stable |
| Secret scan across every new file | **PASS** — none found |

**Regression (§29):** `pnpm run test`'s existing 45(→57 with this checkpoint's own new tests, before the fix) tests all continued passing throughout; the one failure encountered (§8) is fully explained as a pre-existing test-fixture fragility unrelated to this checkpoint's own changes, not a regression this checkpoint introduced, and is now fixed with a behavior-preserving one-line change.

## 7. Documentation updated

`docs/adr/ADR-0007-*.md`, `docs/adr/ADR-0008-*.md`, `docs/adr/README.md`, `docs/standards/TESTING_STANDARDS.md`, `docs/standards/CODING_STANDARDS.md` §6, this build log, and the runtime ledgers listed in §11.

## 8. Errors / recovery / risks / issues

**`ISS-2026-004` (found and fixed this checkpoint, full record in `docs/runtime/KNOWN_ISSUES.md`):** running `pnpm run test` on this checkpoint's own designated branch (`claude/lanjut-btusq6`, the first Phase 0 checkpoint not run on `agent/cargogrid-autonomous-build`) surfaced that `scripts/git/check-worktree-collision.test.ts` (added `PH0-87`) hardcoded the literal branch name `agent/cargogrid-autonomous-build` in one assertion instead of reading the actual current branch — a session-identity assumption, not a defect in `checkWorktreeCollision()`'s own divergence logic (confirmed: the function correctly reported `claude/lanjut-btusq6` itself, and the suite's other 3 assertions passed unchanged). Left `checkWorktreeCollision.ts` itself untouched; fixed only the one test assertion to read `git rev-parse --abbrev-ref HEAD` instead of a literal string — a one-line, behavior-preserving, non-weakening correction. This is technically outside `PH0-091`'s literal allowed-file scope (§11/§12 — testing *foundation*, not `scripts/git/**` maintenance), but was fixed rather than left red because an unfixed failure here would leave the shared `quality` CI gate red for every future PR on any branch not literally named `agent/cargogrid-autonomous-build` — a live risk to the whole pipeline `PH0-091` itself is chartered to keep trustworthy (§20 task 4: "integrate safe gates into CI"), not a hypothetical one. Fully disclosed in `KNOWN_ISSUES.md`, not silently patched. No other error occurred.

## 9. Rollback/recovery note

Last trusted checkpoint: `92d698f` (`origin/main`, PR #14). Rollback: `git revert` this checkpoint's commit(s) removes both ADRs, `TESTING_STANDARDS.md`, `playwright.config.ts`, `e2e/**`, `tests/factories/**`, the `package.json`/lockfile/CI additions, and the one-line `check-worktree-collision.test.ts` fix, reverting `docs/adr/README.md`/`docs/standards/CODING_STANDARDS.md`/`docs/runtime/KNOWN_ISSUES.md` to their prior state — no data loss (documentation + tooling only, no schema/data exists to roll back). Forward-fix preferred.

## 10. Acceptance criteria / Definition of Done

- Configured test layers run reproducibly (`node:test` 58/58, Playwright/axe-core 3/3) or are explicitly `NOT_RUN` with an owner (`TESTING_STANDARDS.md` §7's table) — confirmed, no fabricated pass.
- No shared data leak, hidden skip/failure, or weakened baseline — confirmed (§6); the one pre-existing failure encountered was surfaced and fixed, not hidden (§8).
- Mandatory gates pass, worktree reconciled, no unauthorized scope changed — confirmed (§6); the one out-of-literal-scope file touched (`check-worktree-collision.test.ts`) is fully disclosed with rationale (§8), not silent.

**Task `CG-S5-PH0-012` is `COMPLETED`.**

## 11. Commit / branch / next eligible prompt

- Branch: `claude/lanjut-btusq6`.
- Ledgers updated in the same checkpoint: `docs/runtime/TASK_LEDGER.md` (`CG-S5-PH0-012` → completed, `CG-S5-PH0-013` → READY), `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`, `docs/build-log/phase-00/00_PHASE0_EXECUTION_INDEX.md` (row `012` done, `013` READY), `HANDOFF.md`, `docs/runtime/KNOWN_ISSUES.md` (`ISS-2026-004`).
- Next eligible prompt: `CG-S5-PH0-013` (Prompt 92, Documentation Foundation) — per the execution index, `PH0-092`'s upstream (`PH0-082..091`) is now fully satisfied.
