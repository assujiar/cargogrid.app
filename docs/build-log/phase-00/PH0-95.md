# PH0-95 — Data Classification Foundation

**Prompt:** `CG-S5-PH0-016` (`CG-AABPP-PH0-095` v0.6.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/05-phase-00-discovery-foundation/95_DATA_CLASSIFICATION_FOUNDATION_PROMPT.md`
**Status:** `COMPLETED` (pending calling-process review to mark `CG-S5-PH0-016` `VERIFIED` in `TASK_LEDGER.md`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app`, working dir `/home/user/cargogrid.app` |
| Branch | `claude/lanjut-btusq6` |
| HEAD at authoring time | `c5f2da4` ("agent: complete Phase 0 security baseline controls") |
| Worktree state | Clean at start (confirmed `git status`); this task's writes only (§4) |
| Upstream dependency | `CG-S5-PH0-081..095` (Prompts 81–94) all `VERIFIED` per `TASK_LEDGER.md`/`00_PHASE0_EXECUTION_INDEX.md` row `016` |
| Downstream | `CG-S5-PH0-017` (Prompt 96, Initial Threat Model); every Phase 1+ `server/contracts/<domain>/` field, `files.classification` value, and new environment variable is classified via this registry before it ships (§7 of the standards doc, mechanically enforced for env vars today) |
| Mutation performed | New: `docs/standards/DATA_CLASSIFICATION_STANDARDS.md`, `scripts/data-classification/registry.ts`(+test), `scripts/data-classification/check-registry.ts`(+test). Edited: `package.json`, `.github/workflows/ci.yml`. **No domain schema/API/UI field classified beyond this repository's one existing secret** — no `server/contracts/<domain>/` exists yet (Phase 1 scope, same discipline every prior Phase 0 foundation checkpoint applied). No ADR — confirmed via grep, no `ADR-CAND-ARCH-0NN` names this task (same as `PH0-92`). |

## 1. Pre-flight collision check

`mcp__github__list_pull_requests` (owner `assujiar`, repo `cargogrid.app`, state `open`) → `[]`. `git fetch origin main claude/lanjut-btusq6` then `git rev-parse HEAD origin/main` confirmed `HEAD` (`c5f2da4`) is exactly one commit ahead of `origin/main` (`92d698f`). No collision risk.

## 2. Scope decision — real registry for what exists today, every future field routed through one adoption gate (disclosed, not silently narrowed)

Prompt 95 §20 asks to "classify Phase 0 assets" and "define adoption/validation gates for later schema/API/UI prompts" — not to invent classifications for fields that do not exist. This repository's only classified-sensitive asset today is `scripts/env/schema.ts`'s one `secret`-classified variable (`SUPABASE_SERVICE_ROLE_KEY`). **Decision:** build the real taxonomy/handling-matrix/retention-mapping foundation (`docs/standards/DATA_CLASSIFICATION_STANDARDS.md`), a real, tested registry mechanism (`scripts/data-classification/registry.ts`), and a real, CI-enforced cross-check that the one existing sensitive asset is actually registered (`check-registry.ts`) — rather than fabricate `server/contracts/<domain>/` field classifications with no domain schema to classify. The adoption gate itself (§7 of the standards doc) is the concrete mechanism Phase 1's first domain contract extends, not re-invents.

## 3. Two-axis model — disclosed construction (not silently invented)

`docs/architecture/02_CANONICAL_DATA_FLOW_MAP.md` §10 (already `VERIFIED`, reproduced from Tech Arch §12.4) fixes 6 field groups with prose defaults but no named severity scale. Prompt 95 §16/§22 require a `public`/`internal`/`confidential`/`restricted`/`credential` scale and a "strongest applicable handling" resolution rule — neither is named in `02_*.md` §10's own text. This checkpoint constructs the scale from `02_*.md` §10's own prose ("Hidden unless permission" → `confidential`; "Restricted"/"HR/payroll only"/"Platform security only" → `restricted`; "Masked always; never exported plain" → `credential`, the ceiling) and states this construction explicitly in `docs/standards/DATA_CLASSIFICATION_STANDARDS.md` §1, rather than presenting it as a direct quotation of an already-ratified scale that does not exist in those words.

## 4. Files created/edited

| Path | Action | Purpose |
|---|---|---|
| `docs/standards/DATA_CLASSIFICATION_STANDARDS.md` | ADD | Two-axis taxonomy, handling matrix (8 dimensions), retention mapping (RPD-025), registry mechanism, adoption gate, `NOT_RUN` table |
| `scripts/data-classification/registry.ts` | ADD | `SensitivityLevel`/`Category` types, `strongest()` resolver, `HANDLING_MATRIX`, `CATEGORY_RETENTION_CLASS`, `validateRegistry()`, `PHASE_0_REGISTRY` |
| `scripts/data-classification/registry.test.ts` | ADD | 25 tests |
| `scripts/data-classification/check-registry.ts` | ADD | Cross-checks every `secret`-classified `scripts/env/schema.ts` variable has a registry entry |
| `scripts/data-classification/check-registry.test.ts` | ADD | 5 tests, including a real regression guard against this repository's actual env schema |
| `package.json` | EDIT | `data-classification:check` script |
| `.github/workflows/ci.yml` | EDIT | New "Data-classification registry check" step in `quality` job |

## 5. Database / API / UI / security / performance / audit / data-migration effects

**Database (§13):** no schema change; `files.classification` column values are named in the standards doc §8 as a future consumer, not implemented (no `files` table exists). **API (§14):** none — request/response/log/export classification metadata expectations are fixed in the standards doc, `NOT_RUN` (no API exists). **UI (§15):** none. **Security (§16):** this checkpoint's real deliverable — the one real `credential`-level asset in this repository (`SUPABASE_SERVICE_ROLE_KEY`) is now formally registered and mechanically cross-checked, closing the loop between `ADR-0010`'s mechanism decision, `scripts/observability/logger.ts`'s redaction pattern, and `scripts/security/check-secrets.ts`'s scanning — all three now trace to one named classification. **Performance (§17):** `data-classification:check` runs in well under a second (a handful of registry entries); `HANDLING_MATRIX`/`CATEGORY_DEFAULT_LEVEL` are static `Record` lookups, not per-row computation — no N+1 risk by construction. **Audit (§18):** every `ClassificationEntry` carries a mandatory `owner` (`validateRegistry()` rejects a missing one) — the audit trail this checkpoint's own registry entry addition *is* the record. **Data migration (§19):** none — no business data transformed.

## 6. Tests / commands / results (§28/§30)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **PASS** (exit 0) |
| `pnpm run lint` | **PASS** (exit 0) — same pre-existing informational message as every prior checkpoint |
| `pnpm run test` (`node:test`) | **PASS** — 147/147 (117 carried from `PH0-94` + 30 new: 25 in `registry.test.ts`, 5 in `check-registry.test.ts`) |
| `pnpm run docs:check` | **PASS** — 0 issues |
| `pnpm run security:check` | **PASS** — 0 secret-shaped findings |
| `pnpm run data-classification:check` (new) | **PASS** — registry internally valid, 0 unclassified secret env vars |
| `pnpm run test:e2e` | **PASS** — 3/3, unchanged from `PH0-91` |
| `pnpm run standards:check` | **PASS** — 0 suppression-governance violations |

**Regression (§29):** all 117 tests carried from `PH0-94` continue passing unchanged; zero existing test weakened or disabled. `strongest()`'s empty-input rejection and `validateRegistry()`'s three violation kinds are each proven with a real failing-then-passing fixture pair, not asserted only in prose.

## 7. Documentation updated

`docs/standards/DATA_CLASSIFICATION_STANDARDS.md`, this build log, and the runtime ledgers listed in §11.

## 8. Errors / recovery / risks / issues

No error. One disclosed, deliberately-unresolved item, not hidden: payroll's retention class is inferred (Finance/tax, 10 years) rather than itemized in RPD-025's own "Applies to" column — flagged in the standards doc §5 with a named resolution point (Phase 4 Finance / Phase 7 HRIS SME evidence), the same pattern `PH0-90`'s brand-color disclosure and `PH0-91`'s DR-cadence-execution disclosure already established for "decide the policy now, confirm the specific unresolved detail later."

## 9. Rollback/recovery note

Last trusted checkpoint: `c5f2da4`. Rollback: `git revert` this checkpoint's commit(s) removes the standards doc, registry + check script + tests, and the `package.json`/`ci.yml` edits, with no data loss (documentation + zero-dependency utility code only, no schema/data exists to roll back). Forward-fix preferred.

## 10. Acceptance criteria / Definition of Done

- Classification taxonomy/registry/handling rules are complete and enforceable for later prompts — confirmed: two-axis model, 8-dimension handling matrix, retention mapping, and a real, tested, CI-wired validation/adoption gate all exist today.
- No sensitive content is exposed or silently left unclassified — confirmed: `check-registry.ts` proves (not merely documents) that this repository's one real secret-classified variable is registered; a future unregistered secret variable fails CI (`registry.test.ts`'s adoption-gap tests prove the detection logic).
- Mandatory gates pass, worktree reconciled, no unauthorized scope changed — confirmed (§6).

**Task `CG-S5-PH0-016` is `COMPLETED`.**

## 11. Commit / branch / next eligible prompt

- Branch: `claude/lanjut-btusq6`.
- Ledgers updated in the same checkpoint: `docs/runtime/TASK_LEDGER.md` (`CG-S5-PH0-016` → completed, `CG-S5-PH0-017` → READY), `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`, `docs/build-log/phase-00/00_PHASE0_EXECUTION_INDEX.md` (row `016` done, `017` READY), `HANDOFF.md`.
- Next eligible prompt: `CG-S5-PH0-017` (Prompt 96, Initial Threat Model) — per the execution index, `PH0-096`'s upstream (`PH0-083..095`) is now fully satisfied.
