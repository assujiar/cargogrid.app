# PH0-94 — Security Baseline Controls

**Prompt:** `CG-S5-PH0-015` (`CG-AABPP-PH0-094` v0.6.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/05-phase-00-discovery-foundation/94_SECURITY_BASELINE_CONTROLS_PROMPT.md`
**Status:** `COMPLETED` (pending calling-process review to mark `CG-S5-PH0-015` `VERIFIED` in `TASK_LEDGER.md`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app`, working dir `/home/user/cargogrid.app` |
| Branch | `claude/lanjut-btusq6` |
| HEAD at authoring time | `c70ccec` ("agent: complete Phase 0 observability baseline") |
| Worktree state | Clean at start (confirmed `git status`); this task's writes only (§4) |
| Upstream dependency | `CG-S5-PH0-082..094` (Prompts 82–93) all `VERIFIED` per `TASK_LEDGER.md`/`00_PHASE0_EXECUTION_INDEX.md` row `015` |
| Downstream | `CG-S5-PH0-016` (Prompt 95, Data Classification Foundation); every future secret-bearing file is scanned by `scripts/security/check-secrets.ts` before merge; Phase 1's first auth/upload/route slice implements the `NOT_RUN` contracts fixed in `docs/standards/SECURITY_STANDARDS.md` §4 |
| Mutation performed | New: `docs/adr/ADR-0010-secret-manager-mechanism.md`, `docs/standards/SECURITY_STANDARDS.md`, `scripts/security/check-secrets.ts`(+test), `docs/runbooks/secret-leak-incident-response.md`. Edited: `docs/adr/README.md`, `docs/standards/OBSERVABILITY_STANDARDS.md` (one citation-accuracy fix), `package.json`, `.github/workflows/ci.yml`, `docs/runtime/KNOWN_ISSUES.md` (`ISS-2026-007`). **No auth/session/upload/header/CORS code created** — no `app/`/`server/` route exists yet to attach any of those controls to (Phase 1 scope, same discipline every prior Phase 0 foundation checkpoint applied). |

## 1. Pre-flight collision check

`mcp__github__list_pull_requests` (owner `assujiar`, repo `cargogrid.app`, state `open`) → `[]`. `git fetch origin main claude/lanjut-btusq6` then `git rev-parse HEAD origin/main` confirmed `HEAD` (`c70ccec`) is exactly one commit ahead of `origin/main` (`92d698f`). No collision risk.

## 2. Scope decision — controls buildable today only, every route/auth/upload control fixed as contract (disclosed, not silently narrowed)

`docs/discovery/06_SECURITY_BASELINE.md` (`SEC-2026-001`, the Step 2 audit) found every control family `NOT_IMPLEMENTED` because no application code exists — "a coverage gap by construction, not a defect ... until Phase 0/Platform Core ships without these controls," and named "Prompts 94–96" as where the baseline lands. Prompt 94 §16's "primary scope" lists secret scanning, secure defaults, dependencies, headers, validation, session/token, upload primitives, and incident/key-rotation readiness together — but headers/session/upload/validation all require a real HTTP surface that does not exist (`docs/discovery/05_ROUTE_MODULE_INVENTORY.md`, empty). **Decision (consistent with `PH0-90`/`PH0-91`/`PH0-92`/`PH0-93`):** implement the two controls with a real, buildable-today subject — secret scanning (a real scan of this repository's actual tracked files) and the secret-storage-mechanism ADR — write the threat/control mapping and every other control's contract now so Phase 1 has zero re-litigation, and name every `NOT_RUN` control explicitly with its owner (`docs/standards/SECURITY_STANDARDS.md` §1/§4/§6) rather than fabricate a header/session/upload check with nothing real to verify.

## 3. Decisions

1. **`ADR-0010` (secret-manager mechanism, resolves `ADR-CAND-ARCH-025`):** Vercel Environment Variables + Supabase project secrets — the platform-native mechanism `11_DEVOPS_WORKSTREAM.md` §10 already recommended, ratified with evidence that `scripts/env/schema.ts`/`ADR-0003` already assume exactly this mechanism (no new pattern introduced).
2. **Dependency/supply-chain audit — real finding, not resolved:** `pnpm audit` (pnpm `10.33.0`) fails against the real, current lockfile with a genuine upstream `410 Gone` (npm's classic advisory endpoints retired). Neither a hard-fail nor a silently-passing `--ignore-registry-errors` gate was wired — both would violate this repository's established "never fabricate a passing gate" rule (`PH0-88.md` §3). Recorded as `ISS-2026-007` (`OPEN`, Medium), with `pnpm install --frozen-lockfile` (already real, already in CI) correctly distinguished as a different, still-working control (install determinism ≠ vulnerability-database checking).

## 4. Files created/edited

| Path | Action | Purpose |
|---|---|---|
| `docs/adr/ADR-0010-secret-manager-mechanism.md` | ADD | Resolves `ADR-CAND-ARCH-025` |
| `docs/standards/SECURITY_STANDARDS.md` | ADD | Threat/control mapping, secret handling/rotation, headers/CORS/CSRF contract (`NOT_RUN`), dependency-audit finding, residual risks |
| `scripts/security/check-secrets.ts` | ADD | Real secret scanner: 5 pattern kinds, redacted output, self-referential exclusion |
| `scripts/security/check-secrets.test.ts` | ADD | 17 tests, including a real zero-findings run against this repository's actual tracked files |
| `docs/runbooks/secret-leak-incident-response.md` | ADD | Second real `docs/runbooks/` instance, `NOT_YET_REHEARSED` against a live leak (disclosed) |
| `docs/adr/README.md` | EDIT | `ADR-CAND-ARCH-025` marked `ACCEPTED`; `ADR-0010` added to index |
| `docs/standards/OBSERVABILITY_STANDARDS.md` | EDIT | One citation-accuracy fix (§9 cited a nonexistent dedicated `redact.ts` test file; corrected to the real `fingerprint()`/`validate.test.ts` relationship) |
| `package.json` | EDIT | `security:check` script |
| `.github/workflows/ci.yml` | EDIT | New "Secret scan" step in `quality` job |
| `docs/runtime/KNOWN_ISSUES.md` | EDIT | `ISS-2026-007` record |

## 5. Database / API / UI / security / performance / audit / data-migration effects

**Database (§13):** none — least-privilege/RLS posture is preserved by construction (no schema exists to weaken). **API (§14):** headers/CORS/CSRF/session/validation contracts fixed (`SECURITY_STANDARDS.md` §4), all `NOT_RUN` — no route exists. **UI (§15):** none. **Security (§16):** this checkpoint's actual, primary deliverable — `scripts/security/check-secrets.ts` is real and enforced today (0 findings against this repository's real tracked files, proven not assumed); `ADR-0010` fixes the secret-storage mechanism before any real secret exists. **Performance (§17):** `security:check` runs in well under a second against this repository's current file count; wired as one more `quality`-job step, no new CI job. **Audit (§18):** `docs/runbooks/secret-leak-incident-response.md` fixes the rotation/incident procedure now, honestly marked unrehearsed against a real leak. **Data migration (§19):** none — no compromised data/keys exist.

## 6. Tests / commands / results (§28/§30)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **PASS** (exit 0) |
| `pnpm run lint` | **PASS** (exit 0) — same pre-existing informational message as every prior checkpoint |
| `pnpm run test` (`node:test`) | **PASS** — 117/117 (100 carried from `PH0-93` + 17 new in `scripts/security/check-secrets.test.ts`) |
| `pnpm run docs:check` | **PASS** — 0 issues (after fixing the one citation-accuracy error this checkpoint's own review found in `OBSERVABILITY_STANDARDS.md`) |
| `pnpm run security:check` (new) | **PASS** — 0 secret-shaped findings across every real tracked file in this repository |
| `pnpm run test:e2e` | **PASS** — 3/3, unchanged from `PH0-91` |
| `pnpm run standards:check` | **PASS** — 0 suppression-governance violations |
| `pnpm run git:check` | **PASS** — no collision risk (dirty-worktree note is this checkpoint's own uncommitted changes, expected pre-commit) |
| `pnpm audit` (attempted, not wired) | **FAIL — genuine upstream 410, disclosed as `ISS-2026-007`, not hidden or faked** |

**Regression (§29):** all 100 tests carried from `PH0-93` continue passing unchanged; zero existing test weakened or disabled. `scanRepository()`'s own test is a real regression guard — any future checkpoint that accidentally commits a matching secret shape fails this exact test, not merely the CLI gate.

## 7. Documentation updated

`docs/adr/ADR-0010-*.md`, `docs/standards/SECURITY_STANDARDS.md`, `docs/runbooks/secret-leak-incident-response.md`, `docs/adr/README.md`, `docs/standards/OBSERVABILITY_STANDARDS.md`, this build log, and the runtime ledgers listed in §11.

## 8. Errors / recovery / risks / issues

No error. Two disclosed findings, neither hidden: `ISS-2026-007` (new — `pnpm audit` upstream endpoint retirement, `OPEN`, Medium) and a citation-accuracy self-correction in `OBSERVABILITY_STANDARDS.md` (caught by this checkpoint's own `docs:check` run before commit — direct evidence the validator built at `PH0-92` continues catching real errors across unrelated checkpoints, not just its own authoring session).

## 9. Rollback/recovery note

Last trusted checkpoint: `c70ccec`. Rollback: `git revert` this checkpoint's commit(s) removes the ADR, standards doc, scanner + tests, runbook, and the `docs/adr/README.md`/`OBSERVABILITY_STANDARDS.md`/`package.json`/`ci.yml`/`KNOWN_ISSUES.md` edits, with no data loss (documentation + zero-dependency utility code only, no schema/data/vendor credential exists to roll back). Forward-fix preferred.

## 10. Acceptance criteria / Definition of Done

- Prioritized Phase 0 controls pass negative evidence with legitimate paths preserved — confirmed: `check-secrets.test.ts` proves both true-positive detection (5 pattern kinds against synthetic fixtures) and true-negative preservation (placeholder/short/non-suspicious values never flagged).
- Residual critical blocker is zero — confirmed: `docs/discovery/06_SECURITY_BASELINE.md`'s sole finding (`SEC-2026-001`) was `Info`, not `Critical`/`High`; this checkpoint reduces its scope further (secret scanning + secret-mechanism ADR now real) without introducing a new blocker.
- Mandatory gates pass, worktree reconciled, no unauthorized scope changed — confirmed (§6).

**Task `CG-S5-PH0-015` is `COMPLETED`.**

## 11. Commit / branch / next eligible prompt

- Branch: `claude/lanjut-btusq6`.
- Ledgers updated in the same checkpoint: `docs/runtime/TASK_LEDGER.md` (`CG-S5-PH0-015` → completed, `CG-S5-PH0-016` → READY), `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`, `docs/build-log/phase-00/00_PHASE0_EXECUTION_INDEX.md` (row `015` done, `016` READY), `HANDOFF.md`.
- Next eligible prompt: `CG-S5-PH0-016` (Prompt 95, Data Classification Foundation) — per the execution index, `PH0-095`'s upstream (`PH0-081..094`) is now fully satisfied.
