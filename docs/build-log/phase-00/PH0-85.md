# PH0-85 — Development Environment Foundation

**Prompt:** `CG-S5-PH0-006` (`CG-AABPP-PH0-085` v0.6.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/05-phase-00-discovery-foundation/85_DEVELOPMENT_ENVIRONMENT_FOUNDATION_PROMPT.md`
**Status:** `COMPLETED` (pending calling-process review to mark `CG-S5-PH0-006` `VERIFIED` in `TASK_LEDGER.md`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app`, working dir `/home/user/cargogrid.app` |
| Branch | `agent/cargogrid-autonomous-build` |
| HEAD at authoring time | `ce0039c` ("agent: update Phase 0 handoff — resume point Prompt 85") |
| Worktree state | Clean at start; this task's writes only (listed §4) |
| Upstream dependency | `CG-S5-PH0-004..005` both `VERIFIED` (`PH0-83.md`, `PH0-84.md`) |
| Downstream | `CG-S5-PH0-007` (Prompt 86, Environment Validation), `CG-S5-PH0-008` (Prompt 88, CI/CD Baseline — consumes the pnpm/lockfile decision) |
| Mutation performed | **First non-documentation files in this repository.** `.gitignore`, `package.json`, `pnpm-lock.yaml`, `tsconfig.json`, `next-env.d.ts`, `eslint.config.js`, `supabase/config.toml`, `.env.example`, `scripts/preflight-env-check.ts`. No production/staging config, no real secret, no applied migration, no `app/` directory (forbidden per §12 and `ADR-CAND-ARCH-011`/`04_REPOSITORY_TARGET_STRUCTURE.md` wave-2 boundary). |

## 1. Hierarchy / objective / source

- **Hierarchy:** Workstream: Developer Experience; Epic: Reproducible Local Development; Capability: Developer environment bootstrap; Feature slice: One-command documented safe local setup; Atomic task: `CG-S5-PH0-006` (WBS `CG-WBS-085`).
- **Objective (§4):** Implement a reproducible local development setup aligned with the verified repository toolchain and isolated from production data/services.
- **Source requirement (§6):** `docs/discovery/03_TOOLCHAIN_DEPENDENCY_BASELINE.md` (package-manager authority explicitly `MISSING`, deferred to this task); `docs/architecture/04_REPOSITORY_TARGET_STRUCTURE.md` (wave-1 scope); `docs/architecture/11_DEVOPS_WORKSTREAM.md` (`ADR-CAND-ARCH-024`); `AGENTS.md` "Stack baseline" (ratified target).

## 2. Baseline

Before this task: 498 tracked files, 100% documentation (483 `.md` + 15 `.sha256`), zero package manager/manifest/lockfile/tsconfig/`.gitignore` (`docs/discovery/03_*.md` §2, re-confirmed unchanged by `PH0-83.md` §2's drift re-check). `ADR-0001` (this branch's own prior checkpoint, `PH0-84.md`) already established that no empty domain-folder stub may be created — respected throughout this task (no `app/` directory created).

## 3. Task #1 — version detection and pinning

No repository-declared version existed to detect (greenfield). Pins were selected using **real registry evidence gathered this checkpoint** (`npm view <pkg> version` / `versions` / `dist-tags`), not from training-data memory — see `docs/adr/ADR-0002-package-manager-and-toolchain-pins.md` for the full evidence table and reasoning, including the deliberate choice to pin `typescript@5.9.3` rather than the registry's `latest` (`7.0.2`, a compiler-implementation-magnitude jump with no verified ecosystem compatibility evidence available to this agent).

**Package manager: pnpm `10.33.0`** (Node.js `>=22.11.0`, LTS "Jod"). Resolved via `ADR-0002`, which formally closes the package-manager component of `ADR-CAND-ARCH-024` (the CI/CD-platform-product component stays open for `PH0-088`).

## 4. Files created

| Path | Purpose |
|---|---|
| `.gitignore` | First file written to this repository (per `ISS-2026-003`) — `node_modules/`, `.next/`, `.env*.local`, Supabase local runtime state, caches, editor/OS files, `.vercel/` |
| `package.json` | `engines.node`, `packageManager` (pnpm pin), ratified-stack dependencies (`next`, `react`, `react-dom`, `@supabase/supabase-js`, `@supabase/ssr`), dev tooling (`typescript`, `eslint`, `eslint-config-next`, `@types/*`), scripts: `typecheck`, `lint`, `preflight` |
| `pnpm-lock.yaml` | Real, generated lockfile from an actual `pnpm install` run this checkpoint (349 packages resolved) — the deterministic-install artifact `ADR-CAND-ARCH-024` requires |
| `tsconfig.json` | `strict: true` + `noUncheckedIndexedAccess`, matching `AGENTS.md`'s "TypeScript strict mode"; `include` covers `scripts/**/*.ts` now and `**/*.tsx` for Phase 1's future `app/` |
| `next-env.d.ts` | Standard Next.js TypeScript ambient-types reference (hand-authored boilerplate; no `next dev`/`next build` was run to auto-generate it, since no `app/` exists yet) |
| `eslint.config.js` | Flat config wrapping `eslint-config-next`'s default export |
| `supabase/config.toml` | Empty local-project scaffold from a real `supabase init` run (CLI `2.109.1`), `project_id` corrected to `cargogrid`, Postgres major version `17` (Supabase-managed default) — the wave-1 "`supabase/` (empty project scaffold)" deliverable per `04_REPOSITORY_TARGET_STRUCTURE.md` line 174. Not a domain-folder stub (`ADR-0001` is scoped to business-domain route/query folders, not infrastructure project config). |
| `.env.example` | Safe placeholder schema for local Supabase URL/anon/service-role keys — no real value |
| `scripts/preflight-env-check.ts` | Production-link + missing-variable safeguard (task #3) — see §6 for the four scenarios tested |
| `README.md` (edited) | Expanded from a 1-line stub: prerequisites, one-command-ish setup, local-DB commands, troubleshooting table, recovery note |
| `docs/adr/ADR-0002-package-manager-and-toolchain-pins.md`, `docs/adr/README.md` (edited) | Formalize the package-manager decision, update the ADR index/register |

**Deliberately not created:** `app/` (any route group), `next.config.*`, `dev`/`build` npm scripts — all Phase 1 scope per `04_REPOSITORY_TARGET_STRUCTURE.md`'s migration-wave table (wave 1 = `package.json`/`tsconfig.json`/`.gitignore`/`supabase/` only; wave 2 = Phase 1 Platform Core creates `app/`). Running `next dev`/`next build` today would correctly fail ("no pages or app directory found") — documented as expected in `README.md`, not silently worked around with a stub page (would violate `ADR-0001`'s spirit).

## 5. Database / API / UI / security / performance / audit / data-migration effects

**Database (§13):** `supabase/config.toml` only — no migration applied, no live database contacted; `major_version = 17` recorded as the target, matching Supabase's managed default. **API (§14):** none — no endpoint exists. **UI (§15):** none — no `app/` directory. **Security (§16):** `.env.example` uses placeholder values only; `scripts/preflight-env-check.ts` is the production-link safeguard (verified working, §6); `SUPABASE_SERVICE_ROLE_KEY` is documented server-only per `AGENTS.md`; no secret committed (verified by scan, §6). **Performance (§17):** `pnpm install` completed in 4.6s this checkpoint (349 packages, warm registry cache) — recorded as evidence, not an SLA. **Audit (§18):** every tool version and command below is recorded verbatim. **Data migration (§19):** none.

## 6. Tests / commands / results (§28/§30)

All commands run for real this checkpoint (not simulated), exit codes recorded:

| Command | Purpose | Result |
|---|---|---|
| `pnpm install` | Deterministic install from lockfile | **PASS** — 349 packages resolved, 0 vulnerabilities reported, exit 0 |
| `pnpm run typecheck` (`tsc --noEmit`) | TypeScript strict-mode compile check | **PASS** (exit 0) — caught and fixed one real `noUncheckedIndexedAccess` violation in `scripts/preflight-env-check.ts` during authoring (not silently ignored) |
| `pnpm run lint` (`eslint .`) | Flat-config lint against `eslint-config-next` | **PASS** (exit 0) — one cosmetic warning (anonymous default export in `eslint.config.js`) found and fixed during authoring |
| `pnpm run preflight` — no env vars set | Negative test: missing-variable detection | **PASS** — correctly failed with exit 1, named all 3 missing variables |
| `pnpm run preflight` — local URL (`127.0.0.1:54321`) | Positive test: local dev is allowed | **PASS** — exit 0 |
| `pnpm run preflight` — production-looking URL (`https://abcxyz.supabase.co`), no override | Negative test: production-link safeguard | **PASS** — correctly failed with exit 1, explained the override mechanism |
| `pnpm run preflight` — production-looking URL + `ALLOW_NON_LOCAL_SUPABASE_URL=true` | Positive test: explicit override honored | **PASS** — exit 0 |
| Secret scan (`grep` for AWS key patterns, PEM private-key headers, Stripe live-key prefix across every new file) | Forbidden-content check | **PASS** — none found |
| `find . -maxdepth 2 -type d -empty` | `ADR-0001` compliance (no empty domain stub) | **PASS** — zero empty directories |
| `ls app` | Wave-1 boundary check (`04_*.md` line 174) | **PASS** — confirmed absent |
| `git status --short` | Forbidden-change / worktree check (§12) | **PASS** — only this task's authorized files |

**Regression (§29):** no prior developer scripts, package lock, or CI existed to regress (greenfield, confirmed by `PH0-83.md` §2). No test/control was disabled or weakened.

## 7. Documentation updated

`README.md`, `docs/adr/ADR-0002-*.md`, `docs/adr/README.md` (§5.2, §6), this build log, and the runtime ledgers listed in §11 below.

## 8. Errors / recovery / risks / issues

No error. Two minor issues were found and fixed **during authoring, before this checkpoint closed** (not left as known defects): the `noUncheckedIndexedAccess` type error in the preflight script, and the anonymous-default-export lint warning in `eslint.config.js`. Neither is carried forward as an open item. `ISS-2026-003` (root `.gitignore`) is **closed** by this checkpoint. The CI/CD-platform-product component of `ADR-CAND-ARCH-024` remains open, owned by `PH0-088` (unchanged, not a new risk).

## 9. Rollback/recovery note

Last trusted checkpoint: `ce0039c`. Rollback: `git revert` this task's commit removes every file listed in §4 with no data loss (pure configuration, no schema/data exists). Forward-fix preferred. Recovery steps for a broken local environment are documented in `README.md` "Troubleshooting"/"Recovery".

## 10. Acceptance criteria / Definition of Done

- Clean isolated setup is reproducible from docs/scripts with passing smoke/baseline — confirmed: `pnpm install` → `pnpm run typecheck` → `pnpm run lint` → `pnpm run preflight` (with env vars) all pass for real (§6).
- No secret, real data, or production dependency introduced — confirmed (§6 secret scan; `.env.example` placeholders only).
- Mandatory gates pass, worktree reconciled, no unauthorized scope changed — confirmed (§6, §4 "deliberately not created").

**Task `CG-S5-PH0-006` is `COMPLETED`.**

## 11. Commit / branch / next eligible prompt

- Branch: `agent/cargogrid-autonomous-build`.
- Ledgers updated in the same checkpoint: `docs/runtime/TASK_LEDGER.md` (`CG-S5-PH0-006` → completed, `CG-S5-PH0-007` → READY), `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md` (`CHG-2026-026`), `docs/build-log/phase-00/00_PHASE0_EXECUTION_INDEX.md` (row `006` done, `007` READY), `docs/runtime/KNOWN_ISSUES.md` (`ISS-2026-003` → `RESOLVED`).
- Next eligible prompt: `CG-S5-PH0-007` (Prompt 86, Environment Validation Foundation) — per the execution index, `PH0-086`'s upstream (`PH0-083..084`, this task) is satisfied. `PH0-086` validates the fail-fast environment contract this task established (the `preflight` script and `.env.example` schema) across a genuinely clean checkout, not just this authoring session.
