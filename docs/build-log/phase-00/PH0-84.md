# PH0-84 — ADR Baseline and Decision Governance

**Prompt:** `CG-S5-PH0-005` (`CG-AABPP-PH0-084` v0.6.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/05-phase-00-discovery-foundation/84_ADR_BASELINE_DECISION_GOVERNANCE_PROMPT.md`
**Status:** `COMPLETED` (pending calling-process review to mark `CG-S5-PH0-005` `VERIFIED` in `TASK_LEDGER.md`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app`, working dir `/home/user/cargogrid.app` |
| Branch | `agent/cargogrid-autonomous-build` |
| HEAD at authoring time | `157cd9a` ("agent: complete Phase 0 Prompt 83 …") |
| Worktree state | Clean at start except this task's writes: `docs/adr/**` + this log + ledger updates |
| Upstream dependency | `CG-S5-PH0-002..004` all `VERIFIED` (execution index rows `002`–`004`) |
| Downstream | `CG-S5-PH0-006..021` (`PH0-085` onward) — each consumes this ADR framework and register |
| Mutation performed | Documentation only. Zero runtime source/config/schema/data/deployment change (forbidden per §12). |

## 1. Hierarchy / objective / source

- **Hierarchy:** Workstream: Architecture Governance; Epic: Decision Records; Capability: ADR repository and lifecycle; Feature slice: Bootstrap approved architecture decision mechanism; Atomic task: `CG-S5-PH0-005` (WBS `CG-WBS-084`).
- **Objective (§4):** Create the repository-native ADR framework and resolve only Phase 0 technical decisions supported by verified evidence and authorized approvers.
- **Source requirement (§6):** `ARCH-035..051` runtime outputs (the 27 `ADR-CAND-ARCH-*` candidates raised across `01_*.md`–`13_*.md`); `ADR_REQUIRED` items; Step 0 decision/conflict registers (`02_CONFIRMED_DECISION_REGISTER.md`, `04_CONFLICT_REGISTER.md`).

## 2. Baseline

No ADR mechanism existed before this task (`find docs -iname '*adr*'` excluding the prompt package → none). All 27 architecture ADR candidates existed only as inline `ADR-CAND-ARCH-*` references scattered across `docs/architecture/01_*.md`–`13_*.md`, with a partial resolved/open split in `HANDOFF.md` §7. No repository-native template, status vocabulary, authority model, or index existed.

## 3. Files created/updated

| Path | Action | Reason |
|---|---|---|
| `docs/adr/README.md` | ADD | ADR framework: definition, status vocabulary, authority boundaries, file template, full 27-candidate register, index |
| `docs/adr/ADR-0001-no-empty-domain-folder-stubs.md` | ADD | The one bounded Phase 0 ADR with verified evidence now (formalizes `ADR-CAND-ARCH-011`, resolved in `PH0-83.md`) |
| `docs/build-log/phase-00/PH0-84.md` | ADD | This log |
| `docs/runtime/TASK_LEDGER.md`, `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`, `00_PHASE0_EXECUTION_INDEX.md` | EDIT | Advance `CG-S5-PH0-005` → completed, `CG-S5-PH0-006` → READY; record `CHG-2026-025` |

## 4. Decisions reconciled (tasks §20.1–§20.3)

- **Framework created (§20.1):** ADR template, 5-state status vocabulary (`PROPOSED`/`ACCEPTED`/`BLOCKED`/`SUPERSEDED`/`REJECTED`), append-only lifecycle, and a 4-tier authority model that explicitly keeps CPD/RPD out of ADR scope (product-change-control only). See `docs/adr/README.md` §1–§4.
- **Candidates reconciled (§20.2):** all 27 `ADR-CAND-ARCH-*` cataloged with status — **11 resolved** in Step 3 (`001..010,016`), **16 open** (`011..015,017..027`), each open one scoped to its exact owning task. Corrected `HANDOFF.md` §7's "10 resolved / 17 open" to the accurate 11/16 split (a one-item miscount; no candidate missing). See `README.md` §5.
- **Phase 0 ADR approved (§20.3):** only `ADR-CAND-ARCH-011` had verified evidence available now (discovery placeholder findings + the greenfield decision + the `PH0-83.md` scaffold-hygiene adoption). Approved as **`ADR-0001` — No empty domain-folder stubs** (`ACCEPTED`), with options/evidence/consequences/rollback/downstream and propagation. Every other open candidate stays `BLOCKED`/`PROPOSED` because its deciding evidence (a tool selection, a schema slice, a numeric enforcement surface) does not exist until its owning task runs — approving any of them now would violate the "recommendation is not approval" and "no ADR accepted without evidence" rules (§24/§25).
- **Propagation (§20.4):** `ADR-0001` referenced from `PH0-83.md` §4/§5(T3), `README.md` §5.2/§6, and `KNOWN_ISSUES.md`. No CPD/RPD rewritten; no `docs/architecture/**` decision altered.

## 5. Database / API / UI / security / performance / audit / data-migration effects

No schema/API/UI/data change (§13–§19). Security (§16): `ADR-0001` is protective (no unrouted/dead surface for RLS/RBAC to special-case); no protected control weakened; no secret introduced. Audit (§18): ADR lifecycle is append-only with status/approver/supersession fields per the template. Data migration (§19): none; ADRs may authorize a future migration but this task executes none.

## 6. Tests / commands / results (§28/§30)

Document-level validation (no toolchain exists), all run this checkpoint:

- **ADR schema/ID/status (§28):** `ADR-0001` has a unique ID, question, options with trade-offs, decision, cited evidence, owner/approver, status, consequences, and rollback. PASS.
- **Missing-approval gate (§25):** no dependent task is marked `READY` on an unaccepted ADR — the 16 open candidates carry no downstream `READY` claim; only `ADR-0001` (`ACCEPTED`) is relied upon. PASS.
- **Traceability propagation (§28):** `ADR-0001` back-references resolve (`PH0-83.md` §4/§5, `README.md`, `KNOWN_ISSUES.md`); register §5 accounts for all 27 candidates (`grep -oE 'ADR-CAND-ARCH-[0-9]{3}' docs/architecture` → 27 unique, all listed). PASS.
- **CPD/RPD non-alteration (§24 regression):** `02_CONFIRMED_DECISION_REGISTER.md` unchanged (`git status` shows no edit). PASS.
- **Forbidden-change (§12):** `git status --short` shows only `docs/adr/**`, this log, and ledger docs. PASS.
- **Count reconciliation:** 11 resolved + 16 open = 27 = unique candidate count. PASS.

Exit codes: all read-only `git`/`grep` commands returned 0. No install/build/unsafe default run.

## 7. Documentation updated

`docs/adr/README.md`, `docs/adr/ADR-0001-*.md`, this log, `TASK_LEDGER.md`, `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md` (`CHG-2026-025`), `00_PHASE0_EXECUTION_INDEX.md` (row `005` done, `006` READY).

## 8. Errors / recovery / risks / issues

No error. No new risk. The 16 open ADR candidates remain non-blocking (each scoped to its owning task, per `13_*.md` §11). `ISS-2026-002`/`ISS-2026-003` unchanged.

## 9. Rollback/recovery note

Last trusted checkpoint: `157cd9a`. Rollback: `git revert` this task's commit removes `docs/adr/**` and the additive ledger edits with no data loss. Forward-fix preferred.

## 10. Acceptance criteria / Definition of Done

- ADR framework usable and required Phase 0 decisions approved or explicitly blocking — done (§4).
- No product policy or runtime behavior changed — confirmed (§5, §6).
- Mandatory gates pass, worktree reconciled, no unauthorized scope — confirmed (§6).

**Task `CG-S5-PH0-005` is `COMPLETED`.**

## 11. Commit / branch / next eligible prompt

- Branch: `agent/cargogrid-autonomous-build`.
- Next eligible prompt: `CG-S5-PH0-006` (Prompt 85, Development Environment Foundation) — per the execution index, `PH0-085`'s upstream (`PH0-083..084`) is now satisfied. `PH0-085` is the first task that will resolve `ADR-CAND-ARCH-024..027` (CI/CD platform, package manager, secret manager, observability, hosting) as its own deliverables and will add the root `.gitignore` (`ISS-2026-003`) before the first non-documentation file lands.
