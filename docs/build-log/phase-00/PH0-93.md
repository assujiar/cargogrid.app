# PH0-93 — Observability Baseline

**Prompt:** `CG-S5-PH0-014` (`CG-AABPP-PH0-093` v0.6.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/05-phase-00-discovery-foundation/93_OBSERVABILITY_BASELINE_PROMPT.md`
**Status:** `COMPLETED` (pending calling-process review to mark `CG-S5-PH0-014` `VERIFIED` in `TASK_LEDGER.md`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app`, working dir `/home/user/cargogrid.app` |
| Branch | `claude/lanjut-btusq6` |
| HEAD at authoring time | `8caebb9` ("agent: complete Phase 0 documentation foundation") |
| Worktree state | Clean at start (confirmed `git status`); this task's writes only (§4) |
| Upstream dependency | `CG-S5-PH0-082..093` (Prompts 82–92) all `VERIFIED` per `TASK_LEDGER.md`/`00_PHASE0_EXECUTION_INDEX.md` row `014` |
| Downstream | `CG-S5-PH0-015` (Prompt 94, Security Baseline Controls); Phase 1's first `app/` slice registers `@vercel/otel` in `instrumentation.ts` against Better Stack, consuming `scripts/observability/logger.ts`'s event shape/redaction/correlation contract unchanged |
| Mutation performed | New: `docs/adr/ADR-0009-observability-platform.md`, `docs/standards/OBSERVABILITY_STANDARDS.md`, `scripts/observability/logger.ts`(+test), `docs/runbooks/observability-exporter-outage.md`. Edited: `docs/adr/README.md`, `docs/standards/DOCUMENTATION_STANDARDS.md` (runbooks-path addition), `scripts/docs/check-doc-links.ts` (scan `docs/runbooks/`), `docs/build-log/phase-00/PH0-92.md` (one cosmetic backtick-formatting fix, §8). **No Better Stack account/API key/`instrumentation.ts` created** — no `app/` exists to instrument yet (Phase 1 scope, same discipline `PH0-90`/`PH0-91`/`PH0-92` applied to components/domain factories/audience-facing docs). |

## 1. Pre-flight collision check

`mcp__github__list_pull_requests` (owner `assujiar`, repo `cargogrid.app`, state `open`) → `[]`. `git fetch origin main claude/lanjut-btusq6` then `git rev-parse HEAD origin/main` confirmed `HEAD` (`8caebb9`) is exactly one commit ahead of `origin/main` (`92d698f`). No collision risk.

## 2. Scope decision — vendor-neutral foundation only, no real export (disclosed, not silently narrowed)

Prompt 93 §20 asks for "foundation middleware/hooks for safe request/job/integration/db/error health signals" and "alert/runbook stubs." No `app/`, `server/`, database, or job queue exists yet (`docs/discovery/05_ROUTE_MODULE_INVENTORY.md`) — there is no real request/job/DB call to instrument. **Decision (consistent with every prior Phase 0 foundation checkpoint):** resolve the vendor ADR with real evidence, build the vendor-neutral structured-logging/redaction/correlation/cardinality utility that *will* back Phase 1's real instrumentation, prove its safe-degrade behavior with real failure-injection tests, and instantiate the one runbook whose content is already fully knowable today (the safe-degrade design itself) rather than inventing a fake incident history. Every layer that genuinely cannot be real yet (11 dashboards, 8 alerts, health/readiness endpoints, DB/queue metrics, correlation propagation through a real request) is named explicitly `NOT_RUN` with its owning future task (`docs/standards/OBSERVABILITY_STANDARDS.md` §7) — never fabricated as wired.

## 3. Vendor decision (`ADR-0009`)

**Better Stack**, selected over Grafana Cloud, Axiom, Datadog, and a composed multi-tool stack — full trade-off analysis and real evidence (fetched this checkpoint from `betterstack.com/logs`, `betterstack.com/pricing`, and comparison sources, plus `nextjs.org`/`vercel.com`'s official OpenTelemetry integration docs) in the ADR itself. Key reasoning: single product covers all 4 signal types + incident management (avoids the "four-tool correlation problem" `ADR-CAND-ARCH-026` names explicitly), is OpenTelemetry-native (so the *code-side* integration point is Next.js's own vendor-neutral `@vercel/otel`, not a proprietary SDK), and its free tier is real and sufficient to verify the foundation without a paid commitment, with paid tiers scaling the same product to "Scale-up" capability without a migration — directly satisfying the candidate's own stated requirement.

## 4. Files created/edited

| Path | Action | Purpose |
|---|---|---|
| `docs/adr/ADR-0009-observability-platform.md` | ADD | Resolves `ADR-CAND-ARCH-026` |
| `docs/standards/OBSERVABILITY_STANDARDS.md` | ADD | Signals/dashboards/alerts (by reference), structured log schema, correlation ID contract, redaction, cardinality, retention, `NOT_RUN` table, safe-degrade rule |
| `scripts/observability/logger.ts` | ADD | Vendor-neutral structured logger: event builder, `redact()`, `compareSeverity()`, `generateCorrelationId()`, `assertBoundedDimension()`, safe-degrade `emit()` |
| `scripts/observability/logger.test.ts` | ADD | 20 tests, including real failure-injection for the safe-degrade rule |
| `docs/runbooks/observability-exporter-outage.md` | ADD | First real `docs/runbooks/` instance, instantiated from `SUPPORT_RUNBOOK_TEMPLATE.md`, `NOT_YET_REHEARSED` against a live vendor outage (disclosed) |
| `docs/adr/README.md` | EDIT | `ADR-CAND-ARCH-026` marked `ACCEPTED`; `ADR-0009` added to index |
| `docs/standards/DOCUMENTATION_STANDARDS.md` | EDIT | `docs/runbooks/` added to the scanned-prefix list and audience-class table (first real instance) |
| `scripts/docs/check-doc-links.ts` | EDIT | `docs/runbooks/` added to `DOC_SCAN_PREFIXES` |
| `docs/build-log/phase-00/PH0-92.md` | EDIT | One cosmetic fix — the "before" value in a typo-fix description was itself a valid-looking backtick path, tripping the (correctly stricter, newly-runbooks-aware) validator; changed to non-backtick prose with zero change to the described fact (§8) |

## 5. Database / API / UI / security / performance / audit / data-migration effects

**Database (§13):** none — no speculative telemetry schema added, matches the explicit prompt constraint. **API (§14):** correlation-ID header/field name and generation function defined (`X-CargoGrid-Request-Id`/`correlation_id`, `generateCorrelationId()`); no real endpoint exists to carry it yet. **UI (§15):** none — no business dashboard, no operator health view (out of approved scope per §15's own text). **Security (§16):** `redact()` mechanically strips every sensitive-key-pattern match (secret/password/token/key/authorization/cookie/ssn/npwp/bank/account_number/salary/payroll) from structured `fields` before it is ever emitted, proven by real tests including Indonesia-specific tax/bank/payroll key names; `message` is honestly disclosed as **not** mechanically redacted (free-text detection is unreliable) — a caller-discipline rule, not a false mechanical guarantee. **Performance (§17):** the safe-degrade `emit()` wraps every sink call so a slow/failing telemetry backend can never block a real request — proven by failure-injection tests, not merely documented. **Audit (§18):** `docs/standards/OBSERVABILITY_STANDARDS.md` §1 restates (does not weaken) "logs are not audit substitutes" — audit retains its own stronger 7-year retention class independent of whatever retention a logging vendor's tier provides. **Data migration (§19):** none.

## 6. Tests / commands / results (§28/§30)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **PASS** (exit 0) |
| `pnpm run lint` | **PASS** (exit 0) — same pre-existing informational message as every prior checkpoint |
| `pnpm run test` (`node:test`) | **PASS** — 100/100 (80 carried from `PH0-92` + 20 new in `scripts/observability/logger.test.ts`) |
| `pnpm run docs:check` | **PASS** — 0 issues (after the one cosmetic `PH0-92.md` fix, §8) |
| `pnpm run test:e2e` | **PASS** — 3/3, unchanged from `PH0-91` |
| `pnpm run standards:check` | **PASS** — 0 suppression-governance violations |
| `pnpm run git:check` | **PASS** — no collision risk (dirty-worktree note is this checkpoint's own uncommitted changes, expected pre-commit) |

**Regression (§29):** all 80 tests carried from `PH0-92` continue passing unchanged; zero existing test weakened or disabled. The safe-degrade rule (§21/§22's own acceptance criteria) is proven, not merely asserted: `logger.test.ts`'s three-test "emit — safe-degrade rule" suite injects a real throwing primary sink, then a real throwing primary **and** fallback sink, and asserts `emit()` never rethrows either way.

## 7. Documentation updated

`docs/adr/ADR-0009-*.md`, `docs/standards/OBSERVABILITY_STANDARDS.md`, `docs/runbooks/observability-exporter-outage.md`, `docs/adr/README.md`, `docs/standards/DOCUMENTATION_STANDARDS.md`, `scripts/docs/check-doc-links.ts`, this build log, and the runtime ledgers listed in §11.

## 8. Errors / recovery / risks / issues

No error. One cosmetic self-correction: `docs/standards/DOCUMENTATION_STANDARDS.md`/`scripts/docs/check-doc-links.ts` (`PH0-92`) did not scan `docs/runbooks/` because no file existed there yet at that checkpoint — extending the scan this checkpoint (§4) correctly caught that `PH0-92.md`'s own typo-fix description quoted the *wrong* path as illustrative "before" text inside backticks, which the validator (reasonably) treated as a real reference. Fixed by removing the backticks around the illustrative value (zero change to the described fact) rather than special-casing the validator for a one-off prose-formatting choice.

## 9. Rollback/recovery note

Last trusted checkpoint: `8caebb9`. Rollback: `git revert` this checkpoint's commit(s) removes the ADR, standards doc, logger module + tests, runbook, and the `docs/adr/README.md`/`DOCUMENTATION_STANDARDS.md`/`check-doc-links.ts`/`PH0-92.md` edits, with no data loss (documentation + zero-dependency utility code only, no schema/data/vendor credential exists to roll back). Forward-fix preferred.

## 10. Acceptance criteria / Definition of Done

- Core foundation signals are traceable, safe, and documented with bounded overhead — confirmed: event shape/correlation/redaction/cardinality contract fixed and tested; safe-degrade proven under real failure injection.
- No sensitive leak, false health, or unsupported monitoring claim — confirmed: `redact()` tested against real sensitive-key patterns including Indonesia-specific fields; `message`'s non-redaction is honestly disclosed, not hidden; no dashboard/alert/health-endpoint is claimed wired when none exists (`NOT_RUN` table, §7 of the standards doc).
- Mandatory gates pass, worktree reconciled, no unauthorized scope changed — confirmed (§6).

**Task `CG-S5-PH0-014` is `COMPLETED`.**

## 11. Commit / branch / next eligible prompt

- Branch: `claude/lanjut-btusq6`.
- Ledgers updated in the same checkpoint: `docs/runtime/TASK_LEDGER.md` (`CG-S5-PH0-014` → completed, `CG-S5-PH0-015` → READY), `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`, `docs/build-log/phase-00/00_PHASE0_EXECUTION_INDEX.md` (row `014` done, `015` READY), `HANDOFF.md`.
- Next eligible prompt: `CG-S5-PH0-015` (Prompt 94, Security Baseline Controls) — per the execution index, `PH0-094`'s upstream (`PH0-082..093`) is now fully satisfied.
