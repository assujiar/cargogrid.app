# HRT-290 — Ticket Assignment

**Status: `VERIFIED`** — implementation complete, Tier A gates green, Tier B taxonomy self-check performed and recorded (§8), adversarially self-tested against a live disposable Postgres 16 database with real forged-session `psql` probes and a real two-process concurrency race, and **Tier C batch review complete** (§13) — a combined batch with Prompt 289, per `BUILD_EXECUTION_PROTOCOL.md` §3.4/§3.2 trigger 6. Four adversarial lenses ran against the combined `289+290` diff; one CRITICAL and one HIGH finding were CONFIRMED live and fixed at root cause via an additive corrective migration — see §13 for the full disposition table, propagation sweep, fresh gate suite, and the milestone assessment of the complete `286-290` Ticket workstream arc.

**Prompt ID:** `CG-S12-HRT-018` (Prompt 290, Phase 7 — HRIS and Ticketing)
**Branch:** `claude/prompt-281-290-la1lvj`
**Batch:** second and FINAL prompt of the "lanjut prompt 281-290" authorized range — a combined Tier C review runs across Prompts 289 and 290 together after this checkpoint lands, per `BUILD_EXECUTION_PROTOCOL.md` §3.4/§3.2 trigger 6 (Prompt 290 explicitly consumes Prompt 289's own SLA/routing precedents).

## 1. What this checkpoint builds

Explainable, versioned ticket-routing rules; an assignment event ledger (claim/accept/decline/reassign/unassign/transfer/auto_route, with reason/source); atomic, idempotent claim/assign/reassign under real concurrency; a read-only workload snapshot; effective-eligibility (employee lifecycle status + currently-dated approved leave) binding; and the UI to drive all of it — bounded to the `internal`/`customer` ticket channels only, matching HRT-288's own already-established, precedented "helpdesk has no non-Supreme-Admin eligibility model" shape rather than fighting it.

## 2. Files changed

- **Migration (1, additive):** `supabase/migrations/20260731140000_create_ticket_assignment.sql` — zero lines of any prior ticketing migration (`060000`/`070000`/`080000`/`090000`/`100000`/`110000`/`120000`/`130000`) touched. Two additive columns on `app.tickets` (`assignment_confirmed_at`, `assignment_confirmed_by`); two new tables (`app.ticket_routing_rules`, `app.ticket_routing_rule_versions`); one new ledger table (`app.ticket_assignment_events`); `app.assign_ticket` explicitly `drop function if exists` + re-created (two new trailing DEFAULTed parameters genuinely widen its type signature — a bare `create or replace` would have left a stale, ambiguous second overload, per this migration's own mandatory reading of HRT-287's established discipline); `app.transfer_ticket_queue` widened via a genuine `create or replace` (unchanged signature, body now also logs the new ledger).
- **DB test (1 new, self-contained):** `scripts/db-tests/ticketing-assignment.sql` — tenants `asn1`/`asn2`, own UUID range (`...0000002901xx`/`...02xx`), 11 numbered assertion groups including a real two-OS-process concurrent claim race.
- **Service layer:** extends `server/contracts|queries|mutations/ticketing.ts` (+ `.test.ts` for all three) — never a parallel module, matching this task's own explicit instruction and HRT-289's own "one canonical ticket service" precedent.
- **UI (2 new, 5 edited):** `app/(tenant)/[tenantSlug]/tickets/routing/{page,routing-admin-panel}.tsx` (new — routing-rule authoring/publish + a live scope preview tool); `tickets/[ticketId]/page.tsx` + `ticket-detail-panel.tsx` (new claim/accept/decline/auto-route controls, an explainable-eligibility candidate list, and assignment-history section); `tickets/actions.ts` (new HRT-290 server actions); `tickets/page.tsx` + `tickets-list-panel.tsx` (a read-only per-queue workload panel).
- **Docs:** `docs/build-log/phase-07/HRT-290.md` (this file); `docs/runtime/TASK_LEDGER.md` (new `CG-S12-HRT-018` row, left at `COMPLETED`, not `VERIFIED`, per instruction — the batch closes together with `CG-S12-HRT-017`). `docs/runtime/HANDOFF.md` untouched, per instruction.

## 3. The three-channel routing/assignment shape — exact decision

Re-confirmed by direct code read (not assumed) before writing a single line: `app.assign_ticket`/`app.transfer_ticket_queue` (HRT-286, re-shaped by HRT-287/288) already reject nothing for `channel='customer'` — a customer-channel ticket routes/claims/assigns through the identical internal staff/queue mechanism as an internal ticket; only the customer-facing READ surface (HRT-287's own `get_customer_ticket`/`list_customer_tickets`) omits assignee/queue identity. Helpdesk is different in kind, not merely narrower: `app.is_ticket_staff` is hardened (HRT-288 decision 3) so a helpdesk ticket's staff bar is Supreme-Admin-only, with dedicated, unmodified `app.assign_helpdesk_ticket`/`app.transfer_helpdesk_support_queue` siblings. This checkpoint's entire new routing-rule/eligibility/ledger/claim/accept/decline/auto-route machinery is therefore bounded to `internal`/`customer` channels — every new function explicitly, cleanly rejects a helpdesk-channel ticket (`channel_not_supported`, pointing at the existing helpdesk RPCs), never fighting or duplicating that shape. Disclosed, bounded limitation: no non-Supreme-Admin "Platform support agent" eligibility model exists yet for helpdesk; a future prompt introducing one could extend `app.ticket_routing_rule_versions.channel`'s CHECK the same additive way HRT-287/288 widened `channel` itself.

## 4. Routing precedence and concurrency — the exact decisions

- **Precedence** (`app._resolve_ticket_routing_rule_for_ticket`) mirrors HRT-289's `app.resolve_effective_sla_policy_version` verbatim in shape: `rank()`-ranked by specificity (category set > priority set > explicit `precedence_rank` tie-break — only two scope dimensions vary here since the queue is the engine's OUTPUT, not an input), a genuine tie raises `ticket_routing_rule_ambiguous_match` rather than picking arbitrarily — live-reproduced (two different rules, identical scope, identical rank) and live-resolved. `app.publish_ticket_routing_rule_version` supersedes the SAME rule's own prior published version under a parent-row lock, applying HRT-289's own self-found Tier C fix (`app.publish_sla_policy_version`'s "a revised version tied against its own predecessor") **from the start**, not rediscovered.
- **Concurrency**: optimistic-version-plus-row-lock, matching every prior ticket RPC — no advisory lock introduced. `app.claim_ticket` takes `select ... for update` exactly like `app.assign_ticket` always has; the record_version check is ordered immediately after existence/access/(queue-membership authority), before every business-rule check that depends on a fresh read, mirroring the established order every sibling RPC in this migration set already uses (channel-reject → authority → version → business rule). Live-reproduced with two real, concurrent OS `psql` processes (`scripts/db-tests/wms-picking-concurrency-helper.sh`, reused verbatim): exactly one winner, the loser's own captured error is a clean `stale_version`/`serialization_failure`, never a raw constraint violation. A separate, narrower `ticket_already_assigned` conflict exists for the genuinely different "caller supplies a current version but the ticket is already owned by someone else" case — both are clean, discriminated errors.
- **Workload cap**: hard block, no override, on self-service `app.claim_ticket`; an explicit, TKT:Assign-authorized, disclosed override (`p_override_workload_limit`) on manager-driven `app.assign_ticket` — a manager legitimately needs to push a genuinely urgent ticket onto someone already at capacity; a self-serving agent should not load-balance around their own configured limit. Eligibility (see §5), by contrast, is a hard block on BOTH paths with **no** override anywhere — live-proven: `assign_ticket` with `p_override_workload_limit=true` still refuses an on-leave employee.

## 5. Effective eligibility and "delegation" — exact decisions

`app._is_employee_ticket_eligible` reuses ONLY existing canonical concepts: `app.employees.lifecycle_status='active'` (HRT-274) AND no currently-dated, `status='approved'` `app.leave_requests` row (HRT-278/280) — no parallel "agent status" column or table invented. "Delegation" (section 13's own named requirement) is honestly disclosed as N/A for ticket routing rather than silently dropped: this repository's one existing delegation concept (`app.approval_delegations`, PLT-118) is scoped to approval-DECISION authority, a materially different shape from ticket routing, where "coverage while an agent is away" is already provided by queue-membership breadth itself — any other active, eligible member of the same queue may already claim/be assigned any ticket in that queue, with no named 1:1 backup required. Reusing the wrong existing concept would not have been better than disclosing none.

## 6. Workload snapshot — exact decision

`app.get_ticket_queue_workload` is a live `COUNT(*)` aggregation over `app.tickets`/`app.ticket_queue_members`, computed on every call — never a second, separately-maintained counter that could drift from the real current-owner columns (section 13's own "never a second source of truth for who's assigned"). The same live-count helper (`app._count_employee_active_ticket_assignments`) backs the workload cap check, `app.list_ticket_assignment_candidates`, and `app.get_ticket_queue_workload` — one source, three consumers.

## 7. Self-found defects fixed before commit

1. **(C-13-adjacent, self-found by a direct Tier B code re-read against the established precedent, not by review)** — three new read RPCs (`app.list_ticket_assignment_candidates`, `app.get_ticket_queue_workload`, `app.list_ticket_assignment_events`) omitted `perform app.assert_actor_is_session_identity(p_actor_auth_user_id);`, present in every comparable read RPC in the prior ticketing migrations (`app.get_ticket`, `app.list_ticket_watchers`, `app.list_sla_policies`, ...). Without it, a caller could supply a colleague's `auth_user_id` as `p_actor_auth_user_id` and the function would evaluate authority (queue membership, `is_ticket_staff`, etc.) AS that other identity without ever verifying the caller genuinely IS them. Fixed by adding the assertion to all three, matching every sibling read RPC exactly. Re-verified live against the full `scripts/db-tests/ticketing-assignment.sql` after the fix — all 11 assertion groups still pass.
2. **(C-20, self-found by the same Tier B walk)** — `app.preview_ticket_routing` (section 14's own named "routing preview" API operation) was built, migrated, and unit-tested, but had no UI caller anywhere — an unreachable capability. Fixed by wiring a real admin "Preview a scope" tool into `tickets/routing/routing-admin-panel.tsx` (a new `previewTicketRoutingAction` server action with its own richer result-carrying state type, since this reads rather than mutates), not merely disclosed.

## 8. Tier B taxonomy self-check (`docs/standards/RECURRING_DEFECT_TAXONOMY.md` §4)

| Class | Result |
|---|---|
| C-01 idempotency replay verifies target | N/A — no new `p_idempotency_key` parameters; `create_ticket_routing_rule`/`_version` use the established natural-key (code/rule_id+version_number) idempotent-create pattern, verified clean against `create_sla_policy`'s own precedent. |
| C-02 guard raised inside its own catching handler | Verified clean — the one `exception when unique_violation` handler (`create_ticket_routing_rule`) bare-`raise`s, never self-swallows. |
| C-03 version predicate checked without a not-found guard | Verified clean by direct re-read — every versioned `UPDATE ... where record_version = p_expected_version` (claim/accept/`_apply_ticket_assignment`/assign/transfer/auto_route/publish) is immediately followed by `if not found then raise stale_version`. |
| C-04 decision on an unlocked parent row | Verified clean — every ticket-mutating function takes `select ... for update` on the ticket first; `publish_ticket_routing_rule_version` locks version-then-rule, matching the SAME order `app.publish_sla_policy_version` already uses (no inconsistent pair introduced). |
| C-05 permission check after a disclosing read | Verified clean — every "does this exist" pre-check either discloses nothing more than existence (matching `add_ticket_queue_member`'s own established queue-existence-before-authority order) or is folded into the identical `ticket_not_found`/`insufficient_authority` an unauthorized caller already gets. |
| C-06 implicit `auth.uid()` reached from an explicit-actor RPC | Verified clean — the sole `is_ticket_queue_member(...)` call passes `p_actor_auth_user_id` explicitly. |
| C-07 unmasked `jsonb` snapshot | N/A — no new snapshot columns. |
| C-08 widened function leaks to existing holders | N/A — `assign_ticket`'s OUTPUT type is unchanged; new params are inputs only. |
| C-09 handler catches too broad an SQLSTATE | N/A/clear — the one `unique_violation` handler has exactly one possible source constraint. |
| C-10 file/evidence re-validation | N/A — no file handling in this migration. |
| C-11 grants absent or blanket | Verified clean — explicit `grant select`/`grant execute` per object, `revoke execute on all functions in schema app from public;` present (ERR-2026-004 discipline), internal helpers granted to `service_role` only. |
| C-12 `SECURITY DEFINER` with no authority check | Verified clean — every function granted to `authenticated` gates on TKT:Edit, TKT:Assign, structural queue membership, "is the current assignee", or `is_ticket_staff`. |
| C-13 authority-of-claimed-actor vs. identity-of-caller | Two genuine findings, both fixed — see §7 item 1. |
| C-14 identifier > 63 chars | Verified clean — longest new identifier is 46 characters. |
| C-15 missing domain constraints | Verified clean — `max_active_assignments_per_member` CHECK `> 0` when set; every enum column CHECK-closed. |
| C-16 non-function export from a `"use server"` module | Verified clean — every new export from `tickets/actions.ts` is an `async function`; confirmed independently by a clean `next build`. |
| C-17 `select("*")` | N/A — no new raw-table TypeScript reads. |
| C-18 maker-checker/MFA on the happy path only | N/A (no dual-control requirement in scope) — every real state transition (claim/assign/reassign/unassign/decline/auto-route) enumerated and confirmed to carry its own appropriate bar (queue membership+eligibility+workload for claim; TKT:Assign+eligibility+overridable-workload for assign; current-assignee-only for accept/decline; `is_ticket_staff` for transfer/auto-route). |
| C-19 supersede/uniqueness on a narrower scope tuple | Verified clean — `publish_ticket_routing_rule_version` supersedes scoped to `rule_id` only, deliberately (mirrors the SLA precedent exactly): two different rules may legitimately publish overlapping scope, surfaced as `ticket_routing_rule_ambiguous_match` at resolution time, not suppressed at publish time. |
| C-20 built-but-unreachable capability | One genuine finding, fixed — see §7 item 2. |
| C-21 inconsistent lock order between sibling functions | Verified clean — the only two-parent-lock function (`publish_ticket_routing_rule_version`) locks version-then-rule, the identical order `app.publish_sla_policy_version` already established; no other function in this migration locks two of the same tables. |
| C-22 threshold compared without normalizing unit | N/A — `max_active_assignments_per_member`/`precedence_rank` are dimensionless counts/ranks, not currency-denominated. |
| C-23 named spec requirement silently dropped | Two items, both disclosed (never silently dropped) — see §9. |
| C-24 unmasked reason/money into `app.audit_logs` | Verified clean by direct re-read of all 9 new `capture_audit_event` call sites — every one passes `p_reason=null` and either a `jsonb_build_object` explicit-key allowlist or the existing, already-audited `app.ticket_audit_projection`; every real free-text reason (decline reason, transfer reason, auto-route rule-match note) lives only in `app.ticket_events`/`app.ticket_assignment_events`, both governed by the narrower ticket-participant RLS, never `app.audit_logs`. |

## 9. Disclosed scope boundaries (C-23 discipline — named, not silently dropped)

- **"claim/unclaim" (section 14's own API-impact wording) is unified into ONE mechanism** (`app.decline_ticket_assignment`), not two near-duplicate RPCs. Both a self-initiated give-back and a "return this to the backlog" action are the identical state transition (`assignee_employee_id → null`, current-assignee-only, ledger-logged) — a mandatory reason is required for either case, a deliberate, minor discipline choice (audit value regardless of who initiated it), not a functional gap.
- **"Bulk reassignment after revocation" (section 22's own alternative-flow wording) is not built.** When an employee is removed from a queue (`app.remove_ticket_queue_member`, unchanged) or becomes ineligible (suspended/terminated/on-leave), their EXISTING already-assigned tickets remain assigned to them — visible as an ineligible current owner in the candidate/workload views, but not automatically swept. A bulk/automatic reassignment is a genuinely separate, batch-shaped operation with its own partial-failure/concurrency semantics, and touching potentially many `app.tickets` rows from within the existing, already-`VERIFIED` `app.remove_ticket_queue_member` would be a significant, out-of-mandate behavior change to that function. A manager can already compose today's primitives (`app.list_ticket_assignment_candidates`/`app.get_ticket_queue_workload` to find affected tickets, `app.assign_ticket` to reassign each) — just not as one atomic bulk call.
- **Round-robin (section 22's own wording) is deliberately implemented as deterministic LEAST-LOADED, not literal rotation** — `assignment_mode='least_loaded'`, not `'round_robin'`, named accurately rather than overclaiming a rotating-pointer algorithm this checkpoint does not build. Both are legitimate instances of "configured deterministic routing" (the spec's own broader category); least-loaded was chosen as the more useful default and is fully deterministic/explainable/tie-broken, matching every acceptance-criteria requirement that actually applies.
- **A dedicated "queue backlog" list view with an inline per-row claim action is not built as a separate page.** The existing `tickets/page.tsx` "Queue" tab (HRT-286) already lists every visible ticket; claim/accept/decline/auto-route/candidates/workload live on the ticket detail page (`tickets/[ticketId]`) instead, reachable in one click from that list. A future prompt could add an inline quick-claim affordance to the list rows themselves; this checkpoint's own bounded slice covers the full capability through one additional click rather than duplicating it in two places.

## 10. Gate suite (exact results)

| Gate | Result |
|---|---|
| `pnpm run typecheck` | 0 errors |
| `pnpm run lint` | 0 errors, 271 warnings (identical baseline to HRT-289's own close — zero new warning class) |
| `pnpm run test` | 4059/4059 passed (+20 new tests: 8 contract, 7 query, 8 mutation — net of shared assertions) |
| `pnpm exec next build` | Clean; `/[tenantSlug]/tickets/routing` present alongside every pre-existing route |
| `pnpm run db:test` (full official harness, run fresh end-to-end twice — once before, once after the self-found-defect fix pass) | Both runs: `EXIT=0`, `==> db-tests: ALL PASSED` — every migration including `20260731140000`, every prior ticket-channel test file (`ticketing-internal`/`ticketing-customer-channel`/`ticketing-helpdesk`/`ticketing-sla`/`ticketing-knowledge-base`) re-confirmed passing, plus the new `ticketing-assignment.sql`'s own 11 assertion groups |
| `pnpm run git:check-paths` | Clean, 15 files staged, 0 forbidden, 0 caution |
| `pnpm run security:check` | Clean, no secret-shaped pattern |

## 11. Adversarial verification summary (live, not merely read)

- **Real two-process concurrent claim race** (`scripts/db-tests/wms-picking-concurrency-helper.sh`, two genuinely separate OS `psql` processes): exactly one of two employees wins; the loser's own captured error is `stale_version`/`serialization_failure` (SQLSTATE), never a raw `unique_violation`; exactly one `claim` ledger row exists after the race.
- **Cross-channel isolation**, live: `claim_ticket`/`accept_ticket_assignment`/`decline_ticket_assignment`/`auto_route_ticket`/`list_ticket_assignment_candidates` all cleanly reject a real helpdesk-channel ticket; a real customer-channel ticket successfully claims/assigns staff-side (proving decision 3, §3) while `list_ticket_assignment_candidates`/`list_ticket_assignment_events` refuse the customer's own identity for their own ticket with the identical `ticket_not_found` an RLS predicate would produce (no raw employee list ever reachable by a customer, section 16).
- **Cross-tenant isolation**, live: an unrelated tenant's admin reads zero rows via every new RPC and via raw-table RLS on all three new tables.
- **Eligibility**, live: a `lifecycle_status='suspended'` employee AND an employee on a currently-dated `status='approved'` leave request are both refused by `claim_ticket` (`employee_not_eligible`) and by `assign_ticket` even with the workload override flag set `true`.
- **Workload cap**, live: `claim_ticket` refuses at cap with no override; `assign_ticket` refuses at cap by default and succeeds with `p_override_workload_limit=true`, logging the override reason to the ledger.
- **"Assignment never broadens linked-record access"** (section 24), live: claiming a ticket changes zero `app.role_assignments`/`app.principal_memberships` rows for the claiming employee, and grants no access to an unrelated ticket in a queue they are not a member of (mirrors HRT-287's own "a ticket link grants no access" proof).
- **Schema-privilege defense in depth**, live: `anon` holds zero `SELECT`/`EXECUTE` on any of the three new tables or any new function; `authenticated` holds zero `EXECUTE` on the four internal (`_`-prefixed) helpers.

## 12. Recommended next task

None within this authorization. This is the FINAL prompt of the "lanjut prompt 281-290" range. Per `BUILD_EXECUTION_PROTOCOL.md`, a combined Tier C review (four lenses, live-tested) now runs across the full Prompt 289 + Prompt 290 diff together; neither `CG-S12-HRT-017` nor `CG-S12-HRT-018` reaches `VERIFIED` until that review, its fix pass, and independent re-verification all close. No further prompt (`CG-S12-HRT-019`/Prompt 291 or beyond) is authorized to begin without a fresh, separate operator authorization.

## 13. Tier C batch review (adversarial review + fix pass) — closes the whole `289-290` batch to `VERIFIED`

Performed `2026-08-14` by the orchestrating fix session, independent of the two implementing agents (`HRT-289.md` §1-§13 and this file's own §1-§12 above), covering the combined `289+290` diff (commits `6ae4895`/`9ef006a`) — the FINAL batch of the "lanjut prompt 281-290" operator authorization. Per this repository's own established HRT-278/279/280 and HRT-283/284/285 convention, the full cross-batch disposition table, propagation sweep, and fresh gate suite live here once, at the last-in-batch build log; `HRT-289.md` §14 carries only that prompt's own findings, cross-referencing back to this section for the full picture.

### 13.1 Method

Four parallel adversarial review lenses (spec-compliance; security/RLS/tenant isolation; correctness/concurrency; cross-prompt integration with an explicit mandate to assess whether the full `286-290` Ticket workstream arc composes soundly) each independently reviewed the combined diff against a live, disposable Postgres 16 database. Per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §5.3, every finding was independently RE-DERIVED live by this fix session before being accepted — never fixed from a lens citation alone. Verification used disposable databases (`cg_racecheck` pre-fix, `cg_fixverify` post-fix, both built from a full, real 215-migration apply including the corrective migration for the post-fix leg) with real seeded fixture data from all six ticketing db-test files, forged-session `psql` probes (`request.jwt.claims` + `set role authenticated`, the same technique every db-test file in this repository already uses), and two genuinely concurrent OS `psql` processes (`scripts/db-tests/wms-picking-concurrency-helper.sh`) for the workload-cap race, reproduced both broken (pre-fix) and correctly-blocked (post-fix).

### 13.2 Full disposition table (both prompts)

| # | Prompt | Lens(es) | Finding | Severity | Verdict | Disposition |
|---|---|---|---|---|---|---|
| 1 | 289 | Spec-compliance | `app.ticket_sla_clocks`'s RLS SELECT policy (`ticket_sla_clocks_select_scoped`) omitted the `not app.actor_holds_customer_user_layer(tenant_id)` narrowing every sibling SLA/KB/routing/assignment table in this same batch correctly carries — a customer-channel requester's own raw table read could see `sla_policy_version_id`/`sla_calendar_version_id`/`started_at`/`response_met_at`/`response_breached_at`/`resolution_met_at`/`resolution_breached_at`/`last_evaluated_at`/`record_version`, every field decision 10's own written guarantee withholds from a requester | **CRITICAL** | **CONFIRMED**, independently RE-DERIVED (not accepted from lens citation): the shipped `ticketing-sla.sql` never actually constructs a customer-channel ticket (see finding 2), so this review built one from scratch — `app.create_customer_ticket` + a real published customer-scoped SLA policy + `app.start_ticket_sla_clock` + a forged session for the genuine customer-account-member requester — and confirmed, pre-fix, `select * from app.ticket_sla_clocks` returned the full row, all 9 withheld fields included | **FIXED** — `supabase/migrations/20260731150000_harden_ticketing_sla_assignment_batch_289_290_review_fixes.sql`, `drop policy` + `create policy` adding the identical narrowing every sibling table already carries. Re-verified live post-fix on the same reproduction: the identical forged session now reads zero rows; `app.get_ticket_sla_status_for_requester` (the legitimate customer-safe RPC) still returns its own correct, narrower projection, unaffected. |
| 2 | 289 | Spec-compliance, Integration | `scripts/db-tests/ticketing-sla.sql` never calls `app.create_customer_ticket`/`app.create_helpdesk_ticket` — the official `db:test` "ALL PASSED" evidence never exercised the customer/helpdesk-channel SLA policy-matching or RLS/RPC boundary paths, the exact gap that let finding 1 ship undetected | HIGH (spec lens) / LOW-MEDIUM (integration lens) | **CONFIRMED** by direct grep — zero occurrences of either function anywhere in the file | **Disclosed** (`docs/runtime/KNOWN_ISSUES.md` `ISS-2026-090`) rather than test-file-edited, per §5.6 — the concrete live exposure this gap enabled (finding 1) is independently fixed and re-verified using the exact scenario the shipped test never built; both the security and integration lenses' own independent live channel-shape probing further reduce residual risk. Editing an already-committed db-test file is outside this batch's own established additive-migration-only fix-scope convention (matching HRT-286/287/288's own three prior fix commits, none of which touched a db-test file). |
| 3 | 290 | Correctness/concurrency | The workload cap ("hard, no-override" on `app.claim_ticket`; override-only-with-explicit-flag on `app.assign_ticket`) reads `app._count_employee_active_ticket_assignments` — a live, UNLOCKED `COUNT(*)` — with no lock forcing two concurrent callers to serialize; two concurrent claims/assigns of two DIFFERENT tickets to the SAME employee could both read the pre-commit count under cap and both commit | **HIGH** | **CONFIRMED**, independently live-reproduced with two genuine OS `psql` processes racing `app.claim_ticket` on two fresh tickets under a routing rule capped at 1: BOTH succeeded, leaving the employee at 2 active assignments against a cap of 1, zero error to either caller (`app._count_employee_active_ticket_assignments` confirmed `= 2` post-race) | **FIXED** — same migration, a `pg_advisory_xact_lock` keyed on `(tenant_id, queue_id)` acquired immediately before any workload-cap-relevant read, in `app.claim_ticket` AND `app.assign_ticket`. Re-verified live: the identical two-process race on the identical fixture now produces exactly one winner (assignee set, `record_version` advanced) and one clean, discriminated `workload_limit_exceeded` for the loser — no raw constraint violation, no double-assignment. |
| 3b | 290 | Correctness/concurrency (propagation, §5.4) | `app.auto_route_ticket`'s own least-loaded candidate scan reads the identical unlocked helper over the whole candidate pool — flagged by the correctness lens as "very likely subject to the identical race," not independently live-reproduced by that lens given its own time budget | HIGH (same root cause as #3) | **CONFIRMED as same-root-cause by direct code read** (`w.active_count < v_rule.max_active_assignments_per_member` reads the same `app._count_employee_active_ticket_assignments`, no lock) — not separately live-raced (two concurrent `auto_route_ticket` calls require staging two ticket-row locks plus the shared queue-workload race in one script; the fix closes the identical mechanism `claim_ticket`/`assign_ticket` demonstrably had, so the same live proof applies by construction) | **FIXED proactively**, same migration and same lock key: `pg_advisory_xact_lock(tenant_id, target_queue_id)` acquired before the candidate scan+apply in `app.auto_route_ticket`, so all three workload-cap-relevant entry points into one queue's bookkeeping are now mutually exclusive. Deadlock-safe by construction (each function takes at most one advisory lock, always after its own single ticket-row `for update`, never nested). |
| 4 | 290 | Cross-prompt integration | `app.list_ticket_assignment_events` is missing the explicit `channel = 'helpdesk'` rejection every sibling assignment RPC in this same migration already carries (claim/accept/decline/assign/transfer/auto_route all reject a helpdesk ticket with `channel_not_supported`) — a recurrence of the exact "missing channel-rejection guard on a channel-inapplicable RPC" shape HRT-288's own Tier C review already found and fixed once (for `add_ticket_watcher`/`remove_ticket_watcher`) | MEDIUM (currently inert) | **CONFIRMED** by direct code read — no guard present; independently confirmed structurally INERT today (every `insert into app.ticket_assignment_events` call site is downstream of one of the six functions above, all of which already reject the channel before any insert, so the ledger can never actually hold a helpdesk-channel row) | **FIXED** — same migration, added the identical `channel_not_supported` guard every sibling function already carries. Re-verified live: a Supreme Admin calling the function directly against a real helpdesk ticket now gets `channel_not_supported` instead of a (previously always-empty) result set. |
| 5 | 290 | Spec-compliance, Integration | Prompt 290's own "queue, team and user assignment" wording names a "team" dimension nowhere built (no team/team-membership entity exists anywhere in this repository) and nowhere disclosed in HRT-290's own §9 C-23 walk, unlike its three other named narrowings; separately, routing precedence reuses only the ticket's own `priority` column and never a live `app.ticket_sla_clocks` signal, also undisclosed in §9 | MODERATE / LOW | **CONFIRMED**, both by direct grep/code read — no `team` entity anywhere; `app._resolve_ticket_routing_rule_for_ticket` never references `app.ticket_sla_clocks` | **Disclosed**, not code-fixed (`docs/runtime/KNOWN_ISSUES.md` `ISS-2026-089`) — queue-based grouping is the correct, working team-analog every other capability in this phase already uses (independently re-verified by three of the four lenses); the SLA-signal narrowing is a real, correct, deterministic engine that simply does not yet consume one additional live signal, a genuine feature addition suited to Prompt 291 (Ticket Escalation), not a bounded review-fix. Both now named explicitly in this section and in `HRT-290.md`'s own §3 cross-reference below. |
| — | 289+290 | Security/RLS/tenant-isolation | Full lens verdict: **CLEAN, zero findings within scope** (C-05/06/07/08/10/11/12/13/17) | — | Independently spot-re-confirmed by this session for the two highest-priority properties this lens exists to guard in this batch: (a) the `ticket_sla_clocks` leak (finding 1, actually a spec-lens-owned RLS-narrowing finding, re-derived and fixed above) and (b) that no OTHER new table in either migration is missing the same narrowing — a full sweep of `pg_get_expr` output for every `create policy` in both migrations found exactly one instance (finding 1), matching this lens's own "areas verified clean" list exactly. The lens's own "false lead, ruled out by live testing" (`run_ticket_sla_evaluation_batch`/`expire_kb_article_versions_batch` missing an explicit `assert_actor_is_session_identity` call, protected transitively via `evaluate_permission`) was not independently re-tested by this session (not a finding — the lens itself ruled it out with a live reproduction and did not report it) | No fix needed. |

### 13.3 Propagation sweep (`BUILD_EXECUTION_PROTOCOL.md` §5.4)

- **`actor_holds_customer_user_layer` narrowing shape (finding 1):** every RLS SELECT policy on every new table across BOTH migrations (`sla_calendars`, `sla_calendar_versions`, `sla_calendar_business_hours`, `sla_calendar_holidays`, `sla_policies`, `sla_policy_versions`, `ticket_sla_clocks`, `ticket_sla_clock_events`, `kb_articles`, `kb_article_versions`, `kb_ticket_article_links`, `ticket_routing_rules`, `ticket_routing_rule_versions`, `ticket_assignment_events`) was individually re-read against its own live `pg_get_expr` output — not by grep. Exactly one instance (`ticket_sla_clocks_select_scoped`) was missing the narrowing; `ticket_sla_clock_events_select_scoped` uses a different, even narrower, deliberately-staff-only mechanism (documented in its own policy comment) and was independently confirmed correct as-is; every other table already carries the narrowing correctly from the start. No further instance found in either migration.
- **Workload-cap unlocked-count shape (finding 3):** every call site of `app._count_employee_active_ticket_assignments` across the migration was enumerated (grep-confirmed) — exactly three (`claim_ticket`, `assign_ticket`, `auto_route_ticket`'s candidate scan). All three now serialize on the same `pg_advisory_xact_lock(tenant_id, queue_id)` key, closing the race across all three entry points into one queue's workload bookkeeping, not just the one the lens live-reproduced.
- **Missing channel-rejection-guard shape (finding 4):** every client-callable RPC in `20260731140000` was checked for a `v_ticket.channel = 'helpdesk'` guard against whether it structurally needs one (i.e., can ever be called against/mutate a helpdesk ticket) — `list_ticket_assignment_candidates`/`get_ticket_queue_workload` already correctly reject via `ticket_not_found`/authority failure upstream of ever reaching a helpdesk row in practice, but `list_ticket_assignment_events` was the one genuine gap; now fixed. The SAME shape was also checked against HRT-289's two migrations (no analogous per-ticket read RPC omits an existing guard) and against the rest of the repository's ticketing surface — no further instance found beyond the one HRT-288 already fixed once and this one.
- **No further instances found in any of the sweeps beyond what's listed in §13.2.**

### 13.4 Corrective migration

One new, additive migration: `supabase/migrations/20260731150000_harden_ticketing_sla_assignment_batch_289_290_review_fixes.sql` — fixes findings 1, 3, 3b, and 4 (one `drop policy`/`create policy` pair; three `create or replace function` statements for `claim_ticket`/`assign_ticket`/`auto_route_ticket`, each adding the shared advisory-lock serialization; one `create or replace function` for `list_ticket_assignment_events`, adding the channel guard). Neither `20260731120000`, `20260731130000`, nor `20260731140000` was edited in place. No service-layer/UI changes were required (all four fixes are contained entirely within the database layer; no TypeScript contract/query/mutation signature changed).

### 13.5 New recurring-defect-taxonomy class

No new class. Finding 1 (a new RLS table missing the `actor_holds_customer_user_layer` narrowing a sibling table in the same migration correctly carries) is a same-shape recurrence of the class this repository has now seen at `CG-S10-ATW-023`/`ATW-032` (`ISS-2026-010`) and is exactly the discipline `20260730560000_harden_customer_user_layer_default_deny.sql` established repository-wide — reinforcing, not creating, that class. Finding 3/3b (optimistic-version check with no lock on the SHARED aggregate resource a decision depends on, as distinct from the ticket row itself) is a new variant worth naming precisely: `docs/standards/RECURRING_DEFECT_TAXONOMY.md`'s existing C-04 ("decision on an unlocked parent row") is about the row being WRITTEN; this is a decision based on a COUNT over a set of OTHER rows never locked at all, which C-04 as written does not cleanly cover. Registered as a new taxonomy note under C-04 in `docs/standards/RECURRING_DEFECT_TAXONOMY.md` §4: "C-04b — an aggregate-based business-rule check (a workload cap, a capacity count, a running total) computed live over a set of sibling rows with no lock of any kind is a TOCTOU race between any two callers whose target rows differ, even when each caller correctly locks its OWN row; close it with an advisory lock (or an equivalent serializing mechanism) keyed to the shared resource the aggregate is computed over, not to either caller's own row." Finding 4 is the same class already registered as part of HRT-288's own Tier C review (no new entry needed, cross-referenced).

### 13.6 Fresh gate suite (full, independent re-run — not accepted from any self-report)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**, 271 warnings — identical pre-existing baseline, zero new warnings from this batch's own files |
| `pnpm run test` | **4059/4059 pass, 0 fail** (unchanged from HRT-290's own original close — this fix pass is SQL-only, no new TypeScript surface) |
| `pnpm run db:test` (official harness, full run from scratch) | **`==> db-tests: ALL PASSED`, exit 0** — 215 migrations applied cleanly (including `20260731150000`), 166 test files run, including all six ticketing files and the full `rbac-enforcement.sql`/ATW-032 authority-surface and optimistic-concurrency sweeps |
| `pnpm exec next build` | `✓ Compiled successfully` — all routes present, including `/[tenantSlug]/tickets/sla`, `/[tenantSlug]/knowledge-base`, `/[tenantSlug]/knowledge-base/[articleId]`, `/[tenantSlug]/tickets/routing` |
| `pnpm run docs:check` | clean |
| `pnpm run security:check` | clean — no secret-shaped pattern |
| `pnpm run security:audit` | clean — no dependency advisories |
| `pnpm run data-classification:check` | clean |
| `pnpm run threat-model:check` | clean — 25 entries (4 critical, 11 high, 9 medium, 1 low), unchanged by this fix |
| `pnpm run standards:check` | clean — no suppression-governance violations |
| `pnpm run git:check-paths` | clean |
| `pnpm run git:check` | clean (branch name and worktree-collision checks both pass; expected uncommitted changes and unmerged-ahead-of-origin state noted, matching every prior batch-close run) |

Independently, this task's own mandated standalone-per-file technique was ALSO run directly against a separately-built, fresh, full 215-migration database (`cg_fixverify`, the same database the live re-reproductions in §13.2 above ran against): all six ticketing db-test files exit 0 — `ticketing-internal.sql` (15/15), `ticketing-customer.sql` (19/19), `ticketing-helpdesk.sql` (16/16), `ticketing-sla.sql` (`ALL PASSED`, including its own §6 live two-process concurrent breach-evaluation race), `ticketing-knowledge-base.sql` (`ALL PASSED`), `ticketing-assignment.sql` (`ALL PASSED`, including its own §6 live two-process concurrent claim race). **All five prior-checkpoint ticketing files plus this batch's own two new ones are confirmed passing, byte-identical to their own pre-fix baselines**, proving zero regression to any of the five already-`VERIFIED`/newly-`VERIFIED` ticket-channel/capability checkpoints.

Plus direct code reads of every Critical/High fix, confirming each is genuinely present and correctly shaped (not merely asserted): the `ticket_sla_clocks_select_scoped` policy text, all three `pg_advisory_xact_lock` call sites (`claim_ticket`/`assign_ticket`/`auto_route_ticket`), and the `list_ticket_assignment_events` channel guard were each read directly from the corrective migration and independently confirmed to match the live catalog (`pg_get_functiondef`/`pg_get_expr`) after apply — not merely trusted from the migration file's own text.

### 13.7 Milestone assessment — does the complete `286-290` Ticket workstream arc compose into one sound, coherent capability?

**Yes.** Five checkpoints, four independent Tier C review rounds (286 and 287 and 288 each standalone; 289+290 combined here), one canonical `app.tickets`/`app.ticket_messages` model extended — never re-derived — across three genuinely distinct principal shapes (employee/internal, customer-account, tenant-as-requester/helpdesk) via one generalized `app._is_ticket_requester_party`/`app.is_ticket_staff`/`app.can_access_ticket` predicate family; one SLA precedence-engine shape (HRT-289) reused verbatim in structure by the routing-rule precedence engine (HRT-290); one `actor_holds_customer_user_layer` RLS-narrowing discipline applied consistently across all thirteen new tables this final batch adds, with exactly one lapse found and fixed; one workload/eligibility model reusing only canonical `app.employees`/`app.leave_requests` data, never a parallel "agent status" concept; channel-appropriate staffing preserved end-to-end into both the SLA and assignment layers (helpdesk cleanly, consistently rejected everywhere a non-Supreme-Admin model would not apply). Every one of the six ticketing db-test files, run together against one from-scratch schema build, shows zero regression anywhere in the chain. `ISS-2026-085` (the identity-model gap HRT-286 originally disclosed) remains `RESOLVED IN FULL` from HRT-288's own close, unaffected by this batch. The two Critical/High findings this final review surfaced were both genuine, both fixed at root cause, both independently re-verified live, and both swept for propagation across the rest of the batch (finding 3's own propagation instance, 3b, being the clearest proof the sweep discipline works as intended — a lens-flagged-but-unconfirmed suspicion turned into a second confirmed, fixed instance of the same defect). The residual disclosed gaps (`ISS-2026-089` team/SLA-signal wording, `ISS-2026-090` SLA test-channel coverage) are genuinely narrow, honestly stated, and none carry live exposure. **The complete five-prompt Ticket workstream is ready to be called `VERIFIED`.**

### 13.8 Task state

`CG-S12-HRT-017` (Prompt 289) and `CG-S12-HRT-018` (Prompt 290) are now both **`VERIFIED`**, moved together per `BUILD_EXECUTION_PROTOCOL.md` §4's all-or-nothing batch rule. `docs/runtime/TASK_LEDGER.md` and `docs/build-log/phase-07/00_EXECUTION_INDEX.md` rows 289/290 updated accordingly.

**This closes the FINAL batch of the "lanjut prompt 281-290" operator authorization.** Prompts 291-297 (Ticket Escalation, Typed Linked Records, and whatever follows in Phase 7) — and any further Phase-7/Ticket-workstream work of any kind — remain OUTSIDE this authorization. `00_EXECUTION_INDEX.md`'s own rows for 291+ are updated to explicitly note this; nothing in this batch's own closure constitutes or implies a pre-authorization for any further prompt. A fresh, separate, explicit operator authorization is required before any further work on this workstream may begin.

One commit made across the whole batch's fix pass: "agent: fix batch review findings (CG-S12-HRT-017..018, Prompts 289-290)" — not pushed, per this task's own instruction. `docs/runtime/HANDOFF.md` intentionally not touched (outside this fix task's own scope).
