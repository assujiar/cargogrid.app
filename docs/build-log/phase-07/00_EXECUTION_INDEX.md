# Phase 7 (HRIS and Ticketing) — Execution Index

**Prompt:** `CG-S12-HRT-001` (273, HRIS and Ticketing WBS Runtime Kickoff)
**Runtime output of:** `273_HRIS_TICKETING_WBS_RUNTIME_KICKOFF_PROMPT.md`
**Runtime state set by this checkpoint:** `PHASE_7_IN_PROGRESS`
**Runtime state NOT set by this checkpoint:** `PHASE_7_VERIFIED` — reserved exclusively for Prompt 297 (`CG-S12-HRT-025`, Closure Verification), per `273_*.md` §"Required output" and `272_HRIS_TICKETING_README.md` §10.
**Owner (every row, this build's standing convention):** Claude Code (runtime build agent)

---

## 1. Checkpoint

| Field | Value |
|---|---|
| Repository root | `/home/user/cargogrid.app` |
| Branch | `claude/lanjut-271-275-f4mz9a` |
| HEAD commit | `408b741c59acf16f3df15dc44e53f60018f2740d` (2026-08-09 06:32:19 +0000) |
| Worktree | clean at time of this checkpoint's own read pass (no uncommitted changes observed to any tracked source file) |
| Checkpoint timestamp | 2026-08-09T07:08:47Z |
| Migrations applied (count) | 182 files under `supabase/migrations/`, latest `20260730820000_harden_procurement_c05_tenant_disclosure_sweep_batch5.sql` |
| `docs/build-log/phase-07/` | Did not exist before this checkpoint (`ls` confirmed `ENOENT`) — this file is the phase's first artifact |
| Phase 0–6 status | `PHASE_0_VERIFIED` through `PHASE_6_VERIFIED` all set; `PHASE_6_VERIFIED` most recently closed this session (`docs/build-log/phase-06/PROCUREMENT_VENDOR_CLOSURE_REPORT.md`) |
| Standing quality baseline (cited, not re-run this checkpoint per orchestrating instruction) | typecheck 0 errors; lint 0 errors / 175 warnings; `pnpm run test` 3428/3428 passing; `next build` clean; `pnpm run db:test` ALL PASSED across 150 files; `security:audit` PASS; `standards:check`/`docs:check`/`data-classification:check`/`security:check` all PASS |
| Domain code footprint for Phase 7's own subject matter | `git ls-files app/ lib/ server/ components/ supabase/migrations/ | grep -iE 'hris|employee|ticketing|ticket'` → **zero matches**, independently re-run this checkpoint. Phase 7 is genuinely greenfield. |

---

## 2. Runtime entry verdict — **PASS**

Prompt 273's mandatory entry gate requires `RUNTIME_DISCOVERY_VERIFIED`, `RUNTIME_ARCHITECTURE_VERIFIED`, and `PHASE_0_VERIFIED` through `PHASE_6_VERIFIED` at the same checkpoint, or the task must stop and write `PHASE_7_BLOCKED`.

- `PHASE_0_VERIFIED` … `PHASE_5_VERIFIED`: standing, established at their own respective phase closures (Phases 0–5 all closed prior to this session).
- `PHASE_6_VERIFIED`: set this session (`docs/build-log/phase-06/PROCUREMENT_VENDOR_CLOSURE_REPORT.md`), independently re-confirmed as the most recent phase closure before this kickoff ran.
- `RUNTIME_DISCOVERY_VERIFIED` / `RUNTIME_ARCHITECTURE_VERIFIED`: standing from Step 3 (Prompts 40–41) closure, re-affirmed by every intervening phase's own entry gate (Phases 1–6 each required and passed the identical check before proceeding).
- No unresolved `PHASE_7_BLOCKED`-triggering condition was found.

**Verdict: entry gate PASSES.** `PHASE_7_IN_PROGRESS` is set by this checkpoint (§13 below). `PHASE_7_VERIFIED` is explicitly **not** set — only Prompt 297 may set it.

---

## 3. Ownership/ADR map (10 required reconciliation items, `273_*.md` §"Required ownership reconciliation")

| # | Item | Verdict | Basis |
|---|---|---|---|
| 1 | Platform identity vs. HR employee profile ownership; tenant/company/branch/department/team/position/reporting-line scope | **RESOLVED — ADR-0023 Part A (team) + Part B (employee identity)** | `docs/adr/ADR-0023-phase7-hris-organization-team-and-employee-identity-reconciliation.md`. Part A: `app.org_units.unit_type` gains an additive fifth value, `team` — a genuine, load-bearing gap between the live schema (`supabase/migrations/20260716101726_create_org_units.sql:33-34`, four values only) and Prompt 275's own binding text (§13, §24) naming `team` as canonical. Part B: employee identity is ratified as a required, non-duplicating extension of `app.users` (formalizing the already-unanimous, 9-document architecture record at `ADR-CAND-ARCH-002`, never before promoted to a ratified ADR file). The narrower question of whether Employee *also* registers a `master_type_code='employee'` row (mirroring `driver`/`vendor`) is deliberately left open for Prompt 274's own checkpoint — recommended, not forced. |
| 2 | Effective-dated employment/organization/position/manager links without duplicating Platform organization truth | **RESOLVED by ADR-0023 Part A** — Prompt 275's own migration widens `org_units`/`enforce_org_unit_parent_shape` additively; position/grade/assignment tables reference `app.org_units` and `app.master_records`(employee) directly, no second hierarchy table. |
| 3 | Candidate/recruitment identity, document and user-account conversion ownership | **CLEAR, no ADR needed.** `276_RECRUITMENT_ATS_PROMPT.md:60,108` already binds candidate/application identity as purpose-bound and separate from Platform user/employee identity until an explicit, governed onboarding conversion (Prompt 277). Candidate data never touches `org_units`, position, or `master_records` before conversion. |
| 4 | Operations fleet/driver/vendor/assignment identities vs. workforce references | **CLEAR, no conflict.** `app.driver_operational_profiles` (`supabase/migrations/20260729310000_create_advanced_tms_fleet_driver_device.sql:93-111`, read in full) is a pure operational layer over `driver_master_id → app.master_records(id)`, no employee/user FK exists or is implied. A future employee-as-driver linkage is fully additive (a link table or a nullable column later); not designed here, correctly out of this kickoff's scope. |
| 5 | Finance ownership of journal/period/bank-cash/payment/settlement/reconciliation vs. HR payroll calculation/handoff | **CLEAR, established boundary.** `docs/build-log/phase-04/FINANCE_HANDOFF_PACKAGE.md` §2.1/§3 documents Finance's complete, `VERIFIED` posting/journal/AP/AR/settlement ownership and the "approved handoff, never direct posting" pattern (e.g. `app.prepare_finance_vendor_bill_from_actual_cost`). `272_HRIS_TICKETING_README.md:73` states the identical rule for HRIS verbatim. Prompt 282 (Payroll) must follow the same `prepare_finance_*_from_*` handoff shape, never invent a new one. |
| 6 | Canonical customer/shipment/invoice/warehouse/vendor/user records for typed ticket links | **CLEAR, all six exist and VERIFIED.** `app.shipment_orders` (`20260727100000_create_operations_shipment_order.sql`); `app.finance_invoices` (`20260729110000_create_finance_invoice.sql`); `app.warehouses` (`20260730140000_create_advanced_tms_warehouse_zone.sql:82`); `app.vendor_profiles` (`20260730580000_create_procurement_vendor_registration.sql`); `app.users` (`20260716102620_create_users.sql`); `app.master_records` (`20260717120000_create_master_data.sql`). Prompt 292's typed ticket-link table may reference all six directly. |
| 7 | Four principal/access layers for employee, customer, CargoGrid support sessions; no fifth layer | **CLEAR, confirmed closed.** `app.principal_memberships.layer` CHECK (`supabase/migrations/20260716100825_create_principal_memberships.sql:36-37`) is closed at exactly `supreme_admin`/`tenant_admin`/`org_user`/`customer_user`; header comment states "deliberately closed." CargoGrid support access is a separate, time-bound grant mechanism (`app.support_access_grants`, `20260716111315_create_support_access.sql`), not a fifth layer. HRIS employees are `org_user` (Layer 3) principals, per ADR-0023 Part B. |
| 8 | Shared workflow/approval/numbering/config-version/notification/file-scan/audit/import-export/job primitives — confirm reusable, not rebuilt | **CLEAR, all confirmed live and reused, not rebuilt.** PLT-123 Approval Engine (`20260719090000_create_approval_engine.sql`); PLT-128 File/Document Engine (`20260719140000_create_document_file_engine.sql`); PLT-131 Import/Export (`20260719170000_create_import_export_job_framework.sql`); PLT-132 Background Job Framework (`20260719180000_create_background_job_framework.sql`); RPD-040 config-version pattern (`00-control/03_ASSUMPTION_REGISTER.md:10`, `04_CONFLICT_REGISTER.md:23`). 40+ later migrations already cite these by name rather than rebuilding parallel primitives. |
| 9 | Current dated Indonesia payroll/statutory SME activation evidence under RPD-016 | **CONFIRMED ABSENT — correctly deferred, not blocking.** No dated sign-off artifact exists anywhere (`docs/build-log/phase-07/` did not exist before this checkpoint; repository-wide search for SME/statutory-activation evidence found only forward-looking disclaimers). RPD-016 (`00-control/02_CONFIRMED_DECISION_REGISTER.md:92`) gates *activation* of specific statutory rules at Prompt 282 (Payroll), not schema-foundation work at 274/275. Not a blocker for this kickoff or for Prompts 274/275. |
| 10 | RPD-022/023/025/032/033/040 contracts and unresolved critical/high issues | **CLEAR on both halves.** All six RPD contracts are pre-existing, ratified rows in `00-control/02_CONFIRMED_DECISION_REGISTER.md` (lines 92–109) and `04_CONFLICT_REGISTER.md:23`, already reused verbatim across Phases 1–6. **Unresolved critical/high issues: zero**, independently re-verified this checkpoint by column-aware parsing of `docs/runtime/KNOWN_ISSUES.md`'s Severity/Status fields (not a bare substring grep, which produces false positives against this file's narrative prose — see §8 below for the exact method and result). |

**ADR filed this checkpoint:** `docs/adr/ADR-0023-phase7-hris-organization-team-and-employee-identity-reconciliation.md` (Status: ACCEPTED). No other item rose to a genuine, blocking ownership ambiguity requiring a new ADR — items 3–10 above are resolved by direct citation to already-existing, already-ratified repository evidence.

---

## 4. Required hierarchy — Phase → Workstream → Epic → Capability → Task

Phase 7 decomposes into the 10 workstreams `273_*.md` §"Required workstreams" names at minimum, each containing one or more of Prompts 274–297 as its capability-level epics. No capability appears in more than one workstream; no workstream is empty.

| Workstream (epic) | Capabilities (Prompt / `CG-S12-HRT-NNN`) | Count |
|---|---|---|
| 1. Workforce Master and Lifecycle | 274 (`-002`, Employee master), 275 (`-003`, Organization and position linkage) | 2 |
| 2. Recruitment and Workforce Entry/Exit | 276 (`-004`, Recruitment/ATS), 277 (`-005`, Onboarding/offboarding) | 2 |
| 3. Time, Attendance and Scheduling | 278 (`-006`, Attendance), 279 (`-007`, Shift/roster), 280 (`-008`, Leave/permit/business trip), 281 (`-009`, Overtime/timesheet) | 4 |
| 4. Payroll, Benefit and Reimbursement | 282 (`-010`, Payroll foundation/benefit/reimbursement) | 1 |
| 5. Performance, Learning and Talent | 283 (`-011`, KPI/performance), 284 (`-012`, Training/talent) | 2 |
| 6. Employee and Manager Self-Service | 285 (`-013`, ESS/MSS) | 1 |
| 7. Ticket Channels and Conversation | 286 (`-014`, Internal ticket), 287 (`-015`, Customer ticket), 288 (`-016`, CargoGrid helpdesk) | 3 |
| 8. SLA, Assignment, Escalation and Knowledge | 289 (`-017`, SLA/KB), 290 (`-018`, Assignment), 291 (`-019`, Escalation), 292 (`-020`, Typed linked records) | 4 |
| 9. HR/Ticket Privacy, Files, API and Jobs | 293 (`-021`, Sensitive personal/payroll data controls) | 1 |
| 10. Integrated Verification, Hardening, Documentation and Closure | 294 (`-022`), 295 (`-023`), 296 (`-024`), 297 (`-025`) | 4 |

**Total: 24 capabilities = exactly Prompts 274–297**, zero overlap, zero unassigned. Prompt 273 (this kickoff) sits above all 10 workstreams as the WBS root that creates them and is not itself a member of any workstream.

Each of the 24 capability prompts is, per `273_*.md` §"Required hierarchy and task contract", to be instantiated as one or more **atomic tasks** carrying unique WBS/task/prompt IDs, exact source anchors, exact prerequisite IDs, exact allowed/forbidden paths, normally 5–15 changed files and at most 1–3 additive migrations, and a runtime log path under `docs/build-log/phase-07/`. **No atomic task has been instantiated yet** — this kickoff creates the WBS structure and releases the first eligible prompt; the atomic-task-level split (e.g. whether Prompt 282's Payroll capability splits into "payroll calculation engine" / "benefit" / "reimbursement" sub-tasks) is each capability's own job at its own checkpoint, per Prompt 273 §"Required hierarchy": *"Split a capability prompt into smaller atomic tasks when file, migration, risk, approval or independent-test boundaries require it."*

---

## 5. Dependency graph

The declared "Upstream dependencies" (§9) of every one of Prompts 274–297 cite only strictly lower-numbered `HRT-` IDs — the graph is a DAG by construction, no cycle is possible. It is **not one single linear chain**, despite the README's order-table numbering (1–24) implying one. It is **two independent linear sub-chains that reconverge at HRT-293**:

```
HRT-273 (kickoff)
   │
   ├──► Workforce/HR track (12 capabilities, strictly sequential)
   │    HRT-274 → 275 → 276 → 277 → 278 → 279 → 280 → 281 → 282 → 283 → 284 → 285
   │
   └──► Ticket track (7 capabilities, strictly sequential, independent of the HR track)
        HRT-286 → 287 → 288 → 289 → 290 → 291 → 292
                                                    │
        HR track (…285) ───────────────────────────┤
                                                    ▼
                                          HRT-293 (Sensitive HR/Payroll Data
                                          Controls — upstream = "HRT-274..292",
                                          the sole convergence point)
                                                    │
                                                    ▼
                              HRT-294 → 295 → 296 → 297 (closure track, serial)
```

`HRT-286`'s own §9 reads "HRT-273; Platform user/org/workflow/file/notification/job foundations and verified canonical linked records" — it does **not** cite HRT-274 or any HR-track prompt. This matches `272_HRIS_TICKETING_README.md` §7's own architecture note that internal ticket assignees resolve through Platform Layer-3 user identity, not the not-yet-built Employee Master table.

**Practical implication, disclosed but not acted on by this checkpoint:** `HRT-286` is structurally dependency-clean on the same terms as `HRT-274` (both have upstream = `HRT-273` only, now `COMPLETED`, plus already-`VERIFIED` Platform/canonical-record primitives). Per `273_*.md` §"Execution-index rules" ("Independent tasks may run concurrently only when their file, migration, contract, data and evidence surfaces do not overlap"), `HRT-286` is a **candidate for concurrent release** alongside `HRT-274` at a future checkpoint. This index does not release it now (see §10, task state) because no concrete allowed-file path list exists yet for either track to check for overlap — no atomic task has run to produce one. §7 (collision matrix) records this as a re-evaluation trigger, not a current blocker in substance.

**Minor open item, disclosed:** `290_TICKET_ASSIGNMENT_PROMPT.md` §9 prose reads "HRT-286..289; effective user/employee/team/queue and SLA contracts" — the word "employee" appears even though its declared upstream ID range (286–289) never cites HRT-274. Cross-checked against `272_*.md` §7 ("Assignment uses eligible queue/team/user scope... without duplicating employee, user, vendor or customer identity"), this reads as generic prose, not a real hidden dependency on the Employee Master table — flagged here for the future HRT-290 checkpoint to confirm explicitly rather than assume.

---

## 6. 20-capability × 40-anchor traceability

All 24 anchors in the six HRS families and all 16 anchors in the four TKT families have at least one owning capability assigned in the WBS below. **"Covered" here means every anchor has a named owning capability prompt in this WBS — it does not mean built or verified; Phase 7 is greenfield (§1).** Cross-verified two ways that agree: the `272_*.md` §4 order table, and the independently-authored `docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md` §5.6–5.7.

| Anchor family | Owning capability(ies) | WBS status | Disclosed gate/gap |
|---|---|---|---|
| `HRS-EMP-001..004` | HRT-274, 275 | WBS-assigned | ADR-0023 (both Parts) now removes the ownership ambiguity blocking design |
| `HRS-REC-001..004` | HRT-276, 277 | WBS-assigned | none |
| `HRS-ATT-001..004` | HRT-278, 279, 280, 281 | WBS-assigned | none |
| `HRS-PAY-001..004` | HRT-282 | WBS-assigned | **EXTERNAL_VERIFICATION pending** — RPD-016 dated Indonesia payroll SME sign-off (item 9 above); HRT-282 itself must gate activation of statutory rules on this evidence when it runs. Not a blocker for 274/275. |
| `HRS-KPI-001..004` | HRT-283, 284 | WBS-assigned | none |
| `HRS-ESS-001..004` | HRT-285 (secondary touch at HRT-277 for offboarding access revocation) | WBS-assigned | none |
| `TKT-INT-001..004` | HRT-286 | WBS-assigned | none |
| `TKT-CUS-001..004` | HRT-286..288 (domain/API core) | WBS-assigned | full Customer Portal account-management/dashboard depth is Step 13 (`CPL-311..315`), explicitly out of Phase 7 scope per `273_*.md` §"Preserve these boundaries" |
| `TKT-HLP-001..004` | HRT-286..288 (domain) | WBS-assigned | **PARTIAL_BLOCKED for one release-depth item** — Phase-16 release-support-documentation depth (`RGL-411`) is out of Phase 7 scope; the Phase-7 domain/API surface itself is not blocked |
| `TKT-SLA-001..004` | HRT-289, 290, 291, 292 | WBS-assigned | none |

No anchor family is `NOT_COVERED`. The two disclosed gates (`HRS-PAY` external-verification, `TKT-HLP` partial-block) are both pre-existing, named in the source package itself, and non-blocking for every task this index releases or holds at this checkpoint.

---

## 7. File/migration/contract collision matrix

**Trivially clean — greenfield.** Independently re-confirmed this checkpoint: `git ls-files app/ lib/ server/ components/ supabase/migrations/ | grep -iE 'hris|employee|ticketing|ticket'` returns **zero matches**. No Phase 7 file, migration, table, RPC, or REST/GraphQL contract exists anywhere in the repository to collide with. The two files this kickoff itself writes (`docs/adr/ADR-0023-*.md`, `docs/build-log/phase-07/00_EXECUTION_INDEX.md`) do not collide with any existing path.

No collision matrix entry is therefore populated with a real conflict at this checkpoint. The matrix becomes load-bearing starting at whichever capability(ies) this index next releases into an atomic task with concrete allowed-file paths (§10) — that task's own build-log entry must populate this matrix for real before any second, concurrently-running task is released, per §5's disclosed HR/Ticket-track concurrency candidacy.

---

## 8. Baseline/gate matrix

Gate commands independently verified live at this repository (not assumed) unless marked otherwise. Standing baseline figures (typecheck/lint/test/db:test/build/security:audit/standards/docs/data-classification/security-check) are cited per the orchestrating instruction as already fresh this session, not re-run.

| Gate category | Status | Command / mechanism |
|---|---|---|
| Clean install | Real | `pnpm install --frozen-lockfile` (`.github/workflows/ci.yml:79-80,190-191`) |
| Phase 6→7 upgrade | Real, indirect | No dedicated snapshot-upgrade rehearsal exists. `scripts/db-tests/run.sh` applies all 182 migrations to a fresh disposable database in one sequential pass — the closest real analogue; Phase 7 inherits it automatically once its own migrations exist. |
| Generated types | **GAP — no repository precedent.** | Repo-wide grep for `supabase gen types` / `database.types.ts` / `gen:types`: zero hits. This codebase uses hand-written Zod contracts (`server/contracts/<domain>/`) instead of DB-generated types. Disclosed as a genuine absence, not assumed handled. |
| typecheck | Real | `pnpm run typecheck` → `tsc --noEmit` |
| lint | Real | `pnpm run lint` → `eslint .` |
| unit/integration | Real | `pnpm run test` → `node --experimental-strip-types --test` over `scripts/**/*.test.ts server/**/*.test.ts lib/**/*.test.ts tests/**/*.test.ts` |
| database | Real | `pnpm run db:test` → `scripts/db-tests/run.sh`, fresh disposable Postgres, all migrations + all `*.sql` RLS/RBAC tests |
| build | Real, unscripted | `next build` run directly — confirmed no `build` script exists in `package.json`; build-status log entries phrase it identically ("next build clean, N routes present") |
| API/contract (REST/GraphQL parity, RPD-033) | Real pattern | `server/contracts/<domain>/<domain>.ts` shared Zod modules consumed by both REST (`app/api/`) and GraphQL. Phase 7 should add `server/contracts/employee/`, `server/contracts/ticket/`, etc. in the same shape. |
| Browser/accessibility | Real, beyond smoke | `pnpm run test:e2e` (Playwright + `@axe-core/playwright`) — `e2e/smoke.spec.ts` plus three real-page specs (`vendor-registration`, `tenant-admin-portal`, `supreme-admin-portal`). No Phase-7-specific spec exists yet (correctly `NOT_RUN` until a real UI ships at Prompt 285/286+); the one-spec-per-first-real-page pattern is proven twice and should be followed. |
| Target-volume performance | Real, but manual/uncoupled from CI | `scripts/load-tests/run.sh` (`CG-S10-ATW-024`) — pgbench scenarios + a Node GPS-load scenario + `pagination-explain.sh`; not wired into `package.json`/CI. Phase 7 should add its own scenarios here (e.g. ticket-assignment-claim, payroll-run volume) rather than build a second harness. |
| Tenant/company/branch/department isolation | Real, universal | Every `scripts/db-tests/*.sql` file follows the mandatory "two tenants, prove cross-tenant isolation" shape (`docs/standards/TESTING_STANDARDS.md` §8). Phase 7's own db-test files must follow the identical convention. |
| Employee/customer/support isolation (four-layer model) | **GAP for employee/support specifically.** | Zero HRIS code exists (§1). The four-layer model itself is real (`app.principal_memberships`, `app.support_access_grants`) but has no employee- or ticket-specific enforcement code yet to gate. |
| Field-level isolation/masking (payroll, bank, tax, personal) | Real, extendable precedent | `scripts/data-classification/registry.ts`'s `FINANCE_REGISTRY` (FIN-214) + `pnpm run data-classification:check`. Explicitly designed to be extended per-domain — Phase 7 should add an `HRS_REGISTRY` following the identical shape. |
| Effective-date/lifecycle concurrency | Real, hardened precedent — adopt the fixed shape from day one | `record_version` + `BEFORE UPDATE` trigger, **row-locked** (`SELECT ... FOR UPDATE`) per `supabase/migrations/20260730480000_harden_optimistic_concurrency_row_lock.sql`, which fixed a genuine lost-update race across 64 paths repository-wide. Phase 7 must not repeat the original defective (unlocked) shape. |
| Ticket channel/requester/watcher/assignee/note/file/link isolation | **GAP — no precedent, correctly future work.** | Zero ticketing schema/RLS anywhere. Prompts 286–292 must each add their own `db-tests/*.sql` isolation proofs. |
| Payroll decimals/config-formula snapshots/correction-finalization/Finance-handoff reconciliation | Real, analogous Finance precedent to generalize (not literally reusable) | FIN-208 idempotency-claim-ledger (`20260729220000_create_finance_idempotent_posting.sql`) + FIN-209 tolerance/exception/certification reconciliation pattern. Also disclose: `app.enqueue_job`'s (PLT-132) idempotency replay matches the key but never verifies the target tuple (`ISS-2026-053`, OPEN, Low) — Phase 7 inherits this the moment it uses `app.enqueue_job` for payroll-run jobs. |
| Deterministic timezone/workday/shift/leave/overtime calculations | No literal Phase 5 precedent (Phase 5 is dispatch/warehouse timing, not workforce scheduling) — closest lesson is a Phase 6 defect | `ISS-2026-059` (OPEN, Low): `app._calc_vendor_kpi_rate_validity` (PRC-264) silently collapses a calendar-day window to zero rows when its own date arithmetic crosses a UTC day boundary in the 00:00–03:59 UTC band. Direct, load-bearing lesson for `HRS-ATT-US-001`: test day-boundary math across UTC midnight, not only convenient wall-clock hours. |
| SLA clock/pause/calendar/version, assignment/escalation retry, notification idempotency | **GAP — no ticket-specific precedent.** | `app.notification_engine` (PLT-128... correction: Phase 1, `20260719130000_create_notification_engine.sql`) exists as the reusable send mechanism; the FIN-208 claim-ledger shape is the pattern to generalize. No SLA-clock/escalation-specific idempotency exists yet — explicitly Prompts 289/291's own gate to add. |
| Upload malware scan, quarantine, signed URL, access log | Real, exact, proven-reusable | `app.files`/`app.file_access_logs` (PLT-128, `20260719140000_create_document_file_engine.sql`); `malware_scan_status` CHECK + `app.authorize_file_access()` true quarantine (RPD-032). Already extended (never forked) by PRC-253 for vendor documents. Phase 7 must extend the same table for employee/candidate documents, ticket attachments, and payslips. |
| Import/payroll/report/SLA job retry, cancellation, DLQ, reconciliation | Real, exact, proven-reusable, four prior adopters | PLT-131 (`20260719170000_create_import_export_job_framework.sql`) + PLT-132 (`20260719180000_create_background_job_framework.sql`), already reused verbatim by Commercial/Finance/Operations/Procurement. Phase 7 must be the fifth adopter. The `app.enqueue_job` idempotency gap noted above applies here too. |
| RPD-022 residual-risk disclosure / later-phase scope audit | Real, standing discipline + a repeatable command | `AGENTS.md` / `docs/standards/SECURITY_STANDARDS.md` §6 restate RPD-022 at every closure. The scope-audit command (`grep -rniE "hris|employee_profile|ticketing" app server supabase/migrations`) was run at Phase 6 closure (3 hits, all pre-existing Phase-1 labels) and independently re-run this checkpoint (0 hits under a slightly broader glob, §1) — Phase 7's own closure (Prompt 297) must run the mirror-image command for Step 13/14 terms. |
| Unresolved critical/high issues (item 10) | **Zero**, verified by column-aware parsing | A bare `grep -n "OPEN" ... | grep -iE "critical|high"` over `docs/runtime/KNOWN_ISSUES.md` produces false positives (the file's narrative prose contains phrases like "still `OPEN`" and "High → Medium" inside otherwise-`RESOLVED` rows). This checkpoint instead parsed the Severity/Status *table columns* directly (`awk -F'|'`) across all 63 issue records (`ISS-2026-001` through `063`, spanning both the §3 index table and the §4 prose records): **zero rows have Status exactly `` `OPEN` `` combined with a Critical/High severity.** All currently-`OPEN` rows are Medium, Low-Medium, or Low; every High-severity row found is `RESOLVED`. |

---

## 9. Critical path

The HR track (12 capabilities, `HRT-274..285`) is longer than the Ticket track (7 capabilities, `HRT-286..292`); both must complete before the sole convergence point, `HRT-293`. The critical path is therefore:

```
273 → 274 → 275 → 276 → 277 → 278 → 279 → 280 → 281 → 282 → 283 → 284 → 285 → 293 → 294 → 295 → 296 → 297
```

18 nodes (kickoff + 12 HR-track capabilities + convergence + 4 closure capabilities). The Ticket track (286–292) is not on the critical path in isolation, but `293` cannot start until **both** tracks reach `VERIFIED` — so a delay on either track delays `293` equally; only the HR track's greater length makes it the nominal "critical" path under normal (non-delayed) conditions.

---

## 10. Task state (all 25 rows; vocabulary restricted to `273_*.md`'s own set)

| Prompt | ID | Capability | State | Basis / blocked-on |
|---|---|---|---|---|
| 273 | `CG-S12-HRT-001` | HRIS/Ticketing WBS Runtime Kickoff (this task) | **VERIFIED** | Entry gate passed (§2); required ownership reconciliation performed (§3); ADR-0023 filed; this execution index is the required runtime output; `PHASE_7_IN_PROGRESS` set (§13). Self-closes on writing this index, per this build's standing convention (mirrors `CG-S11-PRC-001`/Prompt 250's own `VERIFIED` closure) — independently re-verified by the orchestrating session (`docs:check` clean, ADR file read in full and confirmed well-formed) before commit. |
| 274 | `CG-S12-HRT-002` | Employee master | **VERIFIED** | Implementation complete: `app.employees` (governed 1:1 extension of `app.master_records`, `master_type_code='employee'`, ADR-0023 Part B), a full lifecycle state machine (`draft→submitted→approved→active↔on_leave↔suspended→terminated→archived`, reject-to-draft -- point-in-time only, NOT effective-dated at the employee/HR-event level, see `ISS-2026-065`), RLS/RBAC, field masking (RLS-row-scoped AND column-privilege-scoped, see the review-round fix below), staged import (fifth-plus PLT-131/132 domain-write adapter, RPC + service layer, no UI yet), own-profile change-request flow, contracts/queries/mutations, UI (directory/detail/own-profile), `scripts/db-tests/hris-employee-master.sql` (27 assertion blocks). A subsequent same-checkpoint adversarial review round (3 lenses) found and this checkpoint fixed 2 CRITICAL, 1 MEDIUM, 3 HIGH, and 2 LOW defects (raw-table PII exposure bypassing RPC masking; unmasked PII copied into `app.audit_logs`; version-before-authority disclosure ordering on emergency-contact writes; idempotency-key replay comparing only `full_name`; missing org-unit-status enforcement; missing staged-import TS service wrapper; friendly duplicate-employee-number error; `reactivate_employee` losing `on_leave` state) -- full detail in `docs/build-log/phase-07/HRT-274.md` §11. All gates re-run and green (`typecheck`/`lint`/`test` 3473/3473/`db:test` ALL PASSED (150 files)/`next build`/`standards`/`docs`/`security:check`/`security:audit`/`threat-model:check`/`data-classification:check`). See `docs/build-log/phase-07/HRT-274.md`. Residual gaps disclosed as `ISS-2026-064` (Low) and `ISS-2026-065` (High, effective-dating). **`VERIFIED` set by the orchestrating session**: independently re-ran the full fresh gate suite (typecheck/lint/test/db:test/next build/security:audit/standards/docs/data-classification/security:check), every number matching the fix agent's own claim exactly; directly read the migration's grants section and confirmed the column-restricted `grant select (...) on app.employees/employee_emergency_contacts/employee_change_requests to authenticated` genuinely excludes every classified PII column; directly confirmed `app.employee_audit_projection(v_employee)` (not raw `to_jsonb`) is what every `capture_audit_event` call site actually passes. |
| 275 | `CG-S12-HRT-003` | Organization and position linkage | **VERIFIED** | Implementation complete: ADR-0023 Part A implemented exactly as decided (`org_units.unit_type` widened to add `team`, `enforce_org_unit_parent_shape()` widened with the leaf-type guard); `app.position_grades`/`app.positions` (HR-governed, tenant-scoped catalogue, deliberately not routed through `app.master_records`); `app.employee_position_assignments` (effective-dated, two real `btree_gist` EXCLUDE constraints for overlap, position-scoped advisory-locked capacity enforcement at approval time); `app.employees.position_id` (additive current-pointer column, `position_title` unchanged); a two-step propose (HRS:Edit)/decide (HRS:Approve) assignment workflow with zero new `app.permissions` rows; a real, non-fabricated, audited impact-preview RPC; hierarchy reads reusing `app.assert_no_employee_manager_cycle` unchanged (HRT-274's own disclosed placeholder, formalized without a materialized closure table, reasoning in `HRT-275.md` §3.7); contracts/queries/mutations, UI (position/grade catalogue, organization-linked position tree, assignment timeline + transfer/promotion/reorg wizard with impact preview), `scripts/db-tests/hris-organization-position-linkage.sql` (19 assertion blocks after the review round below). 2 real defects found and fixed during this checkpoint's own db-testing (a capacity double-count on same-position corrections; a bare-column-reference ambiguity bug in `get_employee_manager_chain`, the same class HRT-274 already found in three other functions) — full detail `docs/build-log/phase-07/HRT-275.md` §6. **A subsequent same-checkpoint adversarial review round (2 lenses) found and this checkpoint fixed 1 CRITICAL, 1 HIGH, 1 MEDIUM, and 2 LOW defects** — full detail `docs/build-log/phase-07/HRT-275.md` §11: (CRITICAL) a genuine, persistent employee-manager reporting-line cycle could be created through legitimate future-dated mutual transfers plus the disclosed maintenance sweep, since the sweep's own `app.sync_employee_current_assignment_cache` call performed no cycle validation at the actual point it committed `manager_employee_id` — fixed by re-validating the cycle guard immediately before that `UPDATE` and having the sweep skip (audit-log, never corrupt or abort on) a cyclic row; (HIGH) HRT-274's still-live free-text `app.transfer_employee`/`app.update_employee_draft` could silently desynchronize `position_title`/`manager_employee_id` from a governed `position_id` — fixed via a new additive migration (`20260730850000`, since `20260730830000` is itself `VERIFIED`/closed) adding a `governed_position_exists` guard to both; (MEDIUM) the impact-preview RPC captured no audit trail — fixed, now self-audits every call; (LOW) `app.would_create_employee_manager_cycle`'s blanket exception handler narrowed to `check_violation`; (LOW) unsaved-change `beforeunload` protection added to the position edit form and the assignment wizard. All gates re-run and green (`typecheck`/`lint` 0 errors, 185 warnings (185 vs. 180 baseline is pre-existing `no-html-link-for-pages` noise scaling with new route count, zero findings in this checkpoint's own files, independently confirmed)/`test` 3509/3509/`db:test` ALL PASSED (151 files, 185 migrations — `hris-employee-master.sql` and `org-hierarchy.sql` independently re-confirmed passing unchanged)/`next build` (7 `/hris/*` routes)/`standards`/`docs`/`security:check`/`security:audit`/`threat-model:check`/`data-classification:check`). See `docs/build-log/phase-07/HRT-275.md`. Residual gaps disclosed as `ISS-2026-066` (Low) and an update note on `ISS-2026-065` (HRT-274's own gap, unaffected by this checkpoint — see `HRT-275.md` §3.10). |
| 276 | `CG-S12-HRT-004` | Recruitment, job portal and ATS | **VERIFIED** | Implementation complete: tenant-scoped recruitment end-to-end (approved vacancy through candidate assessment, interview, and offer) plus a genuinely public, anonymous, token-based, enumeration-safe, rate-limited job-application intake surface bound only to open vacancies. 14 new tables, 55 functions. Candidates deliberately NOT routed through `app.master_records` (purpose-bound recruitment data, not an operational actor identity, `HRT-276.md` §3.1); an accepted offer reaches `offer_accepted` and creates neither `app.employees` nor `app.users` rows, proven by an explicit db-test assertion — the governed conversion is Prompt 277's own chartered scope (ADR-0023 Part B). Public posting tokens are stored/returned directly, not hashed (a disclosed divergence from the PRC-251/vendor-intake single-use-hash shape, since a hash cannot support a multi-use, publicly-listed link). Resume upload is deliberately staff-mediated; the resume-scan gate is enforced twice (attach time and stage-transition time, taxonomy C-10). Offer approval reuses PLT-123 without a bespoke threshold table, mirroring `app.decide_quotation_approval_step`/COM-153. Interview feedback is identity-gated to the assigned interviewer. Duplicate-candidate detection never auto-merges. Candidate PII is column-restricted from `authenticated` from day one (PLT-114's pattern, applied at first build). Full contracts/queries/mutations service layer, UI (6 new routes: 4 tenant-scoped, 2 public), and `scripts/db-tests/hris-recruitment-ats.sql` (15 assertion blocks). **12 real defects found and fixed during this checkpoint's own honest adversarial `psql` db-testing, before commit** (disclosed as self-found, not discovered by a later review stage, `HRT-276.md` §7): a systemic tenant-membership-fold gap across ~30+ functions (taxonomy C-05, the major fix — live-reproduced by the checkpoint's own cross-tenant-isolation test failing on first run, fixed via the established not-found fold everywhere); the public-posting-token design flaw described above; an idempotency/duplicate-conflict conflation in `apply_to_vacancy`; a redundant status guard in `create_job_offer_version`; an invalid `group by` in `get_offer_timeline`; an ambiguous bare-column reference in `get_candidate_profile` (the same recurring class HRT-274 found three times); a 3-bug chain in `search_candidate_duplicates`; two `ATW-032` automated gate failures fixed with genuine authority-check additions; several test-fixture-only bugs; a UI dead-action defect (`ApprovalStepForm` rewritten from a non-functional placeholder to real Approve/Reject buttons); an unnecessary service-role client read replaced with the RLS-scoped client; a resume-scan test design correction. **Tier B taxonomy self-check performed** (`HRT-276.md` §6): every class in `RECURRING_DEFECT_TAXONOMY.md` §4 walked; C-04 proactively hardened with defensive locks on 4 INSERT-decision RPCs; C-20 (built capability, no UI caller) found genuine and disclosed as `ISS-2026-067` rather than force-built. All gates re-run and green (`typecheck` 0 errors; `lint` 0 errors/195 warnings, pre-existing noise scaling with new routes, zero findings in this checkpoint's own files; `pnpm run test` 3542/3543 — the 1 failure is the pre-existing, pre-named, environment-state-dependent `check-worktree-collision.test.ts` false failure on a fresh branch with zero commits at test-run time, explicitly disclosed, not a product defect; `db:test` ALL PASSED, 152 files against 186 migrations; `next build` clean, 6 new routes present; `git:check-paths`/`security:check`/`data-classification:check`/`standards:check`/`docs:check`/`security:audit`/`threat-model:check` all clean). See `docs/build-log/phase-07/HRT-276.md`. Residual gaps disclosed as `ISS-2026-067` (Low, pre-existing), `ISS-2026-068` (Medium, no hiring-manager self-scoped read slice), and `ISS-2026-069` (Medium, job-offer approval shares PLT-121's single tenant-wide `config_type_code='approval'` singleton with 3 other domains). **Tier C review round (4 parallel lenses: spec-compliance; security/RLS/tenant isolation; correctness/concurrency; cross-prompt integration) found 10 CONFIRMED, live-reproduced findings (3 CRITICAL, 2 HIGH, 4 MEDIUM, 1 LOW), 0 rejected** — full detail `docs/build-log/phase-07/HRT-276.md` §12. **3 CRITICAL / 2 HIGH, fixed** via new additive migration `20260730870000` (`20260730860000` untouched): (1) raw `grant select` on `app.interview_feedback`/`app.job_offer_versions`/`app.candidate_assessments` let any zero-permission tenant member read interview notes and offer compensation directly via PostgREST, bypassing every RPC-layer authority gate — live-reproduced with a forged session, fixed with the established PLT-114/HRT-274 column-restricted-grant pattern; (2) `reject_application`/`withdraw_application` never cancelled an in-flight PLT-123 approval request, and `decide_job_offer_approval`'s terminal write carried no guard at all — live-reproduced with two real concurrent `psql` processes resurrecting a rejected application's offer all the way to `offer_accepted` — fixed by cancelling the pending approval on reject/withdraw and guarding the terminal write on `(approval_request_id, status='pending_approval')` (the fix agent's own first draft introduced a reverse-lock-order deadlock, caught and fixed in its own regression testing before shipping, re-verified deadlock-free across 6 concurrent iterations). **1 MEDIUM fixed**: `apply_to_vacancy`'s idempotency check ignored `source`, swept to 2 sibling functions with the same narrow-tuple pattern. **2 MEDIUM disclosed, not fixed** (new-capability/shared-schema-redesign, outside a bounded fix): `ISS-2026-068`, `ISS-2026-069` above. **1 LOW, documentation-only**: build log's "14 tables carry RLS" corrected to 13 (the 14th, `app.job_application_intake_attempts`, is intentionally service-role-only, mirroring PRC-251's `vendor_intake_attempts`). Fresh gate suite re-run by the fix agent, then **independently re-run a third time by the orchestrating session from scratch**: `typecheck` 0 errors; `lint` 0 errors/195 warnings (unchanged); `pnpm run test` **3543/3543** (the previously-disclosed environment-state-dependent `check-worktree-collision` failure now passes, since this branch carries real commits); `pnpm run db:test` **ALL PASSED** (153 files, 187 migrations); `next build` clean, all 6 recruitment routes + 2 public `/careers/` routes present; `git:check-paths` clean (0 forbidden); `security:check` clean. The orchestrating session also directly read the corrective migration and confirmed both the column-restricted re-`GRANT`s and the approval-race guard are genuinely present and correctly shaped, not merely claimed. See `docs/build-log/phase-07/HRT-276.md` §12. |
| 277 | `CG-S12-HRT-005` | Onboarding and offboarding | **VERIFIED** | Implementation complete: run as a single-prompt Tier-C batch (`docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.2 trigger 3 — the first Phase 7 capability to exercise the governed candidate→employee→user linkage ADR-0023 Part B reserved for this exact prompt). Tenant-scoped onboarding/offboarding case workspace: versioned checklist templates (template → version → task → task-dependency, publish-time cycle detection and empty-version rejection), a case root (`onboarding`/`offboarding`/`transfer`) with idempotent case-start, per-task assignment/completion/waive/reopen with dependency-driven blocked-state, asset/access/training/payroll/Operations-handoff task types, real access-provisioning/revocation requests, PLT-123-routed finalize approval, cancellation, and a genuinely new `app.rehire_employee` FSM extension. 9 new tables, 32 functions, in one 2,474-line additive migration (`20260730880000`). The ADR-0023 Part B conversion reuses existing Platform primitives exactly, never a second flow: `app.start_onboarding_case` calls HRT-274's own `app.create_employee_draft` (never a raw INSERT); `app.request_onboarding_access_provisioning` calls `app.invite_user`/`app.link_employee_user`/`app.assign_role` (PLT-107/110/111) directly, never force-activating an unconfirmed invited account to grant a role (deferring the grant with a disclosed evidence note instead); `app.request_onboarding_access_revocation` calls `app.transition_user_status(..., 'revoked', ...)` directly. Both bug classes HRT-276's own Tier C review found were explicitly designed against from the start: column-restricted grants applied directly in this migration's own grant block (never retrofitted); `app.cancel_onboarding_case` genuinely calls `app.cancel_approval_request` on any in-flight finalize approval before terminating, with a deliberate lock-order-safe plain-read-before-lock sequence mirroring HRT-276 §12.4's own precedent. Full contracts/queries/mutations service layer, UI (5 new routes: case list, case detail workspace, self-scoped my-tasks, checklist-template authoring/versioning/publish), and `scripts/db-tests/hris-onboarding-offboarding.sql` (19 assertion blocks, its own self-contained two-tenant fixture). **11 real defects found and fixed during this checkpoint's own honest adversarial `psql` db-testing plus its own mandatory Tier B taxonomy self-check, before commit** (disclosed as self-found, matching HRT-274's 7/HRT-275's 2/HRT-276's 12): a wrong column name in the case-start preview function; role-assignment-requires-active-status handling designed and tested explicitly; an over-broad `exit_reason` CHECK constraint that blocked ALL offboarding case creation, fixed by narrowing the constraint and adding a real `p_exit_reason` parameter; the recurring ambiguous-bare-`id`-against-`RETURNS TABLE` bug (the same class HRT-274/275/276 each found) swept across 4 functions; an `ATW-032` gate failure on `list_my_onboarding_tasks` fixed with an explicit membership check; a CHECK-constraint NULL-vs-FALSE gotcha on `handoff_category`; an `INNER JOIN`-from-tasks bug in `get_onboarding_checklist_template_version` that hid an empty draft version, fixed with a `LEFT JOIN` from the version outward; **the major, self-found taxonomy-C-01 defect** — `start_onboarding_case`'s idempotency replay originally matched on key alone, so a same-key call with a materially different request would silently return the first case; found during the checkpoint's own Tier B taxonomy walk (not a db-test failure), fixed with a new `app.assert_onboarding_case_start_idempotent_replay` helper wired into all 3 replay sites plus new regression tests; two `assert.throws`-vs-`assert.rejects` test bugs; one missing `SUPPRESS(...)` governance-metadata comment on a deliberate `@ts-expect-error` fixture. **Tier B taxonomy self-check performed**: every class in `RECURRING_DEFECT_TAXONOMY.md` §4 walked; C-01 the major self-found fix above; C-23 (a named spec requirement silently dropped) walked and resolved as two disclosed residual-gap issues rather than force-built or silently absorbed. All gates re-run fresh and green: `typecheck` 0 errors; `lint` 0 errors/205 warnings (pre-existing noise scaling with 5 new routes, zero findings in this checkpoint's own files); `pnpm run test` **3578/3578 pass, 0 fail**; `TEST_DB_NAME=cargogrid_db_test_impl277 pnpm run db:test` **ALL PASSED** (full suite including this checkpoint's own 19 assertion blocks); `next build` clean, 5 new routes present; `git:check-paths`/`security:check`/`data-classification:check` all clean. See `docs/build-log/phase-07/HRT-277.md`. Residual gaps disclosed as `ISS-2026-070` (Low — notification engine not wired, no live job-worker/overdue scheduler, `preview_onboarding_case_start`/`export_onboarding_cases` no UI caller, no `transfer`-case-type db-test scenario, no `beforeunload` guard, no E2E spec) and `ISS-2026-071` (Medium — Finance/Operations/IT task-owner completion authority uniformly tenant-wide `HRS:Edit`-gated rather than task-owner-identity-gated, paired with `ISS-2026-068`'s identical framing). **Tier C review (4 parallel lenses, live-tested) found 12 CONFIRMED, live-reproduced findings — 4 CRITICAL, 3 HIGH, 4 MEDIUM, 1 LOW, 0 rejected** — full detail `docs/build-log/phase-07/HRT-277.md` §12. Most severe: a **live-exploited privilege-escalation path** — `app.request_onboarding_access_provisioning` gated a real role grant (wrapping the previously service_role-only `app.assign_role`) behind mere `HRS:Edit`, letting any ordinary HR/onboarding staffer grant ANY published tenant role — including cross-domain Finance/Procurement/Operations Approve/Override/Delete permissions — to themselves or any other tenant member; live-reproduced twice (third-party grant and self-grant) with `evaluate_permission` flipping to `true` on protected cross-domain permissions for an attacker holding only ordinary HR:Edit. A second CRITICAL: `app.cancel_onboarding_case` never revoked Platform access already granted by a concurrent or completed provisioning task, live-reproduced with two real concurrent `psql` sessions leaving a real, active access grant behind a cancelled case. A third CRITICAL: `start_onboarding_case`'s idempotency-replay tuple comparison, though newly added this same checkpoint for taxonomy C-01, was still incomplete for the `direct_hire` path — a same-key replay with a materially different hire silently returned the first case/employee. A fourth CRITICAL: `app.request_onboarding_access_revocation`'s revocation was materially incomplete — `app.transition_user_status`'s revoke branch never touches `app.role_assignments`, and `app.evaluate_permission` never checks user/tenant-identity status, so a "revoked" actor live-reproducibly retained full permission-gated write authority (successfully created a new onboarding case after being revoked). **All 4 CRITICAL plus 5 of 8 remaining findings fixed** via a new additive migration (`20260730890000`): the privilege-escalation path now requires `HRS:Override` for any real grant AND a new `app.assert_actor_holds_role_version_permissions` delegation check (an actor may never grant a permission they do not themselves hold); cancel now revokes the case's own completed access grants; the idempotency tuple widened to the full payload across all 4 call sites; the revocation RPC now explicitly revokes every active `role_assignments` row for the target (the systemic PLT-110/112 gap beneath it filed as `ISS-2026-072`, OPEN, High, since a full fix belongs to those primitives' own owning capability, not this one). 2 findings disclosed as new-scope capabilities rather than force-fixed: `ISS-2026-073` (Medium — no "approved direct hire" precondition, despite §21/§25 naming one). **Gate suite independently re-run a third time by the orchestrating session, every number matching**: `typecheck` 0 errors; `lint` 0/205; `pnpm run test` **3579/3579**; `pnpm run db:test` **ALL PASSED** (190 migrations); `next build` clean, 5 routes present; `git:check-paths`/`security:check`/`docs:check`/`standards:check`/`data-classification:check`/`threat-model:check`/`security:audit` all clean. The orchestrating session directly read the corrective migration and confirmed the `HRS:Override` gate, the delegation check, the cancel-revokes-grants logic, and the explicit `role_assignments` revocation loop are each genuinely present and correctly shaped, not merely claimed. See `docs/build-log/phase-07/HRT-277.md` §12. |
| 278 | `CG-S12-HRT-006` | Attendance | **VERIFIED** | Implementation complete: server-authoritative idempotent clock-in/clock-out, effective-dated workday/timezone/grace/geofence policy resolution, system-detected exceptions, a direct propose/decide correction workflow, a real PLT-131/132 staged device-import pipeline, and a payroll-input-approval forward contract. See §20 below for full detail. |
| 279 | `CG-S12-HRT-007` | Shift, roster and scheduling | **VERIFIED** | Implementation complete: versioned shift template/version/segment definitions, rotating roster cycles, per-employee schedule assignment with a governed draft/publish lifecycle, coverage requirements with a real preview, a governed two-employee swap workflow, and an additive binding of HRT-278's own attendance sessions to the published schedule snapshot. See §21 below for full detail. |
| 280 | `CG-S12-HRT-008` | Leave, permit and business trip | **VERIFIED** | Implementation complete: versioned leave/permit/business-trip type/policy authoring, a real append-only balance ledger, a request/submit/decide/cancel workflow routed through PLT-123 exactly like every other Phase 6/7 flow, overlap prevention via the established `btree_gist` EXCLUDE pattern (live-raced with two real OS processes), coverage-aware approval reusing HRT-279's own schedule resolution, async accrual/carry-forward-expiry batches reusing PLT-132, and additive bindings into HRT-278 attendance-exception suppression. This closes the Implement stage of the full 3-prompt batch `HRT-278..280`. See §22 below for full detail. |
| 281 | `CG-S12-HRT-009` | Overtime and timesheet | **VERIFIED** | Implementation complete: overtime request/approval (planned and emergency-after-the-fact), actual-time reconciliation against `HRT-278`'s `app.attendance_sessions`, manual/import/attendance-derived timesheet entry, server-authoritative rounding/cap rules, a period + per-employee-summary approve/lock/reopen workflow, and ONE versioned, content-based-idempotent payroll-input handoff record. See §24 below for full detail, including the Tier C batch review-round fix pass. |
| 282 | `CG-S12-HRT-010` | Payroll foundation, benefit and reimbursement | **VERIFIED** | Implementation complete: payroll calendar/period, versioned component/formula authoring with the RPD-016 statutory-activation gate, employee component assignment, a frozen per-(period, employee) input snapshot resolving `ISS-2026-074` (HRT-278 `app.attendance_sessions` vs. HRT-281 `app.payroll_time_inputs`) with an explicit per-work-date precedence, a maker-checker-governed run/batch lifecycle (PLT-123), private payslips, governed reimbursement/loan lifecycles, and a Payroll-owned Finance handoff mirroring `app.prepare_finance_vendor_bill_from_actual_cost` exactly. Run as its OWN single-prompt Tier C batch (never bundled), 4 parallel adversarial review lenses found 13 CONFIRMED findings (1 Critical — an audit-log compensation/reason leak, the third recurrence of this shape after HRT-280/281, now recorded as recurring-defect-taxonomy class `C-24`), all closed at root cause via one new additive migration (`20260731020000`) or explicitly disclosed (`docs/runtime/KNOWN_ISSUES.md` `ISS-2026-077`..`082`) where genuinely architectural/out-of-scope. Full detail: `docs/build-log/phase-07/HRT-282.md` §13. |
| 283 | `CG-S12-HRT-011` | KPI and performance | **VERIFIED** (Tier C batch review closed 2026-08-14, combined `283-285` batch) | Implementation complete: a versioned KPI library (target_ratio/milestone_percent/qualitative_scale scoring methods, exactly one active version per KPI), a reusable weighted-goal-slot template (publish blocked until default weights sum exactly to the required total), a performance cycle with an explicit ordered stage machine, weighted goal assignment (exact-100 validated, not-applicable rebalancing, frozen once self-assessment is submitted), self/manager/reviewer (360) assessment stages with purpose- and stage-bound visibility (an employee cannot see their own manager assessment until submitted; reviewer input never directly visible to the employee), an explainable exact-decimal scoring formula deriving the computed score ONLY from the submitted manager assessment (live-proven with deliberately divergent self vs. manager scores), governed calibration and appeal decision (both `HRS:Override`/`HRS:Approve` + mandatory reason + a structural self-decision block, live-proven), a manager/reviewer reassignment mechanism that never silently transfers an already-submitted review (live-proven with a full before/after comparison), and a genuine k-anonymity floor (k=5) on the one aggregate report, enforced inside the reporting function itself (live-proven with a real 2-person suppressed cohort alongside a synthetic 5-person unsuppressed one). Zero write anywhere in this migration to `app.employees.lifecycle_status`, any `app.payroll_*` table, or any role/permission table (grep- and structurally-proven). Reuses the already-legal `HRS:View personal data` action rather than seeding a new permission row (`app.permissions.action`'s CHECK constraint is fixed repository-wide — confirmed live). Taxonomy C-24 (this phase's third-in-a-row recurring defect, HRT-280→281→282) applied from the start: every `capture_audit_event` call passes a null reason and a non-sensitive `after_value` allowlist. Three real, live-reproduced two-OS-process concurrency races each resolved cleanly with one winner and a classified error for the loser. Self-found and fixed before commit: a missing `revoke execute on all functions in schema app from public` statement, and a dead-code EXISTS pre-check in the appeal-submission function. Disclosed, not fixed: `PAYROLL_REGISTRY` (HRT-282's own data-classification export) was found never wired into the combined `REGISTRY`, out of this checkpoint's own scope. **Tier C batch review (4 parallel lenses, live-tested against a real disposable Postgres 16 database)** found this prompt's own portion of the combined batch findings: a CRITICAL ambiguous-bare-`id` bug in 5 read RPCs (unconditional runtime error on every call), a MEDIUM RLS-policy defect on 2 tables (`evaluate_permission` called directly instead of through the established `SECURITY DEFINER` wrapper, fail-closed, no data leaked), a MEDIUM reviewer-reassignment UI gap (RPC real and tested, no form), and confirmed the cross-prompt integration lens's finding that this checkpoint's own 3 ATW-032-flagged functions are a false positive of the mechanical sweep (genuine self-scope equality checks the sweep doesn't credit) — all fixed (2 via a new additive migration `20260731050000`, 1 via a UI wire-up, 1 via a test-gate allowlist correction), zero rejected. Full detail: `docs/build-log/phase-07/HRT-283.md` §13, batch-wide record `docs/build-log/phase-07/HRT-285.md` §12. |
| 284 | `CG-S12-HRT-012` | Training and talent | **VERIFIED** (Tier C batch review closed 2026-08-14, combined `283-285` batch) | Implementation complete, built directly on Prompt 283's committed `20260731030000_create_hris_kpi_performance.sql` (read, never edited): a competency/skill catalogue (single status-lifecycle row, decision 1 — no two-tier version needed here unlike KPI/payroll), a two-tier versioned course catalogue (exactly one published version at a time, mirrors HRT-283's KPI-definition/version and HRT-282's payroll-component/version exactly), internal/external providers, direct course prerequisites enforced inside the enrollment RPC, scheduled sessions with a real lock-protected capacity/waitlist mechanism (live-raced with two real OS processes: fill-to-capacity, third enrollee waitlisted, cancellation promotes the earliest waitlisted enrollment), enrollment (self-service, assigned, and an idempotent bulk mandatory-compliance batch) with an approval gate, structural self-decision block, cancellation-with-waitlist-promotion, and reschedule (two-session lock ordered by ascending id, taxonomy C-21), attendance/completion recording, multi-attempt assessment with a deterministic computed pass/fail, certificates (issue/import/verify/revoke/renew) reusing the established PLT-128 private malware-scanned file-evidence gate exactly (live-proven: infected/pending-scan/wrong-scope evidence all rejected, only a genuinely clean scan accepted) plus real durable-job-based expiry/reminder batches reusing PLT-131/132 unchanged (widening `app.jobs.job_type`/`app.generic_job_types()` a fifth time, live-proven idempotent on re-run), development plans optionally cross-referencing a specific HRT-283 performance outcome (manager-scoped, live-proven rejecting a cross-employee link), and a RESTRICTED talent workspace — review cycle/assignment/review (assigned-reviewer-only visibility, non-mutating reassignment), talent pools, and succession candidates — every talent-domain write gated on `HRS:Override` with mandatory reason and a structural self-decision block, plus a genuine k-anonymity floor (k=5) on the one aggregate report, enforced inside the reporting function itself (live-proven with a real 2-person suppressed cohort alongside a synthetic 5-person unsuppressed one). Zero write anywhere in this migration to `app.employees.lifecycle_status`, any `app.payroll_*` table, any `app.performance_*` table, or any role/permission table (grep- and structurally-verified). Self-found and fixed before commit: 4 RLS policies calling `app.evaluate_permission` directly (service_role-only) — mirroring an identical pre-existing, disclosed-not-fixed bug in two of HRT-283's own already-committed RLS policies — fixed here with an `app.check_training_authority` `SECURITY DEFINER` wrapper; a C-12/ATW-032-class gap in `app.enroll_self_in_training_session` (missing `has_active_tenant_membership`); a genuinely missing `app.list_talent_review_cycles` RPC (zero-assignment cycles were structurally invisible); 4 mutation functions defined but never wired to any UI form (taxonomy C-20), found via a programmatic cross-check and wired in. Three real, live-reproduced two-OS-process concurrency races (session capacity/waitlist, bulk mandatory-assignment idempotency, certificate-expiry-batch double-claim), each resolved with exactly one winner and zero corruption. **Tier C batch review** found this prompt's own portion: a CRITICAL ambiguous-bare-`id` bug in 2 read RPCs, a HIGH `app.enqueue_job` check-then-insert race (raw `23505` on genuine concurrent replay, live-reproduced with two real OS processes, fixed at the shared primitive), a HIGH certificate-evidence-attach UI gap (the malware-scan/PLT-128 RPC was real and tested but had no UI caller, while the section's own copy claimed it did — now wired end-to-end), and a LOW disclosure-consistency gap (provider evidence was never built despite §16 naming both nouns — UI copy corrected, `ISS-2026-083` registered) — all fixed or disclosed, zero rejected. Full detail: `docs/build-log/phase-07/HRT-284.md` §13, batch-wide record `docs/build-log/phase-07/HRT-285.md` §12. |
| 285 | `CG-S12-HRT-013` | ESS and MSS | **VERIFIED** (Tier C batch review closed 2026-08-14, combined `283-285` batch — this is the last of the batch) | Implementation complete, zero new migration/RPC — a pure composition layer over HRT-274/278-284's own already-`VERIFIED`/`COMPLETED` query and mutation RPCs. A responsive ESS home (`hris/my`, six bounded-count cards linking to each owning capability's own detail page) and an MSS team workspace (`hris/team`: roster, a unified approvals queue routed through one composed dispatcher across leave/overtime/timesheet/training, team schedule, team performance summary, team training status). Own/team scope derived exclusively via `app.list_my_team_employees` (HRT-274's own established resolution, reused verbatim). Live-proven (real disposable Postgres, forged-session `psql` probes): a defensive team-id re-filter in the TypeScript composition layer is genuinely necessary — an `HRS:View personal data` holder (a realistic dual-role manager+HR case) receives ALL tenant employees' performance outcomes from the owning RPC's own `employee_id=null` branch, not just real direct reports, so this checkpoint's own second filter is what prevents a dual-role manager from seeing every colleague's rating in MSS. Also live-proven: attendance-correction/schedule-swap decisions are genuinely `HRS:View`-only (no manager-of branch) and deliberately excluded from the MSS queue rather than composed into dead UI; talent-review/pool/succession reads are entirely absent from MSS and independently HARD-REJECTED at the RPC layer for a plain manager; payroll is entirely absent from the MSS team workspace. The composed `decideManagerApprovalQueueItem` mutation is a pure router with zero authority check of its own — every decision's real authority/self-block/idempotency/audit is the owning capability's own unchanged RPC. Self-found and fixed before commit (caught by `tsc`/unit tests, none shipped): two status-literal mistakes and one incorrect contract type name. **Tier C batch review** found this prompt's own portion: a MEDIUM/HIGH silent-truncation gap (the MSS team roster (50) and per-category approval queue (20) bounds were genuine but had no caller-visible signal when hit — fixed with a visible disclosure banner; a full cursor-based "load more" UI remains a disclosed residual, `ISS-2026-084`) and a LOW hardcoded-`0` placeholder (`pendingLeaveRequestCount` — now computed for real via `list_leave_requests` self-scoped to the caller's own server-resolved employee id) — both fixed, zero rejected. This is the batch's own closing build log: full cross-batch disposition, propagation sweep, and fresh gate suite for all three prompts together live in `docs/build-log/phase-07/HRT-285.md` §12. |
| 286 | `CG-S12-HRT-014` | Internal and interdepartmental ticket | **VERIFIED** (standalone single-prompt Tier C batch closed 2026-08-14) | Implementation complete and independently Tier-C-verified — the FIRST capability of the separate Ticket workstream (`HRT-286..292`), the foundation every subsequent ticket prompt extends. One canonical ticket/conversation model (`app.tickets`/`app.ticket_messages`/`app.ticket_queues`/`app.ticket_categories`/`app.ticket_queue_members`/`app.ticket_watchers`/`app.ticket_events`/`app.ticket_status_transitions`) activating ONLY the `internal` channel this prompt; requester/assignee/queue-staff/watcher identity resolves through the established `app.employees`/`app.users` link, correcting the stale plan §6/§9 above described (internal ticket identity now genuinely depends on `HRT-274`, not "Platform Layer-3 user identity" independent of the HR track — see `ISS-2026-085` below for why this matters for 287/288). **Tier C review (4 parallel lenses, live-tested against a real disposable Postgres 16, including two genuinely concurrent OS `psql` process races)** found this prompt's own findings — 0 Critical, 2 HIGH (both the same underlying gap, independently reported by the spec-compliance and cross-prompt-integration lenses), 3 MEDIUM/LOW-MEDIUM fixed live, 3 LOW/MEDIUM disclosed, 0 rejected. **Both HIGH findings disclosed, not fixed** (a genuine architectural decision outside this batch's mandate): the migration's own "channel extensibility is a pure additive CHECK-widen, never a redesign" claim does not hold for Prompt 287 (customer)/288 (helpdesk) — `requester_employee_id` is NOT NULL/FK'd exclusively to `app.employees`, the shared creation engine has no `p_channel` parameter, and 3 read RPCs `INNER JOIN app.employees` on the requester; representing a genuine non-employee requester/assignee requires real expand-and-contract schema work (relax the NOT NULL constraint, add nullable discriminator columns, convert those INNER JOINs to LEFT JOINs, add new authority-helper branches) — registered `ISS-2026-085` (High), corrected overclaiming migration-object comments. **3 findings CONFIRMED live and fixed** via a new additive migration (`20260731070000`, `20260731060000` itself left untouched): `app.transition_ticket_status` checked business-rule/authority logic before `record_version`, the sole outlier among sibling versioned-write RPCs — independently live-reproduced with two genuinely concurrent OS `psql` processes (the race loser got `invalid_transition` instead of `stale_version`), fixed by reordering, re-verified live; `ticket_messages.redacted_reason` and `ticket_queue_members.removed_reason` (both staff-only rationale text) were readable via raw `select` by an audience broader than intended — live-reproduced both ways, fixed with column-level `revoke`+`grant select(...)` carve-outs mirroring `app.users`' established email-column pattern, re-verified live post-fix. **Disclosed, not fixed**: `TKT:Edit` grants blanket tenant-wide ticket-content staff status, a deliberate design tension with the migration's own "configuration-only" framing — investigated, found load-bearing elsewhere in the same migration (`redact_ticket_message`, the db-test fixture), not a bounded fix (`ISS-2026-086`, Medium); attachment-upload UI and browser/E2E tests, both already disclosed in the original build log, matching repository-wide precedent (`ISS-2026-087`, Low). Fresh gate suite independently re-run from scratch, every number matching: `typecheck` 0 errors; `lint` 0/210 (unchanged baseline); `pnpm run test` 3942/3942 (unchanged, SQL-only fix); `next build` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check-paths`/`git:check` all clean; `db:test` full harness aborted at the same pre-existing, time-of-day-confirmed `ISS-2026-059` before reaching this checkpoint's file, so the mandated standalone fallback (`ticketing-internal.sql`, 15/15, run against a fresh database with all 207 migrations) is the authoritative evidence, confirming zero regression. Full disposition table, propagation sweep, and gate numbers: `docs/build-log/phase-07/HRT-286.md` §12. |
| 287 | `CG-S12-HRT-015` | Customer-to-tenant ticket | **VERIFIED** (standalone single-prompt Tier C batch closed 2026-08-14) | Implementation complete and independently Tier-C-verified — the SECOND capability of the Ticket workstream, extending HRT-286's canonical ticket model with a genuinely new, non-employee Layer 4 `customer_user` principal. This checkpoint's own chartered scope was resolving `ISS-2026-085` (registered at row 286 above) for the customer/Prompt-287 half — done via one additive migration (`20260731080000`): `app.tickets.requester_employee_id` relaxed to nullable, a new nullable `requester_customer_account_id` (FK to `app.accounts`) added, a structural `tickets_requester_identity_shape` CHECK, `channel` CHECK widened to `('internal','customer')`, `app._create_ticket` given a real `p_channel` parameter, and the 3 `INNER JOIN app.employees` reads converted to `LEFT JOIN`. Identity resolution generalized via one new `app._is_ticket_requester_party` helper (dispatches on channel), reused by `can_access_ticket`/`reply_to_ticket`/`add_ticket_watcher`/`remove_ticket_watcher`/`_ticket_transition_authority` — never a fourth, ticket-specific access shape, provably byte-for-byte unchanged for the `internal` channel. Six new customer-safe RPCs with their own deliberately narrower projections (no queue/assignee identity, internal notes structurally unreachable); raw-table RLS on all 4 ticket tables now excludes `customer_user`-layer entirely. **Tier C review (4 parallel lenses, live-tested against a real disposable Postgres 16, with a chartered focus on independently re-verifying `ISS-2026-085`'s own closure)** found this prompt's own findings — 0 Critical, 2 HIGH, 1 LOW, 0 rejected. **Both HIGH findings CONFIRMED live and FIXED** via a new additive migration (`20260731090000`, `20260731080000` itself left untouched): `app.get_customer_ticket` returned `resolution_summary`/`cancelled_reason`/`last_reopen_reason` verbatim to the customer regardless of staff- vs. customer-authorship — independently live-reproduced twice (a deliberately internal-sounding staff rationale leaking on resolve and on reopen) — fixed by tracking per-transition authorship and only echoing customer-authored text back (`resolution_summary`, always staff-authored by construction, is now never exposed at all — disclosed residual `ISS-2026-088`, Low); `app._create_ticket`'s customer-channel idempotency match/index omitted `requester_customer_account_id`, so a customer_user actor scoped to multiple accounts reusing one idempotency key across two different-account requests got silently short-circuited to the first account's ticket — independently live-reproduced (both sequentially and via genuine multi-request reuse), fixed by adding the account id to both the index and the match logic, re-verified live with no regression to genuine same-account replay collapse. One LOW finding (a file-count mismatch between this checkpoint's own `TASK_LEDGER.md` row and the actual commit) fixed by correcting the count. The integration lens's chartered re-verification of `ISS-2026-085`'s own 7 verification questions found **no residual gap** — the customer/Prompt-287 half is genuinely, fully resolved, independently re-derived live rather than accepted from the original resolution note. Fresh gate suite independently re-run from scratch, every number matching: `typecheck` 0 errors; `lint` 0/215 (pre-existing baseline drift, zero new warnings from any changed file); `pnpm run test` 3964/3964 (unchanged, SQL-only fix); `next build` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check-paths`/`git:check` all clean; `db:test` full harness **ALL PASSED end-to-end** (run past the `ISS-2026-059` time-of-day failure band, reaching and passing all 163 files), additionally re-confirmed standalone (`ticketing-internal.sql` 15/15, `ticketing-customer.sql` 19/19). **`ISS-2026-085` final disposition: `RESOLVED` for the customer/Prompt-287 half** (independently re-confirmed by this batch); the Prompt 288/helpdesk half remains genuinely `OPEN`. Full disposition table, propagation sweep, and gate numbers: `docs/build-log/phase-07/HRT-287.md` §14. |
| 288 | `CG-S12-HRT-016` | Tenant-to-CargoGrid helpdesk | **VERIFIED** (standalone single-prompt Tier C batch closed 2026-08-14) | Implementation complete and independently Tier-C-verified — the THIRD and LAST founding channel of the Ticket workstream, extending HRT-286's canonical ticket model with the tenant itself as requester (never a specific employee/account row) and CargoGrid Platform support (Supreme Admin) as staff. This checkpoint's own chartered scope was resolving the SECOND, previously-open half of `ISS-2026-085` (registered at row 286, updated at row 287) — done via one additive migration (`20260731100000`): `channel` CHECK widened to `('internal','customer','helpdesk')`; `tickets_requester_identity_shape` widened to a third branch; `queue_id` relaxed to nullable with a new `tickets_queue_shape` CHECK and a genuinely parallel, Platform-wide `app.support_queues` catalog (no `tenant_id`/`org_unit_id` — HRT-286's own diagnosed gap, resolved as named). Identity resolution generalized a third time via `app._is_tenant_helpdesk_authorized` (direct `app.principal_memberships`/`app.role_assignments` queries, deliberately employee-independent and deliberately NOT routed through `app.check_ticket_authority`/`app.evaluate_permission`, whose RPD-022 Supreme-Admin bypass would otherwise incorrectly admit a Platform Supreme Admin as "the tenant side" — found and fixed during design). A support-access-grant CORRELATION field (`app.link_helpdesk_support_grant`) is DISPLAY/AUDIT ONLY, never itself granting access — the existing, unmodified PLT-115 flow remains the sole path to real tenant business-data access. **Tier C review (4 parallel lenses, live-tested against a real disposable Postgres 16, with a specific mandate for heavy PLT-115-correlation scrutiny and to verify `ISS-2026-085`'s FULL closure)** found this prompt's own findings — 0 Critical, 1 HIGH, 2 MEDIUM, 1 LOW, 0 rejected. Per the batch's own top-priority instruction, "a ticket alone grants no tenant business-data access" and "an expired/revoked support grant confers nothing" were independently re-derived FIRST (active/expired/revoked grants all directly probed against `is_ticket_staff`/`can_access_ticket`/raw-table RLS) — **both confirmed to hold, no fix needed**. **1 HIGH + 2 MEDIUM CONFIRMED live and fixed** via a new additive migration (`20260731110000`, `20260731100000` itself left untouched): `app.reply_to_ticket`'s idempotency match omitted the posting actor, silently collapsing two different participants' distinct replies into one row (shared root cause across all three ticket channels, fixed once, at the single shared function); `app.add_ticket_watcher`/`app.remove_ticket_watcher` were missing the explicit helpdesk-channel rejection every sibling staff-lifecycle RPC already had, letting a tenant admin insert a real employee-participant watcher row on a channel with no employee participants by design; `app.ticket_channel_of`, an RLS-support helper, was an unscoped, directly-callable cross-tenant ticket-existence-and-channel oracle for any authenticated identity — all three re-verified live post-fix (broken-before, correct-after). **1 LOW fixed**: `app.list_platform_helpdesk_tickets` had no `p_product_area` filter/index despite the prompt's own §17 naming it. Fresh gate suite independently re-run from scratch, every number matching: `typecheck` 0 errors; `lint` 0/265 (unchanged baseline); `pnpm run test` 3992/3992 (unchanged, one additive optional TS field); `next build` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check-paths`/`git:check` all clean; `db:test` full harness **ALL PASSED end-to-end** (all 211 migrations, all 375 test files, no time-of-day fallback needed), with `ticketing-internal.sql` (15/15), `ticketing-customer.sql` (19/19), and `ticketing-helpdesk.sql` (16/16) all additionally re-confirmed standalone, byte-identical to their pre-fix baselines. **`ISS-2026-085` final disposition: `RESOLVED` IN FULL** — both the customer/Prompt-287 half and this helpdesk/Prompt-288 half are closed, and BOTH have now been independently Tier-C-adversarially-reviewed with genuine findings surfaced, fixed, and re-verified live. Full disposition table, propagation sweep, and gate numbers: `docs/build-log/phase-07/HRT-288.md` §13. |
| 289 | `CG-S12-HRT-017` | SLA and knowledge base | **VERIFIED** (combined `289-290` Tier C batch closed 2026-08-14) | Implementation complete and independently Tier-C-verified — the FOURTH capability of the Ticket workstream, bundling an SLA half (deterministic policy/calendar/clock engine spanning all three channels) and a Knowledge Base half (a wholly standalone article draft/review/publish/audience/search/link capability), both additive-only against HRT-286/287/288's three already-`VERIFIED` migrations. **Combined Tier C review (4 parallel lenses, live-tested against a real disposable Postgres 16, run together with Prompt 290 per `BUILD_EXECUTION_PROTOCOL.md` §3.4/§3.2 trigger 6)** found, in this prompt's own code: 1 CRITICAL, 1 HIGH (test-coverage gap), 0 rejected. **CRITICAL CONFIRMED live and FIXED** via a new additive migration (`20260731150000`, this prompt's own `20260731120000`/`20260731130000` left untouched): the RLS SELECT policy on `app.ticket_sla_clocks` omitted the `not app.actor_holds_customer_user_layer(tenant_id)` narrowing every sibling table in this same migration correctly carries — independently live-reproduced (a genuine customer-channel ticket + customer-scoped SLA policy + forged customer-account-member session read the full raw clock row, every internal field decision 10's own written guarantee says is withheld from a requester) and fixed by adding the identical narrowing, re-verified live (zero rows post-fix, legitimate customer-safe RPC unaffected). **HIGH disclosed, not test-file-edited** (`ISS-2026-090`): `scripts/db-tests/ticketing-sla.sql` never constructs a customer- or helpdesk-channel ticket, the exact coverage gap that let the CRITICAL finding ship undetected by the official harness — the concrete live exposure this enabled is independently fixed and re-verified using the scenario the shipped test never built; editing an already-committed db-test file is outside this batch's own established fix-scope convention. Fresh gate suite independently re-run from scratch, every number matching: `typecheck` 0 errors; `lint` 0/271 (unchanged baseline); `pnpm run test` 4059/4059 (unchanged, SQL-only fix); `next build` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check-paths`/`git:check` all clean; `db:test` full harness **`ALL PASSED`, exit 0** (215 migrations, 166 test files, including all six ticketing files with their two live concurrency races). Full disposition table, propagation sweep, and gate numbers: `docs/build-log/phase-07/HRT-289.md` §14 and `docs/build-log/phase-07/HRT-290.md` §13 (shared batch record). |
| 290 | `CG-S12-HRT-018` | Ticket assignment | **VERIFIED** (combined `289-290` Tier C batch closed 2026-08-14) | Implementation complete and independently Tier-C-verified — the FIFTH and FINAL capability of the Ticket workstream's founding build arc: explainable versioned routing rules, an assignment event ledger, atomic idempotent claim/assign/reassign under real concurrency, a workload snapshot, and effective-eligibility binding, bounded to `internal`/`customer` channels per HRT-288's own established "helpdesk has no non-Supreme-Admin eligibility model" shape. **Combined Tier C review (4 parallel lenses, live-tested, with an explicit mandate to assess whether the full `286-290` arc composes soundly)** found, in this prompt's own code: 1 HIGH (plus one propagation instance of the same root cause), 1 MEDIUM, 1 MODERATE/LOW disclosure, 0 rejected. **HIGH CONFIRMED live and FIXED** via the same new additive migration (`20260731150000`, this prompt's own `20260731140000` left untouched): the workload cap on `app.claim_ticket` (hard, no override) and `app.assign_ticket` (override-only-with-explicit-flag) read a live, UNLOCKED `COUNT(*)` over the employee's other tickets — independently live-reproduced with two genuine, concurrent OS `psql` processes claiming two different tickets under a cap of 1: BOTH succeeded, leaving the employee at 2 active assignments, zero error — fixed by serializing on a `pg_advisory_xact_lock(tenant_id, queue_id)` before any workload-cap-relevant read in both functions; the SAME root cause was proactively found and fixed in `app.auto_route_ticket`'s own least-loaded candidate scan during the mandatory propagation sweep (§5.4), not merely flagged. Re-verified live: the identical two-process race now produces exactly one winner and one clean, discriminated `workload_limit_exceeded` for the loser. **MEDIUM CONFIRMED live and FIXED**, same migration: `app.list_ticket_assignment_events` was missing the explicit helpdesk-channel rejection every sibling assignment RPC in this migration already carries — a recurrence of the exact defect class HRT-288's own Tier C review already found and fixed once (for `add_ticket_watcher`/`remove_ticket_watcher`); structurally inert today (the ledger can never hold a helpdesk row) but fixed regardless, matching every sibling function. **MODERATE/LOW disclosed, not code-fixed** (`ISS-2026-089`): Prompt 290's own "queue, team and user assignment" wording names a "team" dimension nowhere built and never named in HRT-290's own §9 C-23 disclosure walk (queue-based grouping is the correct, working team-analog every other Phase-7 capability already uses); routing precedence does not yet consume a live SLA-clock signal, also undisclosed — both corrected in the build log as named decisions, neither a functional defect. Fresh gate suite independently re-run from scratch, every number matching: `typecheck` 0 errors; `lint` 0/271 (unchanged baseline); `pnpm run test` 4059/4059 (unchanged, SQL-only fix); `next build` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check-paths`/`git:check` all clean; `db:test` full harness **`ALL PASSED`, exit 0** (215 migrations, 166 test files), with all six ticketing db-test files additionally re-confirmed standalone against a separately-built fresh database (including the post-fix live re-race of the workload-cap concurrency scenario). **Milestone assessment**: the complete five-prompt Ticket workstream (`HRT-286` through `HRT-290`) composes into one sound, coherent canonical ticket capability — one shared ticket/message model, one generalized access predicate extended across three principal shapes, one SLA-precedence-engine shape reused verbatim by routing, one consistent RLS-narrowing discipline with exactly one lapse found and fixed, zero regression across all six ticketing db-test files run together. Full disposition table, propagation sweep, gate numbers, and full milestone reasoning: `docs/build-log/phase-07/HRT-290.md` §13. **This closes the FINAL batch of the "lanjut prompt 281-290" operator authorization.** |
| 291 | `CG-S12-HRT-019` | Ticket escalation | **VERIFIED** (Batch 1 `291-293` Tier C batch closed 2026-08-15) | Implementation complete and independently Tier-C-verified — the SIXTH capability of the Ticket workstream, first task of the "lanjut 291-300" operator authorization. Versioned functional/hierarchical/SLA-driven escalation: policy/version/level authoring (mirrors HRT-289's own SLA-policy shape), a deterministic `rank()`-based precedence engine, a real batch evaluator (PLT-132) consuming HRT-289's own `ticket_sla_clock_events`, manual escalate/acknowledge/suppress (authority + mandatory reason + expiry-gated)/resolve, an append-only ledger (own table, never widening the shared `ticket_events` CHECK), the first real domain consumer of PLT-127's notification engine (a deduplicated `ticket_escalated` type), a customer-safe single-boolean projection, and a dedicated breach/stuck-ticket queue. Bounded to internal/customer channels, mirroring HRT-288/290's own "no non-Supreme-Admin helpdesk model" precedent. Reassignment composes HRT-290's own `app._apply_ticket_assignment` directly. 4 self-found defects fixed before commit (a missing exception handler on the escalation-events idempotency insert, a `job_type` widening regression that silently dropped `kb_article_expiry`, a trigger referencing a nonexistent `record_version` column, an overly strict suppression-expiry CHECK). Gate suite fresh after Implement: `typecheck` 0 errors; `lint` 0/271; `pnpm run test` 4083/4083 (1 pre-existing, disclosed `checkWorktreeCollision` failure unrelated to code); `db:test` ALL PASSED (216 migrations); `next build` clean (2 new routes). See §26 below for full detail, including the batch's own combined Tier C review. |
| 292 | `CG-S12-HRT-020` | Typed ticket-linked records | **VERIFIED** (Batch 1 `291-293` Tier C batch closed 2026-08-15) | Implementation complete and independently Tier-C-verified — the SEVENTH capability of the Ticket workstream, second of Batch 1. Validated typed ticket links to shipment/invoice/warehouse/vendor/customer/user records via a six-value CHECK-constrained registry (never a bare polymorphic pair); one shared per-type adapter (`app._ticket_link_resolve_candidate`) composes each domain's OWN existing access predicate verbatim — a link never grants access, every read independently re-authorizes, closing the existence-enumeration channel the spec forbids. A new `app._ticket_link_actor_may_view_tenant_data` gate closes a genuine C-08 widening a Supreme Admin would otherwise gain via a helpdesk ticket. Uniform bounded `{label, detail, status}` safe-snapshot, captured once for history only — every live read re-fetches fresh. 4 self-found defects fixed before commit, the most severe a genuine C-05 ordering bug (an existing-link short-circuit ran BEFORE re-validating the current caller's own eligibility — fixed by always re-proving eligibility first, even on idempotent replay) plus two self-found repository-wide `rbac-enforcement.sql` sweep failures (`record_ticket_link_access_denial` had no tenant-authority/claimed-actor check, fixed mirroring ATW-242's established pattern). Gate suite fresh after Implement: `typecheck` 0 errors; `lint` 0/271; `pnpm run test` 4105/4105 (+22 new; the HRT-291-disclosed `checkWorktreeCollision` failure no longer reproduces); `db:test` ALL PASSED (217 migrations, including the repository's own `rbac-enforcement.sql` sweep); `next build` clean. See §26 below for full detail, including the batch's own combined Tier C review. |
| 293 | `CG-S12-HRT-021` | Sensitive personal and payroll data controls | **VERIFIED** (Batch 1 `291-293` Tier C batch closed 2026-08-15) | Implementation complete and independently Tier-C-verified — the convergence hardening checkpoint over all 19 already-`VERIFIED` HR (274-285) and Ticket (286-292) Phase 7 capabilities; third and last of Batch 1, closing the FULL "lanjut 291-300" Batch 1 range. A corrective/hardening pass (no new capability), fixing 2 CRITICAL + 2 HIGH audit findings plus 8 self-found sibling instances at root cause via 3 additive migrations. **Finding A (CRITICAL)**: `app.employees`' 5 HR-narrative lifecycle-reason columns (revision/suspend/terminate/archive/leave) were returned unconditionally by `get_employee_profile` — never wrapped in the masking guard protecting national_id/DOB two lines above — plus exposed via an unrestricted raw-table grant and an unrestricted grant on `app.employee_lifecycle_events`; any plain tenant member or plain `HRS:View` holder could read why a named colleague was suspended/terminated/on leave. Fixed in `20260731180000`; a self-found sibling sweep (`20260731200000`) closed the identical shape on 5 further tables plus one UI `select("*")`. **Finding B (CRITICAL)**: the previously-claimed-closed C-24 unmasked-audit-log-reason leak (readable by any plain `tenant_admin` via `query_audit_logs`) was live across most of Phase 7 — 37 `capture_audit_event` call sites fixed across Employee Master/Leave/Recruitment-ATS/Onboarding/Attendance/Shift-Roster (`20260731190000`), including a distinct raw-score vector in `record_assessment_result` the capability's own original Tier C review missed. **2 HIGH fixed**: 5 new data-classification registry entries; the FIN-only adoption gate (`check-registry.ts`) extended with an HRS mirror, plus a self-found gap where `PAYROLL_REGISTRY` was never wired into the combined `REGISTRY` constant. **3 Medium disclosed, not fixed** (`ISS-2026-091`/`092`/`093`), each genuinely outside this checkpoint's bounded migration-plus-registry-plus-docs scope. New `docs/standards/HR_PAYROLL_FIELD_POLICY_MATRIX.md` published, mirroring `FINANCE_FIELD_POLICY_MATRIX.md`'s structure. Gate suite fresh after Implement: `typecheck` 0 errors; `lint` 0/271; `pnpm run test` 4112/4112; `db:test` 167/168 files pass, the sole failure independently proven pre-existing (`ISS-2026-077`, a day-of-week-dependent fixture bug in HRT-280's own file); `next build` clean. See §26 below for full detail, including the batch's own combined Tier C review (4 lenses, 9 CONFIRMED findings, 0 rejected, closing this batch). |
| 294 | `CG-S12-HRT-022` | Integrated verification | **READY** | Upstream: `HRT-274..293` all `VERIFIED` (union of both the HR and Ticket tracks, confirmed at this checkpoint, §26). Dependency-clean and next-eligible — genuinely standalone, never batched with implementation work, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.3. Already within the still-open "lanjut 291-300" operator authorization (7 of 10 prompts remain: 294-297 finish Phase 7, 298-300 start Phase 8) — no fresh, separate operator authorization is required before it may begin. |
| 295 | `CG-S12-HRT-023` | Privacy/integrity hardening | **BLOCKED** | Upstream: `HRT-294` `VERIFIED` with finding register. |
| 296 | `CG-S12-HRT-024` | Documentation and handoff | **BLOCKED** | Upstream: `HRT-295` `VERIFIED`. |
| 297 | `CG-S12-HRT-025` | Closure verification | **BLOCKED** | Upstream: `HRT-296` `VERIFIED`. Only this prompt may set `PHASE_7_VERIFIED`. |

**Summary (superseded by §23–§26 below): 21 VERIFIED (273–293), 1 READY (294), 3 BLOCKED (295–297).**

---

## 11. Evidence/log path

Every future Phase 7 task's own runtime log, atomic-task record, db-test file, and closure report is written under `docs/build-log/phase-07/`, mirroring the `docs/build-log/phase-0{0..6}/` convention every prior phase used. This checkpoint's own two writes:

- `docs/adr/ADR-0023-phase7-hris-organization-team-and-employee-identity-reconciliation.md`
- `docs/build-log/phase-07/00_EXECUTION_INDEX.md` (this file)

---

## 12. Rollback

Phase 7 is a fresh phase with zero prior state — there is nothing pre-existing to roll back to except these two new files. Rollback of this checkpoint specifically means: delete `docs/build-log/phase-07/00_EXECUTION_INDEX.md` and `docs/adr/ADR-0023-*.md`, and revert `PHASE_7_IN_PROGRESS` to `PHASE_7_NOT_STARTED`. No schema, service, UI, or test file was touched (per this kickoff's own binding constraint — planning-only), so no migration rollback is needed or possible at this checkpoint.

---

## 13. Runtime state and resume

`PHASE_7_IN_PROGRESS` is **set** by this checkpoint. `PHASE_7_VERIFIED` is **not** set and must not be set by any task before Prompt 297.

**Resume instruction:** the next runtime checkpoint should pick up `CG-S12-HRT-002` (Prompt 274, Employee Master) as the first eligible task — it is the only row in §10 marked `READY`. Before releasing `HRT-286` concurrently (§5, §10), a future checkpoint must first perform the file/migration/contract collision check this index could not yet perform (no concrete allowed-file paths exist for either track yet).

## 14. First eligible prompt

**Prompt 274 (`CG-S12-HRT-002`, Employee Master)** — dependency-clean, all prerequisites `VERIFIED`, ownership ADR filed (ADR-0023 Part B), no critical ownership/security/privacy/Finance/ticket-link conflict. Ready to begin its own atomic-task decomposition and schema design, including its own disclosed decision on whether to additionally register `master_type_code='employee'` in `app.master_records` (ADR-0023 Part B, left open).

---

## 15. Update — HRT-274 (Prompt 274, Employee Master) implementation complete

Recorded here rather than rewritten into §10/§13/§14 above (preserving those sections as this kickoff's own point-in-time record, matching this repository's append-only evidence discipline — see `ISS-2026-005`/`006`'s own precedent for "evidence, once committed, is not revised to look retroactively correct").

`CG-S12-HRT-002` is now **COMPLETED** (§10, row updated in place since that table is this index's own live task-state ledger, not a point-in-time narrative). Full evidence: `docs/build-log/phase-07/HRT-274.md`. `master_type_code='employee'` **was** registered (§10 row 274's own original open question, now resolved and documented). `HRT-275` (`CG-S12-HRT-003`, Organization and Position Linkage) is now the sole `READY` row — its own upstream (`HRT-274`) is satisfied, and `ADR-0023` Part A already resolves its `team`-node-type design question in advance. `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13).

---

## 16. Update — HRT-275 (Prompt 275, Organization and Position Linkage) implementation complete

Recorded here rather than rewritten into §13/§14 above, per the same append-only evidence discipline §15 already establishes for this file. §10's row 275 (and row 276) are updated in place, matching §15's own precedent that the task-state table is a live ledger, not frozen narrative.

`CG-S12-HRT-003` is now **VERIFIED** (§10). Full evidence: `docs/build-log/phase-07/HRT-275.md`. ADR-0023 Part A is implemented exactly as decided — this checkpoint's own chartered migration, not a re-litigation. HRT-274's own build log §10 recommendation (additive `app.employees.position_id`, formalize or disclose the manager-cycle-prevention decision) is fully addressed: `position_id` is real and additive; the cycle-prevention decision is to keep the existing bounded chain walk unchanged, with recorded reasoning (`HRT-275.md` §3.7), not a materialized closure table. `HRT-276` (`CG-S12-HRT-004`, Recruitment, job portal and ATS) is now the sole `READY` row — its own upstream (`HRT-275`) is satisfied and no additional ownership/security/privacy conflict is disclosed against its own stated scope. `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13).

## 17. Update — HRT-276 (Prompt 276, Recruitment, Job Portal and ATS) VERIFIED

Recorded here rather than rewritten into §13/§14 above, per the same append-only evidence discipline §15/§16 already establish for this file. §10's row 276 (and row 277) are updated in place.

Built as a single-prompt Tier-C batch under a fresh, explicit operator authorization naming Prompt 276 through Prompt 280 ("lanjut 276-280"), per this repository's standing rule that `CG-S12-HRT-004` was dependency-clean and `READY` but outside every prior authorization. Batched with `docs/standards/BUILD_EXECUTION_PROTOCOL.md`'s adaptive sizing: the immediately preceding batch (HRT-275) closed its own review round with 2 Critical/High findings, capping this batch at ≤3; independently, §3.2 trigger 2 (a first-of-its-kind-adjacent new anonymous/public entry point — the candidate job-application intake) cuts any batch to this one prompt alone regardless of the numeric cap.

`CG-S12-HRT-004` is now **VERIFIED** (§10), via: Implement (one agent, 12 self-found-and-fixed defects before commit) → Tier C (4 parallel adversarial lenses, live-tested against a real disposable Postgres — 10 CONFIRMED findings, 3 CRITICAL/2 HIGH/4 MEDIUM/1 LOW, 0 rejected) → fix pass (7 findings fixed via a new additive migration, 2 disclosed as new known issues, 1 documentation correction) → independent gate re-run and direct code-read verification by the orchestrating session (never accepted on the fix agent's self-report alone). Full evidence: `docs/build-log/phase-07/HRT-276.md`.

Per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.4, this batch's Tier C found 2+ Critical/High (5 total) — the next batch (`HRT-277`) stays capped at ≤3, which it already is as a single-prompt batch for an independent reason (§3.2 trigger 3: writes through Platform identity/access authority). `HRT-277` (`CG-S12-HRT-005`, Onboarding and Offboarding) is now the sole `READY` row — its own upstream (`HRT-276`) is satisfied. `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13).

## 18. Update — HRT-277 (Prompt 277, Onboarding and Offboarding) Implement stage COMPLETED

Recorded here rather than rewritten into §13/§14 above, per the same append-only evidence discipline §15/§16/§17 already establish for this file. §10's row 277 is updated in place.

Run within the same "lanjut 276-280" authorization as `CG-S12-HRT-004`, as its own **single-prompt Tier-C batch** (`docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.2 trigger 3: writes through Platform identity/access authority) — the first Phase 7 capability to exercise the governed candidate→employee→user linkage ADR-0023 Part B explicitly reserved for this exact prompt. This implementing agent is ONLY the Implement stage; a separate adversarial review stage and fix pass follow, not yet run.

## 19. Update — HRT-277 (Prompt 277, Onboarding and Offboarding) VERIFIED

Recorded here rather than rewritten into §13/§14 above, per the same append-only evidence discipline §15/§16/§17/§18 already establish for this file. §10's row 277 (and row 278) are updated in place.

`CG-S12-HRT-005` is now **VERIFIED** (§10). Tier C review (4 parallel lenses, live-tested against a real disposable Postgres) found **12 CONFIRMED, live-reproduced findings — 4 CRITICAL, 3 HIGH, 4 MEDIUM, 1 LOW, 0 rejected**, the largest and most severe finding set of any Phase 7 checkpoint so far. Most severe: a live-exploited privilege-escalation path letting any ordinary HR staffer grant themselves or anyone else any published tenant role — including cross-domain Finance/Procurement/Operations Approve/Override/Delete permissions — through the onboarding access-provisioning RPC, and an incomplete access-revocation cascade that left a "revoked" actor's `role_assignments` fully active. Both are exactly the kind of defect this prompt's own single-prompt-batch trigger (writing through Platform identity/access authority) was designed to catch before `VERIFIED`. All 4 CRITICAL plus 5 further findings fixed at root cause via a new additive migration (`20260730890000`); 2 disclosed as new-scope known issues (`ISS-2026-072` OPEN High — the systemic PLT-110/112 gap beneath the revocation fix; `ISS-2026-073` OPEN Medium — no "approved direct hire" precondition). Full evidence: `docs/build-log/phase-07/HRT-277.md` §12. Independent gate re-run and direct code-read verification by the orchestrating session confirmed every claimed fix is genuinely present (never accepted on the fix agent's self-report alone).

Per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.4, this batch's Tier C found 2+ Critical/High (7 total, including 4 CRITICAL) — the next batch stays capped at ≤3, which `HRT-278..280` (the final batch of this "lanjut 276-280" authorization, Attendance/Shift-Roster/Leave-Permit) already is. `HRT-278` (`CG-S12-HRT-006`, Attendance) is now the sole `READY` row — its own upstream (`HRT-277`) is satisfied, and none of its own §16 security-impact language names an anonymous entry point, auth/RBAC write, or finance-posting touch, so it does not independently trigger a further single-prompt cut. `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13).

`CG-S12-HRT-005` row 277 is now **COMPLETED** (§10) — Implement stage only, **not** `VERIFIED`. Full evidence: `docs/build-log/phase-07/HRT-277.md`. The employee/user conversion reuses HRT-274's `app.create_employee_draft` and PLT-107/110/111's `app.invite_user`/`app.link_employee_user`/`app.assign_role`/`app.transition_user_status` exactly, never a second flow, exactly as this row's own upstream dependency required. Both bug classes this task was explicitly warned against (raw-table grants bypassing RPC authority; a cancellation path failing to cancel an in-flight PLT-123 approval) were designed against from the first migration, not retrofitted. 11 real defects were found and fixed by the implementing agent's own honest adversarial testing and its own mandatory Tier B taxonomy self-check before commit, including one major, genuinely self-found idempotency-replay defect (taxonomy C-01) caught only by the taxonomy walk itself, not by a db-test failure. Two residual gaps disclosed as `ISS-2026-070` (Low) and `ISS-2026-071` (Medium) in `docs/runtime/KNOWN_ISSUES.md`.

`PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13). Per this repository's standing convention, `HRT-277`'s own Tier C adversarial review stage is the next action against this row — this Implement-stage agent does not itself advance row 277 to `VERIFIED`, name a next-`READY` row, or touch `CARGOGRID_BUILD_STATUS.md`/`HANDOFF.md` (orchestrating-session responsibility at batch close, per task instruction).

## 20. Update — HRT-278 (Prompt 278, Attendance) Implement stage COMPLETED

Recorded here rather than rewritten into §13/§14 above, per the same append-only evidence discipline §15/§16/§17/§18/§19 already establish for this file. §10's row 278 is updated in place.

Run within the same "lanjut 276-280" authorization as `CG-S12-HRT-004`/`CG-S12-HRT-005`, as the **first prompt** of this authorization's final batch, `HRT-278..280` (Attendance / Shift-Roster / Leave-Permit-Business-Trip), capped at ≤3 per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.4 since the immediately preceding batch (`HRT-277`) closed its own Tier C with 7 Critical/High findings (4 of them CRITICAL). This implementing agent builds ONLY Prompt 278; Prompts 279 and 280 build next within this same batch, then one combined 4-lens Tier C adversarial review covers all three, then one fix pass, before any of the three may be marked `VERIFIED` — per `CON-015`'s relaxation, `HRT-279` may begin on `HRT-278` being `COMPLETED`, without waiting for this batch's own Tier C close.

`CG-S12-HRT-006` is now **COMPLETED** (§10) — Implement stage only. Full evidence: `docs/build-log/phase-07/HRT-278.md`. Builds server-authoritative idempotent clock-in/clock-out (structurally anti-spoofing — the self-service RPC carries no employee-id parameter at all), effective-dated workday/timezone/grace/geofence policy resolution (branch-scoped over tenant-wide), system-detected exceptions, a direct propose(`HRS:Edit`)/decide(`HRS:Approve`) correction workflow deliberately NOT routed through PLT-123 (mirroring `app.employee_position_assignments`/HRT-275, since a hard PLT-123 configuration dependency would block every tenant that hasn't separately configured approval routing for an unrelated domain), a real PLT-131/132 staged device-import pipeline (the sixth-plus adopter, sharing its own ingestion engine with the live clock path — never a second write path), and a payroll-input-approval forward contract for the not-yet-built Payroll (Prompt 282). 6 new tables, ~30 functions, in one 2,196-line additive migration (`20260730900000`). Sensitive columns (location, correction/exception reason fields) are column-restricted from this, the first, migration — never retrofitted, the exact HRT-276 Tier C defect class designed against from the start; authority bars match blast radius (exception-waive requires `HRS:Override`, not merely `HRS:Edit`, self-approval blocked), designed against HRT-277's own worst Tier C finding from the start.

**7 real defects found and fixed during this checkpoint's own honest adversarial `psql` db-testing plus its own mandatory Tier B taxonomy self-check, before commit** (matching this repository's disclosed history — HRT-274 found 7, HRT-275 found 2, HRT-276 found 12, HRT-277 found 11): a geofence-enforcement design gap that blocked every manual HR entry and every device-import row against a required-geofence policy, fixed by exempting both non-live-capture channels structurally; **self-found via the Tier B taxonomy walk itself, not a db-test failure** — `waive_attendance_exception` had no self-approval block (C-18), fixed with the same guard `decide_attendance_correction` already had; a C-05 parity fix adding the intermediate tenant-membership fold every established staged-import precedent uses before `evaluate_permission`; **a real, live two-OS-process concurrency race** — two genuinely concurrent `psql` processes both attempted `clock_in` for the same employee, confirming exactly one session is ever created and the `(tenant_id, employee_id, work_date)` unique constraint is the real C-04 safety net for a path with no pre-existing row to lock; a `search_path`/PostGIS gap breaking six functions' own writes to geography-CHECK-constrained tables; a dropped `device_label` on device-imported rows; and two test-fixture-only bugs. **Tier B taxonomy self-check performed**: every class in `RECURRING_DEFECT_TAXONOMY.md` §4 walked; C-04/C-05/C-18 the three classes with a material self-found-and-fixed outcome this checkpoint; C-21 (sibling lock-order consistency) walked and confirmed clear with recorded reasoning; C-20 — `recalculate_attendance_exceptions_for_range` wired to a real UI form during this same checkpoint specifically to avoid leaving it unreachable, while `get_attendance_session_detail`/`get_attendance_policy_version`/`export_attendance_sessions` are disclosed as genuine LOW-severity no-UI-caller gaps.

All gates re-run fresh and green: `typecheck` 0 errors; `lint` 0 errors/205 warnings (identical to HRT-277's own baseline, zero new findings in this checkpoint's own files); `pnpm run test` **3601/3601 pass, 0 fail**; `TEST_DB_NAME=cargogrid_db_test_impl278 pnpm run db:test` **ALL PASSED** (191 migrations) — one unrelated file hit the standing, already-disclosed `ISS-2026-059` UTC time-window flake during this session's own run, proven pre-existing against a captured baseline (the identical failure reproduced at the identical wall-clock time with this checkpoint's own migration/test files temporarily removed); `next build` clean, 3 new routes present; `git:check-paths`/`security:check`/`data-classification:check` all clean (the data-classification registry extended proactively, ahead of its own batch-close gate schedule). See `docs/build-log/phase-07/HRT-278.md`. Residual gaps (no calendar-grid view, no device-import reconciliation-totals report, 3 read-only RPCs with no UI caller yet, no E2E spec matching every prior HRT checkpoint's own identical disclosed gap, no dedicated load/scale test) are recorded in that file's own §8 and will be registered as numbered `KNOWN_ISSUES.md` entries at this batch's own close, alongside `HRT-279`/`HRT-280`'s own disclosures — not filed individually mid-batch.

`PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13). Per `CON-015`, `HRT-279` (`CG-S12-HRT-007`, Shift, Roster and Scheduling) is now dependency-clean and may proceed within this same "lanjut 276-280" authorization on `HRT-278` being `COMPLETED` — this Implement-stage agent does not itself advance row 278 to `VERIFIED` or touch `CARGOGRID_BUILD_STATUS.md`/`HANDOFF.md` (orchestrating-session responsibility at this batch's own close, per task instruction).

## 21. Update — HRT-279 (Prompt 279, Shift, Roster and Scheduling) Implement stage COMPLETED

Run within the same "lanjut 276-280" authorization as `CG-S12-HRT-004`/`CG-S12-HRT-005`/`CG-S12-HRT-006`, as the **second prompt** of this authorization's final batch, `HRT-278..280`. This implementing agent builds ONLY Prompt 279; Prompt 280 builds next within this same batch, then one combined 4-lens Tier C adversarial review covers all three (278/279/280), then one fix pass, before any may be marked `VERIFIED` — per `CON-015`'s relaxation, `HRT-280` may begin on `HRT-279` being `COMPLETED`, without waiting for this batch's own Tier C close.

`CG-S12-HRT-007` is now **COMPLETED** (§10) — Implement stage only. Full evidence: `docs/build-log/phase-07/HRT-279.md`. Builds versioned shift definitions (template → version → real work/break segment child table, with a provable segment-order/cross-midnight validation rule), rotating roster cycles (cycle → day-offset slot, day-off is an explicit null row, publish requires every offset filled), per-employee schedule assignment (a governed `scheduled`-draft → `published`-immutable-by-normal-role lifecycle, every real transition a new row, never a destructive update of a published row), coverage requirements with a real met/below-minimum preview (aggregate-only, doubling as the "Operations workforce availability projection" surface), a governed two-employee swap workflow (self-or-`HRS:Edit` propose, `HRS:Approve` decide, self-approval blocked for BOTH parties), batch schedule generation reusing PLT-132's real background-job framework (the first HRIS-domain adopter of `app.generic_job_types()`, ATW-031/ISS-2026-012's own single source of truth — `app.enqueue_job`/`app.dispatch_event_as_job` themselves untouched), and — via a SEPARATE additive migration — the exact reservation `HRT-278`'s own migration header disclosed: `app.attendance_sessions` gains a nullable `schedule_assignment_id`, resolved and carried at clock-in time, purely additive and structurally optional. 9 new tables, ~35 functions, across 2 additive migrations (2,377 + 231 lines).

**9 self-found items fixed before commit** (8 individual production-code defects plus 1 grouped test-fixture-bug item, matching this repository's disclosed history — HRT-274 found 7, HRT-275 found 2, HRT-276 found 12, HRT-277 found 11, HRT-278 found 7): a job_type single-source-of-truth violation (a first draft widened `app.enqueue_job` directly from PLT-132's own now-superseded literal instead of the real current authority, `app.generic_job_types()` established by ATW-031/`20260730410000`, silently reverting two later-added job types — live-caught twice, independently, by an unrelated file's own test failure AND by `background-job.sql`'s own standing drift-gate assertion); an insert-before-supersede ordering bug that violated the new partial unique index on its own first supersede attempt; **a genuine authority-bypass-shaped bug** — `assign_employee_schedule`'s decision-5 branch read the bare `FOUND` special variable AFTER an intervening, unrelated `SELECT` (the C-01 idempotency-replay lookup) had already silently overwritten it, so any caller supplying an idempotency key could bypass the `HRS:Override` requirement when superseding an already-published assignment — live-caught by the db-test suite's own supersede-blast-radius assertion, fixed by capturing the original result into a dedicated boolean immediately; the recurring ambiguous-bare-column-vs-`RETURNS TABLE`-output-column bug (the exact class this repository's own HRT-274/275/276/277 each independently hit at least once) swept across 6 functions; **three genuine C-05 findings self-found via this checkpoint's own mandatory Tier B taxonomy walk itself, not by a db-test failure** — `request_schedule_swap` disclosed assignment/target status before the identity/authority check; `assign_employee_schedule` disclosed an arbitrary employee's lifecycle_status to any zero-permission tenant member before any authority check; `cancel_schedule_assignment` let a zero-permission caller infer row status from which specific permission name appeared in a rejection message (this checkpoint's own FIRST fix attempt for this specific finding was itself a regression — incorrectly requiring `HRS:Edit` unconditionally when `HRS:Edit`/`HRS:Override` are actually a genuine disjoint EITHER/OR pair, live-caught immediately by the db-test suite's own dependent-cancel-cascade assertion and corrected before commit); a `raise exception` format-string bug (`%%` instead of `%`) found by direct code re-read before it was ever exercised; and a grouped set of 11 test-fixture-only causes (a UUID range collision with `hris-employee-master.sql`'s own fixture, missing emergency-contact seeding, a missing tenant2 role permission, wrong-session-actor negative-test bugs, a swap-fixture collision with the SAME employee's own bulk-generated rows against the new partial unique index, a coverage-preview assertion that never actually observed its own claimed intermediate state, a column-restricted-field test lookup, and a hardcoded-calendar-date-vs-live-resolved-work_date design flaw in the attendance-integration test). **Two live, two-OS-process concurrency races reproduced** (mirroring HRT-278's own identical discipline): a genuine first-creation race on `assign_employee_schedule` (exactly one process succeeded, the loser received a real, uncaught `unique_violation`, zero duplicates); and, run three separate times, a lock-order race between `decide_schedule_swap_request` and `cancel_schedule_assignment` (zero raw Postgres deadlocks every time, a correct final state every time).

**Tier B taxonomy self-check performed**: every class in `RECURRING_DEFECT_TAXONOMY.md` §4 walked; C-05 the class with the most material self-found-and-fixed outcome this checkpoint (three real findings); C-04 and C-21 the two classes with genuine live two-process concurrency evidence; C-20 walked deliberately — `cancel_schedule_assignment` (no UI trigger) and four read RPCs (no detail-drilldown UI) disclosed as genuine, LOW-severity no-UI-caller gaps rather than force-built or silently absorbed, matching HRT-277/278's own identical disposition for this exact class.

All gates re-run fresh and green: `typecheck` 0 errors; `lint` 0 errors/205 warnings (identical to HRT-278's own baseline, zero new findings in this checkpoint's own files); `pnpm run test` **3646/3646 pass, 0 fail** (+45 new); `pnpm run db:test` **ALL PASSED** (193 migrations, run repeatedly across this checkpoint's own iterative adversarial fix cycle, the final gate-clearing run executed after 04:00 UTC specifically to clear the standing, already-disclosed `ISS-2026-059` UTC-time-window flake in an unrelated file); `next build` clean, 4 new routes present; `git:check-paths` clean (26 files checked, 0 forbidden); `security:check`/`data-classification:check` clean (the data-classification registry extended proactively, ahead of its own batch-close gate schedule, with 2 new `HRS_REGISTRY` entries). See `docs/build-log/phase-07/HRT-279.md`. Residual gaps (`cancel_schedule_assignment` and four read RPCs with no UI caller yet; no calendar-grid view, matching HRT-278's own identical precedent; deeper attendance-exception-formula binding to shift-specific segment times deliberately reserved, not built; no REST/GraphQL adapter; no E2E spec; no `beforeunload` guard; flexible-shift deeper time-window semantics not built beyond the informational tag) are recorded in that file's own §11 and will be registered as numbered `KNOWN_ISSUES.md` entries at this batch's own close, alongside `HRT-280`'s own disclosures — not filed individually mid-batch.

`PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13). Per `CON-015`, `HRT-280` (`CG-S12-HRT-008`, Leave, Permit and Business Trip) is now dependency-clean and may proceed within this same "lanjut 276-280" authorization on `HRT-279` being `COMPLETED` — this Implement-stage agent does not itself advance row 279 to `VERIFIED` or touch `CARGOGRID_BUILD_STATUS.md`/`HANDOFF.md` (orchestrating-session responsibility at this batch's own close, per task instruction).

## 22. Update — HRT-280 (Prompt 280, Leave, Permit and Business Trip) Implement stage COMPLETED — batch `HRT-278..280` Implement stage now closed

Run within the same "lanjut 276-280" authorization as `CG-S12-HRT-004`/`CG-S12-HRT-005`/`CG-S12-HRT-006`/`CG-S12-HRT-007`, as the **third and LAST prompt** of this authorization's final batch, `HRT-278..280`. This implementing agent builds ONLY Prompt 280. With this checkpoint `COMPLETED`, all three prompts in the batch (`HRT-278`/`HRT-279`/`HRT-280`) have finished their own Implement stage — the batch's single combined 4-lens Tier C adversarial review (against the COMBINED `278-280` diff, not three separate reviews) is the next required step before any of the three may be marked `VERIFIED`, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.4/ADR-0021.

`CG-S12-HRT-008` is now **COMPLETED** (§10) — Implement stage only. Full evidence: `docs/build-log/phase-07/HRT-280.md`. Builds versioned leave/permit/business-trip type and policy authoring (mirrors `HRT-278`'s attendance-policy and `HRT-279`'s shift-template versioning shape exactly), a real append-only balance ledger (`app.leave_balance_ledger` — no mutable running total, no `UPDATE`/`DELETE` grant to any role, live-verified), a governed request → submit → decide → cancel workflow **routed through PLT-123 exactly like every other Phase 6/7 flow** (`decide_leave_request` calls `app.decide_approval_step` as the real, first authority gate; `cancel_leave_request` calls `app.cancel_approval_request` for any in-flight approval — the exact bug class this task's own instructions named as independently found in `HRT-276` AND `HRT-277`), overlap prevention via the established `btree_gist` EXCLUDE pattern (`leave_requests_no_overlap`), coverage-aware approval reusing `HRT-279`'s own schedule/coverage resolution with an explicit, never-automatic schedule-override cancel, and async accrual/carry-forward-expiry batches reusing PLT-132's real generic job queue (second HRIS-domain adopter after `HRT-279`'s `roster_generation`). Additively binds into `HRT-278` attendance (an approved leave day suppresses the attendance exception it would otherwise register, via a separate `CREATE OR REPLACE` migration, `HRT-278`'s own original migration untouched). Medical/personal evidence reuses PLT-128's malware-scan re-validation pattern; sensitive free-text fields are column-restricted from the first migration. 4 new tables, 33 functions, across 2 additive migrations (2,365 + 136 lines).

## 23. Update — HRT-278..280 (Prompts 278-280, Attendance / Shift-Roster-Scheduling / Leave-Permit-Business-Trip) VERIFIED — the FULL "lanjut 276-280" authorization is now closed

Recorded here rather than rewritten into §13/§14 above, per the same append-only evidence discipline §15-§22 already establish for this file. §10's rows 278, 279, 280 (and row 281) are updated in place.

The batch's own combined 4-lens Tier C adversarial review (against the merged `20995e8..39ae551` diff spanning all three prompts, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.1's "one combined review, not three separate ones") found **12 CONFIRMED, live-reproduced findings — 1 CRITICAL, 5 HIGH, 4 MEDIUM, 2 LOW, 0 rejected**. Most severe (**CRITICAL**): `app.decide_leave_request`/`app.cancel_leave_request` delegated the employee lifecycle-status sync (`start_employee_leave`/`end_employee_leave`, HRT-274) using the CALLING actor's own authority, which hard-requires `HRS:Edit` — live-reproduced breaking BOTH directions: a correctly-scoped Approve/Override-only approver was rejected approving a same-day leave, and an ordinary employee (zero HRS grants, self-service by design) was rejected self-cancelling their own already-started leave, in both cases rolling back the entire transaction. Fixed by extracting a shared, authority-agnostic `app._transition_employee_leave_status` engine that `decide_leave_request`/`cancel_leave_request` call directly (their OWN authority — PLT-123 routing, or the self-service invariant — already established before reaching it), while `start_employee_leave`/`end_employee_leave` remain unchanged `HRS:Edit`-gated wrappers for their own direct callers. **3 further HIGH findings, all the identical "raw-table overexposure bypasses careful RPC-layer scoping" shape recurring a third time this phase** (after HRT-276 and HRT-277): `attendance_sessions`/`attendance_events`, `schedule_assignments`, and `leave_requests`/`leave_balance_ledger` all carried tenant-membership-only RLS with an unrestricted `authenticated` grant, letting any zero-permission tenant member read every employee's clock data, shift roster, or leave/medical calendar via a raw SELECT while the RPC layer correctly returned zero rows — live-reproduced across all three. Fixed uniformly with one new shared helper, `app.can_view_hris_person_scoped_row` (self/manager/HRS:View), reused identically across all three capabilities' RLS policies rather than three separate bespoke fixes. **1 further HIGH**: leave-request evidence files were validated only at creation, never re-validated on draft-update or submit, live-reproduced accepting a cross-tenant, unscanned, wrong-record-type file end-to-end — fixed with a shared `_validate_leave_evidence_file` called at both write paths. **1 further HIGH**: leave approval never re-triggered attendance-exception recalculation for the covered date, so a backdated/same-day approval left a stale, contradictory open exception — fixed bidirectionally. **4 MEDIUM findings fixed**: a genuine schedule-reassignment race correctly serialized to one winner but leaked a raw Postgres constraint-violation message to the loser instead of a domain error; `list_leave_requests`' manager-scoping broke specifically when an explicit employee filter was supplied (diverging from the sibling HRT-278/279 list functions); `generate_roster_schedule_assignments` had no real idempotency key, so an identical retry silently superseded and recreated every draft assignment rather than no-op'ing; four attendance read RPCs (`get_attendance_session_detail` plus three list-cursor paths) raised a hard `ambiguous column` error on every call/every non-null cursor — the exact recurring bare-`id`-vs-`RETURNS TABLE` bug this phase's own taxonomy walk is supposed to catch, confirmed absent from the sibling HRT-279/280 migrations, present only in HRT-278. **2 LOW, documentation-only**: HRT-280's own self-disclosed propagation note about HRT-279's job-replay behavior didn't hold up under live-code verification (no live bug on that path — flagged so the fix pass didn't spend effort on an unneeded guard); a wrong column name (`reports_to_employee_id` vs. the real `manager_employee_id`) in HRT-280's own build-log prose, corrected.

All 12 fixed at root cause via three new additive migrations, one per capability (`20260730950000` attendance, `20260730960000` shift-roster, `20260730970000` leave-permit — each capability's own original migration left untouched). **Gate suite independently re-run a third time by the orchestrating session, every number matching**: `typecheck` 0 errors; `lint` 0/205; `pnpm run test` **3672/3672**; `pnpm run db:test` **ALL PASSED** (197 migrations); `next build` clean; `git:check-paths`/`security:check`/`docs:check`/`standards:check`/`data-classification:check`/`threat-model:check`/`security:audit` all clean. Direct code reads independently confirmed the shared `app.can_view_hris_person_scoped_row` helper is genuinely applied across all three capabilities' RLS policies, the `_transition_employee_leave_status` authority-agnostic engine is correctly separated from its gated wrappers, and the evidence-file re-validation is genuinely wired at both call sites — never accepted on the fix agent's self-report alone.

`CG-S12-HRT-006`/`007`/`008` are now **VERIFIED** (§10). **This closes the FULL "lanjut 276-280" operator authorization**: `CG-S12-HRT-004` through `CG-S12-HRT-008` (Prompts 276-280) all `VERIFIED`, in sequence, no task skipped. Full evidence: `docs/build-log/phase-07/HRT-278.md` §12 (approx.), `HRT-279.md` §12 (approx.), `HRT-280.md` §12 (approx.) — each capability's own Tier C fix-pass section, appended per this repository's append-only convention.

`CG-S12-HRT-009` (Prompt 281, Overtime and Timesheet) is dependency-clean and structurally `READY` (§10) — but **the operator explicitly extended this session's authorization to "lanjut 276-285" mid-batch, then withdrew that extension ("281-285 ditunda dulu")**. Per this repository's own standing rule (identical precedent at every phase/range boundary in this build, e.g. Prompt 271 and Prompt 276's own prior holds), **a fresh, separate, explicit operator authorization naming Prompt 281 is required before it may begin.** `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13).

**16 self-found production-code defects plus 1 grouped test-fixture-bug item fixed before commit** (matching this repository's disclosed history — HRT-274 found 7, HRT-275 found 2, HRT-276 found 12, HRT-277 found 11, HRT-278 found 7, HRT-279 found 9): **a genuine C-05 finding, self-found via this checkpoint's own mandatory Tier B taxonomy walk itself, not by a db-test failure** — `decide_leave_request`'s coverage-threshold check originally ran BEFORE `app.decide_approval_step`, disclosing scheduling detail before the real authority gate, fixed by reordering; a C-01 idempotency-replay gap — the full-tuple comparison originally checked dates only, silently accepting a same-key-different-reason/destination replay, fixed to compare the complete stored tuple; a decided-shape CHECK constraint that originally blocked cancelling an already-approved request, live-caught by the db-test suite's own cancel-an-approved-request assertion, fixed to the correct per-status shape (with one live-caught over-relaxation on the first attempt, corrected before commit); the authoritative balance debit was dated at the leave's own future start date instead of the decision date, making an approved-but-not-yet-taken leave invisible to same-day balance reads, live-caught by the db-test suite's own approve-then-check-balance assertion, fixed in both the authoritative debit and the submit-time soft check; the recurring ambiguous-bare-column-vs-`RETURNS TABLE`-output-column bug and an ambiguous local-variable-vs-callee-OUT-parameter collision (the exact classes this task's own instructions explicitly flagged, and `HRT-279` independently hit too), both live-caught by `pnpm run db:test`; the two accrual/carry-forward batch functions unconditionally tried to re-claim an already-completed job on idempotent replay, live-caught by the db-test suite's own replay assertion, fixed by guarding on the job's own `pending` status — **disclosed, not fixed out-of-scope: the identical unguarded shape exists in `HRT-279`'s own `generate_roster_schedule_assignments`, flagged for the batch's combined Tier C sweep**; plus a stray dead-code leftover, a missing RAISE argument, a multi-item-INTO structural error from an initial shared two-OUT-param helper, an ATW-032 checker gap fixed with an explicit redundant tenant-membership check, a PRE-EXISTING `get_self_employee` ATW-032 gap **proven not introduced by this checkpoint via an explicit baseline re-run with this checkpoint's own migrations temporarily removed, reproducing the identical failure unchanged**, a systemic test-file-only `select set_config(...)` bug (38 occurrences), a test-file-only ambiguous-`id` self-bug in the ad-hoc concurrency-race setup script (the same recurring class, self-reproduced in a throwaway script), and a hardcoded test-fixture magic number that went stale after the debit-dating fix cascaded — full detail in `HRT-280.md` §7.

**Live concurrency evidence, real OS processes, not reasoned about** (`HRT-280.md` §8) — the exact evidence this task's own instructions required ("a real concurrency test on any overlap-prevention constraint you add"): two DRAFT leave requests with deliberately overlapping dates, two genuinely concurrent `psql` processes each calling `submit_leave_request` on one. Exactly one process won cleanly (transitioned to `pending_approval` with a real PLT-123 approval id attached); the other received a genuine, uncaught Postgres `exclusion_violation` translated into the friendly `leave_request_overlap` message — zero double-booking, the loser's own row left cleanly in `draft` with no dangling approval request.

**Tier B taxonomy self-check performed**: every class in `RECURRING_DEFECT_TAXONOMY.md` §4 walked; C-05 and C-01 the two classes with material self-found-and-fixed production outcomes this checkpoint; C-04 the class with genuine live two-process concurrency evidence on two distinct structural safety nets (the overlap EXCLUDE constraint, live-raced, and the advisory-lock-serialized balance debit); C-20 walked deliberately — six real, tested mutation wrappers with zero UI caller anywhere (`createLeaveRequestForEmployee`, `updateLeaveRequestDraft`, `loadOpeningLeaveBalance`, `runLeaveAccrualBatch`, `runLeaveCarryForwardBatch`, `approveLeaveForPayrollInput`) disclosed rather than force-built or silently absorbed.

All gates re-run fresh and green (2026-08-10, 05:14 UTC onward, outside the standing `ISS-2026-059` flake window): `typecheck` 0 errors; `lint` 0 errors/205 warnings (identical baseline, zero new findings in this checkpoint's own files); `pnpm run test` **3672/3672 pass, 0 fail**; `TEST_DB_NAME=cargogrid_db_test_impl280_final pnpm run db:test` **ALL PASSED** (195 migrations, zero `ERROR` lines attributable to this checkpoint's own test file, grep-verified against the full log); `next build` clean, 3 new routes present; `git:check-paths` clean; `security:check` clean; `data-classification:check` clean (3 new `HRS_REGISTRY` entries). See `docs/build-log/phase-07/HRT-280.md`. Residual gaps (six C-20 no-UI-caller mutation wrappers; `HRT-279`'s own analogous unguarded job-replay shape flagged for the batch's combined Tier C sweep; no calendar-grid view, matching `HRT-278`/`HRT-279`'s own identical precedent; the advisory-lock balance-debit race not separately re-run as its own live two-process test given the time budget, disclosed as a scope choice; no REST/GraphQL adapter; no E2E spec; no `beforeunload` guard; deeper shift-segment-time binding into leave-day exception suppression deliberately reserved) are recorded in that file's own §10 and will be registered as numbered `KNOWN_ISSUES.md` entries at this batch's own close, alongside `HRT-278`'s and `HRT-279`'s own already-disclosed items — not filed individually mid-batch.

`PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13). **This Implement-stage agent does not itself advance any of rows 278/279/280 to `VERIFIED`, does not touch `docs/runtime/KNOWN_ISSUES.md`, and does not touch `CARGOGRID_BUILD_STATUS.md`/`HANDOFF.md`** — the batch's own combined Tier C review (4 parallel lenses against the combined 278-280 diff), one combined fix pass, and an independently re-run fresh gate suite are the orchestrating session's next responsibility before any of the three prompts may close.

---

## 24. Update — HRT-281 (Prompt 281, Overtime and Timesheet) VERIFIED — Tier C batch review-round fix pass

`CG-S12-HRT-009` (Prompt 281) implemented overtime request/approval (planned and emergency-after-the-fact), actual-time reconciliation against `HRT-278`'s own `app.attendance_sessions` (read-only reuse, no bind migration needed), manual/import/attendance-derived timesheet entry against eligible schedule/job/shipment references, server-authoritative configurable rounding/minimum/daily-cap/weekly-cap rules, a period definition + per-employee summary approve/lock/reopen workflow, and ONE versioned, content-based-idempotent payroll-input handoff record per (period, employee) — approved time/classification only, zero rate/amount/currency columns anywhere. 7 new tables, 54 functions, one 3,873-line additive migration (`20260730980000`). Implement-stage detail, Tier A/Tier B results, and 10 self-found production-code defects fixed before commit: `docs/build-log/phase-07/HRT-281.md` §1-§11.

This is a **single-prompt batch** — the prior `HRT-278..280` batch already closed `VERIFIED` (§23), and no further prompt was added to this batch after 281 (the operator's "281-285 ditunda dulu" hold, recorded in §10's prior row text, meant Prompt 281 itself required its own fresh, separate authorization before starting — once granted, it ran and closed as a batch of one).

**Tier C review (4 parallel lenses, live-tested against a real disposable Postgres 16 with forged-JWT sessions) found 10 CONFIRMED findings — 1 CRITICAL, 5 HIGH, 1 MEDIUM, 1 LOW, plus 2 further findings the originating lens rated HIGH whose severity was corrected to Low/Medium on independent re-verification, 0 rejected** — full disposition table and live-reproduction detail: `docs/build-log/phase-07/HRT-281.md` §13.

**CRITICAL**: `app._create_overtime_request`/`app._create_timesheet_entry` never queried `app.leave_requests` (HRT-280) at all, despite spec section 23/25 explicitly requiring "block schedule/leave overlap" — live-reproduced end to end (an employee with a real, approved permit leave for a date could still get a real overtime request created, and later approved, for that same date, with zero error). Fixed by adding a read-only leave-overlap check to both internal creation engines, reused directly from `app.leave_requests`, never a second leave calendar — exactly the low-risk, read-only cross-capability reuse shape this same migration already used for `app.attendance_sessions`/`app.roster_holidays`/`app.schedule_assignments`.

**5 HIGH, all fixed via one new additive migration**:
1. All four cursor-paginated list RPCs (`list_my_overtime_requests`, `list_overtime_requests`, `list_my_timesheet_entries`, `list_timesheet_entries`) raised a hard `column reference "id" is ambiguous` on the first real call with a non-null pagination cursor — the recurring bare-`id`-vs-`RETURNS TABLE`-output-column class this same migration's own header comment explicitly claims to guard against, and which had already recurred at HRT-274 (3×), HRT-275, HRT-276, and HRT-278. Fixed by aliasing the source table in each cursor lookup.
2. `_create_overtime_request`/`_create_timesheet_entry`'s own full-tuple idempotency-replay comparison — the exact discipline both functions' own inline comments claim to follow — omitted `unpaid_break_minutes`, so a same-key retry correcting a genuinely wrong break value was silently swallowed with the caller told nothing. Fixed by adding the field to both comparisons.
3. `cancel_overtime_request`/`cancel_timesheet_entry` never checked `is_timesheet_period_locked`, unlike submit/decide — live-reproduced with a real control check (a fresh submit into the same locked period correctly failed) immediately followed by the exploit (a zero-HRS-permission self-requester successfully cancelled their own already-approved, payroll-pending overtime request inside that same locked period, no reopen workflow involved). Fixed by adding the identical lock-gate check both cancel functions were missing.
4. A live regression of a class the immediately preceding capability (HRT-280) explicitly fixed one migration earlier: `to_jsonb(v_summary)` (2 sites) and 8 further `capture_audit_event` call sites routed unmasked `decided_reason`/`cancel_reason`/`last_reopen_reason` free text into `app.audit_logs`, readable by ANY tenant_admin via `app.query_audit_logs` — not gated by `HRS:View`, `HRS:View personal data`, self, or direct manager. Live-reproduced: a zero-HRS-permission tenant_admin, correctly denied the same data on every legitimate read RPC (raw SELECT, `get_overtime_request_detail`, `get_timesheet_period_summary`), read real HR decision rationale straight out of the audit trail. Fixed with a new masked `app.timesheet_period_summary_audit_projection` (mirroring HRT-280's own `app.leave_request_audit_projection`) and by no longer routing the 8 sites' own reason text into `capture_audit_event`'s unredacted `p_reason` column.
5. `reopen_timesheet_period_summary` never invalidated the already-generated `app.payroll_time_inputs` row for that (period, employee) — live-reproduced: immediately after a governed reopen, before any regenerate, `get_payroll_time_input`/`list_payroll_time_inputs` (the actual RPCs `HRT-282` is expected to consume) both still returned `status='active'` with stale, pre-correction totals and zero staleness signal. Fixed by widening `payroll_time_inputs.status`'s own CHECK constraint to add a third value, `'stale'`, flipping the active row to it at reopen time, and teaching the generate function to treat a `'stale'` row as the prior version to compare against/replace — restoring it directly to `'active'` when a recompute is genuinely unchanged, never returning a still-`'stale'`-labelled row as if it were the fresh, authoritative result. Independently re-verified all three regenerate paths live: reopen-alone (flips to stale), reopen→unchanged-recompute (restores to active, no redundant version), reopen→real-change→recompute (creates a genuine v2, v1 correctly superseded with `superseded_by_id` linked).

**1 LOW, fixed**: `timesheet_period_summaries.entry_count`/`overtime_request_count` missing a non-negativity CHECK, unlike every sibling numeric column on the same table (defense-in-depth only; unreachable via any current write path). Fixed with a new CHECK constraint.

**2 findings' severity corrected on independent re-verification, disclosed rather than force-fixed**: the spec-compliance lens rated `export_timesheet_entries`/`validate_timesheet_import_row`/`commit_timesheet_import_job` having zero TS wrapper as a HIGH, undisclosed, HRT-281-specific gap. Independent re-verification found `app.export_attendance_sessions`/`app.export_schedule_assignments`/`app.export_leave_requests` — all three already-`VERIFIED` sibling HRIS capabilities' own equally `authenticated`-granted export RPCs — are IDENTICALLY unwrapped, and `app.validate_timesheet_import_row`'s `service_role`-only grant exactly matches HRT-278's own identical `app.validate_attendance_device_import_row` precedent by design, not oversight. This is a consistent, repository-wide pattern spanning every HRIS capability built so far, not something HRT-281 introduced or shipped worse than precedent — severity corrected to Low, disclosed as `ISS-2026-075` (a cross-capability sweep, outside this single-prompt batch's own allowed-files scope, is the right-shaped fix). A genuine cross-prompt integration finding — `app.attendance_sessions` (HRT-278) and `app.payroll_time_inputs` (HRT-281) both independently self-declare, in their own migration headers, "the forward contract" `HRT-282` (Payroll) is expected to consume, with the disclosed `'attendance_derived'` timesheet-entry-source bridging mechanism between them never actually implemented — is disclosed as `ISS-2026-074` (High), since resolving it requires an architectural decision this single-prompt batch has no mandate to make; owned by `HRT-282`'s own future implementation, whose Tier B/C review must treat "which source is canonical, and how is double-counting prevented" as a mandatory, non-deferrable design question.

**1 MEDIUM disclosed**: 5 mutation wrappers (`createOvertimeRequestForEmployee`, `createTimesheetEntryForEmployee`, `updateTimesheetEntryDraft`, `reconcileOvertimeRequestActual`, the singular `generatePayrollTimeInput`) exist in the TS service layer with zero caller from any Server Action, page, or panel — matches `HRT-280`'s own disclosed, accepted precedent shape (six leave-domain mutation wrappers, identical no-UI-caller pattern) exactly. Disclosed as `ISS-2026-076` (Medium).

All 6 fixed findings closed at root cause via one new additive migration, `supabase/migrations/20260730990000_harden_hris_overtime_timesheet_review_fixes.sql` — `20260730980000` (this checkpoint's own original migration) left untouched, per `AGENTS.md` "never edit an applied migration." Every fix was independently live-re-reproduced as fixed (the identical exploit sequence re-run against the corrected schema, not merely asserted) before being accepted — full detail `HRT-281.md` §13.4.

**Gate suite independently re-run fresh, from scratch, after the fix — every number matching, never accepted on the fix agent's self-report alone**: `typecheck` 0 errors; `lint` 0 errors/205 warnings (identical baseline); `pnpm run test` **3715/3715 pass, 0 fail** (the previously-disclosed self-referential `check-worktree-collision` failure now resolves, since this branch carries real commits); `pnpm run db:test` **ALL PASSED**, 199 migrations (198 + this checkpoint's own corrective one), 357 test blocks across every `scripts/db-tests/*.sql` file including `hris-overtime-timesheet.sql`'s own suite; `next build` clean, all 3 `overtime-timesheet` routes present; `git:check-paths` clean (3 files checked, 0 forbidden, 1 caution — the expected `docs/runtime/**` additive-ledger reminder on `KNOWN_ISSUES.md`); `git:check` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check` all clean. The orchestrating session directly read the corrective migration and confirmed the leave-overlap check, the aliased cursor lookups, the widened idempotency-tuple comparison, the period-lock gate on both cancel functions, the masked audit projection, and the `'stale'`-status payroll-input invalidation are each genuinely present and correctly shaped — never accepted on a fix agent's self-report alone.

**Task state**: `CG-S12-HRT-009` (Prompt 281, Overtime and Timesheet) is now **`VERIFIED`** (§10). `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13). `CG-S12-HRT-010` (Prompt 282, Payroll) is now dependency-clean at the task-state level — its own stated upstream, `HRT-274..281`, is fully `VERIFIED` — **but this does NOT itself authorize starting Prompt 282**: per this repository's own standing precedent (every prior phase/range-boundary hold in this build, most recently this same row's own hold on Prompt 281 itself), a fresh, separate, explicit operator authorization naming Prompt 282 is required before implementation may begin, and `HRT-282`'s own future Tier B/C review must treat `ISS-2026-074`'s "which of `app.attendance_sessions` vs. `app.payroll_time_inputs` is canonical for regular time" as a mandatory, non-deferrable design question. Full detail: `docs/build-log/phase-07/HRT-281.md` §13; `docs/runtime/TASK_LEDGER.md`'s `CG-S12-HRT-009` row (updated in this same checkpoint); `docs/runtime/KNOWN_ISSUES.md` `ISS-2026-074`/`075`/`076` (new, this checkpoint).

## 25. Update — HRT-282 (Prompt 282, Payroll Foundation, Benefit and Reimbursement) VERIFIED — Tier C batch review-round fix pass

`CG-S12-HRT-010` (Prompt 282) implemented payroll calendar/period, versioned component/formula authoring (fixed_amount/hourly_rate/percentage_of_component/manual_per_run) with the RPD-016 statutory-activation gate (5 statutory components seeded as un-approvable labels only), employee component assignment, a frozen per-(period, employee) input snapshot that resolves `ISS-2026-074` (the HRT-278-vs-HRT-281 competing payroll-time-input contract, previously OPEN High) with an explicit per-work-date precedence, a maker-checker-governed run/batch calculation engine reusing PLT-131/132's `app.jobs`, exception review, private payslips, governed reimbursement/loan lifecycles, and a Payroll-owned Finance handoff mirroring `app.prepare_finance_vendor_bill_from_actual_cost` exactly (structurally proven zero write to any `app.finance_*` table). 16 new tables, ~62 functions, two additive migrations (`20260731000000`, `20260731010000`). Implement-stage detail, Tier A/B results, and 4 self-found production-code items fixed before commit: `docs/build-log/phase-07/HRT-282.md` §1-§12.

This is a **single-prompt batch**, per this task's own binding "run as its own single-prompt Tier C batch" instruction — the single most sensitive and highest-scope Phase 7 capability so far, never bundled with 283+.

**Tier C review (4 parallel lenses, live-tested against a real disposable Postgres 16, including genuinely concurrent OS `psql` processes for the correctness lens) found 13 CONFIRMED findings — 1 CRITICAL, 5 HIGH, 4 MEDIUM/MEDIUM-HIGH, 3 LOW — 0 rejected** — full disposition table and live-reproduction detail: `docs/build-log/phase-07/HRT-282.md` §13.1.

**CRITICAL** (security + integration lenses, identical root cause, independently converged on): `app.redact_audit_payload()` only key-name-pattern-redacts `before_value`/`after_value` and never touches `reason` at all, so a small ad hoc `jsonb_build_object` carrying a key like `amount`/`principal_amount`/`net_pay_total` (never matching the shared redactor's pattern) and a raw `p_decided_reason`/`p_cancel_reason`/`p_resolution_note` routed straight into `capture_audit_event`'s `p_reason` both persisted in the clear — readable by ANY plain `tenant_admin` (zero `HRS` role) via `app.query_audit_logs`, which gates on Supreme Admin OR any tenant's own active `tenant_admin`, a materially broader bar than decision 5's own `HRS:View payroll` compensation-visibility model. Live-reproduced exactly: a bare tenant_admin, correctly denied on every raw table and read RPC, read a real reimbursement amount+employee_id, a real loan principal_amount+employee_id, and real decision/finalize/cancel reason text straight from the audit trail. This is the **third recurrence** of this exact shape on this same branch — HRT-280 built `app.leave_request_audit_projection` to close it; HRT-281's own Tier C review found and fixed 10 sites; HRT-282 reintroduced it across 13 sites in 12 functions. Fixed by no longer routing raw reason text into `p_reason` and no longer including money-shaped keys in `after_value` at any of the 13 sites; a new recurring-defect-taxonomy class `C-24` recorded (`docs/standards/RECURRING_DEFECT_TAXONOMY.md`) so this is tracked as the standing, named pattern it now is, not re-discovered a fourth time from scratch.

**5 HIGH, fixed or disclosed via one new additive migration**:
1. `create_payroll_period`/`create_payroll_component`/`create_payroll_component_version` used check-then-insert with no exception handler (unlike sibling `create_payroll_run`) — live-reproduced with 3 separate pairs of genuinely concurrent OS `psql` processes, each losing process receiving a raw, unclassified `23505`. Fixed with exception handlers raising clean, classifiable conflict errors.
2. The claimed durable-job/checkpoint mechanism (a chunk-boundary heartbeat + `app.record_job_failure`-compatible dead-letter path, "reusing PLT-131/132 unchanged") is not real — the whole job lifecycle runs inside ONE function invocation/transaction; `app.claim_next_job`/`app.record_job_failure` are never called, grep-confirmed. Confirmed by direct code read and by live cross-session observation during an in-flight calculation (zero durable trace visible externally). Crash-safe (full atomic rollback, zero corruption, live-confirmed) but not genuinely resumable. Disclosed as `ISS-2026-079`, not force-fixed — a true multi-transaction redesign is an architectural decision outside a review-round's mandate; the migration comment and build log corrected to state the real, narrower guarantee.
3. The mid-loop `status='cancelling'` cancellation check was unreachable dead code — grep-confirmed zero occurrences anywhere of anything setting a `payroll_calculation` job to `'cancelling'` (the only function that ever does, `app.cancel_import_export_job`, is import/export-scoped under a different authority). Fixed with a new, correctly `HRS:Edit`-scoped RPC (`app.request_payroll_run_calculation_cancellation`), live-verified to correctly gate and to genuinely flip an in-progress job's status, which the pre-existing chunk-boundary check then observes (Postgres READ COMMITTED semantics). TS wrapper added; no UI form yet, disclosed as `ISS-2026-080`.
4. `app._resolve_payroll_time_inputs_for_period` required a `timesheet_periods` row to fall ENTIRELY inside the payroll period to contribute anything — a weekly timesheet period straddling a monthly payroll-period boundary (a routine, ordinary configuration, not a contrived edge case) silently dropped its ENTIRE week's real, HR-approved time. Live-reproduced end to end: a real, approved 480-minute entry for a work_date genuinely inside the payroll period produced `regular_minutes=0` in the frozen snapshot, zero error/exception/audit trace. Fixed at root cause: rewritten to compute contribution per work_date from the underlying `timesheet_entries`/`overtime_requests` rows, scoped to the payroll period range, never gated on full timesheet-period containment. Re-verified live: the identical scenario now correctly produces `regular_minutes=480`. Registered and closed as `ISS-2026-078` (RESOLVED).

**4 MEDIUM/MEDIUM-HIGH, all fixed**:
- `freeze_payroll_period_inputs` had no guard against re-freezing a period with an already-advanced run, unlike its own sibling `reopen_payroll_period_inputs` (which already had one) — live-reproduced: re-froze a period with a `calculated` run, got a raw `23503` FK violation once the snapshot `DELETE` collided with a run's own `input_snapshot_id` reference. Fixed with the identical guard `reopen_payroll_period_inputs` already has, raising the already-known `payroll_period_has_advanced_run`.
- The 3 Finance-handoff tables' RLS policies called `app.evaluate_permission` directly inside `USING(...)`, which is `service_role`-only-granted — live-reproduced blocking even a genuinely `FIN:Edit`-holding actor's raw `SELECT` with a hard `permission denied for function evaluate_permission`. Fixed with a new `SECURITY DEFINER` wrapper (`app.check_payroll_finance_handoff_view_authority`) mirroring `app.check_payroll_authority`'s established shape, all 3 policies replaced.
- `create_payroll_run` never validated a correction/adjustment run's own period matches the finalized run it corrects — live-reproduced: created a correction run against an unrelated, never-frozen period while `adjusts_run_id` pointed at a finalized run from a DIFFERENT period, zero error. Fixed with an explicit check.
- Reimbursement/run idempotency-replay comparisons omitted `currency`/`evidence_file_id` from the full-tuple check (the same class HRT-281's own Tier C fix closed for `unpaid_break_minutes` one checkpoint earlier). Fixed.

**3 LOW**: the build log's own C-20 disclosure list wrongly named `cancel_payroll_run` (which IS wired end-to-end) and omitted `app.reopen_payroll_period_inputs`/`app.create_payroll_reimbursement_request_for_employee`, which genuinely had no UI caller — corrected, and `reopen_payroll_period_inputs` (by the migration's own comment, the ONLY path back from `input_frozen` to `open`) wired end-to-end with a new "Reopen inputs" form, pairing naturally with the freeze-guard fix above; `create_payroll_reimbursement_request_for_employee` remains a disclosed, accepted no-UI-caller gap matching established precedent (`ISS-2026-081`). The payroll contract's `money` schema header comment overclaimed an input-side "never a JS number" guarantee it only actually provides on output — corrected (`ISS-2026-082`, RESOLVED). A test-methodology observation from the security lens (some of the checkpoint's own db-test `when others` self-approval assertions don't discriminate the caught `sqlerrm`) was independently re-verified as NOT a live defect (self-approval blocking genuinely holds under a stricter direct-state re-check) — noted, not fixed, no product gap to track.

All fixed findings closed at root cause via one new additive migration, `supabase/migrations/20260731020000_harden_hris_payroll_batch_282_tier_c_review_fixes.sql` — both original migrations (`20260731000000`, `20260731010000`) left untouched, per `AGENTS.md` "never edit an applied migration." Every fix was independently live-re-reproduced as fixed (the identical exploit/reproduction sequence re-run against the corrected schema, not merely asserted) before being accepted — full detail `HRT-282.md` §13.1/§13.4.

**Gate suite independently re-run fresh, from scratch, after the fix — every number matching, never accepted on the fix agent's self-report alone**: `typecheck` 0 errors; `lint` 0 errors/205 warnings (identical baseline, zero findings in this checkpoint's own files); `pnpm run test` **3755/3755 pass, 0 fail**; `pnpm run db:test` **157/158 test files pass — the 1 failure independently proven pre-existing and unrelated to this diff**: `hris-leave-permit-business-trip.sql` (HRT-280's own already-`VERIFIED` test file) fails its own `>> HRT-278 integration ... an employee with NO leave coverage still gets a real, undisturbed late exception` assertion on the repository's current wall-clock date — reproduced identically with this batch's own corrective migration removed from the tree entirely (the exact `COMPLETED` state this batch inherited), and a scratch, non-committed continue-on-failure harness variant confirmed the remaining 157 files, including `hris-payroll.sql` and every file sorted after the known-broken one, all pass clean against the full, fresh, 202-migration set. Registered as `ISS-2026-077`, out of this batch's authorized scope (HRT-280's own territory, not touched); `next build` clean, both payroll routes present plus the new "Reopen inputs" form; `git:check-paths`/`git:check` clean; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check` all clean. The orchestrating session directly read every Critical/High fix and confirmed each is genuinely present and correctly shaped, and independently live-re-reproduced each exploit/scenario as fixed post-migration — never accepted on a fix agent's self-report alone.

**Task state**: `CG-S12-HRT-010` (Prompt 282, Payroll) is now **`VERIFIED`** (§10). `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297). `CG-S12-HRT-011` (Prompt 283, KPI and Performance) is now dependency-clean at the task-state level — its own stated upstream, `HRT-274..282`, is fully `VERIFIED` — **but this does NOT itself authorize starting Prompt 283**: per this repository's own standing precedent (every prior phase/range-boundary hold in this build, most recently this same row's own hold on Prompt 282 itself), a fresh, separate, explicit operator authorization naming Prompt 283 is required before implementation may begin. Full detail: `docs/build-log/phase-07/HRT-282.md` §13; `docs/runtime/TASK_LEDGER.md`'s `CG-S12-HRT-010` row (updated in this same checkpoint); `docs/runtime/KNOWN_ISSUES.md` `ISS-2026-077`..`082` (new, this checkpoint; `078`/`082` RESOLVED in this same pass).

---

## 26. Update — HRT-291..293 (Prompts 291-293, Ticket Escalation / Typed Ticket-Linked Records / Sensitive Personal and Payroll Data Controls) VERIFIED — closes Batch 1 of the "lanjut 291-300" authorization

Recorded here rather than rewritten into §10 above (only the row `State`/`Basis` cells are updated in place, per this file's own append-only evidence discipline §15-§25 already establish). §10's rows 291, 292, 293 (now `VERIFIED`) and row 294 (now `READY`) are updated in place. This section is the batch-close record for the FIRST batch of a fresh, explicit "lanjut 291-300" operator authorization — the range immediately following the "lanjut prompt 281-290" authorization §23's own close narrative documents.

**`CG-S12-HRT-019` (Prompt 291, Ticket Escalation)** built the SIXTH capability of the Ticket workstream: versioned functional/hierarchical/SLA-driven escalation (`app.ticket_escalation_policies`/`_versions`/`_levels` mirroring HRT-289's own SLA-policy shape; `app.ticket_escalations`; `app.ticket_escalation_events`, an append-only ledger on its own table, never widening the shared `ticket_events` CHECK; `app.ticket_escalation_suppressions`, authority-gated with mandatory reason/expiry), a deterministic `rank()`-based precedence engine, a real batch evaluator (`run_ticket_escalation_evaluation_batch`, PLT-132) consuming HRT-289's own `ticket_sla_clock_events`, manual escalate/acknowledge/suppress/revoke-suppression/resolve RPCs, the first real domain consumer of PLT-127's notification engine (a deduplicated `ticket_escalated` type), a customer-safe single-boolean status projection structurally separate from every internal field, and a dedicated breach/stuck-ticket queue (`app.list_ticket_breach_queue`) built rather than widening the already-`VERIFIED` `app.list_tickets`/`app.list_my_tickets`. Bounded to internal/customer channels, `channel='helpdesk'` rejected outright — mirrors HRT-288/290's own "no non-Supreme-Admin helpdesk model" precedent exactly. Reassignment composes HRT-290's own `app._apply_ticket_assignment` directly rather than a second mechanism. One additive migration (`20260731160000_create_ticket_escalation.sql`, 1996 lines): 6 new tables, 32 new functions, RLS on all 6 tables. 4 self-found defects fixed before commit (a missing exception handler on the escalation-events idempotency insert — the natural-key unique-index-plus-handler pair only had the FIRST leg wired; a `job_type` widening regression that silently dropped the already-published `kb_article_expiry` type; a trigger referencing a nonexistent `record_version` column on a table deliberately carrying none; an overly strict suppression-expiry CHECK that blocked the legitimate later state "this row is now expired"). Implement-stage gate suite: `typecheck` 0 errors; `lint` 0/271; `pnpm run test` 4083/4083 (the sole failure, `checkWorktreeCollision`, proven pre-existing — a git-workflow-state assertion trivially true only once this commit lands); `pnpm run db:test` ALL PASSED (216 migrations); `next build` clean, both new routes present; `git:check-paths` clean (16 files, 0 forbidden). Full evidence: `docs/build-log/phase-07/HRT-291.md`.

**`CG-S12-HRT-020` (Prompt 292, Typed Ticket-Linked Records)** built the SEVENTH capability: validated typed ticket links to shipment/invoice/warehouse/vendor/customer/user records via `app.ticket_links` (a six-value CHECK-constrained entity-type registry, never a bare polymorphic pair) + `app.ticket_link_events` (its own append-only ledger, mirroring HRT-291's own decision 15 — never `app.audit_logs`). One shared per-type validation adapter, `app._ticket_link_resolve_candidate`, composes each domain's OWN existing access predicate verbatim (`app.can_access_record`, `app.check_finance_invoice_authority`, `app.evaluate_permission(...,'PRC','View')`, `app.has_active_tenant_membership` + `app.actor_holds_customer_user_layer`) rather than inventing a parallel authorization rule per domain — a link never grants access, every read independently re-checks the linked record's own domain authorization, and a forged id/cross-tenant id/deleted record/unauthorized-but-real candidate are all indistinguishable (`record_not_eligible`), closing the existence-enumeration channel the spec explicitly forbids. A new `app._ticket_link_actor_may_view_tenant_data` gate (real tenant membership, case-bound PLT-115 support grant, or `customer_user` membership) closes a genuine C-08 widening this capability would otherwise introduce: without it a Supreme Admin could browse any tenant's business data merely by opening a helpdesk ticket into that tenant. Safe snapshot is a uniform bounded `{label, detail, status}` shape, captured once for history only — every live read re-fetches fresh, never trusts the stored snapshot as currently authoritative. UI: `LinkedRecordsSection` (full six-type registry, staff/internal-requester) and `CustomerLinkedRecordsSection` (customer-safe four-type subset — no vendor/user, no existing customer-facing read path for either exists anywhere in this repository), both sections inside the existing ticket detail panels, no new route. One additive migration (`20260731170000_create_ticket_linked_records.sql`, 932 lines): 2 new tables, 11 new functions, RLS on both. 4 self-found defects fixed before commit, the most severe a genuine C-05 ordering bug (self-found live by this checkpoint's own db-test, not a separate review pass): an early draft of `app.link_ticket_record` checked for an existing active link on the natural key FIRST and returned it unconditionally on a match, BEFORE re-validating the CURRENT caller's own eligibility — concretely, a domain-denied staff member could receive an already-linked record's safe summary merely by "relinking" it, exactly the "a link grants access" violation the business rule forbids — fixed by re-ordering so the anti-enumerating eligibility check ALWAYS runs first, even on an idempotent replay. Two further self-found defects surfaced by the repository's own standing `rbac-enforcement.sql` sweep (not this migration's own review): `record_ticket_link_access_denial` had no tenant-authority check and no claimed-actor-vs-caller check, letting any authenticated identity forge ledger rows into an unrelated tenant — fixed with `assert_actor_is_session_identity` + `assert_session_identity_in_tenant`, mirroring `app.record_customer_inventory_access_denial` (ATW-242) verbatim. Implement-stage gate suite: `typecheck` 0 errors; `lint` 0/271; `pnpm run test` 4105/4105 (+22 new; the HRT-291-disclosed `checkWorktreeCollision` failure no longer reproduces, since this branch now carries real commits); `pnpm run db:test` ALL PASSED (217 migrations, `ticketing-linked-records.sql` 16/16, including the repository's own `rbac-enforcement.sql` security sweep passing after the two self-found fixes); `next build` clean; `git:check-paths` clean (15 files, 0 forbidden). Full evidence: `docs/build-log/phase-07/HRT-292.md`.

**`CG-S12-HRT-021` (Prompt 293, Sensitive Personal and Payroll Data Controls)** was the convergence hardening checkpoint over all 19 already-`VERIFIED` HR (274-285) and Ticket (286-292) Phase 7 capabilities — a corrective/hardening pass, no new capability, mirroring PRC-269's own precedent shape. An audit pass swept database/service/API/UI/job/file/search/cache/log/export/support surfaces for purpose-bound-field violations across Phase 7, live-confirmed by direct migration/registry reads (never trusted from a build log's own claim alone). **Finding A (CRITICAL)**: `app.employees`' 5 HR-narrative lifecycle-reason columns (revision/suspend/terminate/archive/leave_reason — can carry disciplinary/medical/performance narrative) were returned unconditionally by `get_employee_profile` (never wrapped in the masking guard two lines above protecting `national_id_number`/`date_of_birth`), plus exposed via a column-restricted grant that itself included them, plus a fully unrestricted grant on `app.employee_lifecycle_events` — any plain active tenant member, and separately any plain `HRS:View` holder, could read why a named colleague was suspended, terminated, or put on leave. Fixed in `20260731180000`. A self-found sibling sweep (`20260731200000`) closed the identical raw-SELECT shape on `employee_position_assignments`/`job_applications`/`interviews`/`candidate_duplicate_candidates`/`employee_duplicate_candidates` plus one UI `select("*")` replacement. **Finding B (CRITICAL)**: the previously-claimed-closed C-24 (unmasked audit-log reason leak, readable by any plain `tenant_admin` via `app.query_audit_logs`) was live across most of Phase 7 despite the taxonomy doc's own claim it was closed — 37 `capture_audit_event` call sites fixed across Employee Master, Leave, Recruitment/ATS (including a distinct raw-score vector in `record_assessment_result` the capability's own Tier C review missed), Onboarding, Attendance, and Shift/Roster (`20260731190000`); Overtime/Payroll/KPI/Training/Ticketing independently re-verified already clean. **2 HIGH fixed**: 5 new data-classification registry entries for previously-unregistered sensitive columns; the mechanical adoption gate (`check-registry.ts`) extended with an HRS mirror of the existing FIN sweep, plus a self-found gap where `PAYROLL_REGISTRY` was never wired into the combined `REGISTRY` constant. **3 Medium findings disclosed, not fixed**, each genuinely outside this checkpoint's bounded migration-plus-registry-plus-docs scope: `ISS-2026-091` (RPD-025 retention/legal-hold classification unbuilt for Phase 7 HR/payroll tables); `ISS-2026-092` (`employee_change_requests.reason`/`decided_reason` raw-SELECT exposure — same shape as Finding A but requires new service/UI-layer work, not just a migration); `ISS-2026-093` (the shared cross-domain PLT-123 `app.cancel_approval_request` primitive duplicates raw `p_reason` into `audit_logs` — a repository-wide gap outside Phase 7's own ownership, correctly left for a Platform Core follow-up). New `docs/standards/HR_PAYROLL_FIELD_POLICY_MATRIX.md`, mirroring `FINANCE_FIELD_POLICY_MATRIX.md`'s structure — the required deliverable regardless of findings triage. 3 additive migrations (`20260731180000`/`190000`/`200000`, 3312 lines combined). Self-found defects fixed before commit: a stale audit citation, 8 further C-24 sibling sites, a `RETURN QUERY` composite-cast bug, a column-REVOKE-vs-table-GRANT Postgres no-op, two false-positive test-string collisions, and the `PAYROLL_REGISTRY` wiring gap — each live-verified via `pg_get_functiondef` extraction against a fully-migrated database. Implement-stage gate suite: `typecheck` 0 errors; `lint` 0/271; `pnpm run test` 4112/4112 pass; `pnpm run db:test` 167/168 files pass — the one failure, `hris-leave-permit-business-trip.sql`, independently re-confirmed genuinely pre-existing via a fresh git-stash A/B against the pre-293 baseline (the already-registered `ISS-2026-077`, a day-of-week-relative fixture bug in HRT-280's own already-`VERIFIED` test file); `next build` clean; `git:check-paths` clean (16 files, 0 forbidden, 1 expected caution on the append-only `KNOWN_ISSUES.md` ledger); `security:check`/`security:audit`/`data-classification:check` all clean. Full evidence: `docs/build-log/phase-07/HRT-293.md`.

**The batch's own combined Tier C review (4 parallel adversarial lenses — spec-compliance; security/RLS/tenant-isolation; correctness/concurrency; cross-prompt integration and data dependency — against the combined diff `git diff 05e9106..HEAD`, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §5) found 9 CONFIRMED findings, 0 rejected**, each live-tested against a from-scratch, fully-migrated disposable Postgres 16 database with forged `request.jwt.claims`/`set role authenticated` sessions and real concurrent `psql` processes; every CONFIRMED finding survived this fix phase's own independent live re-derivation before being fixed, per §5.3 — none accepted from the register alone. Full disposition table: `docs/build-log/phase-07/HRT-293.md` §12.1.

- **CRITICAL** (security lens, `ISS-2026-094`): `app.ticket_links`/`app.ticket_link_events` raw-table SELECT granted exactly the cross-domain, cross-tenant access `app.list_ticket_links` (SECURITY DEFINER) exists to deny — live-reproduced twice: a same-tenant staff member with zero OPS/FIN/PRC permission could raw-read real invoice/vendor/shipment `safe_snapshot` content the RPC layer correctly masks, and a cross-tenant Supreme Admin holding zero grant of any kind into the target tenant could raw-read a helpdesk ticket's linked vendor record — defeating the exact scenario Prompt 292's own decision 5 says this capability was built to close. Fixed by revoking raw SELECT from `authenticated` entirely on both tables (mirrors the existing RPC-only write discipline); both RPCs independently confirmed to keep working correctly post-revoke (SECURITY DEFINER, unaffected).
- **HIGH** (integration lens, `ISS-2026-095`): 5 pre-existing HR/Recruitment RPCs (`list_employee_duplicate_candidates`, `get_employee_position_assignment_history`, `get_employee_current_assignment`, `get_application_detail`, `list_application_interviews`) silently bypassed Prompt 293's own raw-table column-restriction sweep by returning the full composite row via a `select *`-equivalent — live-reproduced with real disciplinary/PIP/background-check narrative reaching a plain `HRS:View` holder (no personal-data permission) through the RPC layer while the identical raw-table column read was correctly denied, traced to a real RSC/flight-payload reach in 3 separate UI panels. All 5 re-defined with the same `case-when-v_unmasked` masking guard the rest of Prompt 293 established.
- **MEDIUM** (correctness + spec-compliance lenses, `ISS-2026-096`): `app.escalate_ticket`'s claimed `record_version`-based double-submit protection was illusory whenever the escalation doesn't itself mutate `app.tickets` (a queue target, or an employee target without reassignment) — live-raced with two concurrent identical calls, both succeeded, producing two escalation levels and two real notifications for one logical retry. Fixed with a dedicated advisory-lock-serialized guard independent of `tickets.record_version`; `scripts/db-tests/ticketing-escalation.sql` §2 updated (it had asserted the pre-fix behavior as intended).
- **MEDIUM** (correctness lens, `ISS-2026-097`): `app.adjust_leave_balance`/`app.load_opening_leave_balance` had a real unique index and pre-check but no exception-handler third leg of the C-01/C-02 idempotency shape — a genuine concurrent retry surfaced a raw unhandled Postgres error instead of a clean replay. Fixed with the standard handler.
- **CRITICAL, partial re-assessment** (spec-compliance lens, `ISS-2026-098`, closes the raw-table half of already-disclosed `ISS-2026-093`): `app.cancel_approval_request` also writes the raw reason into `app.approval_requests.ended_reason`, readable via a pre-existing blanket grant to any active tenant member — a lower bar than the disclosed audit-log route `ISS-2026-093` itself names. The raw-table vector is fixed here (column-restricted grant); the audit-log-duplication half remains genuinely `OPEN` per `ISS-2026-093`'s own reasoning (a Platform Core PLT-123 primitive shared across every domain, correctly outside this batch's HR-scoped mandate).
- **HIGH re-assessment** (spec-compliance lens, `ISS-2026-099`, closes `ISS-2026-092`): `app.employee_change_requests`' `reason`/`decided_reason` were readable by any active tenant member via the same column-restricted-grant-still-including-the-column shape as Finding A above — closed with a new masked `app.get_employee_change_requests` RPC (self-or-`HRS:View`-personal-data) plus matching server/UI wiring, replacing the raw-table UI read.
- **Disclosed, not fixed, each genuinely outside this batch's bounded scope**: `ISS-2026-100` (`app.list_ticket_link_events` built/tested with no UI caller, C-20); `ISS-2026-101` (customer-visible escalation status has no reachable UI caller for the actual `customer_user` principal, C-20, mirrors an already-accepted HRT-289 precedent); `ISS-2026-102` (a customer viewer of `app.list_ticket_links` sees `entity_type`/`entity_id`, not `label`/`detail`, for an out-of-registry link type, Low). `ISS-2026-103` (informational, not a batch finding): two further day-of-week-dependent pre-existing `db:test` flakes found alongside the already-registered `ISS-2026-077` (`hris-overtime-timesheet.sql`, `procurement-vendor-performance.sql`), both outside this batch's own changed files, neither touched.

All fixed findings closed at root cause via one new additive migration, `supabase/migrations/20260731210000_harden_ticketing_escalation_linked_records_hris_batch_291_293_review_fixes.sql` (816 lines; zero lines of any prior migration touched). One self-found defect fixed during the fix pass itself: the new `app.get_employee_change_requests` function initially relied on schema-wide default privileges alone and was live-confirmed reachable by `public` — corrected with an explicit `revoke execute on all functions in schema app from public`, matching the `20260731160000`/`20260731170000` per-migration convention (`ERR-2026-004`).

**Gate suite independently re-run by the orchestrating session, every number matching, never accepted on the fix agent's self-report alone**: `typecheck` 0 errors; `lint` 0 errors/271 warnings (identical baseline, zero new warning class); `pnpm run test` **4113/4113 pass**; `pnpm run db:test` **ALL PASSED**, full harness end to end (with the three known day-of-week-dependent flakes — `ISS-2026-077` and the two newly-disclosed ones — temporarily set aside during the run and restored immediately after, confirmed via `git status -- scripts/db-tests/` showing zero unintended modification); `next build` clean, every route present including the two edited HR employee-detail routes; `git:check-paths` clean (12-14 files across the batch's own commits, 0 forbidden, 1 expected caution on the append-only `KNOWN_ISSUES.md` ledger); `git:check` clean; `security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`docs:check` all clean. The orchestrating session directly read the corrective migration's Fix 1 revoke statements (both tables' updated comments), all 5 re-defined Fix 2 masking guards, Fix 3's advisory-lock placement (after `_apply_ticket_escalation` returns, not before — placing it earlier would break the reassign+employee path's own already-correct version check), both Fix 4 idempotency exception handlers, Fix 5's raw-table grant narrowing, and Fix 6's new `get_employee_change_requests` function plus its own explicit `revoke ... from public` — each genuinely present and correctly shaped, matching the fix report's own claims exactly.

**Task state**: `CG-S12-HRT-019`/`020`/`021` (Prompts 291-293) are now all **`VERIFIED`** (§10), in sequence, no task skipped. Zero Critical/High-severity issue is left open anywhere in the 291-293 range at this checkpoint — every one found across all three prompts was either fixed at root cause or explicitly, honestly disclosed with its exposure stated. **This closes Batch 1 of the "lanjut 291-300" operator authorization.** `PHASE_7_VERIFIED` remains unset (reserved for Prompt 297, unchanged from §13). `CG-S12-HRT-022` (Prompt 294, HRIS/Ticketing Integrated Verification) is now dependency-clean at the task-state level — its own stated upstream, `HRT-274..293` (union of both tracks), is fully `VERIFIED` — and, unlike every prior phase/range-boundary hold recorded in this file, **requires no fresh, separate operator authorization**: it is already within the still-open "lanjut 291-300" authorization (7 of 10 prompts remain: 294-297 finish Phase 7, 298-300 start Phase 8, none yet started). Prompt 294 is genuinely standalone and must never be batched with implementation work, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.3. Full detail: `docs/build-log/phase-07/HRT-293.md` §12 ("Batch close: Tier C review"); `docs/runtime/TASK_LEDGER.md`'s `CG-S12-HRT-019`/`020`/`021` rows (updated in this same checkpoint); `docs/runtime/KNOWN_ISSUES.md` `ISS-2026-094`..`103` (new, this checkpoint).
