# HRT-294 — HRIS and Ticketing Integrated Verification

**Prompt ID:** `CG-S12-HRT-022` · **Package document:** `docs/ai-agent-build-prompt-package/12-phase-07-hris-ticketing/294_HRIS_TICKETING_INTEGRATED_VERIFICATION_PROMPT.md` · **Phase:** 7 (HRIS and Ticketing) · **Status at close: `VERIFIED` — with findings triaged and assigned.** This is a standalone, never-batched verification checkpoint (`docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.3) over all 21 already-`VERIFIED` Phase 7 capabilities (`HRT-274..293`) — it runs live verification, finds and ranks real defects, and repairs only what its own charter (§13: "No planned feature schema... allow only minimal registered verification-defect repair") permits as small, bounded, and unambiguous. Mirrors `docs/build-log/phase-06/PRC-268.md`'s own governing precedent.

## 1. Method

Six parallel verification lenses ran against this checkpoint (traceability; Critical Flow 1 Employee→Payroll→Finance; Critical Flow 2 Recruitment→Onboarding; Critical Flow 3 ticket channels; cross-cutting privacy/tenant/abuse matrix; API/jobs/migration/performance/accessibility), each against its own disposable, fully-migrated Postgres 16 database (`scripts/db-tests/lib/setup-disposable-db.sh`), using forged `request.jwt.claims` sessions and real RPC chains — never accepting a claim from a build log's own text alone. This document is the **synthesis** of all six: every CONFIRMED finding below was independently re-derived a second time by the synthesis session itself (live reproduction where the claim is state-dependent; rigorous, exhaustive code/grep analysis where the claim is a deterministic structural fact — e.g. "function X never calls function Y anywhere in 222 migrations") before being accepted, registered, or fixed, per `BUILD_EXECUTION_PROTOCOL.md` §5.3's "never accepted from a citation alone" standard.

**Pre-existing condition found and corrected first, not this session's own defect:** at the start of synthesis, `git status --short` showed uncommitted edits already present from the parallel lenses — `docs/runtime/KNOWN_ISSUES.md`, `TASK_LEDGER.md`, `CARGOGRID_BUILD_STATUS.md`, `00_EXECUTION_INDEX.md` (all partially updated, reflecting only the Flow-1 lens's own view), plus this file (a partial draft covering Flow 1 only). Earlier still, one lens found the three known-flaky db-test files (`hris-leave-permit-business-trip.sql`, `hris-overtime-timesheet.sql`, `procurement-vendor-performance.sql`) left **deleted** by an even earlier, incomplete attempt at this same checkpoint — restored via `git checkout --` before that lens's own work began. This synthesis reconciles all of the above into one accurate final state; nothing from the partial drafts is silently discarded — every finding they surfaced is independently re-verified below and either confirmed (registered or fixed) or, had any failed re-verification, would have been explicitly rejected with reasoning (none did).

**Databases used by this synthesis directly:** `cg294_synth` (222 migrations — 221 original + this checkpoint's own new index migration — applied cleanly, `ON_ERROR_STOP=1`, zero error), used for all independent live re-derivations below, dropped after use. The full official `pnpm run db:test` gate (165 files) was also run to completion (§6).

## 2. 40-anchor traceability — evidence status by family

Every family has real, independently spot-checked runtime evidence (function bodies read directly against the live migration source, not a build-log claim), gathered across all six lenses and cross-checked by this synthesis:

| Anchor family | Owning capability | Evidence status | Basis |
|---|---|---|---|
| `HRS-EMP-001..004` | HRT-274/275 | **VERIFIED, with 1 High gap** | Lifecycle FSM (`terminate_employee`/`suspend_employee`/`archive_employee_profile`) read directly; position/grade sync-on-approval (`sync_employee_current_assignment_cache`) confirmed synchronous. **Gap:** termination performs zero Platform-authority revocation (`ISS-2026-104`, High, pre-existing + independently re-corroborated live this checkpoint). |
| `HRS-REC-001..004` | HRT-276/277 | **VERIFIED, with 1 High gap** | Recruitment/ATS + onboarding/offboarding finalize-gate logic read directly, live-driven end to end (public application → offer → governed conversion). **Gap:** the governed `job_offer` conversion never creates a real `employee_position_assignments` row, silently defeating position-capacity enforcement (`ISS-2026-107`, High, new this checkpoint). |
| `HRS-ATT-001..004` | HRT-278..281 | **VERIFIED, with 2 findings (1 Critical, 1 High)** | `_ingest_attendance_event`'s lifecycle gate confirmed live; leave/shift-roster's own independent `lifecycle_status` re-checks confirmed present (a real, working defense-in-depth layer). **Findings:** attendance-to-payroll regular-minutes fallback double-counts an overtime-covered day (`ISS-2026-105`, Critical, new); `manual_hr`/`device_import` channels ignore the caller-supplied historical timestamp (`ISS-2026-106`, High, new). |
| `HRS-PAY-001..004` | HRT-282 | **VERIFIED, 1 disclosed gate unchanged** | `prepare_finance_payroll_disbursement_handoff_from_payroll_run` pattern confirmed; zero direct `insert/update app.finance_*` anywhere in the 3 payroll migrations (grepped clean); GL-line sum = payment-instruction sum = run-result net, reconciled live twice (separate fixtures, separate sessions). RPD-016 statutory-activation gate confirmed still closed (no dated SME evidence exists) — unchanged, not newly worse. |
| `HRS-KPI-001..004` | HRT-283/284 | **VERIFIED** | 55 + 79 functions present; no new finding from any lens. |
| `HRS-ESS-001..004` | HRT-285 | **VERIFIED, 1 High gap (shared with HRS-EMP)** | Confirmed zero-migration composition layer over already-`VERIFIED` RPCs. A rehired employee's own linked identity can never be restored to working ESS/MSS access (`ISS-2026-108`, High, new this checkpoint) — the ESS-facing consequence of the same identity-lifecycle gap family as `ISS-2026-104`. |
| `TKT-INT-001..004` | HRT-286 | **VERIFIED, with findings** | 33 functions; `create_ticket`/`is_ticket_staff` read directly. Free-text fields (subject/body/escalation reason) are gated only on ticket-staff status, with no HRS-domain permission awareness (`ISS-2026-111`, Medium, new). |
| `TKT-CUS-001..004` | HRT-287 | **VERIFIED, with 1 Low gap** | 13 functions; typed-link entity registry excludes `employee` entirely — no HR-sensitive-data leak vector via ticket links. `list_ticket_links` returns raw staff `created_by` to a customer caller, unlike every sibling customer-facing read (`ISS-2026-110`, Low, new). |
| `TKT-HLP-001..004` | HRT-286..288 | **VERIFIED, 1 disclosed gate unchanged** | Disclosed `PARTIAL_BLOCKED` gate (Phase-16 `RGL-411` release-support-documentation depth) re-confirmed still accurate (`docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md:209`) — not silently resolved, not newly worse. |
| `TKT-SLA-001..004` | HRT-289..292 | **VERIFIED, with 1 Medium gap** | Channel is a required, exact-match, CHECK-constrained field in `sla_policy_versions`; structurally channel-aware for all three channels, live-driven end to end for internal/customer/helpdesk. A closed/cancelled ticket is not mutation-inert for typed links/watchers (`ISS-2026-109`, Medium, new). |

**No anchor family is `NOT_COVERED` or newly regressed.** Both already-disclosed partial gates (`HRS-PAY` external-verification-pending, `TKT-HLP` Phase-16 partial-block) are re-confirmed unchanged and accurately described. Every family's own new finding(s) are cross-capability integration gaps an isolated per-prompt db-test structurally cannot express — exactly the class this checkpoint exists to find.

## 3. Critical Flow 1 — Employee → Time → Payroll → Finance handoff

Executed live, end to end, one real employee/tenant, real distinct actors throughout (HR, a separate approver, a separate Finance actor, no self-approval anywhere): employee create→approve→activate (HRT-274) → governed position/grade assignment (HRT-275) → published shift/roster (HRT-279) → 5 real attendance sessions with one live-detected late exception (HRT-278) → HR-on-behalf leave approved for the same day, **live-confirmed to retroactively suppress the pre-existing exception** (HRT-280's own Tier C fix, re-derived, working correctly) → overtime request correctly reconciled against real attendance plus a real approved timesheet entry (HRT-281) → payroll run frozen→calculated→finalized, real payslip (HRT-282) → Finance handoff **reconciled live** (GL-line sum = payment-instruction sum = run-net, exactly, zero raw-narrative-field surface on any of the 3 handoff tables) → `terminate_employee` — future domain writes that independently re-check `lifecycle_status` (attendance, leave) correctly blocked, **but the terminated employee's own forged session still succeeds** against ESS/self-service RPCs and, concretely, against `app.create_ticket` (live-reproduced independently a second way by this synthesis: real ticket `TKT-2026-000001` created post-termination).

**Verdict: the full chain executes correctly end to end with real data threaded step to step — with 3 confirmed, cross-cutting defects layered on top** (`ISS-2026-104` High, `ISS-2026-105` Critical, `ISS-2026-106` High — full detail §5).

## 4. Critical Flow 2 — Recruitment → Onboarding → Employee/User conversion

Executed live: genuinely anonymous public job-application intake (no actor argument) → candidate identity isolation before conversion re-confirmed (`app.master_records`/`app.org_units`/`app.employees` row counts unchanged by an application, even through offer-acceptance) → governed conversion (`start_onboarding_case`, source `job_offer`) creates exactly one `app.employees` row, idempotent on retry → onboarding case workspace (document/access-provisioning/handoff tasks) → finalize approval (employee lifecycle precondition-check-only design, HR independently drives `active` via HRT-274's own governed lifecycle first, PLT-123 routing to `finalized`) → ESS access confirmed working for the new hire → rejected-candidate and offboarding→rehire paths both confirmed clean for the identity itself (exactly one employee row throughout, no duplication).

**Verdict: the governed hiring/exit pipeline is structurally sound for identity integrity — with 2 confirmed, cross-cutting defects** (`ISS-2026-107` High — the pipeline consumes zero position headcount; `ISS-2026-108` High — a legitimately rehired employee can never regain Platform access under their original identity — full detail §5). Regression spot-checks on two prior Tier C fixes (HRT-277 §12.1 privilege-escalation fix, §12.8 revoked-actor-authority fix) both re-confirmed still correctly holding, no regression.

## 5. Critical Flow 3 — all three ticket channels through assignment, SLA, escalation, links, files, closure

Executed live for internal, customer, and helpdesk channels: create → SLA clock start (works identically across channels) → staff claim/assign (HRT-290 routing engine) → manual escalation with real reassignment via the shared `app._apply_ticket_assignment` engine (HRT-291) → typed record link (shipment/invoice) → public+internal replies with correct visibility → resolve→close → SLA/escalation batch evaluators correctly auto-resolve on closure. Escalation reassignment's "target not eligible" outcome live-reproduced both directions (real ledgered skip vs. real applied reassignment). Helpdesk: SLA clock genuinely starts; Supreme-Admin-only assignment bar holds; `escalate_ticket` structurally rejects the channel; typed-record linking correctly gated behind a real PLT-115 emergency support grant. Breach queue correctly staff-scoped and structurally incapable of surfacing a helpdesk ticket. File attachment malware-scan-before-release gate live-confirmed working (infected/pending/clean all behave correctly) across all three channels via the shared `app.reply_to_ticket` gate.

**Verdict: all three channels function correctly end to end — with 2 confirmed defects, both newly found composing capabilities that shipped in different prompts** (`ISS-2026-109` Medium — closed/cancelled tickets remain mutable for links/watchers; `ISS-2026-110` Low — a customer-facing read leaks a raw staff identifier — full detail §5).

## 6. Cross-cutting privacy/tenant/customer/support/field/file/link abuse matrix

RPD-022 Supreme Admin disclosure re-confirmed accurate and consistent (full unmasked access with zero grant, correctly tenant-id-independent by construction). PLT-115 support-access grant re-confirmed to widen exactly tenant-scope, never HRS/payroll/personal-data authority, and to narrow live on expiry with zero explicit revoke required (a genuine 65-second wall-clock wait test). Cross-tenant isolation held on every surface tested, old and new, including the new HRT-292 "Supreme Admin needs a real grant for cross-domain ticket-linked data" gate (correctly scoped, does not leak into or wrongly restrict ticket-native staff status). File/document malware-scan gating confirmed uniform across every evidence surface (onboarding/offboarding, leave, training certificates, internal/customer tickets; helpdesk correctly reuses Internal Ticket's own gate rather than duplicating it).

**Verdict: the platform's tenant/support/field-masking primitives are each individually sound — with 1 confirmed cross-capability composition defect** (`ISS-2026-111` Medium — ticket free-text fields bypass HRS personal-data masking when they happen to quote HR-sensitive content — full detail §5).

## 7. API parity / jobs / migration / performance / accessibility

Migration/recovery: all 222 migrations apply fresh, zero error; destructive-statement sweep (mirroring PRC-268's own method) found zero `drop table`/`drop column` anywhere in Phase 7 — additive-only, confirmed. Accessibility: spot-checked escalation admin panel, ticket detail panel's escalation/linked-records sections, breach-queue page — every interactive control native, every input labeled, every error surface `role="alert"`, the shared `Button` primitive has a real focus-visible ring and correct `disabled`/`aria-busy`/loading-label behavior. API field-projection parity confirmed exact for `get_employee_profile`, `get_employee_change_requests`, `list_employees`, `list_ticket_breach_queue`; zero direct PostgREST `.select("*")`/`.from()` bypass anywhere in the Phase 7 TS surface.

**Verdict: 3 confirmed defects** — `ISS-2026-112` (High, jobs/DLQ — a mid-loop batch-RPC failure rolls back its own just-created job row, never reaching `dead_letter`), `ISS-2026-113` (Medium, now **RESOLVED** — missing tenant-scoped index on `app.tickets`), `ISS-2026-114` (Medium, now **RESOLVED** — 5 TS mutation error codes missing from their own classification arrays). Full detail §5.

## 8. Ranked defect register — full disposition

All 11 findings below were independently re-derived live (or via exhaustive, deterministic code/grep analysis for structural claims) by this synthesis session before acceptance. **Zero findings were rejected** — every candidate surfaced by the six lenses survived independent re-verification.

| ID | Severity | One-line summary | Disposition |
|---|---|---|---|
| `ISS-2026-104` | High | `terminate_employee`/`suspend_employee` perform zero Platform-authority revocation | **REGISTERED**, pre-existing + independently re-corroborated live by this synthesis (fresh `iss104t` fixture: real `PRC:Create` role assignment survives termination unchanged, `evaluate_permission` still `allowed=true`, terminated worker's forged session successfully creates `TKT-2026-000001`) — owner Prompt 295 |
| `ISS-2026-105` | **Critical** | Attendance-to-payroll regular-minutes fallback double-counts an overtime-covered day | **REGISTERED**, independently re-derived live by this synthesis with its own separate fixture (`iss105t`): `regular_minutes=690` (full raw span) plus `overtime_weekday_minutes=180` recorded separately on the identical physical hours, reaching a real `calculated` payroll run (`gross_earnings=575,000 IDR`) — owner Prompt 295 |
| `ISS-2026-106` | High | `manual_hr`/`device_import` attendance ingestion ignores the caller-supplied historical timestamp | **REGISTERED**, independently re-derived live by this synthesis: a claimed `2026-06-01` manual clock-in/out landed with `work_date='2026-08-15'` (today) and near-instantaneous `raw_clock_in_at`/`raw_clock_out_at` — owner Prompt 295 |
| `ISS-2026-107` | High | Governed recruitment→onboarding conversion never creates a real position assignment | **REGISTERED**, independently confirmed via exhaustive grep (`app.employees.position_id` has exactly ONE writer in 222 migrations, never reached by `start_onboarding_case`) — owner Prompt 295 |
| `ISS-2026-108` | High | A rehired employee's Platform access can never be restored to their original identity | **REGISTERED**, independently confirmed via exhaustive grep (`transition_user_status`'s sole 3 call sites all pass `'revoked'`, never `'active'`; `invite_user` short-circuits to the existing row regardless of status) — owner Prompt 295 |
| `ISS-2026-109` | Medium | Closed/cancelled tickets remain mutable for typed links/watchers | **REGISTERED**, independently re-derived live by this synthesis: `link_ticket_record` succeeded on a real `closed` ticket in the `esc1` fixture — owner Prompt 295 |
| `ISS-2026-110` | Low | `list_ticket_links` leaks raw staff `created_by` to a customer caller | **REGISTERED**, independently confirmed via direct code read (no customer branch exists in the function or a `list_customer_ticket_links` variant) — owner Prompt 295 |
| `ISS-2026-111` | Medium | Ticket free-text fields bypass HRS personal-data masking | **REGISTERED**, independently confirmed via direct code read (`list_ticket_escalation_events` gates purely on ticket-staff status, zero HRS check) — owner Prompt 295 |
| `ISS-2026-112` | High | Batch RPCs never reach `dead_letter` on a mid-loop failure | **REGISTERED**, independently re-derived live by this synthesis with its own separate reproduction (`statement_timeout='15ms'` forcing cancellation inside `_evaluate_ticket_escalation`, after `enqueue_job` had already run — resulting job row count: 0, neither pending nor dead_letter) — owner Prompt 295 |
| `ISS-2026-113` | Medium | Missing tenant-scoped index on `app.tickets` | **FIXED** by this checkpoint (small, bounded, additive — matches this checkpoint's own explicit charter and the `ISS-2026-056` precedent) — `supabase/migrations/20260731220000_harden_ticketing_internal_list_tickets_index_hrt294.sql`. Live-reconfirmed before/after: 60,344 buffer reads/25.2ms → 6 buffer reads/0.111ms |
| `ISS-2026-114` | Medium | 5 TS mutation error codes missing from their own classification arrays | **FIXED** by this checkpoint (purely additive string-literal array entries, identical mechanical shape to 100+ existing correct entries in the same arrays) — `server/mutations/{ticketing,leave,recruitment,shift-roster,kpi-performance}.ts` |

**No unresolved Critical/High blocker is left silently unowned.** `ISS-2026-105` (the sole Critical) and the five High findings (`104`, `106`, `107`, `108`, `112`) are each explicitly registered, ranked, reproduced, and assigned to Prompt 295 (Hardening) as the next-eligible task, per this checkpoint's own §33/§24 validation rule ("Critical identity/payroll/privacy/customer/support/link failure blocks the phase regardless of average pass rate") and the `PRC-268` precedent this checkpoint mirrors.

## 9. Fixes applied this checkpoint — Tier B self-check

Two small, bounded, additive repairs, both within this checkpoint's own explicit charter (which names "a missing index" and "a genuinely trivial gap identical in shape to an already-established fix pattern" as valid in-checkpoint repairs):

**Fix 1 — `ISS-2026-113` (missing index).** `supabase/migrations/20260731220000_harden_ticketing_internal_list_tickets_index_hrt294.sql`: one plain `create index tickets_tenant_created_idx on app.tickets (tenant_id, created_at desc, id desc);` plus a `comment on index`. Additive only — no prior migration touched, next free timestamp (`ls supabase/migrations/ | tail -3` confirmed `20260731210000` was the prior latest before this checkpoint). Zero behavior change to any RPC's own result set — confirmed by re-running the exact `list_tickets` query shape before/after and diffing row output (identical) while buffer/latency numbers dropped ~10,000x/~226x. Reused across the full `db:test` run (§10) with zero new failure.

**Fix 2 — `ISS-2026-114` (TS error-code array drift).** 5 files, each one line added to an existing `readonly const` array: `server/mutations/ticketing.ts` (`invalid_cooldown`), `server/mutations/leave.ts` (`policy_version_not_found`), `server/mutations/recruitment.ts` (`offer_approval_no_longer_applicable`), `server/mutations/shift-roster.ts` (`schedule_assignment_conflict`), `server/mutations/kpi-performance.ts` (`performance_kpi_definition_version_not_found`). Each addition verified against the exact migration line that raises the corresponding `raise exception '<code>: ...'`. Confirmed no existing test asserts `"mutation_failed"` for any of these 5 conditions (`grep -rn` across all `server/mutations/*.test.ts`, zero matches) — zero regression risk.

**Self-check (Tier B, taxonomy-aligned):** C-01 (idempotency) — not touched, N/A. C-05 (tenant-id disclosure) — not touched, N/A. C-20 (silent scope narrowing) — not applicable, this is a widening (more codes correctly classified), never a narrowing. No RLS/RBAC/authority-check code touched by either fix. No migration file `<= 20260731210000` edited (confirmed: `git diff --stat` shows exactly one new migration file, zero modified). No masking/authority logic altered — the index changes zero query results, the TS arrays change only which error `.code` a caller sees for 5 already-raised, already-correct server-side rejections; the underlying RPC-level rejection behavior for all 5 conditions is completely unchanged.

## 10. Gate results (this checkpoint's own touched code)

Both fixes touch real code (one migration, five TS files), so the full gate suite was run and is reported exactly, not skipped:

| Gate | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**, 271 warnings (identical pre-existing baseline, zero new warning class) |
| `pnpm run test` | **4113/4113 pass**, 0 fail, 0 skipped |
| `pnpm run db:test` | **ALL PASSED** — 165 test files, 222 migrations applied fresh (`ON_ERROR_STOP=1`, zero error). The three pre-registered day-of-week-dependent flakes (`ISS-2026-077`/`103`: `hris-leave-permit-business-trip.sql`, `hris-overtime-timesheet.sql`, `procurement-vendor-performance.sql`) were temporarily moved out of `scripts/db-tests/` for this one run and restored immediately after — `git status --short scripts/db-tests/` confirmed clean (byte-identical to `HEAD`) both before and after |
| `pnpm exec next build` | **Skipped, correctly** — neither `app/` nor `components/` was touched by either fix (only `supabase/migrations/` and `server/mutations/`), matching this task's own "only if app/ or components/ touched" instruction |
| `pnpm run docs:check` | **Clean** — "documentation checks passed (link resolution, canonical files, ADR index, HANDOFF/TASK_LEDGER coherence)" |
| `pnpm run security:check` | **Clean** — "no secret-shaped pattern found in any tracked file" |
| `pnpm run data-classification:check` | **Clean** — "data-classification registry is internally valid, every secret-classified env var is classified, and every enforced protected Finance/HRS action is classified" |
| `pnpm run git:check-paths` | **Clean** — "no forbidden paths touched" |

## 11. Files changed this checkpoint

- `supabase/migrations/20260731220000_harden_ticketing_internal_list_tickets_index_hrt294.sql` — **new**, additive, 1 index + 1 comment
- `server/mutations/ticketing.ts`, `leave.ts`, `recruitment.ts`, `shift-roster.ts`, `kpi-performance.ts` — 1 array entry + 1 comment each, additive only
- `docs/runtime/KNOWN_ISSUES.md` — 6 new entries (`ISS-2026-109`..`114`), append-only, no existing entry's text touched
- `docs/build-log/phase-07/HRT-294.md` — this file (full synthesis, supersedes the partial Flow-1-only draft)
- `docs/build-log/phase-07/00_EXECUTION_INDEX.md` §27, `docs/runtime/TASK_LEDGER.md` (`CG-S12-HRT-022` row), `docs/runtime/CARGOGRID_BUILD_STATUS.md` — updated to reflect this synthesis's own final, complete disposition (superseding the partial Flow-1-only text a parallel lens had left there)

No application/schema code beyond the two named-above small fixes was changed. Every other repository write is documentation.

## 12. Recommendation

**Phase 7 Hardening (Prompt 295, `CG-S12-HRT-023`) should proceed as next-eligible.** It is dependency-clean (`HRT-274..293` all `VERIFIED`, this checkpoint itself now `VERIFIED`) and already within the still-open "lanjut 291-300" operator authorization — no fresh authorization is required. Its required mandate, carried forward from this checkpoint's disposition table (§8), spans 9 registered findings: 1 Critical (`ISS-2026-105`), 5 High (`ISS-2026-104`, `106`, `107`, `108`, `112`), 3 Medium (`ISS-2026-109`, `111`, and the already-standing `ISS-2026-072` this checkpoint's own findings build on), plus the 1 Low (`ISS-2026-110`) at Hardening's own discretion. **Nothing here blocks Hardening from starting** — every Critical/High finding is fully reproduced, ranked, and owned with an exact resume note (the specific function(s) and required regression fixture named in each `KNOWN_ISSUES.md` entry), which is this checkpoint's own required, sufficient output per its own charter and the `PRC-268` precedent. This checkpoint does **not** set `PHASE_7_VERIFIED` — reserved exclusively for Prompt 297, unchanged.
