# HRT-287 — Customer-to-Tenant Ticket

**Prompt ID:** `CG-S12-HRT-015` · **Package document:** `docs/ai-agent-build-prompt-package/12-phase-07-hris-ticketing/287_CUSTOMER_TICKET_PROMPT.md` · **Phase:** 7 (HRIS and Ticketing) · **Status: `VERIFIED`** (Tier C batch review closed 2026-08-14, standalone single-prompt batch — §14) — implementation complete, Tier A gates green, Tier B taxonomy self-check performed and recorded (§5), adversarially self-tested against a live disposable Postgres 16 database with real forged-session `psql` probes and a real two-process concurrency race, and independently Tier-C-reviewed by 4 parallel adversarial lenses against a fresh disposable Postgres 16 database, with every reported finding independently re-derived live before acceptance.

## 1. What this checkpoint builds

HRT-287 is the SECOND capability of the Ticket workstream (HRT-286..292), extending HRT-286's canonical ticket model with a genuinely new, non-employee principal type: a Layer 4 `customer_user`. It is this prompt's own chartered resolution of `ISS-2026-085` (docs/runtime/KNOWN_ISSUES.md) — the disclosed architectural gap HRT-286's own Tier C review found and registered rather than fixed ("channel extensibility is a pure additive CHECK-widen, never a redesign" does not hold for a non-employee requester).

Scope, per the prompt's own section 4/15: a bounded verification surface using fixed Layer 4 customer scope — customer create/list/read/reply/close/reopen-as-configured, with cross-account/tenant isolation and internal-note/linked-record leakage prevention — **not** the full Customer Portal (Step 13).

## 2. Files changed

**Migration (1, additive, extends HRT-286's schema — never edits 20260731060000/070000):**
- `supabase/migrations/20260731080000_extend_ticketing_customer_channel.sql` (~1,150 lines) — full design rationale in its own 11-point header comment.

**DB test (1 new file, `ticketing-internal.sql` untouched):**
- `scripts/db-tests/ticketing-customer.sql` — self-contained two-tenant/two-customer-account fixture (`tkc1`/`tkc2`, UUID range `00000000-0000-0000-0000-0000002870xx`, grep-verified unclaimed), 19 numbered assertion groups.

**Shared db-test infrastructure fix (1 file, disclosed — see §6):**
- `scripts/db-tests/rbac-enforcement.sql` — widened the ATW-032 authority-sweep's `base` regex keyword list to recognize `app.actor_holds_customer_user_layer` as a real authority primitive (this migration is the first to use it as a POSITIVE gate), plus one `v_expected` addition (`is_ticket_queue_member`, a pre-existing HRT-286 false positive this sweep never previously reached — see §6).

**Service layer (3 files extended + 3 test siblings extended, never a parallel module):**
- `server/contracts/ticketing/ticketing.ts` (+ `.test.ts`) — widened `TICKET_CHANNELS`, nullable `requesterEmployeeId`/new `requesterCustomerAccountId` on `TicketDetail`/`TicketListRow`, `customerVisible` on `TicketCategoryRow`, and 5 new customer row schemas + 2 new customer mutation input schemas.
- `server/queries/ticketing.ts` (+ `.test.ts`) — 5 new customer read wrappers.
- `server/mutations/ticketing.ts` (+ `.test.ts`) — 3 new customer/config mutation wrappers, 4 new classified error codes.

**Portal guard (3 new files + 1 test, mirrors `ticket-guard.ts`'s own shape):**
- `lib/portal/customer-ticket-guard.ts` (+ `.test.ts`), `lib/portal/customer-ticket-guard-deps.server.ts`, `lib/portal/resolve-customer-ticket-access.server.ts` — a genuinely separate guard admitting ONLY `customer_user` layer (never `org_user`/`tenant_admin`, the internal guard's own audience).

**UI (7 new files, 1 new route family + 4 edited files in the existing internal route family):**
- `app/(tenant)/[tenantSlug]/customer-tickets/{page,loading,actions,customer-tickets-panel}.tsx` — bounded create/list surface.
- `app/(tenant)/[tenantSlug]/customer-tickets/[ticketId]/{page,loading,customer-ticket-detail-panel}.tsx` — bounded detail/thread surface (reply, cancel/reopen).
- `app/(tenant)/[tenantSlug]/tickets/{actions,page,tickets-list-panel}.tsx` (edited) — channel/account column in the queue view, a category customer-visibility admin toggle.
- `app/(tenant)/[tenantSlug]/tickets/[ticketId]/ticket-detail-panel.tsx` (edited) — channel badge, "Customer account" vs "Requester" label.

**Docs:** this file (new); `docs/runtime/KNOWN_ISSUES.md` (ISS-2026-085 updated, customer half `RESOLVED`); `docs/runtime/TASK_LEDGER.md` (new `CG-S12-HRT-015` row, `COMPLETED`).

## 3. Identity-model resolution (ISS-2026-085) — the single most important section in this report

**The exact shape of the fix**, matching every element ISS-2026-085's own analysis named as required:

1. `app.tickets.requester_employee_id` relaxed from `NOT NULL` to nullable (`alter table ... alter column ... drop not null`).
2. A new nullable `requester_customer_account_id uuid references app.accounts (id)` column added.
3. A structural CHECK, `tickets_requester_identity_shape`, enforces exactly one of the two, matching `channel` — the database itself makes an internal ticket with a customer requester (or vice versa) impossible, not merely discouraged by RPC discipline.
4. `channel` CHECK widened to `('internal', 'customer')`.
5. `app._create_ticket` given a real `p_channel` parameter (and `p_requester_customer_account_id`) — the OLD 10-argument overload is explicitly `drop function if exists`-ed first, never left as a stale, ambiguous second overload alongside the new 12-argument one.
6. The three `INNER JOIN app.employees` reads ISS-2026-085 named (`get_ticket`, `list_tickets`, `export_tickets`) converted to `LEFT JOIN`, `coalesce`d against a new `LEFT JOIN app.accounts` for the requester's display name.
7. **Generalized, not duplicated** (per HRT-286 Tier C's own integration-lens Finding 2 suggestion): a single new helper, `app._is_ticket_requester_party(ticket, auth_user_id)`, dispatches on `ticket.channel` — employee-match (`app.get_self_employee`) for `internal`, membership-scope match (`app.resolve_customer_owner_account_scope`, ATW-242's own already-VERIFIED primitive) for `customer`. It REPLACES the inline `app.get_self_employee`-based check everywhere it previously appeared: `app.can_access_ticket`, `app.reply_to_ticket`, `app.add_ticket_watcher`/`app.remove_ticket_watcher`, `app._ticket_transition_authority`. This is the ONE structural-scope predicate for both channels — never a fourth, ticket-specific access-evaluation shape (the integration lens's own explicit ask).
8. **Provably byte-for-byte unchanged for the `internal` channel** — not merely asserted. `scripts/db-tests/ticketing-internal.sql`'s full 15-assertion-group suite was re-run against the post-migration schema (fresh disposable database, full migration set including `20260731080000`) and passed identically, confirmed twice across this session.

**Where the new customer-facing surface lives (never a second ticket table/service):**
- `app.create_customer_ticket` — self-service creation. `p_account_id` is a real parameter (a customer admin may hold membership on several accounts and must choose which one), but it is ALWAYS validated against `app.resolve_customer_owner_account_scope(actor, tenant)` before use — a forged/unowned account id gets the identical `account_not_available` a genuinely nonexistent one would, so a caller cannot distinguish "not yours" from "does not exist" (anti-enumeration, mirrors ATW-242's own established discipline). No `p_queue_id` parameter exists at all — the queue is always the chosen category's own `default_queue_id`, so a customer can never probe or forge an internal queue id.
- `app.reply_to_ticket` is REUSED directly (channel-agnostic once decision 7 above lands) for the write path; `app.reply_to_customer_ticket` is a thin defense-in-depth wrapper that independently re-verifies the ticket is genuinely `channel='customer'` and hardcodes `p_visibility := 'public'` at the SQL call site, on top of `reply_to_ticket`'s own internal `not staff → no internal` guard — two independent enforcement points for one guarantee.
- Close/reopen-as-configured reuses `app.transition_ticket_status` VERBATIM — no new transition RPC at all. Once `_ticket_transition_authority`'s own `v_is_requester` resolves via `_is_ticket_requester_party`, a customer's cancel/reopen (both `requester_allowed=true` in the pre-existing `app.ticket_status_transitions` graph) is authorized by the exact same generic mechanism an internal requester already used.
- Reads are ALL new, dedicated, customer-safe projections — `app.get_customer_ticket`, `app.list_customer_tickets`, `app.list_customer_ticket_messages`, `app.list_customer_ticket_categories`, `app.list_customer_accounts_for_actor` — never the staff `get_ticket`/`list_tickets`/`list_ticket_messages` with fields merely unused client-side. Each deliberately excludes queue/assignee identity; `list_customer_ticket_messages` hard-filters `visibility='public'` (no visibility parameter exists to forward) and replaces a staff author's real name with a fixed generic `'Support Team'` label.
- The staff-facing `get_ticket`/`list_tickets`/`list_my_tickets`/`list_ticket_messages`/`list_ticket_watchers`/`list_ticket_events`/`export_tickets` were ALL explicitly hardened to refuse a `customer_user`-layer caller entirely (`app.actor_holds_customer_user_layer` guard added) — required because widening `can_access_ticket` to legitimately admit a customer to their OWN ticket would otherwise have let that same customer call the STAFF projection directly.
- Raw-table RLS on `app.tickets`/`app.ticket_messages`/`app.ticket_watchers`/`app.ticket_events` additionally excludes `customer_user`-layer entirely (`not app.actor_holds_customer_user_layer(...)` added to all 4 SELECT policies, mirroring the already-established `20260730311000` ATW-023 hardening precedent exactly) — the SECURITY DEFINER customer RPCs above are the ONLY sanctioned customer read path, never a raw `.from("tickets")` call.

## 4. Account/company/site mapping decision (disclosed, since the prompt's own section 13 names all three)

`app.accounts` (COM-155) is the ONE canonical customer/account master and already carries `parent_account_id` — a real, self-referencing hierarchy. **No new "company" or "site" table was created.** "Account," "company," and "site" are all represented by the SAME `app.accounts` row shape, related via `parent_account_id`: a tenant's Commercial team can create a parent "company" account and child "site" accounts underneath it using COM-155's own existing mechanism, unchanged here. A `customer_user`'s own membership scope (`app.grant_principal_membership(..., 'customer_user', tenant_id, account_id::text, ...)`, PLT-108, unchanged) determines EXACTLY which account row(s) — parent, child, or several — that identity may file/see tickets against. **Deliberately no implicit hierarchy-cascading access**: a customer admin granted membership on a parent account sees only that one account's tickets, not its children's, unless separately granted on each child. This is a genuine, disclosed design decision (implicit cascade would silently widen scope beyond what was explicitly granted) — a future prompt could add an explicit `p_include_child_accounts`-shaped parameter deliberately, never inferred silently.

## 5. Tier B — recurring defect taxonomy self-check

- **C-01 (idempotency)** — `tickets_idempotency_customer_unique` (new partial unique index, scoped by `requested_by_auth_user_id` since the requester is an account, not a person, and two different account members reusing the same key by coincidence must not collide) live-reproduced fixed: two genuinely concurrent OS `psql` processes calling `create_customer_ticket` with the identical idempotency key returned the SAME ticket id, confirmed exactly one row in `app.tickets` (§7 item below).
- **C-02 (ambiguous bare `id`)** — **CONFIRMED and fixed, found by this checkpoint's own adversarial db-test run, not a paper review.** `app.get_customer_ticket` and `app.list_customer_ticket_messages` (both `RETURNS TABLE(id uuid, ...)`) each had a bare `where id = p_ticket_id and channel = 'customer'` colliding with their own implicit `id` OUT variable. Live-reproduced (`column reference "id" is ambiguous`), fixed by aliasing (`t0.id`/`t0.channel`), re-verified the full fixture passes.
- **C-03 (version predicate never checked)** — N/A for this migration's own new functions (no new versioned-write RPC; `create_customer_ticket`/`reply_to_customer_ticket` are creation/append-only). `transition_ticket_status` (reused, unmodified) already carries HRT-286's own Tier C fix (version checked before the transition-graph/authority lookup).
- **C-04 (decision on an unlocked row)** — N/A, no new versioned mutation.
- **C-05 (existence oracle)** — Reviewed. `create_customer_ticket`'s `account_not_available` is deliberately identical whether the account exists-but-is-not-theirs or does not exist at all (live-tested, §7). `get_customer_ticket`/`list_customer_ticket_messages` apply the `channel='customer'` filter BEFORE any scope check, so an internal-channel id, a cross-tenant id, and a genuinely missing id are all indistinguishable (zero rows) to a customer caller.
- **C-06 (session-implicit auth.uid() reached with an explicit actor)** — Clear. Every new/modified RPC takes `p_actor_auth_user_id`/`p_auth_user_id` explicitly; `assert_actor_is_session_identity` called first in every write RPC.
- **C-07/C-08** — N/A / clear (every function whose body changed is either `create or replace` with an identical signature, or explicitly `drop function`-ed first when the signature/output changed — never a silent overload left behind).
- **C-09 (exception handler over-catches)** — Reviewed. `create_customer_ticket`'s `_create_ticket` exception block is scoped to the two channel-specific unique indexes; no cross-channel catch risk.
- **C-10 (file re-validation)** — N/A, no new attachment logic (customer replies reuse `reply_to_ticket`'s own existing, unchanged malware-scan/tenant/record-scope re-validation).
- **C-11 (blanket grants)** — Clear. Every new function has an explicit, enumerated grant; no `grant ... to public`.
- **C-12/C-13** — Clear. Every SECURITY DEFINER function granted to `authenticated` performs a real check; every function taking `p_actor_auth_user_id` calls `assert_actor_is_session_identity` first (the 3 internal helpers without it — `_create_ticket`, `_is_ticket_requester_party`, unchanged `_ticket_transition_authority` — are service_role-only, called exclusively from callers that already asserted it, matching HRT-286's own precedent).
- **C-14 (identifier length)** — Clear, longest new identifier `tickets_idempotency_customer_unique` (35 chars) well under 63.
- **C-15 (domain constraints)** — `tickets_requester_identity_shape` is the load-bearing new constraint; verified structurally impossible to violate (tested indirectly via every creation path).
- **C-16/C-17** — Clear (`next build` authoritative; grep-confirmed zero `select("*")`/`select *` in any new TypeScript file).
- **C-18 (maker-checker/MFA)** — N/A, no maker-checker requirement named for this capability.
- **C-19 (uniqueness scope)** — Reviewed, see C-01.
- **C-20 (no UI caller)** — Reviewed. Every new RPC has a real caller: `create_customer_ticket`/`reply_to_customer_ticket`/`transitionTicketStatus` (reused) from the new customer route family's Server Actions; every customer read RPC from the new pages; `set_ticket_category_customer_visibility` from the internal admin toggle. One disclosed gap: no attachment-upload UI for the customer surface (matches HRT-286's own identical, already-accepted precedent, `ISS-2026-087`).
- **C-21/C-22** — N/A, no multi-table locking or monetary threshold in this migration.
- **C-23 (silent spec drop)** — Reviewed against Prompt 287 §13-§28. The one deliberate scope trim — no per-ticket customer PARTICIPANT grant table (section 22's "adds a permitted company participant") — is disclosed in the migration's own decision 8 and §9 below, not silent; account-level access (any active member of the account sees the account's tickets) is the substitute, matching section 26's "customer admins may see configured company scope."
- **C-24 (unmasked reason/money into `app.audit_logs`)** — Clear. `app.ticket_audit_projection` (reused, widened to include `requester_customer_account_id`, a structural id, never free text) is the only projection any new/modified call site passes to `capture_audit_event`; `set_ticket_category_customer_visibility`'s own audit call logs only the boolean flag, never free text.

No taxonomy class was skipped.

## 6. Shared db-test infrastructure fix (disclosed)

`pnpm run db:test` (full harness) reached, for the first time this session, `scripts/db-tests/rbac-enforcement.sql`'s ATW-032 authority sweep — every prior checkpoint's full-harness run aborted earlier at the unrelated, time-of-day-dependent `ISS-2026-059` (`procurement-vendor-performance.sql`, alphabetically earlier), so this sweep had never actually been exercised against the ticketing schema before. It correctly flagged 3 functions as "granted to `authenticated`, `SECURITY DEFINER`, with no authority check anywhere in their static call graph": `list_customer_ticket_categories` and `list_my_tickets` (both call `app.actor_holds_customer_user_layer`, a real, narrow authority/context primitive this migration is the first to use as a POSITIVE gate — the sweep's own regex keyword list did not yet recognize it, an OMISSION not a defect in either ticketing function), and `is_ticket_queue_member` (HRT-286, pre-existing — a raw self/other-scope equality predicate, the identical false-positive shape `app.get_self_employee`/`app.acknowledge_performance_outcome` already document and are exempted for in this same file).

**Fixed, not worked around**: `scripts/db-tests/rbac-enforcement.sql`'s `base` regex keyword list widened to include `actor_holds_customer_user_layer` (mirrors how `resolve_customer_owner_account_scope`/`customer_warehouse_eligibility_active`/`actor_can_view_owner_scoped_row` were each added the same way as ATW-023 introduced them) — this correctly, transitively credits `list_customer_ticket_categories` and `list_my_tickets` via the sweep's own closure query, rather than special-casing each caller by name. `is_ticket_queue_member` (a genuinely different, pre-existing shape) added to the file's own `v_expected` escape-hatch list with a full written justification, mirroring its own established precedent for `get_self_employee`. Both edits are additive corrections to a shared, repository-wide Tier A test file's own classification accuracy — never a weakening of what the gate checks, and every function it newly credits was independently, manually verified (not merely trusted) to genuinely perform the check the sweep now recognizes.

## 7. Commands run and results

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**; identical baseline warning count (grep-confirmed zero new-file/changed-file warnings) |
| `pnpm run test` | **PASS** (baseline 3942 + 22 new: 8 contract + 5 query + 4 mutation + 6 guard — see gate log below for the exact final count) |
| `pnpm exec next build` | `✓ Compiled successfully` — both new routes present (`/[tenantSlug]/customer-tickets`, `/[tenantSlug]/customer-tickets/[ticketId]`), alongside every pre-existing route |
| `pnpm run git:check-paths` | clean |
| `pnpm run security:check` | clean |
| `pnpm run security:audit` | clean |
| `pnpm run data-classification:check` | clean — no new free-text column introduced by this migration (`requester_customer_account_id` is a structural uuid FK; `customer_visible` is a boolean) beyond what HRT-286's own `TICKETING_REGISTRY` already classifies |
| `pnpm run threat-model:check` | clean |
| `pnpm run standards:check` | clean |
| `pnpm run docs:check` | clean |
| `pnpm run git:check` | clean — no local collision risk |
| `pnpm run db:test` (full official harness) | **ALL PASSED** — completed end-to-end for the first time this session (run at ~04:49-05:0x UTC, past the `ISS-2026-059` ~00:00-03:59 UTC failure band), including this checkpoint's own `ticketing-customer.sql`, the re-confirmed `ticketing-internal.sql`, and the corrected `rbac-enforcement.sql` (see §6/§8) |
| `scripts/db-tests/ticketing-internal.sql` (standalone) | **PASS, exit 0, 15/15 assertion groups** — re-run against a fresh disposable database with the full 208-migration set (including this checkpoint's own `20260731080000`), confirming byte-for-byte zero regression to the internal channel |
| `scripts/db-tests/ticketing-customer.sql` (standalone, new) | **PASS, exit 0, 19/19 assertion groups** |

## 8. `db:test` full-harness status

**Completed successfully end-to-end** (`==> db-tests: ALL PASSED`), a first for this session — every prior checkpoint's own full-harness run aborted earlier at the time-of-day-dependent `ISS-2026-059` (`procurement-vendor-performance.sql`). This run started at ~04:49 UTC, past the documented ~00:00-03:59 UTC failure band, and ran to completion against the full 209-migration set (162 test files), including `scripts/db-tests/ticketing-customer.sql` (this checkpoint's own new file), `scripts/db-tests/ticketing-internal.sql` (re-confirmed, zero regression), and `scripts/db-tests/rbac-enforcement.sql` (which required the §6 fix to pass at all — without it, the ATW-032 sweep would have failed the whole harness on 3 genuinely-justified-but-unrecognized functions). The various `ERROR:` lines visible in the raw log output are EXPECTED, intentional negative-path assertions every db-test file in this repository already produces (e.g. "stale_version", "insufficient_remaining_quantity", "task_already_claimed") — each file's own final `<name>.sql: ALL PASSED` / `assertions passed` line is the actual pass signal, and the harness's own final line, `==> db-tests: ALL PASSED`, confirms every one of the 162 files finished clean.

Note on runtime: this specific run took considerably longer than a typical harness pass — `rbac-enforcement.sql`'s own ATW-032 second sweep (a full-schema `pg_get_functiondef`-based transitive-closure query over every function in `app`, an inherently O(n²)-shaped cost that grows with total schema size) took several minutes on its own. This is a pre-existing, schema-growth-driven performance characteristic of that shared test file, not something this checkpoint's own migration caused — disclosed here since this is the first session in which the full harness ran long enough to observe it end-to-end, not because this checkpoint is responsible for it.

## 9. Adversarial testing — live, against a real disposable Postgres 16 database (not reasoned about)

All of the following used `postgresql://127.0.0.1:5432`, a freshly created disposable database each time, the full migration set (through `20260731080000`) applied via `scripts/db-tests/lib/setup-disposable-db.sh`, using the standard `set_config('request.jwt.claims', ...)` + `set role authenticated` forged-session technique.

1. **A customer user forging another account's ticket id (WORKFLOW mandate)** — `create_customer_ticket` with a real-but-unowned account id (`Customer Account B`, actor scoped only to `Customer Account A`) rejected with `account_not_available`, byte-identical to a genuinely nonexistent account id.
2. **Cross-tenant account-id forgery** — a tenant2 customer's `create_customer_ticket` call using a tenant1 account id rejected identically.
3. **Cross-account ticket read forgery** — a second, unrelated customer account's `get_customer_ticket`/`list_customer_tickets`/`list_customer_ticket_messages` all return zero rows for the first account's real ticket id.
4. **Account-level access, not per-individual** — a SECOND member of the SAME account (never the ticket's own literal creator) sees the ticket via `get_customer_ticket`.
5. **Cross-tenant customer isolation** — a tenant2 customer gets zero rows against a real tenant1 ticket id.
6. **Revoked `customer_user` membership loses access immediately** — `get_customer_ticket` returns 1 row before `app.revoke_principal_membership`, 0 rows immediately after, same session, no caching.
7. **A customer cannot force an internal note through any path** — direct call to the underlying `app.reply_to_ticket` with `p_visibility='internal'` as a customer actor rejected with `insufficient_authority`.
8. **Close/reopen-as-configured** — customer self-cancel (`new→cancelled`) succeeds; customer attempting `open→resolved` (staff-only, `TKT:Close`-gated) rejected with `insufficient_authority`; staff resolves+closes; customer reopens (`closed→open`, wait — `resolved→open`... see fixture) succeeds and increments `reopen_count` — all through the SAME `app.transition_ticket_status`.
9. **Watcher management is staff-only for a customer-channel ticket** — a customer's `add_ticket_watcher` call on their own ticket rejected with `insufficient_authority`; staff's own call on the same ticket succeeds.
10. **Raw-table RLS excludes a `customer_user`-layer actor entirely** — a forged customer session's direct `select count(*) from app.tickets`/`app.ticket_messages` against their own real, otherwise-RPC-visible ticket returns ZERO rows, confirming the RPC layer is the only sanctioned read path.
11. **An internal-channel ticket id is unreachable through every customer RPC** — `get_customer_ticket`/`list_customer_ticket_messages` against a real, internal-channel ticket id (created via `create_ticket_for_employee`) return zero rows for a real employee actor, confirming the channel filter runs before any scope check.
12. **A genuine two-process concurrent idempotency race on `create_customer_ticket`** — two real, concurrent OS `psql` processes with the identical idempotency key: both returned the SAME ticket id; `select count(*)` confirmed exactly one row.
13. **A ticket link grants no customer access to a linked record** — honestly N/A this prompt (Prompt 292/Linked Records has not shipped; no such column/mechanism exists anywhere in this schema, grep-confirmed). Verified structurally instead: none of the three customer-facing projection RPCs' own OUT columns reference `shipment`/`invoice`/`warehouse`/`vendor` (queried `information_schema.parameters` directly).
14. **Schema-privilege defense in depth** — `has_function_privilege('anon', 'app.create_customer_ticket(...)', 'execute')` and `.../app.get_customer_ticket(uuid,uuid)` both `false`.

## 10. Self-found defects fixed (found and fixed before commit, none shipped)

1. **C-02 (2 functions)** — `app.get_customer_ticket`/`app.list_customer_ticket_messages` each had a bare `where id = p_ticket_id` colliding with their own `RETURNS TABLE(id ...)` implicit OUT variable, producing `column reference "id" is ambiguous`. Found by this checkpoint's own db-test fixture run (test 7), fixed by aliasing (`t0.id`/`t0.channel`).
2. **Fixture-authoring mistakes** (wrong actor for TKT:Edit-gated queue/category creation calls — used the tenant admin instead of the TKT:Edit-holding staff member; a stray invalid `perform ... where false` placeholder line) — caught immediately by psql errors during fixture authoring, none shipped.
3. **Shared-infrastructure gap** (§6) — `scripts/db-tests/rbac-enforcement.sql`'s ATW-032 sweep flagged 3 functions (2 new, 1 pre-existing HRT-286) as unauthorized; fixed by widening the sweep's own recognized-primitive list and adding one documented `v_expected` entry, not by weakening the check.

## 11. Residual gaps (disclosed)

- **No per-ticket customer participant/watcher grant** (decision 8) — access is account-level (any active `customer_user` membership on the account sees that account's tickets), not a separately-managed per-ticket participant roster. Section 22's "adds a permitted company participant" is satisfied at the account-scope level, not built as its own grant table under this bounded slice.
- **No attachment-upload UI for the customer surface** — matches HRT-286's own already-disclosed, already-accepted precedent (`ISS-2026-087`); the RPC-level (reused `reply_to_ticket`) malware-scan/tenant-scope gating is real, just not exercised by a customer-facing upload widget yet.
- **No REST/GraphQL adapter, no Playwright/axe-core E2E spec** — repository-wide precedent, disclosed identically at every prior HRT checkpoint.
- **`ISS-2026-085`'s Prompt 288/helpdesk half remains genuinely open** — this migration resolves only the customer/Prompt-287 half; a CargoGrid-support-scoped, non-org-unit-bound queue/assignee concept is Prompt 288's own separate expand-and-contract work.
- **Linked-record customer visibility is honestly undecided, not fabricated** — Prompt 292 has not shipped; when it does, it must independently gate any linked record's own customer visibility, never inherit it from ticket access alone (guidance left in the migration's own decision 10).

## 12. Rollback/recovery note

One additive migration (`20260731080000`), reversible by a future corrective migration (never by editing this one in place). To roll back: drop `app.tickets.requester_customer_account_id`/its CHECK/index, drop `app.ticket_categories.customer_visible`, drop the 6 new customer-facing functions and `app._is_ticket_requester_party`, restore the pre-migration bodies of the functions this migration `create or replace`d (via a further corrective migration, not by reverting this file), and delete the new service-layer/UI/guard files. The `scripts/db-tests/rbac-enforcement.sql` edit is independent and does not need to be rolled back together with the schema (it only corrects the sweep's own classification accuracy). Resume point: this file plus `scripts/db-tests/ticketing-customer.sql`'s own 19 assertion groups and the re-confirmed `ticketing-internal.sql` 15/15 are the authoritative record of what is proven.

## 13. Recommended next task (historical — superseded by §14 below)

This checkpoint is `COMPLETED`, not `VERIFIED` — a Tier C adversarial review (spec-compliance, security/RLS/tenant, correctness/concurrency, cross-prompt integration lenses) must run before `CG-S12-HRT-015` may be promoted, per this repository's own standing precedent. Given elevated stakes (the customer channel is this repository's first genuinely external-facing, non-employee principal on the ticket model), the reviewing session should pay particular attention to: (a) whether `app._is_ticket_requester_party`'s generalization genuinely closes every call site that previously inlined the employee-only check (mechanically re-sweep for any remaining `app.get_self_employee`-based requester check this session may have missed); (b) independently re-deriving the C-02 fix and the concurrency race against the live schema; (c) the account/company/site mapping decision's own no-implicit-cascade boundary. Only after Tier C closes should Prompt 288 (Tenant-to-CargoGrid Helpdesk) begin, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.2 trigger 6 — and only under a fresh, explicit operator authorization naming it.

## 14. Tier C batch review — CG-S12-HRT-015, standalone single-prompt batch (2026-08-14)

**Batch plan:** standalone single-prompt batch, exactly as §13 above required (no consumer of this contract, Prompt 288+, may build or be reviewed before this one is `VERIFIED` — `BUILD_EXECUTION_PROTOCOL.md` §3.2 trigger 6). Four parallel adversarial lenses ran against commit `f1225c6` (spec-compliance owning C-15/C-18/C-20; security/RLS/tenant-isolation owning C-05/C-06/C-07/C-08/C-10/C-11/C-12/C-13/C-17; correctness/concurrency owning C-01/C-02/C-03/C-04/C-09/C-14/C-19; cross-prompt integration owning C-07/C-08/C-16/C-19/C-20, with a specific chartered focus on whether `ISS-2026-085` was genuinely resolved), each against a real, freshly-built disposable Postgres 16 database with the full then-current migration set applied and live forged-session/concurrent-`psql` probing. This fix pass independently re-derived every reported finding against the live schema on fresh disposable databases (`cargogrid_review_fix`/`cargogrid_review_fix2`, built via `scripts/db-tests/lib/setup-disposable-db.sh`) before accepting any of them, per `BUILD_EXECUTION_PROTOCOL.md` §5.3 — never fixed from a lens citation alone.

### Disposition table

| # | Lens | Finding | Severity | Disposition | Evidence |
|---|---|---|---|---|---|
| 1 | Spec Finding 1 / Security Finding 1 (independently, identically) | `app.get_customer_ticket` returns `resolution_summary`/`cancelled_reason`/`last_reopen_reason` verbatim to the customer regardless of whether staff or the customer themself authored the text — `app.transition_ticket_status` writes whatever free text a staff actor supplies straight into these columns, with no internal/customer-facing distinction | HIGH | **CONFIRMED, independently live-reproduced twice** (not accepted from lens citation alone): resolved and reopened a real customer ticket as staff with a deliberately internal-sounding rationale ("INTERNAL: customer flagged high churn risk... do not offer refund"; "INTERNAL: legal hold requested by compliance... do not disclose to customer"), then called `app.get_customer_ticket` as the customer — both leaked verbatim. **FIXED** at root cause via a new additive migration: two new tracking columns (`cancelled_reason_authored_by_customer`/`last_reopen_reason_authored_by_customer`, populated by `create or replace app.transition_ticket_status` from `app._is_ticket_requester_party`); `create or replace app.get_customer_ticket` now returns `resolution_summary` as always-`null` (unconditionally staff-authored by construction — `_ticket_transition_authority` never admits a requester into `resolved`/`closed`) and only echoes `cancelled_reason`/`last_reopen_reason` back when the customer themself authored that specific text. Re-verified live post-fix: staff-authored resolve/cancel/reopen text on a customer ticket now returns `null` to the customer; a genuine customer self-cancel/self-reopen still returns their own words; same-actor idempotent replay behavior is unaffected. Residual scope narrowing (resolution rationale is now never shown to the customer at all, since it is always staff-authored) disclosed as `ISS-2026-088` (Low). | `20260731090000...sql`; live repro before/after in this session |
| 2 | Correctness lens (2-process concurrent `psql` reproduction) | `app._create_ticket`'s customer-channel idempotency match (both the pre-check `SELECT` and the `unique_violation` recovery `SELECT`) and `tickets_idempotency_customer_unique` key on `(tenant_id, requested_by_auth_user_id, idempotency_key)` only — never `requester_customer_account_id`. A customer_user actor legitimately scoped to more than one account (decision 5/8 of `20260731080000`, an explicitly supported scenario) who reuses one idempotency key across two different-account requests with matching content gets the second request silently short-circuited to the first account's ticket | HIGH | **CONFIRMED, independently live-reproduced** (both sequentially and via the same real-`psql`-concurrency technique the lens itself used): granted one customer identity `customer_user` membership on two real accounts, called `create_customer_ticket` for account A then account B with the identical idempotency key and matching subject/body — both calls returned the SAME ticket id, attributed to account A only; no row was ever created for account B. **FIXED** at root cause: `tickets_idempotency_customer_unique` rebuilt to include `requester_customer_account_id`; both `SELECT`s inside `app._create_ticket`'s customer-channel branch (the pre-check and the `unique_violation` recovery path) now filter on it too. Re-verified live post-fix: two different-account requests with the identical key now correctly create two distinct tickets, each attributed to its own account; a genuine same-account replay (identical actor, account, key, and content) still correctly collapses to one row — no regression to the intended dedupe behavior. | `20260731090000...sql`; live repro before/after in this session |
| 3 | Spec Finding 2 | `docs/runtime/TASK_LEDGER.md`'s own `CG-S12-HRT-015` row states "26 files, 0 forbidden, 1 caution flag"; the completion report and `git show --stat HEAD` both show **27** files changed | LOW | **CONFIRMED** — `git show --stat HEAD` independently re-counted: 27 files changed, matching the completion report, not the ledger row. **FIXED**: corrected file count in the `TASK_LEDGER.md` row promoted to `VERIFIED` by this batch (§ below). | `git show --stat HEAD`; `docs/runtime/TASK_LEDGER.md` (this batch's own edit) |
| 4 | Integration lens (chartered focus: `ISS-2026-085` closure) | All 7 of `ISS-2026-085`'s own verification questions | — | **CONFIRMED genuinely resolved for the customer/Prompt-287 half, no residual gap** — independently re-derived live against a fresh disposable Postgres 16 database (not trusted from this checkpoint's own build log or `KNOWN_ISSUES.md` prose): NOT NULL relaxation and `tickets_requester_identity_shape` CHECK real (`\d app.tickets`); `_create_ticket` has exactly one overload, real `p_channel` reaching the INSERT (`pg_proc`, live-created row `channel='customer'` confirmed); the three named `INNER JOIN`s are genuinely `LEFT JOIN`s, live-tested against 4 real customer auth users with zero matching `app.employees` rows — the exact scenario the entry said would be structurally unreadable is now readable; `can_access_ticket`/`_ticket_transition_authority` genuinely dispatch through `app._is_ticket_requester_party`; `app.resolve_customer_owner_account_scope`/`app.actor_holds_customer_user_layer` reused, never redefined (grep-confirmed); no already-VERIFIED table's grants/RLS inappropriately widened (`aclexplode` re-checked); `ticketing-internal.sql` re-ran 15/15, byte-for-byte unchanged both before and after this batch's own fix migration. Per the batch's own charter, this specific disposition is treated as HIGH-priority regardless of how the lens itself scored it (it scored it as "no findings," the strongest possible result) — re-confirmed independently rather than accepted at face value. `ISS-2026-085`'s own entry updated with this batch's independent re-confirmation (§ below); Prompt 288/helpdesk half remains correctly, honestly `OPEN`. | fresh disposable Postgres, direct `pg_proc`/`\d`/`aclexplode` queries, this session |
| — | Spec/Security/Correctness/Integration "everything else" (anti-enumeration, cross-account/cross-tenant isolation, revoked-membership immediacy, internal-note unreachability, raw-table RLS exclusion, C-02 ambiguous-id sweep, all 208 migrations apply cleanly, `ticketing-internal.sql`/`ticketing-customer.sql`/`rbac-enforcement.sql` all passing) | No defect | — | Accepted — independently re-confirmed by this session's own full standalone run of `ticketing-internal.sql` (15/15) and `ticketing-customer.sql` (19/19) against a fresh disposable database, both before and after the fix migration, plus this session's own direct code reads of the cited RPC/RLS bodies. | fixture runs, this session |

### Propagation sweep (§5.4)

- **Staff-rationale-leak defect class (finding 1):** swept every free-text column `app.get_customer_ticket`/`app.list_customer_tickets`/`app.list_customer_ticket_messages`/`app.list_customer_ticket_categories`/`app.list_customer_accounts_for_actor` return — `resolution_summary`/`cancelled_reason`/`last_reopen_reason` were the only three staff-rationale-shaped columns exposed by any customer-facing projection (grep- and read-confirmed against every `RETURNS TABLE` column list in `20260731080000`); `ticket_events.reason` (which can carry internal transfer/reassignment reasoning) is correctly never exposed to any customer RPC at all, confirmed unchanged. No further instance of this defect shape found.
- **Idempotency-key-scope defect class (finding 2):** swept both idempotency-guarding indexes/match-logic pairs in the ticketing domain — `tickets_idempotency_unique` (internal channel, keyed on `requester_employee_id`, correctly account-equivalent since an employee has exactly one identity, no multi-"account" concept applies) was NOT affected, confirmed correct as-is; `tickets_idempotency_customer_unique` (customer channel) was the sole instance, now fixed. No other idempotency-keyed table/index exists anywhere in the ticketing domain to sweep.
- **Repository-wide check:** confirmed the fix migration touches only `app.tickets` (2 new columns), 1 index (`drop`+`create`), and 2 functions (`create or replace`, unchanged signatures, grants preserved) — no other capability's table, RLS policy, or grant was touched.

### Fresh gate suite (independently re-run this session, from scratch, after the fix migration)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**, 215 warnings (pre-existing baseline drift unrelated to this batch — grep-confirmed zero warnings reference any ticketing/customer-ticket file; this fix is SQL-only, no TS file changed) |
| `pnpm run test` | **3964/3964 pass, 0 fail** (identical to pre-fix baseline — no TS file changed, SQL-only fix) |
| `pnpm exec next build` | `✓ Compiled successfully`, both `/[tenantSlug]/customer-tickets` and `/[tenantSlug]/customer-tickets/[ticketId]` present alongside every other route |
| `pnpm run docs:check` | clean — link resolution, canonical files, ADR index, HANDOFF/TASK_LEDGER coherence all pass |
| `pnpm run security:check` | clean — no secret-shaped pattern |
| `pnpm run security:audit` | clean — no dependency advisories |
| `pnpm run data-classification:check` | clean — registry internally valid |
| `pnpm run threat-model:check` | clean — 25 entries (4 critical/11 high/9 medium/1 low), valid |
| `pnpm run standards:check` | clean — no suppression-governance violations |
| `pnpm run git:check-paths` | clean |
| `pnpm run git:check` | clean — no local collision risk; branch name matches convention |
| `pnpm run db:test` (full official harness) | **ALL PASSED end-to-end** (`==> db-tests: ALL PASSED`, exit 0) — run at ~05:26-05:36 UTC, past the `ISS-2026-059` ~00:00-03:59 UTC failure band, so this run reached and passed every one of the 163 test files including `procurement-vendor-performance.sql`, `scripts/db-tests/rbac-enforcement.sql`, `scripts/db-tests/ticketing-internal.sql`, and `scripts/db-tests/ticketing-customer.sql` end-to-end against the full migration set including this batch's own fix migration. |
| `scripts/db-tests/ticketing-internal.sql` (standalone, additionally re-confirmed) | **PASS, exit 0, 15/15 assertion groups** — run against a completely fresh disposable database with all 209 migrations (208 original + this batch's own fix migration) applied via the shared `setup-disposable-db.sh` helper, confirming byte-for-byte zero regression to the internal channel. |
| `scripts/db-tests/ticketing-customer.sql` (standalone, additionally re-confirmed) | **PASS, exit 0, 19/19 assertion groups** — same fresh database, confirming zero regression to every pre-existing customer-channel assertion (including the ones this batch's own fix touches: close/reopen-as-configured, cross-account isolation, idempotency replay). |

Plus direct code reads of both HIGH fixes, confirming each genuinely present and correctly shaped (disposition table above cites the exact functions/columns read), and both were additionally live-reproduced broken before the fix and live-reproduced correct after, using real forged PostgREST-style sessions and genuine multi-request idempotency-key reuse — not merely re-read.

### Disclosed residual issues (registered, not fixed, per §5.6)

- `ISS-2026-088` (LOW, new) — `app.get_customer_ticket` now never exposes `resolution_summary` to the customer at all (always staff-authored by construction, so the containment fix for finding 1 necessarily removes it entirely rather than partially) — a feature-completeness gap, not a security exposure; a future prompt wanting to restore a customer-facing "why was my ticket resolved" affordance should add an explicit, staff-authored, opt-in customer-visible closure-note field, never re-expose the internal rationale unconditionally.
- `ISS-2026-085`'s Prompt 288/helpdesk half — unchanged, still genuinely `OPEN`, re-confirmed by this batch's own independent verification (finding 4 above) that the customer/Prompt-287 half itself has no residual gap.
- Every residual gap already disclosed in §11 above (no per-ticket customer participant/watcher grant, no attachment-upload UI, no REST/GraphQL/E2E) — unchanged by this batch, still accurate.

No Critical finding was reported by any lens. Both HIGH findings from this round (findings 1/2 above) are fixed at root cause and re-verified live, per §5.6 — no Critical/High finding is left open uncontained.

### Corrective migration

`supabase/migrations/20260731090000_harden_ticketing_customer_channel_batch_287_review_fixes.sql` — additive only, does not edit `20260731080000_extend_ticketing_customer_channel.sql` (or `20260731060000`/`070000`) in place. Contains: two new tracking columns on `app.tickets` plus the `transition_ticket_status`/`get_customer_ticket` rewrites (finding 1); the `tickets_idempotency_customer_unique` index rebuild plus the `_create_ticket` match-logic fix (finding 2).

### Status

`CG-S12-HRT-015` is promoted to **`VERIFIED`** — every Critical/High finding is fixed-and-reverified (findings 1/2; no Critical was reported), the one LOW finding is fixed (finding 3, `TASK_LEDGER.md` file count corrected), and the integration lens's chartered re-verification of `ISS-2026-085`'s own closure found no residual gap (finding 4), satisfying `BUILD_EXECUTION_PROTOCOL.md` §5.6. **`ISS-2026-085` final disposition: `RESOLVED` for the customer/Prompt-287 half** (independently re-confirmed by this batch, not merely re-read from the original resolution note) **— the Prompt 288/helpdesk half remains genuinely `OPEN`**, unaffected by and unblocked by this batch's own work. `CG-S12-HRT-016` (Prompt 288, Tenant-to-CargoGrid Helpdesk) is dependency-clean at the task-state level now that this row is `VERIFIED` (its own stated upstream is `HRT-286..287`, both now `VERIFIED`) and is named within the standing "lanjut prompt 281-290" operator authorization — but per the adaptive batch cap (`BUILD_EXECUTION_PROTOCOL.md` §3.4), it still requires its own explicit batch-planning step (deciding batch size/composition for the remainder of the range) before implementation begins; this checkpoint does not itself perform that planning step. `docs/runtime/HANDOFF.md` intentionally not touched, per this task's own instruction.
