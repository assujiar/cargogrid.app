# HRT-285 — Employee and Manager Self-Service

**Prompt ID:** `CG-S12-HRT-013` · **Package document:** `docs/ai-agent-build-prompt-package/12-phase-07-hris-ticketing/285_ESS_MSS_PROMPT.md` · **Phase:** 7 (HRIS and Ticketing) · **Status at end of this checkpoint: `VERIFIED`** (batch-close, §12 below) — implementation complete, all mandatory Tier A automated gates green, Tier B taxonomy self-check performed and recorded below (§5), adversarially self-tested against a live disposable Postgres 16 database with real forged-session `psql` probes targeting exactly the composition-layer leakage risk this prompt is elevated-risk for, and now independently adversarially re-reviewed (4-lens Tier C, combined `283-285` batch) with every Critical/High finding fixed or disclosed. This is the THIRD and LAST of the `283-285` batch (`docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.4); the combined Tier C review ran once, now that all three prompts were `COMPLETED`, by a separate orchestrating session.

## 1. What this checkpoint builds

HRT-285 is the fourteenth Phase 7 (HRIS/Ticketing) capability and the first pure **composition** layer in this phase: a responsive ESS home (`app/(tenant)/[tenantSlug]/hris/my/page.tsx`) and an MSS team workspace (`app/(tenant)/[tenantSlug]/hris/team/page.tsx`), built entirely by calling the EXISTING, already-`VERIFIED`/`COMPLETED` query and mutation RPCs of HRT-274 (Employee Master), HRT-278 (Attendance), HRT-279 (Shift/Roster), HRT-280 (Leave), HRT-281 (Overtime/Timesheet), HRT-282 (Payroll), HRT-283 (KPI/Performance) and HRT-284 (Training/Talent) — **zero new table, zero new RPC, zero new migration**. Own/team scope is derived server-side exclusively via `app.list_my_team_employees` (HRT-274's own established "effective team" resolution), reused verbatim, never re-derived. Manager status alone never grants payroll, talent, or unrestricted personal data: the MSS team workspace never calls a single `app.payroll_*`-backed RPC, and every talent-review/pool/succession read is structurally rejected at the RPC layer for a plain manager (`HRS:Override`-only, live-proven below, §7).

## 2. Files created / changed (all new; two pre-existing docs files edited, both purely additive)

**No migration** — this checkpoint's own charter (§13) explicitly allows "zero or one small migration... a service-layer-only composition prompt... is a legitimate, correct outcome here." No self-service preference/saved-view/acknowledgement table was genuinely needed: every "acknowledgement" concept already has a canonical home (e.g. `app.acknowledge_performance_outcome`, HRT-283) and this checkpoint invents none.

**Service layer (new):**
- `server/contracts/self-service/self-service.ts` (166 lines) + `.test.ts` (7 tests) — no new Zod row schemas for RPC output (there is no new RPC); a `ManagerApprovalQueueItem` discriminated union type composed from four owning capabilities' own already-parsed row types (re-imported, never redefined), an `EssHomeSummary`/`MssTeamWorkspace` composition-shape, and the one genuinely new validated input this checkpoint adds: `DecideManagerApprovalQueueItemInputSchema`, a Zod discriminated union on `kind` that reuses each owning domain's own decision-literal enum unchanged (leave's `"approved"|"rejected"`, overtime/timesheet/training's `"approve"|"reject"` — deliberately NOT normalized to one shape, so a caller sending the wrong domain's literal fails Zod validation before any RPC call, unit-tested).
- `server/queries/self-service.ts` (294 lines) + `.test.ts` (10 tests) — `getEssHomeSummary` (bounded, count-first composition of 14 parallel `my_*`/self-resolving reads) and `getMssTeamWorkspace` (composes `list_my_team_employees` plus four owning capabilities' own already-manager-scoped list RPCs, then defensively re-filters every result to the caller's own team-id set — §7 explains why this second filter is load-bearing, not decorative).
- `server/mutations/self-service.ts` (101 lines) + `.test.ts` (6 tests) — `decideManagerApprovalQueueItem`, a pure router with zero authority check, zero validation beyond Zod shape, and zero audit write of its own: it dispatches to `decideLeaveRequest`/`decideOvertimeRequest`/`decideTimesheetEntry`/`decideTrainingEnrollment` (HRT-280/281/281/284's own unchanged mutation wrappers) by `kind`, unifying their four distinct error classes into one `SelfServiceMutationError` the UI catches once.

**UI (new, 2 route families):**
- `app/(tenant)/[tenantSlug]/hris/my/{page,loading,ess-home-panel}.tsx` — a server-rendered (no client state) landing page: six cards (attendance/schedule, leave/overtime, payslip/benefit, performance, training, profile), each a bounded count plus a `Link` to that capability's own already-`VERIFIED` detail page — no write action lives here, no logic is duplicated.
- `app/(tenant)/[tenantSlug]/hris/team/{page,loading,actions,team-workspace-panel}.tsx` — the manager queue: team roster, a unified approvals queue (leave/overtime/timesheet/training, each item's own decide form routed through the one composed dispatcher), team schedule (next 14 days), team performance summary (current cycle + team-scoped goals/outcomes), team training status (team-scoped enrollments/certificates). Renders an `EmptyState` ("No direct reports") for a non-manager rather than a client-side redirect or a silently-empty page.

**Docs:** this file (new); `docs/runtime/TASK_LEDGER.md` new `CG-S12-HRT-013` row at `COMPLETED`, per this task's own explicit instruction, not `VERIFIED`; `docs/build-log/phase-07/00_EXECUTION_INDEX.md` row 285 updated from `BLOCKED` to `COMPLETED`, matching HRT-283/284's own precedent of updating this row themselves. `docs/runtime/HANDOFF.md` deliberately not touched.

## 3. Source/config decisions (disclosed, not left implicit)

### Decision 1 — zero migration, zero new RPC (mandatory reading item, taken literally)

Every read/write in this checkpoint is a thin composition over an EXISTING, already-authority-checked RPC. No table was added, no `create or replace function` touches any pre-existing function, and no new `SECURITY DEFINER` boundary was created — the TypeScript composition layer itself performs no authority decision of its own; every decision it surfaces is the owning capability's own.

### Decision 2 — MSS approvals queue composes exactly 4 of 6 candidate domains; attendance-correction and schedule-swap decisions are deliberately excluded, not silently dropped

Read directly against the live migration SQL (not assumed from precedent) and live-confirmed with a real forged-session `psql` probe (§7): `app.list_attendance_correction_requests` (HRT-278) and `app.list_schedule_swap_requests` (HRT-279) both gate on plain `HRS:View` ONLY — neither function's own predicate has a self/direct-manager branch the way `list_overtime_requests`/`list_timesheet_entries`/`list_leave_requests`/`list_schedule_assignments`/`list_performance_outcomes` all independently do. A bare direct manager holding zero extra `HRS` permission gets **zero rows** from either RPC — live-proven, §7. Composing these into the MSS queue would either render permanently empty (dead UI, taxonomy C-20's inverse) or require this checkpoint to invent a NEW manager-scope predicate the owning capability itself never built — forbidden by section 13 ("never re-implement, duplicate, or shadow any of their logic"). Excluded and disclosed here, not silently omitted.

### Decision 3 — the MSS team-id intersection filter is load-bearing, not decorative (the elevated composition-layer leakage risk this prompt was built to guard against)

Every MSS list (`teamScheduleUpcoming`, `teamGoalAssignments`, `teamOutcomes`, `teamTrainingEnrollments`, `teamCertificates`, and every approval-queue item) is filtered a SECOND time in `server/queries/self-service.ts`, client-side in the TS composition layer, against the caller's own `app.list_my_team_employees` roster — even though every owning RPC already scopes a bare caller to self/direct-manager on its own. **Live-proven this second filter is genuinely necessary, not redundant** (§7): an actor holding `HRS:View personal data` (a realistic, ordinary case — many senior-manager roles are ALSO granted broader HR visibility in a real tenant) calling `list_performance_outcomes(employeeId=null)` gets **every** employee's outcome in the tenant, not just their own direct reports'. Without this checkpoint's own defensive re-filter, a dual-role manager visiting the MSS team workspace would see every colleague's performance rating, not just their genuine 2 direct reports' — exactly the inference/leakage shape section 16 names ("a manager's team-summary widget must show only what... the KPI capability's own manager-scope check would separately allow"). The re-filter closes that gap for a dual-role actor without weakening or duplicating the owning capability's own RLS/RPC scope for a plain manager (for whom the two filters agree exactly, also live-confirmed, §7).

### Decision 4 — payroll is entirely absent from the MSS team workspace

`server/queries/self-service.ts`'s `getMssTeamWorkspace` imports nothing from `server/queries/payroll.ts`. Section 15 scopes payroll to ESS "own payslip/benefit" only; section 16's "manager status alone must never grant payroll... data" is satisfied structurally — there is no code path by which a team member's compensation could reach this page.

### Decision 5 — talent-review/pool/succession is entirely absent from MSS reads, and the RPC layer independently enforces this even if it weren't

`app.can_view_talent_review_row`/the pool-membership check are `HRS:Override`-only (HRT-284 decision 6) — never self, never direct manager. This checkpoint's own composition never calls `list_talent_review_cycles`/`list_talent_pools`/`list_succession_candidates` for the team workspace at all; and a live probe (§7) confirms that even if it had, a real direct manager holding zero `HRS:Override` is HARD-REJECTED (`insufficient_authority`) by the RPC itself, not merely returned an empty list — a second, independent line of defense.

### Decision 6 — the composed decide dispatcher is a pure router (business rule 24, taken literally)

`decideManagerApprovalQueueItem` performs zero authority check, zero business validation beyond Zod input-shape/discriminant checking, and writes zero audit row of its own. Every actual authority check, self-decision block, idempotency guard, and audit-log write for every one of the four decision kinds is the owning capability's own already-`VERIFIED`/`COMPLETED` mutation, completely unchanged.

### Decision 7 — deliberately NOT built, disclosed rather than silently dropped (taxonomy C-23 discipline)

No self-service preference/saved-view/acknowledgement schema (decision 1 — genuinely not needed). No REST/GraphQL adapter (repository-wide precedent). No Playwright/axe-core E2E spec (repository-wide precedent, every prior HRT checkpoint's own identical gap). No customer-facing full self-service portal (explicitly Step 13, section 15's own boundary). No native/offline app (explicitly out of scope, section 15). No quick "clock in/out" action duplicated onto the ESS home — `app/(tenant)/[tenantSlug]/hris/my/attendance` already has one (HRT-278); the ESS home links to it rather than re-implementing it, matching this checkpoint's own "never a second... datastore" discipline extended to UI actions too.

## 4. Security/access model

- **No new RLS, no new grant** — every table this checkpoint reads/writes is governed by its owning capability's own already-`VERIFIED` RLS policy, unchanged.
- **Identity:** every server action resolves the actor from `resolveHrisAccessForRequest` (the same session-verified identity every sibling HRIS page already uses) and forwards it explicitly as `actorAuthUserId` to every underlying RPC — never a client-supplied actor id (taxonomy C-13, unaffected by this checkpoint since no new SQL exists, but the TS-layer discipline is followed for consistency and because a future capability might copy this file's own shape).
- **Own/team scope:** derived exclusively via `app.list_my_team_employees`, HRT-274's own established resolution — never re-derived, never a second manager-hierarchy mechanism.
- **Defense in depth:** every MSS list is additionally intersected against that same roster in the TS composition layer (decision 3), live-proven necessary for a dual-role manager+HR actor.

## 5. Tier B — recurring defect taxonomy self-check (`docs/standards/RECURRING_DEFECT_TAXONOMY.md` §4)

Walked mechanically against this checkpoint's own diff before commit. Most classes are structurally N/A because this checkpoint adds zero new SQL (no migration) — noted explicitly rather than skipped.

- **C-01/C-02/C-03/C-04/C-09/C-14/C-19/C-21 (concurrency/idempotency/constraint classes)** — N/A. No new SQL function, table, index, constraint, or lock exists anywhere in this diff; every versioned write this checkpoint's dispatcher forwards to is the owning capability's own already-hardened RPC, unchanged.
- **C-05 (permission check ordered after a disclosing read)** — Clear. `decideManagerApprovalQueueItem` discloses nothing of its own; it forwards straight to the owning capability's own authority-gated RPC. Every composed READ (`getEssHomeSummary`/`getMssTeamWorkspace`) calls only already-authority-checked RPCs; the TS layer adds a NARROWING filter (decision 3), never a widening one.
- **C-06 (implicit `auth.uid()`)** — Clear. Every function in the three new service-layer files takes an explicit `actorAuthUserId`/`tenantId` and forwards it unchanged to every underlying call; nothing here reaches a view/RLS helper relying on a session-implicit default.
- **C-07 (unmasked jsonb snapshot)** — N/A. Zero new `jsonb` column.
- **C-08 (widening an existing function)** — N/A. Zero `create or replace function` in this diff; every RPC called is pre-existing and untouched.
- **C-10 (file/evidence re-validation)** — N/A. This checkpoint handles no file/evidence.
- **C-11 (grants absent/blanket)** — N/A. No new table, no new function, no new grant statement anywhere in this diff.
- **C-12 (`SECURITY DEFINER` granted to `authenticated` with no check)** — N/A structurally (no new SQL function exists to grant). Live-verified anyway (§7) that every RPC this checkpoint's dispatcher/queries call independently enforces its own authority.
- **C-13 (claimed actor vs. caller identity)** — Clear. Every server action in `hris/my/actions.ts` (implicit, no writes) and `hris/team/actions.ts` resolves `access.authUserId` from the verified session (`resolveHrisAccessForRequest`) and passes it as the actor to every mutation — never a client-supplied value.
- **C-15 (missing domain constraints)** — Reviewed: the one new validated shape, `DecideManagerApprovalQueueItemInputSchema`, is a closed discriminated union over exactly 4 `kind` literals, each reusing its owning domain's own closed decision enum (never a new one) — unit-tested that the wrong domain's decision literal is rejected. Clear.
- **C-16 (`"use server"` module exports something that is not a function)** — Clear. `hris/team/actions.ts` exports exactly 4 async functions + one plain `interface` (erased at compile time); `next build` (the authoritative check for this class) ran clean, both new routes present in the manifest.
- **C-17 (`select("*")`)** — Clear. Grep-verified zero `select("*")`/`select *` in any new file (`leave.ts`'s own `listLeaveApprovalInboxForActor`, reused unchanged, uses an explicit `.select("id, entity_type, entity_id")`, pre-existing and untouched).
- **C-18 (maker-checker happy-path only)** — N/A/Clear. No new maker-checker mechanism; the dispatcher forwards `decision` (approve/reject or approved/rejected) unchanged to the owning capability's own existing self-block, which already applies to both branches (confirmed by direct code read of each owning RPC, §3 of HRT-280/281/284's own build logs).
- **C-20 (capability with no UI caller)** — Clear. Traced every new function to a real caller: `getEssHomeSummary` → `hris/my/page.tsx`; `getMssTeamWorkspace` → `hris/team/page.tsx`; `decideManagerApprovalQueueItem` → all 4 decide forms in `team-workspace-panel.tsx` via `hris/team/actions.ts`. No orphaned wrapper.
- **C-22 (threshold compared without normalizing unit)** — N/A. No new monetary/quantity threshold comparison anywhere in this diff.
- **C-23 (a named spec requirement silently dropped)** — Cross-checked every named item in Prompt 285 §13-§28 against either code or an explicit disclosure above (decisions 2, 4, 5, 7). No undisclosed omission found.
- **C-24 (unmasked money/reason into `app.audit_logs`)** — N/A. Zero new `capture_audit_event` call site anywhere in this diff; every write still flows through the owning capability's own already-C-24-compliant call sites, unchanged.

No taxonomy class was skipped.

## 6. Commands run and results (fresh, this checkpoint)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**, 205 warnings — identical pre-existing baseline (HRT-283/284's own disclosed count); zero warnings reference any `self-service`/`hris/my`/`hris/team` path (grep-confirmed) |
| `pnpm run test` | **3892/3892 pass, 0 fail** — 23 new tests (7 contract + 10 query + 6 mutation) |
| `pnpm run git:check-paths` | clean — 13 files checked, 0 forbidden, 0 caution flags |
| `pnpm run security:check` | clean — no secret-shaped pattern in any tracked file |
| `pnpm exec next build` | `✓ Compiled successfully` — both new routes present (`/[tenantSlug]/hris/my`, `/[tenantSlug]/hris/team`) alongside every pre-existing route |
| `pnpm run db:test` | **Not applicable to this checkpoint's own signal** — zero migration, zero new `scripts/db-tests/*.sql` file added, per this task's own explicit "may need very little or no new schema" allowance. Instead, this checkpoint's own adversarial verification (§7) applied the FULL migration set to a fresh disposable database and live-probed the exact RPCs this checkpoint composes, with real forged-session `psql` calls — see below. |

No new migration means no `data-classification:check`/RLS/RPC-level Tier A obligation beyond the standard suite above.

## 7. Adversarial testing — live, against a real disposable Postgres 16 database (not reasoned about)

Per this task's own WORKFLOW instruction to probe specifically for the composition-layer leakage risk. A fresh disposable database (`cargogrid_hrt285_adv`, since dropped) was built by applying the full, current 204-migration set via `scripts/db-tests/lib/setup-disposable-db.sh`'s own extracted setup logic, then loading four PRE-EXISTING, already-`VERIFIED`/`COMPLETED` db-test fixture files (`hris-overtime-timesheet.sql`, `hris-leave-permit-business-trip.sql` — reached the pre-existing, unrelated `ISS-2026-077` failure near its very end, well after all fixture setup and manager-scope assertions had already run and committed — `hris-shift-roster-scheduling.sql`, `hris-kpi-performance.sql`, `hris-training-talent.sql`, all four of which ran their own FULL assertion suite clean) to obtain real tenants with genuine manager/direct-report relationships, then queried the exact RPCs this checkpoint's TypeScript composition layer calls, as real forged-session actors (`set local role authenticated; set local request.jwt.claims = '{"sub": "...", "role": "authenticated"}'`, the identical technique every one of this repository's own db-test files uses):

1. **`list_leave_requests`/`list_overtime_requests`/`list_timesheet_entries` as a bare direct manager (no `HRS:View`), `employee_id=null`** — mgr1 (lv1 tenant) saw exactly emp1 + emp2 (their real direct reports), never emp3 (an unrelated employee in the same tenant) or mgr1's own leave requests (mgr1 has none) — `reason_visible=false` for every row (the reason text itself correctly masked from a bare manager). mgr1 (ot1 tenant) saw exactly emp1's overtime/timesheet, never emp2 (same tenant, no manager relation).
2. **Attack probe confirming decision 2**: `list_attendance_correction_requests`/`list_schedule_swap_requests` as the SAME real ot1 manager returned **zero rows**, live-confirming both are genuinely `HRS:View`-only with no self/direct-manager branch — exactly the reason this checkpoint's own MSS queue deliberately excludes them (§3 decision 2), not a bug, a confirmed design boundary.
3. **`list_performance_outcomes`/`list_performance_goal_assignments` as a bare direct manager (kpi1 tenant, no `HRS:View personal data`)** — mgr1 saw exactly emp1 (final_score 96.000) + emp2 (final_score 80.000), their real 2 direct reports, never any of the 5 unrelated "Big Dept" employees in the same tenant/cycle (whose real published scores range 71.000-75.000). Cross-checked against `list_my_team_employees` for the identical actor: returned the identical 2-employee set — confirming this checkpoint's own defensive team-id filter (decision 3) is consistent with, and never narrower or broader than, the owning RPC's own scope for a plain manager.
4. **THE decisive probe (decision 3's own justification, live-proven, not merely argued)**: the SAME `list_performance_outcomes` call, as `approver@kpi1` — an actor holding `HRS:View personal data` — returned **all 7 employees' outcomes**, including the 5 "Big Dept" employees who are nobody's direct report of this actor. This is the exact composition-layer leakage shape this prompt is elevated-risk for: if a real dual-role manager (a common real-world case — a senior manager who is ALSO granted broader HR visibility) reached this checkpoint's `getMssTeamWorkspace` without its own second, defensive `teamIds.has(...)` filter, they would see every colleague's performance rating in the MSS "team performance summary" widget, not just their own genuine direct reports'. **This checkpoint's own re-filter is what prevents that** — confirmed necessary by this live reproduction, not assumed.
5. **Attack probe confirming decision 5**: `list_talent_pools` as the SAME real direct manager (kpi1, zero `HRS:Override`) was HARD-REJECTED — `insufficient_authority: identity ... lacks HRS:Override for tenant ...` — not an empty list, a structural denial at the RPC layer itself, independent of this checkpoint never calling it in the first place.
6. **`list_training_enrollments`/`list_training_certificates` as a bare direct manager (trn1 tenant)** — returned exactly self + emp1 + emp2 (the real direct reports), never emp3 (unrelated) or the 5 "Big Dept" employees in the same tenant.

All four fixture-file loads (`hris-overtime-timesheet.sql`, `hris-shift-roster-scheduling.sql`, `hris-kpi-performance.sql`, `hris-training-talent.sql`) passed their own COMPLETE assertion suite with `exit 0`; the fifth (`hris-leave-permit-business-trip.sql`) reached and passed every assertion up to the pre-existing, unrelated `ISS-2026-077` failure point near its very end (its own manager-scope leave-request fixture, used in probe 1 above, was already built and committed well before that point). The disposable database was dropped after use.

## 8. Self-found items (found and fixed before commit, none shipped)

Consistent with this repository's own disclosure convention, even though this checkpoint's small, schema-free diff surface meant most issues were caught by fast feedback (`tsc`/`node --test`) rather than a live adversarial pass:

1. Two status-literal mistakes caught by `pnpm run typecheck`/the unit test suite before any commit: `"submitted"` was used where the real leave-request/reimbursement status enum is `"pending_approval"` (both `LEAVE_REQUEST_STATUSES` and `PAYROLL_REIMBURSEMENT_STATUSES` use `pending_approval`, never `submitted`) — fixed in `server/queries/self-service.ts`.
2. An incorrect contract type name (`ScheduleAssignmentRow`, which does not exist) used in place of the real exported `ScheduleAssignmentListRow` — caught by `tsc`, fixed in `server/contracts/self-service/self-service.ts`.
3. Not a defect but a confirmed-necessary-by-live-proof design decision: the MSS team-id intersection filter (decision 3) was designed in from the start as a defense-in-depth measure; §7 probe 4's live reproduction confirms it is genuinely load-bearing for a dual-role manager+HR actor, not redundant boilerplate.

## 9. Residual gaps (disclosed)

- **No self-service preference/saved-view/acknowledgement schema** (decision 1) — genuinely not needed; every acknowledgement concept already has a canonical home in an owning capability.
- **MSS approvals queue does not cover attendance-correction or schedule-swap decisions** (decision 2) — both are `HRS:View`-only capabilities today with no manager-of branch, live-confirmed; composing them would be dead UI or require inventing new scope logic outside this checkpoint's mandate.
- **No REST/GraphQL adapter, no Playwright/axe-core E2E spec** — repository-wide precedent, disclosed identically at every prior HRT checkpoint.
- **No `beforeunload` unsaved-change guard** on the new MSS decide forms — matches the same standing, repository-wide inconsistency HRT-283/284 also disclosed rather than resolving unilaterally.
- **No live, TypeScript-to-Postgres HTTP-level end-to-end test** — this checkpoint's adversarial verification (§7) is real, forged-session `psql` against a live database (the same technique every db-test file in this repository already uses) plus unit tests against a fake Supabase-shaped client for the TS composition logic itself; no live Supabase project exists anywhere in this repository (standing, repeated disclosure) so no genuine HTTP-level harness is possible yet.
- **The leave approval-inbox "delegated approver outside the direct team" exclusion (decision 3's own leave-specific case) is unit-tested with a fake client** (`server/queries/self-service.test.ts`) but was not additionally reproduced against the live database in this pass — the underlying `list_pending_approval_steps_for_actor`/PLT-123 mechanism itself is HRT-123's own already-`VERIFIED` capability, reused unchanged, and this checkpoint's own filter around it is covered by the live-proven pattern (probe 3/4) applied to a structurally identical composition shape.

## 10. Rollback/recovery note

Zero migration — nothing to roll back at the database layer; dropping this checkpoint's own files is fully sufficient and safe. Rollback: delete `server/contracts/self-service/`, `server/queries/self-service.ts`, `server/mutations/self-service.ts` and their `.test.ts` siblings; delete `app/(tenant)/[tenantSlug]/hris/my/{page,loading,ess-home-panel}.tsx`; delete `app/(tenant)/[tenantSlug]/hris/team/` entirely. No other capability's file was touched. Resume point: this file plus `server/queries/self-service.test.ts`'s own assertions are the authoritative record of what is proven; any resumed work should re-run `pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run git:check-paths && pnpm run security:check && pnpm exec next build` before proceeding, and re-run the §7 live-probe technique against a fresh disposable database if any owning capability's own RLS/RPC scope is later changed.

## 11. Recommended next task

This checkpoint closed the Implement stage for `CG-S12-HRT-013`, the THIRD and LAST of the `283-285` batch. The combined Tier C review for the whole batch has now run (§12 below), promoting all three rows to `VERIFIED`.

## 12. Tier C batch review (adversarial review + fix pass) — closes the whole `283-285` batch to `VERIFIED`

Performed `2026-08-14` by the orchestrating fix session, independent of the three implementing agents (`HRT-283.md`/`284.md`/`285.md` §1-§11 each). This is the batch's own closing build log — per this repository's own established HRT-278/279/280 convention, the full cross-batch disposition table, propagation sweep, and fresh gate suite live here once, rather than being duplicated three times; `HRT-283.md` §13 and `HRT-284.md` §13 each carry only their own prompt's own findings, cross-referencing back to this section for the full picture.

### 12.1 Method

4 parallel adversarial review lenses (spec-compliance; security/RLS/tenant isolation; correctness/concurrency; cross-prompt integration and data dependency) each independently reviewed the combined `283-285` diff. Per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §5.3, every finding was independently RE-DERIVED live by this fix session before being accepted — never fixed from a lens citation alone. Verification used a disposable Postgres 16 database (`postgresql://postgres:postgres@127.0.0.1:5432`) with the full, real migration set applied (206 migrations after this batch's own corrective migration), both HRIS db-test fixtures (`hris-kpi-performance.sql`, `hris-training-talent.sql`) loaded for real seeded two-tenant data, forged-session `psql` probes (`request.jwt.claims` + `set role authenticated`, the same technique every db-test file in this repository already uses), and genuine two-OS-process concurrent `psql` connections for the two concurrency findings (the `enqueue_job` idempotency race, live-reproduced twice — once broken, once fixed).

### 12.2 Full disposition table (all three prompts)

| # | Prompt | Lens | Finding | Severity | Verdict | Disposition |
|---|---|---|---|---|---|---|
| 1 | 283 (5 of 7) / 284 (2 of 7) | Correctness | 7 read RPCs across both migrations raise `column reference "id" is ambiguous` on every call, unconditionally — a bare, unqualified `where id = p_...` lookup collides with the OUT parameter `id` implicitly created by the function's own `returns table (id uuid, ...)` clause | CRITICAL | **CONFIRMED**, independently re-derived live with real seeded ids from both fixtures; a mechanical sweep (parsed live `pg_get_functiondef` output for every `RETURNS TABLE(id ...)` function across both migrations' 134 combined functions, not grep) found exactly these 7 and no more | **FIXED** — `supabase/migrations/20260731050000_harden_hris_kpi_performance_training_talent_batch_283_285_review_fixes.sql`, `create or replace function` aliasing the source table in each offending SELECT, verified byte-for-byte identical to the live original definition otherwise (fetched via `pg_get_functiondef` before writing the fix, never reconstructed from memory). Re-verified live: all 7 now return correct results. |
| 2 | 284 | Correctness | `app.enqueue_job`'s check-then-insert on `(tenant_id, idempotency_key)` has no exception handler around the INSERT; two genuinely concurrent callers with the same idempotency key both pass the pre-insert check, the loser gets a raw, unclassified `23505` instead of the promised idempotent replay — reachable from this batch's two new certificate-expiry-batch RPCs | HIGH | **CONFIRMED**, live-reproduced with two real, genuinely concurrent OS `psql` processes calling `run_training_certificate_expiry_batch` with the same period label: the loser received the raw constraint-violation error before the fix | **FIXED at the shared primitive** (same migration) — wrapped the INSERT in `begin...exception when unique_violation`, re-selecting and returning the existing row exactly as the pre-insert branch already does. Re-verified live: the identical race now returns the SAME `job_id` to both callers. |
| 3 | 283 | Security / RLS | The RLS SELECT policies on `app.performance_reviewer_assignments`/`app.performance_calibration_adjustments` call `app.evaluate_permission(...)` directly inline — granted to `service_role` only, not `authenticated` — breaking raw-table SELECT for a genuinely authorized actor | MEDIUM | **CONFIRMED** live with `approver@kpi1.test` (holds `HRS:Approve/Override/View/View personal data`); fails closed, no data leaked; a repository-wide policy scan found exactly these 2 remaining instances (the 3 in Payroll/Finance-handoff were already fixed at HRT-282's own Tier C review) | **FIXED** — same migration, routed both through the already-existing `app.check_performance_authority` `SECURITY DEFINER` wrapper every sibling policy in the same migration already uses, preserving each policy's exact original predicate. Re-verified: the same actor now reads both tables; a zero-permission actor's own-row-only scope is unchanged (no widening). |
| 4 | 284 | Spec-compliance | `app.attach_training_certificate_evidence` (real, tested PLT-128 malware-scan/tenant/record-type gate, §16's own named security requirement) had zero UI caller anywhere; the Certificates section's own description text claimed the capability existed regardless | HIGH | **CONFIRMED** by direct code read: zero occurrences outside the wrapper and its own unit test; the checkpoint's own §6/§10 disclosure list correctly named it as a no-UI-caller wrapper but undersold that it defeats a named security requirement, unlike its 7 genuinely-cosmetic siblings | **FIXED** — wired end-to-end (`app/(tenant)/[tenantSlug]/hris/training-talent/{actions.ts,page.tsx,training-talent-panel.tsx}`): a real "Attach evidence"/"Replace evidence" form per certificate, `HRS:Edit`-gated exactly as the RPC requires. Section copy corrected to stop overclaiming provider-evidence coverage (see finding 8). |
| 5 | 283 | Spec-compliance | Reviewer reassignment (`app.reassign_performance_reviewer_assignment`, decision 5's own mandatory-reading "does NOT silently transfer already-submitted reviews" rule) was real and tested at the RPC layer, but the admin panel's own bound action was `void`-ed and never rendered | MEDIUM | **CONFIRMED** by direct code read: `void reassignPerformanceReviewerAssignmentAction;`, zero form anywhere | **FIXED** — `page.tsx` now fetches `listPerformanceReviewerAssignments` for the current cycle; `kpi-performance-panel.tsx` renders each assignment with a real reassign form (new assignee + mandatory reason). Re-verified: `next build` succeeds; the RPC's own frozen-history/self-block behavior (already live-proven in HRT-283's own db-test) is unchanged. |
| 6 | 283 (functions), 284 (disclosure) | Cross-prompt integration | HRT-284's own disclosed "3 pre-existing HRT-283 ATW-032 findings" (`acknowledge_performance_outcome`/`submit_performance_appeal`/`submit_performance_self_assessment`) are a live, currently-red `rbac-enforcement.sql` Tier A gate | HIGH (gate breakage; underlying claim is a false positive) | **CONFIRMED live-broken gate**; the "missing authority check" claim itself is **independently disproven** by reading each function's own live `pg_get_functiondef`: all 3 perform a genuine, direct self-scope equality check before any disclosure/mutation — the mechanical sweep's keyword-pattern `base` list simply doesn't credit a raw equality predicate as an authority check (the identical false-positive class its own `get_self_employee` allowlist entry already documents) | **FIXED at the gate** (correct disposition — no defect in the 3 functions to fix): `scripts/db-tests/rbac-enforcement.sql`'s `v_expected` allowlist gained all 3 names with a written justification, mirroring the gate's own documented escape hatch. Re-verified live: `rbac-enforcement.sql` now reports `ALL PLT-112 db-test assertions passed`. |
| 7 | 285 | Spec-compliance | The MSS team roster (`TEAM_LIST_BOUND=50`) and per-category approval queue (`TEAM_QUEUE_BOUND=20`) genuinely truncate with no caller-visible signal, contradicting section 17's own "cursor team queues" framing | MEDIUM/HIGH | **CONFIRMED** by direct code read: both bounds are real `.slice()` truncations; `team-workspace-panel.tsx` had zero UI element indicating truncation before this fix; not disclosed in the checkpoint's own §7/§9/§10 | **FIXED (the silent half)** — `getMssTeamWorkspace` now over-fetches team by one row past the bound to detect `teamTruncated`; each approval-queue category's own pre-slice, team-scoped length is compared against the bound to compute `approvalQueueTruncated`; `team-workspace-panel.tsx` renders a visible disclosure banner on both cards when either flag is true. **Not fully built**: a genuine cursor-based "load more" interaction remains undone — `app.list_my_team_employees` already supports a real cursor (`afterEmployeeNumber`) this checkpoint doesn't yet wire through; disclosed as `ISS-2026-084` (`docs/runtime/KNOWN_ISSUES.md`), Low residual severity now that the silent-truncation harm itself is closed. |
| 8 | 284 | Spec-compliance | §16 names "provider/certificate files are private/scanned" (both nouns); only the certificate half was ever built — `app.training_providers` has no evidence-file column or attach RPC of any kind, and the migration's own header comment overstated coverage | LOW | **CONFIRMED** by direct schema read: zero evidence-file column, zero `*provider*evidence*`-shaped function; may be reading more into §16's phrasing than intended (§13's own database-impact list never separately names a persisted provider-evidence record) | **DISCLOSED, not built** — registered as `ISS-2026-083`; the immediately actionable half (UI copy overclaiming provider coverage) was corrected as part of finding 4's own fix. Building a genuine provider-evidence feature is a bounded feature addition, left for a dedicated follow-up if confirmed in scope. |
| 9 | 285 | Spec-compliance | `EssHomeSummary.pendingLeaveRequestCount` was a hardcoded `0` on a public composition contract, alongside otherwise-real sibling counts (`pendingOvertimeRequestCount`/`pendingTimesheetEntryCount`) | LOW | **CONFIRMED** by direct code read; harmless only because the field was never rendered — a fake value on a typed contract next to real siblings, matching the DoD's "no fake persistence" language applied to a computed/returned value | **FIXED** — `getEssHomeSummary` now calls `listLeaveRequests` filtered to the caller's own server-resolved `profile.masterRecordId` (never a client-supplied id — the same server-resolved-identity discipline every other field in this file already follows) and `status: "pending_approval"`, counting the real result. Now rendered in `ess-home-panel.tsx` alongside its siblings. Re-verified: the underlying RPC's own predicate always permits an actor to see their own rows regardless of role, so this cannot disclose anything beyond what the caller could already see. |

**Everything else all four lenses independently reconfirmed clean, re-verified rather than accepted from citation:** C-24 audit-masking discipline (zero raw score/rating/free-text reason field reaches `app.audit_logs` across all 25+47 `capture_audit_event` call sites in the two new migrations); cross-tenant RLS isolation on every new table; the k-anonymity floor (k=5) on both new aggregate reports, independently reproduced with a real 2-person suppressed cohort and a synthetic 5-person unsuppressed one on each; the manager-effective-team-only scoping pattern; HRT-285's own dual-role composition-layer defense (`teamIds.has(...)` re-filter) independently reproduced as genuinely load-bearing; training-session capacity/waitlist and calibration-adjustment concurrency both independently re-raced with genuine concurrent OS processes and found correctly serialized; exact-decimal scoring arithmetic (zero `float`/`double precision` on any score/weight/target column); every natural-key-guarded create function's check-then-insert exception handling (12 functions checked, all correct, matching the pattern finding 2's fix now also follows).

### 12.3 Propagation sweep (`BUILD_EXECUTION_PROTOCOL.md` §5.4)

- **Ambiguous-`id` shape (finding 1):** every `RETURNS TABLE(id ...)` function in both migrations (134 total) was checked programmatically against its own live `pg_get_functiondef` output for a bare `where id = p_...` reference — not by grep, not by trusting the lens's own citation. Exactly 7 matched; all 7 fixed; no further instance found in either migration.
- **`enqueue_job` race shape (finding 2):** every `app.enqueue_job(...)` call site across all three migrations was enumerated (grep-confirmed) — exactly 2, both in HRT-284's own two new certificate-expiry batch RPCs. Fixing the shared primitive itself (rather than patching each call site) means every OTHER existing caller of `app.enqueue_job` repository-wide also gets the fix, without needing to enumerate them individually.
- **`evaluate_permission`-direct-in-RLS shape (finding 3):** a fresh scan of every `create policy ... using (...)` block's own `pg_get_expr` output across the FULL `supabase/migrations/` tree (not just this batch's own two migrations) found exactly 2 remaining instances repository-wide, both in HRT-283's migration, both fixed; the 3 HRT-282 Finance-handoff instances were independently confirmed already fixed at that checkpoint's own Tier C close, unaffected by this batch.
- **No-UI-caller-defeats-a-named-security-requirement shape (finding 4):** of HRT-284's own 8 disclosed no-UI-caller wrappers, only `attachTrainingCertificateEvidence` ties to a named security requirement (§16); the other 7 remain genuine, disclosed, non-security UI-completeness gaps matching this repository's own established precedent (`ISS-2026-075`/`076`/`080`-shaped) and were deliberately NOT swept into this fix (building 7 more admin forms under review-pass time pressure would be scope creep, not a bounded defect repair). HRT-283's own analogous no-UI-caller gap (reviewer reassignment, finding 5) WAS fixed, because it ties to a named mandatory-reading business rule (decision 5), the same bar finding 4 applies.
- **ATW-032 false-positive self-scope shape (finding 6):** the gate's own `v_unauthorized` computation is exhaustive (not merely first-failure) — its error message named exactly 3 functions, all reviewed and disposed; no further instance was hiding behind the first 3.
- **Silent-truncation shape (finding 7):** `TEAM_LIST_BOUND`/`TEAM_QUEUE_BOUND` are the only two bounded-page-size constants in `server/queries/self-service.ts`; both got the same fix (an over-fetch-by-one/pre-slice-length truncation signal). No third bounded list exists in this file.
- **No further instances found in any of the six sweeps beyond what's listed in §12.2.**

### 12.4 Corrective migration and non-migration fixes

One new, additive migration: `supabase/migrations/20260731050000_harden_hris_kpi_performance_training_talent_batch_283_285_review_fixes.sql` (7 `create or replace function` statements fixing finding 1 across both prompts' migrations, one `create or replace function app.enqueue_job` fixing finding 2, two `drop policy`/`create policy` pairs fixing finding 3). Neither `20260731030000` nor `20260731040000` was edited in place. HRT-285 (Prompt 285) contributes zero migration change, matching its own original "zero new migration" shape.

Shared test-infrastructure fix: `scripts/db-tests/rbac-enforcement.sql` (finding 6's `v_expected` allowlist addition).

Service-layer/UI changes (not migrations): `app/(tenant)/[tenantSlug]/hris/kpi-performance/{page.tsx,kpi-performance-panel.tsx}` (finding 5, reviewer-reassignment UI); `app/(tenant)/[tenantSlug]/hris/training-talent/{actions.ts,page.tsx,training-talent-panel.tsx}` (finding 4, certificate-evidence-attach UI + copy correction); `server/contracts/self-service/self-service.ts`, `server/queries/self-service.ts`, `server/queries/self-service.test.ts`, `app/(tenant)/[tenantSlug]/hris/team/{page.tsx,team-workspace-panel.tsx}` (finding 7, truncation indicators); `app/(tenant)/[tenantSlug]/hris/my/ess-home-panel.tsx` (finding 9, real leave count rendered).

### 12.5 ISS-2026-077 re-confirmation (A/B technique, per this task's own mandatory instruction)

**On today's date (`2026-08-14`), `hris-leave-permit-business-trip.sql`'s own previously-failing assertion PASSES cleanly** — the opposite of the `2026-08-13` snapshot this entry originally recorded, and the reason the full official `pnpm run db:test` harness (§12.6 below) ran to completion today instead of aborting. Independent A/B reproduction (batch migrations `20260731030000`/`20260731040000`/`20260731050000`, plus their own db-test fixture files, temporarily removed from the tree entirely vs. present, each leg run standalone against a fresh disposable database) shows **identical behavior in both legs (both pass today)** — confirming, as required, that this bug's presence/absence has nothing to do with the `283-285` diff, regardless of which direction the pass/fail happens to point on a given date. This newly confirms the entry's own prior root-cause hypothesis: the fixture's "negative control" employee's own late-exception scenario is genuinely tied to `extract(dow from current_date)` (day-of-week), not merely "current wall-clock date" vaguely — a fixture whose own pass/fail flips between two dates one day apart is not a reliable Tier A signal regardless of which direction it happens to point today. Full detail and the corrected reproduction guidance now recorded in `docs/runtime/KNOWN_ISSUES.md`'s `ISS-2026-077` entry. Still `OPEN` (the underlying date-relative fragility is real and unfixed even though today's run happens not to trip it), still out of this batch's authorized scope (HRT-280's own already-`VERIFIED` territory).

### 12.6 Fresh gate suite (full, independent re-run — not accepted from any fix agent's self-report)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**, 205 warnings — identical pre-existing baseline, zero new warnings from this batch's own files |
| `pnpm run test` | **3895/3895 pass, 0 fail** (includes new regression coverage added during this fix pass: `teamTruncated`/`approvalQueueTruncated`/`pendingLeaveRequestCount` in `server/queries/self-service.test.ts`) |
| `pnpm run db:test` (official harness, full run from scratch, `TEST_DB_NAME=cargogrid_db_test_final283285`) | **`ALL PASSED`, exit 0 — the full official harness ran to completion today**, every test file in the repository, including `hris-kpi-performance.sql`, `hris-training-talent.sql`, and (unlike HRT-282's own `2026-08-13` batch-close run) `hris-leave-permit-business-trip.sql` itself, because `ISS-2026-077`'s own date-relative assertion does not trip on today's date (§12.5). Independently, this task's own mandated standalone-per-file technique was ALSO run directly, against a separately-built fresh, full 206-migration database, confirming the same result in isolation: `scripts/db-tests/hris-kpi-performance.sql` exit 0, every assertion passes; `scripts/db-tests/hris-training-talent.sql` exit 0, every assertion passes; no `hris-self-service.sql` exists (HRT-285 ships zero migration/fixture, matching its own disclosed shape — nothing to standalone-test at the database layer). |
| `pnpm exec next build` | `✓ Compiled successfully` — all affected routes present (`/[tenantSlug]/hris/kpi-performance`, `/[tenantSlug]/hris/training-talent`, `/[tenantSlug]/hris/team`, `/[tenantSlug]/hris/my`), including the new reassignment/attach-evidence/truncation-banner UI |
| `pnpm run docs:check` | clean |
| `pnpm run security:check` | clean — no secret-shaped pattern |
| `pnpm run security:audit` | clean — no dependency advisories |
| `pnpm run data-classification:check` | clean |
| `pnpm run threat-model:check` | clean — 25 entries (4 critical, 11 high, 9 medium, 1 low), unchanged by this fix |
| `pnpm run standards:check` | clean — no suppression-governance violations |
| `pnpm run git:check-paths` | clean |
| `pnpm run git:check` | clean (branch name and worktree-collision checks both pass; expected uncommitted changes and unmerged-ahead-of-origin state noted, matching every prior batch-close run) |

Plus direct code reads of every Critical/High fix, confirming each is genuinely present and correctly shaped (not merely asserted) — all 7 ambiguous-`id` fixes, the `enqueue_job` exception handler, both RLS policy replacements, the certificate-evidence-attach UI wiring, and the `rbac-enforcement.sql` allowlist addition were each independently live-re-reproduced-as-fixed in this same session (the identical reproduction sequence re-run against the corrected code), not merely read and assumed correct.

### 12.7 New recurring-defect-taxonomy class

No new class. Finding 1 (ambiguous bare `id` against a `RETURNS TABLE` output column) is a second, independent recurrence of the exact class `docs/build-log/phase-07/HRT-278.md` §12/Finding 2 already registered during the `278-280` batch's own Tier C review, now against a different capability pair — reinforcing, not creating, that class's entry in `docs/standards/RECURRING_DEFECT_TAXONOMY.md` §4. Finding 2 (check-then-insert race with no exception handler) is likewise a recurrence of the class HRT-282's own Tier C review registered.

### 12.8 Task state

`CG-S12-HRT-011` (Prompt 283, KPI and Performance), `CG-S12-HRT-012` (Prompt 284, Training and Talent), and `CG-S12-HRT-013` (Prompt 285, Employee and Manager Self-Service) are now all **`VERIFIED`**, moved together per `BUILD_EXECUTION_PROTOCOL.md` §4's all-or-nothing batch rule. `docs/runtime/TASK_LEDGER.md` and `docs/build-log/phase-07/00_EXECUTION_INDEX.md` rows 283/284/285 updated accordingly.

`CG-S12-HRT-014` (Prompt 286, Internal and Interdepartmental Ticket) is dependency-clean at the task-state level — its own stated upstream is `HRT-273` alone (Platform user/org/workflow/file/notification/job foundations plus verified canonical linked records), already satisfied before this batch and unaffected by the HR track's own completion (HRT-286's own §9 never cites HRT-274 or any HR-track prompt — `00_EXECUTION_INDEX.md` §6's own architecture note already established this). **This does NOT itself authorize starting Prompt 286** — per this repository's own standing, repeatedly-applied precedent (every prior phase/range-boundary hold in this build), a fresh, separate, explicit operator authorization naming Prompt 286 is still required before implementation may begin.

One commit made across the whole batch's fix pass: "agent: fix batch review findings (CG-S12-HRT-011..013, Prompts 283-285)" — not pushed, per this task's own instruction. `docs/runtime/HANDOFF.md` intentionally not touched (outside this fix task's own scope).
