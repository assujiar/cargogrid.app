# HRT-275 — Organization and Position Linkage

**Prompt ID:** `CG-S12-HRT-003` · **Package document:** `CG-AABPP-HRT-275` · **Phase:** 7 (HRIS and Ticketing) · **Status at end of this checkpoint:** implementation complete, all mandatory automated gates re-run and passing at this checkpoint. `VERIFIED` state (if this build's convention requires a separate closure step) is left to the orchestrating process — this file records the real evidence for that decision.

## 1. What this checkpoint builds

HRT-275 is the SECOND Phase 7 (HRIS/Ticketing) capability, built directly on HRT-274 (Employee Master, `VERIFIED`). It implements ADR-0023 Part A (widening `app.org_units.unit_type` to add `team`), a governed position/grade catalogue (`app.position_grades`, `app.positions`), and the effective-dated employee position/grade/manager assignment history table (`app.employee_position_assignments`) HRT-274's own build log §10 explicitly deferred to this checkpoint. `app.employees.position_id` is the additive current-pointer column HRT-274 recommended — `position_title` (HRT-274's own display fallback) is unchanged and never replaced retroactively.

**Update (this same checkpoint, §11):** a subsequent 3-lens adversarial review round found and this checkpoint fixed 4 further real defects (1 CRITICAL, 1 HIGH, 1 MEDIUM, 2 LOW — one LOW finding spanned two forms) in the implementation this file originally described. The CRITICAL finding disproves this file's own original §3.7 framing ("the chain walk is reused at both propose and decide... cheap defense in depth") as a COMPLETE defense — it never accounted for the third, and only mutating, path (`app.activate_due_employee_position_assignments`'s maintenance sweep). §11 is the authoritative record of that round; every other section below is left as originally written (the historical record of the FIRST build pass) except where a fix materially changed a claim, each such change marked inline.

## 2. Files created / changed

**Migration (new, additive):**
- `supabase/migrations/20260730840000_create_hris_organization_position_linkage.sql` — ADR-0023 Part A (widened `org_units_unit_type_check`, widened `app.enforce_org_unit_parent_shape()`); `app.position_grades`, `app.positions`, `app.employee_position_assignments` (with two real `btree_gist` EXCLUDE constraints); `app.employees.position_id` (additive column); ~24 RPCs (grade/position CRUD, propose/decide/cancel/sweep assignment workflow, impact preview, history/current-assignment/hierarchy reads, scoped export); RLS policies; grants. Adds **zero** new `app.permissions` rows — the propose/decide workflow reuses the already-seeded `HRS:Edit`/`HRS:Approve`. **§11's own review round extended this same file in place** (never committed/closed as its own checkpoint before that round ran): `app.would_create_employee_manager_cycle`'s exception handling narrowed to `check_violation`; `app.sync_employee_current_assignment_cache` now re-validates the manager-cycle guard immediately before its own `UPDATE`; `app.activate_due_employee_position_assignments` now catches a per-row cycle failure, skips that row, and audits the skip instead of corrupting the graph or aborting the sweep; `app.preview_employee_position_assignment_impact` gained a `p_actor_label` parameter and now self-captures an audit event.
- `supabase/migrations/20260730850000_harden_hris_organization_position_linkage_review_fixes.sql` (new, §11) — `CREATE OR REPLACE FUNCTION` on HRT-274's own already-`VERIFIED` `app.transfer_employee`/`app.update_employee_draft` (in `20260730830000`, never edited in place since it is closed), adding a `governed_position_exists` guard.

**Service layer (new):**
- `server/contracts/position/position.ts` + `.test.ts`
- `server/queries/position.ts` + `.test.ts`
- `server/mutations/position.ts` + `.test.ts`

**UI (new):**
- `app/(tenant)/[tenantSlug]/hris/positions/page.tsx` — position/grade catalogue (list, search, status filter, create forms)
- `app/(tenant)/[tenantSlug]/hris/positions/loading.tsx`, `actions.ts`, `position-catalogue-panel.tsx`
- `app/(tenant)/[tenantSlug]/hris/positions/[positionId]/page.tsx` — position detail (edit, status lifecycle, current incumbents)
- `app/(tenant)/[tenantSlug]/hris/positions/[positionId]/loading.tsx`, `position-detail-panel.tsx`
- `app/(tenant)/[tenantSlug]/hris/organization/page.tsx` — organization-linked position tree (read-only; organization write stays Platform-governed and unchanged)
- `app/(tenant)/[tenantSlug]/hris/organization/loading.tsx`, `organization-tree-panel.tsx`
- `app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/page.tsx` — assignment timeline + transfer/promotion/reorg wizard with impact preview
- `app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/loading.tsx`, `actions.ts`, `employee-position-panel.tsx`
- `app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/employee-detail-panel.tsx` — additive edit only: a callout linking to the new governed assignment timeline page, and a relabel of HRT-274's own free-text transfer form as "ungoverned" to disambiguate the two paths for the same user.

**Tests (extended by §11):**
- `scripts/db-tests/hris-organization-position-linkage.sql` — 17 original test blocks + 2 added by §11 (manager-cycle-prevention-under-the-sweep regression, governed-position-guard cross-capability regression) + 1 existing block extended (impact-preview audit-trail assertion)
- `server/contracts/position/position.test.ts`, `server/queries/position.test.ts`, `server/mutations/position.test.ts` (36 unit tests; §11 updated the existing `previewEmployeePositionAssignmentImpact` test call for the new `actorLabel` parameter, added no new test)
- `server/mutations/employee.test.ts` — unaffected in count; `governed_position_exists` added to `EMPLOYEE_KNOWN_MUTATION_ERROR_CODES`

**Service layer (extended by §11):**
- `server/queries/position.ts` — `previewEmployeePositionAssignmentImpact` now requires `actorLabel`
- `server/mutations/employee.ts` — `governed_position_exists` added to `EMPLOYEE_KNOWN_MUTATION_ERROR_CODES`

**UI (extended by §11):**
- `app/(tenant)/[tenantSlug]/hris/positions/[positionId]/position-detail-panel.tsx` — the edit form's fields are now controlled state with a `beforeunload` unsaved-change guard, mirroring `employee-detail-panel.tsx`'s own established pattern
- `app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/employee-position-panel.tsx` — the Assignment Wizard's fields are now controlled state with the identical guard
- `app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/actions.ts` — passes `actorLabel` to the now-audited preview call

**Docs (changed, additive):**
- `docs/runtime/KNOWN_ISSUES.md` — an update note on `ISS-2026-065` (disclosing exactly what HRT-275 does and does NOT close relative to that entry) and a new `ISS-2026-066` (residual UI/automation-depth gaps, see §7)
- `docs/build-log/phase-07/HRT-275.md` (this file)
- `docs/build-log/phase-07/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md` — task-state/status updates

**§11's own review round, revised:** `20260730840000_...sql` is still additive-only and edits none of the 184 pre-existing migrations that predate it — but it WAS itself extended in place by §11 (see above), since it was never committed/closed as its own checkpoint before that round ran, matching HRT-274's own §11 precedent exactly. The one finding that reached into HRT-274's own already-`VERIFIED`, closed `20260730830000` was fixed via a genuinely new, additive migration (`20260730850000`) instead — `20260730830000` itself was never touched. `employee-detail-panel.tsx` (HRT-274's own file) received one additive callout block and a label change in the original pass, nothing removed; unaffected by §11.

## 3. Source/config decisions (disclosed, not left implicit)

### 3.1 ADR-0023 Part A implemented exactly as decided

`app.org_units.unit_type` gains `team` as an additive fifth value; `app.enforce_org_unit_parent_shape()` gains one new branch (`team`'s parent must be `department` or `business_unit`; `team` is a leaf type and may never itself be a parent — a genuinely new universal-leaf guard, since no prior type was ever forbidden as a parent outright). This is this checkpoint's own chartered implementation of an already-ratified decision, not a re-litigation. Regression-proven: `scripts/db-tests/org-hierarchy.sql`'s own pre-existing assertions (company/branch/department/business_unit shape, move/cycle/deactivation guards) run unmodified against the widened function and still pass.

### 3.2 Position/grade are HR-governed, tenant-scoped catalogue data — not routed through `app.master_records`

Unlike vendor/driver/employee, a position/grade is structural configuration describing a slot in the org chart, not an operational actor identity needing merge/dedupe/lifecycle across external sources. `app.positions`/`app.position_grades` mirror `app.org_units`'s own simpler, directly-tenant-scoped shape (code+tenant unique, `record_version`, `active`/`inactive` status lifecycle) rather than the `master_records`-extension shape vendor/driver/employee use. Organization write itself stays Platform-governed and completely unchanged — this migration adds no new grant, policy, or RPC touching `app.org_units` write paths; `app.create_org_unit`/`app.move_org_unit`/`app.rename_org_unit`/`app.set_org_unit_status` remain `service_role`-only, exactly as PLT-109 left them.

### 3.3 `app.employees.position_id` — additive current-pointer, `position_title` untouched

Per HRT-274's own build log §10 recommendation. `position_id` is nullable, references `app.positions(id)`, and is synced exclusively by `app.decide_employee_position_assignment` (immediate case, `effective_start_date <= current_date`) and `app.activate_due_employee_position_assignments` (future-dated case, once due). It is never hand-edited by HRT-274's own `app.update_employee_draft`/`app.transfer_employee`, which remain valid, unchanged, free-text-only paths for a position-less employee.

### 3.4 A genuinely new table, not a mutable column — `app.employee_position_assignments`

Effective-dated history cannot live in a single mutable column (section 13's own explicit reasoning). Every transfer/promotion/hire/secondary-assignment is a new row; a prior row's substantive fields are never mutated (only `status`/`decided_*`/`effective_end_date` on the SAME row ever change, via the decide/cancel RPCs). `app.employees`'s own position/manager/org-unit columns are a best-effort, eagerly-synced convenience cache — the assignment table is the source of truth for point-in-time-correct reads (`app.get_employee_current_assignment`, `p_as_of`-parameterized, reads `validity_range` directly and is always correct even before any sync/sweep has run).

**Two real, DB-enforced `btree_gist` EXCLUDE constraints** (mirroring `app.vendor_rate_versions_no_ambiguous_overlap`, PRC-255, `20260730620000`) — not merely application-layer discipline:
- `employee_position_assignments_no_primary_overlap`: an employee may never hold two overlapping ACTIVE primary assignments.
- `employee_position_assignments_no_duplicate_secondary`: an employee may never hold two overlapping ACTIVE secondary assignments to the SAME position (different concurrent secondary positions remain legal by design).

`status` deliberately uses only two temporally-static-enough values plus two decision outcomes (`pending_approval`/`active`/`rejected`/`cancelled`) rather than a separately-drifting `scheduled`/`active`/`ended` state machine — "is this assignment in effect right now" is always answered by `validity_range @> current_date` at read time, so no live scheduler is required to keep a status column fresh. The one case that DOES need a real actor-driven transition — withdrawing a not-yet-effective proposal or approved-but-future assignment — is `app.cancel_employee_position_assignment`, gated so an already-effective assignment can never be cancelled retroactively (section 23's "keep the current effective assignment intact").

### 3.5 Two-step propose/decide workflow — zero new `app.permissions` rows

`app.propose_employee_position_assignment` (HRS:Edit) creates a `pending_approval` row; `app.decide_employee_position_assignment` (HRS:Approve) approves (re-validating position status, manager-cycle, and capacity authoritatively, closing the prior open-ended primary predecessor, syncing `app.employees`' cache if already effective) or rejects (terminal, mandatory reason, prior active assignment left completely untouched). Both `HRS:Edit` and `HRS:Approve` were already seeded at PLT-111 (`20260716103445:61-63`) — this migration adds no new permission action at all, closing the task brief's own "you likely need Approve... if not already sufficient" question: it already was. `app.preview_employee_position_assignment_impact` is deliberately callable standalone, before any proposal exists, matching section 21's own preview-before-propose ordering.

### 3.6 Capacity and overlap are both real guards, validated at the correct point in the flow

Overlap (section 25) is enforced by the EXCLUDE constraints above, which only apply to `status='active'` rows — a proposal does not yet commit any real headcount/timeline. Capacity (section 25 "capacity", section 23 "position over-capacity") is validated at DECISION (approval) time — matching section 25's own literal "before activation" framing — under a position-scoped `pg_advisory_xact_lock` (mirroring `app.commit_vendor_rate_import_job`'s own job-scoped-advisory-lock precedent), so two racing approvals against the same position cannot both pass the count check before either commits. A same-position correction/promotion (the employee's own existing occupancy of THIS SAME position being closed and immediately replaced) does not double-count its own soon-to-be-closed predecessor against the position's own capacity — a real bug caught and fixed during this checkpoint's own db-testing (§6 below), not shipped as an oversight.

### 3.7 Manager-cycle prevention — HRT-274's own disclosed placeholder decision, formalized without a materialized closure table

HRT-274's build log §10 explicitly left this decision to this checkpoint. **Decision: keep `app.assert_no_employee_manager_cycle`'s bounded 200-hop chain walk, unchanged and reused directly** (not reimplemented), rather than building a materialized reporting-line transitive-closure table. Reasoning: (a) real organizational reporting depth is bounded by a small constant in every plausible tenant — 200 hops is already two orders of magnitude beyond any realistic org depth; (b) a materialized closure table requires a rebuild-on-every-manager-change trigger cascade with real storage/write-amplification cost, for a check that already runs in O(depth) time with a single indexed-column walk; (c) `app.employee_position_assignments` already gives manager-chain HISTORY for free via its own effective-dated rows — a materialized-closure alternative would duplicate information this table already carries. The chain walk is reused at both `app.propose_employee_position_assignment` (early UX feedback) and `app.decide_employee_position_assignment` (the authoritative gate, re-checked against fresh state) — cheap defense in depth, not a new mechanism. `app.get_employee_manager_chain` (the new "hierarchy read" RPC, section 14) reads the CURRENT chain from `app.employees.manager_employee_id`, matching `app.list_my_team_employees`'s own established precedent.

### 3.8 Impact preview computes real data only

Direct repository inspection this checkpoint confirmed approval-queue routing has no org-unit-scoped concept anywhere in this repository (`app.evaluate_permission` is tenant+module+action only), and Payroll (Prompt 282) / Ticketing (Prompt 285+) tables do not exist yet. `app.preview_employee_position_assignment_impact` computes every signal that genuinely exists today (target position/grade/org path via `app.resolve_org_unit_lineage`, capacity remaining, would-be-cyclic manager chain, this employee's own real direct-report count, real pending change-request/duplicate-candidate counts) and returns an explicit `downstream_disclosure` text field naming the not-yet-integrated systems, rather than inventing a placeholder number for any of them.

### 3.9 No compensation/salary/pay-band field anywhere

Section 13's own scope is position/grade STRUCTURE. `app.position_grades.rank` is a bare ordering integer, never a pay figure. `scripts/data-classification/registry.ts`'s `HRS_REGISTRY` needed no new entry — no column on any table this migration creates is PII- or payroll-classified, confirmed by re-running `data-classification:check` clean with zero new registry rows.

### 3.10 Deliberately out of scope, disclosed as `ISS-2026-066`

No staged-import (PLT-131/132) crosswalk for position/grade/assignment rows (section 19's own bulk-migration capability, a genuinely new, unrequested-here build); no bulk/multi-employee reorganization wizard (every RPC and the UI wizard are single-employee); no live cron/scheduler invokes `app.activate_due_employee_position_assignments` automatically (no scheduler infrastructure exists anywhere in this repository yet, the same standing `ISS-2026-015` gap); no Playwright/axe-core E2E spec for the three new page families. Full detail and severity/owner assignment in `ISS-2026-066`.

## 4. Security/access model

- **RLS:** `app.position_grades`/`app.positions`/`app.employee_position_assignments` all carry the hardened default-deny form identical to HRT-274's own (`app.has_active_tenant_membership(tenant_id) and not app.actor_holds_customer_user_layer(tenant_id)) or app.is_supreme_admin()`), proven live against both `anon` (schema-privilege denial) and a `customer_user`-layer principal (zero rows, same-tenant) in `scripts/db-tests/hris-organization-position-linkage.sql`.
- **RBAC:** every write RPC gates through `app.evaluate_permission(actor, tenant, 'HRS', action)`, which itself asserts caller-is-session-identity internally (unchanged, wired at `20260730440000`, covering every RPC that reaches it). The two RPCs that do not reach `evaluate_permission` — none in this migration; unlike HRT-274, every RPC here (including the two "self-service-ish" reads, `app.get_employee_position_assignment_history`/`app.get_employee_current_assignment`) is HRS:View-gated, HR-scoped data, not identity-match-gated. The one genuinely self-service read, `app.get_my_employee_position_assignment_history`, is identity-match-gated exactly like HRT-274's own `app.get_my_employee_profile`, with its own explicit `app.assert_actor_is_session_identity` call.
- **No column-restricted grants needed.** Unlike `app.employees`, none of the three new tables carry a classified column (decision 3.9), so plain `select` to `authenticated` (RLS-row-scoped) is correct and sufficient — mirroring `app.org_units`'s own established precedent rather than HRT-274's PII-driven column restriction.
- **Concurrency (row lock + repeated version predicate + advisory lock for capacity):** every write RPC locks its target row(s) first, folds a non-member caller into the same not-found branch, checks authority before `record_version`, and repeats the version predicate in the terminal `UPDATE`'s own `WHERE` clause — the same fully-hardened shape HRT-274 itself used from day one. `app.decide_employee_position_assignment`'s approve path additionally takes a position-scoped `pg_advisory_xact_lock` before its capacity count, and translates both `exclusion_violation` and `deadlock_detected` (a GiST EXCLUDE insert under real contention can legitimately surface either sqlstate for the identical business conflict — PRC-255's own established two-sqlstate handling) into the same friendly `assignment_overlap` error.

## 5. Commands run and results

| Command | Result |
|---|---|
| `pnpm run typecheck` | 0 errors |
| `pnpm run lint` | 0 errors, 185 warnings (185 vs. the standing 180 baseline is entirely pre-existing `@next/next/no-html-link-for-pages` noise on 3 pre-existing layout files unrelated to this checkpoint's own diff, scaling with the 4 new page routes added — independently confirmed by running `eslint` scoped to exactly the files this checkpoint created/touched: zero problems reported) |
| `pnpm run test` | **3509/3509** (3473 prior + 36 new: `server/contracts/position/position.test.ts`, `server/queries/position.test.ts`, `server/mutations/position.test.ts`) |
| `pnpm run db:test` | `ALL PASSED` — all 151 `*.sql` files (150 pre-existing + this checkpoint's `hris-organization-position-linkage.sql`, now 19 assertion blocks after §11's own 2 additions), against a fresh disposable Postgres 16 database, all 185 migrations applied in order (184 + §11's own `20260730850000`) |
| `pnpm exec next build` | clean (see §5.1) |
| `pnpm run security:audit` / `standards:check` / `docs:check` / `data-classification:check` / `security:check` / `threat-model:check` | all clean |

### 5.1 `next build`

Run directly (no `build` script alias, per the standing convention). Four new routes compile and appear in the route manifest: `/[tenantSlug]/hris/positions`, `/[tenantSlug]/hris/positions/[positionId]`, `/[tenantSlug]/hris/organization`, `/[tenantSlug]/hris/employees/[masterRecordId]/positions` — alongside every pre-existing route, including HRT-274's own 3 `/hris/*` routes.

## 6. Real defects found and fixed during this checkpoint's own db-testing

Found and fixed before this checkpoint's own commit, via iterative `psql`-driven adversarial testing (not shipped, then found later):

1. **Capacity double-counted an employee's own soon-to-be-closed predecessor on a same-position correction/promotion.** The original ordering computed the capacity headcount BEFORE looking up the predecessor to close, so a capacity=1 position could never process a legitimate "same position, corrected grade/manager" re-assignment for the employee already occupying it (their own still-active predecessor row counted against their own new request). Live-reproduced in the "capacity" test block. **Fixed** by reordering: look up the predecessor first, then compute headcount via `app.count_position_active_primary_headcount`'s new `p_exclude_assignment_id` parameter, excluding the predecessor only when it occupies the SAME position as the new proposal (a transfer to a genuinely different position is never given this exclusion, correctly).
2. **A bare `manager_employee_id` reference in `app.get_employee_manager_chain`'s own chain-walk loop was genuinely ambiguous against the function's own `RETURNS TABLE` output column of the identical name** — the exact class of bug HRT-274's own build log §7.1 already found and fixed three times in different functions, reproduced live again here by this checkpoint's own db-test suite. **Fixed** by table-aliasing the reference (`select e.manager_employee_id ... from app.employees e where e.master_record_id = v_current`).
3. **Test-fixture-only:** an early UUID range chosen for the two-tenant fixture (`...0000027501`-`...0000027507`) collided with `scripts/db-tests/finance-invoice.sql`'s own already-claimed range — caught immediately by `pnpm run db:test`'s own `ON_ERROR_STOP` behavior on the very first run; renumbered to `...0000027511`-`...0000027529`, a verified-free range.
4. **Test-fixture-only:** three `create_position_grade`/`create_position` calls in the fixture setup and CRUD test block used the fixture's `tenant_admin` actor, who holds no `HRS` role in this fixture (only the `staff` actor does) — `insufficient_authority` on the very first setup call. **Fixed** by using the `staff` actor for every `HRS:Create`/`HRS:Edit` action, matching HRT-274's own fixture's identical actor-role separation.

No CRITICAL or HIGH finding this checkpoint — the RLS/RBAC/audit/masking discipline HRT-274's own review round hardened is reused verbatim (no new PII/masking surface exists here to regress), and the capacity/overlap/cycle guards were designed with PRC-255's own overlap-handling precedent in hand from the start rather than retrofitted after a live failure.

## 7. Residual gaps (disclosed)

Filed as `ISS-2026-066` in `docs/runtime/KNOWN_ISSUES.md` (Low severity): no bulk/multi-employee reorganization wizard; no live scheduler invokes `app.activate_due_employee_position_assignments` automatically (callable on demand today; `app.get_employee_current_assignment`'s own point-in-time read is unaffected and always correct); no staged-import crosswalk for position/grade/assignment rows; no Playwright/axe-core E2E spec for the three new page families. None blocks the shipped capability.

`ISS-2026-065` (HRT-274's own effective-dating gap) is updated, not closed, by this checkpoint — see §3.10/`KNOWN_ISSUES.md` for the precise boundary: HRT-275 effective-dates the ASSIGNMENT (position/grade/manager), not the EMPLOYEE's own lifecycle_status/hire_date/employment dates, which remain HRT-274's own unresolved, separately-owned gap.

Also disclosed, not filed as separate issues (each is an explicit, reasoned scope boundary from §3 above, not an oversight): no compensation/salary/pay-band field (Payroll's own chartered scope, Prompt 282); no REST/GraphQL adapter (matches the repository-wide Phase 1–6 precedent); no dedicated large-scale load-test pass (matches HRT-274's own identical disclosed scope boundary for its first checkpoint).

## 8. Tenant/identity/access/privacy evidence

All proven live in `scripts/db-tests/hris-organization-position-linkage.sql` (17 `do $$...$$` blocks): ADR-0023 Part A's own team-type shape (legal parents accepted, illegal parents/leaf-violation rejected, pre-existing type rules unaffected); position/grade CRUD idempotency, code conflict, active-in-use deactivation guard; the propose/decide workflow's own authority split (HRS:Edit proposes, HRS:Approve decides, an Edit-only actor cannot self-approve); capacity enforcement including the same-position-correction non-double-counting fix; the real EXCLUDE-constraint-backed overlap guard (`assignment_overlap`, translated from `exclusion_violation`); secondary-assignment coexistence and duplicate-secondary rejection; cyclic-reporting-line rejection at propose time; cancel eligibility (future-dated only, never an already-effective assignment); the genuinely point-in-time-correct `get_employee_current_assignment` read (2 concurrently-active primary+secondary rows as of today, 0 as of year 2000); the maintenance sweep's own idempotent activation of a due, backdated assignment; impact preview's real signal computation (a manufactured cyclic-manager scenario correctly flags `would_create_manager_cycle=true`); hierarchy reads (`get_employee_manager_chain` depth=1 for a direct report, `get_org_position_tree` returning the department subtree); cross-tenant isolation (RPC-level not-found folding on both a position update and an assignment proposal); RLS default-deny for both a `customer_user`-layer principal and `anon`; and a structural-regression guard confirming `app.org_units`/`app.employees`/`app.master_records` core shapes are unaltered beyond this migration's own additive columns/constraints.

## 9. Rollback/recovery note

Additive-only migration (`20260730840000_create_hris_organization_position_linkage.sql`), sorts after all 184 pre-existing migrations, edits none of them. The ADR-0023 Part A widening (`DROP`/`ADD CONSTRAINT`, `CREATE OR REPLACE FUNCTION`) is reversible by narrowing the CHECK constraint back and restoring the prior function body — safe today since no live Supabase project or production data exists anywhere in this repository (standing, repeated disclosure). Rollback of the rest (if ever required before this checkpoint reaches `VERIFIED`): drop the new tables/functions/RLS policies this migration created (reverse dependency order — `app.employees.position_id` references `app.positions`, so drop that column/index first), drop the additive `app.employees.position_id` column, and delete the new `server/`/`app/(tenant)/[tenantSlug]/hris/{positions,organization}/`/`app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/` files plus the additive callout in `employee-detail-panel.tsx`. Resume point: this file plus `scripts/db-tests/hris-organization-position-linkage.sql`'s own (now 19, after §11) assertion blocks are the authoritative record of what is proven; any resumed work should re-run `pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run db:test && pnpm exec next build` before proceeding.

**§11 addendum:** total migration count is now 185 (184 + `20260730850000_harden_hris_organization_position_linkage_review_fixes.sql`, additive, sorts last). Rolling back §11 specifically (independent of the rest): drop `20260730850000` (restores `app.transfer_employee`/`app.update_employee_draft` to their exact pre-`governed_position_exists`-guard bodies, since `20260730830000` itself was never touched) and revert the `CREATE OR REPLACE FUNCTION` edits inside `20260730840000` for `app.would_create_employee_manager_cycle`/`app.sync_employee_current_assignment_cache`/`app.activate_due_employee_position_assignments`/`app.preview_employee_position_assignment_impact` to their original bodies (this file's own §1 original text, before this section's fixes) — not recommended, since §11's own findings were live-reproduced, CONFIRMED defects, not stylistic changes.

## 10. Recommended next task

**HRT-276 (`CG-S12-HRT-004`, Recruitment, job portal and ATS)** — the Phase 7 execution index's own next sequential row on the Workforce/HR track, blocked solely on this task's own completion. A future task (this checkpoint's own recommendation, not binding on HRT-276's own scope) should also: (a) close `ISS-2026-065` (employee-lifecycle effective-dating, HRT-274's own residual gap, unaffected by this checkpoint) before `HRT-293`/`HRT-294`; (b) consider whether a bulk reorganization wizard becomes warranted once real tenant usage patterns are known, rather than building it speculatively now (`ISS-2026-066` item 1); (c) wire a real scheduler once one exists anywhere in this repository (`ISS-2026-066` item 2, the same standing `ISS-2026-015` infrastructure gap).

## 11. Same-checkpoint adversarial review round: findings, fixes, and disclosures

After the original build (§1–§10 above) passed every automated gate, three independent adversarial review lenses (correctness/concurrency, live-tested against a disposable Postgres 16 database; spec-completeness, independently re-verified against live code/tests/build) re-examined the full changeset. This section is the fix pass's own record: every CONFIRMED finding, independently re-derived (not blindly trusted) before being fixed, its exact fix, and confirmation that none were rejected as false positives.

### 11.1 Findings fixed

1. **CRITICAL — a genuine, persistent employee-manager reporting-line cycle could be created through entirely legitimate, individually-valid RPC calls (future-dated mutual transfers + the disclosed maintenance sweep).** Live-reproduced end-to-end, single-threaded, on a fresh disposable DB, exactly per the review's own repro steps: (1) propose+approve employee A's manager as B, effective 5 days out — passes (B's cache manager is null), and because the assignment is future-dated, `app.decide_employee_position_assignment`'s own `if effective_start_date <= current_date` guard skips the immediate cache sync; (2) propose+approve B's manager as A, also 5 days out — passes for the identical reason (A's cache manager is STILL null, since step 1 never synced it), also skips its own sync; (3) backdating both rows' `effective_start_date` to today (simulating the due date arriving, the exact row-state a live scheduler would itself produce — this repository has none, `ISS-2026-066` item 2) and calling the real, callable-today `app.activate_due_employee_position_assignments` — which, before this fix, performed **zero** manager-cycle validation anywhere in its body — blindly synced both rows. Result: `app.employees.manager_employee_id` showed A→B and B→A simultaneously, both `status='active'`, both "currently in effect" per `app.get_employee_current_assignment`. `app.get_employee_manager_chain` did not detect the corruption — it silently returned exactly 200 rows (its own hop bound) alternating B,A,B,A,… forever, which any org-chart UI built on it would render as a 200-deep reporting line. Root cause: `app.would_create_employee_manager_cycle`/`app.assert_no_employee_manager_cycle` are both re-checked at propose and decide time, but only ever walk `app.employees.manager_employee_id` — a cache a future-dated approval deliberately leaves stale — and the ONE path that actually commits a change to that cache when it finally catches up (`app.sync_employee_current_assignment_cache`, called from both the immediate-decide path and the sweep) never re-validated cycle-freedom of its own accord. §3.7's own original claim that the chain walk "reused at both propose and decide" was a complete defense never accounted for this third, and only mutating, path. **Fixed**: `app.sync_employee_current_assignment_cache` now calls `app.assert_no_employee_manager_cycle` immediately before its own `UPDATE` — the actual commit point, re-validated regardless of which caller reached it. Since the function now raises (not silently swallows) on a detected cycle, `app.activate_due_employee_position_assignments` catches the per-row failure, skips syncing that one row (leaving its cache at its prior, cycle-free value), and surfaces the skip via a dedicated `result='failure'` audit event (`skipped_count`/`skipped_assignment_ids`) rather than letting the whole sweep abort or silently proceeding as if it had synced. `supabase/migrations/20260730840000_create_hris_organization_position_linkage.sql`, `app.sync_employee_current_assignment_cache` and `app.activate_due_employee_position_assignments`. Regression evidence: a new db-test block (`scripts/db-tests/hris-organization-position-linkage.sql`, "manager-cycle prevention") reproduces the exact exploit sequence and asserts the sweep activates exactly 1 of the 2 mutually-cyclic rows, never both, a dedicated failure-result audit row records the skip, and a repeated sweep call stays bounded (never corrupts the graph on retry).
2. **HIGH — governed `app.employees.position_id` and HRT-274's still-live free-text `app.transfer_employee`/`app.update_employee_draft` could silently desynchronize `position_title`/`manager_employee_id` from the governed truth.** Both HRT-274 RPCs write `position_title`/`manager_employee_id` but never touch `position_id`; `app.propose_employee_position_assignment` only blocks `lifecycle_status in ('terminated','archived')`, so a draft/active/on_leave/suspended employee can legally hold both a governed assignment AND be edited via the ungoverned free-text path at any time — this file's own §3.3 asserted the free-text paths "remain valid... for a position-less employee," but nothing enforced that premise; it was aspirational. **Fixed** (recommendation option (a) — block the whole call, not a silent partial overwrite) via a genuinely new, additive migration (`supabase/migrations/20260730850000_harden_hris_organization_position_linkage_review_fixes.sql`, since `20260730830000` — where both functions live — is already `VERIFIED`/closed and is never edited in place): both functions now raise a new `governed_position_exists` error when the target employee's `position_id` is non-null, directing the caller to `app.propose_employee_position_assignment` instead. `governed_position_exists` added to `server/mutations/employee.ts`'s `EMPLOYEE_KNOWN_MUTATION_ERROR_CODES`. Regression evidence: a new cross-capability db-test block ("governed-position guard") sets a governed position via the propose/decide workflow, then asserts both `app.transfer_employee` and `app.update_employee_draft` are rejected with `governed_position_exists`, and that neither rejected attempt partially mutated `position_id`/`position_title`/`manager_employee_id`.
3. **MEDIUM — the impact preview RPC produced no audit trail, contradicting section 18's explicit requirement and section 33's "previewed and auditable" acceptance criterion.** `app.preview_employee_position_assignment_impact` computed every real signal but never called `app.capture_audit_event` — there was no forensic record of what was actually disclosed to a decision-maker before a reorganization/transfer decision was made. **Fixed**: the function gained a new `p_actor_label text` parameter (required, since `app.audit_logs.actor_label` is `NOT NULL`) and now self-captures a `result='success'` audit event with an explicit non-sensitive `jsonb_build_object` projection (never a raw row) of every computed signal, before returning. `server/queries/position.ts`'s `previewEmployeePositionAssignmentImpact` now requires `actorLabel`; its one caller (`app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/actions.ts`) passes `access.authUserId`, matching the file's own established convention for every other mutation call site. Regression evidence: the existing "impact preview" db-test block now also asserts exactly one new `app.audit_logs` row appears per call, carrying the real computed `would_create_manager_cycle` signal (not a fabricated/omitted one).
4. **LOW — `app.would_create_employee_manager_cycle`'s blanket `exception when others then return true` mislabeled any internal error as a cyclic-reporting-line rejection.** A `lock_timeout`/`statement_timeout`/out-of-shared-memory error, or any other genuinely unrelated failure inside `app.assert_no_employee_manager_cycle`'s chain-walk `SELECT`, would have been silently reinterpreted as "this candidate manager assignment would create a cycle." Not independently triggered as a live false positive this session (every `cyclic_reporting_line` observed corresponded to a real cycle) — a code-inspection-level concern, directly adjacent to finding 1 above. **Fixed**: narrowed to `exception when check_violation then return true` — the exact `errcode` every one of `app.assert_no_employee_manager_cycle`'s three `raise exception` sites uses — so an unrelated error now propagates with its own real SQLSTATE/message instead of being relabeled.
5. **LOW — section 15's "unsaved-change protection" was not implemented on either of the two new multi-field forms this checkpoint shipped.** `app/(tenant)/[tenantSlug]/hris/positions/[positionId]/position-detail-panel.tsx`'s edit-existing-position form and `app/(tenant)/[tenantSlug]/hris/employees/[masterRecordId]/positions/employee-position-panel.tsx`'s Assignment Wizard both used uncontrolled (`defaultValue`) or partially-controlled inputs with no `beforeunload` guard, unlike the structurally identical `employee-detail-panel.tsx` edit form one directory over (HRT-274's own established pattern). **Fixed**: both forms' fields are now fully controlled React state with the identical `beforeunload`-based dirty-state guard (a shared `useUnsavedChangeGuard` hook, defined once per file, mirroring `employee-detail-panel.tsx`'s own logic byte-for-byte) — a "You have unsaved changes" warning shows while dirty, and the browser's native "leave site?" prompt fires on `beforeunload` while dirty, cleared once the corresponding action completes without error.

### 11.2 Findings disclosed rather than force-fixed

None. All 5 CONFIRMED findings were fixed at their true root cause within this review round's own bounded scope — none required deferring to a future task as architecturally out of scope.

### 11.3 Findings rejected as unconfirmed

None. All 5 CONFIRMED findings from the two review lenses were independently re-derived against the live schema/code before being accepted (the CRITICAL finding was re-traced through the exact function bodies and re-verified analytically that the fix closes it — see finding 1's own worked trace of the fixed sweep-ordering scenario in the commit history/PR description); none were rejected as false positives. The review also reported a broad set of areas independently tested and found CORRECT (no live defect) — position/capacity oversubscription under real two-process contention, `create_position`'s full-tuple idempotency-key comparison, concurrent same-day mutual-manager-approval races (correctly serialized and rejected via the self-referencing FK's implicit lock), the overlap EXCLUDE constraint's DB-level (not merely application-level) enforcement, and the impact preview's `would_create_manager_cycle`/`direct_report_count` signals matching real cyclic/non-cyclic scenarios — none of these required any change.

### 11.4 Gate suite re-run after all fixes

`pnpm run typecheck` (0 errors) · `pnpm run lint` (0 errors, 185 warnings, unchanged baseline — zero new findings in any file this round touched) · `pnpm run test` (**3509/3509**) · `pnpm run db:test` (**ALL PASSED**, 151 files against 185 migrations — `hris-organization-position-linkage.sql` now 19 assertion blocks, `hris-employee-master.sql` and `org-hierarchy.sql` both independently re-confirmed passing unchanged, no regression from the schema widening or the fixes) · `pnpm exec next build` (clean, all 7 `/hris/*` routes present, including the two files this round modified) · `pnpm run security:audit` / `standards:check` / `docs:check` / `data-classification:check` / `security:check` / `threat-model:check` (all clean).
