# HRT-284 — Training and Talent

**Prompt ID:** `CG-S12-HRT-012` · **Package document:** `docs/ai-agent-build-prompt-package/12-phase-07-hris-ticketing/284_TRAINING_TALENT_PROMPT.md` · **Phase:** 7 (HRIS and Ticketing) · **Status at end of this checkpoint: `VERIFIED`** (batch-close, §13 below) — implementation complete, all mandatory Tier A automated gates green, Tier B taxonomy self-check performed and recorded below (§6), adversarially self-tested against a live disposable Postgres 16 database including three real, genuinely concurrent two-OS-process races, and now independently adversarially re-reviewed (4-lens Tier C, combined `283-285` batch) with every Critical/High finding fixed or disclosed. This is the SECOND of the 3-prompt batch (283, 284, 285); the combined Tier C review ran once, after all three prompts were built, per `docs/standards/BUILD_EXECUTION_PROTOCOL.md` §3.4. Built directly on Prompt 283's own just-committed `20260731030000_create_hris_kpi_performance.sql` (read, never edited).

## 1. What this checkpoint builds

HRT-284 is the thirteenth Phase 7 (HRIS/Ticketing) capability, built directly on HRT-274 (Employee Master), HRT-275 (Organization and Position Linkage), HRT-283 (KPI and Performance — `app.training_development_plans.linked_performance_outcome_id` references its `app.performance_outcomes`), and PLT-128 (private/scanned file evidence) and PLT-131/132 (durable job framework), both reused unchanged. It implements: a competency/skill catalogue; a two-tier versioned course catalogue (course + course version, exactly one published version at a time — mirrors HRT-283's KPI-definition-version shape); internal/external providers; direct course prerequisites, enforced inside the enrollment RPC; scheduled sessions with a real, lock-protected capacity/waitlist mechanism; enrollment (self-service, manager/HR-assigned, and a real bulk mandatory-compliance-training batch), with an enrollment-approval gate, decision, cancellation-with-waitlist-promotion, and reschedule; attendance and completion recording; multi-attempt assessment with a deterministic computed pass/fail; certificates (internal issuance, historical import with source/verification labeling, private malware-scanned evidence attach, verify, revoke, renew) plus real durable-job-based expiry and expiry-reminder batches (PLT-131/132, widening `app.jobs.job_type`/`app.generic_job_types()` a fifth time); development plans and their actions, optionally cross-referencing a specific performance outcome; and a RESTRICTED talent workspace — review cycle/assignment/review (assigned-reviewer-only visibility, explicit non-mutating reassignment), talent pools, and succession candidates — every talent-domain write gated on `HRS:Override`, with a genuine k-anonymity floor (k=5) on the one aggregate report. No code path in this migration writes to `app.employees.lifecycle_status`, any `app.payroll_*` table, any `app.performance_*` table, or any role/permission table — training/talent records never automatically alter pay, employment, or position.

## 2. Files created / changed (all additive; two pre-existing files edited, both purely additive)

**Migration (1 new, additive):**
- `supabase/migrations/20260731040000_create_hris_training_talent.sql` (4,202 lines) — 19 tables (`training_competencies`, `training_courses`/`_course_versions`/`_course_competencies`, `training_providers`, `training_course_prerequisites`, `training_sessions`, `training_enrollments`, `training_assessments`, `training_certificates`/`_certificate_expiry_reminders`, `training_development_plans`/`_plan_actions`, `talent_review_cycles`/`_assignments`, `talent_reviews`, `talent_pools`/`_pool_members`, `talent_succession_candidates`); 79 functions. Widens `app.jobs.job_type`'s CHECK constraint and `app.generic_job_types()` (both, together, matching every prior HRIS-domain adopter's own established shape) to add `'training_certificate_expiry'`/`'training_certificate_expiry_reminder'`. Zero new `app.permissions` row — reuses `HRS:Edit`/`Approve`/`Override`/`View personal data`, the same fixed catalogue every sibling capability reuses. RLS policies; grants; the standing `revoke execute on all functions in schema app from public` belt-and-suspenders statement (ERR-2026-004/PLT-118 convention).

**Service layer (new):**
- `server/contracts/training-talent/training-talent.ts` (560 lines) + `.test.ts` (198 lines, 27 tests) — Zod enums, row schemas/parsers. Several entities are read from both a raw-table-returning mutation and a joined list-projection RPC; the joined display columns are modeled `.optional()` on one shared schema rather than duplicating a second schema per entity (documented at the top of the file).
- `server/queries/training-talent.ts` (261 lines) + `.test.ts` (182 lines, 22 tests) — thin typed wrappers around every read RPC, including the newly-added `listTalentReviewCycles` (see §7, self-found defect 2).
- `server/mutations/training-talent.ts` (598 lines) + `.test.ts` (261 lines, 18 tests) — `TrainingTalentMutationError` classifier (69 known error-code prefixes), thin wrapper per write RPC.

**UI (new, 2 route families):**
- `app/(tenant)/[tenantSlug]/hris/training-talent/{page,loading,actions,training-talent-panel}.tsx` — HR/talent-admin workspace: competency/course/version/provider/prerequisite authoring, session scheduling with capacity display, the enrollment-approval queue (approve/reject), attendance/completion/assessment recording (by enrollment id), certificate issue/import/verify/revoke plus the two durable expiry/reminder batch triggers, development plan authoring (create + add action), and the RESTRICTED talent workspace (cycle create/advance, reviewer assign/reassign, pool create/add-member/remove-member, succession propose/confirm/withdraw, the k-anonymity-floored pool-distribution table) — every restricted section renders unconditionally and legitimately shows empty for a non-`HRS:Override` actor, never a client-side redirect.
- `app/(tenant)/[tenantSlug]/hris/my/training-talent/{page,loading,actions,my-training-talent-panel}.tsx` — employee self-service: browse/enroll in available sessions, manage own enrollments (cancel/reschedule), own certificates, own development plan (self-progress an action), and — when the caller holds an active talent-review assignment — their own assigned talent-review cases with a real submit-review form ("restricted talent reviewers see assigned cases" extended to self-service too, section 26).

**Tests (new):**
- `scripts/db-tests/hris-training-talent.sql` (984 lines) — self-contained two-tenant fixture (`trn1`/`trn2`), own unclaimed UUID range `00000000-0000-0000-0000-0000000284xx`.
- `server/contracts|queries|mutations/training-talent.test.ts` — 67 unit tests total.

**Data classification (additive edits):**
- `scripts/data-classification/registry.ts` — new `TRAINING_REGISTRY` (7 entries): 4 under `HRS:View personal data` (training participation/assessment/certificate/development-plan content, mirrors the established person-scoped pattern) and 3 under the deliberately narrower `HRS:Override` (talent review/pool/succession content — decision 6's own restricted tier).
- `scripts/data-classification/check-registry.ts` — wired `TRAINING_REGISTRY` into the combined `REGISTRY`, following `HRS_REGISTRY`/`PERFORMANCE_REGISTRY`'s established, correctly-wired pattern (unlike the pre-existing, disclosed `PAYROLL_REGISTRY` wiring gap, not mine to fix).

**Docs:** this file (new); `docs/runtime/TASK_LEDGER.md` new `CG-S12-HRT-012` row; `docs/build-log/phase-07/00_EXECUTION_INDEX.md` row 284 updated. `docs/runtime/HANDOFF.md` deliberately not touched, per instruction.

## 3. Source/config decisions (disclosed, not left implicit)

All 12 decisions are recorded in the migration's own header comment — summarized here.

1. **Competency is a single status-lifecycle catalogue row (draft/published/archived), not a two-tier parent+version model.** Unlike a KPI definition or payroll component, a competency here carries no scored/weighted configuration a later edit could retroactively disturb — it is a reference taxonomy tag a course links to (`training_course_competencies`). Course curriculum content is where the genuine two-tier versioning need lives (decision 2).
2. **Course/course-version is a two-tier versioned catalogue, mirrors HRT-283's KPI-definition/version and HRT-282's payroll-component/version exactly** — exactly one published version at a time; every session and enrollment freezes the specific version it was scheduled/enrolled against.
3. **Capacity, waitlist, and prerequisite enforcement all happen inside the enrollment RPC under a session-row lock (taxonomy C-04)** — never a UI-side check. Every function that touches both a session and its enrollments locks the session (parent) before any enrollment (child) row, in the identical order throughout; the one function touching two sessions (reschedule) locks both, ordered by ascending id, to avoid a cross-function deadlock against a concurrent reverse-direction reschedule (taxonomy C-21, applied from the start).
4. **Certificate/provider evidence reuses the established PLT-128 private, malware-scanned `app.files` gate (taxonomy C-10) exactly as `app.record_gps_device_installation`/every onboarding-task-evidence RPC already does.** A certificate is created first, then evidence is attached via a dedicated RPC re-validating tenant, record scope, and clean-scan status at the accepting RPC itself — mirrors `app.attach_payroll_component_version_evidence`'s (HRT-282) two-step shape, avoiding the chicken-and-egg problem of uploading a file scoped to a record that does not exist yet.
5. **Certificate expiry and reminder both reuse the established PLT-131/132 `app.jobs` durable-job framework** (enqueue → self-claim → scan → complete), the identical inline-batch shape `app.run_leave_accrual_batch`/`app.run_leave_carry_forward_batch` (HRT-280) already established — never a second job mechanism.
6. **Talent review/pool/succession is the most restricted tier in this checkpoint: every write gates on `HRS:Override`, and read access is narrower still.** A review cycle/assignment is readable by an `HRS:Override` holder OR the specific employee assigned as reviewer for that case ("restricted talent reviewers see assigned cases", section 26, taken literally). A talent pool and a succession candidate carry no per-case reviewer concept and are `HRS:Override`-only, full stop — never self- or manager-visible, deliberately narrower than every other person-scoped table in this migration.
7. **Talent-domain decisions (succession candidate confirm/withdraw) require a mandatory reason and a structural self-decision block**, mirroring HRT-283 decision 4 — never PLT-123 maker-checker (nothing here is the batch-wide, irreversible, fund-disbursing moment HRT-282 decision 6 reserved that for).
8. **A genuine k-anonymity floor (k=5) on the one aggregate report: talent pool distribution by department.** `member_count` (the raw headcount itself, the disclosive fact in this domain) is nulled and `suppressed=true` for any department with fewer than 5 active pool members, enforced inside the `SECURITY DEFINER` function itself.
9. **Taxonomy C-24 audit-masking discipline applied from the start** — every `capture_audit_event` call passes `p_reason => null` and a reviewed, non-sensitive `after_value` allowlist, never `potential_rating`/`readiness_note`/`risk_of_loss`/`decision_reason`/score/notes/any raw `p_*_reason` parameter.
10. **No rating-band/predictive subsystem**; `potential_rating` is a small, closed, human-entered enum (low/moderate/high), never a computed score. No predictive/AI-driven talent ranking (Step 14 boundary).
11. **Deliberately not built, disclosed rather than silently dropped**: no employee-level competency-proficiency-rating subsystem (only a course-to-competency catalogue link — section 13 never names a persisted "employee skill rating" record of its own); no REST/GraphQL adapter (repository-wide precedent); no per-date multi-day attendance granularity (one summary `attended`/`hours_attended` pair per enrollment); no live notification DISPATCH for certificate expiry reminders — the durable job produces a real, queryable `training_certificate_expiry_reminders` record (the actual scan/detection/dedup mechanism section 20 requires) but does not call `app.queue_notification`, since doing so would require this checkpoint to also author and publish a Platform notification-type config version, a cross-cutting concern with zero precedent anywhere in the repository (grep-confirmed: zero other capability calls it either) — disclosed exactly like PRC-261's identical gap for vendor-contract expiry.
12. **`talent_review_assignments` models exactly one active reviewer per (cycle, subject)** — a disclosed V1 simplification, never a multi-reviewer 360 for talent review (unlike HRT-283's own performance-assessment reviewer model, which does support multiple).

## 4. Security/access model

- **RLS:** catalogue tables (`training_competencies`, `training_courses`/`_course_versions`/`_course_competencies`, `training_providers`, `training_course_prerequisites`, `training_sessions`) are broadly tenant-member-readable ("training may be broadly visible", section 16) — mirrors HRT-283's `performance_kpi_definitions`/`_templates` precedent exactly, hardened against a `customer_user`-layer principal. Person-scoped tables (`training_enrollments`, `training_assessments`, `training_certificates`, `training_certificate_expiry_reminders`, `training_development_plans`/`_plan_actions`) use `app.can_view_hris_training_talent_row` (self/direct-manager/`HRS:View personal data`) — "results... are purpose- and field-restricted" (section 16). Talent-domain tables use `app.can_view_talent_review_row` (review cycle/assignment/review) or a direct `HRS:Override` check (pools/pool-members/succession-candidates) — decision 6's deliberately narrower model, never self, never direct manager.
- **RBAC:** every write gates through `app.check_training_authority` (`SECURITY DEFINER` from the start — HRT-281/282/283's own established lesson).
- **Managers do NOT see talent-domain data by hierarchy at all** — the deliberate narrowing beyond even HRT-282's decision 5.
- **Concurrency:** every version-guarded write locks `FOR UPDATE` then re-checks `record_version` at the terminal `UPDATE`; capacity-affecting functions lock the session row first, always in the same order across every function that touches both a session and its enrollments — live-proven with two genuinely concurrent OS processes, §8.
- **Internal engine function** (`_enroll_employee_in_training_session_internal`, `_training_prerequisites_met`, `compute_training_assessment_passed`) is granted `service_role` only, never `authenticated` — live-verified in `scripts/db-tests/hris-training-talent.sql`.

## 5. Commands run and results (final, fresh gate suite)

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors**, 205 warnings — identical pre-existing `@next/next/no-html-link-for-pages` baseline; zero warnings reference any `training-talent` path (grep-confirmed) |
| `pnpm run test` | **3873/3873 pass** (67 new: 27 contract + 22 query + 18 mutation) |
| `pnpm run git:check-paths` | clean — 18 files checked, 0 forbidden, 0 caution |
| `pnpm run security:check` | clean — no secret-shaped pattern |
| `pnpm run data-classification:check` | clean — registry internally valid, `TRAINING_REGISTRY` correctly wired |
| `pnpm exec next build` | `✓ Compiled successfully` — both new routes present (`/[tenantSlug]/hris/training-talent`, `/[tenantSlug]/hris/my/training-talent`) |
| `pnpm run db:test` (official `scripts/db-tests/run.sh` harness, fresh disposable database, full 202→203-migration set) | confirmed reaches and **passes** `hris-kpi-performance.sql`, then aborts at the pre-existing, unrelated `hris-leave-permit-business-trip.sql` (`ISS-2026-077`) — the exact, disclosed shape this task's own mandatory reading named. `hris-training-talent.sql` sorts alphabetically AFTER the broken file, so the harness never reaches it in-suite either way. |
| Standalone re-run of my own test file against the harness's own already-fully-migrated database (the mandatory workaround) | `exit 0`, all assertions pass |
| Every file sorted after the known-broken one, run individually against a freshly-migrated database (continue-past-`ISS-2026-077` proof, matching HRT-282 Tier C's own verification standard) | 60/60 pass, including `hris-training-talent.sql`, **except** `rbac-enforcement.sql` — see §7 self-found defect 1 |

## 6. Tier B — recurring defect taxonomy self-check (`docs/standards/RECURRING_DEFECT_TAXONOMY.md` §4)

Walked mechanically against this checkpoint's own diff before commit.

- **C-01 (idempotency, full-tuple replay)** — `create_training_competency`/`create_training_course`/`create_training_session` are idempotent on their own natural identity key (tenant+code / tenant+session_code) and return the existing row on a match without comparing other fields — the same catalogue-identity shape `create_performance_kpi_definition` (HRT-283) already established and was not flagged as a defect there: the code/session_code itself IS the identity, so there is no "different target under the same key" ambiguity the way a caller-supplied idempotency KEY has. `_enroll_employee_in_training_session_internal`'s `training_enrollments_active_unique` and `record_training_assessment`'s `(enrollment_id, attempt_number)` are both the genuine, unambiguous natural key of "one active enrollment"/"one attempt N". Clear.
- **C-02 (idempotency guard swallowed by its own handler)** — every `exception when unique_violation` block raises with `errcode = 'check_violation'` (distinct from `unique_violation`) and no block is nested inside another handler that could re-catch it. Clear.
- **C-03 (optimistic-version predicate checked but not enforced)** — every versioned `UPDATE ... where record_version = p_expected_version` is immediately followed by a `not found` check raising `stale_version`. Live-reproduced (§8). Clear.
- **C-04 (decision on an unlocked parent/state row)** — every capacity/status decision locks its target row `FOR UPDATE` before deciding; live-reproduced with three genuinely concurrent OS processes (§8). Clear.
- **C-05 (permission check ordered after a disclosing read)** — every mutation's initial `select ... where id = ...` resolves only the row's own `tenant_id` for the authority check that follows immediately after (never returns row content to the caller before the check passes); the shape "not-found vs. insufficient-authority is a distinguishable error code for a cross-tenant probe" is the same repository-wide convention every sibling HRIS capability uses (confirmed against HRT-282/283), not a new pattern introduced here. Clear.
- **C-06 (session-implicit `auth.uid()` reached from an RPC with an explicit actor)** — the only `auth.uid()` defaults are on `can_view_hris_training_talent_row`/`can_view_talent_review_row` (RLS-only use); every RPC passes `p_actor_auth_user_id` explicitly. Clear.
- **C-07 (unmasked snapshot in a jsonb column)** — grep-verified: zero `jsonb` columns anywhere in this migration. N/A.
- **C-08 (widening an existing function)** — the only `create or replace function` is `app.generic_job_types()`, purely additive (widens the array, changes no existing entry, touches no masking/permission logic). Clear.
- **C-09 (exception handler catches too broad a constraint class)** — every `unique_violation` handler sits in a function whose own `INSERT` reaches exactly one unique index. Clear.
- **C-10 (file/evidence linked without re-validating tenant/scope/scan status)** — `attach_training_certificate_evidence` re-validates tenant, `record_type='training_certificate'`/`record_id`, and `malware_scan_status='clean'` at the accepting RPC itself. **Live-proven adversarially**: an infected file, a not-yet-scanned (pending) file, and a clean file scoped to a DIFFERENT record are all three correctly rejected with distinct, classifiable errors; a clean, correctly-scoped file is accepted. Clear.
- **C-11 (grants absent/blanket)** — programmatically cross-checked: all 19 tables carry explicit `select`/`insert,update,delete` grants, all 78 non-`generic_job_types` functions carry an explicit, individually-enumerated grant (zero defined-but-ungranted, zero granted-but-undefined typo risk), zero blanket sweep. Clear.
- **C-12 (`SECURITY DEFINER` granted to `authenticated` with no authority check)** — **FOUND and fixed** during this checkpoint's own adversarial `db:test` iteration (the standing `rbac-enforcement.sql` ATW-032 sweep): `app.enroll_self_in_training_session` relied solely on `app.get_self_employee`'s own join, not credited by the mechanical sweep as an authority check. **Fixed** — see §7 self-found defect 1. **Disclosed, not fixed** (out of this checkpoint's own scope): the SAME sweep, on the SAME live run, independently re-confirmed 3 pre-existing gaps in HRT-283's own already-committed migration (`acknowledge_performance_outcome`, `submit_performance_appeal`, `submit_performance_self_assessment`) — flagged here for the batch's own Tier C propagation sweep, not mine to edit per this task's own explicit instruction ("Do NOT edit Prompt 283's migration or files").
- **C-13 (actor-identity crosscheck)** — every RPC either routes through `check_training_authority`→`evaluate_permission` (which itself calls `assert_actor_is_session_identity`, ATW-031's own established choke point) or, for the handful of `has_active_tenant_membership`-only read RPCs, calls `assert_actor_is_session_identity` explicitly as its own first statement — programmatically cross-checked, all 18 such functions confirmed. Clear.
- **C-14 (Postgres identifier over 63 characters)** — programmatically checked all 124 constraint/index/trigger names; zero violations.
- **C-15 (missing domain constraints)** — non-negativity on every score/hours/capacity/duration column, closed enums on every status/type/enum-shaped column throughout all 19 tables. Clear.
- **C-16 (a `"use server"` module exports something that is not a function)** — every export from both `actions.ts` files is an async function or a plain `interface` (erased at compile time); `next build` ran clean end to end (§5), the authoritative check. Clear.
- **C-17 (`select("*")`/bare `select *` against a column-restricted table)** — grep-verified: every `select *` in the migration is the internal PL/pgSQL `select * into v_row from ...` row-fetch idiom (not a client-facing masking bypass); zero `select("*")` anywhere in the TypeScript service layer. Clear.
- **C-18 (maker-checker enforced happy-path only)** — `decide_training_enrollment` (approve/reject) and `decide_succession_candidate` (confirm/withdraw) both apply the identical self-decision-block check BEFORE branching on the decision value, not per-branch. Clear.
- **C-19 (supersede/uniqueness on a narrower scope tuple)** — every partial unique index's own scope tuple was written out and compared against the real "one active X" invariant it enforces (`training_enrollments_active_unique` on session+employee, `talent_review_assignments_active_unique` on cycle+subject, `talent_pool_members_active_unique` on pool+employee, `talent_succession_candidates_active_unique` on position+candidate). Clear.
- **C-20 (a capability with no reachable UI caller)** — traced every mutation function to a real page/panel form. 24 of 32 admin actions and all 5 self-service actions are wired to a live form. **Disclosed, not silently left** (8 real, tested SQL-layer wrappers with no UI form yet): `archiveTrainingCompetency`, `archiveTrainingCourseVersion`, `updateTrainingProviderStatus`, `removeTrainingCoursePrerequisite`, `attachTrainingCertificateEvidence` (a real file-upload widget is a materially bigger, separate UI concern this checkpoint's own scope does not extend to — matches the repository's own precedent of disclosing file-attach UI gaps rather than fabricating one), `renewTrainingCertificate`, `transitionTrainingDevelopmentPlanStatus`, `archiveTalentPool` — matches the identical, established, repeatedly-accepted shape of HRT-280/281/282's own disclosed no-UI-caller wrapper lists.
- **C-21 (sibling functions lock two shared tables in inconsistent order)** — every function touching both `training_sessions` and `training_enrollments` locks session (parent) before enrollment (child), in that order, throughout (`_enroll_employee_in_training_session_internal`, `cancel_training_session`, `decide_training_enrollment`, `cancel_training_enrollment`); the one function locking TWO sessions (`reschedule_training_enrollment`) locks them in ascending-id order specifically to avoid a cross-function deadlock against a concurrent reverse-direction reschedule. Clear, applied from the start.
- **C-22 (a threshold compared without normalizing its own unit)** — no monetary/currency-denominated threshold exists anywhere in this capability. N/A.
- **C-23 (a named spec requirement silently dropped)** — cross-checked every named item in Prompt 284 §13-§28 against either code or an explicit disclosure above (decision 11/12): competency/skill catalogue — built; course/session/provider/prerequisite — built; enrollment with approve/cancel — built, plus reschedule and a real bulk-mandatory-assignment batch (alternative flow, section 22); attendance/completion — built; assessment/certificate with expiry and reminder jobs — built as real PLT-131/132 durable jobs; development plan — built, cross-references HRT-283; restricted talent review/pool/succession with recorded decision evidence — built. No undisclosed omission found.
- **C-24 (unmasked money/free-text reason reaches `app.audit_logs`)** — applied from the start (decision 9): all `capture_audit_event` call sites individually read and confirmed to pass `p_reason => null` and a reviewed `after_value` allowlist (ids/statuses/counts only, never `potential_rating`/`readiness_note`/`risk_of_loss`/`decision_reason`/score/notes/any raw `p_*_reason` parameter). Live-confirmed absent from `app.audit_logs` for a real talent-review submission and a real succession-candidate decision carrying free-text reasoning.

No taxonomy class was skipped.

## 7. Self-found defects (found and fixed during this checkpoint's own adversarial testing, before commit)

1. **C-12-class (ATW-032 authority-surface sweep) — `app.enroll_self_in_training_session` missing the explicit `has_active_tenant_membership` closure the mechanical sweep credits.** Live-caught by `scripts/db-tests/rbac-enforcement.sql`'s own standing ATW-032 gate, run as part of this checkpoint's own adversarial "every file sorted after the known-broken one" continue-past-`ISS-2026-077` verification pass (§5). **Fixed** by adding the explicit membership check, matching every sibling self-service RPC's established shape (HRT-282's own identical, earlier self-found fix for `list_my_payroll_reimbursement_requests`/`list_my_payroll_loans`/`list_my_payslips`). Re-verified: the fresh `rbac-enforcement.sql` re-run reports only the 3 pre-existing, out-of-scope HRT-283 findings remaining.
2. **A genuine missing capability, not merely a UI gap: no `list_talent_review_cycles` RPC existed at all.** Discovered while wiring the admin panel's talent-cycle-status-transition control — with no way to LIST cycles independent of `list_talent_review_assignments`'s own join, a cycle with zero assignments yet (the normal state immediately after `create_talent_review_cycle`) was structurally invisible to its own creator. **Fixed** by adding `app.list_talent_review_cycles` (RLS-only-gated, deliberately no redundant `HRS:Override` check inside the function itself — the real restriction is `talent_review_cycles_select_scoped`, and an explicit in-function check would incorrectly exclude an assigned reviewer with zero `HRS:Override`), its grant, `server/queries/training-talent.ts`'s `listTalentReviewCycles` wrapper, and real admin-panel wiring including a working "move to active/closed" transition button (previously present as a dead, unused prop).
3. **Both above found and fixed BEFORE the final gate suite / commit**, via this checkpoint's own honest, iterative `psql`-driven adversarial testing — not shipped and found later.

Consistent with this repository's own disclosed history, this checkpoint's own adversarial pass found and fixed 2 real defects before commit.

## 8. Live concurrency evidence (real OS processes, not reasoned about)

Executed against a disposable database (`cargogrid_tt_iter`, since dropped) built from a clean migrated template plus the checkpoint's own full db-test fixture:

1. **Session capacity/waitlist race.** A capacity-1 session; two genuinely concurrent OS `psql` processes both called `app.enroll_self_in_training_session` for two different employees at the same wall-clock moment. **Result: exactly one enrolled, the other waitlisted** — no double-booking of the single seat, the session-row `FOR UPDATE` lock correctly serialized the capacity decision.
2. **Natural-key version-conflict race.** Two genuinely concurrent OS processes both called `app.create_training_course_version` for the SAME course at the same moment. **Result: exactly one version created (version 3)**; the loser received the clean, classifiable `training_course_version_conflict` error (never a raw `23505`) — taxonomy C-01/C-02 applied and proven, not merely reasoned about.
3. **Decision race on the same enrollment.** A single `pending_approval` enrollment at `record_version=1`; two genuinely concurrent OS processes called `app.decide_training_enrollment` — one `approve`, one `reject` — against the identical expected version. **Result: exactly one decision took effect** (the reject, `status=cancelled`, `record_version=2`); the loser received a clean `stale_version` error, zero corruption, zero double-decision.

Cleanup: `cargogrid_tt_base`/`cargogrid_tt_iter` (scratch template/iteration databases used throughout this checkpoint's own fast-iteration testing loop) and `cargogrid_db_test` (the official harness's own disposable database) were all dropped after use.

## 9. Tenant/identity/access/privacy/concurrency evidence

All proven live in `scripts/db-tests/hris-training-talent.sql` (984 lines, two-tenant `trn1`/`trn2` fixture): competency publish lifecycle with an unauthorized-actor rejection on both create and publish; exactly-one-published-course-version-at-a-time (publishing v2 archives v1, session creation against a draft version blocked); direct prerequisite enforcement (blocked until the prerequisite is genuinely completed, then allowed; idempotent re-add); session capacity/waitlist (fill to capacity, third enrollee waitlisted, duplicate-active-enrollment rejected cleanly, cancelling a seat promotes the earliest waitlisted enrollee); the enrollment-approval gate with a structural self-decision block; a real bulk-mandatory-assignment batch (every active employee assigned, re-run safely idempotent with zero duplicates, a non-mandatory session correctly rejected); attendance plus a two-attempt assessment history (fail then pass, `attempt_number` auto-increments, `passed` computed deterministically); the full certificate lifecycle including the malware-scan gate (infected/pending-scan/wrong-scope all three independently rejected, clean+correctly-scoped accepted) and the durable expiry/reminder batches (both `HRS:Override`-gated, correctly scoped to genuinely-due certificates, idempotent re-run produces zero duplicate reminder rows); reschedule (correctly-linked new enrollment, old one cancelled) and session-cancel cascade (every still-active enrollment against the cancelled session is cancelled too); development plan authorship correctly scoped to `HRS:Edit` or the employee's own direct manager, with a genuinely non-existent linked performance outcome rejected; the full restricted talent review workspace (self-assignment structurally blocked, submit gated to the assigned reviewer only, restricted list visibility with a real contrast proof — an unrelated actor sees 0, the assigned reviewer sees exactly their own case — and reassignment proven to never mutate the old submitted review's own content while creating a fresh draft for the new reviewer); talent pool membership requiring `HRS:Override` for BOTH read and write (a plain `HRS:Edit`/`Approve` HR actor gets zero rows via a real RLS session, not merely a denied write) with a mandatory reason on add/remove; succession candidate propose/decide requiring `HRS:Override` and a mandatory reason, with self-decision structurally blocked even for an actor who separately holds `tenant_admin`; the k-anonymity floor (a real 2-person department suppressed vs. a synthetic 5-person department reporting its real count); cross-tenant RLS isolation across catalogue/person-scoped/talent tables plus an independent RPC-layer rejection; full schema-privilege defense in depth (anon zero table privilege, authenticated RLS-scoped select only, internal functions service-role-only); and the structural zero-write proof (zero write to `app.employees.lifecycle_status`/`app.payroll_*`/`app.performance_*`/role-permission tables across every write function's own source).

## 10. Residual gaps (disclosed)

- **8 mutation wrappers with no UI caller yet** (§6 C-20) — real, typed, tested SQL-layer wrappers, matches the identical, repeatedly-accepted shape of every prior HRT checkpoint's own disclosed list.
- **3 pre-existing HRT-283 findings, live-reconfirmed by this checkpoint's own adversarial testing, not mine to fix** (§6/§7): `acknowledge_performance_outcome`/`submit_performance_appeal`/`submit_performance_self_assessment` are `SECURITY DEFINER` and granted to `authenticated` with no authority check the mechanical ATW-032 sweep credits — flagged here explicitly for the batch's own Tier C propagation sweep, per `BUILD_EXECUTION_PROTOCOL.md` §5.4.
- **No live email/push notification dispatch for certificate expiry reminders** (decision 11) — the durable job's own real, queryable output record is the actual detection/dedup mechanism; wiring `app.queue_notification` is a cross-cutting Platform Core concern with zero precedent anywhere in this repository, disclosed rather than fabricated.
- **No employee-level competency-proficiency-rating subsystem** (decision 11) — only a course-to-competency catalogue link; not named as its own persisted record anywhere in the source spec.
- **One active reviewer per (cycle, subject) for talent review** (decision 12) — never a multi-reviewer 360 for the talent domain (unlike HRT-283's own performance-assessment model).
- **No REST/GraphQL adapter, no Playwright/axe-core E2E spec, no `beforeunload` unsaved-change guard** — repository-wide precedent, disclosed identically at every prior HRT checkpoint.
- **RPD-023 MFA gap** — carried forward, not built here or anywhere in this repository yet.

## 11. Rollback/recovery note

One additive-only migration (`20260731040000`), sorts after all pre-existing migrations, edits none of them (including Prompt 283's own `20260731030000`, read but never touched, per this task's own explicit instruction). Safe to roll back today since no live Supabase project or production data exists anywhere in this repository (standing, repeated disclosure). Rollback: drop the 19 new tables/79 functions/RLS policies/triggers this migration created; revert the additive `app.jobs.job_type` CHECK widening and `app.generic_job_types()` widening (both backward-compatible, safe to leave in place even if the rest is rolled back); delete the new service-layer files (`server/contracts/training-talent/`, `server/queries/training-talent.ts`, `server/mutations/training-talent.ts` and their `.test.ts` siblings); delete the 2 new UI route families; delete `scripts/db-tests/hris-training-talent.sql`; revert the `TRAINING_REGISTRY` addition in `scripts/data-classification/registry.ts` and its wiring in `check-registry.ts`. Resume point: this file plus `scripts/db-tests/hris-training-talent.sql`'s own assertions are the authoritative record of what is proven; any resumed work should re-run `pnpm run typecheck && pnpm run lint && pnpm run test && pnpm run db:test && pnpm exec next build` before proceeding.

## 12. Recommended next task

This checkpoint closed the Implement stage for `CG-S12-HRT-012`, the SECOND of the `283-285` batch. Prompt 285 followed next; the combined Tier C batch review (§13 below) has now run and promoted all three rows to `VERIFIED`, including this checkpoint's own §7/§10-disclosed 3 pre-existing HRT-283 ATW-032 findings, which the batch review's cross-prompt integration lens confirmed still live-broke the shared `rbac-enforcement.sql` gate and are now disposed of at §13.1 finding 4 (registered as correct-by-design, not fixed as a defect — see `docs/build-log/phase-07/HRT-283.md` §13.1 for the full independent re-verification, since the 3 functions themselves are HRT-283's own).

## 13. Tier C batch review (adversarial review + fix pass) — closes this task to `VERIFIED`

Performed `2026-08-14` by the orchestrating fix session, independent of the implementing agent (§1-§12 above), covering the combined `283-285` diff (`CG-S12-HRT-011`/`012`/`013`). 4 parallel adversarial review lenses (spec-compliance; security/RLS/tenant isolation; correctness/concurrency; cross-prompt integration and data dependency) each independently live-tested against a disposable Postgres 16 database with the full, real 206-migration set applied (both HRIS db-test fixtures — `hris-kpi-performance.sql`, `hris-training-talent.sql` — loaded for real seeded data). Per `BUILD_EXECUTION_PROTOCOL.md` §5.3, every finding below was independently RE-DERIVED live by this fix session before being accepted — never fixed from a lens citation alone. Full cross-batch detail (all findings, all three prompts, one shared record): `docs/build-log/phase-07/HRT-285.md` §12 (the last-in-batch build log, per this repository's own HRT-278/279/280 convention) — this section states only the subset that is this prompt's own.

### 13.1 Findings landing in this prompt's own code

| # | Lens | Finding | Verdict | Disposition |
|---|---|---|---|---|
| 1 | Correctness | **CRITICAL** — 2 of the correctness lens's 7 cited ambiguous-bare-`id` read RPCs are in this migration (`app.get_training_session`, `app.list_talent_pool_members`): each raises `ERROR: column reference "id" is ambiguous` on EVERY call, unconditionally, identical shape to HRT-283's own 5 instances | **CONFIRMED**, independently re-derived: live-reproduced the exact error for both, using real seeded ids from `hris-training-talent.sql`'s own fixture; a mechanical sweep of all 79 functions this migration defines (checked against live `pg_get_functiondef`) confirms these are the only 2 in this migration with the shape | **FIXED** — `20260731050000_harden_hris_kpi_performance_training_talent_batch_283_285_review_fixes.sql`, `create or replace function` aliasing the source table in each offending SELECT, byte-for-byte identical otherwise (verified against the live original definition before writing the fix). Re-verified live: both now return correct results for a real caller. |
| 2 | Correctness | **HIGH** — `app.enqueue_job`'s check-then-insert on `(tenant_id, idempotency_key)` has no exception handler; two genuinely concurrent callers with the SAME idempotency key both pass the pre-insert "not found" check, one wins the insert, the other gets a raw, unclassified Postgres `23505` instead of the promised idempotent replay — reachable from this checkpoint's own two new certificate-expiry batch RPCs (`app.run_training_certificate_expiry_batch`, `app.run_training_certificate_expiry_reminder_batch`), whose own comments assert an idempotency guarantee concurrency actually breaks | **CONFIRMED**, live-reproduced with two genuinely concurrent OS `psql` processes calling `run_training_certificate_expiry_batch` with the same period label: the loser received a raw `duplicate key value violates unique constraint "jobs_idempotency_key_unique"` before the fix. `app.enqueue_job` itself predates this batch (shared PLT-131/132-adjacent generic-job infra), but this checkpoint is what newly exercises it under real concurrency with a period-derived key | **FIXED at the shared primitive** (same migration): wrapped the terminal INSERT in `begin...exception when unique_violation`, re-selecting and returning the existing row exactly as the pre-insert check branch already does, so a losing concurrent caller now gets the SAME clean replay result the winner (or an earlier caller) produced, not a raw constraint error. Fixing the shared function means every current and future caller of `app.enqueue_job` gets the fix, not just this checkpoint's own two call sites. Re-verified live: the identical two-process race now returns the SAME `job_id` to both callers, zero raw error. |
| 3 | Spec | **HIGH** — `app.attach_training_certificate_evidence` (the real, tested PLT-128 malware-scan/tenant/record-type re-validation gate for certificate evidence files, §16's own "certificate files are private and malware-scanned" requirement) had no UI caller anywhere in the product; the Certificates section's own description text ("Provider/certificate evidence files are private and malware-scanned before attach (PLT-128)") claimed the capability existed regardless | **CONFIRMED** by direct code read: zero occurrences of `attachTrainingCertificateEvidence`/`attachTrainingCertificateEvidenceAction` anywhere under `app/` before this fix (only in the wrapper itself and its own unit test); this checkpoint's own §6/§10 disclosure list correctly named it as one of 8 no-UI-caller wrappers but undersold that this specific one defeats a named security requirement, unlike its 7 siblings (genuine admin conveniences) | **FIXED** — wired end-to-end: `app/(tenant)/[tenantSlug]/hris/training-talent/{actions.ts,page.tsx,training-talent-panel.tsx}` gained a real "Attach evidence"/"Replace evidence" form per certificate (evidence file id input, calling the already-existing, already-tested `attachTrainingCertificateEvidence` mutation, `HRS:Edit`-gated exactly as the RPC itself requires). The section's own description text was also corrected to stop overclaiming provider-evidence coverage (see finding 5 below) — it now reads "Certificate evidence files are private and malware-scanned before attach (PLT-128); provider evidence is not yet a built capability (disclosed in the build log)." Re-verified: `next build` succeeds with the new form present; the RPC's own malware-scan/tenant/record-type gate is unchanged (already live-proven in this checkpoint's own db-test, §9 above). |
| 4 | Cross-prompt integration | **HIGH (confirmed real gate breakage, disposed as correct-by-design)** — this checkpoint's own §6/§7/§10 disclosed "3 pre-existing HRT-283 findings" (`app.acknowledge_performance_outcome`, `app.submit_performance_appeal`, `app.submit_performance_self_assessment`, allegedly lacking an authority check per the mechanical ATW-032 sweep) is a live, currently-red `rbac-enforcement.sql` Tier A gate — reproduced by this fix session exactly as disclosed | **CONFIRMED live-broken gate, but the underlying "missing authority check" claim is a FALSE POSITIVE**, independently re-verified by this fix session reading all 3 functions' own `pg_get_functiondef`: each performs a genuine, direct self-scope equality check (not merely `assert_actor_is_session_identity`) before any disclosure or mutation — the mechanical sweep's keyword-pattern `base` list simply does not credit a raw equality predicate as an "authority check", the identical class its own `get_self_employee` allowlist entry already documents. Since HRT-283 was not yet `VERIFIED` when this batch's own review ran, these 3 functions were genuinely in scope to fix here, not merely re-cited | **FIXED at the gate** (correct disposition — no defect exists in the 3 functions themselves to fix): `scripts/db-tests/rbac-enforcement.sql`'s `v_expected` allowlist gained all 3 names with a written justification. Full detail and the 3 functions' own re-read source: `docs/build-log/phase-07/HRT-283.md` §13.1 finding 4 (these functions are HRT-283's own code; this checkpoint only disclosed the gate failure, HRT-283's build log carries the disposition). |
| 5 | Spec | **LOW, disclosure-consistency** — §16 names "provider/certificate files are private/scanned" (both nouns); only the certificate half was ever built — `app.training_providers` carries no `evidence_file_id` column or attach RPC of any kind, and this migration's own header comment ("certificate/provider evidence reuses... PLT-128") overstated what was actually built | **CONFIRMED** by direct schema read: `app.training_providers` has no evidence-file column, grep-confirmed zero `*provider*evidence*`-shaped function anywhere in the migration. Lower confidence this is a genuine scope miss vs. reading more into §16's phrasing than intended — §13's own database-impact list never separately names a persisted provider-evidence record | **DISCLOSED, not built** — registered as `ISS-2026-083` (`docs/runtime/KNOWN_ISSUES.md`), Low severity (no security exposure — there is no feature to exploit; the product now accurately describes only what it does, per finding 3's UI-copy correction above). Building a genuinely new provider-evidence-file feature (new column, new PLT-128-reusing RPC, RLS/grants, TS wrapper, UI form) is a bounded feature addition, not a defect repair, and is left for a dedicated near-term follow-up if confirmed in scope. |

### 13.2 Propagation sweep (`BUILD_EXECUTION_PROTOCOL.md` §5.4)

- **Ambiguous-`id` shape (finding 1):** see `HRT-283.md` §13.2 — the combined sweep across both migrations found exactly 7 instances (5 in HRT-283's, 2 here), all fixed in the one shared corrective migration.
- **`enqueue_job` race shape (finding 2):** every `app.enqueue_job(...)` call site across both this batch's migrations was enumerated (grep-confirmed) — exactly 2, both in this migration (`run_training_certificate_expiry_batch`, `run_training_certificate_expiry_reminder_batch`), both exercised by the same shared-primitive fix; no HRT-283/HRT-285 call site exists.
- **No-UI-caller-defeats-a-named-security-requirement shape (finding 3):** of this checkpoint's own 8 disclosed no-UI-caller wrappers (§6/§10), only `attachTrainingCertificateEvidence` ties to a named §16 security requirement — the other 7 (`archiveTrainingCompetency`, `archiveTrainingCourseVersion`, `updateTrainingProviderStatus`, `removeTrainingCoursePrerequisite`, `renewTrainingCertificate`, `transitionTrainingDevelopmentPlanStatus`, `archiveTalentPool`) remain genuine, disclosed, non-security UI-completeness gaps, matching this repository's own established precedent (`ISS-2026-075`/`076`/`080`-shaped) — not swept into this fix, since building 7 more admin forms under review-pass time pressure would be scope creep, not a bounded defect repair.
- **ATW-032 false-positive self-scope shape (finding 4):** see `HRT-283.md` §13.2 — the gate's own `v_unauthorized` computation is exhaustive; exactly 3 functions, all HRT-283's own, all disposed there.

### 13.3 Corrective migration

Shared with HRT-283's own portion: `supabase/migrations/20260731050000_harden_hris_kpi_performance_training_talent_batch_283_285_review_fixes.sql` (2 of the 7 ambiguous-`id` fixes plus the `app.enqueue_job` rewrite land in this migration's own functions; the other 5 ambiguous-`id` fixes and the 2 RLS policy fixes are HRT-283's own — see `HRT-283.md` §13.3). Neither `20260731030000` nor `20260731040000` (this prompt's own migration) was edited in place. Shared test-infrastructure fix: `scripts/db-tests/rbac-enforcement.sql`'s `v_expected` allowlist (finding 4, functions owned by HRT-283). Service-layer/UI changes (not migrations): `server/mutations/training-talent.ts` (no change — `attachTrainingCertificateEvidence` already existed, only its caller was added); `app/(tenant)/[tenantSlug]/hris/training-talent/{actions.ts,page.tsx,training-talent-panel.tsx}` (certificate-evidence-attach UI, finding 3).

### 13.4 Fresh gate suite (full, independent re-run — not accepted from a fix agent's self-report)

Recorded once, at the batch level, in `docs/build-log/phase-07/HRT-285.md` §12.6 — summarized here for this prompt's own record: `pnpm run typecheck` clean; `pnpm run lint` 0 errors (205 pre-existing warnings, unchanged baseline); `pnpm run test` **3895/3895 pass** (new self-service tests added at HRT-285's own portion do not touch this checkpoint's own files); `TEST_DB_NAME=cargogrid_db_test_final283285 pnpm run db:test` (official harness, full run) **`ALL PASSED`, exit 0** — reaching and passing every test file including `hris-kpi-performance.sql`/`hris-training-talent.sql` AND `hris-leave-permit-business-trip.sql` itself, since `ISS-2026-077`'s own date-relative assertion does not trip on today's date (re-confirmed via independent A/B reproduction this batch close); standalone per-file re-run of `hris-training-talent.sql` against a separately-built fresh, full 206-migration database independently exits 0; `next build` clean, both `hris/training-talent` and `hris/my/training-talent` routes present; `docs:check`/`security:check`/`security:audit`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check-paths`/`git:check` all clean.

### 13.5 New recurring-defect-taxonomy class

No new class — finding 1 (ambiguous bare `id`) recurs `docs/standards/RECURRING_DEFECT_TAXONOMY.md`'s existing class from the `278-280` batch; finding 2 (check-then-insert race with no exception handler) recurs the existing class HRT-282's own Tier C review already registered (its `create_payroll_period`/`create_payroll_component` findings). Both are reinforcements of already-tracked classes, not new ones.

### 13.6 Task state

`CG-S12-HRT-012` (Prompt 284, Training and Talent) is now **`VERIFIED`**, together with `CG-S12-HRT-011` (Prompt 283) and `CG-S12-HRT-013` (Prompt 285) — all three rows of this batch move to `VERIFIED` together, per `BUILD_EXECUTION_PROTOCOL.md` §4's all-or-nothing batch rule. `docs/runtime/TASK_LEDGER.md` and `docs/build-log/phase-07/00_EXECUTION_INDEX.md` rows 283/284/285 updated accordingly. Full batch-level disposition, propagation sweep, and gate suite: `docs/build-log/phase-07/HRT-285.md` §12. One commit made across the whole batch's fix pass: "agent: fix batch review findings (CG-S12-HRT-011..013, Prompts 283-285)" — not pushed, per this task's own instruction. `docs/runtime/HANDOFF.md` intentionally not touched (outside this fix task's own scope).
