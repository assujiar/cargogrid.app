# FIN-198 — Receipt and Payment Allocation

## 0. Checkpoint

| Field | Value |
|---|---|
| Prompt | `198_RECEIPT_PAYMENT_ALLOCATION_PROMPT.md` (`CG-S9-FIN-009`) |
| Repository | `assujiar/cargogrid.app` |
| Working branch | `claude/prompt-189-195-mb32zh` |
| Dependency | `FIN-194` (`VERIFIED`) + `FIN-196..197` (`VERIFIED`) |
| Authorization | Explicit user instruction naming Finance Phase 4 prompts 195, 196, 197, 198, 199, and 200 in order, each as its own commit -- fourth task in that range. |

## 1. Pre-flight collision check

`git status --short --branch` clean at start except this checkpoint's own new files, continuing directly on top of `FIN-197` (Invoice, `VERIFIED`, this session's own prior commit on this branch).

## 2. Baseline reconciliation

Baseline before this checkpoint: `typecheck`/`lint` (0 errors, 75 pre-existing warnings), `node:test` 1926/1926, `db:test` PASS across 79 db-test files, `next build` 64 routes.

## 3. Implementation

### 3.1 Reuse decision and idempotency design (disclosed, migration header)

Every allocate/deallocate call delegates the actual AR balance mutation to `FIN-196`'s own `app.apply_finance_ar_allocation`/`app.reverse_finance_ar_allocation` directly -- this migration owns receipt capture and the receipt-to-AR-item mapping only, never a second, competing AR-balance mechanism. `app.apply_finance_ar_allocation` is itself row-locked and idempotent per open item, so composing it here preserves `FIN-196`'s own "never over-allocate, even under concurrency" guarantee without reimplementing it.

`app.finance_receipt_allocation_batches` mirrors `FIN-194`'s own staged-import idempotency idiom (`unique (tenant_id, receipt_id, idempotency_key)`) -- a retried `app.allocate_finance_receipt` call for the same batch key returns the receipt unchanged rather than re-applying. Each individual `FIN-196` allocation call additionally carries its own derived idempotency key (`batch idempotency_key || ':' || open_item_id`), so a partial retry after a crash mid-batch cannot double-apply any single line either.

`unapplied_amount` is a generated column (`amount - allocated_amount`), never independently mutable -- any remainder after allocation is simply left unapplied, structurally, not tracked by a separate flag. Deallocation is a governed reversal (`FIN:Approve`-gated, mandatory reason), never a direct edit.

Bank/payment field sensitivity (Prompt 198 section 16): general `FIN:View`/`FIN:Edit` gating applies at this checkpoint, the same posture every prior FIN-19x table has used; per-field masking/redaction beyond role gating is `FIN-214`'s own dedicated scope (Financial Field-Level Security) -- disclosed, not silently promised here.

GL posting scope: this migration captures `posting_period_id` and full source lineage but creates zero GL journal line -- `FIN-202`/`FIN-203`'s own scope, matching every prior FIN-19x checkpoint's own disclosure.

### 3.2 Schema — `supabase/migrations/20260729120000_create_finance_receipt_allocation.sql`

`app.finance_receipts` (bank-reference dedup via a partial unique index, idempotent unique on `tenant_id, idempotency_key`; `unapplied_amount` a generated column), `app.finance_receipt_allocation_batches` (staged-batch idempotency), `app.finance_receipt_allocations` (receipt-side lineage; balance mutation delegated to `FIN-196`). Functions: `app.check_finance_receipt_authority`, `app.capture_finance_receipt` (idempotent, period-aware), `app.search_finance_ar_candidates_for_receipt` (same customer/currency, not fully paid, not held, due-date ordered), `app.allocate_finance_receipt` (idempotent, row-locked, rejects over-allocation and cross-customer/cross-currency lines), `app.request_finance_receipt_deallocation` (governed reversal, `FIN:Approve`, mandatory reason), `app.list_finance_receipts`, `app.get_finance_receipt_allocations`.

### 3.3 Service layer and UI

`server/contracts/receipt-allocation/receipt-allocation.ts(.test.ts)`, `server/queries/receipt-allocation.ts(.test.ts)`, `server/mutations/receipt-allocation.ts(.test.ts)`.

UI: `app/(tenant)/[tenantSlug]/finance/receipts/page.tsx` -- a bounded (200-row) receipt list with unapplied-cash visibility, a capture form, a per-receipt JSON allocate form (bounded MVP UI, mirrors `FIN-191`'s own config-items JSON-editor precedent), and a query-string-addressed allocation-detail view with a governed reversal action. `FINANCE_RECEIPT_STATUS_TONE_MAP` added to `components/domain/status-tone-map.ts`.

## 4. Tests

### 4.1 Real database tests (`scripts/db-tests/finance-receipt-allocation.sql`, via `pnpm run db:test`)

Setup: two tenants; tenant A gets a Finance Manager, a Finance Editor (no `Approve`), a Plain User, tenant B its own Finance Manager, six fiscal periods, one customer account, and three AR open items (1,000,000 / 500,000 / 300,000 IDR, posted directly via `FIN-196`'s own posting primitive). Assertions: idempotent capture (retried call returns the unchanged original receipt; duplicate bank reference under a different idempotency key rejected); candidate search returns exactly the three seeded items; exact multi-invoice allocation (1,300,000 receipt splits across the 1,000,000 and 300,000 items, leaving the 500,000 item untouched and the receipt fully applied); over-allocation rejected; idempotent allocate replay (never double-applies); governed deallocation (authority split proven, mandatory reason, restores both the receipt's own unapplied amount and the AR open item's own open balance, rejected once already reversed); cross-tenant isolation; schema-privilege defense in depth; audit-trail reconciliation. All 80 db-test files pass together, zero regression -- every fixture scenario passed on the first `db:test` run.

### 4.2 TypeScript unit tests

New: `server/contracts/receipt-allocation/receipt-allocation.test.ts` (6 describe blocks), `server/queries/receipt-allocation.test.ts` (3 describe blocks), `server/mutations/receipt-allocation.test.ts` (3 describe blocks) -- 16 net new (1926 at `FIN-197` + 16 = 1942).

## 5. Gate results

| Gate | Result |
|---|---|
| `pnpm run typecheck` | PASS |
| `pnpm run lint` | PASS (0 errors; same pre-existing `no-html-link-for-pages` warning class, unchanged, 75 warnings) |
| `pnpm run test` | PASS -- 1942/1942 |
| `pnpm run db:test` | PASS -- 80 db-test files, zero regression |
| `pnpm run docs:check` | PASS |
| `pnpm run security:check`/`data-classification:check`/`threat-model:check`/`standards:check` | PASS |
| `pnpm run git:check-paths` | disclosed known false positive on the new migration file (standing since `COM-151`) |
| `npx next build` | PASS -- 65 routes (1 new: `/[tenantSlug]/finance/receipts`) |

## 6. Database / API / UI / security / data-migration effects

One new migration -- additive (3 new tables, 8 new functions). Zero prior migration file edited in place, zero new permission-catalogue row. One new route. Security: capture/allocate require `FIN:Edit`; governed deallocation requires `FIN:Approve`; read/candidate-search require `FIN:View`. Money/decimal discipline: every amount column is `numeric(14,2)`; `unapplied_amount` is a generated column. Data migration: not applicable -- greenfield.

## 7. Documentation updated

`docs/build-log/phase-04/FIN-198.md` (this file), `FINANCE_EXECUTION_INDEX.md` (row `198` `READY`->`VERIFIED`; row `199` set `READY`), `docs/runtime/TASK_LEDGER.md`, `CARGOGRID_BUILD_STATUS.md`, `HANDOFF.md`, `CHANGE_MANIFEST.md`.

## 8. Errors / recovery / risks / issues

Zero implementation defect found during authoring -- every fixture scenario, including idempotent-replay, over-allocation rejection, and the full governed-reversal round trip, passed on the first `db:test` run. Zero Critical/High-severity issue remains open. Zero regression to any prior capability.

## 9. Rollback/recovery note

`git revert` of this checkpoint's commit reverts the migration (additive tables/functions only, zero `CREATE OR REPLACE FUNCTION` on any prior migration's function) and the service layer/UI route. No other capability calls this migration's functions yet.

## 10. Acceptance criteria / Definition of Done

Prompt 198 section 33: allocation and unapplied balances equal receipt exactly (proven directly -- the full 1,300,000 receipt splits into a 1,000,000 + 300,000 allocation with 0 unapplied, then a governed reversal restores 300,000 to unapplied); retries and concurrent actions cannot double-allocate (idempotent batch replay proven directly; row-locked delegation to `FIN-196`'s own concurrency-safe allocator); every posted allocation reconciles to AR lineage (each `finance_receipt_allocations` row records its own `ar_open_item_id`, and the corresponding `FIN-196` event trail records the same `source_type`/`source_id`); mandatory gates pass at this one recorded checkpoint; completion evidence maps source requirement -> task -> code/migration/contract/UI -> test -> documentation (this file).

## 11. Commit / branch / next eligible prompt

Branch `claude/prompt-189-195-mb32zh`. This checkpoint proceeds directly to `CG-S9-FIN-010` (Prompt 199, Accounts Payable) -- within this session's explicit authorized range (Finance Phase 4 Prompts 195-200).
