# PLT-138 — Platform Core Tenant and Security Hardening

## 0. Checkpoint

| Field | Value |
|---|---|
| Task ID | `CG-S6-PLT-035` |
| Prompt | `docs/ai-agent-build-prompt-package/06-phase-01-platform-core/138_PLATFORM_CORE_TENANT_SECURITY_HARDENING_PROMPT.md` |
| Parent phase | Phase 1 — Platform Core; Workstream: Phase Hardening; Epic: Platform Risk Reduction |
| Branch/HEAD | `claude/lanjut-0kwbyt`@(this checkpoint's commit) |
| Upstream | `PLT-137` (single exact task, `VERIFIED`) — see §1 |
| Baseline | 31 migrations, `node:test` 929/929, `db:test` 389 total scenario groups across 31 migrations/31 db-test files at last green gate run (`PLT-137`) |
| User authorization | `PLT-137` closed naming `CG-S6-PLT-035` as the next execution-index row, dependency-`READY` (single exact task, not a "required subset") — executed under a fresh, separate, unscoped "lanjut" in the same session |

## 1. Pre-flight collision check

`mcp__github__list_branches` (owner `assujiar`, repo `cargogrid.app`): every branch other than `main` shows zero commits ahead of `origin/main` except this session's own `claude/lanjut-0kwbyt` (5 commits ahead — `PLT-133`–`137`, already pushed, not yet merged). `git status` confirmed a clean worktree before this checkpoint's own changes. **Zero collision risk.**

## 2. Objective and scope (Prompt 138 §4/§11/§12) — a genuinely small hardening pass, not padded to look larger

Prompt 138's own mandate is narrow by design: "Close PLT-137 findings through atomic root-cause repairs and re-verification without adding new capability" (§4). `docs/build-log/phase-01/PLT-137.md` §5 (the entire input to this task, per `00_PLATFORM_CORE_WBS.md` line 85) contains **exactly one finding**, already characterized there as Low severity and zero production impact:

> `app.resolve_access_context`'s own migration comment ("always resolves first and alone") describes the `p_tenant_id IS NULL` branch only — passing an *explicit* tenant id for a Supreme Admin who holds no per-tenant `app.tenant_user_identities` row instead fails closed with `inactive_identity_link`, not a silent Supreme resolution.

There is no Critical/High/Medium finding to rank (Prompt 138 §20 task 1's own "rank findings" step is trivial here — one item, already ranked Low by `PLT-137` itself). This checkpoint does not manufacture additional scope: Prompt 138 §12 forbids "new domain capability, unrelated debt/refactor" and §24 forbids "cosmetic cleanup" beyond the exact finding — the one repair below is the finding's own literal root cause, not an invented pretext for broader change.

## 3. Root-cause repair (Prompt 138 §20 task 2, exact finding-linked only)

The underlying **behavior** of `app.resolve_access_context` was already correct (verified independently at `PLT-137`, re-confirmed at §4 below) — the gap was purely a **documentation-accuracy** one: the function had never carried a `comment on function` at all (only inline `--` comments inside its own body, easy to read as describing every call shape rather than just the `p_tenant_id IS NULL` branch). The root-cause fix is therefore a single, purely additive `comment on function` statement — new migration `supabase/migrations/20260722130000_harden_resolve_access_context_documentation.sql` — describing both branches accurately, including the exact boundary `PLT-137` found (a global-only Supreme Admin has no shortcut in the tenant-qualified branch).

**Why a new migration, not an edit to the original file**: `00_PLATFORM_CORE_WBS.md`'s own convention (already followed identically at `PLT-116`'s `app.users_directory` fix, which used `create or replace view` in a *later* migration rather than editing `PLT-114`'s original file) treats every applied migration as immutable. `COMMENT ON FUNCTION` is fully additive schema metadata — it requires no `REVOKE`/`GRANT` (comments carry no privilege), no function-body rebuild, and no data/behavior change, so it is the minimal correct vehicle for this specific finding.

## 4. Tests (Prompt 138 §27/§28/§29 — finding-specific negative/regression, full two-tenant fixtures)

`scripts/db-tests/tenant-security-hardening.sql` (new, 5 scenario groups — this checkpoint's own independent regression evidence, not merely re-pointing at `PLT-137`'s own scenario 3):

1. **Setup**: two tenants' worth of identity substrate, including a global-only Supreme Admin with no per-tenant identity link to either tenant.
2. **Regression evidence 1** (the exact behavior `PLT-137` found, re-proven here independently): a tenant-qualified `resolve_access_context` call for that Supreme Admin still fails closed with `inactive_identity_link` — unchanged, since this repair never touched the function body; the `p_tenant_id`-omitted branch is separately re-confirmed still resolving Supreme Admin correctly.
3. **Repair evidence 2** (the actual fix, proven present and accurate, not merely "migration applied without error"): `obj_description('app.resolve_access_context(uuid, uuid, text)'::regprocedure)` is now non-null, explicitly mentions both the omitted and supplied `p_tenant_id` branches, and explicitly states the tenant-qualified branch has "no Supreme Admin shortcut" — the literal finding this repair closes, verified by content, not just presence.
4. **Regression check**: every other four-layer/RLS control this repair could plausibly have touched is unaffected — a `tenant_admin` still resolves correctly for their own tenant, and RLS on `app.tenants` still isolates.
5. **Closing summary.**

### 4.1 Full and affected gates (Prompt 138 §30)

| Gate | Result |
|---|---|
| `pnpm run typecheck` | PASS |
| `pnpm run lint` | PASS |
| `pnpm run test` | PASS — `node:test` 929/929, unchanged (no new TypeScript surface; this repair is entirely at the database-comment level) |
| `pnpm run db:test` | PASS — **394 total scenario groups** across **32 migrations/32 db-test files** (5 net new, this checkpoint's own `tenant-security-hardening.sql`), up from 389/31 at `PLT-137`. Every one of the 393 pre-existing scenario groups re-ran unmodified and passed, confirming zero regression from the one-line additive migration. |
| `pnpm run docs:check` | PASS |
| `pnpm run security:check` | PASS |
| `pnpm run data-classification:check` | PASS |
| `pnpm run threat-model:check` | PASS — unchanged, 25 entries |
| `pnpm run standards:check` | PASS |
| `pnpm run git:check-paths` | PASS — 0 forbidden paths touched |
| `pnpm run git:check` | PASS |

`pnpm run test:e2e`/`next build` are not applicable to this checkpoint — no `app/`, `lib/`, `server/`, or `components/` TypeScript file was touched (Prompt 138 §14/§15's own "no new endpoints"/"no redesign" scope was honored by construction, not merely claimed).

## 5. PLT-137 re-verification (Prompt 138 §20 task 4, "repeat PLT-137 until mandatory blockers close")

`PLT-137`'s own acceptance criteria required zero Critical/High/Medium finding remain open — that was already true before this checkpoint (its own failure matrix, §5, contained only the one Low item this checkpoint now closes). Re-running `PLT-137`'s own `scripts/db-tests/platform-core-integrated-verification.sql` (unmodified, part of the same `db:test` run, §4.1) confirms all 14 of its scenario groups still pass unchanged — the integrated checkpoint remains trustworthy, and the one finding it recorded is now closed by this checkpoint's own repair (§3) with independent regression evidence (§4). **`PLT-137` re-passes; zero mandatory blocker remains.**

## 6. Database / API / UI / security / data-migration effects (Prompt 138 §13–§19)

**Database:** one new, purely additive migration (`comment on function` only — no table, column, index, constraint, or privilege change; `pnpm run db:test`'s own full-rebuild-from-scratch run, §4.1, confirms the 32-migration set still applies cleanly in order). **API:** N/A — no REST/GraphQL surface exists to repair (Prompt 138 §14's own "no new endpoints" is honored trivially, since nothing was touched). **UI:** N/A — no UI file was touched (§15). **Security:** the repair strengthens introspectable documentation of an already-correct fail-closed security boundary; it does not itself change any access-control decision — confirmed by §4's own regression check that every other four-layer/RLS control this could plausibly have affected remains unchanged. **Performance:** N/A — no measured regression existed to fix (Prompt 138 §17 forbids speculative tuning; none was performed). **Audit:** finding→root cause→repair→test→closure is the complete chain this build log documents end to end (Prompt 138 §18); RPD-022's own Supreme Admin disclosure is unaffected (the fixed comment describes a *fail-closed* boundary that has no bearing on Supreme Admin's already-disclosed absolute-CRUD authority once actually authenticated). **Data migration:** N/A.

## 7. Errors / recovery / risks / issues

No error. Zero Critical/High/Medium finding existed to begin with — this checkpoint's entire scope was the one Low, pre-characterized, zero-production-impact item `PLT-137` recorded. No new issue opened; no residual risk remains from `PLT-137`'s own failure matrix (Prompt 138 §22's "non-critical residual risk has authorized owner/expiry/monitoring" does not apply here, since the one item is fully closed, not merely monitored).

## 8. Rollback/recovery note

Purely additive — the new migration contains a single `comment on function` statement with no privilege, data, or behavioral effect; `git revert` of this checkpoint's commit is safe and complete, restoring the function to its prior (correct-but-undocumented) state with no data loss. Every pre-existing test/gate re-runs clean (proven directly, §4.1).

## 9. Acceptance criteria / Definition of Done (Prompt 138 §33/§34)

- All mandatory blockers close and `PLT-137` re-passes: confirmed (§5) — zero Critical/High/Medium finding existed, and the one Low finding is now closed with independent regression evidence.
- Residual risks non-critical, authorized and visible: N/A — no residual risk remains; the one finding is fully closed, not deferred.
- Mandatory gates pass, worktree/schema/docs reconcile and no unauthorized scope changed: confirmed (§4.1) — `git status` shows only the one new migration, one new db-test file, this build log, and ledger updates; nothing outside Prompt 138 §11's allowed scope was touched.

**Task `CG-S6-PLT-035` is `VERIFIED`.**

## 10. Commit / branch / next eligible prompt

Branch: `claude/lanjut-0kwbyt`. Ledgers updated in the same checkpoint: `docs/runtime/TASK_LEDGER.md`, `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`, `docs/build-log/phase-01/00_PLATFORM_CORE_EXECUTION_INDEX.md` (row `035` done). Next eligible prompt per the execution index: `CG-S6-PLT-036` (Prompt 139, Documentation and Handoff) — its own dependency is the single exact task `PLT-138` (now `VERIFIED`). Per this build's own standing discipline, the next runtime agent/session must stop here and obtain fresh explicit user authorization before proceeding to `CG-S6-PLT-036` or beyond, since this checkpoint itself was authorized by a single, unscoped "lanjut" (one task), not a multi-prompt range.
