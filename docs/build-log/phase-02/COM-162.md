# COM-162 — Integrated Commercial Verification

**Prompt:** `CG-S7-COM-021` (`CG-AABPP-COM-162` v0.8.0)
**Runtime output of:** `docs/ai-agent-build-prompt-package/07-phase-02-commercial/162_COMMERCIAL_INTEGRATED_VERIFICATION_PROMPT.md`
**Status:** `COMPLETED` — the Phase 2 equivalent of Platform Core's own `PLT-137.md`, scaled from 32 Platform Core capabilities to 20 shipped Commercial capabilities (`142`–`161`)

## 0. Checkpoint

| Field | Value |
|---|---|
| Repository | `assujiar/cargogrid.app` |
| Branch | `claude/lanjut-kv0mze` |
| HEAD at authoring time | this checkpoint's own prior commit (`COM-161`, "Add Commercial No-Reentry Enforcement") |
| Worktree state | Clean at start; this task's own new/modified files only (§9 below) |
| Upstream dependency | `COM-143..161` (an exact range, all 20 capability/kickoff prompts) — all `VERIFIED` per `TASK_LEDGER.md`/`COMMERCIAL_EXECUTION_INDEX.md` row `020` |
| Downstream | `CG-S7-COM-022` (Prompt 163, Tenant/Security/Financial/Data Hardening) — consumes this checkpoint's failure matrix (§5) as its exact remediation scope |
| Mutation performed | One new cross-capability integration test suite (`scripts/db-tests/commercial-integrated-verification.sql`, 6 scenario groups, real and executable, no mocks); this build log; runtime ledger updates. **No new feature, no application-code change, no production/shared-service mutation** — Prompt 162 §11/§13's own "no feature schema by default" / verification-only mandate, confirmed against `git status`. |

## 1. Pre-flight collision check

`git status --short --branch` clean at start (continuing directly on top of `COM-161`, already pushed to `claude/lanjut-kv0mze`). `mcp__github__list_pull_requests` (state `open`): `[]`.

## 2. Methodology (Prompt 162 §20, §24 "isolated component pass does not imply integrated pass")

Following `PLT-137.md`'s own precedent exactly (the identical phase-closing verification pattern one phase up):

1. **Freeze checkpoint, collect all `COM-143`–`161` outputs** — every `docs/build-log/phase-02/COM-143.md`–`COM-161.md` file confirmed present (§4).
2. **Run integrated fresh-install/build/tests/cross-control scenarios** — §3, including a fresh `rm -rf node_modules && pnpm install --frozen-lockfile`, plus one new cross-capability db-test suite composing 13 of the 19 shipped capability prompts together through one continuous **two-tenant** golden path (not each in isolation) — §6.
3. **Check requirement/WBS/ADR/docs links, allowed-scope audit, baseline deltas, stale evidence** — §4.
4. **Failure matrix and exact recovery tasks; no fix outside this prompt** — §5.
5. **Compare baseline/post-change evidence, update persistent records** — §9/§12.

### 2.1 Scope decision: representative composition, not exhaustive re-implementation (disclosed, matching `PLT-137`'s own precedent)

`PLT-137`'s own new integration suite composed 12 of Platform Core's 32 capabilities in specific, meaningful cross-cutting scenarios — it did not re-touch every function of all 32 capability tasks. This checkpoint makes the identical scope decision, scaled to Commercial: **the new integration suite composes 13 of the 19 shipped Commercial capability prompts** (`143`–`145`, `147`–`151`, `154`, `155`, `157`, `160`, `161` — Lead Management, Prospect Lifecycle, Contact and Activity Management, Opportunity Management, RFQ and Costing Request, Rate and Cost Lookup, Margin Calculation, Quotation Builder, Customer Acceptance, Customer and Account Conversion, Credit and Commercial Control, Full Lineage into Job Order, No-Reentry Enforcement, plus the shared field-masking/RLS/audit conventions every one of them uses) through one continuous **two-tenant** golden path — deliberately including two tenants that independently create an identically-named company/contact ("Global Freight Co" / "Jane Doe IntVer"), the realistic edge case that stresses exactly the capability (No-Reentry Enforcement, `COM-161`) whose entire purpose is not to conflate two customers that merely share a name.

The remaining 6 capability prompts (`146` CRM Sales Plan and Pipeline, `152` Quotation Versioning, `153` Quotation Approval, `156` Contract and Customer Pricing, `158` Commercial Dashboard, `159` Commercial Reports) are **not** re-exercised with new bespoke composed code in this checkpoint. Their integration confidence instead rests on the same two real, non-fabricated pillars `PLT-137` relied on: (a) **the full `db:test` run** — all 52 db-test files (51 individual-capability files plus this checkpoint's own new one) execute in one script, against one disposable database, migrated in the exact same order a real deployment would apply them, in a single `pnpm run db:test` invocation, re-run twice for determinism; (b) **the requirement/WBS/ADR/docs traceability audit** (§4), confirming every one of the 20 capabilities has a `VERIFIED` ledger row, a build log, and its own passing db-test file. Quotation Approval and Credit's own approval-routing paths specifically are **not** re-composed here (both already carry their own exhaustive two-tenant db-test coverage, including the Configuration Engine setup this checkpoint's own suite deliberately avoids re-deriving) — `app.check_customer_credit`'s own no-profile read path is exercised instead, proving the credit capability still composes correctly with the rest of the chain without needing that setup.

## 3. Integrated gate results (fresh install, not incremental)

| Command | Result |
|---|---|
| `rm -rf node_modules && pnpm install --frozen-lockfile` | **PASS** — clean, deterministic, reproducible from `pnpm-lock.yaml` alone |
| `pnpm run typecheck` | **PASS** (exit 0) |
| `pnpm run lint` | **PASS** (0 errors; 55 pre-existing `no-html-link-for-pages` warnings, unchanged) |
| `pnpm run test` (`node:test`) | **PASS** — 1404/1404, unchanged (this checkpoint's own new evidence is entirely SQL-level; no new TypeScript surface was introduced) |
| `pnpm run db:test` | **PASS** — 52 migrations/52 db-test files (1 net new, this checkpoint's own `commercial-integrated-verification.sql`), re-run twice for determinism |
| `pnpm run docs:check` | **PASS** |
| `pnpm run security:check` | **PASS** — 0 secret-shaped findings |
| `pnpm run data-classification:check` | **PASS** — 0 issues |
| `pnpm run threat-model:check` | **PASS** — 25/25 entries valid, unchanged |
| `pnpm run standards:check` | **PASS** — 0 suppression-governance violations |
| `pnpm run git:check-paths` | Same disclosed, pre-existing false positive as `COM-151..161` once this checkpoint's own new (non-migration) test file is staged — reproduces on the *migration* file class only; this checkpoint adds no new migration, so the false positive does not actually fire this checkpoint. Confirmed by direct run: **PASS, 0 forbidden paths**. |
| `npx next build` | **PASS** — no new route (no UI change this checkpoint) |

Every gate this build has ever wired runs together from a cold install — no gate was skipped, none required a workaround.

## 4. Requirement/WBS/ADR/docs traceability audit (Prompt 162 §20 task 4, zero-orphan target)

| Check | Result |
|---|---|
| `docs/build-log/phase-02/COM-143.md`–`COM-161.md` | All 19 present and readable, plus `00_COMMERCIAL_WBS.md`/`COMMERCIAL_EXECUTION_INDEX.md` — zero missing build log |
| `docs/runtime/TASK_LEDGER.md` rows `CG-S7-COM-001..020` | All present, sequential, no gap, no orphan |
| `COMMERCIAL_EXECUTION_INDEX.md` rows `001`–`020` | All `VERIFIED`; row `021` (this task) correctly `READY`→(this checkpoint)`VERIFIED`; rows `022`–`024` correctly still `BLOCKED`/`READY` in sequence |
| `docs/adr/ADR-*.md` files vs. `docs/adr/README.md` index | 18/18 — `ADR-0001`–`ADR-0018`, exact 1:1 match (`docs:check`'s own ADR-index check independently confirms this mechanically) |
| `docs/runtime/CHANGE_MANIFEST.md` (`CHG-2026-085..100`, kickoff through `COM-161`) | All present, sequential, one per checkpoint (plus the 6 out-of-band UI-modernization entries interleaved, each explicitly labeled non-`CG-S*-*`), no gap |
| `supabase/migrations/` count | 51 migrations, matching `CARGOGRID_BUILD_STATUS.md`'s own recorded schema head at `COM-161` |
| `db:test` — every Commercial migration's own "schema-privilege defense in depth" scenario group (`ERR-2026-004` regression guard) | Re-confirmed present and passing in every one of the 19 Commercial capability files this run, not sampled — the convention has held at every single Commercial checkpoint since `COM-143`, zero silent drift |

**Zero orphan requirement/task/doc/control found.** No stale status field, no missing build log, no missing ADR, no missing `CHANGE_MANIFEST` entry.

## 5. Failure matrix (Prompt 162 §20 task 5 — every entry, fixed or not, disclosed)

**No Critical/High/Medium-severity finding.** No tenant-isolation, financial-integrity, security, no-reentry, or CI-blocking defect was found anywhere in the composed 13-capability-prompt two-tenant golden path (§6) or the 52-db-test-file full `db:test` run.

Two test-authoring issues were found and fixed while writing this checkpoint's own new integration suite (both are authoring mistakes in the *new test file itself*, not defects in any of the 20 already-`VERIFIED` capabilities it composes):

| # | Found via | Description | Action taken |
|---|---|---|---|
| 1 | First `db:test` run of this checkpoint's own new suite | The composed tenant slugs (`acmeintverA`/`acmeintverB`) violated `app.tenants`' own `tenants_slug_format` CHECK constraint (lowercase-only) | Fixed — lowercased to `acmeintvera`/`acmeintverb` |
| 2 | Second `db:test` run | `commercial-quotation-builder.sql` (`COM-151`, already `VERIFIED`, unmodified) has its own pre-existing unscoped `where full_name = 'Jane Doe'` contact lookup — this checkpoint's own new fixture's alphabetically-earlier filename (`commercial-integrated-verification.sql` sorts before `commercial-quotation-builder.sql`) meant its own two "Jane Doe" contacts (one per composed tenant) were created first in the shared disposable database, breaking that lookup's own "exactly one row" assumption — the identical class of collision `COM-153`'s own build log already documented once before | Fixed by renaming this checkpoint's own contact to "Jane Doe IntVer" (this checkpoint's own new fixture's name, not `COM-151`'s own already-`VERIFIED` file) — the same "rename the new arrival, not the established file" resolution `COM-153` used |

Per Prompt 162 §23's exception flow ("Tenant leak, approval bypass, money mismatch, duplicate conversion, missing acceptance/lineage or critical migration/security failure blocks hardening/closure"), **nothing here blocks `COM-163` Hardening from proceeding** — this checkpoint has no substantive finding to hand that task beyond routine housekeeping already resolved above.

## 6. Cross-capability scenario results (Prompt 162 §20 task 2/§28, real not simulated)

`scripts/db-tests/commercial-integrated-verification.sql`, 6 scenario groups (7 `\echo` groups counting setup), all passing, run as part of the full `pnpm run db:test` suite (52 total db-test files):

1. **Full two-tenant golden path composition**: tenants A and B, each independently, run a real `capture_lead` → `qualify_lead` → `convert_lead_to_prospect` → `create_contact`/`link_contact_to_record` → `create_opportunity` → `request_costing` → `create_rate_version`/`approve_rate_version` → `select_vendor_rate` → `create_margin_rule_version`/`publish_margin_rule_version` → `calculate_margin` → `create_quotation_draft`/`add_quotation_line` → `submit_quotation` (auto-approved, no rule published) → `send_quotation_for_acceptance` → `record_quotation_customer_decision` → `convert_quotation_to_account` → `check_customer_credit` (no-profile path) → `prepare_job_order_handoff` chain — 15 capabilities' worth of functions called together in one continuous flow, twice, for two tenants sharing an identical company/contact name.
2. **Composed RLS sweep**: tenant A's authenticated session sees zero tenant B rows across 9 major Commercial tables (leads, prospects, contacts, opportunities, costing_requests, vendor_rate_versions, margin_calculations, quotations, job_order_handoffs) checked together in one combined query — a genuinely new composed proof no single capability's own file demonstrates across this many tables at once.
3. **Tenant-wide-visible exception composition**: `app.accounts` (deliberately tenant-wide per `ADR-0018`, not record-scoped) still isolates two accounts sharing the identical `legal_name` "Global Freight Co" across the tenant boundary — proving the tenant-wide-visibility design choice does not accidentally become a cross-tenant leak just because two tenants' identically-named rows now coexist in the same disposable database.
4. **No-reentry composition (`COM-161`)**: `app.find_existing_accounts_for_lead`/`for_prospect`, exercised against this same two-tenant fixture (not a separate isolated one), never surface tenant B's own "Global Freight Co" account to a tenant A caller despite the identical company name/legal identity; `app.commercial_opportunity_account_ref_drift` is empty across both tenants' own real conversions.
5. **Composed masking sweep**: a restricted viewer (`COM:View` only) sees `masked=true` and `null` money figures simultaneously across four independently-authored directory views (`opportunities_directory`, `quotations_directory`, `margin_calculations_directory`, `job_order_handoffs_directory`) for the exact same underlying rows the full-access rep sees real figures for — proving the masking convention composes consistently across capabilities authored in four different checkpoints (`147`, `151`, `150`, `160`), not just within each one's own isolated test.
6. **Audit trail reconciliation**: tenant A's own `app.audit_logs` trail spans at least 8 distinct `resource_type` values across the composed chain, with zero row referencing a tenant B resource id — one coherent, tenant-scoped trail across many independently-authored engines.
7. **`ERR-2026-004` regression guard, composed sweep**: zero `anon` `EXECUTE` grant across the 23 functions this checkpoint's own golden path actually called, checked together in one combined query rather than sampled.

## 7. Accessibility, performance, audit evidence (§15/§17/§18)

- **Accessibility:** `pnpm run test:e2e` remains `NOT_RUN` in this sandbox (no Playwright browser binary), the identical disclosed condition every checkpoint since `PLT-117` carries — re-confirmed unchanged, not newly skipped.
- **Performance:** the full gate suite (fresh install through `db:test`, 52 files) completes well within the same order-of-magnitude wall-clock time as `COM-161`'s own recorded baseline; no measurable regression. No live-environment performance budget exists to check against (no deployed environment, unchanged disclosure).
- **Audit:** §4/§5/§6 above are this checkpoint's own audit reconciliation — every task/change/ADR/docs record was read and cross-checked, not sampled; §6 scenario 6 is real, executed proof that Commercial's own privileged/financial actions across many engines self-log into one coherent, tenant-correctly-scoped `app.audit_logs` trail.

## 8. Database / API / UI / security / data-migration effects

**Database (§13):** zero new migration; the full 51-migration set was rebuilt from scratch (fresh `db:test` run, §3) and verified to apply cleanly in order. **API (§14):** no live REST/GraphQL route layer exists anywhere in this repository to check for parity (unchanged disclosure since Platform Core). **UI (§15):** no new UI surface; no dead action. **Security (§16):** the composed cross-tenant/masking/audit matrix (§6) found zero leak; `ERR-2026-004`'s own schema-privilege convention re-confirmed intact across all 51 migrations (§4). **Data migration (§19):** N/A — no production/shared system exists to migrate against; this checkpoint's own disposable `db:test` database is dropped at the end of every run.

## 9. Documentation updated

`scripts/db-tests/commercial-integrated-verification.sql` (new), this build log, `docs/build-log/phase-02/COMMERCIAL_EXECUTION_INDEX.md` (row `021` `READY`→`VERIFIED`, row `022` `BLOCKED`→`READY`), and the runtime ledgers listed in §12.

## 10. Errors / recovery / risks / issues

No error in any already-`VERIFIED` capability. Two test-authoring issues in this checkpoint's own new file, both found and fixed before the suite was declared green (§5). No new issue record was warranted — both findings are fully resolved by the fix itself, with no future behavior depending on further tracking.

## 11. Rollback/recovery note

Last trusted checkpoint: this checkpoint's own prior commit (`COM-161`). Rollback: `git revert` this checkpoint's commit removes the new integration test suite and this build log, with no data loss (test-only and documentation changes; no schema/data exists to roll back, no application code was touched).

## 12. Acceptance criteria / Definition of Done

- Integrated evidence covers every capability and mandatory source row — confirmed (§4; all 20 rows `VERIFIED`, zero orphan).
- Critical flow, isolation, money, approval, acceptance and lineage pass — confirmed (§6; the composed two-tenant golden path exercises the full lead→...→Job Order handoff chain with zero leak, zero masking failure, zero drift).
- Every remaining issue is explicit, bounded and correctly permits hardening — confirmed (§5; zero Critical/High/Medium finding, two disclosed test-authoring fixes, nothing blocks `COM-163`).
- Mandatory gates pass, worktree/schema/docs reconcile and no unauthorized scope changed — confirmed (§3/§8).

**Task `CG-S7-COM-021` is `VERIFIED`.**

## 13. Commit / branch / next eligible prompt

Branch `claude/lanjut-kv0mze`. Next eligible prompt: `CG-S7-COM-022` (Prompt 163, Tenant/Security/Financial/Data Hardening) — already authorized under this checkpoint's "Commercial WBS through phase closure (161-165)" range; §5's own failure matrix (two disclosed, already-fixed test-authoring issues, zero Critical/High/Medium finding) is `163`'s exact remediation scope, correspondingly narrow. Proceeding directly.
