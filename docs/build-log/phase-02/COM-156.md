# COM-156 — Contract and Customer Pricing

## 0. Checkpoint

| Field | Value |
|---|---|
| Prompt | `156_CONTRACT_CUSTOMER_PRICING_PROMPT.md` (`CG-S7-COM-015`) |
| Repository | `assujiar/cargogrid.app` |
| Working branch | `claude/cargogrid-design-system-expansion-ljbzkm` (this session's harness-assigned branch; prior checkpoints on this branch: an out-of-band design-system task, `COM-151..155`) |
| Dependency | `COM-150` (Margin Calculation, `VERIFIED`), `COM-154..155` (Customer Acceptance, Customer and Account Conversion, both `VERIFIED`) |
| Authorization | A single, unscoped user message ("lanjut") — read as authorizing exactly this one task, the same precedent `docs/runtime/HANDOFF.md` records repeatedly for a bare "lanjut." This checkpoint does not proceed to `157` without a further explicit authorization. |

## 1. Pre-flight collision check

`git status --short --branch` clean at start except this checkpoint's own new files. No open PR exists for this branch (`mcp__github__list_pull_requests` returned `[]`); no other remote branch carries unmerged Commercial work at this range.

## 2. Baseline reconciliation

`docs/build-log/phase-02/COMMERCIAL_EXECUTION_INDEX.md` row `015` was `READY` — dependencies `150`,`154..155` all `VERIFIED`. Read in full before implementation: `156_CONTRACT_CUSTOMER_PRICING_PROMPT.md`. Researched the codebase for reusable precedent: confirmed `app.vendor_rate_versions` (`COM-149`) as the closest schema precedent for service/lane/rate/component structure on the vendor-cost side; confirmed `app.quotations`' own `root_quotation_id`/`version_number` self-referencing-insert pattern (`COM-152`) as directly reusable for a contract's own version identity, adapted since a contract (unlike a quotation) may legitimately carry more than one simultaneously-published, non-overlapping-window version; confirmed the fixed `app.permissions_action_check` 19-action enum (re-discovered at `COM-155`) still permits *new* `(module, action)` pairs for an already-listed action — `'View margin'` is already a real enum value (currently seeded only for `FIN`/`PRC`), so a new `COM:View margin` row is a legitimate, bounded addition using exactly the same mechanism `COM-148` already used to add `COM:View cost`, unlike `COM-155`'s own blocked `'View billing'` attempt (an enum value that does not exist at all). `docs/runtime/CARGOGRID_BUILD_STATUS.md`/`TASK_LEDGER.md`/`HANDOFF.md` reviewed — no new blocking error/issue since `COM-155`.

## 3. Implementation

### 3.1 Scope boundaries (disclosed, migration header)

1. **Governance is a direct `COM:Approve` gate on publish/retire, not a second Approval Engine routing.** Unlike `COM-153`'s own quotation approval (which does instantiate `PLT-121`/`123`), a contract price list is a materially smaller governance surface — reinstantiating the full engine a second time in the same phase would exceed Prompt 156's own file-count boundary (5–15 files, 1–3 migrations). Mirrors the same "governance-weighted, hard-to-reverse action gated by `COM:Approve` directly" precedent `app.convert_quotation_to_account` (`COM-155`) and `app.publish_margin_rule_version` (`COM-150`) already established.
2. **`COM:View margin` is newly seeded for the `COM` module** — reusing the already-real `'View margin'` enum value. Selling price and discount figures on a contract's price components are masked behind the already-established `COM:View selling price` (`COM-147`); this migration stores no separate margin figure anywhere (margin was already fully computed and locked at quotation time by `COM-150`'s own margin engine). `COM:View margin` is seeded now, unused by any masked column yet, so a future capability that does compute a contract-level margin figure has a real permission ready.
3. **No standalone site/address master** — still deferred (`COM-145`/`155`'s own disclosed boundary, restated as still not resolved).
4. **No persisted price-selection/snapshot table.** `app.get_effective_customer_price` returns a fully-shaped, deterministic snapshot row directly, but nothing persists it — no Job Order or other downstream consumer exists yet in this repository to snapshot into (`COM-160`, "Full Lineage into Job Order," is a later, separate capability). Building a persisted table with no real writer would be the exact premature-model risk this repository's binding rules forbid.
5. **Legacy contract/pricelist migration is not applicable** — greenfield, no live environment.

### 3.2 Schema — `supabase/migrations/20260724300000_create_commercial_customer_contract_pricing.sql`

New `app.customer_contracts` (one row per contract/pricelist version — `root_contract_id`/`version_number` identity, self-referencing-insert technique reused from `app.create_quotation_draft`, `COM-152`; `status` draft/published/retired). Unlike a quotation's single `is_current` row, multiple versions of one root may be `status=published` simultaneously as long as their `[effective_from, effective_to)` windows do not overlap — enforced at publish time via `SELECT ... FOR UPDATE` row locking against every other published sibling under the same root (closing the same concurrent-publish race `app.create_quotation_revision`, `COM-152`, already closed for its own analogous invariant), not a partial-unique index. New `app.customer_contract_price_components` (one priced service/lane condition per row; a unique index on `(contract_id, service_type, mode, origin_lane, destination_lane, equipment_type)`, coalesced for nulls, makes `app.get_effective_customer_price` structurally resolve to at most one row).

### 3.3 Functions

`app.create_customer_contract_draft` serves both flows identically: main flow (`p_source_quotation_id` set) requires `customer_decision='accepted'` and an existing `app.account_conversions` row (`COM-155`) — one contract root per source quotation, ever (`quotation_already_contracted` otherwise); alternative flow (`p_source_contract_id` set) is a renewal/amendment — copies the source version's own price components into the new draft, requires a non-empty reason, appends the next `version_number` under the same root. `app.add_customer_contract_price_component`/`app.remove_customer_contract_price_component` — only while the owning contract is `status=draft`; the add function is **dual-gated on `COM:Edit` + `COM:View selling price`** (mirrors `app.select_vendor_rate`'s own dual gate, `COM-149`), since it returns the raw, unmasked row directly rather than a masked view row — a defect caught and fixed during authoring, see §8. `app.publish_customer_contract` — `COM:Approve`-gated, requires ≥1 price component, rejects a date-overlapping already-published sibling (`overlapping_active_version`). `app.retire_customer_contract` — `COM:Approve`-gated, mandatory reason, only a published contract may be retired. `app.get_effective_customer_price` — the deterministic, reproducible effective-price lookup: exact-match on every identity dimension (`IS NOT DISTINCT FROM`, never a wildcard), combined with the publish-time overlap guarantee and the price-component identity unique index, structurally resolves to at most one row; raises `no_effective_price` explicitly on zero rows rather than returning an empty set silently (Prompt 156 §25's own "or fails explicitly"). Masked the same way `app.customer_contract_price_components_directory` is.

### 3.4 Service layer and UI

`server/contracts/contract/contract.ts` (schemas/parsers for `CustomerContract`/`CustomerContractPriceComponent`/`EffectiveCustomerPrice`, an XOR-refined `CreateCustomerContractDraftInputSchema`). `server/queries/contract.ts` — `listCustomerContracts`, `listCustomerContractVersions`, `getCustomerContractById`, `getCustomerContractForQuotation`, `listCustomerContractPriceComponents` (reads the masked directory view), `getEffectiveCustomerPrice` (resolves `asOf` to the current time **in JS** before calling the RPC — the SQL function's own `default now()` only applies when an argument is omitted entirely, never when explicitly passed as `null`, a real correctness detail caught before it could silently break every unscoped lookup). `server/mutations/contract.ts` — thin wrappers around the five RPCs.

UI: `commercial/contracts/` list + `contracts/[contractId]/` detail (price components table, `AddComponentForm`, publish/retire bound actions, `RetireForm`, `RenewalForm`, version history). Quotation Detail page gained a `ContractCreationPanel`/`CreateContractForm` (renders once the quotation is accepted and converted to an account — COM-155's own precondition — either "already has a contract → view" or the create form) and `createContractFromQuotationAction` in `actions.ts`. `commercial/layout.tsx` gained a "Contracts" nav link.

## 4. Tests

### 4.1 Real database tests (`scripts/db-tests/commercial-customer-contract-pricing.sql`, via `pnpm run db:test`)

Setup: one tenant, a rep (`COM:Create/Edit/Approve/View/View selling price/View cost`), a sibling-team viewer (`COM:View` only), a full lead→prospect→opportunity→costing→rate→margin→quotation chain, accepted and converted to an account. Assertions: `COM:View margin` newly seeded, granted to nobody; `create_customer_contract_draft` authority-gated (viewer lacks `Edit`), creates version 1 as its own root sourced from the quotation, a second attempt from the same quotation rejected (`quotation_already_contracted`); `publish_customer_contract` rejects an empty draft (`no_price_components`); `add_customer_contract_price_component`/`remove_customer_contract_price_component` — duplicate identity rejected, remove leaves the correct count; `publish_customer_contract` authority-gated (viewer lacks `Approve`), succeeds once ≥1 component exists; `get_effective_customer_price` resolves deterministically, raises `no_effective_price` for an unmatched service, masks money fields for the viewer; renewal flow copies components, mandatory reason enforced; an overlapping renewal publish is rejected (`overlapping_active_version`); a second, non-overlapping renewal (a different price point) publishes alongside the still-published version 1, and the lookup disambiguates by `as_of` date (+10d resolves v1's price, +45d resolves v3's price); `retire_customer_contract` authority-gated, mandatory reason, a retired version drops out of the lookup; tenant-wide RLS visibility (the sibling-team viewer sees all 3 versions and the masked components); schema-privilege defense in depth (`authenticated` has no direct column access to `base_amount`); full audit trail (3 successful creates, 2 successful publishes — the overlapping attempt left no trace — 1 successful retire). All 46 db-test files pass together, zero regression to `COM-144..155`'s own test files.

### 4.2 TypeScript unit tests

New: `server/contracts/contract/contract.test.ts` (14), `server/queries/contract.test.ts` (9), `server/mutations/contract.test.ts` (6) — 29 net new.

## 5. Gate results

| Gate | Result |
|---|---|
| `pnpm run typecheck` | PASS |
| `pnpm run lint` | PASS (0 errors — one real `react/no-unescaped-entities` error was caught and fixed during authoring, see §8; same pre-existing `no-html-link-for-pages` warning class, unchanged) |
| `pnpm run test` | PASS — 1265/1265 (29 net new) |
| `pnpm run db:test` | PASS — 46 migrations/46 db-test files, zero regression |
| `pnpm run docs:check`/`security:check`/`data-classification:check`/`threat-model:check`/`standards:check` | PASS |
| `pnpm run git:check-paths` | Same disclosed, pre-existing false positive as `COM-151..155` — flags this checkpoint's own new migration file as a forbidden "applied migration edit." Not a new finding. |

Note: this repository's `package.json` defines no `build` script — `next build` is not part of this repository's own gate set (reconfirmed identical to `COM-155`'s own gate table).

## 6. Database / API / UI / security / data-migration effects

One new migration — additive for every object (2 new tables, 1 new permission row + 1 new gate function, 5 new functions, 1 masked view). Zero prior migration file edited. One new permission-catalogue row (`COM:View margin`, reusing the existing enum value — not a widened enum, the same bounded mechanism `COM-148` already used). Security: `app.customer_contract_price_components` carries real column-level masking (unlike `COM-155`'s own `app.accounts`, which disclosed it had none available) — `COM:View selling price` gates the five money-bearing columns both on the read path (the masked directory view) and on the one mutation that returns them directly (`add_customer_contract_price_component`'s own newly-added dual gate). Governance (publish/retire) is a direct `COM:Approve` gate, not a second Approval Engine instantiation. Data migration: no backfill performed — a pre-existing accepted quotation simply has no contract until one is explicitly created.

## 7. Documentation updated

`docs/build-log/phase-02/COM-156.md` (this file), `COMMERCIAL_EXECUTION_INDEX.md` (row `015` `READY`→`VERIFIED`, row `016` `BLOCKED`→`READY`, tally, collision-inspection rows), `docs/runtime/TASK_LEDGER.md`, `CARGOGRID_BUILD_STATUS.md`, `HANDOFF.md`, `CHANGE_MANIFEST.md` (`CHG-2026-088`).

## 8. Errors / recovery / risks / issues

Three real defects found and fixed during authoring, all caught before this checkpoint declared itself green: (1) `app.get_effective_customer_price`'s initial plpgsql body used `select ... into v_row` followed by `if v_row is null then` to detect a zero-row match — a `record`-typed variable does not reliably evaluate as `IS NULL` after a `select into` on zero rows; fixed by checking the implicit `FOUND` boolean immediately after the select instead, the correct plpgsql idiom; (2) `app.add_customer_contract_price_component` initially gated only `COM:Edit` while returning the raw, unmasked price-component row directly — an actor holding `Edit` but not `View selling price` could round-trip a selling price back to themselves through the RPC's own return value; caught by comparing against `app.select_vendor_rate`'s own established dual-gate precedent (`COM-149`) before any test was written, fixed by adding an explicit `has_view_selling_price` check alongside the `Edit` gate; (3) a real `react/no-unescaped-entities` lint error (an apostrophe in `renewal-form.tsx`'s own help text) — fixed by escaping it (`&apos;`). Zero Critical/High-severity issue remains open. Zero regression to any prior capability (full `db:test`/`test` re-run, all green).

## 9. Rollback/recovery note

`git revert` of this checkpoint's commit reverts the migration (additive tables/columns/functions/permission-row only — dropping them is safe, no other capability depends on them yet). No downstream Commercial capability (`COM-157` Credit and Commercial Control) has run yet to depend on any object this checkpoint adds.

## 10. Acceptance criteria / Definition of Done

All Prompt 156 §33 acceptance criteria met: contract pricing is versioned, effective-dated, and traceable to acceptance (`source_quotation_id`, `root_contract_id`/`version_number`); exactly one deterministic authorized price resolves for a valid context (proven directly — the overlap invariant plus the price-component identity unique index structurally guarantee at most one match, and the function raises explicitly rather than guessing); no issued quote or active transaction is silently repriced (the lookup is a stateless, on-demand read — nothing is retroactively rewritten, and a retired version simply drops out of future lookups without altering any historical record). All mandatory gates pass except the one disclosed, reproducing-only-once-staged `git:check-paths` false positive; worktree/schema/docs reconcile; no unauthorized scope changed.

## 11. Commit / branch / next eligible prompt

Branch `claude/cargogrid-design-system-expansion-ljbzkm`. Next eligible prompt: `CG-S7-COM-016` (Prompt 157, Credit and Commercial Control) — dependency-`READY` once this checkpoint's `156` is `VERIFIED` (`COM-155..156` both `VERIFIED`). **Not started this checkpoint** — this checkpoint's own authorization was a single, unscoped "lanjut," read as authorizing exactly one task per `docs/runtime/HANDOFF.md`'s own standing precedent. A fresh explicit authorization is required before `157` proceeds.
