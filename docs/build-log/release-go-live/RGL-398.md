# `RGL-398` — Seed Validation

**Task ID:** `CG-S16-RGL-008`
**Prompt:** `docs/ai-agent-build-prompt-package/16-release-go-live/398_SEED_VALIDATION_PROMPT.md` (`CG-AABPP-RGL-398`, package `0.17.0`)
**Date:** 2026-08-25, session timezone `Etc/UTC`
**Branch:** `claude/step-16-prompt-390-412-okbd6v`
**Release candidate:** `RC-2026.08.25-1` (frozen at `RGL-392`, amended four times to date — none by this checkpoint)
**Upstream:** `CG-S16-RGL-007` (`RGL-397`), `COMPLETED`
**State:** **`COMPLETED`** — first round. Tier C review required before `VERIFIED`.

> Not a promotion, a go decision, or a production/pilot/GA/market-ready claim.

---

## 1. Objective, restated against what this checkpoint actually did

Prompt 398 §4: *"Validate seed, fixture and reference data for release candidate, UAT, staging and
production bootstrap without leaking tenant-real data."*

1. Reconfirmed (from `RGL-396`) that no CLI-managed seed script exists — genuinely absent, not
   re-litigated.
2. Scanned every seed/fixture SQL source in the repository for realistic-looking tenant PII —
   emails, Indonesian tax-ID (NPWP) and national-ID (NIK) shaped literals (§2).
3. Ran the repository's own `data-classification:check` gate (§2).
4. **Queried live production directly** for tenant-real data, not only source control (§3) — the
   part of this objective most checkpoints would stop short of, since "seed data" usually means
   local fixtures, but Prompt 398 §4's own wording ("production bootstrap... without leaking
   tenant-real data") is explicitly about the live database too.
5. **Found one finding**: a single orphaned, unlinked `auth.users` row in live production (§4) —
   synthetic, not customer PII, but a stray artifact. Documented and left for a checkpoint
   authorized to mutate production, per this prompt's own explicit constraint (§5).
6. UAT/staging seed data: confirmed there is nothing to validate, since neither tier exists yet
   (§6) — an already-disclosed gap, not rediscovered here as new.

---

## 2. Source-control seed/fixture data — scanned, not merely trusted

**Email domains** across every `.sql` file under `scripts/db-tests/`, `scripts/load-tests/`, and
`supabase/migrations/`: extracted every email-shaped literal, filtered to domains outside the
expected safe set (`example.*`, `test.*`, `cargogrid.{app,net,test,local}`, `localhost`). Result:
every remaining domain uses the RFC 2606-reserved `.test` TLD (`acmecontact.test`,
`acmefinconf.test`, etc.) — standard, non-routable, unambiguously synthetic test-fixture naming.
Zero real-looking domains (`gmail.com`, `yahoo.com`, etc.) found anywhere.

**Indonesian tax-ID (NPWP) shaped literals** (`XX.XXX.XXX.X-XXX.XXX`): found only sequential or
repeating-digit placeholders (`01.111.111.1-111.000`, `01.234.567.8-901.000`, and similar) —
synthetic pattern data, not real registered tax IDs.

**Indonesian national-ID (NIK) shaped literals** (16 consecutive digits): zero matches anywhere in
`scripts/db-tests/` or `scripts/load-tests/`.

**`scripts/load-tests/seed.sql`**, the one substantial bulk fixture in this repository (1 tenant, 2
owner accounts, 400 item masters, ~800 inventory balances, 5,000 jobs, etc., per its own header):
confirmed via direct read of its header that its own volume figures are disclosed and its identity/
permission/warehouse setup goes through real RPCs exactly like every `db-tests` fixture — no
different treatment, no separate risk class.

**`pnpm run data-classification:check`**: `✔ data-classification registry is internally valid,
every secret-classified env var is classified, and every enforced protected Finance/HRS action is
classified.`

---

## 3. Live production — queried directly, not assumed empty

Prompt 398 §4's own wording covers "production bootstrap... without leaking tenant-real data" —
this is a claim about the live database's actual content, not only about what ships in source
control. Queried `awdlicmwzdxquopwtcfd` directly:

```
select count(*) from app.tenants;                    -- 0
select count(*) from app.tenant_user_identities;      -- 0
select count(*) from app.accounts;                    -- 0
select count(*) from app.job_orders;                  -- 0
select count(*) from app.finance_journals;             -- 0
select count(*) from auth.users;                       -- 1
```

Every tenant-scoped table is genuinely empty — the correct, expected state for a pre-launch
release candidate. `auth.users` alone returns 1 row, investigated in §4.

---

## 4. Finding — one orphaned `auth.users` row, synthetic, not tenant-real, not fixed here

**What it is.** `id = 2d0b7791-83db-4f58-8053-172fd67af697`, `created_at = 2026-08-25 04:58:25
UTC` (inside this session's own working window, before this checkpoint began), email domain
`cargogrid.net` (a placeholder domain — not the product's real `cargogrid.app` domain, not a real
customer's), `raw_user_meta_data = {}`. **Zero rows in `app.tenant_user_identities` reference it**
— no tenant membership, so no path into any application data under this schema's own access model
(every RLS policy and RBAC check in this schema gates on tenant membership; an identity with none
can reach nothing).

**Not real customer data.** The domain, empty metadata, and complete absence of any tenant linkage
are all consistent with a leftover artifact from earlier live-forced authenticated-session testing
this same day (`RGL-BLK-002`'s own remediation did real PostgREST probing against live), not a
genuine signup. No build log this session explicitly records creating it, so the exact originating
action could not be reconstructed with certainty — disclosed as uncertain rather than guessed at.

**Deliberately not fixed at this checkpoint.** Prompt 398 §12 explicitly forbids "production
mutation... in this prompt." Deleting this row is a one-line, low-risk cleanup with no cascading
effect (nothing references it), but Seed Validation's own charter is to validate and report, not to
mutate live data even for a low-risk hygiene item — that is left for a checkpoint actually
authorized to touch production (`RGL-015`, Production Deployment, or a direct operator-directed
action).

Registered `ISS-2026-294`, Low severity — no tenant-real data, no access path, no security impact;
the finding is that production's `auth.users` table is not perfectly empty, a hygiene item, not a
defect.

---

## 5. UAT/staging seed data

Nothing to validate. Neither a UAT nor a staging tier exists yet (execution index §7.1, already
disclosed at prior checkpoints and not rediscovered here as new) — there is no environment to seed.
This is `RGL-009`/`RGL-010`'s own charter to formally address; noted here only so this checkpoint's
own silence on UAT/staging seed data is not mistaken for an oversight.

---

## 6. What was not attempted, and why

- **The orphaned `auth.users` row was not deleted.** Prompt 398 §12 explicitly forbids production
  mutation in this prompt — see §4.
- **No new seed/fixture file was authored.** This checkpoint's charter is validation, not creating
  seed data this project has never had (which would itself be a scope expansion, not a fix).
- **`RGL-BLK-001` and `RGL-BLK-003` were not touched.** Outside this checkpoint's charter; remain
  `RGL-404`'s.

---

## 7. Next eligible task

`CG-S16-RGL-009` (Prompt 399, Staging Deployment) — dependency-clean. Inherits confirmation that no
tenant-real data exists anywhere this checkpoint could reach, source control or live, and one
disclosed, low-severity, deliberately-unfixed hygiene item for a later authorized checkpoint to
clean up.
