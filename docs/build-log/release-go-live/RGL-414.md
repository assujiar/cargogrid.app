# RGL-414 — `GO_DECIDED`: the go decision, and the production deployment it authorizes

**Date:** 2026-08-31
**Release candidate:** `RC-2026.08.25-1`
**Authorized commit:** `d48b589018321bba1c1fbb12f8befd7664b4fc34`
**Decided by:** the project owner (`as.sujiar@gmail.com`)
**Machine-readable record:** `docs/build-log/release-go-live/GO_DECISION.json`

---

## 1. The two conditions `GO_NO_GO_REPORT.md` §7 still had open

That report closed items 1–4 at Step 16 and left two:

> **5.** This branch merged and its own content re-verified as the actual candidate that would
> deploy — not `main`'s current, older state. Still open.
>
> **6.** An explicit, separate operator instruction to proceed to a go decision. […] A future
> `RGL-404` re-run (or this same report, revised again) needs the operator to say, separately and
> explicitly, that they want to proceed to `GO_DECIDED` now.

**Item 5 — satisfied by fact.** `claude/step-17-implementation-0fbul7` was 49 commits ahead of
`origin/main` and 0 behind, so the merge was a genuine fast-forward: `main` is now
`d48b589018321bba1c1fbb12f8befd7664b4fc34`, byte-identical to the branch head that every gate in
this session was run against. There is no divergence between "what was verified" and "what would
deploy", which is exactly what item 5 asked for.

**Item 6 — satisfied deliberately, not inferred.** That report is explicit that item 6 must be
"made deliberately, not inferred from an adjacent instruction". So it was not inferred from the
owner's earlier "lanjut sampai siap deploy dan publish". It was put to them as a direct question,
naming what would happen, and answered: **"Ya — merge + GO + deploy"**.

The same exchange recorded two further decisions, both acted on:

- the `ISS-2026-300` ledger reconciliation — *"Jalankan sekarang"* (executed and verified; see
  `RGL-413.md` §5 and the entry itself);
- the `cargogrid.app` domain — *"Pakai subdomain saja"* (see §4 below).

## 2. What this go decision does and does not authorize

**Authorizes:** promoting the build of commit `d48b589` to `target: production` on the Vercel
project `cargogrid-app`.

**Does not authorize, and is not:**

- a claim that the human-only gates were passed. UAT, the external penetration test, branch
  protection and the tax-rate confirmation remain **`ACCEPTED_RISK (OWNER_OVERRIDE)`** under
  ADR-0027 Part B — accepted, dated, not performed. Nothing here upgrades an acceptance into a
  pass.
- a domain cutover. See §4.
- a claim that the backlog is empty. 89 items remain open (5 High, 41 Medium, 43 Low), each with
  a disposition.

## 3. Why the authorized SHA is the commit it is, and not the commit containing this decision

Worth stating, because it looks like an off-by-one until you follow it through.

`scripts/release/check-go-decision.ts` compares `authorizedCommitSha` against
`VERCEL_GIT_COMMIT_SHA` — the commit being built — and refuses on any mismatch, deliberately
using a full-length comparison so no prefix collision can authorize the wrong commit. A file
naming the SHA of the commit that contains it is impossible to write: a commit's hash covers its
own contents, so adding the decision changes the hash the decision would have to name.

That is not a defect in the gate, because Git-triggered production builds are disabled at the
same time: `vercel.json` sets `git.deploymentEnabled.main = false`. The intended path — the one
the plan states — is a **manual promote** of a build that already exists and has already been
verified.

So the decision names `d48b589`: the exact commit whose build is promoted. The decision is
recorded *after* that artifact exists and *about* it, which is the correct order for a decision
that is supposed to rest on evidence. This record and `GO_DECISION.json` land in the commit
after, as the audit trail of a decision already made.

One consequence, and it is the safe direction: a future Git-triggered production build at the
*later* commit would find `authorizedCommitSha` naming `d48b589`, not itself, and refuse. A new
deployment needs a new decision. That is the gate working, not a problem to route around.

## 4. What "publish" turned out to mean, and the decision taken

`RGL-413.md` §6 established live that `cargogrid.app` is served by **Cloudflare**, renders a
different site built with a website builder, returns `404` on `/api/health`, and has never been
attached to this Vercel project. Promoting a production build therefore does **not** put
CargoGrid on that domain.

Put to the owner with the consequence stated — that a cutover would *replace* the page currently
live there — the decision was **"Pakai subdomain saja"**: leave `cargogrid.app` as it is, and
give CargoGrid a subdomain instead. Nothing currently public is replaced.

The DNS and domain-attachment steps are outside this session's access and are written up for the
owner to run in `docs/runbooks/human-execution-pack.md`. Tracked as `ISS-2026-311`.

## 5. Evidence this decision rests on

| Gate | Result |
|---|---|
| `typecheck` | pass |
| `lint` | pass |
| `test` | 5,648 pass, 0 fail |
| `db:test` | 390 migrations, 236 runner files, ALL PASSED |
| `issues:check` | pass (3 informational re-read warnings) |
| `release:check-freeze` | `RC-2026.08.25-1` matches its frozen migration, db-test and lockfile digests |
| `git:check-paths` | no forbidden paths touched |
| Supabase security advisors | 1 ERROR (PostGIS `spatial_ref_sys`, tracked `ISS-2026-234`); no application-owned SECURITY DEFINER function has a mutable `search_path` |
| Supabase performance advisors | zero WARN, zero ERROR |
| `public.*` wrapper grant parity, live | zero wrappers wider than their `app.*` counterpart |
| Migration ledger, live | 390 of 390 repository migrations present under their own version |

One defect was found by this session's own live verification and fixed before this decision:
`ISS-2026-309`, two `anon`-executable SECURITY DEFINER wrappers introduced earlier the same day.
It is recorded in full, including the reason a correct gate did not catch it.

## 6. The authorized artifact

| Property | Value |
|---|---|
| Deployment | `dpl_4iu6XpA4b4evMqgx9zJN6LiFVEqj` |
| URL | `cargogrid-18fjypfdd-saiki-tech.vercel.app` |
| Commit | `d48b589018321bba1c1fbb12f8befd7664b4fc34` (matches the go decision exactly) |
| Commit verification | `verified` |
| State | `READY` |
| Build | completed clean — every route compiled, zero error/stderr/fatal lines in the build log |
| Region | `iad1` |
| Target | `null` (preview) — awaiting promotion |

## 7. Promotion — what happened, stated plainly

**The promotion has not been performed, and I could not perform it.**

Everything up to it is done: `main` is at the authorized commit, the go decision is recorded and
machine-verifiable, and the artifact is built and `READY`. The remaining step is promoting
`dpl_4iu6XpA4b4evMqgx9zJN6LiFVEqj` to `target: production`.

This session has no way to do that:

- `git.deploymentEnabled.main = false` means merging to `main` deliberately does not deploy —
  that is `RGL-BLK-001`'s fix working, exactly as intended;
- no Vercel API token or CLI is present in the environment (checked, not assumed);
- the Vercel tools available here can list, inspect and read logs, but none can promote a
  deployment or change its target.

**One thing I deliberately did not do.** There is a `deploy_to_vercel` tool that accepts an
uploaded file tree with `target: "production"`. Using it would have produced something that looks
like success and is not: a deployment with no Git provenance, built from an ad-hoc upload rather
than the verified commit, and therefore never evaluated by `check-go-decision.ts` at all. It would
have gone to production by *bypassing the very gate this release exists to honour*. ADR-0027
Part C — "No gate may be disabled, weakened, or relabelled to obtain a green result" — covers
routing around one just as much as switching it off.

So the last click is the owner's, which is also the correct place for it. The exact steps are in
`docs/runbooks/human-execution-pack.md` §0.1, along with the subdomain steps (§0.2) for the
`"Pakai subdomain saja"` decision.

**Live verification is therefore also outstanding.** The health endpoints could not be probed from
here: SSO protection covers every `*.vercel.app` URL, and the share-link bypass would not issue a
link for this deployment. Once the domain is attached, `app.cargogrid.app` is exempt from that
protection and the probes will work. The results — good or bad — get recorded in this section.

## 8. Status

**`GO_DECIDED`.** Merged, recorded, built, verified as far as this session's access reaches.

Outstanding, both owner actions, neither of them a gate this session may close:

1. promote `dpl_4iu6XpA4b4evMqgx9zJN6LiFVEqj` to production;
2. attach `app.cargogrid.app` and repoint DNS (subdomain only — `cargogrid.app` is untouched).

Live post-deploy verification follows those and is recorded in §7.
