# `RGL-404` — Go/No-Go Report

**Task ID:** `CG-S16-RGL-014`
**Prompt:** `docs/ai-agent-build-prompt-package/16-release-go-live/404_GO_NO_GO_REPORT_PROMPT.md` (`CG-AABPP-RGL-404`, package `0.17.0`)
**Date:** 2026-08-25, session timezone `Etc/UTC`
**Branch:** `claude/step-16-prompt-390-412-okbd6v`
**Release candidate:** `RC-2026.08.25-1`
**Upstream:** `CG-S16-RGL-013` (`RGL-403`), `COMPLETED`
**State:** **`COMPLETED`** — sets `NO_GO`. Tier C review required before `VERIFIED`.

> **Verdict: `NO_GO`.** See `docs/build-log/release-go-live/GO_NO_GO_REPORT.md` for the full
> decision record — this file documents how that decision was reached.

---

## 1. Objective, restated against what this checkpoint actually did

Prompt 404 §4: *"Produce a go/no-go report with release scope, evidence status, blocker status,
risk acceptance, deployment plan, rollback plan and approvals."*

This checkpoint:

1. Re-verified, live, whether `RGL-BLK-001` (ungated production auto-deploy) is still armed — it
   is (§2).
2. Read the full content of all 17 inherited Step 15 `ACCEPTED_EXCEPTION` High blockers
   (`RGL-BLK-003`'s own aggregate) and re-ruled each against a production go-live bar, per
   execution index §8.2 condition 4 — not carried forward by reference (§3).
3. Found that one of those 17 items matches §8.1's own Critical definition verbatim
   ("financial mis-posting") and escalated it, registering a new blocker (§3.1).
4. Found a second item not safely acceptable as routine residual risk (a security incident-
   response control that silently does not work) and escalated it separately (§3.2).
5. Produced `GO_NO_GO_REPORT.md` — the primary decision artifact this task's own WBS row names —
   covering release scope, evidence status, blocker status, risk acceptance, deployment plan,
   rollback plan, and approvals, reaching `NO_GO`.
6. Reasoned explicitly about what a `NO_GO` verdict means for the remaining 8 WBS rows
   (`RGL-405`–`412`) given this range's own strict sequential dependency rule (§7).

---

## 2. `RGL-BLK-001` — re-verified live, unchanged, still armed

`mcp__ada53b8f...__get_project_deployment_protection` against
`prj_9ND1BsfbppHiqeKrSEldYh8xbC68`/`team_jYIRP8E0gAnewOGS5H7yD3BL` this checkpoint:

```json
{
  "passwordProtection": { "enabled": false },
  "ssoProtection": { "enabled": true, "deploymentType": "all_except_custom_domains" },
  "trustedIps": { "enabled": false }
}
```

Identical to the configuration recorded at `RGL-391` kickoff and re-confirmed at `RGL-394` —
`ssoProtection`'s own `all_except_custom_domains` scope means the production custom domain
(`cargogrid-app.vercel.app`) is **not** SSO-gated; only preview deployments are. `get_project`
confirms `main`'s own latest deployment tracking is unrelated to this branch's own work (the
project's `latestDeployment` reflects whatever branch most recently pushed, not `main` itself).
`get_commit` against `main` (`mcp__github__`) confirms `main` is still at `2670cb5849c2ab7b653fef
586f51130eb54ef321` — the same commit recorded at kickoff. This entire Step 16 range's own 13
completed checkpoints (`RGL-391`–`403`) live only on `claude/step-16-prompt-390-412-okbd6v`; none
has been merged. `list_branches` spot-check re-confirms `protected: false`, unchanged from
`RGL-393`'s own 47-branch audit.

**Conclusion: `RGL-BLK-001` is unchanged, still Critical, still armed.** No mechanism exists today
to prevent the merge that closes this range from itself triggering an ungated production
deployment.

---

## 3. The 17 inherited Step 15 High items — full re-ruling

Read in full: `docs/build-log/full-system-hardening/BLOCKER_LEDGER.md` entries `HDN-BLK-016`,
`017`, `018`, `022`, `024`, `027`–`038` (all 17 named by `RGL-BLK-003`'s own "The items" field).

### 3.1 `HDN-BLK-016` → escalated to `RGL-BLK-009` (Critical)

`app.request_finance_settlement_reversal` posts no reversing GL journal — live-forced at `HDN-374`,
a posted settlement's reversal correctly reopens the AP open item but leaves the original GL
journal `posted`, unchanged, with zero system-generated correction path. Reachable by any ordinary
`FIN:Approve` holder, on **every** occurrence, not a rare edge case.

Step 15's own severity model rated this **High**. Re-reading `00_RELEASE_GO_LIVE_EXECUTION_INDEX.md`
§8.1's Critical definition — *"Tenant isolation breach, authentication/authorization bypass,
**financial mis-posting**, data loss or corruption, migration failure, no working rollback, or a
production outage"* — this finding is a live-forced, deterministic instance of exactly the named
category. This is not a reclassification of Step 15's own historical ruling (that record is
unchanged, per this ledger's own append-only discipline) — it is this checkpoint's own independent
application of Step 16's own binding severity model to the same, still-unfixed underlying fact,
which Step 15's own ruling never had occasion to test against a production-money-at-stake bar.

Registered `RGL-BLK-009`, Critical, `OPEN`, **not accepted** — full ruling:
`docs/build-log/release-go-live/BLOCKER_LEDGER.md`.

### 3.2 `HDN-BLK-035` → escalated to `RGL-BLK-010` (High, not routinely acceptable)

`app.user_sessions.status` (the session-revocation mechanism) is never consulted by any
enforcement path — live-proved at `HDN-384`: revoking a session leaves it fully functional, while
`docs/runbooks/incident-response.md` documents session revocation as an effective containment
step. Kept High (no session gains access it should not have; the *removal* mechanism fails, not
an access grant), but not accepted as routine residual risk — a documentation actively claiming a
non-working control is a different, worse risk shape than an undisclosed gap.

Registered `RGL-BLK-010`, High, `OPEN`, not accepted as routine — full ruling:
`docs/build-log/release-go-live/BLOCKER_LEDGER.md`.

### 3.3 The other 15 items — conditionally re-accepted, with a new "tenant zero" gate

`HDN-BLK-017`/`018` (audit-tamper-evidence gaps), `022` (RLS/RPC gate gap on a ~33-table
remainder, reachable only by an unusual dual-role actor shape), `024` (MFA/IP-restriction wiring
gap, 61 functions), `027`–`034`/`036`–`038` (alerting/monitoring, DR/backup/restore, and
data-quality gaps).

All 5 §8.2 conditions applied explicitly (full text in `BLOCKER_LEDGER.md`'s own ruling, not
duplicated here): the compensating factor this checkpoint applies is that **production currently
holds zero real tenant rows** (`RGL-398`, re-confirmed `RGL-402`/`RGL-403`) — none of these 15
findings can cause real tenant harm today, because there is no real tenant data or user to harm.
This is a genuine, disclosed, evidence-based re-examination against the production bar (§8.2
condition 4), not a by-reference carry-over — and it is explicitly **time-limited**: a new
obligation is added, named the "tenant zero" gate, requiring every one of these 15 to be
re-examined before, or immediately upon, the first real tenant's data entering production.

### 3.4 Why this re-ruling does not change the top-line verdict

Even had all 17 items cleanly re-accepted, `RGL-BLK-001` alone still forces `NO_GO` (§8.1's
"never accepted... at any authority" is unconditional). This re-ruling was performed anyway
because Prompt 404's own charter and §8.2 condition 4 require it regardless of the top-line
outcome, and so a future `RGL-404` re-run — once `RGL-BLK-001` is actually fixed — does not have
to redo this analysis from nothing.

---

## 4. `RGL-BLK-007`/`RGL-BLK-008` — disposition

Both genuinely fixed in this candidate's own content (`RGL-401`, `RGL-402`), regression-tested,
not yet deployed anywhere. **`ACCEPTED` as fixed-not-yet-deployed** — not a §8.2 risk acceptance
of an unmitigated finding, since the underlying defect no longer exists in the code that would
ship. Their only remaining relevance to this verdict: `RGL-405` (whenever it eventually runs) must
deploy this exact branch, not merely whatever is on `main` today.

---

## 5. `GO_NO_GO_REPORT.md` — the primary decision artifact

Produced this checkpoint: `docs/build-log/release-go-live/GO_NO_GO_REPORT.md`. Covers release
scope, evidence status (all 14 completed lanes to date, one-line summary each), blocker status
(§3 above, restated formally), risk acceptance (the 15-item conditional acceptance only — nothing
else), deployment plan (none — `RGL-405` does not run under `NO_GO`), rollback plan (n/a, nothing
new deployed), and approvals (none sought or required for a `NO_GO`).

---

## 6. Verdict

**`NO_GO`.** Forced independently by two Critical findings, either alone sufficient:

1. `RGL-BLK-001` — ungated production auto-deploy, re-verified live this checkpoint, unchanged.
2. `RGL-BLK-009` — newly escalated, a live-forced financial mis-posting matching §8.1's own
   Critical definition verbatim.

Reinforced by, but not dependent on: `RGL-BLK-010` (session-revocation control inert), and the
three still-open tracked gaps (no staging tier, no UAT acceptor, no external pentest engagement).

---

## 7. What a `NO_GO` means for the remaining WBS rows — reasoned explicitly, not assumed

The execution index §5.1 states the dependency graph is *"strictly linear, `1 → 2 → … → 22`. No
parallelism is authorized anywhere in this range."* `RGL-405` (Production Deployment) is gated on
this checkpoint and does not become eligible. Every row after it is gated on its own immediate
predecessor, chained back to `RGL-405` — `RGL-406` has nothing to validate without a deployment;
`RGL-407` has no deployment to decide about rolling back; `RGL-408`/`409` presuppose a live release
event; `RGL-410`–`412` would be verifying, handing off, and closing a release that did not happen.
**Running any of `RGL-405`–`412` as if they could meaningfully execute would fabricate progress on
gated, dependency-blocked work.**

Correct disposition, stated in `GO_NO_GO_REPORT.md` §10 and restated here: **`RGL-405` through
`RGL-412` (8 remaining WBS rows) are `BLOCKED` on this checkpoint's own `NO_GO`** — not
`COMPLETED`, not skipped, not silently reinterpreted as "documentation checkpoints that can run
anyway." The operator's own "jalankan semua step 16" instruction named the full range; this
checkpoint does not unilaterally decide that instruction no longer applies — it reports, honestly,
that the range's own governing rules do not permit further execution while `RGL-BLK-001`/
`RGL-BLK-009` remain open, and escalates that fact rather than working around it (§9 of
`GO_NO_GO_REPORT.md`).

---

## 8. What was not attempted, and why

- **`RGL-BLK-001` was not fixed.** Infrastructure/governance configuration (branch protection,
  deployment-promotion gating) is outside this checkpoint's own bounded-evidence scope (Prompt 404
  §11/§12) and was never attempted; its own Owner field names `RGL-405` for the mechanism work,
  which itself does not run under `NO_GO`.
- **`RGL-BLK-009`/`010` were not fixed.** Both require real design decisions (a governed reversing-
  journal mechanism; wiring session-status into an enforcement path or correcting a runbook) —
  explicitly outside a documentation-and-evidence checkpoint's own scope, and forbidden as "new
  product features"/"critical-action automation expansion" under §12 regardless.
- **No production deployment, no customer-facing action, no external GA claim.** All explicitly
  forbidden by Prompt 404 §12 and moot under a `NO_GO` verdict regardless.
- **`RGL-405`–`412` were not executed.** See §7.

---

## 9. Fix pass, following an explicit operator instruction to run every available fix

After this report's first round reached `NO_GO`, the operator explicitly instructed: check every
un-verified prompt and run all available fixes so blockers can be closed. This checkpoint
attempted both open Critical findings:

**`RGL-BLK-009` — fixed and deployed live.** Designed and implemented a real fix mirroring this
codebase's own established `app.post_finance_correction` reversal technique (locate the original
GL journal via the settlement's own subledger batch, flip debit↔credit on its lines, post via
`app.create_and_post_finance_system_journal`). Found and fixed a second, previously undiscovered
live defect in the same function during implementation: it had been silently reverted from
`SECURITY DEFINER` to `SECURITY INVOKER` by a later, same-day Step 15 migration that recreated it
without restating the clause — making it completely unreachable by any real authenticated tenant
user, the identical bug class `RGL-397`/`RGL-BLK-006` already closed for 95 other functions.
Verified via a new db-test regression and a full local `db-tests` re-run (`ALL PASSED`), including
self-catching and correcting one defect in the fix's own first draft (a wall-clock-date dependency
that would have made the fix's own success depend on which fiscal period happens to be open on the
day it runs) and one downstream consequence (the `public.*` wrapper's own security mode needed the
identical fix, caught by this repository's own zero-tolerance wrapper-parity regression guard).
Applied to the hosted project directly via `apply_migration` — in effect immediately, not merely
committed to this branch. Migration: `supabase/migrations/20260826030000_harden_finance_
settlement_reversal_gl_journal_and_reachability.sql`.

**`RGL-BLK-001` — could not be fixed.** Searched this session's full tool surface for a way to
configure GitHub branch protection (no such tool exists anywhere in the GitHub MCP toolset made
available to this session) or a Vercel deployment-promotion gate (`update_project_deployment_
protection` exists, but only configures password/SSO/trusted-IP authentication-gating — applying
that to the production target would solve a different problem, and would break real customer
access to the product, the wrong fix for a governance/approval gap). This is a genuine tooling
limitation, not a judgment call or a scope decision — confirmed by direct search, not assumed.

**`RGL-BLK-010` — re-examined, found already substantially resolved.** While investigating a real
enforcement-wiring fix (which would have required either modifying `app.evaluate_permission`, the
single RBAC gate ~1,124 functions call transitively, or calling Supabase's Auth Admin API to
invalidate live JWTs — both judged too high-risk to attempt without dedicated testing time this
checkpoint's own remaining scope did not have), re-reading `docs/runbooks/incident-response.md`'s
own current content found the documentation-layer fix this finding originally demanded was
**already made at `HDN-384` Tier C**, before this Step 16 range began — this checkpoint's own
initial escalation had cited a stale, pre-Tier-C reading. Corrected in place: re-ruled to Medium,
folded into `RGL-BLK-003`'s own 15-item conditional-acceptance group.

**Net effect on the verdict.** `NO_GO` stands, now forced by `RGL-BLK-001` alone. One of two
Critical findings that forced it is genuinely closed; the other requires access this session's
own tooling does not have. Full updated accounting: `GO_NO_GO_REPORT.md` §3 (revised in place),
`BLOCKER_LEDGER.md`'s own `RGL-BLK-009`/`RGL-BLK-010` entries.

---

## 10. Operator override on `RGL-BLK-001`, same checkpoint, following §9's own escalation

§9 above reported `RGL-BLK-001` as the sole remaining reason for `NO_GO`, unfixable with this
session's tooling, and escalated it to the operator. The operator responded directly: *"yg auto
deploy gapapa ke production itu, gausa dijadiin blocking"* — the production auto-deploy risk is
acceptable, do not make it blocking — and separately asked whether anything else remained unfixed.

**This is recorded as an operator override, not an `RGL-404` ruling.** §8.2 condition 5 restricts
acceptance authority within this pipeline to `RGL-404`/`RGL-412`, and §8.1 states Critical findings
are never accepted "at any authority" — both are this pipeline's own self-imposed rules, binding
rulings issued inside it. They do not bind the actual operator, whose standing over the real
GitHub repository and Vercel project this finding concerns precedes this pipeline entirely. The
instruction is honored as exactly what it is — an out-of-band decision by the party with real
authority to make it — not reframed as if `RGL-404` itself had found a way to satisfy §8.2's five
conditions for a Critical item (it has not; §8.1 forecloses that regardless of how thorough the
analysis is).

**What changed and what did not.** `RGL-BLK-001`'s status moved from `OPEN, not accepted` to
`ACCEPTED (operator override)`. The underlying mechanism — `main` unprotected, Vercel production
target ungated, every push or merge to `main` still auto-deploying with no approval step — is
**unchanged**; this checkpoint did not gain any new tool or capability to fix it. Full record:
`BLOCKER_LEDGER.md`'s own `RGL-BLK-001` "OPERATOR RISK ACCEPTANCE" section;
`GO_NO_GO_REPORT.md`'s own second Verdict addendum and revised §3.1/§3.4/§7/§9/§10.

**Answering the operator's second question — is anything else unfixed?** Re-surveying every open
item in `BLOCKER_LEDGER.md` as of this checkpoint, honestly, not selectively:

- **Genuinely still open, real remediation work, not yet done** (`RGL-BLK-003`'s 15-item group):
  audit-tamper-evidence gaps (`HDN-BLK-017`/`018`), an RLS/RPC access-control gate gap on a
  ~33-table remainder (`HDN-BLK-022`), MFA/IP-restriction wiring on 61 functions (`HDN-BLK-024`),
  and a cluster of DR/backup/restore/monitoring/data-quality gaps (`HDN-BLK-027`–`034`,
  `036`–`038`). These are **conditionally risk-accepted for this release window only**, on the
  documented, time-limited basis that production currently holds zero real tenant rows — not
  fixed, and each carries a "tenant zero" obligation to be re-examined before, or immediately
  upon, the first real tenant's data entering production. Genuinely fixing all 15 is out of this
  checkpoint's scope (each is its own capability-sized remediation, and Step 16's own charter is
  bounded evidence-gathering, not new hardening work).
- **Fixed in this candidate's own content, but not yet live** (`RGL-BLK-007`, `RGL-BLK-008`): the
  `/api/v1/**` 500-on-invalid-key defect and the 3 webhook routes' 500-on-malformed-input defect
  are both genuinely fixed and regression-tested on this branch, but this candidate has not been
  deployed anywhere — they ship automatically the moment this branch reaches production, whichever
  mechanism does that.
- **Structurally unfixable by any agent** (§3.4's three tracked gaps, restated because they are
  now the only thing left forcing `NO_GO`): no staging tier exists; no UAT tier or named human
  acceptor exists and `UAT_ACCEPTED` cannot be set by any agent by the state table's own
  construction; no licensed external penetration-test engagement exists. None of these three was
  raised or addressed by the operator's `RGL-BLK-001` instruction.

**Net effect on the verdict.** Zero open Critical findings remain. The sole open High entry
(`RGL-BLK-003`) is an aggregate tracking record for items already individually ruled, not a fresh
unruled blocker. **`NO_GO` still stands**, now resting entirely on §3.4's three tracked gaps —
see `GO_NO_GO_REPORT.md`'s revised §3.4 and §10.

---

## 11. Operator override on the three tracked gaps; instruction to resolve the full historical issue backlog

Told plainly that Step 16's own blocker accounting no longer contained an unresolved Critical or
unruled High item, and that the three tracked gaps in §3.4 (no staging tier, no named UAT
acceptor, no licensed external penetration test) were now the sole reason `NO_GO` stood, the
operator was asked directly how to handle those three. The operator instructed they be accepted
**the same way as `RGL-BLK-001`** — an operator override, not an in-pipeline acceptance. In the
same turn, asked separately what else remained unfixed, the operator instructed: *"pokoknya
seluruh issue baik yg critical high medium low harus solved semua tanpa terkecuali"* — every
issue, whatever its severity, must be resolved, without exception — and, asked to clarify scope,
confirmed this means the **entire project's historical issue register**
(`docs/runtime/KNOWN_ISSUES.md`), not only this Step 16 range's own blocker set.

**Recorded honestly: this instruction cannot be honored literally for three specific items.**
Provisioning a real staging environment, obtaining a real human business acceptor's UAT sign-off,
and commissioning a real licensed external penetration-test engagement are not things any AI
coding agent can do — not a matter of effort, but of capability and authority (organizational,
financial, physical-world) no tool in this session's surface has or could have. This is why they
were surfaced as a distinct question rather than silently attempted or silently declared solved.
The operator's own answer — accept them, the same way as `RGL-BLK-001` — is recorded in
`BLOCKER_LEDGER.md` and `GO_NO_GO_REPORT.md` §3.4's own third addendum, honestly, as an accepted
risk, not a fabricated resolution.

**Effect on the verdict.** Mechanically, nothing in Step 16's own blocker accounting continues to
force `NO_GO`. This report deliberately does not flip to `GO_DECIDED` as a side effect — see
`GO_NO_GO_REPORT.md`'s own third Verdict addendum for the reasoning (a still-armed auto-deploy
mechanism, and the operator's own priority, stated in the same breath, being the historical backlog
rather than an immediate deploy). `RGL-405`–`412` remain `BLOCKED` pending an explicit, separate
operator instruction to proceed.

**The historical backlog itself — honest scope, not yet fully audited.** `docs/runtime/
KNOWN_ISSUES.md` is a large, append-only register spanning every phase of this project (Phase 0
through Step 15, `ISS-2026-001` through `ISS-2026-297`, 252 distinct entries as of this checkpoint).
A first-pass mechanical scan (regex over `Status \`...\`` markers) is **not reliable** for this
file: many entries carry a `Status \`OPEN\`` mention in their own *historical* narrative — the
state at the time of discovery — superseded later in the SAME entry by a `Status \`RESOLVED\`` (or
similar) paragraph once a fix landed, sometimes years' worth of build-log-equivalent history
compressed into one continuously-appended entry. A dumb "last regex match wins" pass produced
inconsistent counts across attempts (127, 130, 131) precisely because format varies entry to entry
(inline `Status \`X\`, Severity severity` phrasing vs. a header-line parenthetical vs. neither).
**The true current-status count requires an entry-by-entry read, not a scripted count** — that
read is the next step, not yet performed as of this section. A rough, honestly-labeled estimate
from the unreliable scan: on the order of **125-135 entries currently `OPEN`**, spanning Low
through Critical, the great majority pre-dating Step 16 and already carrying a named future owner
from whatever phase originally found them (a normal, deliberate backlog — not evidence those
phases were wrongly marked `VERIFIED`; each was individually judged out of that phase's own bounded
charter when found, per this project's own consistent "log unrelated/pre-existing failures, do not
fix them inside an unrelated task" discipline, `AGENTS.md`).

**Plan.** Given the scale, this is executed as a distinct, ongoing body of work, not a single
checkpoint: (1) produce an accurate, entry-by-entry inventory (ID, true current status, severity,
one-line statement, current owner) rather than trust the mechanical scan; (2) work through
genuinely open entries in severity order (Critical first, then High, Medium, Low), applying this
project's own established per-item discipline (investigate root cause, implement a real fix,
regression-test it, verify via a fresh local `db-tests`/gate run, document, commit) rather than
batch-closing entries; (3) report progress incrementally rather than claiming completion before it
is real. This will span many commits over an extended effort, not one. Full record of progress:
future dated sections in this file and dedicated entries in `docs/runtime/CHANGE_MANIFEST.md`.

---

## 12. Historical-issue-backlog remediation — running progress log

Per §11's plan. An entry-by-entry audit of `docs/runtime/KNOWN_ISSUES.md` (a careful
agent read, not a regex scan — the mechanical scan proved unreliable, per §11) found
**168 genuinely `OPEN` entries**: 0 Critical, 16 High, 78 Medium, 74 Low, out of 297 total
tracked entries. This section is a running log of what has actually been fixed, updated as
work proceeds — kept here rather than duplicated into `BLOCKER_LEDGER.md`/`KNOWN_ISSUES.md`
in full, both of which carry the authoritative per-issue detail and are linked from each
row below.

| # | ID | Statement (one line) | Fix | Verification | Status |
|---|---|---|---|---|---|
| 1 | `ISS-2026-267`/`HDN-BLK-036` | No mutual-exclusion mechanism for the composed in-place restore procedure | `docs/runbooks/database-restore.md` §4 item 4: mandatory `pg_try_advisory_lock(872314, 1)` step (0)/(i), no new schema object, auto-releases on disconnect | New `scripts/db-tests/database-restore-lock.sql`, two real concurrent `psql` processes, full suite `ALL PASSED` | `RESOLVED` |
| 2 | `ISS-2026-072` | `app.evaluate_permission` never re-checks `app.users.status` (the still-open half; role_assignments/tenant-membership halves were already fixed at HRT-295/HDN-373) | `supabase/migrations/20260826040000_harden_rbac_evaluator_platform_user_status_check.sql` — one new defense-in-depth branch, placed after the Supreme Admin exception | New regression in `scripts/db-tests/rbac-enforcement.sql` (raw out-of-band status flip denied, Supreme Admin unaffected), full suite `ALL PASSED`, applied live, `pg_get_functiondef` re-confirmed on the hosted project | `RESOLVED` |
| 3 | `ISS-2026-257` | 3 plaintext secret columns (`credential_value`, `webhook_secret_value`, `secret_value`) captured verbatim by a full `pg_dump`/`pg_restore`, no encryption-at-rest | `supabase/migrations/20260826050000_harden_integration_secrets_encryption_at_rest.sql` — extends the already-proven vendor-financial `pgp_sym_encrypt`/`pgp_sym_decrypt` pattern; 6 writers + 11 readers redefined via `CREATE OR REPLACE`, zero call-site changes | Full suite `ALL PASSED` (2 regression tests updated to decrypt-and-compare); applied live (0 existing rows, backfill a no-op); `information_schema.columns` re-confirmed | `RESOLVED` |
| 4 | `ISS-2026-156` (bonus, found while verifying #3) | `scripts/db-tests/n8n-integration.sql`'s own webhook-endpoint lookup had no `ORDER BY`/status filter — item #3's own `ALTER TABLE` row rewrite flipped it from "happened to work" to a real, reproducing failure | Query now filters `status = 'active' order by created_at limit 1` — the semantically correct fix, not merely a stable tie-break | Full suite `ALL PASSED` after the fix; this entry's own prior "confirmed non-reproducing in the real suite" claim corrected — it held only for the specific trigger it tested (file execution order), not for a schema-level physical row-order change | `RESOLVED` |
| 5 | `ISS-2026-265`/`HDN-BLK-034` | The in-place restore procedure's own `TRUNCATE` step bypasses 9 security/integrity triggers with zero audit trail | `supabase/migrations/20260826060000_harden_database_restore_audit_trail.sql` — new `app.record_database_restore_event(...)`, a mandatory runbook step (j) writing one explicit `app.audit_logs` row; closes only the "zero audit trail" half, `TRUNCATE`-bypasses-triggers itself is not fixable at this layer, disclosed inline | New regression in `database-restore-lock.sql` (real persisted row + 3 rejection cases); Option 2 wrapper caught missing by the repo's own zero-tolerance guard, added before the freeze digest changed; full suite `ALL PASSED`; applied live | `RESOLVED (partial by design — disclosed)` |
| 6 | `ISS-2026-269` | Auto-generated-employee-number import rows have zero duplicate detection on a fresh re-import — a live-reproduced real duplicate person record | `supabase/migrations/20260826070000_harden_employee_import_duplicate_detection.sql` — flags a `pending` `app.employee_duplicate_candidates` row on an exact `work_email`/`full_name` match, the same definition the original reproduction used; never a hard block; deliberately NOT routed through `app.flag_employee_duplicate_candidate` (would have required every importer to also hold `HRS:Edit`) | New regression in `hris-employee-master.sql` (reproduces the original scenario, now flagged; confirms a distinct new employee is NOT flagged); full suite `ALL PASSED` (first attempt caught a `min(uuid)` bug in the new test itself, fixed before applying); applied live | `RESOLVED` |
| 7 | `ISS-2026-254` | A restore predating an active legal hold/revoked API key/disabled webhook/suspended user or membership silently reverts that decision, no compensating control | `supabase/migrations/20260826080000_harden_restore_security_state_reconciliation.sql` — `app.capture_security_state_snapshot`/`app.detect_reverted_security_state`, a voluntary pre/post-restore compensating control (does not close the "snapshot never taken" case — no mechanism forces the step) | New regression in `database-restore-lock.sql` (snapshot correctly captures all 5 categories, detection correctly reports all 5 after a simulated revert under `session_replication_role = replica`, a same-point-in-time snapshot reports nothing, unknown snapshot id rejected); full suite `ALL PASSED`; applied live; wrapper grants live-verified | `RESOLVED (partial by design — disclosed)` |
| 8 | `ISS-2026-263` | `app.transition_user_status`'s own `event_type` `CASE` mapping falls through to the raw target-status value for any transition outside 5 explicit pairs, including any no-op — deterministically fails with a spurious `CHECK` violation | `supabase/migrations/20260826100000_harden_user_status_transition_invalid_event_type.sql` — `else` branch changed to `null`, new explicit `invalid_status_transition` rejection before the history insert; deliberately did NOT widen the `CASE` to accept every combination as a synthetic event type | New regression in `user-lifecycle.sql` (all 5 real transitions still succeed with the correct `event_type`; 3 no-op/unrecognized calls, incl. the exact `suspended→suspended` scenario `HDN-384` reproduced, now rejected with a clear error); full suite `ALL PASSED` first attempt; applied live; `pg_get_functiondef` re-confirmed | `RESOLVED` |
| 9 | `ISS-2026-264` | `app.user_sessions.status` (the session-revocation flag) was never consulted by `app.evaluate_permission` or anything else — zero real enforcement effect; deeper investigation found `app.register_user_session` was also never called from the real app at all, so a database-only fix would be a structural no-op | `supabase/migrations/20260826110000_harden_evaluate_permission_session_revocation_enforcement.sql` — new narrow `all_sessions_revoked` check (only when ≥1 tracked session exists and all are revoked); paired with `lib/auth/register-login-session.ts`/`-deps.server.ts` wired into the real sign-in Server Action, best-effort, never blocking login | New regression in `rbac-enforcement.sql` (untracked actor unaffected, active session allowed, all-revoked denied, fresh session restores, Supreme Admin unaffected); 5 new unit tests; full JS suite 5468/5468; full db-tests suite `ALL PASSED`; applied live; `pg_get_functiondef` re-confirmed. Browser-driven login verification not attempted — no `dev` script/local Supabase Auth stack reachable in this sandbox, disclosed as the same class of gap as `ISS-2026-255`/`ISS-2026-244` | `RESOLVED` |
| 10 | `ISS-2026-266` | The restore procedure's own step (h) was a raw, per-view-name `REFRESH MATERIALIZED VIEW CONCURRENTLY` instruction, bypassing the already-existing governed `app.refresh_analytics_view` (IAE-005) entirely — no ledger row, no automatic coverage of future views | `supabase/migrations/20260826120000_harden_restore_materialized_view_refresh_completeness.sql` — `app.refresh_all_registered_analytics_views` delegates to `app.refresh_analytics_view` for every `active` registered view in one governed call; runbook step (h) updated to call it | New regression in `analytics-materialized-views.sql` (Supreme-only, refreshes every active view, surfaces a per-view failure without aborting the batch, identical persisted ledger rows); full suite `ALL PASSED` first attempt; applied live; wrapper grants live-verified correct from first principles | `RESOLVED` |
| 11 | `ISS-2026-270` | No safe import/registration path exists for migration-seeded reference tables (`app.finance_currencies`, `app.uoms`) — a raw insert collision rolls back an entire batch, including genuinely-new rows in the same statement | `supabase/migrations/20260826130000_create_reference_data_import_registration.sql` — `app.import_reference_currency`/`app.import_reference_uom`, idempotent "return existing row if found" pattern (not `ON CONFLICT DO NOTHING`, which would mask a real data conflict), Supreme Admin only | New `reference-data-import.sql` (Supreme-only, idempotent re-import returns the existing row, a genuinely-new code survives alongside a colliding sibling call, CHECK constraint still enforced, real audit event recorded); full suite `ALL PASSED` first attempt; applied live; wrapper grants live-verified correct from first principles | `RESOLVED` |
| 12 | `ISS-2026-272` | No mechanism tracks "migration rehearsals completed" for enterprise tenants — the checklist's own item allow-list is closed, requiring a schema migration to extend | `supabase/migrations/20260826140000_create_migration_rehearsal_tracking.sql` — `app.migration_rehearsal_tests`/`app.record_migration_rehearsal_test` mirroring `app.dr_restore_tests`'s own honesty discipline; a 7th checklist item, `migration_rehearsal_verified`, computed live but deliberately NOT wired into the `status='ready_for_production'` gate (business rule is "where contracted", no contract-flag exists) — confirmed with operator via `AskUserQuestion` | New regression in `disaster-recovery-enterprise-support.sql` (authority/validation discipline, >=2-passed-rehearsals computation, and direct proof that flipping the item never changes `status`); full suite `ALL PASSED` first attempt; applied live; wrapper grants + function body live-verified | `RESOLVED (partial by design — disclosed)` |
| 13 | `ISS-2026-271` | No `rollback_import_job` RPC exists anywhere; a manual raw-SQL delete leaves a dangling `audit_logs` reference and an untouched employee-number counter, and the downstream-reference risk was never exercised | `supabase/migrations/20260826150000_create_employee_import_rollback.sql` — `app.rollback_employee_import_job`: job row moves to `status='rolled_back'` (never deleted, keeping audit references resolvable); counter deliberately untouched (never reused, by design); downstream-reference safety relies on Postgres's own default `RESTRICT` FK behavior, not a hand-maintained table list | New regression in `hris-employee-master.sql` (`HRS:Import`-gated; rejects non-completed/blank-reason/re-rollback; a real downstream reference via `employee_duplicate_candidates` blocks rollback atomically, both employees survive intact, succeeds once cleared); full suite `ALL PASSED` first attempt; applied live; wrapper grants live-verified | `RESOLVED` |
| 14 | `ISS-2026-275`/`ISS-2026-276` | `app.finance_journals_protect_posted` never fires on `INSERT` (only `UPDATE`/`DELETE`), so a direct bulk insert of an already-posted journal is unguarded, unbalanced-checked, and has no legitimate `source_type` to record its true provenance | `supabase/migrations/20260826160000_create_finance_journal_historical_import.sql` — new `app.import_historical_finance_journal` (modeled on `app.create_and_post_finance_system_journal`, but itself `FIN:Approve`-gated, re-validates balance, requires a real `source_id`/reason, deliberately skips the `posting_eligible` open-period check for migration data only — confirmed with operator via `AskUserQuestion`); `finance_journals_source_type_check`/`finance_journals_source_check` widened to add `'migration'` (preserving `'correction'` byte-for-byte) | New regression in `finance-journal.sql` (authority/source_id/reason/balance rejections, a real journal posts into an already-CLOSED isolated historical period — the central point of this fix — idempotent retry, audit event, and a defense-in-depth proof that the table-level CHECK constraint itself still rejects a sourceless migration row); full suite `ALL PASSED` after 4 authoring mistakes caught and fixed by this same local run; applied live; wrapper grants + security-definer mode live-verified | `RESOLVED` |
| 15 | `ISS-2026-279` | `app.employee_number` uniqueness on an explicit staged value is case/whitespace-sensitive — `EMP-001`, `emp-001`, `EMP-001 ` (trailing space) all commit as 3 distinct employees in one job | `supabase/migrations/20260826170000_harden_employee_import_number_normalization_detection.sql` — widened `app.commit_employee_import_job` a third time: an explicitly-numbered row whose number normalizes (`lower`+`trim`) to an existing employee's own number, without being byte-identical, is flagged into the existing `app.employee_duplicate_candidates` table, mirroring `ISS-2026-269`'s own already-shipped soft-flag answer for the identical risk class — a hard functional unique index was deliberately rejected (risk of breaking existing live data/legitimate future rows) | New regression in `hris-employee-master.sql` (the entry's own exact 3-variant reproduction all commit successfully and are flagged pairwise; a genuinely unrelated number is not flagged); full suite `ALL PASSED` first attempt; applied live; function body live-verified via `pg_get_functiondef` | `RESOLVED` |
| 16 | `ISS-2026-260` | `app.dr_restore_tests.component_scope`'s own component/mechanism enum has no slot for 3 of the 4 DR scenarios this repository's own charter names | `supabase/migrations/20260826180000_create_dr_restore_scenario_taxonomy.sql` — new parallel, nullable `dr_scenario` column (confirmed with operator via `AskUserQuestion` over widening `component_scope` itself); `app.record_dr_restore_test` widened with a new trailing default `p_dr_scenario` param — self-caught mid-authoring: `CREATE OR REPLACE` cannot append a parameter without creating an ambiguous second overload, fixed by explicit `DROP FUNCTION` first (both `app.*` and its `public.*` wrapper), matching this repo's own already-established FIN-206 convention | New regression in `disaster-recovery-enterprise-support.sql` (all 4 scenarios recordable, invalid value rejected at RPC + table CHECK layer, every pre-existing 13-arg call site unaffected); full suite `ALL PASSED` after the overload ambiguity was caught and fixed by this same local run; applied live; single-overload + grants live-verified | `RESOLVED` |
| 17 | `ISS-2026-268` | `app.files` went unexercised in the DR/backup drill's seeded slice across 2 consecutive checkpoints, disclosed inline both times but never tracked or closed | New, permanent, re-runnable drill script `scripts/db-tests/database-restore-files-drill.sh` — a real `pg_dump -Fc`/`DROP`/`CREATE`/`pg_restore -j 4` cycle (item 1's own already-proven procedure) against a freshly seeded database carrying a real `app.files` row (genuine `initiate_file_upload`/`record_file_scan_result`, not a raw insert); no schema change, no migration | Live-executed 2 independent runs: file-metadata row survives byte-for-byte alongside the already-proven jobs row (≈16.27s / ≈19.44s total, 0 errors); `docs/runbooks/database-restore.md` §4/§7/§8 updated with real evidence and a new dated rehearsal-history row | `RESOLVED` |
| 18 | `ISS-2026-278` | No MFA/step-up/IP-allowlist gate exists on any import-commit RPC, unlike the 4 functions HDN-378 already hardened for the identical risk class | `supabase/migrations/20260826190000_harden_import_commit_ip_allowlist_gating.sql` — widened all 5 real `commit_*_import_job` functions (plus their `public.*` wrappers) with a new trailing `p_client_ip` param, the same IP-allowlist + bypass-grant composition HDN-378 established — confirmed with operator via `AskUserQuestion`: IP-allowlist only, no mandatory MFA step-up (over-restriction risk on routine imports, explicitly flagged by the entry itself); every widen done via explicit `DROP`+`CREATE`, never bare `CREATE OR REPLACE` (`ISS-2026-260`'s own finding applied correctly from the first draft) | Full 4-scenario regression for the generic framework function plus a lighter 3-scenario proof for each of the 4 domain adapters, across 5 db-tests files; full suite `ALL PASSED` first attempt; applied live; grants + single-overload correctness live-verified for all 10 functions | `RESOLVED` |
| 19 | `ISS-2026-295` | All 9 `app/api/v1/**` routes returned an uncaught `500` for an invalid Bearer key instead of a clean `401` — fixed in code at `RGL-401`, deployment withheld by that prompt's own no-deploy rule | No new fix — the code fix already shipped on this branch at `RGL-401`. This item closes on **deployment**, not code | Track A release: branch merged to `main` (PR #69), Vercel deployed (commit `c11c616`); live-verified directly against `https://cargogrid-app.vercel.app/api/v1/status` with an invalid key — clean `401`, not `500` | `RESOLVED (deployed)` |
| 20 | `ISS-2026-296` | All 3 unauthenticated webhook ingestion routes returned an uncaught `500` on a malformed `connectionId` instead of a clean `400` — fixed in code at `RGL-402`, deployment withheld by that prompt's own no-deploy rule | No new fix — the code fix already shipped on this branch at `RGL-402`. This item closes on **deployment**, not code | Track A release: same deploy as item 19; live-verified directly against `https://cargogrid-app.vercel.app/api/webhooks/third-party-gps/<malformed>` — clean `400 {"ingestStatus":"invalid"}`, not `500` | `RESOLVED (deployed)` |
| 21 | `ISS-2026-285` | `app._calc_vendor_kpi_rate_validity` returns not-computable for sub-24h windows — already fixed at `RGL-394` (`RGL-BLK-004`), but this specific entry was never annotated `RESOLVED` (its 2 duplicate registrations, `ISS-2026-059`/`204`, correctly were) | No new fix — documentation-only correction, found by a Track B Batch 1 research agent investigating the item as if still open | Confirmed live against the actual migration (`20260826020000_harden_vendor_kpi_rate_validity_window_calc.sql`) and its regression coverage in `procurement-vendor-performance.sql`; annotated `RESOLVED` with full cross-reference | `RESOLVED (doc-only, already fixed)` |
| 22 | `ISS-2026-249` | 3 real failure producers (webhook-delivery-replay divergence, `IAE-008` integration health-check auto-disable, AI governed-action failure) never called `app.raise_observability_alert` | `supabase/migrations/20260827000000_wire_observability_alert_producers.sql` — 3 `CREATE OR REPLACE FUNCTION` parts on unchanged signatures, one new alert call each, placed before that branch's own normal `return`. A 4th part (`app.assert_ip_allowed`, a security-denial slice) was drafted, applied locally, and withdrawn before being applied live: its own denial mechanism is `raise exception`, which every real caller catches — PL/pgSQL's implicit savepoint at the caller's own catching block rolls back the alert INSERT the callee already made, so the alert would silently never persist. Caught by the local `db-tests` suite itself, not assumed correct from a design read. Genuinely needs an autonomous-transaction mechanism or a design change (alert call moved to the caller), not a same-signature body edit — left explicitly open, alongside `app.evaluate_permission`'s general RBAC-denial path and `app.assert_current_step_up_authorization` (MFA step-up), both also confirmed NOT bounded fixes | New regression in each of the 3 closed producers' own db-test file (`webhook-management.sql`, `integration-hub.sql`, `ai-governance-provider-boundary.sql`) proving a real `app.incidents` row now exists at each producer's own already-proven terminal-failure moment; full suite `ALL PASSED` after the Part D withdrawal; applied live; grants unaffected (composition through an already-`service_role`-granted `SECURITY DEFINER` caller) | `RESOLVED (partial by design — disclosed)` |
| 23 | `ISS-2026-167` | Cross-tenant existence oracle via verbatim error text, ~41 sites/15 code families across the repo | `supabase/migrations/20260827010000_harden_cross_tenant_error_disclosure_representative.sql` — 3 representative sites (`app.create_quotation_draft`, this entry's own cited reproduction, plus `app.revoke_api_key`/`app.rotate_api_key`, the api-keys surface). Self-caught mid-authoring by a local `db-tests` failure: a naive full collapse of "not found or unauthorized" into one generic error also swallowed a legitimate, non-leaky, same-tenant-member-without-authority case — corrected to discriminate via `app.has_active_tenant_membership` before choosing which error to raise | New regression in `commercial-quotation-builder.sql` (cross-tenant vs. fully-nonexistent opportunity raise the byte-identical error) and `api-key-webhook.sql` (same proof for both key functions); full suite `ALL PASSED` after the mid-authoring fix; applied live | `RESOLVED (partial by design — disclosed, 3 of ~41 sites, remaining ~38 unchanged owner HDN-376)` |
| 24 | `ISS-2026-174` | `app.analytics_refresh_runs` discloses platform-wide row counts and a named admin's real identity to any authenticated user of any tenant | `supabase/migrations/20260827030000_harden_analytics_refresh_runs_grant.sql` — `authenticated`'s table-level grant narrowed to a column list keeping only `row_count_after` (the existing, legitimate freshness-badge UI feature) and dropping `row_count_before`/`triggered_by_auth_user_id`/`triggered_by_label`; no application code change needed | New regression in `analytics-materialized-views.sql` proving the 3 dropped columns carry zero `authenticated` privilege, the 8 kept ones remain readable, `service_role` unaffected; full suite `ALL PASSED`; applied live | `RESOLVED` |
| 25 | `ISS-2026-175` | `FlagCache`'s cache key omits tenant/environment/cohorts — a shared instance would serve one tenant's cached flag decision to another | `scripts/feature-flags/flags.ts` — new `cacheKey()` composing `[flagId, tenantId, environment, sorted cohorts]`, mirroring the already-correct sibling `FeatureFlagCache`; `get`/`set`/`invalidate` widened to take a context alongside `flagId` | New regression in `flags.test.ts` (2 tenants for the same flag no longer share an entry, invalidate is tenant-scoped, 2 environments don't collide); `pnpm run test` 34/34 in this file; TS-only, no migration | `RESOLVED` |
| 26 | `ISS-2026-176` | 35 of 37 `app`-schema views granted `SELECT` to `authenticated` run in owner mode (no `security_invoker`) — correct today, but structurally fragile against a future addition | `scripts/db-tests/rbac-enforcement.sql` — a new regression guard (not a schema change): fails if a future `authenticated`-granted, non-invoker view appears outside a reviewed 35-name allow-list, or if any of the 35 drops off that live set (catching allow-list drift too). Independently re-derived the 37-view/2-invoker/35-target count from scratch rather than trusting the entry's own figure | Full suite `ALL PASSED`, confirming zero unreviewed views exist today; no migration, no schema change | `RESOLVED` |
| 27 | `ISS-2026-013` | `app.commit_import_job` (`PLT-131`) never writes to any canonical destination table — the generic Import/Export Job Framework had no entity-write adapter for any domain, per the entry's own text | No new fix — documentation-only correction, found the same way `ISS-2026-285` (item 21) was: a Track B Batch 2 research agent investigating the item as if still open | `app.commit_vendor_rate_import_job` (PRC-255, `20260730620000_extend_commercial_vendor_rate_for_procurement.sql`) is a real, tested domain-write adapter — its own `comment on function` cites "PLT-131 (ISS-2026-013)" verbatim, exercised end-to-end in `procurement-vendor-rate-tiers.sql`; confirmed live before annotating `RESOLVED` | `RESOLVED (doc-only, already fixed)` |
| 28 | `ISS-2026-036` | Repository-wide WCAG AA color-contrast violation on `text-neutral-600` (`#737373` on `#eaf0f6`, 4.13:1, required 4.5:1) | No new fix — documentation-only correction. The cited pairing was never `--color-neutral-600`; it was `--color-neutral-500`'s pre-fix value, already remediated by `CG-S15-HDN-012` (`HDN-380.md` §6) before this entry was ever reconciled | Confirmed live against `app/globals.css` (`--color-neutral-500: #6c6c6c`, 4.57:1; `--color-neutral-600: #525252`, unmodified stock, 6.82:1) and `HDN-380.md`'s own before/after table; independently recomputed via the WCAG relative-luminance formula rather than trusted at face value | `RESOLVED (doc-only, already fixed)` |
| 29 | `ISS-2026-044` | `app.request_approval`'s own `(tenant_id, idempotency_key)` idempotency check is unlocked SELECT-then-INSERT with no `unique_violation` handler — a genuinely concurrent double-call crashes the loser with a raw constraint-violation error | `supabase/migrations/20260827110000_harden_request_approval_unique_violation_handler.sql` — the INSERT wrapped in `begin ... exception when unique_violation then select existing and return ... end`, mirroring this repository's own established `app.convert_quotation_to_account` pattern. Drafted against the correct LATEST body (`20260730390000`'s own `idempotency_key_conflict` widening) after an earlier draft based on the stale original was caught before being applied anywhere | Sequential-proxy regression in `approval.sql` (the literal race window needs genuine concurrent sessions this harness cannot create — the same limitation this repository's own `batch4-tier-c-review-fixes.sql` Fix 13 already accepted for the identical function); full suite `ALL PASSED`; applied live | `RESOLVED` |
| 30 | `ISS-2026-043` / `ISS-2026-048` | Repository-wide by-id read/write RPC tenant-id-disclosure oracle (~334 candidate sites by a heuristic scan) — 043 (reads) and 048 (writes) already fixed 13 and 32 sites respectively in their own originating batches | `supabase/migrations/20260827130000_harden_tenant_disclosure_representative_extension_batch2.sql` — 4 more read-RPC sites (`app.get_sourcing_request`, `app.select_vendor_rate`, `app.get_wms_inbound_order`, `app.reveal_vendor_bank_account_number`) and 2 more write-RPC sites (`app.cancel_approval_request`, `app.approve_finance_invoice`) fixed with the same established `has_active_tenant_membership`-folded-into-not-found pattern — 2 of the 6 (`get_sourcing_request`, `cancel_approval_request`) were themselves missed by the SAME migrations that fixed their own sibling functions, concrete proof the class needs a second pass | New/updated regressions across `procurement-sourcing.sql`, `procurement-vendor-rate-tiers.sql`, `advanced-tms-wms-inbound.sql` (one pre-existing test corrected from its pre-fix `insufficient_authority` expectation), `procurement-vendor-financial-security.sql`, `finance-invoice.sql`, `approval.sql`; full suite `ALL PASSED`; applied live | `RESOLVED (partial by design — disclosed, 17/34 of ~334 candidate sites respectively)` |
| 31 | `ISS-2026-049` (second half) | The shared `app.decide_approval_step` echoes the request's real `tenant_id` into its own `insufficient_authority` error — the same C-05 shape as 043/048, inherited from shared Platform Approval Engine code | Same migration as item 30 — `app.decide_approval_step` now folds `app.check_approval_request_authority` into the same `approval_step_not_found` a nonexistent step id produces, mirroring the identical fix already independently applied one checkpoint earlier at the wrapper level (`app.decide_automation_rule_publish_approval`, IAE-007) | New regression in `approval.sql` (a genuine stranger gets `approval_step_not_found`; a real tenant member merely ineligible to decide still gets the specific `insufficient_authority`, not a leak); full suite `ALL PASSED`; applied live. This item's own **first half** (entity_type checked before authority in 5 `decide_*_approval_step` wrappers) is unrelated and stays open — the review's own explicit "disclose, not partially fix" choice, unchanged | `RESOLVED (partial — second half only; first half stays OPEN, disclosed)` |
| 32 | `ISS-2026-092` | `app.employee_change_requests.reason`/`decided_reason` readable via a raw table SELECT by any active tenant member — the identical shape as this checkpoint's own Finding A, self-found, never named by the original audit | No new fix — documentation-only correction, the same `ISS-2026-285`/`013`/`036` class found a fourth time: a LATER entry in the same file, `ISS-2026-099`, had already fixed this exact shape (`app.get_employee_change_requests`, a new masked RPC; raw grant on the table excludes `reason`/`decided_reason`; `page.tsx` updated to call the RPC) but never cross-referenced back to `092`'s own row | `ISS-2026-099`'s own text explicitly directs "`ISS-2026-092`'s own text remains unchanged, per this file's append-only discipline; this entry supersedes its disposition" — so its `###` section is deliberately NOT edited in place; this row plus `BACKLOG_INVENTORY.md`'s own "Batch 3 status" section is the closure record instead | `RESOLVED (doc-only, already fixed, not annotated in place per ISS-2026-099's own directive)` |
| 33 | `ISS-2026-063` | Procurement dashboard query-budget mechanism had no dedicated test (unlike every sibling dashboard query module); separately, `non_po`/`is_partial_invoice`/`is_consolidated_invoice` match-mode paths in `app.create_vendor_bill_match_case` were real but never exercised by any live test; a third, unrelated sub-item (large-scale load proof covers only 4 of ~9 named surfaces) is out of scope | `server/queries/procurement-dashboard.test.ts` created (mirrors `finance-dashboard.test.ts`'s own pattern — `recordingClient` stub, RPC-arg-mapping/error-wrap/non-array-reject/timeout assertions), 31 tests; `scripts/db-tests/procurement-vendor-invoice-matching.sql` gained one new block: a vendor2 (no active contract, no PO) case with both `is_partial_invoice=true`/`is_consolidated_invoice=true`, asserting `match_mode='non_po'` and both flags genuinely stored — deliberately kept off vendor1 to avoid perturbing the fixed 2-case denominator the file's own `invoice_accuracy` KPI aggregate test depends on (caught live: an earlier draft using a vendor1 case broke that aggregate, corrected before commit) | `pnpm run test` 5502/5502 (TS); full `db-tests` suite `ALL PASSED`; no migration, no schema change, nothing applied live. The load-proof sub-item is NOT fixed — extending `scripts/load-tests/` to the remaining 5 surfaces is materially larger than a bounded test addition | `RESOLVED (partial — 2 of 3 sub-items; load-proof sub-item stays OPEN, disclosed)` |
| 34 | `ISS-2026-115` | `scripts/db-tests/hris-overtime-timesheet.sql` hardcodes a `'weekday'` classification assertion against a `current_date`-pinned fixture, failing on Saturday/Sunday runs — this entry's own text still said `OPEN` | No new fix — documentation-only correction, the same `ISS-2026-285`/`013`/`036`/`092` class found a fifth time: already fixed by commit `cdbccc7` ("test(db): mirror Supabase's session search_path, and de-flake the overtime fixture", 2026-08-23), which pins `work_date` to the nearest weekday, never reconciled | Confirmed live by direct read of the current file before annotating `RESOLVED` | `RESOLVED (doc-only, already fixed)` |
| 35 | `ISS-2026-116` | `app.accept_customer_portal_invite`/`app.set_customer_portal_account_membership_status` each raised a distinguishable `insufficient_authority` for a real-but-wrong-actor `p_membership_id` vs. `customer_portal_membership_not_found` for a nonexistent one — an anti-enumeration oracle deviating from the ATW-023 precedent of one identical error regardless of true cause | `supabase/migrations/20260828030000_harden_customer_portal_membership_anti_enumeration.sql` — verbatim current bodies (verified against the true latest, `20260801260000`), with only the wrong-actor raise in each collapsed into the identical not-found shape the entry's own text already recommended | New regression in `customer-portal-scope.sql` (both branches now byte-identical in message text); full suite `ALL PASSED`; applied live | `RESOLVED` |
| 36 | `ISS-2026-117` | Every actor-taking function in ATW-023's customer-inventory-access migration (10 functions) never called `app.assert_actor_is_session_identity` on its own actor parameter — an actor-identity-forgery gap this migration predated (ATW-031 postdates it) | `supabase/migrations/20260828040000_harden_advanced_tms_customer_inventory_access_actor_identity.sql` — all 10 functions widened via `CREATE OR REPLACE` (same signatures, no grant changes), each now calling the assert first. **Escalated in severity during verification**: `20260826000000` (RGL-394, landed 2026-08-26 after this entry was written) had granted `public.*` `EXECUTE` to `authenticated` for all 10, making the gap directly reachable, not merely theoretical | New forged-actor regression for all 10 in `advanced-tms-customer-inventory-access.sql`; full suite `ALL PASSED`; applied live; grants live-verified unaffected | `RESOLVED` |
| 37 | `ISS-2026-124` | `app.get_customer_portal_invoice`/`app.list_customer_portal_invoices` omit `customer_account_id` from their own projection, the one outlier among sibling Phase 8 customer-portal read RPCs | `supabase/migrations/20260827140000_harden_customer_portal_invoice_account_id_projection_iss2026124.sql` — `DROP`+`CREATE` (required for a `RETURNS TABLE` column-list change) on both functions plus their `public.*` wrappers (a dependency added 2026-08-26, after this entry was written, widened in lockstep or every call would mismatch and fail) | Live-verified both `app.*` and `public.*` functions now project `customer_account_id`; full suite `ALL PASSED`; applied live. TS-contract/UI-layer half explicitly NOT done — outside this batch's DB-layer mandate, disclosed | `RESOLVED (partial — DB layer only; TS-contract/UI layer stays OPEN, disclosed)` |
| 38 | `ISS-2026-128` (item 2) | Points Ledger's lot-expiry window is a bare caller-supplied `p_expiry_days` parameter, no persisted per-program config, unlike CPL-317's own `review_period_days` precedent | `supabase/migrations/20260828000000_create_loyalty_point_program_expiry_config.sql` — new additive `app.loyalty_point_program_configs` table + `set_`/`get_` RPCs, `app.post_loyalty_points_earned` widened via `CREATE OR REPLACE` with an IDENTICAL signature (only the `p_expiry_days` default changes from `365` to `NULL`, a no-op for every existing caller). Self-caught twice mid-drafting: an ordering regression (validation deferred past the idempotency short-circuit, silently skipping it on replay) broke a pre-existing test, fixed by validating an explicit override unconditionally up front; and a missing `public.*` wrapper pair, caught by `public-api-wrapper-regression.sql` | New regression block in `customer-loyalty-points-ledger.sql`; full suite `ALL PASSED` after both self-caught fixes; applied live; grants live-verified (`anon` denied, `authenticated` allowed on both `app.*` and `public.*`) | `RESOLVED (partial — item 2 only; item 1 stays OPEN, same job-scheduler class as ISS-2026-126)` |
| 39 | `ISS-2026-133` (item 1) | Self-approval was not structurally blocked between `app.open_loyalty_fraud_review_case` and `app.decide_loyalty_fraud_review_case` — the same staff member could open a fraud review case (applying a provisional account hold) and then decide it themselves | `supabase/migrations/20260828050000_harden_customer_portal_loyalty_fraud_review_case_self_approval.sql` — a new nullable `opened_by_auth_user_id` column (captured going forward only, not retroactive, disclosed) plus the identical `self_approval_not_allowed` convention already established repository-wide (`app.decide_loyalty_point_adjustment`, `app.decide_vendor_assessment_review`, `app.decide_claim_responsibility`) | New regression in `customer-loyalty-expiry-fraud-prevention.sql`; two pre-existing tests updated to use a second manager identity (previously same-actor open+decide); full suite `ALL PASSED`; applied live | `RESOLVED (partial — item 1 only; items 2-3 stay OPEN)` |
| 40 | `ISS-2026-134` (item 3) | A null `reward.internal_cost` on an actively-fulfilling redemption silently contributed 0 to the reward-fulfillment liability line with no exception, unlike the two other derivation-mismatch types this same function already raises | `supabase/migrations/20260828070000_harden_customer_portal_loyalty_liability_reward_internal_cost_missing_exception.sql` — a third exception type, `reward_internal_cost_missing`, built on the CURRENT post-CPL-325 single-snapshot-atomicity body (self-caught mid-drafting: an early version was accidentally based on the stale pre-atomicity body, live-reproduced breaking the file's own atomicity regression with `CRITICAL: ... a torn, non-atomic read`, rebuilt correctly before finalizing). Currency-scoped identically to the accumulation itself, avoiding `ISS-2026-136` item 2's own disclosed noise shape | New regression in `customer-loyalty-liability-reconciliation.sql`; full suite `ALL PASSED` after the self-caught atomicity fix; applied live | `RESOLVED (partial — item 3 only; items 1, 2, 4, 5 stay OPEN)` |
| 41 | `ISS-2026-137` | `app.loyalty_account_tier_movements` carried the identical Supreme-Admin-override gap `ISS-2026-130` already closed for 5 other append-only ledger tables, but was omitted from that fix's own scope | `supabase/migrations/20260828060000_harden_customer_portal_loyalty_tier_movements_supreme_admin_override.sql` — mechanically extends the same already-generic `app.protect_loyalty_ledger_append_only()` trigger to this 6th table, plus the matching grant, mirroring `20260801280000` exactly | New regression in `customer-portal-loyalty-ledger-supreme-admin-override.sql`; a pre-existing fixture in `customer-loyalty-membership-tier.sql` that directly `UPDATE`d this table was corrected to use a genuine Supreme Admin actor; full suite `ALL PASSED`; applied live; trigger + grant live-verified | `RESOLVED` |

**Investigated and withdrawn from Batch 2, not counted as an item**: `ISS-2026-038` (self-approval
gate on `app.create_rate_version`/`app.approve_rate_version`) — a migration adding a
`created_by_auth_user_id` column plus a self-approval check was drafted and ready to apply, but a
repo-wide check first found `create_rate_version`+`approve_rate_version` used as a generic "seed an
approved rate" fixture-seeding shortcut across ~75 unrelated `scripts/db-tests/*.sql` files (117
total `approve_rate_version` call sites), almost universally with the SAME actor as both creator
and approver — enforcing self-approval as drafted would have broken dozens of unrelated domain
tests. Deleted before being applied anywhere, redispositioned to a dedicated future task with the
corrected scope; see `KNOWN_ISSUES.md`'s own entry for the full accounting.

`ISS-2026-040` (RPC-level `evaluate_permission` gate against a customer_user-layer principal) was
also drafted and withdrawn, for a more fundamental reason: this entry's own premise ("no tenant...
and no seed/fixture data anywhere does either [assign a staff role to a customer_user-layer
principal]") is factually wrong for this repository. At least 8 `scripts/db-tests/*.sql` files
deliberately grant a customer_user-layer principal a narrow, purpose-built staff role (e.g.
"CC Viewer Role", `OPS:View` only) specifically so that identity can read their own owner-scoped
records through the normal RPC path, with a SEPARATE ownership-scoping check providing the actual
isolation — a real, deliberate, working customer-portal pattern, not incidental fixture noise. A
blanket RPC-level deny would have broken this real capability across at least 8 domains.
Redispositioned to a dedicated future task that must first understand the ownership-scoping
architecture before any RPC-level gate is added; see `KNOWN_ISSUES.md`'s own entry.

**Investigated and withdrawn from Batch 1, not counted as an item**: `ISS-2026-170` — a migration
was drafted (a `p_record_id`/`p_record_type` tenant-ownership dispatch for `app.initiate_file_
upload`) and then deleted before being applied, after a wider check across all 34
`scripts/db-tests/*.sql` files calling this RPC (not just the 5 production TypeScript callers a
first-pass grep found) revealed the true `record_type` enumeration is roughly 17 values, several
using deliberately-synthetic placeholder ids by design — a fail-closed dispatch covering only the
5 known-production types would have broken legitimate tests across a dozen unrelated domains.
Redispositioned to a dedicated future task with the corrected scope handed forward; see
`KNOWN_ISSUES.md`'s own entry for the full accounting.

`ISS-2026-189` was drafted, applied locally (an `HRS:View` RLS gate with a self-access carve-out),
and then also reverted before being applied live, for a related but distinct reason: the local
`db-tests` suite surfaced ~90 sites across ~15 HRIS/ticketing test files relying on the current,
broader access as their own normal fixture-construction pattern — real evidence supporting this
entry's own "plausibly an intentional org directory feature" framing, not just inconvenient test
debt. Rather than force through a design interpretation this entry itself calls genuinely
undecided (by rewriting ~90 test-fixture call sites to prove a point still unsettled), the ruling
is deferred to a human, with both options and the new evidence laid out in `KNOWN_ISSUES.md`.

**Remaining as of this entry: 131** (0 Critical, 5 High, 62 Medium, 64 Low). Items 34-36 and 41
close `ISS-2026-115` (Low, doc-only), `ISS-2026-116` (Low), `ISS-2026-117` (Medium), and
`ISS-2026-137` (Low) — 4 items. Items 37-40 (`ISS-2026-124`, `128`, `133`, `134`) are each partial
— a real sub-item genuinely fixed, a distinct sub-item stays open — so none is counted toward this
reduction, matching this file's own discipline of only counting an item once its own disposition
is a real terminus. The 13 items re-verified still genuinely open this batch (118, 119, 120, 121,
122, 123, 126, 127, 129, 131, 132, 136, 138) were independently re-checked against current code and
redispositioned in `KNOWN_ISSUES.md` with a fresh Batch 4 update paragraph each — none closes, none
counted here. Prior to items 34-41, remaining was 135 (0 Critical, 5 High, 63 Medium, 67 Low) —
see items 32-33 immediately below for that batch's own accounting.

**Remaining as of that entry: 135** (0 Critical, 5 High, 63 Medium, 67 Low). Item 32
(`ISS-2026-092`, doc-only) closes 1 Medium item. Item 33 (`ISS-2026-063`) is NOT counted toward
this reduction — only 2 of its 3 sub-items closed, and its own load-proof sub-item stays genuinely
open, matching this file's own discipline of only counting an item once its own disposition is a
real terminus. The other 17 items examined this batch (057, 058, 060, 061, 062, 064, 066, 067,
068, 069, 070, 071, 073, 076, 091, 093, 100) were independently re-verified still genuinely open
against current code by a dedicated verification pass (specifically checked for the same hidden-
superseding-entry pattern `092` itself turned out to be) and redispositioned in `KNOWN_ISSUES.md`
with a fresh Batch 3 update paragraph each — none closes, none counted here, consistent with
`00_EXECUTION_INDEX.md` §8.1's "registered with a named owner and a disposition" treatment for
this class; `064` additionally received a doc-drift correction on one of its own three sub-items
(a UI route claimed missing was in fact built later at HRT-285), without changing its own overall
`OPEN` disposition. Prior to items 32-33, remaining was 136 (0 Critical, 5 High, 64 Medium, 67
Low) — see items 27-31 immediately below for that batch's own accounting.

Items 27-31 close
`ISS-2026-013`/`036` (Medium, doc-only), `ISS-2026-044` (Medium), `ISS-2026-043`/`048` (Medium,
partial by design, tracked as one combined item since both share the identical fix migration and
disclosure text) — 5 items. `ISS-2026-036`'s own compound "Low-Medium" severity is bucketed as
Medium here for tally arithmetic, consistent with the file's own disclosed Medium/Low-split
uncertainty (`BACKLOG_INVENTORY.md`'s own "Count reconciliation" section). `ISS-2026-049` (second
half fixed via the same item-30/31 migration) is NOT counted toward this reduction — its own first
half stays genuinely, deliberately open, so the item as a whole is not closed, matching this
file's own discipline of only counting an item once its own disposition is a real terminus, not a
partial step still awaiting a second fix. `ISS-2026-038`/`040` were drafted and withdrawn (see
above) — neither counted, neither closed; their own true scope was found materially larger than
this batch's own bounded-fix assessment, exactly the discipline this file's own Batch 1 withdrawals
(`ISS-2026-170`/`189`) already established. Prior to items 27-31, remaining was 141 (0 Critical,
5 High, 69 Medium, 67 Low) — see items 22-26 immediately below for that batch's own accounting.
Prior to items 22-26, remaining was 146 (0 Critical, 6 High, 70 Medium, 70 Low) — item 21
corrects a stale-documentation gap, not a new fix; the underlying defect closed 2 days before this
session's backlog remediation began. Prior to item 21, remaining was 147 (0/7/70/70); prior to
items 19-20, remaining stood at 149 (0 Critical, 9 High, 70 Medium, 70 Low), itself a correction to
the prior "166" figure recorded after items 1-2: that cell's own severity breakdown (15
High) was an arithmetic slip -- both items 1 and 2 are High, so it should have read 14
High (168 - 2 = 166 = 0 + 14 + 78 + 74). Caught and fixed here rather than silently
carried forward; does not change either item's own disposition, only this running
tally's own bookkeeping. Each is worked with the same discipline as every other fix
this range has made: real root-cause investigation (delegated to research-only agents where
a wide read is needed, design/implementation judgment never delegated), a real code/config
fix where one is genuinely agent-executable, a live-executed regression test proving the
fix (not merely asserting it), a full local `db-tests` suite re-run before applying live,
and application to the hosted project via `apply_migration` where the fix is a schema
change. Items that are structurally new capabilities (not bugs — "no mechanism exists for
X at all") or that require real external infrastructure/vendor/contract access are handled
per the operator's own stated preference: build whatever is genuinely code/config-shaped
now, and structure anything needing real external credentials as ready-to-configure
infrastructure rather than attempting to fabricate the external dependency itself.

**Two self-caught security regressions, not counted against the 168-item backlog** (both
introduced and closed within this same item-7 pass, not pre-existing `KNOWN_ISSUES.md`
entries at the time work began — registered afterward as `ISS-2026-298`/`ISS-2026-299` for
transparency, since both were live and exploitable in production for a period before being
caught): `ISS-2026-298` — a `public.record_database_restore_event` wrapper (item 5's own
fix) granted `anon`/`authenticated` live `EXECUTE` via Supabase's own default-privilege
bootstrap, a bare `revoke ... from public` missing the amended named-roles form; found while
drafting item 7's own wrappers, fixed live before item 7 was applied.
`ISS-2026-299` — `public.security_state_snapshots` (item 7's own new table, the first this
repository has ever placed directly in `public` schema) shipped with RLS disabled and the
same default-privilege bootstrap left ungranted-back for tables, giving `anon`/`authenticated`
live `SELECT`+`INSERT`; found while live-verifying item 7's own fix immediately after
applying it, fixed live within minutes. Both are fully detailed in `KNOWN_ISSUES.md` and
recorded as their own corrective migrations (`20260826081000`, `20260826090000`) rather than
edits to the already-applied files that introduced them. The mitigation both established —
live-verify `has_function_privilege`/`has_table_privilege` immediately after every
`apply_migration` call that creates a new `public.*` object, before considering that item's
fix complete — is now standing practice for the rest of this backlog.

This log continues in later dated sections of this file (or a successor tracking document
if this file grows unwieldy) — not restated in `GO_NO_GO_REPORT.md`, whose own verdict is
already independent of this backlog per §11.

---

## 12A. Track A — release execution (2026-08-27)

Per the operator's explicit plan-mode instruction (translated: "continue fixing the remaining
149 issues until done, plan carefully so the fix completes quickly and correctly so the webapp
can be released soon") and the resulting approved plan's own explicit operator choice — "release
now, then continue backlog, release again at the end" — this section records `RGL-405`
(Production Deployment) and the `RGL-406` (Post-Deployment Validation) evidence the plan called
for, executed as Track A of that plan.

**Gate re-verification on branch head before release** (all green): `typecheck` (`tsc --noEmit`,
clean); `lint` (0 errors, 337 pre-existing warnings — unchanged from before this pass, not a new
regression); `test` (5468/5468 unit tests); `db:test` (`bash scripts/db-tests/run.sh`, full
354-migration + 234-test-file suite, `ALL PASSED`); `security:check`; `standards:check`;
`docs:check`; `git:check-paths`; `release:check-freeze` (matches `RC-2026.08.25-1`'s frozen
digests).

**The merge itself was already done.** Investigating the migration-ledger question below (next
paragraph) surfaced that pull requests #68 and #69 for this branch had already merged into
`main` and Vercel had already deployed to production, evidently via GitHub PR auto-merge enabled
earlier in this same operator session, firing once CI passed — not a further explicit action in
this pass. Confirmed via `mcp__github__list_pull_requests`: PR #69 (head `fbbec14`, this branch's
exact tip before this section's edits) merged into `main` at `2026-08-27T10:21:38Z`, producing
merge commit `c11c616`. Confirmed via Vercel: deployment `dpl_2ofmyZzhDJEoCNDSphZjh8uiH3aU`,
`target: production`, `state: READY`, built from commit `c11c616`, alias
`cargogrid-app.vercel.app`.

**Post-deploy verification, live against production** (not assumed from the merge alone):

| Check | Expected | Actual | Result |
|---|---|---|---|
| `GET /api/v1/status` with an invalid Bearer key (`RGL-BLK-007`/`ISS-2026-295`) | Clean `401` | `401 {"error":{"code":"unauthenticated",...}}` | Confirmed fixed live |
| `POST /api/webhooks/third-party-gps/<malformed-uuid>` (`RGL-BLK-008`/`ISS-2026-296`) | Clean `400` | `400 {"ingestStatus":"invalid"}` | Confirmed fixed live |
| `GET /api/ready` | Healthy | `200 {"status":"ok"}` | Healthy |

Both previously-live High-severity defects are now confirmed fixed **in production**, not merely
in code — closing items 19-20 in §12's table above.

**Migration-ledger bookkeeping gap found and disclosed, not silently patched.** While confirming
the deploy pipeline's own behavior (does it run migrations against the hosted project? — no:
direct read of `.github/workflows/ci.yml` shows the `db` CI job runs `scripts/db-tests/run.sh`
against an ephemeral, disposable Postgres service container, never the real project; Vercel's
`main` auto-deploy is a pure Next.js build with no database step; a repo-wide search found no
`supabase db push`/migration-replay step anywhere), live-queried
`supabase_migrations.schema_migrations` on the hosted project (`awdlicmwzdxquopwtcfd`) and found
363 rows against 354 local `supabase/migrations/*.sql` files. The 9-row gap traces to the 9
migrations applied live during items 11-18 of this same backlog pass via the Supabase MCP
`apply_migration` tool, each recorded under Supabase's own auto-generated wall-clock `version`
rather than the repository filename's own embedded timestamp — the live schema is correct and
current, only the ledger's bookkeeping drifted from the filenames.

**Confirmed non-blocking to this release** (no migration-replay step exists in any path that
touched this deploy), but a real, forward-looking risk: a future `supabase db push` or equivalent
replay tool run against this project would consider these 9 files "unapplied" and attempt to
re-run non-idempotent DDL against an already-migrated schema. Attempted a direct, metadata-only
reconciliation (`update supabase_migrations.schema_migrations set version = ... where version =
... and name = ...`, verified first that none of the 9 target version values collide with an
existing row); denied by this session's own Auto Mode write-safety classifier as an unattended
write to a platform-internal system table — correctly not worked around, per the classifier's own
instruction to surface rather than bypass. Registered as `docs/runtime/KNOWN_ISSUES.md`
`ISS-2026-300` (Medium, `OPEN`) with the full mapping table and the exact reconciliation SQL for
an operator with direct Supabase Dashboard SQL-editor access, or a session with this specific
write pre-approved, to apply.

Full change record: `docs/runtime/CHANGE_MANIFEST.md` `CHG-2026-265`.

**Verdict**: `RGL-405` (Production Deployment) — `DONE`. `RGL-406` (Post-Deployment Validation) —
`DONE`, all 3 checks passed. `ISS-2026-300` is the one open item this section leaves behind,
explicitly disclosed rather than silently deferred.

---

## 13. Next eligible task

`RGL-407` (Rollback Decision) — not needed; all post-deploy checks in §12A passed, no rollback
indicated. `RGL-408`-`RGL-412` (Hypercare through Closure Verification) remain the formal
remainder of the Step 16 WBS but are not the operator's stated priority; per the approved plan,
work continues on Track B (the backlog) below, with a second release planned at the end.

**The actual next work**: Track B of the approved plan — build
`docs/build-log/release-go-live/BACKLOG_INVENTORY.md`, an authoritative entry-by-entry inventory
of the (now) 147 still-open `KNOWN_ISSUES.md` items, then work through them in ~20-item themed
batches. Progress tracked in future dated sections of this file and in `docs/runtime/
CHANGE_MANIFEST.md`, not as a Step 16 WBS row (this backlog spans every prior phase of the
project, not this range's own charter).

<!-- Superseded planning note, retained for history: the paragraph below predates §12A and
Track A's release; RGL-405 is no longer ineligible, it is done. -->

None within the Step 16 WBS, deliberately, not because a condition still blocks it. As of §11,
every item in this range's own blocker accounting is fixed, ruled, or operator-accepted — but this
report withholds `GO_DECIDED` pending a separate, explicit operator instruction to proceed (§11's
own reasoning: a still-armed auto-deploy mechanism, and the operator's own stated next priority
being the historical issue backlog, not a deploy). `RGL-405` remains ineligible until that
instruction is given.

**The actual next work, outside the Step 16 WBS**: building an accurate inventory of
`docs/runtime/KNOWN_ISSUES.md`'s open entries and working through them in severity order, per §11's
own plan. Progress tracked in future dated sections of this file and in `docs/runtime/
CHANGE_MANIFEST.md`, not as a Step 16 WBS row (this backlog spans every prior phase of the project,
not this range's own charter).
