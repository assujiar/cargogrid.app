# RGL-416 — Bagian 5 production re-verification, and `GO_NO_GO_REPORT.md` §7 item 5 re-satisfied

**Date:** 2026-09-03
**Trigger:** owner instruction to finish the remaining Step 17 batch. Continuation of `RGL-415.md`
(the pre-deploy gate-suite re-run and browserslist fix).

---

## 1. Security and performance advisors — live project `awdlicmwzdxquopwtcfd`

Pulled fresh via `get_advisors` (both `security` and `performance`), not reused from any earlier
pull. Full output (2,749 security + 1,548 performance findings) analyzed programmatically rather
than sampled, since neither fits in a single response.

**Zero `ERROR`-level findings in either category.** Everything present is `WARN` or `INFO`, and
every category is one this project's own design already accounts for:

| Category | Level | Count | Read |
|---|---|---|---|
| `authenticated_security_definer_function_executable` | WARN | 1,939 | The RPC layer's deliberate shape — every public wrapper is `SECURITY DEFINER`, callable by `authenticated`. One row per distinct function, zero duplicates. |
| `function_search_path_mutable` | WARN | 668 | Internal `app.*`/`public.*` functions with a mutable search_path. Zero overlap with the security-definer-executable set above. |
| `anon_security_definer_function_executable` | WARN | 10 | All `public.*`, all plausibly-intentional anonymous entry points (webhook ingestion, public tracking lookup, tenant/locale resolution). |
| `auth_leaked_password_protection` | WARN | 1 | Global Auth setting, HaveIBeenPwned check off. See §2. |
| `rls_enabled_no_policy` | INFO | 131 | RLS-on-with-no-policy fails closed (denies all access) — a lockout risk, not an exposure risk. |
| `unindexed_foreign_keys` | INFO | 919 | Performance-only. |
| `unused_index` | INFO | 629 | Performance-only. |

Nothing here is new since the last full advisor sweep referenced in this session's prior rulings,
and nothing rises to a blocking finding.

## 2. `auth_leaked_password_protection` — attempted, blocked by plan tier

Tried to close the one WARN with a concrete remediation (`PATCH
/v1/projects/awdlicmwzdxquopwtcfd/config/auth` with `password_hibp_enabled: true`), since it is a
single Auth-config toggle with zero schema/application blast radius. The Management API refused:

```
HTTP 402 {"message":"Configuring leaked password protection via HaveIBeenPwned.org is available
on Pro Plans and up."}
```

**Disposition:** plan-gated, not a code or configuration gap this session can close. Left `WARN`.
Owner action if desired: upgrade the Supabase project's plan tier, then re-apply the same PATCH
(or toggle it in the Dashboard's Auth settings).

## 3. Migration ledger reconciliation

Re-run and re-closed under `ISS-2026-300`'s own entry in `KNOWN_ISSUES.md` (see that entry's
2026-09-03 dated addendum for the full evidence trail). Summary: found 41 rows of ledger drift (31
new duplicates from this session's own `apply_migration` calls, 1 mis-versioned row, and the 9
rows `RGL-413.md` had deliberately left as orphans on 2026-08-31), verified every one at the
content level before touching anything, and closed all 41 in a single asserted transaction. Fresh
reconciliation query after: `live_not_local = 0`, `local_not_live = 0`, `total_live = 498` — exact
parity with `supabase/migrations/*.sql`.

## 4. `GO_NO_GO_REPORT.md` §7 item 5 — re-satisfied for the current branch head

Item 5 reads: *"This branch merged and its own content re-verified as the actual candidate that
would deploy — not `main`'s current, older state."* `RGL-414.md` satisfied this for commit
`d48b589` on 2026-08-31. The branch has since advanced 150+ commits closing the remaining backlog
(`RGL-415.md`, this ruling, and the commits either side of them). Re-satisfying item 5 for the
*current* head requires the same two things `RGL-414` required: a fast-forward merge with zero
divergence, and a full gate suite run against the exact commit that would deploy.

**Gate suite** — see `RGL-415.md` §4 for the full table (typecheck, 5,939 unit tests, full
`db:test` `ALL PASSED`, `next build`, `ui:check`, all policy/security/standards gates,
`release:check-freeze`, `issues:check`). Re-run again after this ruling's own `KNOWN_ISSUES.md`
edit: `issues:check` and `docs:check` both pass clean.

**Merge** — `origin/main` was already at this branch's pre-`RGL-415` head (`1f158af`, fast-forwarded
by an earlier turn this session) and 0 commits ahead of it, so the two commits this ruling and
`RGL-415.md` add (`a43cfb9` and this ruling's own) are a pure fast-forward with zero divergence —
matching `RGL-414.md` §1's "item 5 — satisfied by fact" pattern exactly.

## 5. What this ruling does not do

It does **not** record a new go decision. `GO_DECISION.json` still names `d48b589`
(`RGL-414`), now well behind `main`'s head — `scripts/release/check-go-decision.ts` will correctly
refuse a production build at the new head until a fresh go decision names it, exactly as
`RGL-414.md` §3 describes for any future commit. Recording that decision requires the same thing
`RGL-414.md` §1 required for the last one: an explicit, separate operator instruction to proceed,
put to the owner directly rather than inferred from the instruction to finish the batch.

Health-endpoint live verification (`GO_NO_GO_REPORT.md`'s remaining post-deploy check) also stays
outstanding for the same reason `RGL-414.md` §7 recorded it as outstanding: it requires an actual
deployment to probe, and none has been promoted to production. It is recorded in the go-decision
ruling that follows a production promotion, not here.
