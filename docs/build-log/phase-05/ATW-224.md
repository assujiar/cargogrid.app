# ATW-224 — Route and Load Planning Using Canonical Position

## 0. Checkpoint

| Field | Value |
|---|---|
| Prompt | `224_ROUTE_LOAD_PLANNING_PROMPT.md` (`CG-S10-ATW-005`) |
| Repository | `assujiar/cargogrid.app` |
| Working branch | `claude/prompt-224-n1ek85` |
| Dependency | `CG-S10-ATW-002` (Prompt 221, `VERIFIED`); `CG-S10-ATW-004` (Prompt 223, `VERIFIED`); verified PostGIS/location/config foundations |
| Authorization | User message "lanjut prompt 224" — a scoped, single-task, named authorization matching exactly the row `ADVANCED_TMS_WMS_EXECUTION_INDEX.md`'s prior checkpoint left `READY`. |

## 1. Pre-flight collision check

`pnpm run git:check` clean at start, continuing directly on top of the Prompt 223 commit (`f8f482d`, merge of PR #33). This sandbox required the same one-time environment setup the Prompt 220 kickoff already documented (`postgresql-16-postgis-3` installed, local Postgres 16 started, superuser password set to the script's own documented default) — a sandbox condition, not a repository change; it had reset between sessions and was redone here so `pnpm run db:test` could run for real against every migration, including this checkpoint's own new one.

## 2. Baseline reconciliation

`docs/build-log/phase-05/ADVANCED_TMS_WMS_EXECUTION_INDEX.md` row `224` (`CG-S10-ATW-005`) was `READY` (dependency-clean, unblocked by `223`'s own verification) after Prompt 223's own reconciliation. Read in full: `224_ROUTE_LOAD_PLANNING_PROMPT.md`. Fresh baseline gate suite confirmed green before any Phase 5 file for this task was written: `typecheck`/`lint` 0 errors (80 pre-existing warnings unchanged), `db:test` PASS across the pre-existing 98 migrations/100 db-test files.

## 3. Implementation

### 3.1 Scope boundaries and design decisions (disclosed, migration header)

1. **No second canonical-position surface.** `ATW-226F` (canonical telemetry storage, normalization, ordering, and source arbitration) has not been built yet. `app.shipment_tracking_health` (`ATW-222`) is already the single canonical per-shipment tracking-health *projection* this prompt's own §6/§14 require planning to read through, and it remains a genuinely real, empty, honest table (`tracking_status = 'not_tracked'` for every row) until `ATW-226F` ships a live writer. `app.get_canonical_position_for_planning()` reads that table directly (`LEFT JOIN`, identical to `ATW-222`'s own dispatch-board read model) and reports the same honest `not_tracked`/`unknown` projection every other caller already sees. This prompt's own §22 alternative flow ("plan manually when no trusted position exists") is therefore this checkpoint's *real, exercised* path, not a theoretical fallback — every scenario validated against this repository's current state degrades to position-unaware planning, the honest, correct outcome.
2. **One RPC per row-level mutation**, mirroring `ATW-221`'s own established granularity (`app.add_shipment_leg`/`app.add_shipment_leg_stop`/`app.allocate_shipment_leg_cargo`, never a single bulk-jsonb-array entry point). A scenario is prepared empty (`app.prepare_route_planning_scenario`), then stops and constraints are added one at a time, both gated to the scenario's own `draft` status.
3. **The deterministic baseline planner is bounded and capacity-fit driven, never a claimed-optimal routing/TSP solver.** Every candidate for one scenario shares the identical stop sequence and the identical governed default speed constant (`app.route_planning_default_speed_kmh()` = 40 km/h, a disclosed reasoned default in the same "no blueprint-mandated number exists for this dimension" class `ADR-0002`/`ADR-0004`/`ADR-0011`/`ADR-0012`/`ADR-0013`/`ADR-0014` already used — not minted as its own ADR since it is a coarse duration *label* for human review, never a governed enforcement threshold). What genuinely differentiates candidates is resource capacity fit: up to 3 active vehicle/driver pairs ranked by tightest sufficient `app.vehicle_operational_profiles.capacity_weight_kg` match, each candidate's score components stored individually for explainability (§4's own "explainable route and load planning" requirement). Hard constraints that depend on a later-known fact (`max_distance_km`, computed only once stops are geocoded and summed) are enforced at candidate-generation time; hard constraints naming a specific resource (`required_vehicle_master_id`/`required_driver_master_id`) are checked for existence/eligibility at validation time.
4. **"Async jobs" reuses `PLT-132`'s own generic `app.jobs` queue verbatim**, adding exactly one new `job_type` (`route_load_planning`) to the already-widened `jobs_job_type_check` constraint and to `app.enqueue_job`'s own `v_valid_job_types` array (both via `ALTER ... DROP/ADD CONSTRAINT` + `CREATE OR REPLACE FUNCTION`, the identical extension mechanic `PLT-132`'s own migration header already used for `app.record_job_failure`). No live continuous worker-polling process exists anywhere in this repository yet (`NOT_RUN`, the same disclosed class `PLT-132`'s own header, `PLT-123`'s escalation timer, and `PLT-125`'s numbering counter already carry) — `app.run_next_route_planning_job` is the real, directly-invokable domain worker entry point (`app.claim_next_job` → deterministic planner → `app.complete_job`/`app.record_job_failure`), proven by this checkpoint's own db-test invoking it directly and exposed in `server/mutations/route-load-planning.ts` the same way `PLT-132`'s own `claim_next_job`/`complete_job` are exposed in `server/mutations/background-job.ts`.
5. **Selection history follows `app.device_vehicle_assignments`' own `is_current`/`superseded_by_id` precedent** (`ATW-223`) — a human selection is never overwritten in place, always superseded.
6. **Planning is decision support only (§24 business rule 1) — this migration never mutates `app.shipment_legs`.** Selecting or overriding a plan records the human decision; a dispatcher still executes it through `ATW-221`'s own existing leg/assignment mutations exactly as today. Replanning (`app.replan_route_planning_scenario`) copies the prior scenario's own stops and constraints into a fresh `draft` scenario and blocks only the one concrete case this checkpoint can actually prove: every one of the shipment's own legs has already left `planned` (nothing left to replan). `ATW-228` (Advanced Milestone and Exception) does not exist yet, so the "approved tracking-derived exception" trigger named in §22 is captured here as a disclosed, audited free-text `p_reason` only, never a verified upstream exception record — `ATW-228` will supply the structured trigger once it ships.
7. **Record-scope reuses `app.can_access_record`/`app.lead_record_scope_org_unit_ids`** joined through to the owning Shipment Order, identical to `ATW-221`'s own `shipment_legs`/`shipment_leg_stops` policy shape (a planning scenario has no owner/org-unit column of its own).
8. Permission catalogue: reuses the already-seeded `OPS` action set (`Create`, `Edit`, `Override`) — no new `app.permissions` row, no new module code. `Override` (an existing `OPS` workflow action, not new) is the concrete mechanism behind §26's "managers approve configured overrides."
9. Per `ERR-2026-004`: this migration carries its own explicit `REVOKE EXECUTE ON ALL FUNCTIONS IN SCHEMA app FROM PUBLIC` statement before its final grants.

### 3.2 Schema — `supabase/migrations/20260729320000_create_advanced_tms_route_load_planning.sql`

Seven tables: `app.route_planning_scenarios`, `_stops`, `_constraints`, `_candidate_plans`, `_score_components`, `_selected_plans`, `_replan_events`. One `app.jobs` widening (new `job_type` `route_load_planning`, plus `CREATE OR REPLACE` of `app.enqueue_job`). Fourteen new functions: two governed constants (`route_planning_default_speed_kmh`, `route_planning_position_staleness_tolerance_seconds`), `get_canonical_position_for_planning`, `prepare_route_planning_scenario`, `add_route_planning_stop`, `add_route_planning_constraint`, `validate_route_planning_scenario`, `execute_route_planning_scenario`, `generate_route_planning_candidates`, `run_next_route_planning_job`, `cancel_route_planning_scenario`, `select_route_planning_plan`, `override_route_planning_selection`, `replan_route_planning_scenario`, `get_route_planning_stops`. RLS joins every table back to the owning Shipment Order via `app.can_access_record`, mirroring `ATW-221`'s own `shipment_legs` policy shape.

### 3.3 Service layer and UI

`server/contracts/route-load-planning/route-load-planning.ts` (full Zod schemas for all seven entities, the canonical-position projection, plus ten mutation input schemas/parsers), `server/queries/route-load-planning.ts` (`listRoutePlanningScenarios`, `getRoutePlanningScenario`, `listRoutePlanningStops`, `listRoutePlanningConstraints`, `listRoutePlanningCandidatePlans`, `listRoutePlanningScoreComponents`, `getCurrentRoutePlanningSelection`, `listRoutePlanningSelections`, `listRoutePlanningReplanEvents`, `getCanonicalPositionForPlanning`), `server/mutations/route-load-planning.ts` (ten typed mutation wrappers plus `runNextRoutePlanningJob`, `RouteLoadPlanningMutationError` with a 24-code error classification list).

New route `app/(tenant)/[tenantSlug]/operations/shipment-orders/[shipmentOrderId]/route-planning/` (`page.tsx`, `loading.tsx`, `actions.ts`, `route-planning-workspace.tsx`) — a dedicated planner workspace (not a panel bolted onto the already-large shipment-order detail page, matching `ATW-222`'s own precedent of a dedicated route for a workflow-heavy capability): scenario picker, stops/constraints with add-forms (draft-only), a trusted-position summary shown at safe granularity (tracking status/freshness/source-class label only — never raw coordinates, matching `ATW-222`'s own dispatch-board "no raw device or provider data" precedent), validate/execute/run-planner/cancel controls, candidate alternatives with their own explainable score breakdown and infeasibility reasons, select/override, and replan. The shipment-order detail page (`[shipmentOrderId]/page.tsx`) gained one added link to this new route, no other change. `runRoutePlanningJobAction` directly invokes `app.run_next_route_planning_job` on demand — this checkpoint's own honest stand-in for the worker poller that does not exist yet, disclosed in the UI copy itself ("No live worker polls this queue yet").

## 4. Tests

`scripts/db-tests/advanced-tms-route-load-planning.sql` (new) — full fixture (tenant/org hierarchy, a rep holding `OPS:Create/Edit/View`, a manager additionally holding `OPS:Override`, an `OPS:View`-only viewer, a sibling-team outsider, a global Supreme Admin, a second tenant; two active vehicle operational profiles at different capacities and two active driver operational profiles; three confirmed land-freight Shipment Orders off one confirmed Job Order), then eleven assertion sections: scenario preparation (idempotent, authority-gated); stop/constraint addition (draft-only gate, structural value-shape validation, GeoJSON round-trip); scenario validation (stop-count/sequence-gap rejection, required-resource existence check, an honestly `not_tracked`/unusable canonical-position snapshot captured — proving §22's alternative flow is real, not theoretical); execute + the worker (`app.run_next_route_planning_job` claims and runs two queued jobs back-to-back, returns `null` once nothing remains due, best-capacity-fit ranking confirmed by asserting `plan_rank 1` is the tighter-capacity vehicle, a real bounded Jakarta–Bandung geodesic distance asserted `>0`/`<300km`, exactly one infeasible placeholder with `no_eligible_vehicle` among its reasons when nothing qualifies); selection (rejects an infeasible candidate, succeeds on a feasible one, re-selection supersedes the prior one via `is_current`/`superseded_by_id`, partial-unique-index invariant confirmed); override (`OPS:Override` required — denied for the `OPS:Create/Edit`-only rep even with a valid reason, `override_reason_required` for an empty reason, succeeds once the rep's own role is additionally granted `OPS:Override`, selecting the infeasible candidate); cancellation (allowed pre-selection, blocked once `selected` or already `cancelled`); replan (copies stops/constraints into a fresh draft scenario linked via `app.route_planning_replan_events`, requires a non-empty reason, and a dedicated case drives one shipment's own leg through the full `planned→dispatched→in_transit→arrived→completed` chain via `ATW-221`'s own already-verified functions to prove `nothing_to_replan` fires once every leg has left `planned`); cross-tenant isolation; schema-privilege defense in depth (zero `anon` EXECUTE on any of the 15 new functions, zero `authenticated` direct INSERT/UPDATE/DELETE on any of the 7 new tables); exact-count audit-trail assertions.

`server/contracts/route-load-planning/route-load-planning.test.ts` (14 cases), `server/queries/route-load-planning.test.ts` (6 cases), `server/mutations/route-load-planning.test.ts` (11 cases) — **25 net new** `node:test` cases. `node:test` **2230/2230** (25 net new, zero regression against the 2205 baseline — one test, `checkWorktreeCollision`'s own "the current branch is reported as diverged from origin/main" assertion, failed transiently *before* this checkpoint's commit, since `claude/prompt-224-n1ek85` was still identical to `origin/main` pre-commit; it is expected to self-resolve the moment this checkpoint's own commit lands, and was re-confirmed passing after commit — not a defect in this checkpoint's own code). `db:test` PASS across 99 migrations/**101** db-test files (1 net new, zero regression). `npx next build` PASS (**81 routes**, 1 new: `/[tenantSlug]/operations/shipment-orders/[shipmentOrderId]/route-planning`). `docs:check`/`security:check`/`data-classification:check`/`threat-model:check`/`standards:check`/`git:check` all pass. `git:check-paths` correctly flags this checkpoint's own new migration file under its own blunt `^supabase/migrations/\d.*\.sql$` "applied migration" regex — confirmed via `git diff --cached --name-status -- supabase/migrations/` that the file's own status is `A` (added), never `M` (modified) against any previously-merged migration, satisfying `AGENTS.md`'s actual "never edit an applied migration; add a new migration instead" rule in spirit. The regex is path-only and cannot itself distinguish an addition from an edit — a structural limitation of the check script, not a violation of the underlying policy, and (by the same structural argument) the identical experience every prior migration-adding Phase 5 checkpoint (`221`/`222`/`223`) already had.

### 4.1 Real bugs found and fixed during development (not pre-existing test fragility — genuinely new to this checkpoint's own code)

1. **`app.select_route_planning_plan`/`app.override_route_planning_selection`: insert-before-supersede ordering would have transiently violated the partial unique index.** Both functions initially inserted the new `is_current = true` selection row before marking the prior current row `is_current = false`, momentarily double-booking `route_planning_selected_plans_current_scenario_unique on (scenario_id) where is_current` — the exact defect class `ATW-223`'s own `app.assign_device_to_vehicle` header already disclosed and fixed. Root-caused by the db-test's own re-selection assertion failing with `duplicate key value violates unique constraint`. Fixed by following `app.reassign_resource`'s (OPS-172) own precedent exactly in both functions: UPDATE the prior row to `is_current = false` first, then INSERT the new row, then UPDATE the prior row's `superseded_by_id`.
2. **`app.replan_route_planning_scenario`: a missing `public` schema on `search_path` broke PostGIS type resolution.** The function copies a prior scenario's own stops into a fresh draft scenario via a plain `INSERT ... SELECT`, which triggers `route_planning_stops`' own `location_geog` CHECK constraint (`app.validate_geography_point`) — that function has no `SET search_path` of its own, so it resolves `geometry`/`ST_IsEmpty` etc. under whichever search_path is active in the *calling* function's context. `replan_route_planning_scenario` was declared `set search_path = app, pg_temp` (correct for a function with no direct geography construction), but the constraint evaluation happening *inside* its own execution still needed `public` (where PostGIS installs its types) reachable. Root-caused directly against a real Postgres 16 + PostGIS 3.4.2 instance (`type "geometry" does not exist`), not assumed from documentation. Fixed by widening the search_path to `app, public, pg_temp`, matching `app.add_route_planning_stop`'s own already-correct convention.

### 4.2 Db-test fixture defect found and fixed (not this checkpoint's own production code)

`register_vehicle_operational_profile`/`register_driver_operational_profile` (`ATW-223`) call `app.create_master_record` (PLT-120) internally, hard-gated to tenant_admin/Supreme Admin authority regardless of `OPS` role grants — this checkpoint's own tenant_admin fixture user held no `OPS` role at all initially. Fixed by additionally granting it the manager role (via Supreme Admin, avoiding self-assignment), the identical fixture fix `ATW-223`'s own db-test already applied for its "Fleet Rep" actor.

## 5. Residual disclosures

- No live continuous worker-polling process exists anywhere in this repository yet (`NOT_RUN`) — `app.run_next_route_planning_job` and its TypeScript wrapper are real and directly invokable (proven in this checkpoint's own db-test and by a manual "Run planner now" UI action), matching `PLT-132`'s own disclosed precedent for the generic job queue's own claim/complete lifecycle.
- `app.get_canonical_position_for_planning` currently always reports `not_tracked`/unusable — `app.shipment_tracking_health` has no live writer yet (`ATW-226F`). Every scenario this checkpoint validated therefore planned in position-unaware manual mode; this is the honest, correct, currently-only-reachable outcome, not a gap in this checkpoint's own logic.
- Replanning's own "approved tracking-derived exception" trigger (§22) is a disclosed, audited free-text reason only — `ATW-228` (Advanced Milestone and Exception) does not exist yet to supply a structured, verified exception record.
- No REST/GraphQL surface exists for this or any domain yet (repository-wide, unchanged).

## 6. Completion

This checkpoint corrects `ADVANCED_TMS_WMS_EXECUTION_INDEX.md` row `224` (`READY` → `VERIFIED`) and marks row `225`'s (`CG-S10-ATW-006`, Prompt 225) own dependency-clean status `READY`. Per this repository's own standing discipline, this session stops here and awaits fresh explicit user authorization before starting `CG-S10-ATW-006` (Prompt 225) or any further Phase 5 task.
