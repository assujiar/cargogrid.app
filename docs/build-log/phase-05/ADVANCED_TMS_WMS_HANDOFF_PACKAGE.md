# Phase 5 (Advanced TMS/WMS) Handoff Package

**Produced by:** `CG-S10-ATW-028` (Prompt 247 — Advanced TMS/WMS Documentation and Handoff), per Prompt 247 §20's "Publish at minimum" list and §21 ("Readers start at one index, choose role/use case, follow versioned procedures, and reach exact diagnostics/recovery").
**Audience:** an independent Phase 6 (Procurement/Vendor) / Phase 7 (HRIS/Ticketing) / Phase 8 (Customer Portal/Loyalty) / Phase 9 (Intelligence/Enterprise) agent with **zero prior context** from this build session, plus operators, device technicians, drivers'-app developers, and support staff who need Phase 5 explained from real, evidence-backed fact — every claim below is either directly cited to a `VERIFIED` build log/migration or explicitly marked as this checkpoint's own reconciliation.
**Status of this package itself:** complete pending one external precondition — `CG-S10-ATW-029` (Prompt 248, Advanced TMS/WMS Closure Verification) has not yet run. **Nothing in this document should be read as `PHASE_5_VERIFIED` being set** — only Prompt 248 may set that.
**This is a new, self-contained artifact**, distinct from `docs/runtime/HANDOFF.md` (the intra-Phase-5, checkpoint-to-checkpoint runtime handoff). It exists specifically for the "fresh downstream-phase agent reconstructs Phase 5 and starts the exact eligible next task safely" flow, mirroring `docs/build-log/phase-04/FINANCE_HANDOFF_PACKAGE.md`'s own precedent, one phase up, and `docs/build-log/phase-01/PLATFORM_CORE_HANDOFF_PACKAGE.md`/`docs/build-log/phase-02/COMMERCIAL_HANDOFF_PACKAGE.md`/`docs/build-log/phase-03/OPERATIONS_HANDOFF_PACKAGE.md`'s own precedent further back.

**This checkpoint's own scope split:** Prompt 247's own work was produced by two agents in the same tree with zero file overlap. This document and the six guides/one contract it links to under "PRODUCT/ARCHITECTURE/HANDOFF" below were produced by that half. The "TECHNICAL/HARDWARE/PROTOCOL/OPERATIONS" half (device/SIM/installation, gateway deployment, endpoint/firewall/scaling, provider onboarding/rotation, queue/DLQ/reconciliation, the deferred-evidence record, and the outage runbooks) was produced concurrently by the sibling agent — every one of its real, on-disk deliverables is linked below by its real, confirmed filename, not a placeholder.

## 1. How to use this package (start here)

| If you are... | Start with |
|---|---|
| A fresh downstream-phase agent (Steps 11–14) | §2 (verified dependencies) → §3 (preserved assets) → `docs/build-log/phase-05/STEPS_11_14_HANDOFF_CONTRACT.md` |
| A developer building against the telemetry pipeline | `docs/build-log/phase-05/guides/architecture-index-and-canonical-telemetry-data-flow.md` |
| A tenant admin or support engineer explaining a tracking package | `docs/build-log/phase-05/guides/subscription-package-and-entitlement-guide.md` |
| A developer building the eventual Driver Mobile app, or support explaining what exists today | `docs/build-log/phase-05/guides/driver-mobile-tracking-guide.md` |
| Anyone asking "why did the system pick this GPS source over that one" | `docs/build-log/phase-05/guides/source-arbitration-and-fallback-explanation.md` |
| A dispatcher/fleet-ops user, or someone explaining what a customer can see | `docs/build-log/phase-05/guides/fleet-control-tower-and-customer-projection-guide.md` |
| Compliance/support asking what data is collected and how long it is kept | `docs/build-log/phase-05/guides/privacy-consent-and-retention-guide.md` |
| A device technician or ops admin procuring/installing physical hardware | `docs/build-log/phase-05/guides/gps-hardware-procurement-installation-guide.md` (sibling deliverable) |
| DevOps deploying/operating the GPS Gateway | `docs/build-log/phase-05/guides/teltonika-codec8e-gateway-deployment-guide.md`, `docs/build-log/phase-05/guides/gps-gateway-endpoints-firewall-health-metrics-scaling-guide.md` (sibling deliverables) |
| An integration admin onboarding a third-party GPS platform | `docs/build-log/phase-05/guides/third-party-provider-onboarding-and-credential-rotation-guide.md` (sibling deliverable) |
| On-call responding to an outage | `docs/runbooks/gps-gateway-outage.md`, `docs/runbooks/gps-ingestion-database-outage.md`, `docs/runbooks/third-party-provider-outage.md`, `docs/runbooks/observability-exporter-outage.md`, `docs/runbooks/secret-leak-incident-response.md`, and `docs/build-log/phase-05/queue-dlq-replay-reconciliation-procedure.md` (sibling deliverables) |
| Asking what remains untested against real hardware/a real provider | `docs/build-log/phase-05/deferred-physical-device-test-plan-and-provider-evidence-record.md` (sibling deliverable) |

## 2. Verified dependencies (what Phase 6/7/8/9 may rely on as fact)

| Closure | Status | Evidence |
|---|---|---|
| Phase 0 — Discovery and Foundation | `PHASE_0_VERIFIED` | `docs/build-log/phase-00/PHASE0_CLOSURE_REPORT.md` |
| Phase 1 — Platform Core | `PHASE_1_VERIFIED` | `docs/build-log/phase-01/PLATFORM_CORE_CLOSURE_REPORT.md` |
| Phase 2 — Commercial | `PHASE_2_VERIFIED` | `docs/build-log/phase-02/COMMERCIAL_CLOSURE_REPORT.md`, `COMMERCIAL_HANDOFF_PACKAGE.md`, `JOB_ORDER_HANDOFF_CONTRACT.md` |
| Phase 3 — Operations | `PHASE_3_VERIFIED` | `docs/build-log/phase-03/OPERATIONS_CLOSURE_REPORT.md`, `OPERATIONS_HANDOFF_PACKAGE.md`, `OPERATIONS_DOWNSTREAM_CONTRACTS.md` |
| Phase 4 — Finance | `PHASE_4_VERIFIED` | `docs/build-log/phase-04/FINANCE_CLOSURE_REPORT.md`, `FINANCE_HANDOFF_PACKAGE.md`, `FINANCE_DOWNSTREAM_CONTRACTS.md` |
| Phase 5 kickoff (`220`, `CG-S10-ATW-001`) | `VERIFIED` | `docs/build-log/phase-05/00_ADVANCED_TMS_WMS_WBS.md`, `ADVANCED_TMS_WMS_EXECUTION_INDEX.md` §1.1 |
| 24 Transport/Telemetry/WMS capability tasks (`221`–`230`, `242`, `011A`/`011B`/`016A` inserted, plus the 9-child `226` decomposition) | All `VERIFIED` | `docs/runtime/TASK_LEDGER.md`; individual build logs `ATW-221.md`–`ATW-230.md`, `ATW-226A.md`–`ATW-226I.md`, `ATW-011A.md`, `ATW-011B.md`, `ATW-016A.md`, `ATW-012.md`–`ATW-023.md` |
| High-Volume TMS/WMS and Multi-Source Telemetry Controls (`243`, `CG-S10-ATW-024`) | `VERIFIED` | `ATW-024.md` |
| Advanced Claim and Incident Operations (`244`, `CG-S10-ATW-025`) | `VERIFIED` | `ATW-025.md` |
| Advanced TMS/WMS Integrated Verification (`245`, `CG-S10-ATW-026`) | `VERIFIED`, zero Critical/High finding | `ATW-026.md` |
| Advanced TMS/WMS Integrity and Security Hardening (`246`, `CG-S10-ATW-027`) | `VERIFIED`, zero Critical/High finding remains open | `ATW-027.md` |
| Advanced TMS/WMS Documentation and Handoff (`247`, `CG-S10-ATW-028`, this checkpoint) | `IN_PROGRESS` → `VERIFIED` on this checkpoint's own close | This document + its 6 guides + `STEPS_11_14_HANDOFF_CONTRACT.md` + the sibling agent's own 6 deliverables (§1 table above) |
| Advanced TMS/WMS Closure Verification (`248`, `CG-S10-ATW-029`) | `NOT_STARTED` — the one remaining gate before `PHASE_5_VERIFIED` | `docs/ai-agent-build-prompt-package/10-phase-05-advanced-tms-wms/248_ADVANCED_TMS_WMS_CLOSURE_VERIFICATION_PROMPT.md` |

**Domain-code status:** zero Procurement/Vendor, HRIS/Ticketing, Customer-Portal/Loyalty-UI, or Intelligence/Enterprise business code exists anywhere in this repository (re-confirmed §9 below). Everything in §3 is Transport/Tracking/WMS-domain evidence — Phase 5's own scope ends at the surfaces named in `STEPS_11_14_HANDOFF_CONTRACT.md`.

For the full 40-row task list (task ID, prompt, capability, build log, and a one-line description of what each capability actually is), see `docs/build-log/phase-05/guides/architecture-index-and-canonical-telemetry-data-flow.md` §2 — not reproduced a second time here.

## 3. Preserved assets (what already exists — do not recreate)

### 3.1 Database (42 Phase 5 migrations of 137 total, `supabase/migrations/`)

Every migration lives under `app` schema ownership, RLS enabled on every tenant-scoped table, and the `ERR-2026-004` per-migration convention (`revoke execute on all functions in schema app from public;` before every explicit grant) present in every Phase 5 migration without exception — independently re-confirmed at `ATW-027`'s own hardening audit and at every intervening checkpoint's own gate run.

| Capability | Migration(s) | Key tables/functions |
|---|---|---|
| Multi-Leg and Multimodal Shipment (`221`) | `20260729290000_create_advanced_tms_multi_leg_shipment.sql` | Ordered legs over the existing `app.shipment_orders` root — no second shipment root |
| Advanced Dispatch Board with Tracking Health (`222`) | `20260729300000_create_advanced_tms_dispatch_board.sql` | `app.shipment_tracking_health` (deliberately empty/write-path-less until `243`), `app.dispatch_board_queue` view, `app.is_shipment_tracking_entitled` (disclosed stub, replaced real at `226A`) |
| Fleet, Vehicle, Driver, Device and SIM Baseline (`223`) | `20260729310000_create_advanced_tms_fleet_driver_device.sql` | `app.vehicle_operational_profiles`, `app.driver_operational_profiles` (incl. `mobile_tracking_consent`), `app.gps_devices`, `app.sim_cards`, `app.device_vehicle_assignments`, `app.provider_vehicle_mappings`, `app.vehicle_tracking_source_priorities`, `app.register_gps_device`, `app.transition_gps_device_status` |
| Route and Load Planning Using Canonical Position (`224`) | `20260729320000_create_advanced_tms_route_load_planning.sql` | `app.route_planning_position_staleness_tolerance_seconds` and replanning against live position |
| First/Middle/Last-Mile Orchestration with Tracking Policy (`225`) | `20260729330000_create_advanced_tms_mile_orchestration.sql` | `app.shipment_leg_tracking_policies`, `app.shipment_leg_tracking_sessions`, `app.check_leg_tracking_source_eligible`, `app.start_leg_tracking_session`, `app.end_leg_tracking_session` |
| Tracking entitlement and source policy (`226A`) | `20260729340000_create_advanced_tms_tracking_entitlement_source_policy.sql` | `app.tenant_tracking_source_policies`, `app.resolve_tenant_tracking_package`, `app.is_shipment_tracking_entitled` (real), `app.resolve_tenant_tracking_source_policy` |
| Device, SIM, provider, installation, mapping management (`226B`) | `20260729350000_create_advanced_tms_device_installation_evidence.sql` | `app.gps_device_installations`, `app.record_gps_device_installation`, `app.verify_gps_device_installation` |
| Driver Mobile GPS session and HTTPS ingestion (`226C`) | `20260729360000_create_advanced_tms_driver_mobile_tracking.sql` | `app.driver_mobile_tracking_sessions`, `app.driver_mobile_position_reports`, `app.start_driver_mobile_session`, `app.ingest_driver_mobile_report` (the first real `anon`-callable telemetry surface) |
| Always-on GPS Gateway and Teltonika Codec 8E adapter (`226D`) | `20260729370000_create_advanced_tms_gps_gateway_ingestion.sql` | `app.direct_device_telemetry_reports`, `app.ingest_direct_device_telemetry_batch`, `app.resolve_gps_device_for_handshake`; `services/gps-gateway/` (the standalone TCP server) |
| Third-party GPS platform adapter contract (`226E`) | `20260729380000_create_advanced_tms_third_party_provider_adapter.sql` | `app.third_party_provider_connections`, `app.third_party_telemetry_reports`, `app.ingest_third_party_provider_webhook_event`, HMAC-SHA256 signature verification |
| Canonical telemetry storage/normalization/ordering/source arbitration (`226F`) | `20260729390000_create_advanced_tms_canonical_telemetry_arbitration.sql` | `app.canonical_telemetry_events`, `app.vehicle_current_positions`, `app.vehicle_source_health`, `app.vehicle_source_switches`, `app.arbitrate_and_project_vehicle_position` — the single arbitration entry point |
| Geofence, milestone, exception, route-deviation signals (`226G`) | `20260730090000_create_advanced_tms_geofence_route_deviation_signals.sql` | `app.shipment_milestone_candidates`, `app.shipment_exception_signals`, `app.evaluate_geofence_and_deviation_signals`, staged confirm/dismiss into Operations' `app.milestone_events`/`app.operational_exceptions` |
| Fleet Control Tower, device administration, sanitized projections (`226H`) | `20260730100000_create_advanced_tms_fleet_control_tower.sql` | `app.get_tenant_vehicle_tracking_overview`, `app.get_tenant_pending_milestone_candidates`, `app.get_tenant_pending_exception_signals`; widens `app.lookup_public_shipment_tracking` (sanitized vehicle position) |
| Third-party connection recovery hardening (`226I`) | `20260730110000_harden_advanced_tms_third_party_provider_connection_recovery.sql` | 10-consecutive-failure auto-disable, manual disable/re-enable |
| Capacity, Utilization and Tracking Coverage (`227`) | `20260730120000_create_advanced_tms_capacity_utilization.sql` | `app.vehicle_capacity_reservations` (row-lock-then-sum overbooking prevention), `app.reserve_vehicle_capacity`, `app.get_tenant_tracking_coverage`, `app.get_tenant_tracking_utilization_summary` |
| Advanced Milestone/Exception with Multi-Source Telemetry (`228`) | `20260730130000_create_advanced_tms_milestone_exception_telemetry.sql` | `app.evaluate_telemetry_confidence_and_freshness`, `app.detect_shipment_leg_tracking_health_signals`, `app._compute_shipment_leg_eta`, `app.rebaseline_shipment_leg_schedule`; further widens `app.lookup_public_shipment_tracking` (`live_eta_status`/`live_eta_at`) |
| Warehouse and Zone (`229`) | `20260730140000_create_advanced_tms_warehouse_zone.sql` | `app.warehouses`, `app.warehouse_zones`, `app.warehouse_customer_eligibility` |
| Bin and Racking (`230`) | `20260730150000_create_advanced_tms_bin_racking.sql` | `app.warehouse_locations` (materialized-path hierarchy reusing `app.org_units`'s own mechanism) |
| Item/SKU and UOM Master (`011A`, inserted) | `20260730160000_create_advanced_tms_item_uom_master.sql` | `app.item_masters`, `app.uoms`, `app.uom_conversions` — `ADR-0019` |
| Warehouse/zone dependency hardening + known-issue backfill (`011B`, inserted) | `20260730170000_harden_advanced_tms_warehouse_zone_location_dependency.sql` | Widens `app.set_warehouse_zone_status`/`app.set_warehouse_status` to block deactivation while active child locations exist |
| WMS Inbound (`231`) | `20260730180000_create_advanced_tms_wms_inbound.sql` | `app.wms_inbound_orders`, `app.wms_inbound_order_lines` — `source_type` deliberately excludes `'purchase_order'` (see `STEPS_11_14_HANDOFF_CONTRACT.md` §2) |
| Inventory Ledger (`234`) | `20260730190000_create_advanced_tms_inventory_ledger.sql` | `app.inventory_movements`, `app.inventory_movement_lines`, `app.inventory_balances`, `app.inventory_reservations`, `app.post_inventory_movement` (the one generic posting primitive) |
| WMS Receiving (`232`) | `20260730200000_create_advanced_tms_wms_receiving.sql` | Physical receipt capture composing `app.post_inventory_movement` |
| WMS Putaway (`233`) | `20260730210000_create_advanced_tms_wms_putaway.sql` | Directed storage into `app.warehouse_locations` |
| Lot, Batch, Serial and Expiry (`235`) | `20260730220000_create_advanced_tms_lot_batch_serial_expiry.sql` | `app.lot_identities`, `app.serial_identities`, `app.register_lot_identity`/`app.register_serial_identity`, `app.list_allocation_candidates` (FEFO/FIFO) |
| WMS Outbound Order (`016A`, inserted) | `20260730230000_create_advanced_tms_wms_outbound_order.sql` | `app.wms_outbound_orders`, `app.wms_outbound_order_lines` — the confirmed outbound demand contract |
| WMS Picking (`236`) | `20260730240000_create_advanced_tms_wms_picking.sql` | Real inventory reservation against outbound demand |
| WMS Packing (`237`) | `20260730250000_create_advanced_tms_wms_packing.sql` | Package/container hierarchy, `app.reparent_wms_package` |
| WMS Outbound / ship execution (`238`) | `20260730260000_create_advanced_tms_wms_outbound.sql` | `app.wms_outbound_shipments`, `app.wms_shipment_load_events`, `app.wms_shipment_issue_lines`, `app.wms_billing_eligibility_events` |
| Cycle Count and Inventory Adjustment (`239`) | `20260730270000_create_advanced_tms_cycle_count_adjustment.sql`, `20260730280000_harden_advanced_tms_inventory_balances_reservation_record_version.sql` | Blind-count scope items, reconciling ledger movements |
| Label and Barcode Operations (`240`) | `20260730290000_create_advanced_tms_label_barcode_operations.sql` | Governed label templates, `app.resolve_label` (never itself an authorization — reauthorizes from the live subject row) |
| Warehouse Billing Events (`241`) | `20260730300000_create_advanced_tms_warehouse_billing_events.sql` | `app.warehouse_billing_rate_components`, `app.warehouse_billing_events`, `app.warehouse_billing_handoffs` — zero writes to any Finance schema table |
| Customer Inventory Access Contract (`242`) | `20260730310000_create_advanced_tms_customer_inventory_access.sql`, `20260730311000_harden_customer_inventory_access_rls_isolation.sql`, `20260730312000_add_customer_inventory_access_pagination_indexes.sql` | `app.evaluate_customer_inventory_access`, `app.resolve_customer_owner_account_scope`, 10 read/export RPCs — see `STEPS_11_14_HANDOFF_CONTRACT.md` §4 |
| Shipment Tracking Health writer (`243`) | `20260730320000_create_advanced_tms_shipment_tracking_health_writer.sql`, `20260730330000_harden_advanced_tms_background_job_claim_index.sql` | `app.recalculate_shipment_tracking_health`, `app.reconcile_shipment_tracking_health`; per-vehicle/per-shipment `pg_advisory_xact_lock` concurrency fix |
| Advanced Claim and Incident Operations (`244`) | `20260730340000_create_advanced_tms_claim_incident_operations.sql` | 8 child tables (`claim_case_extensions`, `claim_items`, `claim_evidence_links`, `claim_investigation_findings`, `claim_responsibility_reviews`, `claim_recovery_records`, `claim_settlement_readiness_evaluations`, `claim_settlement_readiness_handoffs`) extending Operations' own `app.operational_exceptions` |
| Integrity and Security Hardening (`246`) | `20260730350000_harden_advanced_tms_third_party_hybrid_tracking.sql`, `20260730360000_harden_advanced_tms_device_driver_mobile_tracking.sql`, `20260730370000_correct_route_planning_staleness_tolerance_comment.sql` | The 24-hour future-timestamp bound, the column-scoped webhook-secret grant, cross-tenant IMEI collision rejection, `app.deregister_gps_device`, live consent recheck — see §7 |

**Advanced TMS/WMS Integrated Verification (`245`) introduces zero migration** — verification-only, per its own mandate, exactly mirroring `FIN-215`'s own precedent. **Documentation and Handoff (`247`, this checkpoint) introduces zero migration** — documentation-only, per its own mandate.

### 3.2 Application code (`app/`, `server/`, `services/`)

- **`server/contracts/<capability>/`** — Zod schemas for every one of the ~26 Phase 5 capabilities' own public shape.
- **`server/queries/`/`server/mutations/`** — typed client wrappers per capability, the same two-client (`authenticated` RLS-scoped vs. `service_role`) architecture every prior phase established.
- **`app/(tenant)/[tenantSlug]/operations/{dispatch-board,fleet,fleet-control-tower,route-load-planning,...}/`, `app/(tenant)/[tenantSlug]/admin/tracking/`, and the full WMS portal tree** — Server Component pages behind `resolveOperationsAccessForRequest`/`resolveTenantAdminAccessForRequest`, Server Actions in a sibling `actions.ts`.
- **`app/(public)/tracking/[token]/page.tsx`** — the deliberately-anonymous, pre-authentication customer tracking route (`OPS-180`, widened at `226C`/`226H`/`228`).
- **`app/api/tracking/driver-mobile/route.ts`** — the first real HTTP API route this repository built (`226C`); a `service_role`-backed webhook-shaped endpoint, never a REST/GraphQL resource surface. **No REST/GraphQL live route exists anywhere in this repository for any domain** — unchanged since Platform Core (`PLT-130` remains contract/logging infrastructure only), independently re-confirmed at `ATW-023`'s own checkpoint.
- **`services/gps-gateway/`** — the standalone, always-on Node process speaking raw TCP Teltonika Codec 8 Extended (`src/codec8e.ts`, `src/buffer.ts`, `src/server.ts`, `src/ingestClient.ts`, `src/logger.ts`, `src/health.ts`), with its own independent `package.json`/test suite. See the sibling agent's own deployment/operations guides (§1 table above) for full detail — not duplicated here.

### 3.3 Verification and hardening evidence (`scripts/db-tests/`, 133 files repository-wide)

97 pre-Phase-5 files (unchanged since Finance's own close) plus **36 Phase 5 `.sql` db-test files** (of 133 total repository-wide, independently re-counted this checkpoint via direct `ls`), plus one non-`.sql` helper script (`wms-picking-concurrency-helper.sh`, used by `advanced-tms-wms-picking.sql`'s own two-process race proof). Notable cross-cutting files: `advanced-tms-wms-integrated-verification.sql` and `advanced-tms-wms-critical-path-verification.sql` (`245`, `ATW-026`'s own continuous cross-capability golden-path proofs) and `advanced-tms-gps-telematics-integrated-verification.sql` (`226I`'s own re-derived deployment/observability/load/security/outage/recovery evidence). All passing together against one disposable, sequentially-migrated Postgres+PostGIS database in a single `pnpm run db:test` invocation — **137 migrations, 133 test files, ALL PASSED**, independently re-run twice fresh from a clean shell at `ATW-027`'s own checkpoint (`ATW-027.md` §6/§7).

`scripts/load-tests/` (`ATW-024`) is the real concurrency/recovery harness — `app.post_inventory_movement` under 13,866+ concurrent posts, WMS pick/putaway claiming, `app.claim_next_job` under a 5,000-job backlog, real TCP/Codec 8 Extended GPS ingestion, hybrid arbitration under 59,534 real concurrent conflicting-source transactions (`ISS-2026-009`'s own TOCTOU fix), and a genuine client `SIGKILL` plus Postgres cluster restart proving idempotent recovery. Coverage gaps against Prompt 243's own five named load-target profiles are honestly disclosed as `ISS-2026-019` (§5 below), not silently claimed complete.

### 3.4 Ratified ADRs

Exactly **one** new ADR was ratified during Phase 5: `docs/adr/ADR-0019-canonical-item-sku-and-uom-master-identity.md` (`CG-S10-ATW-011A`) — a flat-column `app.item_masters` table (mirroring `ADR-0018`'s own identical structural choice for `app.accounts`, for the identical reason: a shared `app.master_records` unique index has no owner dimension and could never let two different 3PL customers hold the same SKU code), mandatorily owned by `app.accounts` via `owner_account_id`, plus a governed UOM registry (`app.uoms`/`app.uom_conversions`) mirroring `app.finance_currencies`/`app.validate_currency_code` verbatim. Every other Phase 5 architectural decision (RLS/RBAC evaluation pattern, audit trail, the reused Configuration Engine, the `app.master_records`/`app.resource_assignments` reuse, the `has_active_tenant_membership` RLS baseline) reused an already-ratified ADR directly rather than re-deciding anything — see `docs/adr/README.md` §6.

## 4. The downstream handoff contracts

See the dedicated companion document: **`docs/build-log/phase-05/STEPS_11_14_HANDOFF_CONTRACT.md`** — covering all four downstream steps in one file, mirroring `docs/build-log/phase-02/JOB_ORDER_HANDOFF_CONTRACT.md`'s own structure:

- **§2 — Step 11 (Phase 6, Procurement/Vendor)**: deliberately not integrated — `app.wms_inbound_orders.source_type`'s own closed enum names the exact, deliberate boundary ("Future PO/vendor linkage does not implement Step 11 lifecycle," Prompt 231 §24).
- **§3 — Step 12 (Phase 7, HRIS/Ticketing)**: no relationship built, disclosed rather than omitted, with the one plausible future touchpoint (`app.driver_operational_profiles`) named honestly as undecided.
- **§4 — Step 13 (Phase 8, Customer Portal/Loyalty)**: the one real, shipped, read-only contract — the full 13-RPC surface from `ATW-023` (Customer Inventory Access), the `ISS-2026-010` pre-launch audit obligation stated as the launch trigger it actually is, and the customer-safe tracking projection (`fleet-control-tower-and-customer-projection-guide.md` §2).
- **§5 — Step 14 (Phase 9, Intelligence/Enterprise)**: no relationship built, disclosed rather than omitted, with Phase 5's own dashboard-shaped reads named as natural future analytics inputs, no contract designed.

**The exact first eligible Phase 6 prompt and its own contingency:** `Prompt 250` (`CG-S11-PRC-001`, `CG-AABPP-PRC-250`, "Procurement/Vendor WBS and Runtime Kickoff," `docs/ai-agent-build-prompt-package/11-phase-06-procurement-vendor/250_PROCUREMENT_VENDOR_WBS_RUNTIME_KICKOFF_PROMPT.md`). Its own mandatory entry gate states the contingency explicitly, in its own words: *"Stop with `PHASE_6_BLOCKED` unless one active checkpoint proves `RUNTIME_DISCOVERY_VERIFIED`, `RUNTIME_ARCHITECTURE_VERIFIED`, `PHASE_0_VERIFIED`, `PHASE_1_VERIFIED`, `PHASE_2_VERIFIED`, `PHASE_3_VERIFIED`, `PHASE_4_VERIFIED` and `PHASE_5_VERIFIED`."* As of this checkpoint, `PHASE_5_VERIFIED` is not yet set (§2 above) — Prompt 250 remains correctly blocked until Prompt 248 sets it. Prompt 250's own closing line names the identical downstream discipline this package itself follows: *"Do not set `PHASE_6_VERIFIED`; only Prompt 271 may do that."* (`docs/ai-agent-build-prompt-package/11-phase-06-procurement-vendor/249_PROCUREMENT_VENDOR_README.md` is the equivalent of this repository's own `219_ADVANCED_TMS_WMS_README.md` — informational, not itself an atomic task.)

## 5. Known issues carried forward (from `docs/runtime/KNOWN_ISSUES.md`, current state as of this checkpoint)

Read `docs/runtime/KNOWN_ISSUES.md` directly for the authoritative, current list — this table is a snapshot, not a substitute.

| ID | Status | Severity | What it is | Relevance to Phase 5/6/7/8/9 |
|---|---|---|---|---|
| `ISS-2026-005` | `OPEN` | Low | `CHANGE_MANIFEST.md` gap for historical Prompts 83–90 | Repository-wide documentation-completeness gap, predates Phase 5, no Phase 5 code/decision affected |
| `ISS-2026-007` | `OPEN` | Medium | No working automated dependency/supply-chain audit gate (`pnpm audit` endpoint retired) | Repository-wide; `pnpm install --frozen-lockfile` remains the real, working deterministic-install control |
| `ISS-2026-010` | `OPEN` (narrowed) | Medium | ~74 tenant-scoped RLS `SELECT` policies repository-wide (none of them a Phase 5 telemetry table) never individually audited for the same latent `customer_user`-owner-scope-branch pattern `ATW-023` found and fixed on 4 WMS tables | **Blocking the instant a real production `customer_user` principal is granted — Step 13 Customer Portal is the actual trigger.** See `STEPS_11_14_HANDOFF_CONTRACT.md` §4.4 and `privacy-consent-and-retention-guide.md` §4 |
| `ISS-2026-012` | `OPEN` | Low | `app.jobs.job_type`'s valid-list is independently duplicated in 4 places; `224` widened only 2 of 4 when it added `route_load_planning` | Maintainability risk only; no current inconsistency |
| `ISS-2026-013` | `OPEN` | Medium | `app.commit_import_job` (`PLT-131`) never writes to any canonical destination table — no domain has adopted staged import yet | Not exercised by any Phase 5 capability; WMS Inbound (`231`) uses its own bounded bulk-RPC instead, deliberately not this framework |
| `ISS-2026-015` | `OPEN` | Medium | No PWA manifest, service worker, barcode/scanner component, or scheduler/worker runtime exists anywhere in this repository | **Directly relevant** — see `driver-mobile-tracking-guide.md` §1/§7 and `privacy-consent-and-retention-guide.md` §3 (no scheduler means no retention-purge job is even possible today, regardless of the declared `history_retention_days` limit) |
| `ISS-2026-016` | `OPEN` | Medium | `app.register_lot_identity`/`app.register_serial_identity` (`235`) are real and correct but nothing in live receiving capture (`232`) calls them yet | Lot/serial governance is inert against live `232` receiving traffic until a future capability wires it in |
| `ISS-2026-017` | `OPEN` | Medium (repository-wide) | No `SECURITY DEFINER` RPC anywhere (~130 migrations, Phase 5 included) cross-checks `p_actor_auth_user_id` against real `auth.uid()` | Pre-existing, repository-wide; not Phase-5-introduced, not fixed by Phase 5 |
| `ISS-2026-018` | `OPEN` | Low | `app.recalculate_shipment_tracking_health` resolves the authoritative vehicle at the shipment-order level only, never per-leg | Only matters for a multi-leg shipment mid-transit with a vehicle handoff between legs — no current UI distinguishes this case |
| `ISS-2026-019` | `OPEN` | Medium | Prompt 243's own 5 named load-target profiles and 6 test categories: only Direct Device Gateway, Hybrid arbitration, query-plan/index, and DLQ-replay are real load-tested today | Driver Mobile HTTPS/Third-Party Platform as dedicated load profiles, canonical-projection-against-a-live-database, RLS/cache isolation under load, and persisted audit/monitoring infrastructure remain unbuilt |
| `ISS-2026-020` | `OPEN` | Medium | `advanced-tms-lot-batch-serial-expiry.sql` (`235`) has a genuine, pre-existing, non-deterministic tie-break defect in FIFO/FEFO ordering when two lots share an identical `now()`-derived timestamp inside one transaction | A `db:test` flake, not a live production defect; independently confirmed 4 separate times via A/B testing |
| `ISS-2026-024` | `OPEN` | Medium | `advanced-tms-wms-picking.sql` (`236`, already `VERIFIED`) has an intermittent, non-deterministic idempotency-key regression-proof flake (2 of 11 re-runs) | A `db:test` flake in an unrelated, already-`VERIFIED` file; plausible root cause recorded, not yet confirmed |
| `ISS-2026-025` | `OPEN` | Medium | The "salami-slicing" residual on `app.vehicle_source_health.last_location` — see §7 below | **Directly relevant, disclosed in full at `source-arbitration-and-fallback-explanation.md` §4** |
| `ISS-2026-026` | `OPEN` | Low | `getThirdPartyProviderConnection` (`server/queries/third-party-provider-adapter.ts`) does `select("*")` against a table now column-restricted to exclude `webhook_secret_value` — zero live callers today, so no live regression, but would need an explicit column list the moment a real caller is wired up | Directly relevant to any future Phase 5-adjacent work on the third-party adapter |

**Zero Critical or unresolved High-severity issue exists as of this checkpoint** — `ATW-027`'s own adversarial hardening pass repaired all 10 of its 12 live-reproduced findings rated Critical/High/Medium/Low except the two explicitly disclosed, bounded residuals above (`ISS-2026-025`/`026`); `ATW-026`'s own three registered findings (`ISS-2026-021`/`022`/`023`) are all `RESOLVED`.

## 6. Verified environment/commands

```
pnpm install --frozen-lockfile           # deterministic install
pnpm run typecheck                       # tsc --noEmit
pnpm run lint                            # eslint .
pnpm run test                            # node:test, scripts/**|server/**|lib/**|tests/**/*.test.ts
pnpm run test:e2e                        # Playwright + axe-core (sandbox chrome-headless-shell gap, unchanged since PLT-117)
pnpm run db:test                         # bash scripts/db-tests/run.sh -- 137 migrations + 133 test files, disposable DB, requires PostGIS
pnpm run docs:check                      # scripts/docs/check-doc-links.ts
pnpm run security:check                  # scripts/security/check-secrets.ts
pnpm run data-classification:check
pnpm run threat-model:check
pnpm run standards:check                 # scripts/standards/check-suppressions.ts
pnpm run git:check-paths                 # scripts/git/check-protected-paths.ts
bash scripts/load-tests/run.sh           # ATW-024's own concurrency/recovery harness
(cd services/gps-gateway && pnpm run typecheck && pnpm run test)   # the GPS Gateway's own independent gate surface
```

`db:test` requires a reachable Postgres with PostGIS available (`postgresql-<major>-postgis-3` locally; CI uses `postgis/postgis:17-3.4`). Results as independently re-run and re-verified fresh (not accepted on any agent's own self-report) at `ATW-027`'s own checkpoint (`ATW-027.md` §6/§7): `typecheck`/`lint` 0 errors (85 pre-existing warnings, unchanged); root `node:test` **3084/3084**; `services/gps-gateway` package `typecheck` 0 errors, `test` **41/41**; `db:test` **ALL PASSED — 137 migrations, 133 test files**, run twice fresh; `docs:check`/`security:check`/`data-classification:check`/`standards:check`/`git:check-paths` all pass. Do not treat any specific count in this document as durable — read the live gate output at the checkpoint that actually needs it.

## 7. Residual risks Phase 6/7/8/9 should be aware of (not blocking, all already-disclosed across `221`–`247`'s own build logs)

- **`RPD-022`** (Supreme Admin absolute CRUD) — unchanged from every prior phase; no tamper-proof/immutability claim may ever be made anywhere in Phase 5, including the audit trail on canonical telemetry events, source switches, or claim/incident records.
- **No live Supabase project exists anywhere** — unchanged from every prior phase; a real sign-in flow, real RLS-against-a-live-database session, and real deploy pipeline all remain `NOT_RUN`.
- **No live GPS hardware and no live third-party GPS provider have ever existed in this repository, at any checkpoint through `ATW-027`.** Both are formally recorded per Prompt 247's own External-evidence policy in `docs/build-log/phase-05/deferred-physical-device-test-plan-and-provider-evidence-record.md` (the sibling agent's own deliverable) as `DEFERRED_EXTERNAL_HARDWARE_EVIDENCE` (physical Teltonika device) and `CONDITIONALLY_SKIPPED_PROVIDER_UNAVAILABLE` (live third-party platform), exactly matching the disposition every other ATW-226* build log already carried. **Do not claim "tested on physical hardware" or "live with [named provider]" anywhere** — protocol simulators, recorded vendor frames, and deterministic mocks/contract fixtures are what this repository's own real evidence rests on for these two surfaces.
- **The accepted IMEI-spoofing residual risk (`ATW-027` §3.4)**: IMEIs are not secret (printed on the device/box, often externally queryable), so IMEI presentation alone is not, and must never be represented as, device-identity proof. `ATW-027` closed the *concurrent*-connection half of this risk (two simultaneous handshakes with the same IMEI, in-process rejection at `services/gps-gateway/src/server.ts`) but explicitly did not close the *sequential* half: a patient attacker who waits for the real device's own connection to genuinely end can still pass a fresh handshake with a known victim IMEI, and — if the spoofed traffic also respects the existing 200 km/h impossible-movement ceiling — can still inject plausible false telemetry. Fixing this fully would require a real device-level PKI/certificate provisioning scheme, out of reach without physical Teltonika hardware to design or validate against, matching this repository's own External-evidence policy. Recorded as an accepted residual risk in the migration header (`20260730360000`) and `services/gps-gateway/README.md`, requiring compensating network-layer controls — not a repository-controlled defect left unfixed, a genuine hardware-dependent limit.
- **`ISS-2026-025`, the salami-slicing residual** — `app.vehicle_source_health.last_location`/`last_seen_event_at` still update unconditionally on every candidate regardless of arbitration outcome (deliberate original design: "even a rejected report is real evidence the source is alive"). An already-credentialed source can still walk the same-source impossible-movement baseline through a sequence of individually-plausible, always-losing position deltas, without ever needing a future-dated timestamp. Full account: `source-arbitration-and-fallback-explanation.md` §4. Medium severity, no confirmed live exploit chain, a bounded fix direction is already recorded in the issue itself.
- **The remaining `ISS-2026-010` scope** (§5 above) is the single most important item for Step 13 (Customer Portal) specifically to re-verify before granting the first live `customer_user` membership.
- **No REST/GraphQL surface exists for Phase 5, or any domain, anywhere in this repository yet** — `PLT-130` remains contract/logging infrastructure only. Whichever phase first wires a domain REST/GraphQL route must re-verify every anti-enumeration/masking/rate-limiting behavior this package describes against that route's own real serializer — none of it is guaranteed to survive a naive pass-through.
- **Tracking-package limits (`max_tracked_vehicles`/`max_mobile_sessions`/`history_retention_days`) are declared, not enforced** — see `subscription-package-and-entitlement-guide.md` §4. Do not represent any of the three as a hard cap in customer-facing material.
- **No retention/purge job exists for any Phase 5 telemetry table** — see `privacy-consent-and-retention-guide.md` §3. The ratified `RPD-025` retention schedule is a real product/legal decision with no corresponding enforcement mechanism in Phase 5's own code today.
- **`pnpm run test:e2e` has the same persistent, disclosed sandbox condition** since `PLT-117` — `chrome-headless-shell` executable missing. Verification for every Phase 5 UI capability is `next build` plus `next dev` + `curl` reachability probing, never a claim of full visual/interactive verification.

## 8. Corrections made this checkpoint (disclosed, not hidden)

None found. This checkpoint's own read-back of `docs/adr/README.md` §6, `docs/runtime/KNOWN_ISSUES.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md` §3, and every cited Phase 5 build log found no stale citation, no missing evidence link, and no orphaned reference that this checkpoint's own bounded documentation-only scope is authorized to fix — `ATW-026`'s own integrated-verification checkpoint and `ATW-027`'s own hardening sweep already independently confirmed the schema/docs were internally consistent immediately before this checkpoint, and nothing in the underlying evidence changed in the interim. One genuine drafting correction was made and independently re-verified *during this checkpoint's own authoring*, disclosed here rather than silently smoothed over: an early draft of `driver-mobile-tracking-guide.md` §8 cited a nonexistent db-test file name (`advanced-tms-device-driver-mobile-tracking.sql`); direct inspection of `scripts/db-tests/` found the real file the `ATW-027` consent-recheck coverage actually extends (`advanced-tms-driver-mobile-tracking.sql`, confirmed by grepping its own `ATW-246` findings-4-and-5 assertions), and the citation was corrected before this document was written.

## 9. Forbidden-scope confirmation (Prompt 247 §12/§24, re-checked this checkpoint)

This checkpoint touched zero migration, service, API, or UI file — confirmed by `git status` immediately before writing this document (only new files under `docs/build-log/phase-05/` were added). `STEPS_11_14_HANDOFF_CONTRACT.md` §6 independently re-runs and confirms zero Procurement/HRIS/Ticketing/Customer-Portal-UI/Intelligence-Enterprise domain concept exists anywhere in application code — not reproduced a second time here.

## 10. Fresh-context reconstruction check (Prompt 247 §21, rehearsed this checkpoint)

Reading only this document plus its cited paths (no other session context), an agent can determine: what phase the repository is in (Phase 5, pending Prompt 248 closure), what exists on disk (§3), the exact contracts Phase 6/7/8/9 must consume and how (§4, `STEPS_11_14_HANDOFF_CONTRACT.md`), what is decided vs. still open (§3.4, §5, §7), what commands verify the current state (§6), what the exact next prompt is once Phase 5 formally closes (Prompt 250, `CG-S11-PRC-001` — currently `PHASE_6_BLOCKED` on `PHASE_5_VERIFIED`, per §4), and what residual risks/design boundaries to respect rather than "fix" without re-reading history first (§7). This satisfies Prompt 247 §21's "readers start at one index, choose role/use case, follow versioned procedures, and reach exact diagnostics/recovery."
