# PRC-268 — Procurement/Vendor Integrated Verification

**Prompt ID:** `CG-S11-PRC-019` · **Package document:**
`docs/ai-agent-build-prompt-package/11-phase-06-procurement-vendor/268_PROCUREMENT_VENDOR_INTEGRATED_VERIFICATION_PROMPT.md`
· **Status at end of this checkpoint:** `VERIFIED` — with findings triaged and assigned, per
this prompt's own §25 ("a properly triaged-and-assigned Critical/High finding, handed to the
Hardening prompt with full reproduction, is the intended, correct outcome"). This is a
standalone, docs-only verification checkpoint — it runs its own review, finds and registers
defects, and fixes none of them except one trivial, zero-risk documentation correction disclosed
in §8. Prompt 269 (Hardening) is the dedicated next task that fixes what this one found.

This build log itself is the synthesis of a 5-lens live-verification pass
(critical-e2e-uat-flow, security-isolation-sweep, finance-operations-regression,
performance-and-migration, spec-anchor-completeness) run against a fully-migrated disposable
Postgres 16 database, preceded by a read-only 17×5-family evidence-matrix synthesis stage. Every
finding those stages produced was independently re-derived by this synthesis pass against the
live schema/database before being accepted — none was carried forward on the strength of another
stage's own report alone (see §6 for the specific re-derivations and their evidence; §7 for the
premise-error corrections independently reconfirmed a second time).

## 1. Scope and governing documents

- `docs/adr/ADR-0022-phase6-integrated-verification-scope-when-optional-portal-is-blocked.md` —
  read in full before starting. Governs why this evidence matrix proceeds over 16 internally-
  scoped `VERIFIED` capabilities plus one disclosed `BLOCKED` row, never silently treating
  `CG-S11-PRC-018` (Prompt 267) as satisfied or omitted.
- `PRC-VND-US-001` (`docs/blueprint/05_CargoGrid_Delivery_Testing_GoLive_Plan.md` line ~432):
  "Procurement dapat register vendor, verify document, approve onboarding... Vendor approved
  only after mandatory docs complete."
- `FINTEST-016`: "Vendor invoice matched to shipment/PO/cost... Variance flagged; duplicate
  blocked."
- Per §13 ("prefer no production schema change") and §12 ("forbidden: new unplanned capability,
  production-data mutation"), this checkpoint changed **zero** application/schema code. All live
  testing ran against disposable local-only Postgres databases (`prc268_clean`, `prc268_upgrade`,
  `prc268_verify`), each dropped or left disposable at the end of its own use; `git status
  --short` shows a clean working tree except for the two files this checkpoint is authorized to
  write.

## 2. The 17-row × 5-anchor-family evidence matrix

Anchor families per `249_PROCUREMENT_VENDOR_README.md` §4: **PRC-VND-001..004** (vendor
identity/lifecycle), **PRC-ASM-001..004** (assessment/qualification), **PRC-RTE-001..004**
(rate/pricelist), **PRC-SRC-001..004** (sourcing/eligibility/capacity/assignment),
**PRC-POI-001..004** (purchasing/contract/invoice instrument). Reproduced from the read-only
Stage 1 synthesis; independently spot-checked in this pass (§6.5, spec-anchor-completeness lens)
and found accurate — no correction required to the matrix itself.

| # | Capability (Prompt / Task ID) | PRC-VND | PRC-ASM | PRC-RTE | PRC-SRC | PRC-POI | Build log / db-test |
|---|---|---|---|---|---|---|---|
| 1 | 251 Vendor Registration & Onboarding (`CG-S11-PRC-002`) | **Primary** — canonical `app.vendor_profiles` 1:1 extension of `app.master_records`, 8-state lifecycle, 36 RPCs, duplicate review, intake tokens | N/A — assessment is PRC-252's own scope | N/A — no rate table touched | N/A — no sourcing/candidate logic | N/A — no PO/contract | `PRC-251.md`; `procurement-vendor-registration.sql` |
| 2 | 252 Vendor Assessment (`CG-S11-PRC-003`) | Secondary — reads `vendor_profiles.master_record_id` as identity anchor only, never mutates lifecycle | **Primary** — versioned templates, explainable weighted scoring, maker-checker, findings/corrective actions, reassessment | N/A | N/A | N/A | `PRC-252.md`; `procurement-vendor-assessment.sql` |
| 3 | 253 Compliance & Document Expiry (`CG-S11-PRC-004`) | **Primary (dual)** — eligibility-hold projection is a vendor-identity-scoped governance signal | **Primary (dual)** — versioned compliance requirements/waivers | N/A | N/A — deliberately deferred | N/A — deferred | `PRC-253.md`; `procurement-vendor-compliance.sql` |
| 4 | 254 Vendor Banking & Tax Security (`CG-S11-PRC-005`) | **Primary** — sensitive vendor-identity financial master, `payment_term_days` change-control | Secondary — same maker-checker/versioning discipline | N/A | N/A | N/A — no `app.finance_*` write, no settlement | `PRC-254.md`; `procurement-vendor-financial-security.sql` |
| 5 | 255 Vendor Rate & Pricelist (`CG-S11-PRC-006`) | Secondary — `vendor_master_id` FK (ADR-0020), trigger-enforced | N/A | **Primary** — extends `app.vendor_rate_versions`/COM-149 with tiers, overlap `EXCLUDE`, `calculate_vendor_rate`, first PLT-131 import adapter | N/A | N/A | `PRC-255.md`; `procurement-vendor-rate-tiers.sql` |
| 6 | 256 Sourcing (`CG-S11-PRC-007`) | Secondary — read-only eligibility scan | Secondary — composes PRC-253's eligibility, read-only | Secondary — `has_active_rate` informational-only | **Primary** — `sourcing_requests`/`sourcing_candidates`, nine-state lifecycle | N/A — deferred | `PRC-256.md`; `procurement-sourcing.sql` |
| 7 | 257 Procurement RFQ (`CG-S11-PRC-008`) | N/A | N/A | Secondary — `rfq_responses` carry vendor-quoted amount, masked | **Primary (dual)** — invitation scoped to PRC-256's `shortlisted=true`, response capture | Secondary — feeds PO downstream | `PRC-257.md`; `procurement-rfq.sql` |
| 8 | 258 Vendor Comparison (`CG-S11-PRC-009`) | N/A | N/A | **Primary (dual)** — composes `calculate_vendor_rate`+`convert_finance_amount`+`apply_finance_rounding` | Secondary — reads PRC-257's `comparison_eligible=true` | Secondary — reaches `submitted` | `PRC-258.md`; `procurement-vendor-comparison.sql` |
| 9 | 259 Procurement Approval (`CG-S11-PRC-010`) | **Primary (all)** — widens `decide_vendor_profile_review`/`activate_vendor_profile` | Secondary | **Primary (all)** — widens `create_rate_version`/`approve_rate_version` | **Primary (all)** — widens `submit_vendor_comparison_for_approval` | **Primary (all)** — registers `purchase_order`/`vendor_contract` entity types | `PRC-259.md`; `procurement-approval.sql` |
| 10 | 260 Purchase Order (`CG-S11-PRC-011`) | N/A | N/A | Secondary — `normalized_amount` verbatim | Secondary — lineage traces sourcing→RFQ→comparison | **Primary** — PO root/version/number, lines snapshotted from RFQ, PRC-259 wired end-to-end | `PRC-260.md`; `procurement-purchase-order.sql` |
| 11 | 261 Vendor Contract (`CG-S11-PRC-012`) | **Primary (dual)** — standing commitment identity | N/A | Secondary — `rate_version_id` masked commercial term | N/A | **Primary (dual)** — versioned root/version-collapsed, `resolve_effective_vendor_contract` for PRC-263/264/265 | `PRC-261.md`; `procurement-vendor-contract.sql` |
| 12 | 262 Vendor Capacity & Availability (`CG-S11-PRC-013`) | N/A | N/A | N/A | **Primary** — declared quantity/window offers, concurrency-safe `reserve_vendor_capacity` (live two-process-race-proven) | Secondary — optional `contract_id` link, unexposed in UI | `PRC-262.md`; `procurement-vendor-capacity.sql` |
| 13 | 263 Vendor Assignment (`CG-S11-PRC-014`) | N/A | N/A | N/A | **Primary (dual + OPS-TMS)** — extends OPS-172's `app.resource_assignments`, `role='vendor'` | **Primary (dual)** — capacity consumption, contract active-dependency guard | `PRC-263.md`; `procurement-vendor-assignment.sql` |
| 14 | 264 Vendor Performance (`CG-S11-PRC-015`) | Secondary — suspend/blacklist calls straight through to unmodified PRC-251 RPCs | **Primary (dual)** — 11 KPI calculators, source-reconciled scorecards, maker-checker | Secondary — `rate_competitiveness` reads `vendor_rate_versions.base_amount` | Secondary — `acceptance_rate`/`response_time` read PRC-263 | **Primary (dual)** — reads PO/contract/assignment as scorecard inputs | `PRC-264.md`; `procurement-vendor-performance.sql` |
| 15 | 265 Vendor Invoice Matching (`CG-S11-PRC-016`) | N/A | Secondary — feeds `invoice_accuracy` into PRC-264's KPI catalogue | Secondary — reads `vendor_rate_versions` for rate-evidence | N/A | **Primary (+ FIN-AP)** — match-case workflow vs. canonical Phase 4 vendor bill, zero `app.finance_*` writes confirmed (§6.3) | `PRC-265.md`; `procurement-vendor-invoice-matching.sql` |
| 16 | 266 Procurement Dashboard & Reports (`CG-S11-PRC-017`) | **Primary (all)** — status/risk/compliance-expiry summary | **Primary (all)** — performance-band summary, reuses PRC-264's scores | **Primary (all)** — rate validity/competitiveness | **Primary (all)** — RFQ cycle-time, capacity/acceptance | **Primary (all)** — PO/contract + match-variance (reuses PRC-265's `get_vendor_bill_match_reconciliation_status` verbatim) | `PRC-266.md`; `procurement-vendor-dashboard-reports.sql` |
| 17 | **267 Optional Vendor Portal (`CG-S11-PRC-018`) — `BLOCKED`, not built** | Intended primary | Not applicable | Intended | Intended | Intended | **BLOCKED — no code, migration, schema, service, or UI exists.** `docs/build-log/phase-06/PRC-267.md` §1 reproduction (`grep -rlE "portal-surface\|four-layer\|external.*identity.*membership\|membership.*ADR" docs/adr` → no matches) independently re-run this checkpoint (§6.5) and confirmed unchanged. Recorded per `ADR-0022`: `BLOCKED`, not `VERIFIED`, not omitted. |

Cross-cutting: every capability's own build log confirms, by direct grep, that no REST/GraphQL
surface exists anywhere in Phase 6 — a repository-wide, not capability-specific, scope boundary,
independently re-confirmed this checkpoint (`grep -c` against `app/api/` returns only the two
pre-existing, unrelated Advanced-TMS webhook routes named by the performance-and-migration lens).

## 3. Critical end-to-end UAT flow — step-by-step proof

Executed live in one continuous psql session against a fresh disposable Postgres database
(`prc268_clean`, all 180 migrations applied cleanly with zero error as a precondition —
independently re-confirmed in this synthesis pass, §6.1). One real tenant, one real vendor (PT
UAT Ocean Freight), real least-privilege distinct actors throughout (staff/approver/manager/
finance/viewer — no self-approval anywhere in the chain), the SAME identifiers (master_record_id,
rate_version_id, PO id, contract id, capacity reservation id, assignment id, actual-cost id,
ePOD capture id, bill id) threaded from one step's real output into the next step's real input.

**`PRC-VND-US-001` — "Vendor approved only after mandatory docs complete" — PROVED both ways:**

1. Vendor registered and activated with an incomplete mandatory blocking compliance requirement
   → `app.vendor_compliance_status.eligibility_hold = true`, confirmed by direct query.
2. Same vendor, same requirement, document uploaded → scanned-clean → submitted → verified →
   `eligibility_hold` flips to `false` on the **same row**, confirmed by direct query. No gap.

**Full chain, step-by-step, real return values (not merely "did not error"):**

| Step | Capability | Real evidence produced |
|---|---|---|
| Registration | 251 | vendor `active`; `eligibility_hold=false` |
| Assessment | 252 | `approved`, `score_band=pass` (91.20) |
| Rate | 255 | rate version `approved`, base 15,000,000 IDR |
| Sourcing | 256 | vendor shortlisted as eligible candidate |
| RFQ | 257 | issued → real response captured (16,000,000 IDR) → `closed` |
| Comparison | 258 | recommended → submitted; a **second** chain deliberately routed through a real published 2-step sequential Approval Engine policy (manager then finance, distinct actors) to genuine `approved` |
| PO | 260 | drafted from the approved comparison → submitted → issued → acknowledged (`PO-2026-000001`, 16,000,000 IDR, `vendor_master_id` matches) |
| Contract | 261 | drafted (linking the real approved `rate_version_id`) → submitted → `active` |
| Capacity | 262 | offer published → reservation `held` → `accepted` |
| Assignment | 263 | invitation proposed (referencing the real PO id AND the real capacity reservation id) → accepted → confirmed — correctly auto-resolved PRC-261's currently-effective contract via `app.evaluate_vendor_assignment_eligibility`, consumed the capacity reservation inline, produced a real `app.resource_assignments` row (`role=vendor`, `is_current=true`) |
| Shipment lifecycle | OPS-172/178/177 (unmodified) | `confirmed → planned → assigned → dispatched → in_transit → delivered` |
| Actual cost / ePOD | OPS-178/177 (unmodified) | real `app.shipment_actual_costs` (16,000,000 IDR, `approved`) + a real completed `app.epod_captures` row (signature evidence, `reviewed`+`completed`) against the same shipment |

**`FINTEST-016` criterion 1 — "Variance flagged" — PROVED LIVE:** a real Finance vendor bill
(FIN-200, `prepare_finance_vendor_bill_from_actual_cost`) capturing a genuinely different
vendor-stated amount (16,300,000 IDR) against the real actual-cost evidence (16,000,000 IDR)
correctly computed `variance_amount=300,000`/`variance_status=requires_approval` on the bill row
itself. A PRC-265 match case against this bill AND the real PO
(`match_mode=po_three_way`, `has_epod_evidence=true`, confirming the real ePOD capture was
picked up) correctly computed `total_variance_amount=300,000.00` and forced
`overall_status=exception`/`readiness_status=not_ready`. `app.request_vendor_bill_match_
exception_approval` succeeded without error.

**`FINTEST-016` criterion 2 — "Duplicate blocked" — PROVED LIVE**, freshly composed into this
real chain: a second real shipment/actual-cost/bill with the SAME vendor/currency/total_amount
(16,000,000 IDR, different `vendor_reference`, same `bill_date`) correctly triggered
`is_duplicate_flagged=true`, `duplicate_of_case_id` pointing at the real first match case,
`overall_status` forced to `exception`. Positively confirmed the block is real: calling
`app.accept_vendor_bill_match_within_tolerance` against the duplicate-flagged case was correctly
rejected (`invalid_transition`) — the duplicate genuinely cannot clear to `ready_for_finance`.

**Cross-tenant isolation spot-check:** a genuinely separate second tenant/actor called
`app.get_purchase_order` against tenant-1's real PO id (correctly `purchase_order_not_found`, no
row disclosed) and `app.get_vendor_bill_match_readiness` against tenant-1's real bill id while
claiming tenant-2 (confirmed via direct follow-up query: zero rows, not the real cross-tenant
data). Note for the record: one probe's own first-pass assertion in the live-verification pass
was miswritten (treating "did not raise" as an automatic violation without checking the returned
row count) and produced a spurious notice in that pass's raw run log; this synthesis independently
re-checked the underlying claim (the RPC itself correctly returns zero rows, not cross-tenant
data) and confirms it is a defect in that pass's own verification harness, not in the product —
**not** carried forward as a registered finding.

**Migration-application precondition:** applying all 180 `supabase/migrations/*.sql` files to a
fresh disposable database, in order, with `ON_ERROR_STOP=1`, completed with zero error —
independently re-run in this synthesis pass (§6.1), not merely accepted from the prior stage's
report.

**Outcome: PRC-VND-US-001 and FINTEST-016 both proved end-to-end, live, against one continuous
real chain of evidence. No step substituted stale/cached state. No gap in the critical flow.**

## 4. Finance/Operations regression evidence

- Full `db:test` suite (149 top-level `*.sql` files under `scripts/db-tests/`) independently
  re-run fresh this checkpoint via
  `DATABASE_ADMIN_URL="postgresql://postgres:postgres@127.0.0.1:5432/postgres" pnpm run db:test`
  at commit `4115904` — see §9 for the exact result of this checkpoint's own gate run.
- `commercial-rate-cost-lookup.sql` (COM-149/150), `finance-vendor-bill.sql` (FIN-200),
  `finance-accounts-payable.sql` (FIN-199), `operations-resource-assignment.sql` (OPS-172),
  `operations-actual-cost.sql` (OPS-178), `operations-epod-capture-review.sql` (OPS-177) —
  confirmed unmodified since their own pre-Phase-6 commits via `git log --follow`, each re-run
  standalone against the full current 180-migration schema, all pass clean.
- Independently re-confirmed this checkpoint (§6.3): zero `INSERT`/`UPDATE` against
  `app.finance_vendor_bills`/`app.finance_vendor_bill_lines` anywhere in the 22 Phase 6
  migrations (`grep -nE "insert into app\.finance_vendor_bill|update app\.finance_vendor_bill"`
  → no matches); zero `ALTER TABLE` against `app.resource_assignments` or
  `app.shipment_actual_cost_components` in any Phase 6 migration (the only matches for those two
  table names are their own pre-Phase-6 creation migrations, dated 2026-07-27/28). `app.vendor_
  rate_versions` (COM-149, Phase 2) is the one canonical table Phase 6 does extend — purely
  additive (4 new nullable columns, 1 scoped `EXCLUDE` constraint, 1 not-null-with-default
  governance column), already reviewed and accepted under `ADR-0020`.
- The specific defect class named in the task (COM-only actor calling the PRC-255-widened
  `app.select_vendor_rate`) was live re-tested by the finance-operations-regression lens: an
  all-null-trailing-params call succeeds unchanged for a pre-existing COM-only caller; supplying
  the new `p_weight` parameter as the same zero-`PRC:*`-permission actor correctly raises
  `insufficient_privilege` — the widened function's documented promise holds live, not merely as
  a code comment.

## 5. Migration / rollback / performance evidence

- **Clean install:** 180 migrations applied to a fresh database in strict filename order,
  `ON_ERROR_STOP=1`, zero error — independently re-confirmed this checkpoint (§6.1).
- **Phase 5→6 upgrade rehearsal:** migrations 1–157 applied (last Phase 5 =
  `20260730570000_wire_lot_serial_identity_registration_into_receiving.sql`), intermediate state
  verified (Phase 2/3/4/5 canonical tables present with their Phase-6-only columns confirmed
  absent; zero Phase 6 procurement/vendor tables present), then migrations 158–180 applied on
  top with zero error. Final `app`-schema table count identical between the two-stage upgrade
  path and the one-shot clean-install path (392 tables, both).
- **Additive-only confirmed by direct grep** of all 23 Phase 6 migration files: zero `drop
  column`, zero `drop table`, zero bare `drop`/`truncate` against any pre-Phase-6 object; zero
  `ALTER TABLE` against any of the 9 canonical pre-existing roots
  (`app.master_records`, `app.resource_assignments`, `app.shipment_actual_cost_components`,
  `app.finance_vendor_bills`/`finance_ap_open_items`/`finance_settlements`,
  `app.vehicle_capacity_reservations`, `app.warehouses`/`app.warehouse_zones`, `app.files`,
  `app.document_types`, `app.jobs`) except the one disclosed, purely additive
  `app.vendor_rate_versions` extension.
- **Rollback doctrine** (`docs/git/GIT_STRATEGY.md` §5.1 — forward-fix preferred, no down
  migrations exist by design): structural review, not a live-executed rollback. Because every
  Phase 6 migration is additive-only against every pre-Phase-6 table, a forward-fix migration
  disabling a Phase-6 RPC's behavior never needs to drop a pre-existing table to restore
  pre-Phase-6 behavior. No Phase 6 migration is fundamentally unrollback-safe in the forward-fix
  sense.
- **Target-volume performance:** a synthetic 25-tenant × 200-vendor (5,000 identity) scale was
  seeded with proportional downstream chains (15,000 rate versions, 1,250 each of sourcing
  request/RFQ/comparison/PO chains, 12,500 vendor contracts). `app.search_vendor_rates`,
  `app.list_rfqs`, and both paths of `app.list_purchase_orders` all use a real, tenant-scoped
  covering index with zero seq scan at this scale. **One confirmed finding** — see §6.4 — on
  `app.list_vendor_contracts`.
- Independently re-ran the official `pnpm run db:test` harness against the same 180-migration set
  via its own disposable-database mechanism: `ALL PASSED`.

## 6. Findings — independently re-derived and CONFIRMED this checkpoint

Every finding below was independently re-verified against either the live, fully-migrated
`prc268_verify`/`prc268_clean` database, a fresh `pnpm run security:audit` run, or direct
`grep`/file inspection performed in this synthesis pass — not accepted on the strength of the
live-verification lenses' own reports alone. No finding was rejected; all six survive independent
re-derivation, described below with the specific commands/queries this pass itself ran.

### 6.1 [HIGH] Task-framing premise error: "Zero OPEN Critical/High" claim is false at this checkpoint

**Owner:** repo-wide `KNOWN_ISSUES.md` discipline / next `package.json` touch (for the two CVEs);
dedicated currency-threshold design task (for the approval threshold).

Independently re-confirmed by directly reading `docs/runtime/KNOWN_ISSUES.md`'s own §3 index
this checkpoint:

```
$ awk 'NR>=66 && NR<=76' docs/runtime/KNOWN_ISSUES.md | awk -F'|' '{print $2, "|SEV:", $4, "|STATUS:", $5}'
 `ISS-2026-042`  |SEV: High ...  |STATUS: `OPEN`
 `ISS-2026-045`  |SEV: High       |STATUS: `OPEN`
 `ISS-2026-051`  |SEV: High ...  |STATUS: `OPEN`
```

Three OPEN High-severity entries exist: `ISS-2026-042` (js-yaml CVE-2026-59870), `ISS-2026-045`
(currency-blind procurement approval threshold, ~15,000× USD/IDR skew live-confirmed at its own
original checkpoint), `ISS-2026-051` (nanoid CVE GHSA-2v37-7h3g-55p8). Independently re-ran
`pnpm run security:audit` in this synthesis pass:

```
✖ 2 blocking dependency vulnerability/vulnerabilities (critical/high):
    high js-yaml >=4.0.0 <4.3.1 -> >=4.3.1  (CVE-2026-59870)
    high nanoid <3.3.17 -> >=3.3.17          (GHSA-2v37-7h3g-55p8)
```

— unchanged, no new advisory. None of the three is a new discovery; all were registered with
owner/reproduction/workaround before this checkpoint. The task brief's own precondition claim
does not match the repository as it stands. Per §33's "only owned non-blocking residual issues
remain," these three require an explicit disposition statement here (§8), not silent
pass-through. Separately re-confirmed the companion premise-error claim about the C-23 taxonomy
class: `docs/standards/RECURRING_DEFECT_TAXONOMY.md` §4 (C-23) cites its own originating evidence
as Prompt 260 alone ("two instances"), not "PRC-265 and PRC-266" as an earlier framing implied —
independently re-read and confirmed accurate as stated in the Stage 1 synthesis.

### 6.2 [MEDIUM] C-05 tenant-id-disclosure oracle confirmed live in ≥10 by-id RPCs spanning PRC-251/252/253/254/256

**Owner:** PRC-251/252/253/254/256 (`CG-S11-PRC-002/003/004/005/007`) — same repair pattern
`ISS-2026-043`/`048` already established.

Independently re-derived two ways in this synthesis pass, not merely accepted from the
live-verification lens's own report:

1. **Structural sweep**, live against `prc268_verify` (all 180 migrations applied): pulled
   `pg_get_functiondef` for `suspend_vendor_profile`, `get_vendor_assessment_score_breakdown`,
   `list_vendor_assessment_findings`, `list_vendor_assessment_template_criteria`,
   `get_vendor_compliance_requirement`, `get_vendor_compliance_waiver`,
   `get_vendor_compliance_document`, `decide_vendor_compliance_waiver`,
   `get_vendor_payment_term_proposal`, `get_sourcing_request_history`,
   `list_sourcing_candidates`, `list_vendor_assessment_corrective_actions`. Every one of the 12
   resolves the target row's real `tenant_id` into a local variable, THEN calls
   `app.evaluate_permission`, and on denial raises
   `'insufficient_authority: identity % lacks PRC:... for tenant %'` with that real `tenant_id`
   interpolated — before any check that the caller belongs to that tenant. `app.get_sourcing_
   request` itself (the one already-disclosed `ISS-2026-043` instance) reconfirmed unchanged;
   none of the other 11 appear in `ISS-2026-043`/`048`'s own enumerated disposition text.
2. **Live reproduction**, freshly built by this pass (not merely re-quoted from the
   live-verification lens): seeded a real tenant, a real `app.document_types` row, and a real
   `app.vendor_compliance_requirements` row in `prc268_verify`; created an `auth.users` identity
   with **zero** `principal_memberships` rows anywhere; simulated its authenticated session
   (`set local request.jwt.claims`) and called `app.get_vendor_compliance_requirement` against
   the real requirement id:
   ```
   ERROR: insufficient_authority: identity 00000000-0000-0000-0000-0000000c0502 lacks PRC:View
   (no_active_assignment) for tenant 11111111-1111-1111-1111-111111111111
   ```
   The real tenant_id is disclosed verbatim to a caller who is not a member of that tenant or any
   tenant — the exact oracle `ISS-2026-043`/`048` already describe, independently reproduced here
   rather than merely re-cited.

Widens `ISS-2026-043`/`048`'s already-registered pattern all the way back to the phase's very
first, unbatched, pre-`ADR-0021` capability (PRC-251). The two write-path instances tested
(`suspend_vendor_profile`, `decide_vendor_compliance_waiver`) correctly blocked the actual
state-changing action — only the tenant_id disclosure is the defect, never a
privilege-escalation-to-write.

### 6.3 [LOW] `vendor_profiles` lifecycle-transition write path discloses real `record_version` before the authority check

**Owner:** PRC-251 (`CG-S11-PRC-002`) — same hardening pass as §6.2.

Independently re-derived by direct `pg_get_functiondef` inspection of `app.suspend_vendor_
profile` this checkpoint:

```sql
if v_profile.record_version <> p_expected_version then
  raise exception 'stale_version: vendor profile % expected version % but found %', ...;
end if;
...
v_decision := app.evaluate_permission(p_actor_auth_user_id, v_profile.tenant_id, 'PRC', 'Override');
```

The `stale_version` check runs and can raise, disclosing the row's real current
`record_version`, strictly before `evaluate_permission` is ever called. A much lower-value oracle
than §6.2 (a numeric version counter, not a tenant/business-data value), and the same
authorization-check-ordering class already disclosed at LOW severity for the Platform Approval
Engine (`ISS-2026-049`). Confirmed by code inspection only in this pass, matching how the
live-verification lens itself flagged it — not independently live-executed as a separate test
case, since the exact same code path was already exercised live in §6.2's reproduction and the
ordering is unambiguous from the function body.

### 6.4 [MEDIUM] `app.list_vendor_contracts` lacks the `(tenant_id, created_at desc)` covering index its three siblings all have

**Owner:** unclear (a schema-touching hardening task; owning capability PRC-261,
`CG-S11-PRC-012`).

Independently re-derived by direct `pg_indexes` query against `prc268_verify` this checkpoint:

```
vendor_contracts_tenant_vendor_status_idx    (tenant_id, vendor_master_id, status)
vendor_contracts_tenant_contract_number_idx  (tenant_id, contract_number, version_no)
vendor_contracts_tenant_effective_end_idx    (tenant_id, effective_end) WHERE status='active'
```

— no index on `(tenant_id, created_at desc)`, confirmed by exhaustive listing (7 indexes total,
none matching). Confirmed by contrast that its three sibling list-instrument tables in the same
capability family each carry the matching index:

```
purchase_orders_tenant_created_idx
rfqs_tenant_created_idx
vendor_comparisons_tenant_created_idx
```

`app.list_vendor_contracts`'s own internal query is the identical shape
(`... where tenant_id = p_tenant_id ... order by created_at desc limit ...`) to its siblings',
but lacking the matching index, the planner cannot do an ordered Index Scan with early-LIMIT
termination the way `app.list_purchase_orders` does. This synthesis independently confirmed the
structural index gap and the sibling contrast directly against the live schema; it did **not**
independently re-run the live-verification lens's own large-scale skew reproduction (5,100/7,500
rows on one tenant → full Seq Scan) inside this synthesis pass's own time budget — that specific
EXPLAIN output is accepted from the lens's own report as a plausible, standard-Postgres-planner
consequence of the independently-confirmed missing index, not re-executed a second time. The
structural claim (the gap itself, and its inconsistency with 3 sibling tables in the same
capability family) is fully independently confirmed; the precise skew threshold at which it
degrades to a Seq Scan is not.

### 6.5 [MEDIUM] PRC-251 §22 "Bulk-import staged vendors" — undisclosed C-23 gap

**Owner:** `CG-S11-PRC-002` (Prompt 251, Vendor Registration & Onboarding).

Independently re-confirmed this checkpoint, three ways:

1. Source prompt confirmed to name it: `251_VENDOR_REGISTRATION_ONBOARDING_PROMPT.md:100` —
   "Bulk-import staged vendors, request revision, link an intake to an existing canonical vendor,
   or keep self-registration disabled per tenant."
2. Only trace in the codebase: `bulk_import` is an accepted `intake_source` enum value
   (`supabase/migrations/20260730580000_create_procurement_vendor_registration.sql:279,583`) and
   an accepted string in the mirrored TypeScript contract
   (`server/contracts/vendor-profile/vendor-profile.ts:22,444`) — i.e. a caller can tag ONE
   vendor draft as `bulk_import`-sourced via the ordinary single-vendor RPC. No batch/CSV
   ingestion RPC, no staged-multiple-rows table, no commit/review-staged-rows workflow, and no
   PLT-131-style import adapter exists anywhere (`grep -rln "bulk_import|bulkImport" app/
   server/ src/ lib/` returns only the one contract-file enum definition, no pipeline).
3. `docs/build-log/phase-06/PRC-251.md` §8 ("Residual disclosures / not built") independently
   re-read this checkpoint: itemizes nine other real gaps (no REST/GraphQL, no evidence
   attachment in onboarding UI, no token-issuance-history UI, no data-classification entries, no
   new threat-register row, no trigram perf evidence, E2E sandbox-flakiness, the repo-wide WCAG
   defect, true-concurrency-races-unprovable-sequentially) and never names bulk-import.

Same undisclosed-named-requirement shape the taxonomy already tracks twice this phase
(`ISS-2026-046`/`047` for PRC-260, `ISS-2026-050` for PRC-261) — a third, previously-unregistered
instance, in a different capability (PRC-251).

### 6.6 [MEDIUM] PRC-262 §22 "manual confirmation with evidence" — undisclosed C-23 gap

**Owner:** `CG-S11-PRC-013` (Prompt 262, Vendor Capacity & Availability).

Independently re-confirmed this checkpoint, three ways:

1. Source prompt confirmed to name it: `262_VENDOR_CAPACITY_AVAILABILITY_PROMPT.md:100` —
   "Recurring capacity, ad-hoc offer, partial acceptance, blackout, substitution of an eligible
   canonical resource or manual confirmation with evidence."
2. `grep -n "evidence|file_id|attachment|initiate_file_upload"
   supabase/migrations/20260730710000_create_procurement_vendor_capacity.sql` → zero matches, run
   directly by this synthesis pass. No evidence/file-linking mechanism exists on
   `app.vendor_capacity_offers`/`app.vendor_capacity_reservations` at all.
3. `docs/build-log/phase-06/PRC-262.md` §5 ("Residual, disclosed limitations") independently
   re-read this checkpoint: itemizes exactly four items (no recurring-availability, no
   vendor-portal surface, no UI caller for the by-id reservation read, no UI picker for
   resource/contract linkage) — none names the missing evidence-attachment capability.

A fourth C-23 instance registered this phase (`ISS-2026-046`/`047` PRC-260, `ISS-2026-050`
PRC-261, `ISS-2026-052` PRC-265, and now `ISS-2026-057`/`058` for PRC-251/262 — see §8).

## 7. Findings considered and rejected

None. All six findings supplied to this synthesis stage were independently re-derived against
the live schema/database or direct file inspection and confirmed accurate (§6). No
counter-evidence was found for any of them. The one item examined and explicitly **not** carried
forward as a registered finding is the cross-tenant-isolation probe's own harness bug (§3,
"cross-tenant isolation spot-check") — independently re-checked and confirmed to be a defect in
that probe's own assertion logic (treating "did not raise" as an automatic violation without
checking the returned row count), not in the product; the underlying RPC itself correctly returns
zero rows for the cross-tenant case.

## 8. Trivial, zero-risk correction made in this checkpoint

Per this prompt's own instruction ("if you find something so trivially safe to fix that leaving
it un-fixed would be absurd... you may fix it and disclose exactly what and why"): none was found
during this synthesis. No code, migration, or non-target documentation file was edited. The two
files this checkpoint writes (this build log and `KNOWN_ISSUES.md`'s append-only new rows) are
the deliverable itself, not a "fix."

## 9. Disposition table

### Critical/High — MUST be addressed by Prompt 269 before Phase 6 can close

| Issue | Title | Reproduction (this checkpoint) |
|---|---|---|
| `ISS-2026-042` | `js-yaml` CVE-2026-59870 — `security:audit` gate red | `pnpm run security:audit` → exit 1, `high js-yaml >=4.0.0 <4.3.1 -> >=4.3.1` |
| `ISS-2026-045` | Currency-blind procurement approval threshold (`rate_version`/`vendor_selection`/`purchase_order`), ~15,000× USD/IDR skew | Already live-confirmed at its own originating checkpoint (batch 1 Tier C); status reconfirmed `OPEN` in `KNOWN_ISSUES.md` this checkpoint, not re-executed a second time |
| `ISS-2026-051` | `nanoid` CVE GHSA-2v37-7h3g-55p8 — `security:audit` gate red | `pnpm run security:audit` → exit 1, `high nanoid <3.3.17 -> >=3.3.17` |

These three are not new discoveries of this checkpoint — they were already registered with
owner/reproduction/workaround before this checkpoint started. They are elevated to an explicit
disposition here, rather than carried forward silently, because (a) they are Critical/High per
this prompt's own §25 validation rule, and (b) two of the three are presently causing a real gate
(`security:audit`) to report red, which is a stronger claim than a disclosed design gap. Phase 6
cannot correctly claim `PHASE_6_VERIFIED` (Prompt 271) while `security:audit` remains red or
while a live-confirmed ~15,000× currency-blind approval-threshold skew remains unaddressed or
un-accepted-as-risk by an explicit decision — Prompt 269 is the dedicated next task with the
mandate to fix or make an explicit, ratified accepted-risk decision for each.

### Medium/Low — registered in `KNOWN_ISSUES.md`, triage discretion is Prompt 269's / the phase owner's

All pre-existing Medium/Low entries (`ISS-2026-036` through `-041`, `-043`, `-044`, `-046`
through `-050`, `-052`, `-053`) carry forward unchanged — twelve Medium, three Low, all
previously registered with owner/reproduction/workaround, none newly discovered by this
checkpoint.

Five new entries added by this checkpoint (§6.2–§6.6, full detail in `KNOWN_ISSUES.md`):

| New Issue | Severity | Title |
|---|---|---|
| `ISS-2026-054` | Medium | C-05 tenant-id-disclosure oracle, widened to ≥10 more by-id RPCs across PRC-251/252/253/254/256 |
| `ISS-2026-055` | Low | `vendor_profiles` lifecycle-transition write path discloses `record_version` before the authority check |
| `ISS-2026-056` | Medium | `app.list_vendor_contracts` missing `(tenant_id, created_at desc)` covering index |
| `ISS-2026-057` | Medium | PRC-251 "Bulk-import staged vendors" (§22) — undisclosed C-23 gap |
| `ISS-2026-058` | Medium | PRC-262 "manual confirmation with evidence" (§22) — undisclosed C-23 gap |

## 10. Finding disposition counts

- **6 findings supplied** to this synthesis stage by the live-verification pass.
- **6 confirmed** by independent re-derivation in this checkpoint (§6); **0 rejected** (§7).
- **3 Critical/High** — pre-existing, elevated to an explicit obligation for Prompt 269 (§9).
- **5 new Medium/Low** — registered in `KNOWN_ISSUES.md` this checkpoint (`ISS-2026-054`
  through `ISS-2026-058`).
- **18 pre-existing OPEN Medium/Low** entries carried forward unchanged, no silent absorption.

## 11. What this checkpoint did not do

Per §12/§20's own "this prompt does not fix defects, it finds and registers them": no code,
migration, or application file was changed. Per §13 ("prefer no production schema change"): zero
schema change. All findings above are registered for Prompt 269 (Hardening) to fix, with full
reproduction, owner, and resume point — the intended, correct outcome of this prompt per its own
§25.

## 12. Next eligible task

`CG-S11-PRC-020` (Prompt 269, Hardening) — upstream dependency "PRC-268 `VERIFIED` with findings
triaged and assigned" is satisfied by §9/§10 above: 3 Critical/High findings with explicit
reproduction and owner, 5 new Medium/Low findings registered in `KNOWN_ISSUES.md`, 0 unconfirmed
claims carried forward as blockers.
