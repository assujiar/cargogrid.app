# PRC-267 — Optional Vendor Portal

**Prompt ID:** `CG-S11-PRC-018` · **Package document:** `CG-AABPP-PRC-267` · **Status at end of this checkpoint:** `BLOCKED` — no code written, no migration, no schema/service/UI change. This is a documentation-only checkpoint recording why the task cannot start, per its own §9 upstream dependency and `249_PROCUREMENT_VENDOR_README.md` §5.

## 1. Why this task is blocked

Prompt 267 §9 (Upstream dependencies) states explicitly: *"PRC-251..266; explicit Platform identity/membership ADR and all internal source contracts."* Prompt 267 §16 (Security impact) restates it: *"Resolve external vendor identity within approved four-layer Platform model/ADR."* `249_PROCUREMENT_VENDOR_README.md` §5 states the same rule at package level: *"Do not invent a fifth access layer. External vendor identity/scope must use an approved Platform membership/portal-surface ADR and remain isolated from Layer 4 customer data. If that runtime ownership is unresolved, block the vendor-portal task while internal procurement continues."*

**Reproduction — no such ADR exists in this repository at this checkpoint:**

```
$ grep -rlE "portal-surface|four-layer|external.*identity.*membership|membership.*ADR" docs/adr
(no matches)
$ ls docs/adr | grep -iE "membership|portal|external.*ident"
(no matches)
```

`docs/adr/` contains ADR-0001 through ADR-0021 at this checkpoint (confirmed by directory listing); none establishes a Platform external-party membership/portal-surface model. `50_PROCUREMENT_VENDOR_EXECUTION_INDEX.md`'s own row for `CG-S11-PRC-004` (Compliance and Document Expiry, Prompt 253, already `VERIFIED`) recorded the identical gap at that checkpoint (§9 there: *"No external vendor portal task is `READY` until identity/invitation/scope/revocation are proven without a fifth role layer — `PRC-267` is out of 250–255 scope"*) — this checkpoint confirms the gap is still open, not newly discovered.

This is consistent with, not a deviation from, the established pattern in this build: Prompt 253's kickoff-time note already anticipated this exact block, and the Phase 6 execution index has carried "blocked until a Platform membership/portal-surface ADR resolves external identity ownership — internal Procurement is never blocked by this" since the row-264-265 checkpoint.

## 2. What was and was not done

- **Not done:** any schema, service, API, or UI work toward the vendor portal itself. Attempting to build a "temporary" or "internal-only" vendor identity/membership layer to unblock this task would itself violate Prompt 267's own binding rule (§24: *"Do not create a fifth access layer or clone the Customer Portal; use an approved Platform external-party membership model/ADR"*) and the package-level rule in `249_*.md` §5. There is no safe partial implementation.
- **Done:** confirmed every other Phase 6 internal capability (`CG-S11-PRC-002..017`, Prompts 251–266) does not depend on the vendor portal — `249_*.md`'s own dependency table (§4) shows Prompt 267 downstream of 251–266, never upstream of any internal capability, so this block has zero effect on internal Procurement, matching the README's own explicit guarantee.
- **Recorded** `docs/adr/ADR-0022-phase6-integrated-verification-scope-when-optional-portal-is-blocked.md`, resolving the literal tension this creates for Prompt 268 (Integrated Verification), whose own §"Upstream dependencies" reads *"PRC-251..267 all `VERIFIED`"* — a precondition Prompt 267 can now never satisfy while the ADR remains unresolved. `docs/ai-agent-build-prompt-package/00-control/04_CONFLICT_REGISTER.md` is itself a `FORBIDDEN` path for a runtime agent (`docs/git/GIT_STRATEGY.md` §4: "Decision-change protocol only"), so the resolution is recorded in the permitted `docs/adr/` mechanism instead — an initial direct-edit attempt against the register was caught by this checkpoint's own `git:check-paths` gate before commit and reverted; a future decision-change-protocol pass may add a corresponding `CON-016` register row citing this ADR, mirroring how `CON-015` cites `ADR-0021`. See `ADR-0022` for the full resolution: Prompt 268 proceeds over the 16 internally-scoped, `VERIFIED` capabilities (`CG-S11-PRC-002..017`) plus this disclosed, reproducibly-blocked row; it does not proceed by silently treating 267 as out-of-scope or as satisfied.

## 3. Resume condition

This task becomes `READY` the moment a Platform membership/portal-surface ADR is ratified, resolving external vendor identity within the existing four-layer access model without a fifth layer. That ADR is a Platform-level decision outside this task's own allowed-files scope (`docs/build-log/phase-06/`, `docs/adr/ADR-0020-*` only) and outside this checkpoint's operator authorization, which named Prompts 266–270, not a new Platform ADR. No further action is taken on `CG-S11-PRC-018` this checkpoint.

## 4. Gates

Docs-only, no code changed. `pnpm run docs:check` re-run clean after this file and `ADR-0022`. No other Tier A gate is applicable (no migration, no test, no build-relevant file touched).
