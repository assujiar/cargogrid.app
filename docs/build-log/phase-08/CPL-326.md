# CPL-326 — Customer Portal and Loyalty Documentation Handoff

**Prompt ID:** `CG-S13-CPL-028` · **Package document:** `docs/ai-agent-build-prompt-package/13-phase-08-customer-portal-loyalty/326_CUSTOMER_PORTAL_LOYALTY_DOCUMENTATION_HANDOFF_PROMPT.md` · **Phase:** 8 (Customer Portal and Loyalty) · **Package version:** `0.14.0`

**Status: `VERIFIED`** — see §9 (Verdict) for the full rationale. This is the third of Phase 8's four closure prompts; it does **not** set `PHASE_8_VERIFIED` (reserved for Prompt 327).

## 1. Checkpoint

| Field | Value |
|---|---|
| Repository root | `/home/user/cargogrid.app` |
| Branch | `claude/eksekusi-prompt-299-327-khz22n` |
| HEAD at start of this checkpoint | `86ed226` (unchanged — this checkpoint's own edits are staged, uncommitted, in the working tree; HEAD moves only when the orchestrating session commits) |
| Migrations before this checkpoint | 264 |
| Migrations after this checkpoint | 264 (zero new migrations — this is a documentation-only checkpoint per source spec §13/§14/§15's own "None planned" mandate) |
| `scripts/db-tests/*.sql` files | 194 (unchanged) |
| Upstream dependency | CPL-325 `VERIFIED` (`docs/build-log/phase-08/CPL-325.md`) |
| Task type | Documentation, traceability, runbook, role-guide, UX/accessibility/performance-evidence, and handoff-package closure. Four independent parallel lenses ran against the same checkpoint (traceability-reconciliation; runbooks-and-role-guides; ux-accessibility-performance; handoff-package-and-step14-boundary). This document is the synthesis: every distinct claim from every lens was independently re-confirmed by this synthesizing session via direct file/code/schema read before being trusted, per this checkpoint's own explicit charter — never accepted on a lens's own word alone. |

## 2. Scope discipline

Per source spec §11/§12/§13: no planned feature schema except minimal registered defect repair; no new unplanned capability; no edit to an already-applied migration file; `docs/build-log/phase-08/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md`, and `docs/runtime/HANDOFF.md` are the orchestrating session's own files, never edited by this checkpoint. Every repair made this checkpoint (§5) is documentation-only — an additive `docs/runtime/KNOWN_ISSUES.md` entry/correction, a one-cell correction plus a visible supersession blockquote in `docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md` (mirroring `CPL-324`'s own identical-shaped precedent), or a new section/table-row addition to this checkpoint's own three companion documentation artifacts. Zero migration, zero route, zero RPC, zero contract, zero `server/`, `app/`, `lib/`, or `scripts/db-tests/` file was touched.

## 3. Method — four lenses, independently re-verified

**3.1 traceability-reconciliation** — re-verified CPL-324's own 24-capability × 36-anchor matrix remains current; confirmed the 5 new CPL-325 migrations exist on disk exactly as named; confirmed migration/db-test counts; found the `KNOWN_ISSUES.md` §3 index-table/§4 narrative divergence and the `ISS-2026-130` header/body contradiction; ranked a 5-item defect register.

**3.2 runbooks-and-role-guides** — authored `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_RUNBOOKS.md` (six scenario runbooks) and `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_ROLE_GUIDES.md` (five role guides), each citing real RPCs/tables/migrations/`ISS-2026-1xx` entries.

**3.3 ux-accessibility-performance** — read every Customer Portal screen and admin Loyalty route directly; ran a repository-wide grep sweep for fake/dead actions (zero hits); confirmed RPD-004 compliance (zero offline/PWA claims); found two genuine, previously-undisclosed evidence gaps (zero accessibility-audit evidence for any Customer Portal/admin-Loyalty route; zero load/performance-test evidence for any Phase 8 route/RPC) plus one informational UX observation (nav overflow).

**3.4 handoff-package-and-step14-boundary** — authored `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_HANDOFF_PACKAGE.md` (§1 navigation, §2 verified dependencies, §3 preserved assets, §4 Step 14 boundary, §5 known issues, §6–9 gates/corrections/scope/reconstruction check), reading every `CG-S13-CPL-001..027` row fresh from `TASK_LEDGER.md`/`00_EXECUTION_INDEX.md`.

**This synthesis pass's own independent re-verification** (not accepted from any lens's own word alone): read all three companion documents (`CUSTOMER_PORTAL_LOYALTY_RUNBOOKS.md`, `CUSTOMER_PORTAL_LOYALTY_ROLE_GUIDES.md`, `CUSTOMER_PORTAL_LOYALTY_HANDOFF_PACKAGE.md`) in full, confirmed each is substantive and matches its own claimed summary; independently re-derived migration count (264), Phase 8 migration count (32), and `scripts/db-tests/*.sql` count (194) directly via `ls`; independently re-read every line cited in lens 1's own defect register at its exact quoted location before trusting it (§4 below); independently re-verified every load-bearing claim in lens 3's own report by direct grep/read (e2e spec files, CI workflow, `pagination-explain.sh`'s real target functions vs. CPL-309's real function, `eslint.config.js`'s plugin list, `customer-portal-nav.tsx`'s own className and link count, zero PWA/offline hits, zero fake-action hits) rather than trusting the lens's own claim; independently ran all four gate commands fresh (§7).

## 4. Ranked, deduplicated defect register

Escalation check applied per this checkpoint's own governing instruction (cross-tenant/cross-account leak, balance/liability manipulation, or double-spend → Critical regardless of original label): **none of the findings below meets that bar** — every one is a documentation-accuracy, documentation-completeness, or evidence-completeness gap with zero runtime/security/ledger effect.

| # | Finding | Severity | Source | Disposition |
|---|---|---|---|---|
| 1 | `docs/runtime/KNOWN_ISSUES.md:828` (as originally cited by lens 1; line 832 after this checkpoint's own earlier §5.4 insertion shifted it — same entry either way) — `ISS-2026-130`'s own `###` header still read `OPEN, Medium` though the same entry's body (and its own inline resolution table) already correctly stated `RESOLVED` (2026-08-20, CPL-325) — independently re-confirmed at the exact cited line before trusting the lens's claim; sibling entries `ISS-2026-128`/`131`/`136` (which carry mixed item-level status) had already correctly updated their own headers, `130` was the one overlooked | Medium | Lens 1 | **FIXED** — §5.1 |
| 2 | `docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md:112` — the `RPD-022` row's own WBS capability ID(s) cell read `LYL-316..323`, not a valid WBS capability ID prefix anywhere in this repository (Phase 8's Loyalty capabilities are numbered `CPL-316..323`; `LYL` is the entitlement-module code for the same capabilities' permission actions, a genuinely different namespace) — independently re-confirmed by direct read of the exact cited line and cross-checked against every other Loyalty row in the same document (all of which correctly use `CPL-3xx`) before trusting the lens's claim | Low | Lens 1 | **FIXED** — §5.2 |
| 3 | `docs/runtime/KNOWN_ISSUES.md` `ISS-2026-125` (index row `:87`, narrative `:770`) still asserts the MFA/RPD-023 gap is "already disclosed identically at CPL-300 §9 and CPL-314" — `ISS-2026-138` (registered at CPL-325) already grep-confirmed `docs/build-log/phase-08/CPL-314.md` contains zero MFA/RPD-023 mentions and named this exact correction as its own recommended fix, but the correction was never applied to `ISS-2026-125`'s own text — independently re-confirmed this checkpoint (`grep -ic "RPD-023\|MFA\|step-up" docs/build-log/phase-08/CPL-314.md` → `0`) before trusting the lens's claim | Low | Lens 1 | **FIXED** — §5.3 |
| 4 | `docs/runtime/KNOWN_ISSUES.md` §3 "Issue index" master table stops at `ISS-2026-125` (line 87); none of `ISS-2026-126` through the highest-numbered entry in §4 has an index-table row, only a §4 narrative entry — independently re-confirmed by direct line-range read (`§3` ends line 87/88, `§4` begins line 89) before trusting the lens's claim; pre-existing structural drift, not introduced by CPL-325 | Low | Lens 1 | **FIXED** — §5.4 |
| 5 | `docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md` §5.9 tags only `LYL-PNT-001..004` `ACCEPTED_RISK` (the RPD-022 ledger exception); `LYL-PRG-001..004`/`LYL-RDM-001..004` remain plain `COVERED` even though CPL-325's own Supreme-Admin-override trigger now also covers ledger tables those two families own (`app.loyalty_earning_events` under `LYL-PRG`'s `CPL-316`; `app.loyalty_reward_stock_reservations`/`app.loyalty_redemption_events` under `LYL-RDM`'s `CPL-320`/`321`) | Informational, not a confirmed defect | Lens 1 | **CONSIDERED, NOT APPLIED** — §5.5 (rationale below) |
| 6 | Zero automated accessibility-audit evidence exists for any of Phase 8's ~30 Customer Portal routes or 9 admin Loyalty routes (independently re-confirmed: 9 admin Loyalty route directories, 21 top-level `customer-*` route directories, directly `find`-counted this checkpoint) — a real, CI-wired Playwright+`@axe-core/playwright` harness exists but has only ever scanned synthetic fixtures and two unauthenticated public routes (`/login`, `/vendor-intake/*`); root cause is the absence of a live Supabase auth backend in this sandbox, a repository-wide constraint | Medium (Customer Portal) / Low (admin Loyalty) | Lens 3 | **DISCLOSED** — `ISS-2026-140` (new), §6 |
| 7 | Zero load/performance-test evidence exists for any Phase 8 route or RPC at any declared target volume — no `k6` (or equivalent) exists anywhere in the repository; the one same-shaped `pagination-explain.sh` script targets pre-Phase-8 `app.list_customer_inventory_balances` (ATW-023, Phase 5, `20260730310000`), independently re-confirmed by direct name comparison to be a genuinely different function from CPL-309's real `app.list_customer_portal_inventory_balances` (`20260801100000`) | Medium | Lens 3 | **DISCLOSED** — `ISS-2026-141` (new), §6 |

## 5. Repairs made this checkpoint (bounded, additive/correction-only)

### 5.1 `ISS-2026-130` header/body contradiction (Medium → Fixed)

`docs/runtime/KNOWN_ISSUES.md` (line 828 at the time lens 1 read it; now line 832, after this same checkpoint's own §5.4 scope-note insertion earlier in the file shifted everything below it down by 2 lines — the identical `ISS-2026-130` entry either way) — the entry's own `###` header corrected from `... OPEN, Medium)` to `... \`RESOLVED\` 2026-08-20 at CPL-325, was Medium)`, mirroring the `RESOLVED at HRT-294, was Medium` style already used by `ISS-2026-113`/`114` elsewhere in the same file. The entry's own body and inline resolution table were already correct and untouched.

### 5.2 `14_REQUIREMENT_PHASE_TRACEABILITY.md` RPD-022 row (Low → Fixed)

`docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md:112` — the `RPD-022` row's `LYL-316..323` cell corrected to `CPL-316..323`. A visible supersession blockquote was added immediately after the RPD table (before §5), per this directory's own `git:check-paths` convention and mirroring `CPL-324`'s own identical-shaped correction of the `CPT-CX-001..004` row in §5.8 of the same document (which uses the identical "edit the cell, add a `> **Correction (date, checkpoint, task-id):**` blockquote naming the prior value and the fix" pattern).

### 5.3 `ISS-2026-125` false citation (Low → Fixed)

`docs/runtime/KNOWN_ISSUES.md` — an append-only correction note added immediately after `ISS-2026-125`'s own existing "Not fixed here" paragraph (never deleting or rewriting the original text, per this file's own "do not delete resolved issues; mark RESOLVED/SUPERSEDED" maintenance rule, applied here to a citation fix). The note states the CPL-314 citation is false, cites `ISS-2026-138`'s own prior finding of the same fact, and names the correct disclosure lineage (CPL-300 §9 plus CPL-301–313/315, never CPL-314 or CPL-316–323).

### 5.4 `KNOWN_ISSUES.md` §3 index-table scope note (Low → Fixed)

`docs/runtime/KNOWN_ISSUES.md` — a `> **Scope note (2026-08-20, CPL-326, \`CG-S13-CPL-028\`):**` blockquote added immediately after the §3 table (before §4), stating the table's own rows stop at `ISS-2026-125`, that §4 is the authoritative complete list, and that silence in §3 on an ID ≥126 is not evidence of non-existence or resolution. Smallest-footprint fix per lens 1's own recommendation — no attempt to retroactively backfill 14+ index rows.

### 5.5 Item 5 (informational) — considered, not applied

Extending the `ACCEPTED_RISK` tag from `LYL-PNT-001..004` to `LYL-PRG-001..004`/`LYL-RDM-001..004` was evaluated and deliberately **not** applied this checkpoint. Rationale: `LYL-PRG-001..004` maps to `CPL-316..319` in the same document's own table — a range that includes `CPL-317` (Membership Tier), whose own append-only ledger table (`app.loyalty_account_tier_movements`) is specifically **not** covered by CPL-325's Supreme-Admin-override mechanism (`ISS-2026-137`, still `OPEN`). Uniformly tagging `LYL-PRG` `ACCEPTED_RISK` (the RPD-022 exception) would imply the override exists uniformly across that family's own tables, which is not accurate for `CPL-317`'s table specifically — doing so risks introducing a new, subtler inaccuracy while fixing an old one. `LYL-RDM-001..004` (`CPL-320..323`) is more clearly a candidate (both `app.loyalty_reward_stock_reservations` and `app.loyalty_redemption_events` are covered), but the document's own family-to-capability mapping is already loose in this section (both `LYL-PRG` and `LYL-PNT` cite the identical `CPL-316..319` range), and a correct, complete fix would also require updating §5's own closing tally line ("3 families `ACCEPTED_RISK`... 12 IDs" → a different count) — a wider ripple than this optional, informational item's own bounded-repair authority justifies within this checkpoint. Left for a future architecture-doc pass with the bandwidth to reconcile the whole §5.9 family-mapping precisely, rather than a partial fix that could read as more authoritative than it is.

### 5.6 Companion documentation deliverables — verified, one widened

`docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_RUNBOOKS.md` (201 lines, `wc -l`) and `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_ROLE_GUIDES.md` (143 lines, `wc -l`) were read in full this checkpoint and independently confirmed substantive, accurate, and matching their own claimed summaries — no repair needed.

`docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_HANDOFF_PACKAGE.md` (originally 285 lines) was read in full, confirmed substantive and accurate, then widened this checkpoint:
- New §5.3 ("UX state coverage, accessibility, and performance evidence") folding in lens 3's own findings — states-coverage summary, fake/dead-action finding (none), RPD-004 compliance verdict (COMPLIANT), the ePOD/Document-Center download nuance, the nav-overflow UX observation (independently re-counted: 16 links, not 15), the accessibility-evidence summary, and the performance-evidence summary.
- §5.1's own table widened with two new rows, `ISS-2026-140`/`141`.
- §5's own opening citation range widened from `115, 117–139` to `115, 117–141`.
- A new §10 ("Synthesis-checkpoint addendum") disclosing exactly what this synthesis pass added on top of the original lens-authored §1–9, so a future reader does not mistake the addendum's own facts for something the original lens draft already knew.

## 6. Disclosures (additive, `docs/runtime/KNOWN_ISSUES.md`)

- **`ISS-2026-140`** (new) — zero automated accessibility-audit evidence for Phase 8's ~30 Customer Portal routes / 9 admin Loyalty routes. Medium (Customer Portal) / Low (admin Loyalty), `OPEN`. Owner: a future e2e-environment-enablement task (root cause — no live Supabase auth backend reachable from Playwright in this sandbox — is a repository-wide constraint outside this checkpoint's authority).
- **`ISS-2026-141`** (new) — zero load/performance-test evidence for any Phase 8 route/RPC at declared target volume. Medium, `OPEN`. Owner: a future dedicated performance-testing task extending the existing `pgbench`/`EXPLAIN` harness convention.
- Item 5 above (§4/§5.5) — considered, explicitly not applied, rationale disclosed rather than silently dropped.

## 7. Runtime gates — re-run fresh this checkpoint

| Gate | Command | Result |
|---|---|---|
| Typecheck | `pnpm run typecheck` | **0 errors** |
| Lint | `pnpm run lint` | **0 errors / 307 warnings** — matches the CPL-325 baseline exactly |
| Unit/integration tests | `pnpm run test` | **4824/4824 passing**, 0 failing — matches the CPL-325 baseline exactly |
| Database tests | `bash scripts/db-tests/run.sh` | **194/194 files, `ALL PASSED`**, fresh disposable database, all 264 migrations applied cleanly, exit code 0 |

No estimate or prior-checkpoint number was assumed to still hold — all four gates were run fresh this checkpoint, after every repair in §5, against the current working tree. (Local environment note: this sandbox's PostgreSQL 16 service was not running at the start of this checkpoint's own gate pass — `service postgresql start` plus setting the `postgres` role's password to match `scripts/db-tests/run.sh`'s own default `DATABASE_ADMIN_URL` convention was required before the database gate could run at all; this is a one-time local environment fix, not a repository change.)

## 8. Acceptance criteria (source spec §33) disposition

- "Every Phase 8 capability and 36 anchor has current source-to-runtime evidence or explicit blocker." **MET** — unchanged from CPL-324/325 (no capability/anchor ownership altered this checkpoint); lens 1's own independent re-verification (§3.1 above) found the matrix still fully current.
- "Customer Portal flows prove account/site isolation and source-domain ownership." **MET** — unchanged from CPL-324/325; this checkpoint touched no runtime code and introduced no new scope claim.
- "Loyalty flows prove ledger exactness, fraud/approval, expiry, redemption and liability reconciliation." **MET** — unchanged from CPL-324/325; this checkpoint's own new disclosures (`ISS-2026-140`/`141`) concern accessibility/performance evidence, not ledger correctness.
- "Tenant/field/file/API/job/migration/performance gates execute without suppression." **MET for the four gates this checkpoint's own charter names** (§7) — all four re-run fresh, none skipped or suppressed. **The performance-gate evidence itself remains genuinely absent for Phase 8's own routes/RPCs** (§4 Finding 7, `ISS-2026-141`) — disclosed rather than silently claimed satisfied; source spec §30's broader "replay/load... reconciliation commands" list is unchanged from CPL-325's own last-recorded state, not newly regressed by this checkpoint.
- "No unresolved critical/high blocker may advance; findings are ranked and owned." **MET** — zero Critical or High finding this checkpoint (§4); every finding is ranked, and every disposition (fixed, disclosed, or considered-and-declined) is recorded with an explicit owner or rationale.

This is not a production, pilot, GA, or market-ready claim (source spec §24, RPD-034/036).

## 9. Verdict

**Status: `VERIFIED`.** Every claim from every one of the four lenses was independently re-verified by this synthesizing session via direct file/code/schema read rather than accepted on a lens's own word (§3). All three companion documentation deliverables (runbooks, role guides, handoff package) were read in full, confirmed substantive and accurate, and one (the handoff package) was widened with a dedicated UX/accessibility/performance section per source spec §20/§31's own explicit mandate. Four of lens 1's five ranked defect-register items were independently re-confirmed accurate at their exact cited locations and fixed with bounded, additive, documentation-only repairs (§5.1–5.4); the fifth (optional/informational) was evaluated and deliberately not applied, with the reasoning disclosed rather than silently dropped (§5.5). Two genuine, previously-undisclosed evidence gaps lens 3 surfaced (zero accessibility-audit evidence, zero load/performance-test evidence for Phase 8) were registered as new, owned `KNOWN_ISSUES.md` entries rather than silently folded into an existing one (§6). All four regression gates (typecheck, lint, test, db:test) were run fresh this checkpoint, after every repair, with real output — not estimated or assumed from CPL-325's own prior numbers — and all four are clean or match the last-known-good baseline exactly.

**Why not `BLOCKED`:** no Critical or High finding exists anywhere in this checkpoint's own scope; every gate re-run fresh this checkpoint passed; no cross-tenant/cross-account leak, balance/liability manipulation, or double-spend was found or introduced (this checkpoint touched zero runtime code).

**Why not `PARTIALLY_COMPLETE`:** every item in lens 1's own ranked defect register reached a definite, disclosed disposition this checkpoint (four fixed, one deliberately declined with rationale) — none was left open pending a future pass within this checkpoint's own bounded scope. The two new evidence gaps lens 3 found are, by their own nature, not fixable within a documentation-only checkpoint's authority (one requires a live Supabase auth backend the sandbox does not have; the other requires authoring a capability-sized load-test suite) — both were disclosed with a named future owner exactly as source spec §35 requires, which is this checkpoint's own correct and complete handling of a genuinely out-of-scope gap, not an unresolved defect forcing a lesser status.

## 10. Rollback/recovery note

Last trusted checkpoint before this one: HEAD `86ed226` (unchanged — this checkpoint's own file changes are staged/unstaged in the working tree, not yet committed by the orchestrating session). Every change this checkpoint made is documentation-only and additive/correction-shaped (new files, new sections, new table rows, or a corrected cell plus a visible supersession blockquote preserving the prior value in the historical record) — if any repair in §5 needs to be reverted, `git checkout -- <file>` per file reverts cleanly with zero migration, schema, or runtime-code impact. No destructive git or database operation was used or is required. The one local-environment action taken (`service postgresql start`, setting the `postgres` role's password) affects only this sandbox's own disposable local Postgres service, never a shared or persistent database, and requires no rollback.

## 11. Files changed this checkpoint

- `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_RUNBOOKS.md` (new — lens 2, independently re-verified this checkpoint, no repair needed)
- `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_ROLE_GUIDES.md` (new — lens 2, independently re-verified this checkpoint, no repair needed)
- `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_HANDOFF_PACKAGE.md` (new — lens 4, widened this checkpoint with §5.3, two new §5.1 table rows, an updated citation range, and a new §10 addendum)
- `docs/runtime/KNOWN_ISSUES.md` (`ISS-2026-130` header corrected; `ISS-2026-125` correction note appended; §3 index-table scope note appended; `ISS-2026-140`/`141` newly registered)
- `docs/architecture/14_REQUIREMENT_PHASE_TRACEABILITY.md` (`RPD-022` row's `LYL-316..323` → `CPL-316..323`, plus a supersession blockquote)
- `docs/build-log/phase-08/CPL-326.md` (this file)

Not touched (per this task's own explicit instruction): `docs/build-log/phase-08/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md`, `docs/runtime/HANDOFF.md` — updating these remains the orchestrating session's own job.
