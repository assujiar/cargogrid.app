# Customer Portal and Loyalty (Phase 8) Closure Report

**Prompt ID:** `CG-S13-CPL-029` · **Package document:** `docs/ai-agent-build-prompt-package/13-phase-08-customer-portal-loyalty/327_CUSTOMER_PORTAL_LOYALTY_CLOSURE_VERIFICATION_PROMPT.md` · **Phase:** 8 (Customer Portal and Loyalty) · **Package version:** `0.14.0`

**Status: `PHASE_8_VERIFIED`** — see §17 (Closure state and rationale) for the full reasoning. This is the fourth and final of Phase 8's four closure prompts, and the ONLY prompt authorized to set `PHASE_8_VERIFIED`.

## 1. Checkpoint

| Field | Value |
|---|---|
| Repository root | `/home/user/cargogrid.app` |
| Branch | `claude/eksekusi-prompt-299-327-khz22n` |
| Migrations at start of this checkpoint | 264 (`20260801320000` = last — CPL-325's own ticket-attachment file-privacy fix) |
| Migrations after this checkpoint | 264 (zero new migrations — this is a closure-verification checkpoint with no planned-feature-schema authority) |
| `scripts/db-tests/*.sql` files | 194 (unchanged) |
| Upstream dependency | CPL-326 (`CG-S13-CPL-028`, Documentation and Handoff) `VERIFIED` — `docs/build-log/phase-08/CPL-326.md`; `00_EXECUTION_INDEX.md` row for `CG-S13-CPL-029` reads `READY — dependency-clean, next eligible; never batched; the only task authorized to set PHASE_8_VERIFIED` |
| Task type | Independent SYNTHESIS re-verification of four parallel lenses (WBS/traceability/evidence-completeness/closure-boundary; Customer Portal scope and security re-confirmation; Loyalty ledger, reward/fraud, liability re-confirmation; cross-cutting compliance/non-functional/REST-GraphQL-parity), covering the source spec's own 42-item "Required verification" checklist, followed by this synthesis pass's own independent re-derivation of the most consequential claims — never accepting any lens's claim on its word alone, mirroring CPL-324/325/326's own established precedent. |

## 2. Method — this synthesis pass's own independent re-verification

Four lenses ran in parallel against the same checkpoint and reported in full (verbatim summaries retained by the orchestrating session). This synthesis pass did not accept any lens's claim as given. It independently:

1. Read `scripts/data-classification/registry.ts` in full and extracted every distinct `id:` prefix directly (`env`, `fin`, `hrs`, `prc`, `tkt` — zero `cpl`/`lyl`), confirming lens 4's own "NEW-1" retention-classification finding firsthand rather than trusting the lens's own text.
2. Grepped all 32 Phase 8 migrations (`supabase/migrations/20260801*.sql`) for `retention`/`legal_hold` directly and confirmed the single non-binding comment hit at `20260801030000_create_customer_portal_quote_requests.sql:68`.
3. Read `docs/runtime/KNOWN_ISSUES.md`'s own `ISS-2026-091` entry and `docs/build-log/phase-07/HRIS_TICKETING_CLOSURE_REPORT.md`'s own §3 item 34/§19 to independently confirm the Phase 7 disclosed-not-blocking precedent this checkpoint's new finding is modeled on is real, not invented.
4. Independently re-derived, by direct migration grep, that `20260801300000` (redemption hold-race fix) is genuinely the LAST migration touching all four of `app.hold_loyalty_account_tier_benefits`/`app.release_loyalty_account_tier_benefits`/`app.submit_loyalty_redemption`/`app.decide_loyalty_redemption` — no `20260801310000`/`20260801320000` (or any other later file) references any of the four function names.
5. Independently re-derived, by direct migration read, that `20260801280000`'s five Supreme-Admin-override triggers (`loyalty_earning_events_protect_append_only`, `loyalty_point_ledger_entries_protect_append_only`, `loyalty_benefit_entitlement_events_protect_append_only`, `loyalty_reward_stock_reservations_protect_append_only`, `loyalty_redemption_events_protect_append_only`) each appear in exactly one migration file, and zero `DROP TRIGGER` statement exists anywhere in the 32 Phase 8 migrations.
6. Independently re-derived that `app.loyalty_point_balances.available` is a real `numeric generated always as (total_earned - total_consumed) stored` column, and that its only two write statements (one `UPDATE`, one `INSERT`) both live inside `app.post_loyalty_point_ledger_entry`; the underlying table's own direct `INSERT`/`UPDATE` grant is `service_role`-only (`authenticated` cannot write the table directly — it can only reach the function, which is itself RBAC-gated via `app.evaluate_permission(..., 'LYL', 'Edit')`, not left open).
7. Independently re-read every Phase-8-owned `KNOWN_ISSUES.md` entry (`ISS-2026-116`, `118`–`129`, `131`–`141`) at its own current text (not header alone) and confirmed the severity tally lens 1 reported (5 Medium, 19 Low, zero Critical, zero High) by direct count, and separately confirmed the two adjacent, explicitly non-Phase-8-owned entries (`ISS-2026-115`, Phase 7; `ISS-2026-117`, Phase 5/ATW-023) are correctly excluded from that tally.
8. Independently re-read `docs/adr/ADR-0024-phase8-customer-portal-access-and-transport-pattern.md` Part C and confirmed the RPD-033 REST/GraphQL-parity-via-shared-contract-shape decision is honestly disclosed, not a live-GraphQL-server claim.
9. Independently re-read `docs/build-log/phase-08/CPL-324.md`'s own 24-capability × 36-anchor evidence matrix and confirmed it cites real, on-disk migrations/functions for every row — reused rather than rebuilt below (§5), per this checkpoint's own charter (CPL-324's matrix is unchanged since CPL-324, re-confirmed current by lens 1 and by CPL-326's own traceability-reconciliation lens already).
10. Ran all four regression gates fresh, from scratch, this checkpoint (§6) — not estimated or carried forward from CPL-326's own numbers.

No finding contradicting any lens's own conclusion was produced by this independent re-derivation. One genuinely new artifact was produced beyond the four lenses' own reports: `ISS-2026-142` was formally registered in `docs/runtime/KNOWN_ISSUES.md` (§4 narrative only, per the CPL-326-established index-table convention — see §11), since lens 4's own "NEW-1" finding had been verbally reported but not yet committed to the issue ledger.

## 3. Artifact / task / capability / requirement checklist

- **Artifact required:** this file, `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_CLOSURE_REPORT.md` — produced here.
- **Task:** `CG-S13-CPL-029` (Prompt 327), the 29th and final task row of the Phase 8 WBS, the FINAL closure checkpoint for Phase 8, the only prompt authorized to set `PHASE_8_VERIFIED`.
- **WBS reconciliation:** `docs/build-log/phase-08/00_EXECUTION_INDEX.md` rows `CG-S13-CPL-001` through `CG-S13-CPL-028` (Prompts 299–326) independently re-read this checkpoint: `CG-S13-CPL-001` (299, kickoff) `COMPLETED`; `CG-S13-CPL-002`–`CG-S13-CPL-025` (300–323, the 24 capabilities) all `VERIFIED`; `CG-S13-CPL-026`/`027`/`028` (324/325/326, integrated verification/hardening/documentation) all `VERIFIED`. **28 of 29 WBS rows are `VERIFIED`/`COMPLETED`; `CG-S13-CPL-029` (this checkpoint) was the sole `READY` row before this report.**
- **Capability checklist (24 capabilities named in Prompt 327 item 2):** independently re-derived — every one of `customer user scope; portal dashboard; request quotation; booking; shipment order; tracking; shipment monitoring; ePOD; document center; warehouse inventory visibility; warehouse order/fulfillment visibility; invoice/billing visibility; payment visibility; complaint/ticket; customer profile; customer user management; loyalty program/earning; membership tier; points ledger; cashback/discount/voucher; reward catalogue; redemption approval/fulfillment; expiry/fraud prevention; liability/reconciliation analytics` has a real migration (32 Phase 8 migrations, `20260801010000`–`20260801320000`) + service layer (`server/{queries,mutations}/customer-portal-*`, `server/{queries,mutations}/loyalty*`) + UI (21 top-level `customer-*` route directories + 9 `admin/loyalty*` route directories) + a dedicated `scripts/db-tests/*.sql` file (25 files named `customer-*`) + a `docs/build-log/phase-08/CPL-3NN.md` build log, each `VERIFIED`. Full detail §5.
- **Requirement checklist:** all 42 numbered items of Prompt 327's own "Required verification" section, reconciled verbatim against the source prompt file (re-read in full this checkpoint), tabulated in §4.

## 4. Requirement-by-requirement disposition (Prompt 327 items 1–42, verbatim)

| # | Requirement (paraphrased) | Disposition | Evidence |
|---|---|---|---|
| 1 | Verify Prompts 299–326 at one checkpoint; reconcile WBS/dependency/traceability/evidence | `PROVEN` | 28/29 WBS rows `VERIFIED`/`COMPLETED` (§3). Every capability build log cites its own upstream dependency `VERIFIED`. CPL-324's 36-anchor traceability matrix independently re-confirmed current, zero orphan (§5, §2 item 9). |
| 2 | Confirm all 24 capabilities have implementation/migration/UI/tests/docs/owner | `PROVEN` | §3, §5. 32 migrations, 25 `customer-*`-named db-test files (plus loyalty admin coverage), 21 `customer-*` + 9 `admin/loyalty*` route directories, 24 `docs/build-log/phase-08/CPL-3NN.md` build logs each `VERIFIED`. |
| 3 | Confirm all 36 anchors map to durable runtime evidence, no orphan | `PROVEN` | CPL-324's own matrix (reproduced §5) independently re-read this checkpoint: every one of `CPT-QBK-001..004`, `CPT-TRK-001..004`, `CPT-WHS-001..004`, `CPT-BIL-001..004`, `CPT-CX-001..004`, `LYL-PRG-001..004`, `LYL-PNT-001..004`, `LYL-RDM-001..004`, `LYL-ANL-001..004` cites a real, granted, `SECURITY DEFINER` RPC or trigger. "Zero orphans" independently re-confirmed by lens 1 and by this checkpoint's own spot-read. |
| 4 | Prove Customer Portal is Layer 4 only, no fifth access layer | `PROVEN` | Lens 2: `app.resolve_customer_account_scope` is the sole Layer 4 trust root; no fifth principal layer introduced anywhere in the 32 Phase 8 migrations (independently spot-checked, no new `*_layer` construct found). |
| 5 | Prove customer scope is the trust root for every list/detail/action/count/search/saved view/export/cache/notification/signed URL/realtime update | `PROVEN`, with 5 disclosed, pre-existing legacy-resolver exceptions | Lens 2 sampled 6 domains (dashboard, quotation, tracking, ePOD, invoice, ticket) directly against `app.resolve_customer_account_scope`. Exceptions `ISS-2026-117`/`119`/`120`/`121`/`122` are honestly disclosed, pre-existing, correctly labeled Low/Medium (117 is Phase-5-owned). |
| 6 | Prove forged IDs/copied URLs/stale sessions/revoked users/guessed IDs cannot access other customer data | `PROVEN`, with `ISS-2026-116` disclosed non-blocking | Lens 2 confirmed identical anti-enumeration error shape across 3 sampled by-id RPCs. `ISS-2026-116` (distinguishable error shape on 2 specific RPCs) independently re-read: Low, no practical enumeration path (a non-guessable UUID the caller must already possess). |
| 7 | Prove dashboard uses safe projections, no existence leakage via counts/snippets/errors/empty states | `PROVEN`, `ISS-2026-118` disclosed cosmetic gap | CPL-301/CPL-324 `VERIFIED`; `ISS-2026-118` (two stub cards not yet wired to real data) is honest "launching soon" copy, not a fabrication — Low, non-blocking. |
| 8 | Prove request quotation hands off to Commercial without creating approved price truth/margin/duplicate records | `PROVEN` | CPL-302 `VERIFIED`; source-domain ownership boundary independently re-confirmed §7 (zero write to Commercial quotation-truth tables from customer-portal RPCs beyond the request/handoff record itself). |
| 9 | Prove booking is idempotent, file-scanned, schedule-validated, safe reschedule/cancel | `PROVEN` | CPL-303 `VERIFIED`; malware-scan gate re-confirmed uniform (§10, CPL-325 Finding 14). |
| 10 | Prove shipment order visibility is a customer-safe projection, no vendor rates/internal notes/driver PII/cost/prediction | `PROVEN` | CPL-304 `VERIFIED` (defect found and fixed at CPL-324 §7.2, independently re-confirmed still fixed — the fix migration `20260801260000` remains the last touch on the affected function, no later migration reverts it). |
| 11 | Prove tracking is source-derived, timezone-safe, freshness-marked, hides internal events | `PROVEN` | CPL-305 `VERIFIED`. |
| 12 | Prove monitoring alerts are scope-revalidated at emission, idempotent, storm-controlled | `PROVEN` | CPL-306 `VERIFIED`; CPL-325 Finding 15 (idempotency-key replay storm, N=12, 3 RPCs) confirmed safe. |
| 13 | Prove ePOD requires current scope, malware scan, private storage, short-lived signed URL, access audit | `PROVEN` for scope/scan/audit; signed-URL/private-storage is a standing, honestly disclosed repository-wide gap (no live Storage backend exists anywhere in this repository — CPL-325 Finding 11, "CONFIRMED NOT A DEFECT — honestly disclosed at every call site, never falsely claimed") | CPL-307 `VERIFIED`. |
| 14 | Prove document center requires dual scope; unscanned/quarantined files never preview/download/index/email | `PROVEN` | CPL-308 `VERIFIED`; CPL-325 file-privacy-audit lens (Finding 14) confirmed malware-scan gate at every non-clean status. |
| 15 | Prove warehouse inventory visibility is WMS-owned, customer-safe, no other-customer/internal-location leakage | `PROVEN`, `ISS-2026-119` disclosed scope narrowing | CPL-309 `VERIFIED`; WMS-ownership boundary independently re-confirmed §7 (zero mutation of WMS execution tables from any customer-portal RPC). |
| 16 | Prove warehouse order/fulfillment visibility derives from WMS lifecycle, no direct execution-task mutation | `PROVEN`, `ISS-2026-120` disclosed scope narrowing (outbound-only) | CPL-310 `VERIFIED`. |
| 17 | Prove invoice/billing visibility is Finance-owned, cannot edit posted/finalized/journals/GL/tax/margin/period locks | `PROVEN` | CPL-311 `VERIFIED`; Finance-ownership boundary independently re-confirmed §7 (zero write to any `app.finance_*` table from any customer-portal RPC). `ISS-2026-121`/`124` disclosed, Low. |
| 18 | Prove payment visibility is Finance-owned, cannot post/allocate/reverse/reconcile/delete; loyalty earning uses only approved paid eligibility | `PROVEN` | CPL-312 `VERIFIED`; lens 3 confirmed Loyalty only READS `app.finance_ar_open_items` for earning eligibility, zero writes — independently spot-checked §9. |
| 19 | Prove ticket portal uses Phase 7 canonical model, hides internal notes, reauthorizes ticket+source | `PROVEN` | CPL-313 `VERIFIED` (+ `20260801160000` harden migration). Ticket-attachment file-privacy fix (`20260801320000`) independently re-confirmed still the LAST migration in the entire 264-file chain (§2 item 4-adjacent, §6). |
| 20 | Prove profile changes are governed requests/effective versions, no silent overwrite | `PROVEN` | CPL-314 `VERIFIED` (+ CPL-324's `now()`-vs-`clock_timestamp()` ordering fix, `20260801290000`). `ISS-2026-123` disclosed deliberate scope decision, Low. |
| 21 | Prove user management uses Platform identity, MFA/current-auth for high-risk changes, scope preview, invite/revoke/session invalidation, no out-of-scope delegation | **PROVEN for identity/scope-preview/invite/revoke; MFA/current-auth and session/token invalidation are standing, honestly disclosed repository-wide gaps, not built** | CPL-315 `VERIFIED`. `ISS-2026-125` (items 1–2, Low, standing MFA/session-revocation gaps; item 3, Medium, a real "last active account_admin" gap in CPL-300's own already-`VERIFIED` code) and `ISS-2026-138` (Medium, RPD-023 disclosure practice silently dropped for CPL-314/316–323) both `OPEN`, honestly disclosed, non-blocking per the same "disclose absence, never falsely claim built" governing posture RPD-023/RPD-022 both require. |
| 22 | Prove loyalty earning rules are effective-dated, approved, deterministic, source-linked to paid transactions | `PROVEN`, `ISS-2026-126` disclosed (on-demand trigger only) | CPL-316 `VERIFIED`. |
| 23 | Prove tier calculation/upgrades/downgrades/benefits are versioned, explainable, ledger/audit-backed | `PROVEN`, `ISS-2026-127` disclosed | CPL-317 `VERIFIED`. |
| 24 | Prove points balance derives only from ledger events; earning/reversal/adjustment/expiry/redemption/fraud-hold idempotent, auditable | `PROVEN` | CPL-318 `VERIFIED`. This checkpoint independently re-derived: `app.loyalty_point_balances.available` is a real `numeric ... generated always as ... stored` column; its only two write statements both live inside `app.post_loyalty_point_ledger_entry`; the table's direct `INSERT`/`UPDATE` grant is `service_role`-only. |
| 25 | Prove cashback/discount/voucher entitlements are governed records with value caps/eligibility/expiry/reversal/Finance-liability treatment | `PROVEN`, `ISS-2026-129` disclosed (no Finance liability-handoff mechanism yet) | CPL-319 `VERIFIED`. |
| 26 | Prove reward catalogue visibility/eligibility/media/stock/terms are versioned, revalidated at redemption | `PROVEN` | CPL-320 `VERIFIED` (Batch 5 Tier C Critical fix). |
| 27 | Prove redemption prevents double-spend/stock races/stale eligibility/fraud-detail leakage | `PROVEN`, `ISS-2026-139` disclosed (Medium, deliberate design tension) | CPL-321 `VERIFIED` + CPL-325's High-severity TOCTOU hold-race fix (`20260801300000`). This checkpoint independently re-derived `20260801300000` remains the LAST migration touching all four of `app.hold_loyalty_account_tier_benefits`/`app.release_loyalty_account_tier_benefits`/`app.submit_loyalty_redemption`/`app.decide_loyalty_redemption` (§2 item 4). |
| 28 | Prove expiry jobs write ledger events without deleting history, idempotent/replayable/reconciled | `PROVEN` | CPL-322 `VERIFIED`; lens 3 confirmed zero `DELETE` statement anywhere in the Loyalty domain's migrations, independently spot-checked §9. |
| 29 | Prove fraud prevention is governed/rule-based; predictive/AI remains Step 14 | `PROVEN` | CPL-322 `VERIFIED`; zero ML/AI/predictive content found (one explicit "predictive fraud depth remains Step 14" deferral comment, independently spot-checked). `ISS-2026-133` (self-approval structural gap) disclosed, Low. |
| 30 | Prove liability/reconciliation reproduces totals from ledger/config versions, aligns with Finance handoff where configured | `PROVEN`, `ISS-2026-134`/`136` item 2 disclosed | CPL-323 `VERIFIED` + CPL-325's High-severity snapshot-atomicity fix (`20260801310000`). Independently re-confirmed this migration remains the last touch on `app.execute_loyalty_liability_reconciliation_run`, with the prior currency-scope fix (`20260801270000`) still correctly composed inside it (transitively re-derived from §2 item 4's own methodology applied to this function). |
| 31 | Prove Finance remains sole owner of GL/journal/payment/reconciliation; Loyalty does not silently post/mutate Finance truth | `PROVEN` | Independently spot-checked §7/§9: zero `insert`/`update` against any `app.finance_*` table anywhere in the Loyalty domain's migrations; Loyalty only reads `app.finance_ar_open_items`. |
| 32 | Prove RPD-022 residual risk disclosed; normal roles retain ledger/scope/file safeguards; reject immutable-for-all/tamper-proof claims | `PROVEN` | §12. This checkpoint independently re-derived all 5 Supreme-Admin-override triggers from `20260801280000` remain attached, undropped, in exactly one migration file each; zero `DROP TRIGGER` anywhere in Phase 8's 32 migrations. Zero false tamper-proof/immutable-for-all claim found anywhere (grep-confirmed by lens 4, independently spot-checked). |
| 33 | Prove RPD-023 MFA/current-auth on customer-admin, reward approval, fraud release, privileged support, export actions | **DISCLOSED GAP, not built** — honestly and consistently disclosed, not falsely claimed satisfied | No MFA/step-up primitive exists anywhere in this repository (standing, repository-wide, pre-Phase-8 gap). `ISS-2026-125`/`138` both `OPEN`, honest disclosure at the cross-cutting level. `ISS-2026-138`'s own recommended fix item (b) — adding an explicit disclosure line to CPL-314's and CPL-316–323's own individual build logs — was never executed (only item (a), the `KNOWN_ISSUES.md` citation fix, was done at CPL-326); this does not change `ISS-2026-138`'s already-`OPEN` status, independently re-confirmed here rather than silently treated as closed. |
| 34 | Prove RPD-025 retention/legal hold applies to portal documents, invoice/payment documents, ticket files, loyalty ledgers, audit/security evidence | **PARTIAL** — files fully covered; Loyalty ledgers and every other new Phase 8 structured table are NOT | §13. Files are structurally covered via the pre-existing PLT-128 `app.files.retention_class`/`legal_hold` mechanism (independently re-confirmed). Every Phase 8 structured-data table — the 6 append-only Loyalty ledger tables plus every other new quote/booking/tracking/tier-definition/reward-definition table — carries zero `retention_class`/`legal_hold` mechanism, and `scripts/data-classification/registry.ts` carries zero `cpl:`/`lyl:` entry. Newly registered as `ISS-2026-142` this checkpoint (§11, §14), modeled explicitly on the already-accepted Phase 7 precedent `ISS-2026-091`. |
| 35 | Prove RPD-032 every upload is private/malware-scanned across quote/booking/ePOD/documents/invoice support/tickets/reward media | `PROVEN` | Lens 4 PASS across 5 spot-checked file-touching capabilities; CPL-325 file-privacy-audit lens independently confirmed uniform malware-scan gating. |
| 36 | Prove RPD-033 REST/GraphQL parity for record/field/file/job/idempotency/audit/version behavior | `PROVEN` (via honest disclosure, not a live GraphQL server) | `ADR-0024` Part C independently re-read this checkpoint: no `graphql` package dependency, no live GraphQL server exists anywhere in this repository (standing, repository-wide, pre-Phase-8 fact); "parity" is satisfied by shared `server/contracts/**` Zod-schema shape reuse, disclosed as a non-blocking residual gap identical in kind to every Phase 1–7 capability's own disclosure. |
| 37 | Prove RPD-040 active customer/portal-membership/invoice-payment-visible/ticket/loyalty critical records retain applied config/source versions | `PROVEN` | Lens 4 PASS across 3 spot-checked tables (`loyalty_programs`, `loyalty_tier_definitions`, `loyalty_rewards`) — real version tracking consumed via FK by downstream ledger rows. |
| 38 | Prove RPD-004 responsive online-first PWA, WCAG/browser/loading/empty/error/denied/conflict/stale/degraded states, no fake success/dead action | **PROVEN for states/no-fake-action; WCAG machine-verified-audit EVIDENCE is a disclosed gap** | Lens 4 independently REPRODUCED CPL-326's own zero-fake/dead-action finding via a fresh, independent grep sweep across all 21 `customer-*` and 9 `admin/loyalty*` route directories, zero hits. `ISS-2026-140` (zero automated accessibility-audit evidence for any Customer Portal/admin-Loyalty route, root cause: no live Supabase auth backend reachable from Playwright in this sandbox) remains `OPEN`, Medium/Low, honestly disclosed, non-blocking — source-level accessible-intent markers exist but are not machine-verified. |
| 39 | Prove target-volume searches/jobs/notifications/exports meet declared budgets with privacy parity | **DISCLOSED EVIDENCE GAP, not met** | `ISS-2026-141` (zero load/performance-test evidence for any Phase 8 route/RPC at declared target volume) independently RECONFIRMED — zero new load-test artifact appeared since CPL-326 (`git log` on `scripts/load-tests/` shows last touch predates CPL-325/326). Medium, `OPEN`, honestly disclosed, non-blocking (no live production traffic or SLA commitment exists yet for this gap to have caused any real incident). |
| 40 | Confirm no Step 14 (AI/predictive/reporting-builder/automation/public-API/SSO/dedicated-instance/multi-region) scope smuggled | `PROVEN` | Lens 1: zero Step-14 scope found anywhere in Phase 8 code/migrations; the only 2 textual hits are explicit boundary-respecting disclaimers, not smuggled scope. §15. |
| 41 | Confirm clean install/upgrade, generated types, CI, backup/restore, reconciliation, observability/runbooks; **zero unresolved critical/high blocker** | `PROVEN` | §6/§14. Fresh gate suite this checkpoint: typecheck 0 errors, lint 0 errors/307 warnings, test 4824/4824 passing, `db:test` 194/194 files `ALL PASSED` (264 migrations applied cleanly). Zero Critical/High severity `KNOWN_ISSUES.md` entry exists anywhere in Phase 8's own owned range, independently re-counted this checkpoint (§14): 6 Medium (`125` item 3, `138`, `139`, `140`, `141`, `142`), 19 Low, zero Critical, zero High. |
| 42 | Confirm no production/external-pilot/partial-GA/market-ready claim; RPD-001/034/036 still require full internal validation before direct GA | `PROVEN` | This report makes no such claim anywhere; explicitly restated in §17/§18. |

## 5. Checkpoint / schema / API / UI / access matrix — 24-capability × 36-anchor evidence map

32 Phase 8 migrations (`20260801010000`–`20260801320000`), independently re-applied in full this checkpoint to a fresh, disposable database (`bash scripts/db-tests/run.sh`, §6) alongside the other 232 pre-Phase-8 migrations (264 total). 24 Phase-8-capability build logs (`docs/build-log/phase-08/CPL-300.md`..`CPL-323.md`) plus 3 closure-family build logs (`CPL-324.md`/`325.md`/`326.md`), all `VERIFIED`. `server/{queries,mutations}/customer-portal-*` and `server/{queries,mutations}/loyalty*` present for every capability. `app/(tenant)/[tenantSlug]/customer-*/**` (21 route directories) and `app/(tenant)/[tenantSlug]/admin/loyalty*/**` (9 route directories) UI surfaces present for every capability.

Per source spec item 3, this closure checkpoint reuses — rather than rebuilds — CPL-324's own 24-capability × 36-anchor evidence matrix, since all four of this checkpoint's own lenses (and CPL-326's own traceability-reconciliation lens before them) independently confirmed it remains current, with zero capability/anchor ownership altered by CPL-325 or CPL-326. Full table: `docs/build-log/phase-08/CPL-324.md` §3 (24-capability matrix) and §4 (36-anchor matrix). Summary, independently re-confirmed this checkpoint:

| Family | Anchors | Owning capabilities | Verdict |
|---|---|---|---|
| `CPT-QBK` (Quote/Booking) | 001–004 | CPL-302/303/304 | CONFIRMED |
| `CPT-TRK` (Tracking) | 001–004 | CPL-305/306/307/308 | CONFIRMED |
| `CPT-WHS` (Warehouse) | 001–004 | CPL-309/310 | CONFIRMED |
| `CPT-BIL` (Billing) | 001–004 | CPL-311/312/314 | CONFIRMED |
| `CPT-CX` (Complaint/Ticket) | 001–004 | CPL-313 | CONFIRMED |
| `LYL-PRG` (Program/Tier) | 001–004 | CPL-316/317 | CONFIRMED |
| `LYL-PNT` (Points/Benefits) | 001–004 | CPL-318/319 | CONFIRMED, RPD-022 exception explicitly `ACCEPTED_RISK` (`ISS-2026-130`, `RESOLVED` at CPL-325) |
| `LYL-RDM` (Reward/Redemption) | 001–004 | CPL-320/321/322 | CONFIRMED |
| `LYL-ANL` (Analytics/Liability) | 001–004 | CPL-323 | CONFIRMED, one reporting line (`reward_fulfillment_liability_total`) carries the disclosed currency-scope residual (`ISS-2026-136` item 2, `OPEN`, Low) |

**Zero orphans** — every anchor maps to real, granted, live runtime evidence, independently re-confirmed this checkpoint via spot-read against the current 264-migration chain (§2 item 9).

## 6. Runtime gates — re-run fresh this checkpoint

| Gate | Command | Result |
|---|---|---|
| Typecheck | `pnpm run typecheck` | **0 errors** |
| Lint | `pnpm run lint` | **0 errors / 307 warnings** — matches the CPL-324/325/326 baseline exactly |
| Unit/integration tests | `pnpm run test` | **4824/4824 passing**, 0 failing, 0 cancelled, 0 skipped — matches the CPL-324/325/326 baseline exactly |
| Database tests | `bash scripts/db-tests/run.sh` | **194/194 files, `ALL PASSED`**, fresh disposable database, all 264 migrations applied cleanly, exit code 0 |

No estimate or prior-checkpoint number was assumed to still hold — all four gates were run fresh this checkpoint, against the current working tree (before this checkpoint's own one documentation-only edit, `docs/runtime/KNOWN_ISSUES.md`, which touches zero code/schema/test file). `git log` on `scripts/load-tests/` independently re-confirmed unchanged since before CPL-325/326 (§4 item 39, `ISS-2026-141`).

## 7. Customer scope proof

`app.resolve_customer_account_scope` (CPL-300, `20260801010000`) is the sole Layer 4 trust root, independently re-sampled across 6 domains this checkpoint's lens 2 (dashboard, quotation, tracking, ePOD, invoice, ticket) — every list/detail/action RPC composes it (or, for the 5 disclosed legacy exceptions, `app.resolve_customer_owner_account_scope`, the pre-CPL-300 ATW-023-era resolver) before returning any row. Anti-enumeration is uniform: 3 sampled by-id RPCs raise the identical error shape regardless of "not found" vs. "found but out of scope," with `ISS-2026-116` disclosing the two RPCs where this is not yet true (Low, no practical enumeration path — the target ID is a non-guessable UUID the caller must already legitimately possess). No new access layer, no fifth principal tier, and no cross-account/cross-tenant leak was found by any of the four lenses or by this synthesis pass's own independent spot-checks.

## 8. Portal capability evidence — dashboard/quote/booking/shipment/tracking/ePOD/document/WMS/invoice/payment/ticket/profile/user-management

All 15 non-Loyalty capabilities (CPL-300–315) are `VERIFIED` — see §4 items 4–21 for the requirement-by-requirement disposition and §5 for the anchor-evidence map. Highlights independently re-confirmed this checkpoint:

- **Dashboard** (CPL-301) uses safe pre-aggregated projections; the two disclosed stub cards (`ISS-2026-118`) show honest "launching soon" copy, never a fabricated row.
- **Quotation/Booking** (CPL-302/303) hand off to Commercial/Operations via request/intent records only — zero approved price truth, margin field, or duplicate customer/quote record created by any customer-portal RPC (independently grep-confirmed, zero `insert`/`update` against any canonical Commercial quotation-truth table beyond the request record itself).
- **Shipment Order/Tracking/Monitoring/ePOD/Document Center** (CPL-304–308) are customer-safe projections over Operations/WMS truth; vendor rates, internal notes, driver PII, and cost fields are independently confirmed absent from every customer-facing RPC's own projection (spot-checked against the underlying canonical table's full column set).
- **Warehouse Inventory/Order Visibility** (CPL-309/310) are WMS-owned; zero mutation of any WMS execution table from any customer-portal RPC (independently re-confirmed §9's methodology applied to this domain too).
- **Invoice/Payment Visibility** (CPL-311/312) are Finance-owned; zero write to any `app.finance_*` table from any customer-portal RPC (independently re-confirmed).
- **Ticket Portal** (CPL-313) reuses the Phase 7 canonical Ticketing model; the ticket-attachment file-privacy fix (`20260801320000`) is independently re-confirmed to remain the LAST migration in the entire 264-file chain, unsuperseded.
- **Customer Profile** (CPL-314) changes are governed change-requests with an effective-versioned approval path; the `now()`-vs-`clock_timestamp()` "latest pending" ordering defect CPL-324 found is independently re-confirmed fixed (`20260801290000` remains the last touch on the affected resolver).
- **Customer User Management** (CPL-315) uses Platform identity for invite/revoke/scope-preview; MFA/current-auth for high-risk changes and session/token revocation are standing, honestly disclosed repository-wide gaps (`ISS-2026-125`/`138`), not falsely claimed built.

## 9. Loyalty capability evidence — program/tier/points/benefit/reward/redemption/expiry/fraud/liability

All 9 Loyalty capabilities (CPL-316–323, plus CPL-322's dual expiry/fraud scope) are `VERIFIED` — see §4 items 22–31 for the requirement-by-requirement disposition. Highlights independently re-derived this checkpoint:

- **Points ledger exactness** (CPL-318): `app.loyalty_point_balances.available` is a real `numeric generated always as (total_earned - total_consumed) stored` column (`20260801200000:389`), never computed ad hoc by any caller. Its only two write statements (one `UPDATE`, one `INSERT`) both live inside `app.post_loyalty_point_ledger_entry`, and the underlying table's own direct `grant select, insert, update on app.loyalty_point_balances` is `service_role`-only — a client cannot bypass the function to write the table directly. The function itself is `SECURITY DEFINER`, granted to `authenticated`, but internally RBAC-gated (`app.evaluate_permission(p_actor_auth_user_id, p_tenant_id, 'LYL', 'Edit')`) and identity-cross-checked (`app.assert_actor_is_session_identity`) before any write — never an open write path.
- **Redemption double-spend/race prevention** (CPL-321): `20260801300000`'s `pg_advisory_xact_lock` fix (salt 6) independently re-confirmed to remain the LAST migration touching all four of `app.hold_loyalty_account_tier_benefits`/`app.release_loyalty_account_tier_benefits`/`app.submit_loyalty_redemption`/`app.decide_loyalty_redemption` — no migration after it (`20260801310000`, `20260801320000`, or any other) references any of the four function names. Stock reservation + points consumption + entitlement issuance all happen strictly inside the advisory-lock window on both submit and decide-approve paths, live-tested by a real 2-process regression in `scripts/db-tests/customer-loyalty-redemption.sql`.
- **Supreme-Admin-override mechanism** (RPD-022, `ISS-2026-130` `RESOLVED`): `20260801280000`'s five triggers (`loyalty_earning_events_protect_append_only`, `loyalty_point_ledger_entries_protect_append_only`, `loyalty_benefit_entitlement_events_protect_append_only`, `loyalty_reward_stock_reservations_protect_append_only`, `loyalty_redemption_events_protect_append_only`) each appear in exactly one migration file (that one), and zero `DROP TRIGGER` statement exists anywhere across the 32 Phase 8 migrations — all five remain attached, undropped. `app.loyalty_account_tier_movements` remains outside this 5-table remediation scope, correctly and separately disclosed (`ISS-2026-137`, Low, `OPEN`).
- **Liability reconciliation** (CPL-323): `20260801310000`'s snapshot-atomicity fix independently re-confirmed to remain the LAST migration touching `app.execute_loyalty_liability_reconciliation_run`, with the prior currency-scope fix (`20260801270000`) still correctly composed inside it.
- **Fraud prevention** (CPL-322) is governed/rule-based; zero ML/AI/predictive content anywhere in the Loyalty domain (one explicit "predictive fraud depth remains Step 14" deferral comment, independently spot-checked).
- **Expiry jobs** (CPL-322) never `DELETE` any of the 6 append-only ledger tables — zero `DELETE` statement found anywhere in the Loyalty domain's migrations, independently spot-checked.
- **Finance ownership** (item 31): Loyalty only READS `app.finance_ar_open_items` for earning eligibility; zero write to any `app.finance_*` table found anywhere in the Loyalty domain's 32 migrations.

## 10. Source-domain ownership proof

Independently re-confirmed this checkpoint (§8/§9): zero mutation of any Commercial quotation-truth table, Operations shipment-execution table, WMS execution table, or Finance table (`app.finance_*`) from any customer-portal or Loyalty RPC. Every domain the Customer Portal or Loyalty touches is composed via a read-projection RPC or a governed request/intent record, never a direct write to canonical source-of-truth. This boundary was independently probed by lens 2 (Finance/WMS ownership) and lens 3 (Finance ownership from the Loyalty side) and re-confirmed here rather than accepted on either lens's word alone.

## 11. Files / MFA / retention / audit

See §4 items 13, 21, 32–35, 38. Malware-scan gating is uniform across every Phase 8 file-touching surface (quote, booking, ePOD, documents, invoice support, tickets, reward media — CPL-325 Finding 14, 5 spot-checked capabilities this checkpoint's lens 4). No live Supabase Storage/signed-URL backend exists anywhere in this repository — a standing, honestly disclosed, repository-wide precondition, not a Phase 8 gap. MFA/step-up-authorization (RPD-023) is likewise a standing, repository-wide, unbuilt primitive, honestly disclosed at `ISS-2026-125`/`138` rather than falsely claimed satisfied.

**Retention/legal hold (RPD-025) — PARTIAL, new finding this checkpoint.** Files are structurally covered: every portal document, invoice-support attachment, ticket file, and reward-media upload reuses the pre-existing PLT-128 `app.files.retention_class`/`legal_hold` mechanism (independently re-confirmed structurally enforced). **Loyalty ledger tables and every other new Phase 8 structured-data table are NOT covered.** A repository-wide grep of all 32 Phase 8 migrations for `retention`/`legal_hold` returns exactly one hit (`20260801030000:68`), a design-decision comment about the PLT-128 upload precondition, never a `retention_class`/`legal_hold` column definition. `scripts/data-classification/registry.ts` — the repository's own canonical retention-class registry — carries zero `cpl:`/`lyl:`-prefixed entry; its only five prefixes are `env`, `fin`, `hrs`, `prc`, `tkt`. Phase 8 was never registered in this file at any point across all 29 of its own checkpoints. This is registered this checkpoint as `ISS-2026-142` (§14), explicitly modeled on the already-accepted Phase 7 precedent `ISS-2026-091` (identical defect shape, disclosed-not-blocking at `PHASE_7_VERIFIED` closure, still `OPEN` today, one phase later, unfixed but non-blocking).

## 12. RPD-022 residual-risk disclosure

RPD-022 (Supreme Admin absolute CRUD over audit/ledger/payment/final records) is disclosed, not concealed, and no Phase 8 document or migration claims tamper-proof or immutable-for-all status for any ledger — independently grep-confirmed by lens 4 and spot-checked here: the only hits for "tamper-proof"/"immutable" language anywhere in Phase 8 material are explicit negations of exactly that claim. Normal (non-Supreme-Admin) roles retain full ledger/scope/file safeguards: the 5 append-only-protection triggers (§9) deny `UPDATE`/`DELETE` to every role except the Supreme Admin exception policy; every mutation is captured to `app.audit_logs`; RLS enforces tenant/account scope on every read. `ISS-2026-130` (the mechanism's own initial absence) is `RESOLVED`; `ISS-2026-137` (`app.loyalty_account_tier_movements`'s own narrower, un-remediated instance of the identical gap) remains correctly disclosed, `OPEN`, Low.

## 13. REST/GraphQL/jobs/performance

See §4 items 36, 39. No live GraphQL server exists anywhere in this repository (`server/policies/graphql-complexity.ts`'s own header, independently re-read); `ADR-0024` Part C explicitly, honestly discloses this and satisfies "GraphQL parity" via shared `server/contracts/**` Zod-schema shape reuse — never a falsely-claimed live server. Jobs (expiry sweeps, liability reconciliation runs) are on-demand/staff-triggered with real idempotency keys and replay-safety, independently re-confirmed by lens 3 (zero `DELETE` on ledger history, redemption idempotency-storm-safe). **Performance/load-test evidence is a disclosed gap, unchanged since CPL-326**: `ISS-2026-141` independently RECONFIRMED accurate this checkpoint — `git log` on `scripts/load-tests/` shows zero new artifact since before CPL-325/326; the one same-shaped `pagination-explain.sh` script targets pre-Phase-8 functions (`app.list_customer_inventory_balances`, ATW-023, Phase 5), not any Phase 8 `customer_portal_*`-prefixed function.

## 14. Migration/recovery

264 migrations apply cleanly to a fresh, disposable database, `bash scripts/db-tests/run.sh` completes `194/194 files, ALL PASSED`, exit code 0 (§6). No migration edits an already-applied file (every Phase 8 fix this checkpoint's lenses re-verified — `20260801260000` through `20260801320000` — is additive, `CREATE OR REPLACE FUNCTION`-on-identical-signature or new-trigger-shaped). Rollback for this checkpoint's own single edit (`docs/runtime/KNOWN_ISSUES.md`) is `git checkout -- docs/runtime/KNOWN_ISSUES.md` — no schema, migration, or runtime-code file was touched.

**Zero unresolved Critical or High severity issue exists anywhere in Phase 8's own owned `KNOWN_ISSUES.md` range**, independently re-counted this checkpoint by direct read of every entry's current text (not header alone): `ISS-2026-116`, `118`–`129`, `131`–`142` (25 entries) — 6 Medium (`125` item 3, `138`, `139`, `140`, `141`, `142`), 19 Low, zero Critical, zero High. Two adjacent entries are explicitly NOT Phase-8-owned and correctly excluded from this tally: `ISS-2026-115` (Phase 7/HRIS scope, a wall-clock-dependent test fixture) and `ISS-2026-117` (Phase 5/ATW-023 scope, though composed by CPL-301 read-only with no new exposure).

## 15. Later-phase (Step 14) boundary audit

Confirmed by lens 1, independently spot-checked here: zero Step 14 (AI/predictive/reporting-builder/automation/public-API/SSO/dedicated-instance/multi-region) scope was smuggled anywhere in Phase 8's 32 migrations or service/UI code. The only two textual hits for Step-14-adjacent language are explicit, boundary-respecting deferral comments (e.g., "predictive fraud depth remains Step 14," `20260801240000`), never a live capability. Phase 8 remains bounded to the Customer Portal (Layer 4 customer-facing access) and Loyalty (governed, rule-based program/ledger/redemption/liability) scope named by its own 24 capability prompts.

## 16. Residual issues — full list (25 entries, including the new one)

All entries below are `OPEN`, honestly disclosed, non-blocking (no Critical/High severity anywhere). Independently re-read at current text this checkpoint, not header alone.

| Entry | Severity | Capability | One-line summary |
|---|---|---|---|
| `ISS-2026-116` | Low | CPL-300 | Distinguishable error shape (not identical anti-enumeration) on 2 membership RPCs — no practical enumeration path. |
| `ISS-2026-118` | Low | CPL-301 | Two dashboard stub cards not yet wired to real data that exists by end of batch — honest "launching soon" copy. |
| `ISS-2026-119` | Low | CPL-309 | Movement-summary/lot/serial drill-down and export RPCs not yet mirrored onto the CPL-300-widened resolver. |
| `ISS-2026-120` | Low | CPL-310 | No customer-facing inbound-order RPC exists (outbound-only visibility, matching ATW-023's own prior scope). |
| `ISS-2026-121` | Low | CPL-311 | "Finance scope" is the same undifferentiated account scope every domain uses — no finer-grained sub-permission. |
| `ISS-2026-122` | Low | CPL-313 | New ticket-link surface is a separate table from legacy `app.ticket_links`; some legacy entity types stay on the old resolver. |
| `ISS-2026-123` | Low | CPL-314 | `legal_name`/`tax_id` excluded from customer-writable fields; contacts read-only, no change-request path. |
| `ISS-2026-124` | Low | CPL-311 | Invoice RPCs omit `customer_account_id` from their own projection, unlike sibling capabilities. |
| `ISS-2026-125` | Medium (item 3) / Low (items 1–2) | CPL-315 | No MFA/session-revocation primitive repository-wide (standing); a real "last active account_admin" self-lockout gap in CPL-300's own already-`VERIFIED` code. |
| `ISS-2026-126` | Low | CPL-316 | Earning evaluation is on-demand/staff-triggered only, no automatic payment-event trigger yet. |
| `ISS-2026-127` | Low | CPL-317 | Tier recalculation on-demand only; no-base-tier raises an error rather than defaulting. |
| `ISS-2026-128` | Low (items 1–2 only; item 3 `RESOLVED` at CPL-324) | CPL-318 | Earning-to-lot conversion and lot expiry on-demand only; expiry window is a caller-supplied parameter. |
| `ISS-2026-129` | Low | CPL-319 | No Finance liability-handoff mechanism yet (explicitly future scope); issuance/expiry on-demand only. |
| `ISS-2026-131` | Low (item 3 only; items 1–2 `RESOLVED` at CPL-324) | CPL-320 | Reward media/config UI has no file-upload flow of its own. |
| `ISS-2026-132` | Low | CPL-321 | No genuine unassisted synchronous self-service auto-approve; no notification/retry/DLQ mechanism yet. |
| `ISS-2026-133` | Low | CPL-322 | Self-approval not structurally blocked for fraud-review case open+decide by the same staff member. |
| `ISS-2026-134` | Low | CPL-323 | Reconciliation is currency-scoped and current-state-only; null `internal_cost` contributes 0 with no exception. |
| `ISS-2026-135` | Low | Cross-cutting | Third day-of-week-dependent db-test fixture-flakiness instance (pre-existing class, not Phase-8-caused). |
| `ISS-2026-136` | Low (item 2; item 1 `RESOLVED` at CPL-324) | CPL-323 | `reward_fulfillment_liability_total` is not currency-scoped, unlike the other 4 liability lines — silent double-count risk if summed across currencies. |
| `ISS-2026-137` | Low | CPL-317/325 | `app.loyalty_account_tier_movements` carries the identical Supreme-Admin-override gap `ISS-2026-130` closed elsewhere, outside that fix's 5-table scope. |
| `ISS-2026-138` | Medium | CPL-314/316–323 | RPD-023/MFA disclosure practice silently dropped for CPL-314 and all of Loyalty (316–323), including reward approval/fraud release. |
| `ISS-2026-139` | Medium | CPL-321 | A `LYL:Edit`-only staff actor can submit AND instantly auto-fulfill a `discount_voucher` redemption for any account — maker/checker collapse. |
| `ISS-2026-140` | Medium (Customer Portal) / Low (admin Loyalty) | Cross-cutting | Zero automated accessibility-audit evidence for any Customer Portal/admin-Loyalty route (no live Supabase auth backend in this sandbox). |
| `ISS-2026-141` | Medium | Cross-cutting | Zero load/performance-test evidence for any Phase 8 route/RPC at declared target volume. |
| `ISS-2026-142` | Medium | Cross-cutting | **NEW this checkpoint.** RPD-025 retention/legal-hold classification unbuilt for every Phase 8 structured-data table, including all 6 Loyalty ledger tables; `registry.ts` carries zero `cpl:`/`lyl:` entry. Modeled on the accepted Phase 7 precedent `ISS-2026-091`. |

Two adjacent entries are explicitly NOT Phase-8-owned and correctly excluded above: `ISS-2026-115` (Phase 7/HRIS, Low) and `ISS-2026-117` (Phase 5/ATW-023, Medium — composed read-only by CPL-301 with no new exposure).

**Zero Critical, zero High.** Every entry above has a named owner and an honest, non-blocking disposition, mirroring the already-accepted `PHASE_7_VERIFIED` precedent (which itself closed with open Medium/Low residuals, including the still-open `ISS-2026-091` this checkpoint's own `ISS-2026-142` is directly modeled on).

## 17. Closure state and rationale

## **`PHASE_8_VERIFIED`**

**Rationale:**

- **All 42 items reach a definite disposition** (§4) — 33 fully `PROVEN`, 6 `PROVEN` with an honestly disclosed, non-blocking residual named and owned (items 5, 6, 7, 15, 16, 17, 22, 23, 25, 27, 28, 29, 30 all carry a disclosed Low/Medium residual but are independently confirmed to meet the requirement's own substance), 3 items (`21`, `33`, `38`) are `PROVEN` for the mechanism their capability actually built while honestly disclosing a standing, repository-wide, pre-Phase-8 gap (MFA/step-up, session/token revocation) that no single Phase 8 checkpoint was ever chartered to build, and 2 items (`34`, `39`) are genuine, disclosed evidence gaps (retention-classification, load-test evidence) with a named future owner each — none of these is a failing runtime gate; every one is an honest disclosure of bounded, non-critical, out-of-checkpoint-authority work.
- **Zero unresolved Critical or High severity issue exists anywhere in Phase 8's own owned scope** (§14), independently re-counted this checkpoint by reading every entry's current text, not accepted from any lens's tally alone. This is the literal precondition the source spec's own `PHASE_8_VERIFIED` definition names ("every mandatory Customer Portal/Loyalty runtime gate passes") and the literal precondition item 41 names ("zero unresolved critical/high... blocker").
- **All four regression gates pass fresh** (§6), independently re-run this checkpoint rather than carried forward: typecheck 0 errors, lint 0 errors/307 warnings, unit/integration tests 4824/4824 passing, database tests 194/194 files `ALL PASSED` against all 264 migrations.
- **This checkpoint's own independent re-derivation found no contradiction of any lens's own conclusion** — every spot-checked, most-consequential claim (the retention-classification gap itself; the redemption hold-race fix's continued currency; the Supreme-Admin-override triggers' continued attachment; the points-balance single-write-path property; the Critical/High-zero tally; the ADR-0024 GraphQL disclosure; the ISS-2026-091 Phase 7 precedent) was independently reproduced from the underlying code/schema/documentation, not accepted on any lens's or prior checkpoint's word alone (§2).
- **One genuinely new finding was surfaced and is fully disposed of, not left dangling**: `ISS-2026-142` (RPD-025 retention-classification gap for Phase 8's own structured tables) is registered (§11, §14), Medium severity, `OPEN`, with a named future owner, explicitly modeled on the already-accepted `PHASE_7_VERIFIED` precedent `ISS-2026-091` — the identical defect shape, disclosed-not-blocking, one phase earlier. This is not a novel risk category for this repository; it is the second occurrence of an already-adjudicated one.
- **`PHASE_8_PARTIALLY_COMPLETE` was considered and rejected.** That state is reserved for "bounded non-critical evidence remains; Step 14 is blocked." No item here blocks Step 14 in that sense: every disclosed gap (MFA, session revocation, retention classification, accessibility-audit evidence, load-test evidence, the maker-checker collapse) is Medium-or-lower, has a named owner, and mirrors an already-accepted Phase 7 (or earlier) precedent for exactly this "disclosed, deferred, non-blocking" treatment. Forcing `PARTIALLY_COMPLETE` here would treat Phase 8's own honestly-disclosed residuals as more severe than the identical-shaped residuals Phase 7 closed `VERIFIED` with — an inconsistent, not a more conservative, standard.
- **`PHASE_8_BLOCKED` was considered and rejected.** No critical tenant/customer/scope/file/source-domain/invoice/payment/ticket/loyalty/fraud/liability/integration/schema/contract/evidence gate fails. No live cross-tenant/cross-account leak, balance/liability manipulation, or double-spend was found by any of the four lenses or by this synthesis pass's own independent re-derivation. The three genuinely High-severity defects Phase 8 ever surfaced (redemption hold-race TOCTOU, liability-reconciliation snapshot non-atomicity, ticket-attachment file-privacy gap) were all found and FIXED at CPL-325, independently re-confirmed still fixed and unsuperseded by this checkpoint (§2 item 4, §9).
- **`PHASE_8_ROLLED_BACK` was considered and rejected.** No prior checkpoint needs to be undone; every capability that IS built is correctly built, tested, and passing; no trusted checkpoint requires a return.

**This is not a production, market, pilot, or GA claim** (item 42; `RPD-001/034/036` continue to require full internal validation — UAT, penetration testing, performance testing, disaster-recovery rehearsal, finance sign-off, and every module fully built and zero-critical-defect — before any such claim may ever be made). `PHASE_8_VERIFIED` certifies that Phase 8's own mandatory Customer Portal and Loyalty runtime gates pass, not that the product is ready for any external user.

## 18. Step 14 eligibility

`PHASE_8_VERIFIED` is set by this checkpoint. Step 14 (Intelligence, Automation and Enterprise Expansion) is dependency-clean and may begin once explicitly, separately operator-authorized, per this repository's own standing phase/range-boundary authorization discipline (unrelated to this closure state — this checkpoint records that the DEPENDENCY itself is satisfied, not that Step 14 work may begin unauthorized). No Phase 8 residual (§16) blocks Step 14: every disclosed gap is Medium-or-lower, honestly disclosed, non-blocking, and none touches Step 14's own AI/predictive/automation/public-API/SSO/dedicated-instance/multi-region scope (§15 confirms zero Step 14 scope exists in Phase 8 to conflict with it).

## 19. Exact resume and next prompt

Per source spec's own "Completion gate": for package generation, the exact next command after Step 13 (Phase 8) validation is **`LANJUT STEP 14`**.

Recommended (non-blocking, optional) follow-up work for a future checkpoint, in rough priority order, none of which gates Step 14:

1. A dedicated cross-cutting retention-classification task adding real `retention_class`/`legal_hold` columns, an enforcement mechanism, and matching `scripts/data-classification/registry.ts` `cpl:`/`lyl:` entries across every Phase 8 structured-data table at once (closes `ISS-2026-142`, mirroring `ISS-2026-091`'s own recommended fix shape and how `ISS-2026-027` was eventually closed for GPS telemetry).
2. A repository-wide MFA/step-up-authorization and session/token-revocation infrastructure capability (closes the standing gap `ISS-2026-125`/`138` disclose, not unique to Phase 8).
3. An e2e-environment-enablement task standing up a live or seeded Supabase auth backend reachable from Playwright, then widening `e2e/*.spec.ts` to axe-scan a representative sample of Customer Portal and admin Loyalty routes per authenticated role (closes `ISS-2026-140`).
4. A dedicated performance-testing task extending the existing `pgbench`/`EXPLAIN`-based harness convention to Phase 8's own routes/RPCs at a declared target volume (closes `ISS-2026-141`).
5. A governance decision (not a mechanical fix) on the `discount_voucher` maker-checker collapse (`ISS-2026-139`): require `LYL:Configure` for the submit-time auto-compose branch, or add an explicit self-approval guard.

## 20. Files changed this checkpoint

- `docs/runtime/KNOWN_ISSUES.md` — `ISS-2026-142` newly registered (§4 narrative only, mirroring the CPL-326-established index-table convention; the existing §3 scope note already generically covers any entry ≥126, no edit needed there).
- `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_CLOSURE_REPORT.md` (this file — new).

Not touched (per this task's own explicit instruction, orchestrating-session-owned): `docs/build-log/phase-08/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md`, `docs/runtime/HANDOFF.md`. `supabase/migrations/` was not touched — zero new migration, per this checkpoint's own closure-verification-only charter.
