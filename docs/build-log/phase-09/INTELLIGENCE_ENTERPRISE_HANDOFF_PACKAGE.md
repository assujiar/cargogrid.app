# Phase 9 (Intelligence, Automation and Enterprise Expansion) Handoff Package

**Produced by:** `CG-S14-IAE-038` (Prompt 366 — Intelligence, Automation and Enterprise Documentation Handoff), per Prompt 366 §4/§10/§20's own "complete Phase 9 documentation, runbooks, API/webhook specs, AI governance records, enterprise control handoff and exact Step 15 eligibility package after hardening" mandate. Mirrors `docs/build-log/phase-08/CUSTOMER_PORTAL_LOYALTY_HANDOFF_PACKAGE.md`'s own exact shape and citation discipline (read in full before drafting this document), which itself mirrors `docs/build-log/phase-07/HRIS_TICKETING_HANDOFF_PACKAGE.md` and earlier phase precedents.
**Audience:** an independent Step 15 (Full-System Hardening) or Step 16 (full release) agent with **zero prior context** from this build session, plus Prompt 367 (Closure Verification), and a Supreme Admin who needs Phase 9 explained from real, evidence-backed fact — every claim below is either directly cited to a `VERIFIED` build log, migration, or `docs/runtime/KNOWN_ISSUES.md` entry, and every citation load-bearing to this document's own §2/§3 claims was independently re-verified this checkpoint by reading the actual migration file, not merely restated from a build log's own prose.
**Status of this package itself:** complete pending one external precondition — `CG-S14-IAE-039` (Prompt 367, Closure Verification) has not yet run. **Nothing in this document should be read as `PHASE_9_VERIFIED` being set — only Prompt 367 may set that** (per `docs/build-log/phase-09/00_EXECUTION_INDEX.md` §10's own row: "Sole task authorized to set `PHASE_9_VERIFIED`").
**This is a new, self-contained artifact**, distinct from `docs/runtime/HANDOFF.md` (the intra-phase, checkpoint-to-checkpoint runtime handoff, which this checkpoint deliberately does not edit). It exists specifically for the "fresh Step 15/16 agent reconstructs Phase 9 and starts the exact eligible next task safely" flow.
**No production/pilot/GA/market-ready claim is made anywhere in this document** (RPD-001/034/036). **Step 15–16 full-system-hardening and full-release work is explicitly OUT OF SCOPE for this checkpoint** (Prompt 366 §12, "Forbidden files/folders": "Step 15–16 full release work") — §5 below only *names* what a future Step 15/16 agent needs to know; it does not attempt any of that work.

---

## 1. How to use this package (start here)

| If you are... | Start with |
|---|---|
| A fresh Step 15 (Full-System Hardening) or Step 16 (release) agent | §2 (navigation table) → §3 (verified dependencies) → §4 (preserved assets) → §5 (Step 15/16 boundary, the most important section) → §6 (known issues) |
| Prompt 367 (`CG-S14-IAE-039`, Closure Verification) | §2 (traceability), §6 (open-issue enumeration), §7 (this document's own forbidden-scope confirmation) |
| A downstream reader wanting the full per-capability build record | `docs/build-log/phase-09/IAE-330.md` through `IAE-365.md` (36 build logs — 34 capabilities plus `IAE-036`/`IAE-364` Integrated Verification and `IAE-037`/`IAE-365` Security/AI Hardening), plus `docs/build-log/phase-09/00_EXECUTION_INDEX.md` (WBS, dependency DAG, batch plan, all 8 Tier C review closures) |
| A future maintainer needing the four Phase 9 reuse-boundary decisions | `docs/adr/ADR-0025-phase9-api-webhook-automation-and-ai-provider-boundary-foundation.md` (4 parts: A — API-key reuse; B — webhook reuse; C — Integration Hub governance-layer boundary; D — Automation Rule Engine job-queue reuse) — canonical, summarized not duplicated in §3 below |
| Anyone asking "what did Phase 9 verify vs. what remains open" | §6 (known issues) below, plus §5's own "concrete gaps" subsection for Step-15/16-relevant boundaries specifically |

---

## 2. Navigation table — all 34 Phase 9 capabilities

Every row below was read directly from `docs/build-log/phase-09/00_EXECUTION_INDEX.md` §10 (task-state table) at current HEAD this checkpoint (`6da6af5`, branch `claude/execute-prompt-349-368-zi0nqs`), then independently cross-checked against the actual migration filename on disk (`ls supabase/migrations/`) and the actual db-test filename on disk (`ls scripts/db-tests/`) — not re-derived from memory or restated from the index's own prose alone. All 34 are `VERIFIED`.

| # | `CG-S14-IAE-*` | Prompt | Capability | Migration | Contract/service dir | db-test file | Build log |
|---|---|---|---|---|---|---|---|
| 1 | `IAE-002` | 330 | Reporting Engine | `20260802010000_create_intelligence_reporting_engine.sql` | `server/contracts/report/` | `reporting-engine.sql` | `IAE-330.md` |
| 2 | `IAE-003` | 331 | Dashboard Builder | `20260802020000_create_intelligence_dashboard_builder.sql` | `server/contracts/dashboard/` | `dashboard-builder.sql` | `IAE-331.md` |
| 3 | `IAE-004` | 332 | Saved View / Configurable Report | `20260802030000_create_intelligence_saved_report_views.sql` | `server/contracts/saved-report-view/` | `saved-report-views.sql` | `IAE-332.md` |
| 4 | `IAE-005` | 333 | Analytics Materialized Views | `20260802040000_create_intelligence_analytics_materialized_views.sql` | `server/contracts/analytics/` | `analytics-materialized-views.sql` | `IAE-333.md` |
| 5 | `IAE-006` | 334 | Scheduled Reports | `20260802050000_create_intelligence_scheduled_reports.sql` (+ Batch 1 fix `20260802060000_harden_intelligence_batch1_tier_c_review_fixes.sql`) | `server/contracts/scheduled-report/` | `scheduled-reports.sql` | `IAE-334.md` |
| 6 | `IAE-007` | 335 | Automation Rule Engine | `20260803010000_create_intelligence_automation_rule_engine.sql` | `server/contracts/automation-rule/` | `automation-rule-engine.sql` (+ `automation-rule-engine-concurrency-helper.sh`) | `IAE-335.md` |
| 7 | `IAE-008` | 336 | Integration Hub | `20260803020000_create_intelligence_integration_hub.sql` (+ Batch 2 fix `20260803030000_harden_intelligence_batch2_tier_c_review_fixes.sql`) | `server/contracts/integration-hub/` | `integration-hub.sql` | `IAE-336.md` |
| 8 | `IAE-009` | 337 | Public API Platform | `20260804010000_create_intelligence_public_api_platform.sql` | `server/contracts/public-api-platform/` | `public-api-platform.sql` | `IAE-337.md` |
| 9 | `IAE-010` | 338 | Customer API | `20260804020000_create_intelligence_customer_api.sql` | `server/contracts/customer-api/` | `customer-api.sql` | `IAE-338.md` |
| 10 | `IAE-011` | 339 | Vendor API | `20260804030000_create_intelligence_vendor_api.sql` | `server/contracts/vendor-api/` | `vendor-api.sql` | `IAE-339.md` |
| 11 | `IAE-012` | 340 | Webhook Management | `20260804040000_create_intelligence_webhook_management.sql` | `server/contracts/webhook-management/`, `lib/webhooks/` | `webhook-management.sql` | `IAE-340.md` |
| 12 | `IAE-013` | 341 | n8n Integration | `20260804050000_create_intelligence_n8n_integration.sql` (+ Batch 3 fix `20260804060000_harden_intelligence_batch3_tier_c_review_fixes.sql`) | `server/contracts/n8n-integration/` | `n8n-integration.sql` | `IAE-341.md` |
| 13 | `IAE-014` | 342 | Email / WhatsApp / SMS Integrations | `20260805010000_create_intelligence_email_whatsapp_sms_integrations.sql` | `server/contracts/notification/`, `lib/notifications/` | `email-whatsapp-sms-integrations.sql` | `IAE-342.md` |
| 14 | `IAE-015` | 343 | Maps / GPS / Telematics Integrations | `20260805020000_create_intelligence_maps_gps_telematics_integrations.sql` | `server/contracts/maps-geocoding/`, `lib/maps/` | `maps-gps-telematics-integrations.sql` | `IAE-343.md` |
| 15 | `IAE-016` | 344 | Carrier / Port / Airport / Customs Integrations | `20260805030000_create_intelligence_carrier_port_airport_customs_integrations.sql` | `server/contracts/logistics-partner/`, `lib/logistics-partners/` | `carrier-port-airport-customs-integrations.sql` | `IAE-344.md` |
| 16 | `IAE-017` | 345 | Bank / Payment / eInvoice / Tax Integrations | `20260805040000_create_intelligence_bank_payment_einvoice_tax_integrations.sql` | `server/contracts/bank-payment-tax-integrations/`, `lib/finance-integrations/` | `bank-payment-einvoice-tax-integrations.sql` | `IAE-345.md` |
| 17 | `IAE-018` | 346 | External Accounting / HR Integrations | `20260805050000_create_intelligence_external_accounting_hr_integrations.sql` | `server/contracts/external-sync/`, `lib/external-integrations/` | `external-accounting-hr-integrations.sql` | `IAE-346.md` |
| 18 | `IAE-019` | 347 | AI Governance Provider Boundary | `20260805060000_create_intelligence_ai_governance_provider_boundary.sql` (+ Batch 4 fix `20260805070000_harden_intelligence_batch4_tier_c_review_fixes.sql`) | `server/contracts/ai-governance/`, `lib/ai-governance/` | `ai-governance-provider-boundary.sql` | `IAE-347.md` |
| 19 | `IAE-020` | 348 | AI-Assisted Quotation | `20260805080000_create_intelligence_ai_assisted_quotation.sql` (+ solo Tier C fix `20260805090000_harden_iae020_tier_c_review_fixes.sql`) | `server/contracts/ai-quotation/`, `lib/ai-quotation/` | `ai-assisted-quotation.sql` | `IAE-348.md` |
| 20 | `IAE-021` | 349 | OCR Document Processing | `20260806000000_create_intelligence_ocr_document_processing.sql` | `server/contracts/ocr-document/` | `ocr-document-processing.sql` | `IAE-349.md` |
| 21 | `IAE-022` | 350 | Predictive ETA | `20260806100000_create_intelligence_predictive_eta.sql` | `server/contracts/eta-prediction/` | `predictive-eta.sql` | `IAE-350.md` |
| 22 | `IAE-023` | 351 | Optimization Assistance | `20260806200000_create_intelligence_optimization_assistance.sql` | `server/contracts/optimization-assistance/` | `optimization-assistance.sql` | `IAE-351.md` |
| 23 | `IAE-024` | 352 | Fraud/Risk Assistance | `20260806300000_create_intelligence_fraud_risk_assistance.sql` | `server/contracts/fraud-risk/` | `fraud-risk-assistance.sql` | `IAE-352.md` |
| 24 | `IAE-025` | 353 | Forecasting/Recommendation Assistance | `20260806400000_create_intelligence_forecasting_recommendation.sql` | `server/contracts/forecasting-recommendation/` | `forecasting-recommendation.sql` | `IAE-353.md` |
| 25 | `IAE-026` | 354 | Enterprise IAM SSO/SAML/OAuth/SCIM | `20260807000000_create_intelligence_enterprise_iam_sso.sql` | `server/contracts/enterprise-iam/` | `enterprise-iam-sso-scim.sql` | `IAE-354.md` |
| 26 | `IAE-027` | 355 | Enterprise MFA and Session Controls | `20260807100000_create_intelligence_enterprise_mfa_session_controls.sql` | `server/contracts/enterprise-mfa/` | `enterprise-mfa-session-controls.sql` | `IAE-355.md` |
| 27 | `IAE-028` | 356 | IP Restriction and Network Access | `20260807200000_create_intelligence_ip_restriction_network_access.sql` | `server/contracts/ip-restriction/` | `ip-restriction-network-access.sql` | `IAE-356.md` |
| 28 | `IAE-029` | 357 | Advanced Audit and Impersonation | `20260807300000_create_intelligence_advanced_audit_impersonation.sql` | `server/contracts/advanced-audit/` | `advanced-audit-impersonation.sql` | `IAE-357.md` |
| 29 | `IAE-030` | 358 | Enterprise Monitoring and Observability | `20260807400000_create_intelligence_enterprise_monitoring_observability.sql` | `server/contracts/enterprise-monitoring/`, `server/mutations/enterprise-monitoring.ts` | `enterprise-monitoring-observability.sql` | `IAE-358.md` |
| 30 | `IAE-031` | 359 | Data Retention and Archival | `20260807500000_create_intelligence_data_retention_archival.sql` | `server/contracts/data-retention/` | `data-retention-archival.sql` | `IAE-359.md` |
| 31 | `IAE-032` | 360 | Dedicated Enterprise Deployment | `20260808000000_create_intelligence_dedicated_enterprise_deployment.sql` | `server/contracts/dedicated-enterprise-deployment/` | `dedicated-enterprise-deployment.sql` | `IAE-360.md` |
| 32 | `IAE-033` | 361 | Multi-Region and Data Residency | `20260808100000_create_intelligence_multi_region_data_residency.sql` | `server/contracts/multi-region-data-residency/` | `multi-region-data-residency.sql` | `IAE-361.md` |
| 33 | `IAE-034` | 362 | Scale-Up Architecture | `20260808200000_create_intelligence_scale_up_architecture.sql` | `server/contracts/scale-up-architecture/` | `scale-up-architecture.sql` | `IAE-362.md` |
| 34 | `IAE-035` | 363 | Disaster Recovery and Enterprise Support | `20260808300000_create_intelligence_disaster_recovery_enterprise_support.sql` | `server/contracts/disaster-recovery-enterprise-support/` | `disaster-recovery-enterprise-support.sql` | `IAE-363.md` |

**Whole-system checkpoints (not individual capabilities, listed for completeness):** `IAE-036` (Prompt 364, Integrated Verification, `VERIFIED`) — fix migration `20260809000000_harden_intelligence_iae036_integrated_verification_fixes.sql`, build log `IAE-364.md`. `IAE-037` (Prompt 365, Security and AI Hardening, `VERIFIED`) — fix migration `20260809100000_harden_intelligence_iae037_security_ai_hardening.sql`, build log `IAE-365.md`. `IAE-038` (Prompt 366, this document) is `READY`/in progress. `IAE-039` (Prompt 367, Closure Verification) is `BLOCKED` on this checkpoint, per `00_EXECUTION_INDEX.md` §10.

**Independently re-confirmed this checkpoint:** `ls supabase/migrations/ | grep -c 2026080[2-9]` counts exactly the 34 `create_intelligence_*` capability migrations plus 6 hardening/fix migrations (`20260802060000`, `20260803030000`, `20260804060000`, `20260805070000`, `20260805090000`, `20260809000000`, `20260809100000` — 7 fix migrations in total, one per batch/solo-review closure) = 41 Phase-9-numbered files under the `2026080[2-9]*` prefix range, consistent with `00_EXECUTION_INDEX.md`'s own running migration-count narrative (303 total repository-wide as of Group 8's close, growing to 306 after `IAE-037`'s own fix migration per §19 of that index).

---

## 3. Verified dependencies (what a Step 15/16 agent may rely on as fact)

### 3.1 The four `ADR-0025` reuse boundaries (confirmed against the actual migrations, not merely the ADR's own prose)

| Part | Boundary | Confirmed by direct file read this checkpoint |
|---|---|---|
| **A** | Public/Customer/Vendor API (`IAE-009/010/011`) extends `app.api_keys` (`PLT-129`) — no parallel key table | `supabase/migrations/20260719150000_create_api_key_webhook_primitives.sql` is the sole `app.api_keys`/`app.webhook_endpoints` definition repository-wide (`create table app.api_keys` / `create table app.webhook_endpoints` both grep-confirmed to occur in this one file only). `IAE-009/010/011`'s own migrations (`20260804010000`–`20260804030000`) contain no second `create table app.api_keys`-shaped statement. |
| **B** | Webhook Management (`IAE-012`) builds the real outbound delivery worker on `app.jobs`, calling `app.record_webhook_delivery_attempt()`; the pre-existing inbound GPS receiver (`app/api/webhooks/third-party-gps/[connectionId]/route.ts`, Advanced TMS/WMS) is left untouched, not merged | `app.record_webhook_delivery_attempt` is defined once, in `20260719150000_create_api_key_webhook_primitives.sql` (`PLT-129`); `20260804040000_create_intelligence_webhook_management.sql` calls it rather than redefining it. `git log --follow` shows no Phase 9 commit touching `app/api/webhooks/third-party-gps/**`. |
| **C** | Integration Hub (`IAE-008`/336) is a catalog/credential/health/ownership governance layer only — provider adapters (`IAE-014..018`) register into its catalog, never implement a shared protocol interface, per `RPD-038` | `app.integration_connections` is defined once, at `supabase/migrations/20260803020000_create_intelligence_integration_hub.sql:171` (grep-confirmed, sole `create table app.integration_connections` repository-wide). `IAE-014..018`'s own migrations each `insert`/`select` against this same table via distinct adapter-specific functions (`app.get_notification_dispatch_info`, `app.get_geocode_dispatch_info`, etc.) rather than defining a shared interface type. |
| **D** | Automation Rule Engine (`IAE-007`/335) registers new action types into the single canonical job-type registry and enqueues onto `app.jobs`; n8n (`IAE-013`/341) is an optional external trigger/consumer, never a second engine | `app.jobs` is defined once, at `supabase/migrations/20260719170000_create_import_export_job_framework.sql:245`; the canonical job-type single-source-of-truth is `supabase/migrations/20260730410000_harden_job_type_single_source_of_truth.sql`. `IAE-007`'s own migration (`20260803010000`) widens `GENERIC_JOB_TYPES`/the CHECK constraint rather than creating a second queue table — confirmed no `create table app.*jobs*`-shaped statement appears in any Phase 9 migration. |

Full ADR text: `docs/adr/ADR-0025-phase9-api-webhook-automation-and-ai-provider-boundary-foundation.md` (Status: ACCEPTED, 2026-08-21).

### 3.2 Other already-`VERIFIED` phases' primitives Phase 9 composes (each independently confirmed against the cited migration, not merely the claiming build log)

| Primitive | Origin (already `VERIFIED`) | Migration | How Phase 9 composes it |
|---|---|---|---|
| Approval Engine (`PLT-123`) | Platform Core, `CG-S6-PLT-020` | `supabase/migrations/20260719090000_create_approval_engine.sql` (header line 1: "Platform Core capability PLT-123 (Approval Engine, CG-S6-PLT-020)") | `IAE-007` (Automation Rule Engine) calls `app.request_approval(...)` directly at `20260803010000_create_intelligence_automation_rule_engine.sql:693` inside its own `app.request_automation_rule_publish_approval` proxy — grep-confirmed, not merely claimed. `IAE-019` (AI Governance) mirrors the identical `SECURITY DEFINER` domain-scoped proxy pattern for `app.request_ai_output_approval`/`app.decide_ai_output_approval`, composing the same underlying `app.request_approval`/`app.decide_approval_step` primitives (per `IAE-347.md` §4, itself citing a direct read of `app.decide_approval_step`'s own code before the TS contract was written). |
| API keys/webhooks (`PLT-129`) | Platform Core | `supabase/migrations/20260719150000_create_api_key_webhook_primitives.sql` | §3.1 Parts A/B above. |
| Notifications (`PLT-127`) | Platform Core | `supabase/migrations/20260719130000_create_notification_engine.sql` (`create table app.notifications`/`app.notification_delivery_attempts`, grep-confirmed sole definition repository-wide) | `IAE-014` (Email/WhatsApp/SMS) extends `app.notifications`/`app.notification_delivery_attempts` directly — confirmed no second delivery-attempt table shape exists in `20260805010000_create_intelligence_email_whatsapp_sms_integrations.sql`. |
| HRIS employee master (`HRT-274`) | Phase 7 (HRIS/Ticketing) | `supabase/migrations/20260730830000_create_hris_employee_master.sql` (`create table app.employees`) | `IAE-018` (External Accounting/HR) never writes to `app.employees` directly (grep-confirmed zero `insert into app.employees`/`update app.employees` in `20260805050000_create_intelligence_external_accounting_hr_integrations.sql`) — reads only, with an explicit ownership-direction primitive gating any future write-back. |
| Ticketing (`app.create_ticket_for_employee`, HRT-era) | Phase 7 (HRIS/Ticketing) | `supabase/migrations/20260731120000_create_ticket_sla.sql` and related Ticketing migrations | `IAE-021` (OCR Document Processing) composes the existing, unmodified `app.create_ticket_for_employee` as its own sole apply path (`apply_ocr_document_job_to_ticket`) rather than writing a new domain table — confirmed by `IAE-349.md` §7's own self-caught fix narrative (a real concurrency bug in the pre-existing function's idempotency-key handling, live-reproduced under a 6-way race, fixed with a catch-and-reselect wrapper in the OCR-owned caller, the pre-existing function itself untouched). |
| Org units (Platform Core) | Platform Core | `supabase/migrations/20260716101726_create_org_units.sql` (`create table app.org_units`) | Composed by `IAE-020` (AI-Assisted Quotation) via the pre-existing, unmodified `app.create_quotation_draft` (`COM-151`), which itself validates `org_unit_id` — the tenant-validation gap Batch-4-Tier-C found and fixed there (§4 below) was in this composition path specifically. |
| Finance accounts / chart of accounts (Finance domain) | Phase 4 (Finance) | `supabase/migrations/20260728210000_create_finance_chart_of_accounts.sql` | `IAE-018` (External Accounting/HR) never writes to `app.finance_accounts`/`app.finance_journals` directly (grep-confirmed zero matches in `20260805050000_create_intelligence_external_accounting_hr_integrations.sql`) — read/reference only, mirroring the identical employee-table boundary discipline in the same migration. |
| Finance invoices (`FIN-197`) | Phase 4 (Finance) | `supabase/migrations/20260729110000_create_finance_invoice.sql` (`create table app.finance_invoices`) | `IAE-017` (Bank/Payment/eInvoice/Tax) e-invoice submission requires an already-issued invoice and never mutates `app.finance_invoices` — confirmed only an evidence table (`app.finance_provider_call_evidence`) is written in `20260805040000_create_intelligence_bank_payment_einvoice_tax_integrations.sql`. |
| Finance tax rule versions (`FIN-195`, `RPD-016`) | Phase 4 (Finance) | `supabase/migrations/20260729090000_create_finance_tax_baseline.sql` (`create table app.finance_tax_rule_versions`) | `IAE-017`'s tax-authority-rate lookup writes only its own evidence row; any actual rate-rule approval still routes through the pre-existing, evidence-CHECK-enforced `app.approve_finance_tax_rule`, unmodified by Phase 9. |
| Finance bank statement import (`FIN-211`) | Phase 4 (Finance) | `supabase/migrations/20260729250000_create_finance_cash_bank.sql` | `IAE-017`'s bank-feed sync worker (`lib/finance-integrations/process-finance-bank-feed-sync-job.server.ts`) calls the existing, unmodified `app.import_finance_bank_statement` and inherits its idempotency/dedup machinery, per `IAE-345.md` §4. |
| Advanced TMS/WMS telematics adapter registry (`ATW-226E`) | Phase 5 | Advanced TMS/WMS migrations (`ATW-226` family) | `IAE-015` (Maps/GPS/Telematics) confirmed the "additional approved provider adapters" half of its own brief was already fully solved by the existing, unmodified `app.register_third_party_provider_connection` — live-proven in its own db-test rather than a new schema, per `IAE-343.md`. |

### 3.3 Cross-batch/module-seeding corrections (load-bearing, do not re-derive from the dependency diagram alone)

The API/Webhook Ecosystem track's cross-batch dependency is **`IAE-007`'s own `INTHUB` permission-module seed** — confirmed at `20260803010000_create_intelligence_automation_rule_engine.sql:155` (`insert into app.entitlement_modules (code, name, owning_phase) values ('INTHUB', ...)`) — **not** `IAE-008`/Integration Hub, which `00_EXECUTION_INDEX.md` §5/§9 discloses was originally mis-attributed and corrected by Batch 3's own Tier C review. A Step 15/16 agent tracing `INTHUB`-gated authority back to "where the module was born" must land on `IAE-007`, not `IAE-008`.

---

## 4. Preserved assets (what Phase 9 built — must not be assumed away or duplicated)

A Step 15/16 (or any later) agent must treat the following as already-built, already-`VERIFIED` primitives — building a parallel version of any of these would be a reviewable Tier B defect against `ADR-0025` or the capability's own migration, not an ambiguous judgment call:

- **`app.integration_connections`** (`20260803020000_create_intelligence_integration_hub.sql:171`) — the shared credential/health/ownership record every one of `IAE-014..018`'s five provider adapters registers into. A future integration must extend this table's own adapter-registration pattern, never fork a second connections table.
- **`app.ai_governed_requests`** (`20260805060000_create_intelligence_ai_governance_provider_boundary.sql:133`) — the shared AI-dispatch/approval/cost-metering boundary every one of `IAE-020..025`'s six AI-assisted capabilities composes via `dispatchAiGovernedRequest` (`lib/ai-governance/dispatch-ai-governed-request.server.ts`). No AI-touching capability writes to a domain table directly — confirmed structurally (no `INSERT`/`UPDATE`/`DELETE` grant on any table outside the `AI` module's own schema in `IAE-019`'s migration) and empirically (Batch 4 Tier C review's own table-write diff, `00_EXECUTION_INDEX.md` §14).
- **`app.jobs` job-type registry** — widened, never forked, by `IAE-007` (automation), `IAE-012` (`webhook_retry`), `IAE-016/017/018` (provider sync jobs), `IAE-029` (`audit_export`), `IAE-031` (`retention_archive`). The four-place lockstep (`app.jobs` CHECK, `app.generic_job_types()`, `GENERIC_JOB_TYPES`, `IMPORT_EXPORT_JOB_TYPES`) is itself a preserved convention a Step 15/16 agent must keep in sync when adding any new job type, per the repeated self-caught lessons recorded across `IAE-016/017/018`'s own build logs.
- **The eight new Phase 9 module codes**, each seeded exactly once (grep-confirmed this checkpoint): `INTHUB` (`IAE-007`, `20260803010000:155`), `AI` (`IAE-019`, `20260805060000:103`), `IAM` (`IAE-026`, `20260807000000:68`), `SEC` (`IAE-027`, `20260807100000:54`), `MON` (`IAE-030`, `20260807400000:48`), `RET` (`IAE-031`, `20260807500000:69`), `DEPLOY` (`IAE-032`, `20260808000000:67`), `SUP` (`IAE-035`, `20260808300000:76`). A future capability needing one of these authority domains extends the existing module's own action set (as `IAE-027`/`IAE-028` both reuse `SEC`, and `IAE-033/034` both reuse `DEPLOY`/`MON`) rather than minting a ninth Phase-9-flavored module for the same concern.
- **The Loyalty-style "governance record, never a domain mutation" shape**, reused six times across Group 6 (`app.ocr_document_jobs`, `app.eta_predictions`, the optimization "acknowledge, never mutate" record, `app.risk_signal_*`, the forecasting advisory records) — a Step 15/16 agent extending any of these should preserve the "AI output surfaces for human/UI consumption only, never an autonomous write" invariant rather than treat it as an incidental Phase 9 implementation detail.
- **`server/mutations/enterprise-monitoring.ts`** — written at Group 7's own Tier C review (not at `IAE-030`'s original commit, whose build log had falsely claimed it existed; `00_EXECUTION_INDEX.md` §16) — is now real and tested. A Step 15/16 agent should not assume any other build log's own "created" claim is automatically accurate without spot-checking; this specific gap is closed, but the review found it precisely because it independently verified rather than trusted the log.
- **The region/service-capability matrix schema** (`app.region_service_capabilities`, `app.region_capability_exceptions`, six categories including `files` added at Group 8's own Tier C review) exists and is correctly *recorded*, but see §5's own explicit warning — it has **zero run-time enforcement consequence** (`ISS-2026-152`). Do not assume registering an "unsupported" exception for a region actually blocks anything at dispatch time; it does not, today.
- **The step-up-authorization mechanism** (`app.assert_current_step_up_authorization`, `IAE-027`) exists, is independently correct, and is fully tested in isolation — but see §5; it has **zero live callers** anywhere in the repository (`ISS-2026-151`). Preserve the mechanism; do not assume it does anything yet.

---

## 5. Step 15/16 boundary — what a future Full-System-Hardening or full-release agent needs to know

**This section is documentation only.** Per Prompt 366 §12's own forbidden-scope rule ("Step 15–16 full release work"), none of the gaps named below were attempted, partially wired, or scoped into a fix by this checkpoint. This section exists solely so a future Step 15/16 agent does not have to rediscover these boundaries from scratch, and does not itself constitute Step 15/16 work.

### 5.1 Concrete extension points that already exist

- **The Integration Hub catalog** (`app.integration_connections`, §4 above) is a ready adapter-registration point for any future provider (a 6th, 7th, ... adapter registers the same way `IAE-014..018` did — no schema change needed).
- **The AI governance dispatch boundary** (`dispatchAiGovernedRequest`, §4 above) is a ready entry point for a 7th AI-assisted capability — the `_parse_*` defensive-extraction pattern and the recursive-masking pattern (`mask_optimization_sensitive_fields`/`mask_forecast_small_cohort_fields`, both now depth-capped at 32 per `IAE-365.md`) are proven, reusable conventions.
- **The `app.jobs` job-type registry** is ready for a new async job type via the established four-place lockstep (§4 above).
- **`app.audit_logs.support_access_grant_id`** (`IAE-029`) now has a real default-from-live-session behavior (fixed at `IAE-037`/Prompt 365, finding 7) — a Step 15/16 agent building further impersonation-adjacent audit linkage can compose this rather than re-deriving actor-session correlation from scratch.
- **The enterprise onboarding checklist** (`app.get_enterprise_onboarding_checklist`, `IAE-035`) now live-recomputes all five automated items on every read (fixed at `IAE-036`/Prompt 364) — a genuine, tested precedent for "never trust a stored verification snapshot" that a Step 15/16 agent extending onboarding/readiness checks should follow, not re-litigate.

### 5.2 Concrete gaps a Step 15/16 agent must NOT assume are already solved

- **RPD-023 step-up-authorization has a real, correct mechanism with zero live enforcement anywhere (`ISS-2026-151`, High, OPEN).** `app.assert_current_step_up_authorization` (`IAE-027`) was built, and `IAE-037`/Prompt 365 deliberately wired it into 4 bounded target functions (`app.decide_ai_output_approval`, `app.activate_enterprise_idp_connection`, `app.approve_mfa_exception`, `app.create_integration_connection`), live-tested the change, and found it broke 17 already-`VERIFIED` capabilities' own db-test fixtures across 5 groups (Integration Hub, all 5 provider integrations, all 5 Group 6 AI capabilities, AI governance, enterprise IAM/MFA, disaster recovery/support) — none of which model a step-up challenge today. **The wiring was reverted, not shipped half-adapted.** A Step 15/16 agent must not assume RPD-023 is enforced for any of these four functions, or for any other high-risk action `app.is_high_risk_action` classifies — it must wire the 4 named functions **and** adapt all 17 named fixture files in the same change, never one without the other (a partial wire-up silently breaks whichever of the 17 capabilities happens to call a newly-gated function without a fresh step-up challenge). The exact function list and fixture list are recorded in `ISS-2026-151`'s own full text and `IAE-365.md` §6.
- **IP-restriction enforcement is structurally unreachable from any real business mutation (`ISS-2026-150`, High, OPEN).** `app.assert_ip_allowed` (`IAE-028`) is real and correct when called directly, but has zero real callers outside its own migration/db-test/TS wrapper — a fully-configured, `enforced`-mode IP allowlist provides **zero actual protection** today. Worse, a `service_role`-executed background job/worker has no HTTP request context at all, so no future middleware alone can close this gap for that class of caller — the RPC layer itself needs the check. A Step 15/16 agent must (a) build real HTTP-client-IP extraction/threading through the TypeScript mutation layer and relevant route handlers, (b) wire `assert_ip_allowed` into the same bounded highest-risk `SEC`/`IAM`/`INTHUB` mutations `ISS-2026-151` already names, and (c) decide the correct behavior for service-role/background-job callers with no client IP (likely: exempt).
- **Zero load/performance-test evidence exists for any Phase 9 route or RPC at any declared target volume (`ISS-2026-148`, Medium, OPEN; consistent with `ISS-2026-141`'s identical Phase 8 disclosure).** No `k6` (or equivalent) exists anywhere in this repository for Phase 9's reporting engine, dashboard builder, materialized-view refresh, scheduled reports, automation rule engine, the whole `/api/v1` surface, the webhook delivery worker, any AI-governed dispatch path, or enterprise IAM/monitoring/DR. A Step 15/16 agent must not assume any Phase 9 capability has been load-tested at any volume — this is a real, undone task, ideally covering Phase 8 and Phase 9 routes/RPCs together (per `ISS-2026-148`'s own recommended scope) rather than piecemeal.
- **The region/service-capability matrix (`IAE-033`) has zero run-time consequence on any data-plane function (`ISS-2026-152`, Medium, OPEN).** A tenant can have `ai_provider` explicitly registered as an accepted-risk *unsupported* category for its own region, and an ordinary `app.request_ai_governed_action` call still succeeds end to end with zero reference to the matrix — the matrix is checked exactly once, at region-assignment approval time (paperwork), never again at actual dispatch time. A Step 15/16 agent must not assume registering a capability exception has any live enforcement, warning, or audit-flag effect at the point of use — it does not, until `app.request_ai_governed_action` (and any future `region_service_capabilities`-gated entry point) is made to consult live region/capability state at dispatch time.
- **A pre-existing, repository-wide cross-tenant `tenant_id`-in-exception-message pattern (`ISS-2026-146`, Low, OPEN, taxonomy class C-05) occurs 2,087+ times since Phase 6, reproduced (not introduced) in Group 8.** A Step 15/16 agent should not attempt to patch only the ~8 Group 8 occurrences named in the finding — a real fix needs one dedicated, repository-wide sweep, not another partial one.
- **RPD-023's own disclosure completeness across the whole platform is not this checkpoint's to re-audit** — `ISS-2026-151`/`150` are the Phase 9-specific instances; a Step 15/16 agent doing platform-wide MFA/step-up hardening should treat these two issues as the authoritative Phase 9 starting point, not assume Phase 9's own capabilities are already covered by whatever Phase-1-era disclosure exists elsewhere.
- **Zero automated REST-route-level test coverage exists for the 9 `/api/v1` handlers `IAE-009/010/011` expose (`ISS-2026-147` item 1, Medium, OPEN)** — the underlying RPCs are fully covered by their own db-test files, but the HTTP-layer glue (request parsing, header/auth extraction, response shaping, error-code-to-HTTP-status mapping) has zero dedicated `.test.ts` coverage (`find app/api -name "*.test.ts"` returns zero matches, re-confirmed this checkpoint). A Step 15/16 agent building further REST surface on top of this platform should not assume the existing 9 routes are safe to refactor without first adding this coverage.
- **This is not a production/pilot/GA/market-ready system** (RPD-001/034/036, restated per this document's own governing instruction) — no live Supabase project, no real sign-in flow, no real deploy pipeline exists anywhere in this repository, unchanged since every prior phase's own identical disclosure. A Step 15/16 agent's own "full release" framing must not be read as license to skip re-establishing this from scratch; Phase 9 changes nothing about this standing fact.

---

## 6. Known issues — every currently-OPEN Phase-9-owned `KNOWN_ISSUES.md` entry

Read `docs/runtime/KNOWN_ISSUES.md` directly for the authoritative, current, append-only list — this table is a snapshot, read in full this checkpoint (every ID below read in its entirety, not grepped for a header line alone). Grep-confirmed this checkpoint: **`ISS-2026-143` through `152` is the complete, current Phase-9-owned range — no `ISS-2026-153` or higher exists anywhere in the file as of this checkpoint.** All ten are `OPEN`; none is Critical.

| ID | Severity | Capability | Owner | One-line summary |
|---|---|---|---|---|
| `ISS-2026-143` | Low | Phase 9 kickoff (`IAE-001`, documentation) | A future documentation-only correction task, or the Phase 9 documentation/handoff prompt (this checkpoint) | `docs/architecture/13_FULL_WORK_BREAKDOWN_STRUCTURE.md:75` labels Phase 9's short code `IEP`; every actual Phase 9 prompt file/build log/coverage-matrix row uses `IAE` — a disclosed, non-blocking naming mismatch, not silently resolved either way |
| `ISS-2026-144` | Low | Phase 6 (Procurement/Vendor, pre-existing) | A dedicated future Procurement/Vendor KPI recovery task | `scripts/db-tests/procurement-vendor-performance.sql:978` intermittently fails a pre-existing, `now()`-relative `rate_validity` computability assertion for a sparse-data "New Vendor" fixture — confirmed intra-day flaky (4 of 6 runs failed, 1 passed, same unmodified file), not a Phase 9 defect |
| `ISS-2026-145` | Low | Phase 9 test infrastructure (`IAE-002`) | A dedicated future test-infrastructure task | `scripts/db-tests/rbac-enforcement.sql`'s O(n²) actor-identity call-graph sweep now takes 15-20+ minutes standalone against the ~2,398-function `app` schema — a build-velocity cost, not a correctness defect; every future Phase 9+ prompt's own Tier A gate pays this same, further-growing cost |
| `ISS-2026-146` | Low | Cross-cutting, repository-wide (reproduced in Group 8, `IAE-032..035`) | A dedicated future repository-wide hardening task | Cross-tenant `tenant_id` disclosure via exception message text before the caller's own authority is evaluated — confirmed 2,087+ pre-existing occurrences since Phase 6; narrow blast radius (a non-secret UUID, requiring an out-of-band record-id leak first) |
| `ISS-2026-147` | Medium (item 1) / Low (item 2) | Phase 9 API/n8n (`IAE-009/010/011`, `IAE-013`) | A dedicated future Phase 9 test-coverage task (item 1); a documentation-evidence follow-up on `IAE-341.md` (item 2) | Two Batch-3-disclosed findings (zero REST-route-level test coverage for 9 `/api/v1` handlers; `IAE-013`'s per-connector log-filtering evidence narrower than claimed) were never promoted into `KNOWN_ISSUES.md` at the time, breaking the discipline every later batch followed |
| `ISS-2026-148` | Medium | Phase 9, all capabilities | A dedicated future performance-test-harness task (scoped jointly with Phase 8's identical `ISS-2026-141`) | Zero load/performance-test evidence exists for any Phase 9 route or RPC at any declared target volume — undisclosed until `IAE-036`'s own Integrated Verification |
| `ISS-2026-149` | Low | Enterprise IAM (`IAE-026`) | Whichever future task first wires `resolveEnterpriseIdpByEmailDomain` to a live HTTP login route | `app.resolve_enterprise_idp_by_email_domain` is an unthrottled, anonymous, cross-tenant SSO-configuration enumeration oracle — currently dead code at the HTTP layer (zero real TS caller, `app` schema not yet PostgREST-exposed), so not live-exploitable at scale today |
| `ISS-2026-150` | **High** | Enterprise Security (`IAE-028`) | A dedicated future task (route-handler IP-extraction/threading, then wiring `assert_ip_allowed` into the bounded high-risk function set) | RPD-023's IP-restriction enforcement is real and correct when called directly, but has zero real callers anywhere — a fully-configured `enforced`-mode allowlist provides zero actual protection; service-role/background callers have no client IP at all, a structural gap no middleware alone can close |
| `ISS-2026-151` | **High** | Enterprise Security (`IAE-027`) | A dedicated future task with an exact worklist (4 named functions + 17 named fixture files, wired together, never one without the other) | The mandatory RPD-023 step-up-authorization ruling: `app.assert_current_step_up_authorization` is real and correct but has zero live callers; wiring it in was built, live-tested, found to break 17 already-`VERIFIED` capabilities' own fixtures, and deliberately reverted rather than shipped half-adapted |
| `ISS-2026-152` | Medium | Multi-Region/Data Residency (`IAE-033`) | A dedicated future task (make `app.request_ai_governed_action`, and any future region-gated entry point, consult live region/capability state at dispatch time) | The region/service-capability matrix has zero run-time consequence on any data-plane function — checked once at region-assignment-approval time, never again at actual point of use; a governance/audit-trail gap, not an active breach, since no region infrastructure is actually deployed anywhere |

**Resolved during Phase 9, not carried forward as open risk (for context only):** every finding fixed within each batch's own Tier C review (`00_EXECUTION_INDEX.md` §14/§16/§17/§18/§19) is not re-listed here — see each batch's own closure narrative for the full fixed-defect record, including the 1 Critical (`app.record_ai_governed_request_outcome` lost-update, Batch 4), the 1 Critical (legal-hold bypass, Group 7), and the 8 findings fixed at `IAE-037`/Prompt 365's own adversarial hardening pass.

---

## 7. Forbidden-scope confirmation (Prompt 366 §11/§12/§24, checked this checkpoint)

`git status --short` immediately before writing this document showed a clean working tree at commit `6da6af5` (branch `claude/execute-prompt-349-368-zi0nqs`); after this checkpoint's own documentation work, `git status --short` shows exactly one new file — this document, under `docs/build-log/phase-09/`. No migration, `server/`, `app/`, `lib/`, or `scripts/db-tests/` file was touched, matching Prompt 366 §13/§14/§15's own "no planned feature schema/no unplanned endpoints/verify-only" mandate. This checkpoint did not edit `docs/build-log/phase-09/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md`, `docs/runtime/HANDOFF.md`, or `docs/runtime/KNOWN_ISSUES.md` — those remain the orchestrating session's own job. No Step 15/16 full-release work, new capability, tenant fork, or applied-migration edit was performed, per §12's own forbidden-scope list.

**This is not a production/pilot/GA/market-ready claim** (RPD-001/034/036) — restated here as this document's own closing disclaimer, mirroring every other Phase 9 checkpoint's identical disclosure. Package completion (this document) is not runtime implementation or evidence, per Prompt 366 §24's own business rule.
