# IAE-348 — AI-Assisted Quotation

**Prompt ID:** `CG-S14-IAE-020` · **Package document:** `docs/ai-agent-build-prompt-package/14-phase-09-intelligence-enterprise/348_AI_ASSISTED_QUOTATION_PROMPT.md` · **Phase:** 9 (Intelligence, Automation and Enterprise Expansion) · **Status: `COMPLETED`** (2026-08-22 — seventh and final capability of the merged Batch 4, Tier A/B self-reviewed and gate-verified individually; unlike `IAE-014..019` this capability was deliberately held OUT of the merged batch's own single Tier C review, since it required `IAE-019` `VERIFIED` — not merely `COMPLETED` — as a precondition to even begin, and that precondition was only satisfied once that review closed. Its own path to `VERIFIED` is recorded in `00_EXECUTION_INDEX.md` §10/§14.)

## 1. What this checkpoint builds

The FIRST real consumer of IAE-019's own AI governance boundary. A sales user requests AI assistance on a quotation; the system dispatches a real, governed AI request citing real, versioned rate/margin sources from the opportunity's own costing chain; a human reviews the suggestion and either dismisses it or explicitly accepts it into a real `app.quotations` draft — at which point the existing, wholly unmodified Commercial quotation submit → approve → send → customer-decision flow (COM-151 through COM-154) is the only path to any customer-facing commitment. No new send/accept mechanism is built here; this checkpoint's own scope is bounded to exactly the AI-suggestion-tracking layer sitting BEFORE that pipeline.

## 2. Design decisions (cited, not re-derived)

Full rationale lives in the migration's own header (`supabase/migrations/20260805080000_create_intelligence_ai_assisted_quotation.sql`, 9 numbered decisions). Summary:

1. **AI output can NEVER reach `app.quotations`/`app.quotation_lines` directly — structurally, not by convention.** The ONLY write path is `app.accept_ai_quotation_suggestion_as_draft`, and even it never parses or trusts the AI's own `output_payload` — only the human-submitted, explicitly-typed `p_accepted_lines`, handed unchanged to the EXISTING, UNMODIFIED `app.create_quotation_draft`/`app.add_quotation_line` (COM-151/152).
2. **Human edits and the final decision are recorded SEPARATELY from the AI suggestion, by construction.** The AI's own suggestion is immutable evidence in IAE-019's own `app.ai_governed_requests.output_payload`; this checkpoint's own `app.ai_quotation_suggestions` tracks only the human's review decision; the actual accepted lines live only in `app.quotation_lines` — three genuinely distinct tables, three genuinely distinct writers.
3. **"Low-confidence/no-source output blocks auto-draft acceptance" is a hard, structural gate at the ONE convenience RPC this blocks, never elsewhere.** `accept_ai_quotation_suggestion_as_draft` refuses unless the governed request's own `confidence_label` is `high`/`medium` (never `low`, never null) AND every accepted line cites a real `margin_calculation_id`.
4. **No new approval step is required before draft status.** Entering `draft` commits nothing customer-facing and is fully reversible; the real critical-status gate is the existing, unmodified COM-153/154 approval/send flow, unchanged by this checkpoint.
5. **`create_quotation_draft`/`add_quotation_line` run inside ONE function-call transaction** — any line failing their own independent validation rolls the whole `accept_ai_quotation_suggestion_as_draft` call back atomically.
6. **The AI dispatch reuses `dispatchAiGovernedRequest` (IAE-019) completely unmodified** — `feature_code = 'ai_assisted_quotation'`, `correlation_record_type = 'opportunity'`. Zero new rows in `app.integration_adapters`, zero new `app.jobs` job types.
7. **Authority reuses the existing `COM` module directly** — no new entitlement module.
8. **Every `authenticated`-granted function is `SECURITY DEFINER` plus `app.assert_actor_is_session_identity` from the very first draft** — the merged Batch 4 Tier C review's own most-repeated lesson, applied proactively.
9. Per `ERR-2026-004`: explicit `revoke execute on all functions in schema app from public;`.

## 3. Files changed

**Migration (1, additive):**
- `supabase/migrations/20260805080000_create_intelligence_ai_assisted_quotation.sql`.

**DB test (1 new file):**
- `scripts/db-tests/ai-assisted-quotation.sql` — a fresh, distinctive two-tenant fixture (`iaeaiq`/`iaeaiq2`, id range `00000000-0000-0000-0000-000023xxxxxx`) building a REAL opportunity → costing_request → rate_selection → margin_calculation chain (mirroring the established costing golden path), a real `openai_multimodal` connection, and covering every function's own authority/status/confidence/rollback behavior plus a live concurrency proof (§10).

**Service layer (3 new files):**
- `server/contracts/ai-quotation/ai-quotation.ts` (+ `.test.ts`).
- `server/queries/ai-quotation.ts`, `server/mutations/ai-quotation.ts`.

**The AI-dispatch orchestration client (1 new file, no worker/queue — synchronous, reuses IAE-019's own dispatch client verbatim):**
- `lib/ai-quotation/generate-quotation-suggestion.server.ts` (+ `.test.ts`, 6 tests against a real local `node:http` server).

**UI:** none — a sales-facing suggestion review UI is disclosed, deferred (§8).

**Docs:** this file, plus `docs/build-log/phase-09/00_EXECUTION_INDEX.md`.

## 4. RPC surface (6 new)

| Function | Grant | Note |
|---|---|---|
| `app.check_ai_quotation_suggestion_authority` | `service_role` only | Thin wrapper over `app.evaluate_permission(..., 'COM', ...)` |
| `app.record_ai_quotation_suggestion` | `authenticated, service_role` | Called AFTER a real, succeeded governed request; idempotent per `ai_governed_request_id` |
| `app.get_ai_quotation_suggestion` | `authenticated, service_role` | `COM:View`-gated, joins in the governed request's own evidence |
| `app.list_ai_quotation_suggestions_for_opportunity` | `authenticated, service_role` | `COM:View`-gated |
| `app.dismiss_ai_quotation_suggestion` | `authenticated, service_role` | `COM:Edit`-gated, atomic pending-only transition |
| `app.accept_ai_quotation_suggestion_as_draft` | `authenticated, service_role` | `COM:Create`-gated, the key function (§1, §10) |

## 5. Schema

One new table: `app.ai_quotation_suggestions` (id, tenant_id, opportunity_id, ai_governed_request_id unique, status, accepted_quotation_id, dismiss_reason, requested/reviewed actor columns, created_at). A CHECK constraint enforces the accepted-shape invariant: `status = 'accepted'` if and only if `accepted_quotation_id is not null`. No change to any existing Commercial or AI governance table.

## 6. RLS and grants

`app.ai_quotation_suggestions` enabled with RLS and zero policies, and carries no direct table grant to `authenticated` at all — the only read paths are the two `SECURITY DEFINER` functions above. `anon` holds zero `EXECUTE` on any of this checkpoint's 6 functions (live-verified, §10).

## 7. Tier B — recurring defect taxonomy self-check

- **C-12/C-13 (`SECURITY DEFINER` authority, actor-identity)** — applied proactively to every `authenticated`-granted function from the first draft (design decision 8), the merged Batch 4 Tier C review's own most-repeated lesson.
- **C-05 (permission check after disclosure)** — `accept_ai_quotation_suggestion_as_draft` checks `COM:Create` before any read of the underlying governed request or any mutation; live-verified the suggestion remains `pending` after a rejected unauthorized attempt (§10).
- **Self-caught, before commit (three real bugs, none reached the applied migration until fixed):**
  1. **Nullable-column `<>` gap.** `record_ai_quotation_suggestion`'s own correlation cross-check used a bare `<>` against `correlation_record_type`/`correlation_record_id`, both nullable columns — `NULL <> 'opportunity'` evaluates to `NULL`, which plpgsql's `IF` treats as false, silently skipping the raise for a governed request with no correlation set at all. Fixed with `IS DISTINCT FROM`; a dedicated db-test regression block proves a null-correlation request is now rejected.
  2. **`RETURNS TABLE` column-shadowing ambiguity.** `get_ai_quotation_suggestion` declares `RETURNS TABLE(id uuid, ...)`, which implicitly declares `id` (and every other out-column name) as a plpgsql variable in the function's own namespace — its bare `where id = p_suggestion_id` lookup was ambiguous against the table's own `id` column, a real runtime error (`column reference "id" is ambiguous`) caught live by the db-test run, not by static reading alone. Fixed with an explicit table alias (`s.id`), matching the pattern the rest of the function already used correctly in its own `return query`.
  3. **CHECK-constraint-violating two-step write.** The original draft set `status = 'accepted'` in an early atomic-guard UPDATE, then a SECOND UPDATE set `accepted_quotation_id` afterward — the FIRST statement alone violated the table's own `ai_quotation_suggestions_accepted_shape_check` (which requires both fields change together), caught live on the very first successful-acceptance test. Fixed by reordering: `create_quotation_draft`/`add_quotation_line` now run BEFORE the suggestion's own atomic transition, and the single final UPDATE sets `status` and `accepted_quotation_id` together, keeping the WHERE-clause-only concurrency guard (Tier C fix 12's own lesson) while satisfying the constraint. A live 6-way concurrent-process race (§10) confirms zero orphaned quotation drafts survive from any losing caller.
- **ISS-2026-034-class (rate-limit/retry discriminator)** — not applicable; this capability has no retry/backoff surface of its own (the underlying dispatch's own retry posture is IAE-019's, unmodified).

No taxonomy class was skipped.

## 8. Residual/disclosed limitations

- **No dedicated suggestion-review UI was built** — consistent with every prior Batch 4 capability's own scope decision. The RPC surface and orchestration client are real and fully tested; a sales-facing "AI suggested this quotation" review screen is deferred to a future checkpoint.
- **Provider unit cost is a real, disclosed PLACEHOLDER computation**, inherited unchanged from IAE-019's own dispatch client — no live provider pricing feed exists anywhere in this repository.
- **The orchestration client's own prompt payload is a first, reasonable composition** (opportunity summary + current margin calculations from the latest costing request only) — it does not walk EVERY historical costing request/margin calculation for an opportunity, only the most recent. A future iteration could widen this; not required to prove the underlying AI-governance-to-quotation-draft mechanism this checkpoint exists to build.

## 9. Commands run and results

| Command | Result |
|---|---|
| `pnpm run typecheck` | **0 errors** |
| `pnpm run lint` | **0 errors** (pre-existing warnings only, none in this checkpoint's own files) |
| `pnpm run test` | **5156/5156 pass** |
| `next build` | not required — this checkpoint touches no `app/`, `components/`, or `"use server"` module (Tier A conditional gate) |
| `pnpm run docs:check` / `security:check` / `security:audit` / `data-classification:check` / `standards:check` / `threat-model:check` / `git:check-paths` | all clean |
| Fresh disposable-database standalone run of `ai-assisted-quotation.sql` | all assertion groups pass — `ALL PASSED` |
| `bash scripts/db-tests/run.sh` (full official harness) | ran 139 files clean including this checkpoint's own new file, then hit a genuinely pre-existing, unrelated flake in `hris-overtime-timesheet.sql` (§9a) |
| Remaining 74 files, run standalone against the same already-migrated leftover database | **74/74 pass** — full suite coverage confirmed (213/214 files pass; the 1 exception is the pre-existing flake below, untouched by this checkpoint) |
| `node --experimental-strip-types --test lib/ai-quotation/generate-quotation-suggestion.server.test.ts` | 6/6 pass |
| 6-way genuinely concurrent `psql` race on `accept_ai_quotation_suggestion_as_draft` | exactly 1 winner, 5 losers, zero orphaned quotation drafts (§10) |

**§9a — pre-existing, batch-unrelated flake, confirmed not caused by this checkpoint.** `scripts/db-tests/hris-overtime-timesheet.sql` derives its own `work_date` from a real attendance session keyed off `current_date` (today's actual wall-clock date), then asserts a HARDCODED `eligible_classification = 'weekday'`. Today (2026-08-22) is a real Saturday, so the assertion is simply wrong whenever this suite runs on a weekend — a genuine, deterministic (not merely intermittent) pre-existing defect in a file last touched at commit `39ef8f4` (Prompt 281, HRT-009), confirmed via `git diff --stat HEAD -- scripts/db-tests/hris-overtime-timesheet.sql` showing zero changes from this session. Unrelated to IAE-020's own migration (no shared table, function, or fixture data) and out of this checkpoint's bounded scope to fix. Worked around, per this session's own established precedent for the separate pre-existing HRIS midnight-boundary flake, by running every remaining file standalone against the same already-migrated database to obtain full coverage without waiting for a weekday.

## 10. Live adversarial proof (against a real disposable Postgres 16 database and a real local HTTP server, not reasoned about)

1. **`record_ai_quotation_suggestion`'s full cross-check chain, each live-triggered**: `insufficient_authority` for a `COM:View`-only actor; `ai_quotation_suggestion_opportunity_not_found` for a non-existent opportunity; `ai_quotation_suggestion_request_tenant_mismatch` for a request genuinely owned by a different tenant (its own real connection, its own real dispatch); `ai_quotation_suggestion_wrong_feature` for a real succeeded `geocode_address` request; `ai_quotation_suggestion_correlation_mismatch` for a real succeeded request with NO correlation set at all (the Tier B self-caught nullable-`<>` regression, §7); `ai_quotation_suggestion_request_not_succeeded` for a still-pending request; idempotent on a retried call for the same governed request (same row returned, zero duplicates).
2. **Read paths surface real evidence and enforce real authority**: `get_ai_quotation_suggestion` returns the real `output_payload`/`confidence_label`/`request_status` from the joined governed request; a genuine cross-tenant actor with zero grants in the suggestion's own tenant is denied `insufficient_authority`, never a silently-empty result.
3. **`dismiss_ai_quotation_suggestion`'s atomic pending-only transition**: `insufficient_authority` for a `COM:View`-only actor; a real dismiss succeeds; a second dismiss on the same suggestion raises `ai_quotation_suggestion_not_pending`.
4. **`accept_ai_quotation_suggestion_as_draft`'s full gate sequence, each live-triggered, suggestion status re-verified `pending` after every blocked attempt**: `insufficient_authority` (authority checked before any read of the governed request); `ai_quotation_suggestion_low_confidence_blocked` for both an explicit `low` and a `null` confidence label; `ai_quotation_suggestion_no_lines_provided` for an empty array; `ai_quotation_suggestion_missing_source` for a line with no `margin_calculation_id` (whole-function rollback confirmed — suggestion still `pending` afterward); a dismiss attempt on an already-accepted suggestion also correctly blocked by the same guard.
5. **A real, successful acceptance** produces a real `app.quotations` row (`status = 'draft'`, correct `opportunity_id`); the resulting `app.quotation_lines` row's own `cost_amount_snapshot`/`margin_pct_snapshot` are byte-equal to the sourcing `app.margin_calculations` row's `cost_amount`/`margin_pct`, read directly from the base tables — not merely asserted from the RPC's own return value.
6. **6-way genuinely concurrent race** (real background `psql` processes, `&` + `wait`, not reasoned about): 6 processes call `accept_ai_quotation_suggestion_as_draft` on the SAME real pending suggestion simultaneously — exactly 1 succeeds, the other 5 each receive `ai_quotation_suggestion_not_pending`; a direct count of `app.quotations` rows for the opportunity afterward confirms exactly 2 (one from the earlier sequential test, one from the race's own single winner) — zero orphaned drafts from any of the 5 losing callers, confirming the reordered whole-function-rollback design (§7) holds under real concurrency, not just in the single-caller case.
7. **`anon` holds zero `EXECUTE`** on any of this checkpoint's 6 functions; `app.ai_quotation_suggestions` refuses a direct `authenticated` `select` at the grant level (`permission denied`), never merely filtering to zero rows via RLS — confirming the SECURITY DEFINER functions are genuinely the ONLY read path.
8. **The real AI-dispatch orchestration client**, proven against a real local `node:http` server: a 2xx response with a valid output object and `confidenceLabel: "high"` succeeds and tracks a real pending suggestion citing the real, current margin calculation as its source; an opportunity with no current costing evidence yet dispatches with a deliberately empty `marginCalculations` array rather than fabricating a source; a real 5xx provider response fails cleanly and never calls `record_ai_quotation_suggestion`; a superseded (non-current) margin calculation is correctly excluded from the prompt; an opportunity that does not belong to the calling tenant throws before any dispatch is attempted.

## 11. Rollback

Purely additive: one new migration (one new table, six new functions, zero altered existing tables/functions/grants), one new db-test file, five new service-layer/client files. A plain `git revert` of this checkpoint's own commit removes all of it cleanly; no prior capability's own evidence is touched.

## 12. Next eligible prompt

`CG-S14-IAE-020` was the last of the merged Batch 4's seven capabilities (`IAE-014..020`, Prompts 342-348) and the last of the currently-authorized Prompt 328-348 range. Per Prompt 348 §36, only the execution index may release the next dependency-clean prompt after this task reaches `VERIFIED`. Prompts 349-353 remain `BLOCKED — outside this checkpoint's authorization; requires fresh operator authorization for 349+ before release` (`00_EXECUTION_INDEX.md`). This capability's own path to `VERIFIED` — since it falls outside the merged batch's own already-closed Tier C review scope — is resolved in `00_EXECUTION_INDEX.md` §10/§14.
