# HDN-387 — Release Blocker Triage and Remediation

**Prompt:** `CG-S15-HDN-019` (Prompt 387, `CG-AABPP-HDN-387`)
**Status:** **`VERIFIED`** — first round + Tier C review both complete.
**Branch:** `claude/step-15-hdn-369-kickoff-w6qren`

## 1. Outcome

Three independent parallel investigation lenses ran against this checkpoint's own charter — read in full from `docs/ai-agent-build-prompt-package/15-hardening/387_RELEASE_BLOCKER_TRIAGE_REMEDIATION_PROMPT.md`: rank every open finding in `BLOCKER_LEDGER.md`, select bounded critical/high repairs, implement each with a real regression test, and escalate anything genuinely unbounded with an exact no-go rationale rather than rushing it. This checkpoint does not attempt a wide-scope remediation sweep — it triages, fixes what is bounded, and hands off the rest with a concrete scope.

Triaged the 32-entry open blocker backlog at entry (1 Critical, 22 High, 8 Medium true count) into three buckets: bounded (mechanical, or mirrors an already-proven fix pattern elsewhere in this exact codebase), genuine design decision (out of scope for this checkpoint), and ambiguous (needs a fresh investigation before the bucket itself can be decided). Cross-referencing all 3 lenses' own independent findings converged on 7 bounded fixes plus 1 administrative-only re-ruling.

**`HDN-BLK-023` (Critical) `RESOLVED`.** The SSO integration-connection activation-gate bypass — `app.set_integration_connection_status` independently reachable by any `INTHUB:Configure` holder, bypassing `app.activate_enterprise_idp_connection`'s own IP-restriction/lockout-guard/step-up-MFA protections entirely. Two prior checkpoints (`HDN-386`'s own first round and Tier C review) had investigated this and declined to fix it, judging the required audit — "does any OTHER connection type have an equivalent specialized wrapper this same generic function also bypasses?" — open-ended, and therefore a genuine design decision rather than a bounded repair. This checkpoint re-ran that exact audit: grepped every integration migration for a call to `app.set_integration_connection_status`, and separately for the step-up-MFA/IP-restriction/lockout-guard pattern in every non-SSO integration file. Zero hits outside the SSO-specific files. `app.activate_enterprise_idp_connection` is the ONLY specialized activation wrapper that exists anywhere in this schema — the "audit every other type" scope that made this look unbounded two checkpoints in a row was never actually open-ended, only unverified. Fixed by mirroring an already-proven precedent in this exact codebase, `app.request_gps_device_status_transition` (`ATW-031`, `ISS-2026-028`): the shared core keeps its full status machine but loses its `authenticated`/`service_role` EXECUTE grants; a new narrow entry point, `app.request_integration_connection_status_change`, carries the grant instead and refuses `p_status='active'` for either SSO adapter code outright, delegating every other transition and adapter type unchanged. `app.activate_enterprise_idp_connection` (`SECURITY DEFINER`, same owner as the core) is completely unaffected by the grant revocation and continues calling the core directly. `server/mutations/integration-hub.ts`'s `setIntegrationConnectionStatus` repointed to the new entry point in the same checkpoint. **The open Critical count for Step 15 is now zero for the first time this session.**

**6 more findings fixed or partially fixed.** `HDN-BLK-013` (High) `RESOLVED` — app-layer tenant-ownership checks added to 7 Server Actions (`app/(tenant)/[tenantSlug]/admin/api-keys/actions.ts`) and 1 API route, mirroring the already-established `account.tenantId !== access.tenant.id` idiom, TS-only, no migration. `HDN-BLK-019` (High) `RESOLVED` — `app.file_access_logs` legal-hold cascade, mirroring `HDN-386`'s own just-proven `app.audit_logs` legal-hold-bridge-plus-trigger pattern exactly (`BEFORE UPDATE OR DELETE`, lazy `auth.uid()`). `HDN-BLK-022` (High) `PARTIALLY_RESOLVED` — the 2 tables `HDN-377`'s own Tier C live-forced as genuinely exploitable (`app.vendor_kpi_scorecards`, `app.position_grades`) closed as a first increment, mirroring the already-proven `app.check_procurement_authority`/`ISS-2026-220` fix shape; the remaining ~33-table sweep stays registered, unchanged owner `HDN-378`. `HDN-BLK-027` (High) `PARTIALLY_RESOLVED` — the 3 inbound webhook-ingestion functions' own `signature_verification_failed` branch now raises a real observability alert, mirroring `app.record_job_failure`'s own dead-letter alert pattern (`HDN-382`); the remaining named producers (webhook-delivery-replay divergence, `IAE-008` health-check auto-disable, AI-governance rejection, security-denial paths) stay registered under `ISS-2026-249`. `HDN-BLK-010` (Medium, narrowed) `RESOLVED` — the 3 remaining non-Finance boundary functions now carry the already-proven "design note 9(a)" nested-exception race guard, plus `ISS-2026-163`'s own distinct defective-handler defect in `app.prepare_job_order` fixed in the same part. `HDN-BLK-007` (High) `RESOLVED` (pending live CI confirmation, no CI access from this session) — the CI collision-check's own false-positive precondition on a `main` checkout narrowed to skip only when the current branch is not an `agent/*`/`claude/*` candidate branch, without weakening the `ISS-2026-002` control the test protects for the branches it actually guards.

**`HDN-BLK-006` (Medium) closed administratively, zero code.** Re-ruled `ACCEPTED_EXCEPTION` under `00_EXECUTION_INDEX.md` §8.2's full 5-condition test at the correct authority (`HDN-387`, not the finding lane, `HDN-379`) — correcting `HDN-386`'s own partial correction (which reclassified the entry `DEFERRED_TO_HDN-387` but did not itself re-rule it, since a Tier C ledger-consistency lens cannot originate a new acceptance ruling any more than the original finding lane could).

**3 real defects caught and fixed live before this round's own commit, none assumed correct.** (1) The webhook-alert-wiring fix's first draft based each of its 3 target functions' own "verbatim reproduction" on the function's ORIGINAL create-table migration, silently reverting every later hardening pass on all 3 (advisory-lock rate-limit scoping, ATW-226F's own canonical-telemetry-arbitration call, ATW-226I's auto-disable wiring, ATW-027's widened exception boundary) — caught by a live `db-tests` run (`advanced-tms-canonical-telemetry-arbitration.sql`'s own "switch_suppressed" case failed once the arbitration call silently vanished), not by inspection. Rewritten from each function's true LATEST body, grep-verified against every later `create or replace` before writing the fix. (2) The same fix's new `app.raise_observability_alert(...)` calls initially passed a `jsonb_build_object(...)` as the final argument, but that parameter (`p_detail`) is declared `text`, not `jsonb` — would have failed to resolve the function overload entirely. Caught before any db-tests run, fixed to pass a plain formatted text string, matching `app.record_job_failure`'s own established call shape. (3) The HRIS/Procurement RLS fix's first draft assumed the pre-existing weak policy on both tables was named `*_tenant_read` and added a SECOND, differently-named policy alongside it — since Postgres OR-combines multiple permissive policies for the same command/role, the real, pre-existing policy (correctly named `*_select_scoped`, matching `HDN-377`'s own precedent exactly) remained fully live, and a zero-HRS-role probe actor still read the fixture row after the "fix" landed (`count=1`, expected `0`). Corrected to drop-and-replace the SAME, correctly-named policy in place. All 3 corrected and re-verified live before this checkpoint's own first-round commit — see §11.

## 2. Baseline at entry

`HDN-386` (Full-System Hardening Integrated Verification) `VERIFIED` at commit `f39cac8`. Gates green: `typecheck` 0, `lint` 0/337 warnings, `pnpm run test` 5443/5443, `pnpm exec next build` clean, `pnpm run test:e2e` 34/34, `bash scripts/db-tests/run.sh` 229/229 files clean (332 migrations), `pnpm install --frozen-lockfile` verified passing.

## 3. Entry gate

`00_EXECUTION_INDEX.md` §8.1 (release-blocker definition — any unresolved Critical/High stops Step 16 until fixed or accepted), §8.2 (the 5-condition accepted-risk framework — severity High-or-below, explicit written ruling, named owner, registered in both ledgers, accepted only at `HDN-387`/`HDN-389`, never the lane that found it). Upstream: `HDN-370..386`, all `VERIFIED`.

## 4. Investigation (3 parallel lenses)

**Lens 1 — bounded-vs-design-decision triage.** Read every open `BLOCKER_LEDGER.md` entry in full and classified each by whether its own fix pattern already exists proven elsewhere in this codebase (bounded) or requires a genuine unbounded judgment call (design decision). Flagged `HDN-BLK-023` for re-investigation rather than accepting the prior "open-ended audit" characterization at face value, since the underlying claim ("audit every other connection type") is itself a concrete, checkable proposition, not an inherently open-ended one.

**Lens 2 — mechanical-fix candidate verification.** For each candidate bounded fix, verified the referenced "already-proven pattern" actually applies structurally to the target (same table shape, same RLS convention, same trigger discipline) before committing to the fix, rather than assuming a superficial resemblance is sufficient. This lens is what surfaced that `HDN-BLK-022`'s target tables' own pre-existing policy name did not match the pattern's own naming assumption — caught before the fix was finalized as correct, later re-caught live when the first draft shipped the wrong name anyway (§11).

**Lens 3 — administrative/ledger-only findings review.** Reviewed `HDN-BLK-006` against `00_EXECUTION_INDEX.md` §8.2's full 5-condition test explicitly, confirming the underlying categorization work from `HDN-379` remains sound (a design question, not a defect, with a real named owner and rationale) and only the "who rules" condition needed correcting — the re-ruling itself is administrative, not a re-investigation of the underlying finding.

## 5. Carry-forward scope addressed this checkpoint

`HDN-386`'s own explicit handoffs — `HDN-BLK-023`/`024` (design decisions per that checkpoint's own text) and `HDN-BLK-016`/`017`/`018`/`019` (disposition corrections with explicit `HDN-387` scope) — were the starting point for this checkpoint's own triage. `HDN-BLK-023` is resolved this checkpoint (§1); `HDN-BLK-024` (61-function step-up-MFA/IP-restriction wiring) remains a genuine design decision, not attempted; `HDN-BLK-019` is resolved this checkpoint; `HDN-BLK-016`/`017`/`018` remain open, unchanged owner (each is itself a genuine design decision or a much larger sweep than this checkpoint's own bounded-repair charter permits).

## 6. Findings

Resolved in `docs/runtime/KNOWN_ISSUES.md`:

- `ISS-2026-235` (was Critical, `HDN-BLK-023`) — `app.set_integration_connection_status` independent bypass, fixed.
- `ISS-2026-166` (was High, `HDN-BLK-013`) — app-layer single point of failure on several high-privilege actions, fixed.
- `ISS-2026-222` (was High, `HDN-BLK-019`) — `app.file_access_logs` legal-hold cascade gap, fixed.
- `ISS-2026-162` (was Medium, `HDN-BLK-010`) — the 3 remaining non-Finance boundary functions, fixed.
- `ISS-2026-163` (was Low) — `app.prepare_job_order`'s defective exception handler, fixed.
- `ISS-2026-158` (was High, `HDN-BLK-007`) — CI collision-check false-positive on `main`, fixed (live CI confirmation pending).

Partially resolved in `docs/runtime/KNOWN_ISSUES.md`:

- `ISS-2026-225` (High, `HDN-BLK-022`) — 2 of ~35 tables closed; ~33-table remainder stays `OPEN`, owner `HDN-378`.
- `ISS-2026-249` (High, `HDN-BLK-027`) — 3 webhook-ingestion alert producers closed; remaining named producers stay `OPEN`.

Re-ruled in `docs/build-log/full-system-hardening/BLOCKER_LEDGER.md` / `docs/runtime/KNOWN_ISSUES.md`:

- `HDN-BLK-006`/`ISS-2026-239` — re-ruled `ACCEPTED_EXCEPTION` under §8.2's full 5-condition test at the correct authority.

Not attempted, unchanged owner:

- `HDN-BLK-016`/`017`/`018` — genuine design decisions or much larger sweeps, out of this checkpoint's own bounded-repair charter.
- `HDN-BLK-024` — the 61-function step-up-MFA/IP-restriction wiring gap.
- `HDN-BLK-022`'s own ~33-table remainder, `HDN-BLK-027`'s own remaining producers.

## 7. Migrations

**One additive migration**, `supabase/migrations/20260819000000_harden_release_blocker_triage_remediation.sql`, 6 parts:

- **Part 1** (`HDN-BLK-023`) — `app.request_integration_connection_status_change` (new entry point, refuses SSO reactivation) plus `revoke execute` on `app.set_integration_connection_status` from `authenticated`/`service_role`.
- **Part 2** (`HDN-BLK-013`) — noted, TS-only, no migration content.
- **Part 3** (`HDN-BLK-019`) — `app.protect_file_access_logs_legal_hold_from_mutation()` plus a `BEFORE UPDATE OR DELETE` trigger on `app.file_access_logs`.
- **Part 4** (`HDN-BLK-022`) — `app.check_hris_authority` (direct structural copy of `app.check_procurement_authority`) plus `position_grades_select_scoped`/`vendor_kpi_scorecards_select_scoped` dropped and replaced in place with the added authority predicate.
- **Part 5** (`HDN-BLK-027`) — `app.ingest_finance_payment_gateway_webhook_event`/`app.ingest_logistics_partner_webhook_event`/`app.ingest_third_party_provider_webhook_event` widened (from each function's true latest body) with one added `app.raise_observability_alert(...)` call in the signature-failure branch.
- **Part 6** (`HDN-BLK-010`, `ISS-2026-163`) — `app.prepare_job_order_handoff`/`app.prepare_wms_inbound_from_shipment`/`app.link_auth_identity` widened with the "design note 9(a)" nested-exception race guard; `app.prepare_job_order`'s own exception handler gains the missing `if found`/`raise;` guard.

No table drop, no destructive change. `git revert`-reversible.

## 8. TypeScript / application changes

- `server/mutations/integration-hub.ts` (`setIntegrationConnectionStatus` repointed to the new entry point) + `server/mutations/integration-hub.test.ts` (updated to match).
- `app/(tenant)/[tenantSlug]/admin/api-keys/actions.ts` — 7 Server Actions gained a tenant-ownership pre-check.
- `app/api/v1/customer/bookings/[bookingRequestId]/submit/route.ts` — tenant-ownership pre-check via `app.get_customer_booking_request`.
- `tests/api/v1/customer-bookings-submit.test.ts` — new cross-tenant-rejection test added.
- `scripts/git/check-worktree-collision.test.ts` — the "diverged from origin/main" assertion narrowed to skip when the current branch is not an `agent/*`/`claude/*` candidate.

## 9. db-test changes

One new committed regression file, `scripts/db-tests/release-blocker-triage-remediation.sql`, covering Parts 1, 3, 4, 5 functionally (4 fixture tenants, 4 targeted assertion blocks). Part 6 (the race-guard mechanism) is proven via live transcript, not a committed test file — see §11 and its own note on why.

## 10. Documentation

- `docs/build-log/full-system-hardening/BLOCKER_LEDGER.md` — `HDN-BLK-023`/`013`/`019`/`022`/`027`/`010` dispositions closed/partially closed with regression-test cross-references; `HDN-BLK-006` re-ruled; `HDN-BLK-007` disposition corrected; "Status as of `HDN-387`" section.
- `docs/build-log/full-system-hardening/HARDENING_MATRIX.md` — `HDN-BLK-007`/`ISS-2026-158` row (§ item 7, dependency scan) noted the root-cause fix, live CI confirmation still pending.
- `docs/runtime/KNOWN_ISSUES.md` — `ISS-2026-158`, `162`, `163`, `166`, `222`, `225`, `235`, `239`, `249` updated to match.
- `docs/build-log/full-system-hardening/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/HANDOFF.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md`, `docs/runtime/CHANGE_MANIFEST.md` — propagated for the first round.

## 11. Live verification evidence

Per this session's own established "live-execute, don't just describe" discipline, no fix in this checkpoint is trusted until it has been run against a real disposable Postgres database. The full 230-file `bash scripts/db-tests/run.sh` suite (§13) covers Parts 1/3/4/5 functionally via the new committed regression file. This section covers the additional live-forced evidence beyond what a committed test file captures, plus the two defects caught only by live re-runs (§1).

**SSO activation gate (`HDN-BLK-023`, Part 1).** Live-forced on a fresh disposable probe database: an `INTHUB:Configure`-only actor's call to `app.request_integration_connection_status_change(sso_conn, 'active', ...)` correctly raised `enterprise_sso_activation_requires_specialized_wrapper`; the same actor's call to disable the same SSO connection (a non-`active` transition) succeeded through the new entry point unchanged; a non-SSO (`payment_gateway`) connection's own disable-then-reactivate cycle succeeded unchanged through the new entry point; `information_schema.role_routine_grants` confirmed `app.set_integration_connection_status` no longer grants `authenticated`/`service_role`, while `app.request_integration_connection_status_change` does.

**Webhook signature-failure alerting (`HDN-BLK-027`, Part 5).** Live-forced: a deliberately bad signature against a real `payment_gateway` connection produced `ingest_status='invalid'` exactly as before, AND a real `app.incidents` row (`source_type='webhook'`, `signal_type='error'`, `severity='high'`, `title` naming the connection and adapter) — confirming the alert-wiring fix has no effect on the pre-existing caller-facing contract, only adds the previously-missing alert.

**RLS gate (`HDN-BLK-022`, Part 4) — the wrong-policy-name defect, caught and re-verified.** First live probe (against the uncorrected first draft): a fixture actor holding only a bare `org_user` role (zero HRS grant) still read `app.position_grades`' own fixture row (`count=1`) after the "fix." Root cause: `\d+ app.position_grades` showed TWO permissive `SELECT` policies for `authenticated` — the real pre-existing one (`position_grades_select_scoped`, unrestricted) and the newly-added, differently-named one (`position_grades_tenant_read`) — Postgres OR-combines multiple permissive policies for the same command/role, so the old, unrestricted policy alone was sufficient to admit the actor regardless of the new one. Corrected to drop-and-replace `position_grades_select_scoped`/`vendor_kpi_scorecards_select_scoped` in place. Re-verified live after the correction, role-switched to `authenticated` (not merely `request.jwt.claims` set — RLS is bypassed for the superuser/table-owner role this whole file otherwise runs as, a second live-caught bug in the regression test's own first draft): the zero-HRS-role actor now sees 0 rows; the HRS:View holder sees 1. Re-ran the full 230-file db-tests suite 3 times consecutively in isolation to rule out flakiness in this specific regression — deterministic, 3/3 clean.

### 11.1 `app.link_auth_identity` — genuine two-process race (`HDN-BLK-010`, Part 6)

Mirrors `HDN-371.md` §6.2's own already-proven "uncommitted-insert-blocking" technique exactly. On a fresh disposable probe database: Session A opened `begin; insert into app.tenant_user_identities (...) values (..., 'racer-A'); \! touch <marker>; select pg_sleep(3); commit;`. Session B waited for the marker file, then called `app.link_auth_identity(<same auth_user_id>, <same tenant_id>, 'racer-B')` while A's insert was still uncommitted. Observed: Session B's call blocked on A's uncommitted unique-index entry, then on A's commit correctly re-selected and returned A's own already-committed row (`invited_by='racer-A'`) — not a raised `unique_violation`, not a duplicate. Final row count for the pair: 1. Per `HDN-371.md`'s own "scope of the claim" precedent, this one forced proof is treated as sufficient for the mechanism; the other 2 siblings sharing the identical nested-exception shape (`app.prepare_job_order_handoff`, `app.prepare_wms_inbound_from_shipment`) were code-verified against their own true effective definitions, not independently re-raced.

### 11.2 `app.prepare_job_order` — the `ISS-2026-163` unrelated-constraint collision

`app.next_job_order_number` is a real, non-recycling monotonic counter, so a natural `job_orders_tenant_number_unique` collision is not plausible under normal operation — this defect's own severity is Low precisely because forcing it requires manual intervention, which is exactly what this proof did. Built a full lead→prospect→opportunity→quotation→acceptance→account→handoff pipeline twice (two independent handoffs, one tenant), converted the first handoff via `app.prepare_job_order` (allocating `job_number='JOB-2026-000001'`), then manually reset `app.job_order_number_counters.last_seq` to 0 for the tenant so the counter would regenerate the identical `job_number` on the next call. Called `app.prepare_job_order` on the SECOND, genuinely distinct handoff: with the pre-fix handler, this reproduced the exact bug (silently returning an all-NULL `app.job_orders` composite instead of raising); with the fixed handler, it correctly re-raised the real `unique_violation` (`duplicate key value violates unique constraint "job_orders_tenant_number_unique"`).

### 11.3 `app.file_access_logs` — legal-hold trigger (`HDN-BLK-019`, Part 3)

Built a real document-type registration, a real file upload under it, a real `app.file_access_logs` row via `app.authorize_file_access`, then placed the file under legal hold via `app.set_file_legal_hold`. A raw `DELETE` and a raw `UPDATE` against the access-log row both correctly raised `file_access_log_legal_hold_blocks_deletion`, and the row survived both attempts unchanged. A Supreme Admin's own RPD-022 absolute-CRUD override (role-switched to `service_role` with `request.jwt.claims` naming the Supreme Admin's `auth_user_id` — NOT `authenticated`, since `app.file_access_logs` grants `authenticated` zero direct table access at all, matching this table's own established "the raw table itself refuses every authenticated session" posture) then successfully applied a real `UPDATE`, and produced a real, correctly-attributed `app.audit_logs` row (`action='update_legally_held_file_access_log'`) — confirmed via a clean value change (`result: 'granted' -> 'denied'`) and a fresh `select` after `commit`, after an earlier attempt in the same probe session was invalidated by an unrelated query error aborting the transaction before its own `commit` could take effect (caught and corrected before treating that result as evidence).

## 12. Taxonomy

`docs/standards/RECURRING_DEFECT_TAXONOMY.md` walked. No new class needed — every fix in this checkpoint reuses an already-registered pattern (the ATW-031 grant-revocation-plus-narrow-entry-point shape for `HDN-BLK-023`; the C-25 dual-mechanism-drift/legal-hold-bridge shape for `HDN-BLK-019`; the already-proven `app.check_procurement_authority` module-authority-wrapper shape for `HDN-BLK-022`; the `app.record_job_failure` dead-letter-alert shape for `HDN-BLK-027`; the "design note 9(a)" nested-exception race-guard shape for `HDN-BLK-010`). The 2 live-caught defects in this checkpoint's own first draft (stale-source verbatim reproduction; wrong-named policy replacement) both instantiate an existing lesson already stated explicitly in this ledger's own text for `HDN-BLK-010`'s "Required of `HDN-374`" field ("working from the original body would silently revert prior hardening migrations") — not a new class, a recurrence of a documented one that this checkpoint itself failed to apply on the first attempt before self-correcting.

## 13. Tier A gate (first round, before Tier C)

`typecheck` 0 errors; `lint` 0 errors/337 warnings (unchanged baseline); `pnpm run test` **5444/5444** (1 new test, `tests/api/v1/customer-bookings-submit.test.ts`'s own cross-tenant-rejection case); `pnpm exec next build` clean; `bash scripts/db-tests/run.sh` **230/230 files clean** (333 migrations, one new committed regression file). All gates green before the first-round commit.

## 14. Tier C review

Four independent adversarial lenses ran against the pushed first-round state (commit `e152f4f`):

**Correctness re-derivation — PASS, no defect.** Independently re-derived all 6 migration parts by reading the actual current schema/function definitions (not the build log's own narrative), grepping full migration history for every touched function/policy to confirm no later redefinition was missed, and diffing each function's true prior effective body against its new one. Part 1: confirmed `app.activate_enterprise_idp_connection` still calls the core directly and is unaffected by the grant revocation; no other caller of `app.set_integration_connection_status` exists anywhere. Part 3: confirmed the trigger's join and hold-state check match `app.protect_audit_logs_legal_hold_from_deletion`'s own established discipline exactly. Part 4 (the highest-risk part): confirmed only one prior SELECT policy ever existed on either table, correctly named, and the new migration drops/recreates that same name — no stray second policy anywhere in migration history. Part 5: confirmed each of the 3 ingest functions' diff against its true latest prior body is purely additive (only the new alert call), and `app.raise_observability_alert`'s real signature (`text` for `p_detail`) matches what the committed calls pass. Part 6: confirmed each of the 4 widened functions' diff against its true prior body is a clean, minimal addition of the race-guard/`if found` pattern, matching `app.prepare_wms_outbound_from_shipment`'s own reference shape. TS changes and the CI test fix independently re-derived correct. All four gates independently re-run and matched exactly.

**Attack-surface adversarial testing — mostly HELD, one live-demonstrated addendum.** Built a fresh disposable database and live-attacked all 6 fixed surfaces plus the CI test fix. `HDN-BLK-023`: no other caller of the core exists, no non-literal bypass of the `'active'` check is possible (the core's own status CHECK constraint only accepts 3 literal values), and the legitimate end-to-end SSO activation path (authority → lockout guard → domain claim → step-up MFA → activation) still works. `HDN-BLK-019`: a mixed-batch UPDATE spanning held and unheld rows correctly aborts atomically; independently discovered that `app.files.id` is `RESTRICT`-referenced by `file_access_logs.file_id`, so a file with any access-log row can never be hard-deleted at all, held or not — a stronger guarantee than the trigger alone provides. `HDN-BLK-027`: the alert call sits outside any exception-handling block (a real raise would propagate loudly, not be swallowed); 3 rapid-fire signature failures against the same connection correctly deduped to exactly 1 incident. `HDN-BLK-010`: two NEW independent two-process races (against `app.prepare_wms_inbound_from_shipment`, and a same-`source_handoff_id` legitimate concurrent retry against `app.prepare_job_order`, both different from this build log's own §11 proofs) both HELD, returning the winner's row correctly. The CI test fix's skip condition was confirmed structurally incapable of masking a real fork (the untouched "no genuine collision" assertion runs unconditionally on every branch). **`HDN-BLK-022`'s RLS fix itself HELD under every direct attack** (raw SELECT/INSERT/UPDATE/DELETE, a revoked role, a customer_user-layer-only actor all correctly denied) — **but a real, live-demonstrated gap adjacent to the fix was found**: an actor who simultaneously holds a `customer_user`-layer membership AND a legitimately assigned staff role (nothing prevents that dual state at grant time) can still read the identical row through the pre-existing, unguarded `app.list_position_grades`/`app.list_vendor_kpi_scorecards` RPCs, and the same shape was found by code review in 2 procurement-dashboard RPCs. Not a regression this checkpoint introduced — folded into `ISS-2026-225`'s own still-open ~33-table remainder as a disclosed addendum (see `BLOCKER_LEDGER.md`'s `HDN-BLK-022` Tier C addendum), owner unchanged `HDN-378`.

**Schema-wide completeness sweep — 2 real gaps found and fixed.** Confirmed `HDN-BLK-022`'s 2-table scope, `HDN-BLK-027`'s 3-function scope, and `HDN-BLK-010`'s 4-function scope are all exactly what was ever promised as fixed (no third table/function silently missed), and independently recomputed the "Status as of `HDN-387`" tally by hand-walking all 39 `HDN-BLK-nnn` entries — matched exactly. **Found two real gaps**: (a) three ledger entries explicitly naming `HDN-387` as owner (`HDN-BLK-008`, `HDN-BLK-014`'s own residual scope `ISS-2026-186`, `HDN-BLK-039`) received no disposition update anywhere in the first round — the identical procedural-gap shape `HDN-386` was itself caught committing one checkpoint earlier for `HDN-BLK-016..019`. Fixed with explicit disposition notes for `HDN-BLK-008` (still a genuine CI-infrastructure design decision, not rushed) and `HDN-BLK-014`'s residual (still needs its own dedicated 14-function per-call-site audit); `HDN-BLK-039` (High, 14 unowned blockers) formally accepted under `00_EXECUTION_INDEX.md` §8.2's full 5-condition test, real owner assigned (`Step 16`, the post-hardening phase Step 15 hands off to — no further numbered checkpoint remains in this session's own range), closing `RESOLVED`. (b) A newly-surfaced sibling gap to `HDN-BLK-013` — `sendTestWebhookDeliveryAction` sharing the exact same unchecked tenant-ownership shape as the 7 just-fixed sibling actions — was disclosed in `ISS-2026-166`'s own resolution prose at the first round but never given a trackable ID. Since it is genuinely bounded (identical mechanical pattern, same file, same already-proven idiom), closed directly in this fix pass rather than minted as a wholly separate finding.

**Ledger/documentation consistency — 1 minor gap found and fixed.** Confirmed cross-document consistency across all 5 top-level ledgers (`00_EXECUTION_INDEX.md`, `TASK_LEDGER.md`, `HANDOFF.md`, `CARGOGRID_BUILD_STATUS.md`, `HDN-387.md` itself), the 333-migration count against the filesystem, every `ISS-2026-nnn` cross-reference against `KNOWN_ISSUES.md` (no duplicate heading, dispositions matching), `CHANGE_MANIFEST.md`'s sequential numbering, `HDN-387.md`'s own §11/§11.1-11.3 cross-references, and the ledger's own High(19)/Medium(6) open-blocker tallies (recounted by hand, both correct at the time of the first-round check). **Found one real gap**: `HDN-387.md` §10 (Documentation) omitted `docs/build-log/full-system-hardening/HARDENING_MATRIX.md` from its own file-accounting, even though that file was genuinely edited by the first-round commit (1 accurate line added to the `HDN-BLK-007` row) — a disclosure-completeness gap, not a factual error. Fixed by adding it to §10.

**Fix pass.** `app/(tenant)/[tenantSlug]/admin/api-keys/actions.ts`'s `sendTestWebhookDeliveryAction` gained the same tenant-ownership check as its 7 siblings. `docs/build-log/full-system-hardening/BLOCKER_LEDGER.md`: `HDN-BLK-008`/`HDN-BLK-014`'s residual gained explicit disposition notes; `HDN-BLK-039` formally accepted and closed `RESOLVED` (owner `Step 16`); `HDN-BLK-022` gained a Tier C addendum disclosing the RPC-layer gap; `HDN-BLK-013` amendment widened to note the sibling-action fix; "Status as of `HDN-387`" section updated (High-open 19 → 18, `HDN-BLK-039` moved to resolved). `docs/runtime/KNOWN_ISSUES.md`: `ISS-2026-282` (`HDN-BLK-039`) marked `RESOLVED`; `ISS-2026-225` gained the matching RPC-layer addendum; `ISS-2026-166` updated to note the sibling-action closure. `HDN-387.md` §10 corrected. No Critical or unowned High finding survives Tier C.

**Independent full gate re-run after the fix pass**: `typecheck` 0; `lint` 0 errors/337 warnings; `pnpm run test` **5444/5444** (unchanged — the `sendTestWebhookDeliveryAction` fix has no dedicated test harness, matching this file's own established precedent); `pnpm exec next build` clean; `bash scripts/db-tests/run.sh` **230/230 files clean** (333 migrations, unchanged — no schema change at Tier C). **`HDN-387` closes `VERIFIED`.**

## 15. Task status

**`VERIFIED`**. `docs/runtime/TASK_LEDGER.md` row `CG-S15-HDN-019`,
`docs/build-log/full-system-hardening/00_EXECUTION_INDEX.md`'s own WBS
row/tally/§16 updated accordingly. Next eligible prompt after Tier C
close and `VERIFIED`: `HDN-388` (Documentation Handoff, `CG-S15-HDN-020`).
