# HDN-386 — Full-System Hardening Integrated Verification

**Prompt:** `CG-S15-HDN-018` (Prompt 386, `CG-AABPP-HDN-386`)
**Status:** **`COMPLETED`** — first round complete, Tier C review pending.
**Branch:** `claude/step-15-hdn-369-kickoff-w6qren`

## 1. Outcome

Three independent parallel investigation lenses ran against this checkpoint's own charter — read in full from `docs/ai-agent-build-prompt-package/15-hardening/386_FULL_SYSTEM_HARDENING_INTEGRATED_VERIFICATION_PROMPT.md`: independently verify that all Step 15 hardening gates have current evidence from ONE compatible checkpoint (not mixed-checkpoint), with no unowned Critical/High blocker, before `HDN-387` (Release Blocker Triage and Remediation) begins actual fix work. This checkpoint verifies, ranks, and hands off — it does not do wide-scope remediation.

Frozen checkpoint: commit `d57ad0b` (`HDN-385`, `VERIFIED`). **The full Tier A gate suite re-run fresh on this exact commit is entirely green**: `typecheck` 0, `lint` 0 errors/337 warnings, `pnpm run test` 5443/5443, `bash scripts/db-tests/run.sh` ALL PASSED (229/229 files, 330 migrations), `pnpm run docs:check`/`security:check`/`security:audit`/`git:check-paths` all passed locally, `next build` clean, `pnpm run test:e2e` 34/34 across all 5 Playwright projects. No mixed-checkpoint evidence found anywhere across the 16 domain-audit lanes — every `VERIFIED` commit is a direct ancestor of `d57ad0b` on one linear history.

**The CI-blindness gap this checkpoint's own charter explicitly forbids waving through was confirmed by exact mechanism, not assumed.** `HDN-BLK-007` (already tracked since `HDN-370`) is the root cause: `scripts/git/check-worktree-collision.test.ts` fails on every CI push (a CI checkout of `main` cannot be "ahead of `origin/main`"), cascading to skip 6 downstream governance steps including `security:audit` and the secret scan. This checkpoint confirmed `security:audit` — added to `ci.yml` 2026-08-05, well before Step 15 began — has **never actually executed in CI on any run**. The same disposition (real, green, `Executed` locally; `Tracked gap` for independent CI confirmation) applies to `HDN-380`'s axe-core coverage and `HDN-381`'s mobile/tablet/iPhone Playwright coverage. `HDN-BLK-007`'s own text widened to record this precisely.

**Two Critical/High findings explicitly named "Required of `HDN-386`" in `BLOCKER_LEDGER.md` were fixed at the root, as a bounded repair**: `HDN-BLK-020` (Critical — `app.audit_logs.legal_hold` enforced nowhere) and `HDN-BLK-021` (High — `app.tenants.legal_hold` invisible to the generic hold mechanism in the direction that mattered). Both mirror `HDN-377`'s own already-proven `app.files` legal-hold bridge pattern exactly, scoped deliberately narrow (NOT `HDN-BLK-018`'s own much larger append-only-guard rollout the two findings were originally bundled with, which remains open, unchanged, owner `HDN-387`).

**Two other Critical/High findings also named "Required of `HDN-386`" were investigated and formally handed to `HDN-387`, not attempted**: `HDN-BLK-023` (`app.set_integration_connection_status`'s own independent bypass) and `HDN-BLK-024` (61-function step-up-MFA/IP-restriction wiring gap) are both genuine, self-disclosed design decisions exceeding a bounded-repair pass — the identical mechanical-fix-vs-genuine-design-decision judgment applied throughout Step 15.

**The blocker ledger reconciliation lens found and this checkpoint fixed 3 pre-existing ledger inconsistencies**, none of them this checkpoint's own regression: `HDN-BLK-001`'s stale disposition text (never updated after `HDN-378`'s own Tier C correction of `ISS-2026-150` from `RESOLVED` to `PARTIALLY RESOLVED`); `HDN-BLK-006`'s procedurally-invalid self-acceptance (ruled on by `HDN-379`, the same lane that found it, violating `00_EXECUTION_INDEX.md` §8.2 condition 5); and a stale Medium-open tally (`HDN-BLK-005` closed at `HDN-379` but never dropped from 6 subsequent checkpoints' own running counts).

**The cross-lane consistency lens found and this checkpoint fixed a real evidence-propagation gap**: `disaster-recovery.md` §7 claimed to be the first rehearsal-history entry for `docs/runbooks/incident-response.md` itself, but that entry was never actually written there — backfilled, and `incident-response.md`'s own stale §4 resolution-order claim (contradicted by `HDN-384`'s own already-established `ISS-2026-264`) corrected in the same pass.

**4 new findings registered, not fixed**: a missing performance/capacity runbook (`HDN-379` was named owner, never created one); the CI-mirrors-hosted Tier B cross-cutting check documented in only 3 of 16 build logs; 14 open blockers carrying no real owning lane, only "a dedicated future task" (paired `HDN-BLK-039`, High); and the stale-tally bookkeeping finding itself.

## 2. Baseline at entry

`HDN-385` (Data Migration Rehearsal) `VERIFIED` at commit `d57ad0b`. Gates green: `typecheck` 0, `lint` 0/337 warnings, `pnpm run test` 5443/5443, `bash scripts/db-tests/run.sh` 229/229 files clean (330 migrations), `next build` clean.

## 3. Entry gate

`00_EXECUTION_INDEX.md` §8 (release-blocker definition, 10 non-negotiable gates, and the 5-condition accepted-risk framework), §9 (baseline gate status format), §10 (Executed/Executed-against-a-substitute/Tracked-gap framework), §11.1 ("mixed-checkpoint evidence is invalid... every result cited at `HDN-386` and `HDN-389` must come from one compatible checkpoint"), and §16's own Tier-C amendment (naming `HDN-378`, `HDN-380`/`381`, `HDN-386` as the three lanes that actually touch the CI-blindness gap, and barring `HDN-386` specifically from waving it through). Upstream: `HDN-370..385`, all `VERIFIED`.

## 4. Investigation (3 parallel lenses)

**Lens 1 — full compatible-checkpoint gate matrix + CI-blindness disposition.** Re-ran the entire canonical Tier A gate suite fresh on the frozen commit, all green (see §1). Built a per-lane pass/fail matrix confirming no mixed-checkpoint citation across all 16 domain-audit lanes. Read `.github/workflows/ci.yml` directly and confirmed the exact CI-blindness mechanism: the `quality` job's step order (Typecheck → Lint → Test → Suppression → docs:check → security:check → security:audit → data-classification → threat-model) has no `if: always()` after `Test`, so `check-worktree-collision.test.ts`'s structural failure on every CI checkout of `main` cascades to skip `security:audit` and 5 other governance steps every single run — verified against real CI run history (#105-109, all `failure`). Gave explicit Executed/Substitute/Tracked-gap dispositions for all three flagged lanes (security, accessibility, browser/device) rather than waving any through.

**Lens 2 — blocker ledger reconciliation.** Read `BLOCKER_LEDGER.md` in full (1,139+ lines) and cross-referenced every cited `ISS-2026-nnn` against `KNOWN_ISSUES.md` — no dangling cross-reference or duplicate heading found anywhere (confirming the `ISS-2026-235`/`236` collision `HDN-385`'s own Tier C fixed holds, no sibling collision introduced since). Found 3 real reconciliation defects (§1, above) predating this checkpoint. Characterized both open Criticals precisely, confirming both are self-named "Required of `HDN-386`" in their own ledger text — not silently routable to `HDN-387` without a documented reason. Built the complete Step 16 impact table: 32 open blockers at entry (2 Critical, 22 High, 8 true Medium — the ledger's own count read 9, stale), 14 of them carrying no real owning lane.

**Lens 3 — cross-lane consistency and completeness sweep.** Verified all 5 of `00_EXECUTION_INDEX.md` §13's carry-forward items were genuinely addressed by their named lane (day-of-week flake class, postgis/pg_trgm/btree_gist, `rbac-enforcement.sql`/unindexed-FK triage, teardown-batching/non-idempotency/`auth.users` survival) — all 4 with a named single lane passed; the fifth ("all lanes" preserve CI-mirrors-hosted) found genuinely partial (documented by name in only 3 of 16 lanes). Checked §11.4's runbook checklist against `docs/runbooks/` — found the missing performance/capacity runbook and the `incident-response.md` evidence-propagation gap (§1). Confirmed all 24 Step 15 migrations are genuinely additive (no `DROP TABLE/COLUMN/SCHEMA`, `TRUNCATE`, or `DELETE FROM` in a 4-file spot-check; `git diff --stat` shows 0 deletions across all 24). Spot-checked 8 build logs for unsubstantiated "fixed"/"verified" claims without a paired observed artifact — found none; the discipline held, and `HDN-378`'s own headline claim being caught and downgraded by its own Tier C is itself evidence the discipline is real. Spot-checked 5 regression tests across different lanes — all real, all exist. Confirmed `HANDOFF.md`/`CARGOGRID_BUILD_STATUS.md`/`TASK_LEDGER.md` all consistently named `HDN-385` `VERIFIED` as latest before this checkpoint began.

## 5. Carry-forward scope addressed this checkpoint

`00_EXECUTION_INDEX.md` §16's own Tier-C amendment named `HDN-386` as one of the three lanes that must produce a real Executed/Substitute/Tracked-gap disposition for the CI-blindness gap rather than waving it through — done in lens 1 (§4), with `HDN-BLK-007`'s own text widened to record the exact confirmed mechanism (§6).

## 6. Findings

Registered in `docs/runtime/KNOWN_ISSUES.md`:

- `ISS-2026-280` (Low, owner `HDN-388`) — no performance/capacity runbook exists; `HDN-379` was its named owner and never created one.
- `ISS-2026-281` (Medium, owner `HDN-387`) — the mandatory CI-mirrors-hosted cross-cutting Tier B check documented by name in only 3 of 16 build logs.
- `ISS-2026-282` (High, `HDN-BLK-039`, owner `HDN-387`) — 14 open blockers (12 High, 2 Medium) carry no real owning lane, only "a dedicated future task."
- `ISS-2026-283` (Low, owner `HDN-387`) — `BLOCKER_LEDGER.md`'s own running Medium-open tally overcounted by 1 since `HDN-379` (a closed blocker never dropped from the count).

Resolved in `docs/runtime/KNOWN_ISSUES.md`:

- `ISS-2026-229` (was Critical, `HDN-BLK-020`) — `app.audit_logs.legal_hold` enforcement gap, fixed.
- `ISS-2026-230` (was High) — `app.tenants.legal_hold`/generic-hold bridge gap, fixed.

Handed to `HDN-387`, not attempted (already registered, disposition text updated with an explicit handoff scope):

- `HDN-BLK-023`/`ISS-2026-235` (Critical) — `app.set_integration_connection_status` independent bypass.
- `HDN-BLK-024`/`ISS-2026-236` (High) — `is_high_risk_action` 61-function wiring gap.

Corrected, pre-existing, not this checkpoint's own regression:

- `HDN-BLK-001` — stale disposition text, now matches `ISS-2026-150`'s own `PARTIALLY RESOLVED` correction.
- `HDN-BLK-006` — procedurally-invalid self-acceptance (violated §8.2 condition 5), reclassified `DEFERRED_TO_HDN-387` pending genuine re-ruling; severity corrected Medium → Low to match `ISS-2026-239`.
- `docs/runbooks/incident-response.md` §4/§7 — backfilled missing rehearsal entry, corrected stale resolution-order claim to match `ISS-2026-264`.

## 7. Migrations

**One additive migration**: `supabase/migrations/20260818000000_harden_integrated_verification_legal_hold_bridge.sql` — bridges `app.audit_logs` and `app.tenants` into the generic (IAE-031) `app._is_under_legal_hold()` primitive (mirroring the existing `app.files` bridge branch exactly); adds a new `app.protect_audit_logs_legal_hold_from_deletion` `BEFORE DELETE` trigger on `app.audit_logs` (mirroring `app.protect_files_legal_hold_from_deletion`'s own already-reviewed pattern); widens `app.enforce_tenant_status_transition` to check both the native and generic hold before blocking tenant termination. Deliberately does NOT touch `HDN-BLK-018`'s own separate, much larger append-only-guard rollout.

## 8. TypeScript / application changes

**None.**

## 9. db-test changes

Two regression blocks added: `scripts/db-tests/audit-trail.sql` (3 directions — native-hold raw-DELETE block with no session-bound actor, generic-hold raw-DELETE block, Supreme Admin override honestly and distinctly audited) and `scripts/db-tests/tenant-lifecycle.sql` (a purely generic, no-native-flag hold on scope `app.tenants` now correctly blocks termination, and succeeds once released). Both live-verified via a full `bash scripts/db-tests/run.sh` run, `ALL PASSED`, 331 migrations.

## 10. Documentation

- `docs/runbooks/incident-response.md` — §4 items 1-2 corrected, §7 backfilled, template version `0.1.0` → `0.2.0`.
- `docs/runtime/KNOWN_ISSUES.md` — `ISS-2026-280..283` registered; `ISS-2026-229`/`230` resolved.
- `docs/build-log/full-system-hardening/BLOCKER_LEDGER.md` — `HDN-BLK-020`/`021` fixed; `HDN-BLK-039` registered; `HDN-BLK-001`/`006`/`007`/`023`/`024` text corrected/widened; new "Status as of `HDN-386` first round" section.
- `docs/build-log/full-system-hardening/00_EXECUTION_INDEX.md`, `docs/runtime/TASK_LEDGER.md`, `docs/runtime/HANDOFF.md`, `docs/runtime/CARGOGRID_BUILD_STATUS.md`, `docs/runtime/CHANGE_MANIFEST.md` — propagated for the first-round close.

## 11. Taxonomy

`docs/standards/RECURRING_DEFECT_TAXONOMY.md` walked. The legal-hold bridge fix is not a new class — it is a third instance of the already-registered C-25 dual-mechanism-drift class (`app.files` at `HDN-377`, now `app.audit_logs`/`app.tenants` at `HDN-386`), reusing the exact already-proven fix pattern rather than inventing a new one. The 3 ledger-reconciliation defects found this checkpoint (stale disposition text, procedurally-invalid self-acceptance, stale running tally) are a new observed pattern — "governance-ledger bookkeeping drifts silently once a checkpoint that created an entry closes and no later checkpoint's own Tier C happens to touch that specific entry" — worth a taxonomy note for a future checkpoint, judged premature to formalize as a full new class from 3 instances found in one sweep.

## 12. Tier A gate (first round, before Tier C)

Run before the first-round commit: `typecheck` 0 errors, `lint` 0 errors/337 warnings (unchanged baseline), `pnpm run test` 5443/5443, `bash scripts/db-tests/run.sh` ALL PASSED (229/229 files, 331 migrations, including the new regression blocks), `next build` clean. All gates green.

## 13. Tier C review

Pending — to be run against this committed first-round state: correctness re-derivation, schema-wide completeness sweep, ledger/documentation consistency, attack-surface adversarial testing.

## 14. Task status

**`COMPLETED`** — first round only; Tier C review has not yet run.
`docs/runtime/TASK_LEDGER.md` row `CG-S15-HDN-018` and
`docs/build-log/full-system-hardening/00_EXECUTION_INDEX.md`'s own WBS
row/tally/§16 updated accordingly. Next eligible prompt after Tier C close
and `VERIFIED`: `HDN-387` (Release Blocker Triage and Remediation, `CG-S15-HDN-019`).
