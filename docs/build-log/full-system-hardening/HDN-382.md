# HDN-382 — Observability Audit

**Task ID:** `CG-S15-HDN-014`
**Prompt:** 382 (`docs/ai-agent-build-prompt-package/15-hardening/382_OBSERVABILITY_AUDIT_PROMPT.md`)
**Date:** 2026-08-24 (Monday)
**Branch:** `claude/step-15-hdn-369-kickoff-w6qren`
**Status:** **`VERIFIED`** — first round complete, Tier C review complete, fix pass applied.
**HEAD at this write-up:** migration `20260816000000_harden_observability_audit_findings.sql`
(329 migrations total)

---

## 1. Outcome

Three independent parallel investigation lenses covered the charter named by
Prompt 382 §20/§24 and `HARDENING_MATRIX.md` §13's own seeded punch list:
(Lens 1) a source-level static evidence sweep mapping observability coverage
across app/DB/queue/jobs/integrations/API/AI/storage/security events; (Lens 2)
live/simulated failure testing — actually driving real failures through a
disposable Postgres and observing whether they produce evidence; (Lens 3) a
runbook/dashboard/alert-ownership/severity-workflow review.

**Headline, live-reproduced finding, convergent across all 3 lenses**: IAE-030
(`supabase/migrations/20260807400000_create_intelligence_enterprise_monitoring_observability.sql`)
built a real, well-constructed alerting/incident schema — real severity enum,
real owner routing, real concurrency-safe deduplication (`pg_advisory_xact_lock`,
already hardened at its own Tier C) — but it had **zero real production
callers anywhere in this codebase**. Lens 2 live-forced this directly: a job
driven through the real DLQ path (`app.enqueue_job` → `app.claim_next_job` →
`app.record_job_failure` with `max_attempts=1`) reached its own terminal
`dead_letter` status with **zero incident, zero alert, zero owner
notification** — only an `app.audit_logs` row a human would have to go
looking for. This directly contradicts Prompt 382's own Main Flow (§21: "A
job/webhook/API/database failure produces actionable alert") and Business
Rule §24 ("No silent DLQ/backpressure accumulation") for the single most
common real failure path in this repository.

**3 real gaps found and fixed this checkpoint:**

- **`app.record_job_failure`'s own dead-letter transition now raises a real,
  deduplicated observability alert** (`source_type='job'`, `signal_type=
  'error'`, `severity='high'`) instead of landing silently. Live-verified via
  a new regression db-test that drives a job to `dead_letter` and confirms an
  incident is created, that a second distinct job type dead-lettering for the
  same tenant within the dedup window correctly collapses into the same
  incident (`ISS-2026-155`'s own known, unrelated, separately-owned
  granularity behavior — not re-litigated here), and that a merely-retryable
  (non-terminal) failure raises no alert at all.
- **`/api/health` and `/api/ready` did not exist**, despite
  `docs/standards/OBSERVABILITY_STANDARDS.md` §7 already fixing their exact
  contract and `docs/runbooks/observability-exporter-outage.md`'s own
  diagnosis step 1 falsely claiming they were "implemented Phase 1" — a live
  404 for any on-call responder who followed that step during a real
  incident. Built both, matching the standards doc's own contract exactly:
  `/api/health` is an unconditional liveness check (`{status: "ok"}`, 200);
  `/api/ready` additionally calls a new, trivial, side-effect-free
  `app.ping()` RPC to confirm DB connectivity, returning `503`/
  `{status: "degraded", reason: [...]}` on failure — never a false "ok".
  Live-tested against a real running production server: `/api/health` →
  `200 {"status":"ok"}`; `/api/ready` against this sandbox's own placeholder
  Supabase URL (genuinely unreachable) → `503
  {"status":"degraded","reason":["database_unreachable"]}`, proving the
  failure path live, not merely asserted.
- **Two stale/false runbook references corrected**: `observability-exporter-
  outage.md`'s own "implemented Phase 1" claim (now genuinely true, fixed
  above, with a correction note added); `gps-ingestion-database-outage.md`'s
  own "no live Supabase project exists yet" note, stale since `HDN-372`
  (2026-08-23) established a real, hosted, live-verified Supabase project
  (`awdlicmwzdxquopwtcfd`) — corrected to name the real, narrower, still-true
  rehearsal blocker (no live GPS Gateway/Driver PWA/provider deployment
  exists to interrupt).

**4 findings registered, not fixed, each with a named owner:**

- **`ISS-2026-249`** (High) — every other real failure producer (webhook
  signature failures, AI governed-action rejections, security/auth denials)
  remains unwired from `app.raise_observability_alert`, confirmed by direct
  code trace. This checkpoint fixes the single highest-value path (job
  dead-lettering, covering every job type this repository has); the rest
  exceeds this lane's own bounded-repair charter.
- **`ISS-2026-250`** (High) — no monitoring/incident dashboard UI exists
  anywhere; IAE-030's own real backend has zero consumer. Self-disclosed at
  build time (`docs/build-log/phase-09/IAE-358.md`: "UI: none"), but not
  previously stated plainly in the Step 15 record.
- **`ISS-2026-251`** (Medium) — alert routes have real owner/severity/dedup
  but no escalation/dispatch mechanism at all (no email/Slack/PagerDuty, no
  unacknowledged-after-N-minutes reminder) — distinct from and narrower than
  the already-disclosed "no live alerting endpoint" framing.
- **`ISS-2026-252`** (Low) — `OBSERVABILITY_STANDARDS.md` §7's own `NOT_RUN`
  table is unrevised Phase-0 prose, still gated on a "no `app/` exists yet"
  precondition that has been false for 14 phases, and not updated even by
  this checkpoint's own `/api/health`/`/api/ready` build.

**2 pre-existing findings independently re-verified accurate, no correction
needed**: `ISS-2026-155` (dedup collapses two distinct job workload types
sharing the same `(source_type, signal_type)` — live-reproduced exactly as
documented by Lens 2, not merely re-read); `ISS-2026-152` (region/service-
capability matrix has zero run-time consequence — re-confirmed via direct
grep for any caller).

**No RPD-022/RPD-025 contradiction found anywhere** — every "tamper-proof"/
"immutable" mention across runbooks and UI copy is a correct RPD-022
disclosure, not a violation (Lens 3). **No tenant-data-leakage found** on any
health/status/metrics surface, including the two new routes this checkpoint
adds (Lens 3 + this checkpoint's own design: `app.ping()` touches no
application table). **Job/DLQ mechanics, API `/v1` request logging
(`app.api_logs`), AI-governance audit, and security-event audit are all
already solid** — real, durable, queryable, redaction-aware — the gap this
checkpoint closes is specifically that none of these existing, working audit
trails feed the separate alerting/incident system.

## 2. Baseline at entry

- HEAD: `9bd9ea7` (`HDN-381` Tier C close, `VERIFIED`).
- 328 migrations, 229 db-test files (unchanged by this checkpoint — 2
  existing files extended with new blocks, not 2 new files), all `VERIFIED`
  through `HDN-381`.
- `HDN-370..381` all `VERIFIED`; `HDN-382` newly `READY`.

## 3. Entry gate

`typecheck` 0; `lint` 0 errors/337 warnings; `pnpm run test` 5443/5443;
`pnpm run test:e2e` 34/34; db-tests last confirmed green at `HDN-381` Tier C
close (229/229 files, 328 migrations).

## 4. Investigation (3 parallel lenses)

1. **Coverage mapping (source-level static sweep)** — mapped every dimension
   Prompt 382 §20 names. Found: `scripts/observability/logger.ts` is a real,
   tested, safe-degrade structured logger with zero real production callers;
   no error-tracking service (Sentry/equivalent) integrated anywhere; no
   `instrumentation.ts`/`@vercel/otel` wiring despite `app/` now having 243
   routes (Tier C correctness re-derivation lens: independently re-counted
   via a live `next build`, correcting this section's own first-round figure
   of 246); IAE-030's real DB schema is entirely orphaned (zero callers outside
   its own test); the real DLQ mechanics (`app.record_job_failure`,
   `app.requeue_dead_letter_job`) never lose a job (durably recorded either
   way) but never alert either; API `/v1` request logging is real and solid
   (`app.api_logs`); AI governance audit is real and solid; security/auth
   denials are real and solid. Re-verified `ISS-2026-155`/`152` directly
   against the current code (not trusted from the ticket text) — both
   confirmed accurate.
2. **Live/simulated failure testing** — confirmed a disposable Postgres 16.13
   was reachable in this sandbox (matching `ISS-2026-140`'s own precedent:
   only the non-Postgres Supabase containers are blocked by the sandbox's
   `RLIMIT_NOFILE`/`runc` constraint), applied all 328 migrations cleanly,
   then live-forced real RPC calls: reproduced `ISS-2026-155` directly (two
   distinct workload types breaching within the dedup window collapsed into
   one incident, confirmed via `select count(*) from app.incidents`); drove a
   real job through `enqueue_job`→`claim_next_job`→`record_job_failure` to
   `dead_letter` and confirmed, before this checkpoint's own fix, zero
   incidents were created. Ran the existing db-test suites live (not
   assumed): `enterprise-monitoring-observability.sql` and
   `scale-up-architecture.sql` both `ALL ASSERTIONS PASSED`;
   `enterprise-monitoring.test.ts` 11/11; `logger.test.ts` 20/20. Webhook
   HTTP handlers were code-traced, not live-fired (no dev server started for
   that specific check) — disclosed as such, not claimed as live evidence.
3. **Runbook/dashboard review** — read all 11 files under `docs/runbooks/` in
   full; built a runbook-by-runbook fidelity table. 10 of 11 fully accurate
   against real, spot-checked code references. Found the two genuine defects
   this checkpoint fixes (the false "implemented Phase 1" health/ready claim;
   the stale "no live Supabase project" note). Confirmed no monitoring
   dashboard UI exists anywhere (grepped `app/`/`components/` for every
   plausible name). Confirmed real severity/dedup/owner-team routing exists
   at the DB layer but no escalation/dispatch mechanism. Confirmed no
   tenant-data leakage on any health/status route (the only pre-existing one,
   `/api/v1/status`, requires a valid API key and returns no tenant data).
   Confirmed zero RPD-022/RPD-025 contradictions in any runbook or UI copy.

## 5. Carry-forward scope addressed this checkpoint

`HARDENING_MATRIX.md` §13's punch list: enterprise monitoring coverage mapped
explicitly across all 8 named dimensions; `ISS-2026-155`/`152` re-verified
live, not merely re-read (both accurate, unchanged); "no deployed environment,
no live alerting endpoint" re-confirmed and narrowed — the real gap is at the
application-wiring layer, independent of deployment (`ISS-2026-249`);
runbook links/severity workflow/alert ownership verified in full (10 of 11
runbooks accurate, 2 genuine defects fixed, dashboard/escalation gaps
registered); `docs/runbooks/gps-ingestion-database-outage.md` refreshed
against the current baseline as instructed.

## 6. Findings

See §1 for the full narrative.

## 7. Migrations

**One additive migration**, `20260816000000_harden_observability_audit_findings.sql`:

1. `app.record_job_failure` — `CREATE OR REPLACE`, same signature/exception
   prefixes as every prior extension of this function. On the dead-letter
   transition only, calls `app.raise_observability_alert` (already exists,
   defined at an earlier migration) with `source_type='job'`,
   `signal_type='error'`, `severity='high'`.
2. `app.ping()` — new, trivial, side-effect-free, `security definer`
   function returning `true`, touching no application table. Blanket
   `revoke execute on all functions in schema app from public` (this
   repository's own established convention for a brand-new function's
   implicit PUBLIC EXECUTE grant — caught live by this checkpoint's own
   regression db-test before it was committed) followed by an explicit
   `grant execute ... to service_role`.

No RLS/table/contract change. No destructive or applied-migration edit.

## 8. TypeScript / application changes

- `app/api/health/route.ts` (new) — unconditional liveness check.
- `app/api/ready/route.ts` (new) — readiness check, calls `app.ping()` via
  the service-role client.
- `eslint.config.js` — `app/api/ready/route.ts` added to
  `serviceRoleImportGuard`'s `ignores` list (the only new file that actually
  imports the service-role client; `/api/health` needs no DB access at all).
- `docs/runbooks/observability-exporter-outage.md` — diagnosis step 1
  corrected (see §1).
- `docs/runbooks/gps-ingestion-database-outage.md` — severity-class note
  refreshed (see §1).

## 9. db-test changes

- `scripts/db-tests/background-job.sql` — new regression block: drives a job
  to `dead_letter` and asserts an observability incident is created
  (delta-based assertions, not absolute-count — an earlier fixture in the
  same file already dead-letters an unrelated job for the same tenant, and
  with this checkpoint's own fix that legitimately opens/reuses an incident
  before this new block ever runs); asserts a second, distinct job type
  dead-lettering within the dedup window correctly collapses into the same
  incident as a `duplicate_signal` timeline event; asserts a merely-retryable
  (non-terminal) failure raises no alert.
- `scripts/db-tests/enterprise-monitoring-observability.sql` — new block:
  `app.ping()` returns `true`, `anon` holds no EXECUTE grant on it.
- Both live-run via `bash scripts/db-tests/run.sh`: **`ALL PASSED`**
  (229 files, unchanged; 329 migrations). One real regression caught and fixed live
  during this checkpoint's own authoring: the delta-based rewrite above
  (an absolute-count assumption failed against the pre-existing fixture);
  the implicit-PUBLIC-EXECUTE-grant gotcha on `app.ping()` (caught by the
  new anon-EXECUTE assertion, fixed with the blanket revoke, §7).

## 10. Documentation

- `docs/runtime/KNOWN_ISSUES.md` — `ISS-2026-249..252` newly registered
  (first round); `ISS-2026-249` widened with 2 more concrete instances and
  `ISS-2026-253` newly registered (Tier C).
- `docs/runbooks/observability-exporter-outage.md`,
  `docs/runbooks/gps-ingestion-database-outage.md` — corrected (§1/§8).
- `docs/build-log/full-system-hardening/BLOCKER_LEDGER.md` —
  `HDN-BLK-027`/`028` newly registered at Tier C (High, paired with
  `ISS-2026-249`/`250`, closing a real gap where both had no paired entry).
- This build log.

## 11. Taxonomy

No new class needed. The headline finding this checkpoint fixes (a
well-built subsystem with zero real callers) is a specific instance of a
general pattern already covered by `RECURRING_DEFECT_TAXONOMY.md`'s existing
classes for "built but unreachable" primitives (the same shape `HDN-381`
found for 33 of 50 shared UI primitives) — not a new taxonomy class, the same
underlying discipline gap (a capability shipped and unit-tested in isolation,
never wired to its real callers) recurring in a different layer (DB/backend
rather than UI). The implicit-PUBLIC-EXECUTE-grant-on-a-new-function gotcha
this checkpoint's own `app.ping()` addition hit is not new either — it is
this repository's own long-established, already-documented convention
(`20260717095000_revoke_default_public_function_execute.sql`,
`20260730950000`'s own inline comment), correctly applied here, not a defect.

## 12. Tier A gate (first round, before Tier C)

- `npx tsc --noEmit`: clean.
- `npx eslint .`: 0 errors, 337 pre-existing unrelated warnings.
- `pnpm run test` (`server/**`, `lib/**`, `scripts/**`, `tests/**`):
  5443/5443.
- `pnpm exec next build`: clean, all routes compile, including the 2 new
  `/api/health`/`/api/ready` routes.
- Live HTTP verification against a real running production server (not
  merely a successful build): `/api/health` → `200 {"status":"ok"}`;
  `/api/ready` against this sandbox's own genuinely-unreachable placeholder
  Supabase URL → `503 {"status":"degraded","reason":["database_unreachable"]}`
  — the failure path proven live, the success path proven by `app.ping()`'s
  own passing db-test (no live Supabase project exists in this sandbox to
  drive the 200 path end-to-end through the real HTTP route, disclosed
  honestly rather than fabricated).
- `bash scripts/db-tests/run.sh` (full 229-file suite, 329 migrations):
  **`ALL PASSED`**, clean, no errors.

## 13. Tier C review

4 independent adversarial lenses ran against the committed first-round state
(`2251bf2`): correctness re-derivation, schema-wide completeness sweep,
ledger/documentation consistency, attack-surface adversarial testing. **No
Critical or High code-correctness defect found by any lens.**

**13.1 Correctness re-derivation** — independently re-derived every
quantitative/factual claim rather than trusting the build log's narrative,
via direct git archaeology, live disposable-Postgres execution, and a real
`next build`/`next start`. **CONFIRMED**: the pre-fix `record_job_failure`
genuinely had no alerting call (`git show HEAD~1`); the fix itself only
fires on the `dead_letter` branch with correct parameters; the db-test
regression genuinely passes live; `/api/health`/`/api/ready` correctly
handle a synchronous `requireEnv()` throw inside the `try` block (no
unhandled-500 gap); the live-HTTP verification (`200 ok` / `503 degraded`)
was independently reproduced on a fresh build/start, byte-identical;
`app.ping()`'s grants are correct (`anon`/`authenticated` denied,
`service_role` allowed) via live query; `ISS-2026-155`/`152`'s own
mechanism claims re-verified directly against the current SQL. **CORRECTED**
(2 numeric errors, both fixed at this Tier C pass): the "231/231 db-test
files" figure was wrong everywhere it appeared — this checkpoint extended 2
*existing* files with new blocks, it never added a new file; the real count
was, and remains, 229/229 (unchanged from `HDN-381`), corrected across all 7
documents it had propagated into (§2/§9/§12 of this file,
`HARDENING_MATRIX.md`, `00_EXECUTION_INDEX.md`, `TASK_LEDGER.md`,
`HANDOFF.md`, `CARGOGRID_BUILD_STATUS.md`, `CHANGE_MANIFEST.md`); §4's own
"246 routes" figure corrected to the actual, live-recounted 243. **Minor,
disclosed, not code-changed**: the new db-test's own first delta assertion
(`background-job.sql`) is a tautology once the baseline open-incident count
is already ≥1 (true in this file's own execution order, since 2 earlier
fixtures already dead-letter jobs for the same tenant) — the test's real
protection comes from the later timeline-delta assertion on the second job,
which is not a tautology; left as-is since it does not currently cause a
false pass and reworking it further was judged lower-value than the other
Tier C fixes.

**13.2 Schema-wide completeness sweep** — swept for other real failure
producers, `app.ping()`'s own scope-sufficiency, the db-test's own path
coverage, and whether any of `HDN-378..381`'s own registered findings
belonged to this checkpoint's charter. **2 more concrete, live-reachable
instances of `ISS-2026-249`'s own gap class found**, folded into that entry
rather than opened as separate tickets (see §10/`KNOWN_ISSUES.md`):
`app.replay_webhook_delivery`'s own post-replay dead-letter divergence (a
delivery that dead-letters a second time, post-replay, produces zero alert
even after this checkpoint's fix, since the bridging job's own `attempts`
counter restarts at 0 while the delivery's own counter does not); `IAE-008`'s
own integration-connection health-check auto-disable path (live-reachable,
unlike the already-disclosed-dormant automation-rule-engine class).
**Confirmed non-gaps** (checked and ruled out, not manufactured):
`app.ping()`'s DB-only scope is correct against
`OBSERVABILITY_STANDARDS.md` §7's own exact contract — no other critical
runtime dependency exists in this architecture (job queue, auth, and search
are all Postgres-backed per `01_MODULE_DEPENDENCY_MAP.md` R7); every
`app.jobs` job-type CHECK-constraint widening (16 migrations) funnels
through the same, now-fixed `app.record_job_failure` — no bespoke
per-domain bypass exists; none of `ISS-2026-235..248` (`HDN-378..381`'s own
registered findings) belong to this checkpoint's own Observability charter.
**Minor, disclosed test-coverage gap, not a correctness defect**: the new
db-test regression only exercises job creation via `app.enqueue_job`, not
the outbox path (`app.dispatch_event_as_job`) — code-level analysis shows
`app.record_job_failure` itself branches on neither job-type nor creation
path, so no divergent behavior is expected, but the regression proof doesn't
cover that path explicitly.

**13.3 Ledger/documentation consistency** — found and fixed the same
"231/231" numeric error independently (see §13.1) and one real structural
defect: this section's own first-round `Result` blockquote was committed
misplaced under `HARDENING_MATRIX.md` §12 (`HDN-381`'s own Browser/Device
Compatibility section) instead of under this checkpoint's own §13 — a real
authoring mistake (appended after §12's "Upstream:" line by accident, not a
stale-status error), fixed by moving it to its correct home. Also flagged
(and now closed, see §10/`BLOCKER_LEDGER.md`) that `ISS-2026-249`/`250`
(both `OPEN`, High) had no paired `HDN-BLK-` entry, breaking an otherwise
universal pattern this session's every other `OPEN` High finding follows —
`HDN-BLK-027`/`028` registered accordingly. Everything else checked (cross-
document numeric agreement on migrations/unit tests/WBS tally, "next
eligible prompt" agreement across all 3 documents that state it, KNOWN_
ISSUES numbering/severity consistency, narrative-vs-real-diff fidelity) was
found consistent and accurate — not manufactured to seem thorough.

**13.4 Attack-surface adversarial testing** — live-forced a real two-session
concurrency race on the dead-letter alert wiring (held up: no deadlock,
cross-tenant calls never serialize against each other, a job's own row
update is never held hostage by the alert-dedup advisory lock); live-forced
a synchronous `requireEnv()` throw inside `/api/ready` (held up: the bare
`catch` cannot leak any error text, confirmed byte-identical to the genuine
DB-unreachable response); live-tested `app.ping()`'s own privilege surface
under `SET ROLE` (held up: `anon`/`authenticated` both denied,
`service_role` allowed, 5000-call loop confirmed genuinely side-effect-
free); live re-ran the new db-test block a second time without re-running
setup (held up: fails loudly with a named exception, `set -euo pipefail`
would abort the suite — no silent false-positive path exists); live-curled
both new routes against a real production server and confirmed the 2
corrected runbook sections match word for word. **1 new, code-confirmed Low
finding registered**: `/api/ready`'s own failure path is fully unlogged
server-side (no `console.error`/`logger.ts` call anywhere on the catch
path), so an on-call responder cannot distinguish a genuine DB outage from
a missing env var or any other exception from the HTTP response alone —
registered `ISS-2026-253`, not fixed (found only during this same
checkpoint's own Tier C review, after the first-round fix pass had already
closed). **1 plausible-but-unverified risk disclosed, not registered as a
finding**: no explicit timeout/`AbortSignal` is passed to the `app.ping()`
RPC call — against a silently-black-holed network path (not the
instantly-refused loopback this sandbox produces), the fetch could
theoretically hang rather than fail fast to `503`; could not be reproduced
live in this sandbox, so disclosed here rather than asserted as a confirmed
defect. No RPD-022/RPD-025 overclaim found in any of this checkpoint's own
new comment text (`app.ping()`'s own doc comment, both route files) — all
three are explicitly self-limiting, `/api/ready` explicitly documents it
can return `503`, the opposite of an "always up" claim.

**13.5 Full Tier A gate re-run after the fix pass**: `tsc --noEmit` clean;
`eslint .` 0 errors/337 warnings (unchanged); `pnpm run test` 5443/5443;
`pnpm exec next build` clean; `bash scripts/db-tests/run.sh` **`ALL
PASSED`** (229/229 files, correcting the first round's own miscounted
231/231; 329 migrations, unchanged — no schema change at this Tier C pass).

## 14. Task status

**`VERIFIED`** — first round and Tier C review both complete; fix pass
applied and re-verified green end to end. No Critical or High finding at
either round. `docs/runtime/TASK_LEDGER.md` row `CG-S15-HDN-014` and
`docs/build-log/full-system-hardening/00_EXECUTION_INDEX.md`'s own WBS
row/tally/§16 updated accordingly. Next eligible prompt: `HDN-383`
(Backup and Restore, `CG-S15-HDN-015`).
